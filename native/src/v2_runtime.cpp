#if defined(_WIN32)
#define WIN32_LEAN_AND_MEAN
#define NOMINMAX
#endif

#include "flyweight_v2.h"
#include "flyweight_gpu_driver.h"
#include <flyweight_backend.hpp>

#include <atomic>
#include "flyweight_v2_provider.hpp"
#include "flyweight_v2_config.hpp"
#include "flyweight_v2_attention_policy.hpp"
#include "flyweight_v2_expert_policy.hpp"
#include "flyweight_v2_moe_align.hpp"
#include "flyweight_v2_format_dispatch.hpp"
#include "flyweight_v2_expert_seed.hpp"
#include "flyweight_v2_qwen_kernels.hpp"
#include "flyweight_v2_native_kernels.hpp"
#include "flyweight_v2_deepseek4_kernels.hpp"
#include "flyweight_v2_bailing.hpp"
#include "flyweight_v2_hf_quantize.hpp"
#include "flyweight_v2_hf_cache.hpp"
#include "flyweight_v2_workspace.hpp"
#include "flyweight_v2_tool_grammar.hpp"
#include "flyweight_v2_byte_alphabet.hpp"
#include "qwen_cpu_kernel.h"
#include "qwen_kquant.h"
#include "qwen_kquant_pack_api.hpp"
#include "turboquant.h"
#include "unicode_categories.h"
#include "flyweight_v2_deepseek4.hpp"

#include <algorithm>
#include <array>
#include <cerrno>
#include <chrono>
#include <cmath>
#include <cstdio>
#include <cstdlib>
#include <cstring>
#include <deque>
#include <fstream>
#include <sstream>
#if defined(_WIN32)
#include <windows.h>
#include <sys/stat.h>
#ifndef S_ISDIR
#define S_ISDIR(m) (((m) & _S_IFMT) == _S_IFDIR)
#endif
#else
#include <dirent.h>
#include <sys/stat.h>
#endif
#include <initializer_list>
#include <limits>
#include <thread>
#include <set>
#include <mutex>
#include <memory>
#include <queue>
#include <stdexcept>
#include <string>
#include <unordered_map>
#include <unordered_set>
#include <utility>
#include <map>
#include <vector>

#if defined(_OPENMP)
#  include <omp.h>
#endif

#if defined(_WIN32)
#  ifndef NOMINMAX
#    define NOMINMAX
#  endif
#  include <windows.h>
#  include <psapi.h>
#else
#  include <fcntl.h>
#  include <sys/mman.h>
#  include <sys/stat.h>
#  include <sched.h>
#  include <unistd.h>
#  include <dlfcn.h>
#endif

thread_local std::string error;
void fail(const char* message) { error = message; }
template <class F> int guarded(F&& f) { try { return f(); } catch (const std::exception& e) { error = e.what(); return -1; } catch (...) { error = "unknown native exception"; return -1; } }

// Guard for the per-layer timing accumulators (route_wait, expert_compute, etc.).
// On Linux, clock_gettime via vDSO is ~20 ns; on Windows QueryPerformanceCounter
// is ~150 ns.  Unconditionally calling steady_clock::now() 2-3 times per layer
// per token added ~50 us/token of pure overhead.  Off unless FLYWEIGHT_TIMING=1.
bool timing_enabled() {
    static const bool on = [] {
        const char* s = std::getenv("FLYWEIGHT_TIMING");
        return s && s[0] == '1';
    }();
    return on;
}

// Promote any layer whose expert tensors finished registering in the background
// to the direct-DMA path. Called at token boundaries only: `expert_dma` is then
// written and read by the same thread, so the per-layer flags need no atomics and
// a layer can never change execution path mid-token.
struct FlyweightV2QwenRuntime;
void qwen_absorb_registration(FlyweightV2QwenRuntime& runtime);

// Host RAM left unpinned when expert tensors are registered for direct paging.
// Registration locks pages, so taking everything would trade the staging memcpy
// for major faults on whatever else needs page cache -- on qwen4exp that is the
// 25.7 GiB n-gram table, which is read 16 rows per token and must stay cheap.
constexpr std::uint64_t kRegisterReserveBytes = 8ull * 1024 * 1024 * 1024;
// Below this there is no point paying registration time for the few layers that
// would fit.
constexpr std::uint64_t kRegisterMinimumBytes = 2ull * 1024 * 1024 * 1024;

// FLYWEIGHT_MOE_PROFILE=1 attributes the decode MoE phase's HOST-SERIAL time to its
// causes. Phase 0b of plans/decode-device-dispatch.md: `expert_page` measures the
// whole phase (~32 ms/token on the 125B UD-IQ1_S) and `expert_compute` only the
// CPU expert dots inside it (~7 ms), leaving ~25 ms/token attributed to nothing.
// The dispatch rewrite that gap motivates is far too large to build on a guess,
// so this splits the gap by cause. Separate from FLYWEIGHT_TIMING because these are
// finer-grained clocks inside the per-expert loop, which is exactly where clock
// overhead would distort what it measures -- read the totals, not the absolute
// token rate, when this is on.
struct QwenMoeProfile {
    double lookup = 0;      // expert_residency hash find, per selected expert
    double access = 0;      // record_expert_access + cache-hit bookkeeping
    double scales = 0;      // the three qwen_expert_role_scale mmap touches
    double admission = 0;   // select_expert_cache_slot's victim scan
    double staging = 0;     // memcpy of a missed expert bundle into pinned staging
    double table = 0;       // the seven pointer/scale memcpys + their upload
    double queue = 0;       // grouped kernel launches + deferred cache uploads
    // Prefill streaming path: the per-expert-group gather/quantize/GEMM/scatter
    // launches, separated from the staging uploads that precede them. ~200k
    // groups of ~20 tokens each per prompt is the shape under suspicion.
    double stream_upload = 0, stream_compute = 0;
    std::uint64_t stream_groups = 0, stream_group_tokens = 0;
    std::uint64_t tokens = 0, lookups = 0, admissions = 0, staged_bytes = 0;
};
inline QwenMoeProfile& qwen_moe_profile() { static QwenMoeProfile p; return p; }
inline bool moe_profile_enabled() {
    static const bool on = [] {
        const char* s = std::getenv("FLYWEIGHT_MOE_PROFILE");
        return s && s[0] == '1';
    }();
    return on;
}
inline double moe_now() {
    return moe_profile_enabled()
        ? std::chrono::duration<double, std::milli>(
              std::chrono::steady_clock::now().time_since_epoch()).count()
        : 0.0;
}
void qwen_moe_profile_report();
// Prefill can finish without ever reaching the decode-side report (a probe that
// generates a handful of tokens never hits the every-50 tick), so guarantee the
// numbers reach stderr.
struct QwenMoeProfileAtExit { ~QwenMoeProfileAtExit(){ qwen_moe_profile_report(); } };
static QwenMoeProfileAtExit qwen_moe_profile_at_exit;

void qwen_moe_profile_report() {
    if (!moe_profile_enabled()) return;
    auto& p = qwen_moe_profile();
    if (!p.tokens) return;
    const double t = static_cast<double>(p.tokens);
    const double sum = p.lookup + p.access + p.scales + p.admission +
                       p.staging + p.table + p.queue;
    std::fprintf(stderr,
        "\n[moe-profile] %llu decode tokens, host-serial MoE work per token:\n",
        static_cast<unsigned long long>(p.tokens));
    const std::pair<const char*, double> rows[] = {
        {"residency lookup", p.lookup}, {"access bookkeeping", p.access},
        {"role scales (mmap)", p.scales}, {"admission victim scan", p.admission},
        {"bundle staging memcpy", p.staging}, {"pointer table build", p.table},
        {"kernel launch + uploads", p.queue},
    };
    for (const auto& [name, ms] : rows)
        std::fprintf(stderr, "  %-26s %7.3f ms/token  %5.1f%%\n",
                     name, ms / t, sum > 0 ? 100.0 * ms / sum : 0.0);
    std::fprintf(stderr, "  %-26s %7.3f ms/token\n", "TOTAL attributed", sum / t);
    if (p.stream_groups)
        std::fprintf(stderr,
            "  [prefill stream] uploads %.2f s, group compute %.2f s over "
            "%llu groups (%.1f tokens each)\n",
            p.stream_upload / 1000.0, p.stream_compute / 1000.0,
            (unsigned long long)p.stream_groups,
            (double)p.stream_group_tokens / (double)p.stream_groups);
    std::fprintf(stderr,
        "  (%.1f lookups, %.1f admissions, %.1f MiB staged per token)\n\n",
        p.lookups / t, p.admissions / t,
        p.staged_bytes / t / (1024.0 * 1024.0));
}

// FLYWEIGHT_EXPERT_TRACE=<path> writes the decode route stream as packed
// (uint32 layer, uint32 expert) pairs so expert-cache policies can be replayed
// offline against a real trace. Diagnostic only; null unless the var is set.
std::FILE* expert_trace_file() {
    static std::FILE* const handle = [] () -> std::FILE* {
        const char* path = std::getenv("FLYWEIGHT_EXPERT_TRACE");
        return (path && path[0]) ? std::fopen(path, "wb") : nullptr;
    }();
    return handle;
}

struct Tensor : flyweight::v2::TensorDescriptor {
    // Null means the primary model mapping. MTP overlays point into the
    // sidecar mapping retained by FlyweightV2Model::mtp_sidecar.
    const uint8_t* source = nullptr;
};

struct Reader {
    const uint8_t* p; const uint8_t* end;
    template <class T> T get() { if (end-p < static_cast<ptrdiff_t>(sizeof(T))) throw std::runtime_error("truncated GGUF"); T v; std::memcpy(&v,p,sizeof(v)); p += sizeof(v); return v; }
    std::string str() { auto n=get<uint64_t>(); if (n > static_cast<uint64_t>(end-p)) throw std::runtime_error("invalid GGUF string"); std::string s(reinterpret_cast<const char*>(p), static_cast<size_t>(n)); p+=n; return s; }
    void skip(size_t n) { if (n > static_cast<size_t>(end-p)) throw std::runtime_error("truncated GGUF value"); p+=n; }
    void value(uint32_t type) {
        switch(type) {
        case 0: case 1: case 7: skip(1); break;
        case 2: case 3: skip(2); break;
        case 4: case 5: case 6: skip(4); break;
        case 8: str(); break;
        case 9: { auto element_type=get<uint32_t>(); auto n=get<uint64_t>(); for(uint64_t i=0;i<n;i++) value(element_type); break; }
        case 10: case 11: case 12: skip(8); break;
        default: throw std::runtime_error("unsupported GGUF metadata type");
        }
    }
};

// Execution consumes this descriptor rather than GGUF internals. GGUF is the
// first WeightProvider; future providers can populate the same
// tensor contract without changing CUDA/runtime code.
struct FlyweightV2Model : flyweight::v2::WeightProvider { const uint8_t* data=nullptr; size_t size=0; uint32_t version=0, alignment=32; uint64_t metadata=0; std::string path, architecture, name, format_name="gguf", chat_template; flyweight::v2::ModelConfig config; uint32_t mtp_layer=std::numeric_limits<uint32_t>::max(); std::vector<std::string> vocabulary, merges; std::unordered_map<std::string,int> merge_ranks; std::unordered_map<std::string,uint32_t> vocabulary_ids; std::vector<Tensor> tensors; std::unique_ptr<FlyweightV2Model> mtp_sidecar;
    // GGUF tokenizer.ggml.pre selects the pre-tokenizer. Control tokens (GGUF
    // token type 3) never come out of BPE and have to be split off ahead of it.
    std::string tokenizer_pre; std::vector<std::uint32_t> token_types;
    std::vector<std::pair<std::string,std::uint32_t>> control_tokens;
    // Split GGUF: `split.no` is this file's 0-based index of `split.count`, and
    // `split.tensors.count` is the descriptor total across every shard. A zero
    // count means an ordinary single-file checkpoint.
    std::uint32_t split_no=0, split_count=0; std::uint64_t split_tensors=0;
    // Shards other than the one that was opened. Their descriptors are merged
    // into `tensors` with Tensor::source pointing at the shard mapping, so a
    // split checkpoint is indistinguishable downstream from a single file.
    std::vector<std::unique_ptr<FlyweightV2Model>> shards;
    // HF checkpoints arrive unquantized and are quantized during open, so their
    // tensor bytes are owned here rather than mapped from a file. Every
    // descriptor then points into this buffer through Tensor::source, which is
    // the same indirection split GGUF shards already use -- so nothing
    // downstream can tell the difference.
    std::vector<std::uint8_t> owned_arena;
#if !defined(_WIN32)
    int fd=-1;
#else
    HANDLE file=nullptr, mapping=nullptr;
#endif
    ~FlyweightV2Model() {
#if !defined(_WIN32)
        if(data&&data!=MAP_FAILED)munmap(const_cast<uint8_t*>(data),size);
        if(fd>=0)::close(fd);
#else
        if(data)UnmapViewOfFile(reinterpret_cast<LPCVOID>(data));
        if(mapping)CloseHandle(mapping);
        if(file&&file!=INVALID_HANDLE_VALUE)CloseHandle(file);
#endif
    }
    const char* format() const override { return format_name.c_str(); }
    uint64_t tensor_count() const override { return tensors.size(); }
    const flyweight::v2::TensorDescriptor* tensor(uint64_t index) const override { return index < tensors.size() ? &tensors[index] : nullptr; }
    int read_tensor(uint64_t index, void* destination, uint64_t bytes) const override { if(index >= tensors.size() || !destination || bytes < tensors[index].size) return -1; const auto& tensor=tensors[index];const auto*base=tensor.source?tensor.source:data;std::memcpy(destination, base + tensor.offset, tensor.size); return 0; }
    // Every mapping that can hold tensor bytes, primary file first. Nothing may
    // assume `data`/`size` spans the model: a split checkpoint keeps all of its
    // metadata in one shard and all of its tensors in the others.
    template <class F> void for_each_mapping(F&& visit) const {
        if(data) visit(data,static_cast<std::uint64_t>(size));
        for(const auto& shard:shards) if(shard&&shard->data) visit(shard->data,static_cast<std::uint64_t>(shard->size));
    }
    std::uint64_t mapped_bytes() const { std::uint64_t total=size; for(const auto& shard:shards) if(shard) total+=shard->size; return total; }
};

const uint8_t* tensor_data(const FlyweightV2Model& model, const Tensor& tensor) {
    return (tensor.source ? tensor.source : model.data) + tensor.offset;
}
struct FlyweightV2KvCache { std::uint64_t keys, values; std::int32_t capacity, kv_heads, head_dim, position=0; };

struct QwenLayerPlan {
    bool attention = false;
    // Dense checkpoints (Qwen3.6-27B and friends) carry one ffn_gate/ffn_up/
    // ffn_down triple per block instead of a router, a shared expert and the
    // stacked routed experts, so there is nothing to route or page.
    bool dense_ffn = false;
    // Set when this block's SwiGLU runs on the CPU straight from the mapping
    // because its weights did not fit in the GPU budget. Only the feed-forward
    // moves: attention and the DeltaNet recurrence stay device-resident.
    bool ffn_on_host = false;
    std::uint32_t attention_window = 0; // 0 = global attention
    std::uint64_t cache_capacity = 0;
    std::uint32_t attention_heads = 0, kv_heads = 0, head_dim = 0;
    std::uint32_t rotary_dim = 0, expert_tensor_count = 3;
    float rope_theta = 0.0f;
    // YaRN extension for this layer. A zero ext_factor means plain RoPE, which
    // is what every architecture except Laguna's full-attention layers uses.
    float rope_freq_scale = 1.0f, rope_ext_factor = 0.0f, rope_attn_factor = 1.0f;
    float rope_beta_fast = 32.0f, rope_beta_slow = 1.0f;
    std::uint32_t rope_orig_context = 0;
    std::vector<std::uint64_t> static_tensors;
    std::array<std::uint64_t, 3> expert_tensors{};
    std::uint64_t shared_graph = 0;
    bool shared_graph_attempted = false;
    // The linear-attention block, captured once and replayed every token.
    // Per sequence slot: the capture bakes in the conv/recurrent state
    // addresses of the arena active at capture time, so a graph recorded on
    // one slot replayed on another reads and writes the wrong sequence's
    // DeltaNet state (deterministically wrong output for the second slot,
    // silent corruption of the first). The shared-expert and dense-FFN graphs
    // below touch only the shared workspace and stay slot-independent.
    std::vector<std::uint64_t> delta_graphs;
    std::vector<std::uint8_t> delta_graph_attempted;
    // The dense SwiGLU block, same treatment. Nothing in it depends on the
    // position or the token -- only fixed weights, fixed workspace slots and
    // fixed widths -- so one capture replays for the whole sequence.
    std::uint64_t dense_ffn_graph = 0;
    bool dense_ffn_graph_attempted = false;
    // Gemma 4's dense GEGLU + router + whole-layer-pinned MoE chain. Only
    // capturable when every expert of the layer is device-resident, because
    // the hybrid path's route download and host sync cannot live in a graph.
    // Slot-independent for the same reason the dense-FFN graph is.
    std::uint64_t gemma_ffn_graph = 0;
    bool gemma_ffn_graph_attempted = false;
    // Laguna's router score-correction bias. Kept out of static_tensors so the
    // feed-forward slots line up with the Qwen layout the FFN code addresses.
    std::uint64_t router_bias = std::numeric_limits<std::uint64_t>::max();
    // qwen4exp gated residual. Named fields rather than static_tensors slots
    // for the same reason as router_bias: the positional layout must keep
    // matching what qwen_ffn_base addresses. Slot 0 holds hc_attn_norm and the
    // ffn-base slot holds hc_ffn_norm (they stand where attn_norm and
    // post_attention_norm stand on qwen), so only the projections live here.
    std::uint64_t hc_attn_down = std::numeric_limits<std::uint64_t>::max();
    std::uint64_t hc_attn_up = std::numeric_limits<std::uint64_t>::max();
    std::uint64_t hc_attn_inject = std::numeric_limits<std::uint64_t>::max();
    std::uint64_t hc_ffn_down = std::numeric_limits<std::uint64_t>::max();
    std::uint64_t hc_ffn_up = std::numeric_limits<std::uint64_t>::max();
    std::uint64_t hc_ffn_inject = std::numeric_limits<std::uint64_t>::max();
    // QSA indexer projections (full-attention layers). Loaded and resident from
    // phase 1 so residency accounting is stable, exercised only once the
    // sparse selection lands (plans/floating-dreaming-moore.md phase 3).
    std::uint64_t indexer_q = std::numeric_limits<std::uint64_t>::max();
    std::uint64_t indexer_k = std::numeric_limits<std::uint64_t>::max();
    std::uint64_t indexer_q_norm = std::numeric_limits<std::uint64_t>::max();
    std::uint64_t indexer_k_norm = std::numeric_limits<std::uint64_t>::max();
    // qwen4exp PLE block (only on the layers ple.layers names): conv, key,
    // value, and the three grouped norms.
    std::uint64_t ple_conv = std::numeric_limits<std::uint64_t>::max();
    std::uint64_t ple_key = std::numeric_limits<std::uint64_t>::max();
    std::uint64_t ple_value = std::numeric_limits<std::uint64_t>::max();
    std::uint64_t ple_norm_conv = std::numeric_limits<std::uint64_t>::max();
    std::uint64_t ple_norm_key = std::numeric_limits<std::uint64_t>::max();
    std::uint64_t ple_norm_query = std::numeric_limits<std::uint64_t>::max();
    std::uint64_t expert_gate_scale = std::numeric_limits<std::uint64_t>::max();
    std::uint64_t expert_up_scale = std::numeric_limits<std::uint64_t>::max();
    std::uint64_t expert_down_scale = std::numeric_limits<std::uint64_t>::max();
    // weight_scale_2 for the shared expert (NVFP4 checkpoints ship one f32 per
    // tensor). Kept separate from the routed-expert scales, which are per expert.
    float shared_gate_scale = 1.0f;
    float shared_up_scale = 1.0f;
    float shared_down_scale = 1.0f;
    std::uint64_t state_first = 0;
    std::uint64_t state_second = 0;
    // qwen4exp PLE conv ring: (kernel-1)*dilation trailing columns of the
    // hc-wide gated-value stream. Only meaningful when ple_conv is set; the
    // block that owns it is a DeltaNet block, so first/second are taken.
    std::uint64_t state_third = 0;
    // Whether this layer's three expert tensors are inside a CUDA-registered host
    // range, i.e. whether a cache miss can DMA straight from the mapping instead
    // of staging through a host memcpy. Per layer, not global: registration is
    // budgeted and a checkpoint larger than the budget gets partial coverage.
    bool expert_dma = false;
    // qwen4exp QSA indexer state (full-attention layers with indexer tensors):
    // qsa_blocks is the pooled block-key store [capacity/ratio + 1][key_len]
    // f32, append-only and bounded by position exactly like the KV slabs (so
    // it is never snapshot-copied); qsa_ring holds the raw per-token index
    // keys of the last `ratio` positions [ratio][key_len] f32 -- the partial
    // block a snapshot must restore, copied like the PLE conv ring.
    std::uint64_t qsa_blocks = 0;
    std::uint64_t qsa_ring = 0;
    // Per-layer attention kind from .attention.compress_ratios: 4 = QSA over
    // 4-token blocks, 0 = no selection (delta layers, or archs without it).
    std::uint32_t qsa_ratio = 0;
    std::uint64_t snapshot_first = 0;
    std::uint64_t snapshot_second = 0;
    // qwen4exp: the PLE dilated-conv ring (state_third) is recurrent state just
    // like the DeltaNet conv, so an MTP rejection has to rewind it too. Without
    // this the verify pass leaves it `wanted` tokens ahead and the target drifts
    // -- fluent output, wrong tokens, and only on the PLE layer.
    std::uint64_t snapshot_third = 0;
    // MTP fold retention (ReplaySSM): during verification the qkv projection
    // and the decay/beta logits -- the only sequence-dependent inputs of the
    // DeltaNet transition -- are written here instead of the shared scratch,
    // so a rejected round can rebuild the recurrent state by replaying just
    // the conv and recurrence kernels over the accepted prefix instead of
    // re-running the whole forward. Sized for the 8-row verifier maximum.
    std::uint64_t mtp_retain_qkv = 0;
    std::uint64_t mtp_retain_logits = 0;
};

struct QwenExpertSlot {
    std::uint64_t key = 0;
    std::uint64_t last_used = 0;
    bool valid = false;
    bool native_valid = false;
    bool pinned = false;
    // Cleared when the slot is handed out, set by the first route that reads
    // the occupant. An eviction with this still clear is an upload that bought
    // nothing -- the yield of the admission policy, which hit rate hides.
    bool used_since_admission = false;
    // One reservation per sequence that predicted this expert for its next
    // layer. A count (rather than a bool) matters under --parallel: two
    // sequences may predict the same resident expert, and the first one to
    // consume its route must not release the second one's protection.
    std::uint32_t prefetch_pins = 0;
};

struct QwenExpertHistory {
    std::uint32_t frequency = 0;
    std::uint64_t last_used = 0;
};

struct QwenCudaLayerProfile {
    std::uint64_t pre_start=0,pre_end=0;
    // Carved out of the pre phase so the gated-delta recurrence can be told
    // apart from the bf16 projections it sits between; the two have completely
    // different cures and the phase total does not distinguish them.
    std::uint64_t recurrent_start=0,recurrent_end=0;
    std::uint64_t shared_start=0,shared_end=0;
    std::uint64_t expert_start=0,expert_end=0;
};

// DeltaNet conv+recurrent states captured at the end of a prompt prefill,
// keyed by the exact prompt token sequence. Lets a follow-up request reuse
// the whole conversation prefix even though the recurrent state cannot be
// rewound: agentic clients re-encode the assistant reply differently than
// it was generated, so the plain processed_tokens extension check misses
// every multi-turn request. Attention KV needs no copy (position bounds it).
struct QwenPrefillSnapshot {
    std::uint64_t device = 0;
    std::vector<std::uint32_t> tokens;
    std::uint32_t last_output = 0;
    // Whether last_output is the unadjusted argmax. A sampled or
    // grammar-overridden token must not seed a later greedy request's first
    // token on a full-prompt hit; only the logits replay can answer that.
    bool last_output_greedy = false;
    std::uint64_t clock = 0;
    bool valid = false;
};

// How many previously decoded tokens the route-recurrence probe keeps per
// layer. Depth 1 answers "does token t reuse token t-1's experts"; the deeper
// slots answer "how much does a small union buy", which is the difference
// between prefetching a point prediction and prefetching a working set.
constexpr std::size_t kRouteRecurrenceDepth = 16;

struct QwenExpertPrefetchState {
    std::vector<std::int32_t> previous_layer_route;
    std::vector<std::int32_t> pending_predictions;
    std::uint32_t previous_route_layer =
        std::numeric_limits<std::uint32_t>::max();
    // Route-recurrence probe (measurement only; nothing reads these to make a
    // decision). Per layer, a ring of the last kRouteRecurrenceDepth decoded
    // tokens' route sets: history is layers*depth*stride entries, counts and
    // the ring cursor/fill are layers*depth and layers respectively.
    std::vector<std::int32_t> recurrence_history;
    std::vector<std::uint8_t> recurrence_counts;
    std::vector<std::uint8_t> recurrence_cursor;
    std::vector<std::uint8_t> recurrence_filled;
    std::size_t recurrence_stride = 0;
};

// Where every per-layer region sits inside a slot arena, for one context
// limit. Slots need not be the same size: agentic side traffic (title/topic
// detection, quota summaries, short subagent calls) never fills a full-context
// slot, and a slot reserves its whole context at prepare whether or not a
// conversation uses it -- on a small card that reservation is expert cache the
// decode would rather have (plans/paged-kv-cache.md).
//
// Everything downstream of the KV slabs shifts with them, so a geometry is the
// whole offset set, not just the capacities. The active slot's geometry is
// mirrored onto runtime.layers exactly as its arena is mirrored onto
// runtime.state, which is what keeps the ~29 kernel dispatch sites (and every
// `capacity` argument they pass) unaware that slots differ at all.
struct QwenLayerOffsets {
    std::uint64_t cache_capacity = 0;
    std::uint64_t state_first = 0, state_second = 0, state_third = 0;
    std::uint64_t snapshot_first = 0, snapshot_second = 0, snapshot_third = 0;
    std::uint64_t qsa_blocks = 0, qsa_ring = 0;
    std::uint64_t mtp_retain_qkv = 0, mtp_retain_logits = 0;
};

struct QwenSlotGeometry {
    std::uint64_t context_limit = 0;
    std::uint64_t state_bytes = 0;
    std::vector<QwenLayerOffsets> layers;
    QwenLayerOffsets mtp_layer;
    std::uint64_t mtp_target_hidden_offset = 0;
    std::uint64_t mtp_draft_hidden_offset = 0;
    std::uint64_t mtp_verified_hidden_offset = 0;
    std::uint64_t mtp_snapshot_offset = 0;
    std::uint64_t mtp_snapshot_bytes = 0;
};

// One decode sequence: its own attention-KV + DeltaNet state arena plus the
// bookkeeping that pins prefix reuse to it. Agentic clients multiplex several
// logical conversations (main agent, subagents, title/quota side-calls) onto
// the runtime; giving each its own arena means a short side-request routed to
// another slot no longer overwrites the main conversation's KV and forces a
// full reprefill next turn. The active slot's fields are mirrored onto the
// runtime (so the compute kernels, which read runtime.state at fixed offsets,
// are untouched); qwen_switch_sequence saves/loads them on a slot change.
struct QwenSequence {
    std::uint64_t state = 0;            // device KV+DeltaNet arena for this slot
    std::size_t geometry = 0;           // index into runtime.geometries
    std::uint64_t position = 0;
    std::uint32_t last_output_token = 0;
    bool last_output_greedy = true;     // see QwenPrefillSnapshot::last_output_greedy
    std::vector<std::uint32_t> processed_tokens;
    std::vector<QwenPrefillSnapshot> prefill_snapshots; // per-slot reuse checkpoints
    std::uint64_t prefill_snapshot_clock = 0;
    std::uint64_t clock = 0;            // LRU stamp across slots
    QwenExpertPrefetchState expert_prefetch;
    // Routes observed while this sequence prefills its current request.
    // Placement is shared, but evidence is not: cooperative requests must not
    // overwrite one another's prompt-local ranking signal.
    std::vector<std::uint32_t> prompt_expert_frequency;
    std::vector<std::uint32_t> prompt_expert_history;
    std::uint64_t prompt_expert_observations = 0;
};

// A reuse checkpoint spilled to host RAM alongside a QwenHostPrompt.
struct QwenHostSnapshot {
    std::vector<std::uint32_t> tokens;
    void* state = nullptr;             // host copy of the DeltaNet checkpoint buffer
    std::uint32_t last_output = 0;
    bool last_output_greedy = false;   // see QwenPrefillSnapshot::last_output_greedy
};

// A conversation's full slot state spilled to host RAM (llama.cpp's prompt
// cache). When an LRU slot is recycled for a new conversation, its arena and
// reuse checkpoints are DtoH-copied here instead of discarded, so a later
// request that continues it restores from RAM rather than reprefilling ~30k
// tokens cold. The checkpoints matter: the end-of-prompt one (tokens = prompt)
// is what lets the next turn reuse past the prompt boundary regardless of how
// the client re-renders the prior assistant reply.
struct QwenHostPrompt {
    std::vector<std::uint32_t> tokens;
    void* state = nullptr;             // host copy of the slot's KV+DeltaNet arena
    std::vector<QwenHostSnapshot> snapshots;
    std::uint64_t position = 0;
    std::uint32_t last_output_token = 0;
    bool last_output_greedy = true;    // see QwenPrefillSnapshot::last_output_greedy
    std::uint64_t bytes = 0;           // total host bytes held (arena + snapshots)
    std::uint64_t clock = 0;           // LRU stamp within the host cache
    // The geometry the packed buffer was written under. Restoring into a slot
    // laid out differently has to re-derive both range lists and check they
    // still describe the same regions.
    std::size_t geometry = 0;
};

// Resumable prompt-processing state shared by the blocking generate path and
// the cooperative engine: where reuse let the prefill start, the token that is
// already known (on a full-reuse hit), and the mid-prefill checkpoint targets.
struct QwenPromptPlan {
    std::uint64_t prompt_start = 0;
    std::uint32_t next_token = 0;
    std::vector<std::uint64_t> targets;
    std::size_t next_target = 0;
};

struct QwenSamplingState {
    float temperature = 0.0f;
    std::uint32_t top_k = 20;
    float top_p = 0.95f;
    // Nothing here used to discourage a token the model had just produced, and
    // "no penalty" is not the neutral setting it looks like -- it is the
    // setting that lets a low-bit checkpoint fall into a loop and stay there.
    // Once the distribution collapses onto `world.add(moonLight);` every
    // sample redraws the same line, because the only evidence against it is
    // the very history the sampler was ignoring.
    //
    // `repetition_penalty` scales a recent token's logit toward zero (the CTRL
    // rule: divide when positive, multiply when negative, so it always
    // discourages). The two OpenAI penalties subtract instead -- `presence`
    // once for any token seen, `frequency` once per occurrence.
    float repetition_penalty = 1.0f;
    float presence_penalty = 0.0f;
    float frequency_penalty = 0.0f;
    // How far back the penalties look. Only *generated* tokens are recorded:
    // penalizing the prompt would push the model away from the user's own
    // wording, which is a different and much worse failure than a loop.
    std::uint32_t penalty_window = 64;
    std::uint64_t rng = 0;
    std::vector<std::uint32_t> recent;

    bool enabled() const { return temperature > 0.0f; }

    bool penalizes() const {
        return penalty_window > 0 &&
            (repetition_penalty != 1.0f || presence_penalty != 0.0f ||
             frequency_penalty != 0.0f);
    }

    void remember(std::uint32_t token) {
        if (!penalizes()) return;
        recent.push_back(token);
        if (recent.size() > penalty_window)
            recent.erase(recent.begin(),
                         recent.begin() + (recent.size() - penalty_window));
    }

    // Constrained decoding for tool calls. Empty unless the caller passed a
    // tool specification, and inert until the model opens a call -- see
    // flyweight_v2_tool_grammar.hpp. It lives here rather than on the task
    // because both decode paths, single-sequence and engine, reach the sampler
    // through this state and both have to be constrained the same way.
    flyweight::v2::tools::Grammar grammar;

    // Constrained decoding for response_format: the visible answer must be one
    // JSON value. Same lifecycle as the tool grammar -- configured per task,
    // disarms once its job is done or the text leaves its language.
    flyweight::v2::tools::ResponseGrammar response_grammar;

    // The inverse constraint, for requests that declared no tools: markup the
    // request cannot accept must not be written. Armed for the whole task,
    // but it costs the fast path one byte comparison per token -- see the
    // ban check beside the fused-argmax early-out in qwen_sample_last_logits.
    flyweight::v2::tools::MarkupBan markup_ban;

    // The grammars constrain the candidate set, so a request that has one
    // needs candidates even when it would otherwise take the fused greedy
    // argmax.
    bool constrains() const {
        return grammar.armed() || response_grammar.armed();
    }

    // Whether this request needs the sampler at all.
    //
    // Not the same question as `enabled()`, and this is the trap: `enabled()`
    // means "temperature > 0", so anything gated on it silently excludes greedy
    // decode -- which is the default. Two things are needed there anyway.
    //
    // A tool specification, because the grammar watches every token for a call
    // opening and a request that skipped the sampler would never arm.
    //
    // And the penalties, which are not a sampling feature: llama.cpp applies
    // its repeat_penalty to the logits before *any* selection, greedy included.
    // Gating them on temperature made a configured 1.1 inert on every default
    // request, which is exactly the regime where a low-bit checkpoint loops --
    // and measurably so: on Qwen3.8-27B at IQ1_M the same prompt writes a tool
    // parameter named "questions" without the penalty and the correct
    // "question" with it.
    // The markup ban counts too: a banned task must reach the sampler (and,
    // like every constrained task, must not draft -- speculative rounds
    // commit tokens the ban never sees).
    bool active() const {
        return enabled() || !grammar.empty() || !response_grammar.empty() ||
               markup_ban.enabled() || penalizes();
    }
};

// One in-flight engine request. phase: 0 = pending (waiting for a slot),
// 1 = prefilling, 2 = decoding.
struct QwenEngineTask {
    std::uint64_t id = 0;
    std::vector<std::uint32_t> prompt;
    std::vector<std::uint32_t> stop_tokens;
    std::uint64_t max_tokens = 0;
    QwenPromptPlan plan;
    std::uint64_t index = 0;
    std::uint32_t next_token = 0;
    std::uint64_t emitted = 0;
    // Tokens an MTP round committed beyond next_token, emitted one per engine
    // visit so a drafting task still interleaves with the others.
    std::deque<std::uint32_t> drafted;
    QwenSamplingState sampling;
    std::size_t slot = 0;
    int phase = 0;
    bool cancelled = false;
};

struct FlyweightV2QwenRuntime {
    FlyweightV2Model* model = nullptr;
    bool gemma4 = false;
    bool laguna = false;
    bool muse = false;
    // qwen4exp (Qwen3.8-Flash-Next): gated-residual streams + PLE n-gram
    // embeddings on the qwen plan shapes. The flag gates the hc_pre/hc_post
    // bookends in the forward paths and, at bring-up, disables CUDA graphs
    // (the delta capture bakes in the plain residual add).
    bool qwen4exp = false;
    // Head-collapse weights: the norm doubles as the model's final norm (the
    // GGUF carries no output_norm.weight; the collapsed stream feeds the LM
    // head directly).
    std::uint64_t output_hc_norm = std::numeric_limits<std::uint64_t>::max();
    std::uint64_t output_hc_down = std::numeric_limits<std::uint64_t>::max();
    std::uint64_t output_hc_up = std::numeric_limits<std::uint64_t>::max();
    // The 320M-row n-gram table. Host-resident always: a token reads 16 rows
    // of 160 elements, so it is served from the mapping like host embeddings.
    std::uint64_t ple_table = std::numeric_limits<std::uint64_t>::max();
    FlyweightV2QwenRuntimeOptions options{};
    flyweight::v2::ExpertExecutionMode expert_mode =
        flyweight::v2::ExpertExecutionMode::streamed_gpu;
    std::vector<QwenLayerPlan> layers;
    QwenLayerPlan mtp_layer_plan;
    std::array<std::uint64_t, 4> mtp_special_tensors{};
    bool mtp_available = false;
    std::uint64_t token_embeddings = 0;
    // The embedding table is a gather of one row per token, so holding all of
    // it in VRAM costs a full vocab x hidden matrix to serve hidden elements
    // per token. When the checkpoint keeps a separate lm_head (untied), the
    // table is staged row-by-row from the mapping instead and the arena bytes
    // go to the expert cache. Tied embeddings must stay resident: lm_head reads
    // the whole matrix every token.
    bool embeddings_host_resident = false;
    std::uint64_t embedding_row_bytes = 0;
    std::uint64_t embedding_stage = 0;        // device scratch, capacity rows
    std::uint64_t embedding_stage_bytes = 0;
    // Staging for the turbo attention path: one layer's live KV window expanded
    // to f16 so cuBLAS can run on it, reused across every attention layer. Only
    // allocated when a turbo cache type is configured.
    std::uint64_t turbo_kv_stage = 0;
    std::uint64_t turbo_kv_stage_bytes = 0;
    std::uint64_t turbo_kv_stage_stride = 0;  // bytes from the K half to the V half
    void* embedding_host = nullptr;           // pinned mirror of the above
    std::uint64_t embedding_row_index = 0;    // device [0,1,..capacity-1]
    std::uint64_t embedding_event = 0;        // guards reuse of embedding_host
    std::uint64_t final_norm = 0;
    std::uint64_t lm_head = 0;
    std::uint32_t lm_head_type = 2;
    std::uint64_t rope_factors = std::numeric_limits<std::uint64_t>::max();
    std::uint64_t static_tensor_bytes = 0;
    std::uint64_t expert_tensor_bytes = 0;
    std::uint64_t mtp_tensor_bytes = 0;
    std::uint32_t scratch_elements = 0;
    std::uint32_t moe_intermediate = 0;
    std::vector<std::uint64_t> device_tensors;
    // Effective on-device type per tensor, which is the checkpoint's own type
    // except where prepare requantized a bf16 weight to Q8_0. Every device
    // dispatch must read this rather than model->tensors[i].type; the host
    // paths still decode the mapping with the original type.
    std::vector<std::uint32_t> device_tensor_types;
    // sm_75 is where mma.*.s8 appears. Below it the MMQ kernels still compile
    // (the instruction is guarded) but fall back to a shuffle emulation that is
    // far slower than the dp4a tile kernel, so the host must not pick them.
    bool int8_tensor_cores=false;
    std::uint64_t requantized_tensors = 0;
    std::uint64_t requantized_saved_bytes = 0;
    std::uint64_t static_arena = 0;
    std::uint64_t static_arena_bytes = 0;
    // Static tensors served directly from the GGUF mapping instead of a device
    // copy (CPU backend only). Reported on stderr at prepare, alongside the
    // requantization summary, rather than through the info struct -- the ABI is
    // shared with the GPU build where these are always zero.
    std::uint64_t aliased_tensors = 0;
    std::uint64_t aliased_tensor_bytes = 0;
    std::uint64_t workspace = 0;
    std::uint64_t workspace_bytes = 0;
    flyweight::v2::workspace::QwenDecodeWorkspaceLayout decode_workspace_layout;
    flyweight::v2::workspace::QwenRowsWorkspaceLayout rows_workspace_layout;
    // Non-zero when the model's DeltaNet layers match the chunked prefill
    // kernels' fixed head_dim, which is what sized the delta_* workspace regions.
    std::uint32_t delta_value_heads = 0;
    flyweight::v2::workspace::QwenDecodeHostLayout decode_host_layout;
    flyweight::v2::workspace::QwenRowsHostLayout rows_host_layout;
    // Pinned scratch for the host-side dense SwiGLU: normalized input, the
    // gate/up activation and the projected output for one token.
    void* dense_host = nullptr;
    std::uint64_t dense_host_bytes = 0;
    std::uint32_t host_ffn_layers = 0;
    std::uint64_t host_ffn_bytes = 0;
    // Device staging for one spilled block's feed-forward triple. A spilled
    // block computes on the host, which is right for a single token and ruinous
    // for a batch: the CPU has to do rows x 3 x hidden x intermediate MACs that
    // the GPU would do in a fraction of the time, and on the IQ formats it does
    // them through a scalar decoder. Above a batch threshold the weights are
    // streamed here instead and the ordinary device path runs on them.
    std::uint64_t host_ffn_stage = 0;
    std::uint64_t host_ffn_stage_bytes = 0;
    // Spilled feed-forward weights re-encoded as Q8_0, keyed by tensor index.
    // The host dot for a codebook format converts every weight to float through
    // a grid lookup and a sign expansion -- about six vector ops per eight MACs,
    // redone for each of the thousands of output rows, which lands at a couple
    // of percent of FMA peak. Q8_0 has no codebook and no signs, so it reaches
    // the existing SIMD row kernels and becomes bandwidth-bound instead. Costs
    // host RAM (2.56 bpw -> 8.5), which is the resource that is spare precisely
    // when weights are being spilled. The device keeps reading the original
    // bytes: its codebook kernels are fast and the compact form is what makes
    // the batch upload cheap.
    std::unordered_map<std::uint64_t,std::vector<std::uint8_t>> host_ffn_q8;
    std::uint64_t host_ffn_q8_bytes = 0;
    std::uint32_t host_ffn_q8_type = 8;
    std::uint64_t dense_host_nanoseconds = 0;
    // Cacheable mirror of the pinned dense scratch; see qwen_cpu_dense_ffn.
    std::vector<float> dense_scratch;
    // 8-row interleaved Q4_0 repacks for the expert GEMM, keyed by tensor
    // index and filled lazily on the first chunk that touches the layer. Same
    // bytes as the mapping, different order, so this trades RSS (the full
    // expert set when every layer has run) for the GEMM's contiguous loads.
    std::unordered_map<std::uint64_t,std::vector<std::uint8_t>> q4_gemm_repack;
    std::uint64_t last_sampling_normalized = 0;
    std::uint64_t last_sampling_logits = 0;
    std::uint64_t state = 0;
    std::uint64_t state_bytes = 0;
    std::uint64_t expert_staging = 0;
    std::uint64_t expert_staging_bytes = 0;
    // Prefill expert-streaming arena (plans/prefill-expert-stream.md): routed
    // experts staged here per half-layer so the grouped kernels can take a
    // byte-budgeted share of the prefill MoE off the CPU. Sized by
    // FLYWEIGHT_PREFILL_EXPERT_STREAM_MIB at prepare; zero keeps prefill
    // experts wholly on the host, which is today's behavior.
    std::uint64_t prefill_stream_arena = 0;
    std::uint64_t prefill_stream_bytes = 0;
    // Scratch for the expert-GEMM path: packed activation tiles, their Q8
    // form, the SwiGLU intermediates and the down output, two span slices.
    // Layout order must match the offsets qwen_forward_rows derives.
    std::uint64_t prefill_stream_scratch = 0;
    std::uint64_t prefill_stream_scratch_bytes = 0;
    std::uint64_t prefill_stream_scratch_span = 0;
    // Importance-matrix capture (FLYWEIGHT_IMATRIX=1): one f32 accumulator per
    // input channel of every matmul weight, laid out per tensor by
    // `imatrix_offsets` (~0 = untracked). The dense prefill path accumulates
    // on the device via qwen_imatrix_accumulate; the CPU expert path
    // accumulates into `imatrix_host`; a read merges the two. `imatrix_rows`
    // counts activation rows seen per tensor, which is the denominator the
    // imatrix file format stores.
    std::uint64_t imatrix_sums = 0;
    std::uint64_t imatrix_elements = 0;
    std::vector<std::uint64_t> imatrix_offsets;
    std::vector<float> imatrix_host;
    std::vector<std::uint64_t> imatrix_rows;
    std::uint64_t expert_cache = 0;
    std::uint64_t expert_cache_bytes = 0;
    std::uint64_t expert_native_cache = 0;
    std::uint64_t expert_native_cache_bytes = 0;
    std::uint64_t expert_slot_bytes = 0;
    std::vector<QwenExpertSlot> expert_slots;
    // Laguna caches complete routed-expert layers instead of spreading a
    // partial working set across every layer. Entries are the first cache slot
    // for that layer, or -1 for layers whose routed experts remain on the CPU.
    std::vector<std::int32_t> whole_expert_layer_slots;
    std::vector<QwenExpertHistory> expert_history;
    std::string expert_history_path;
    std::uint64_t expert_history_fingerprint = 0;
    std::uint64_t expert_history_loaded_entries = 0;
    std::uint64_t expert_history_saves = 0;
    std::vector<std::uint16_t> expert_transitions;
    std::uint64_t next_layer_prefetch_predictions = 0;
    std::uint64_t next_layer_prefetch_hits = 0;
    std::uint64_t next_layer_prefetch_bytes = 0;
    std::uint64_t next_layer_prefetch_trained_pairs = 0;
    // Route-recurrence probe. `observations` is the denominator (routed experts
    // seen at decode with at least one prior token on the same layer);
    // `prev_hits` counts those the immediately preceding token also routed to,
    // `window_hits` those any of the last kRouteRecurrenceDepth tokens did.
    std::uint64_t route_recurrence_observations = 0;
    std::uint64_t route_recurrence_prev_hits = 0;
    std::uint64_t route_recurrence_window_hits = 0;
    std::uint64_t route_recurrence_layer_samples = 0;
    // Distinct experts held by the window, summed over layer samples. The
    // window's hit rate is only meaningful against this: it is the residency
    // the window would cost if it were used as a prefetch set.
    std::uint64_t route_recurrence_window_experts = 0;
    // Miss decomposition against the same window: how much of the residual
    // miss rate a whole-token prefetch plan could have covered in advance.
    std::uint64_t route_recurrence_resident = 0;
    std::uint64_t route_recurrence_miss_in_window = 0;
    std::uint64_t route_recurrence_miss_cold = 0;
    std::uint64_t nvfp4_tensor_core_moe_calls = 0;
    std::uint64_t nvfp4_tensor_core_moe_fallbacks = 0;
    std::int64_t nvfp4_tensor_core_moe_last_status = 0;
    std::unordered_map<std::uint64_t, std::size_t> expert_residency;
    std::uint64_t expert_clock = 0;
    std::uint64_t expert_cache_hits = 0;
    std::uint64_t expert_cache_misses = 0;
    std::uint64_t expert_cache_evictions = 0;
    std::uint64_t expert_cache_admissions = 0;
    std::uint64_t expert_cache_rejections = 0;
    std::uint64_t expert_cache_prompt_bypasses = 0;
    std::uint64_t expert_cache_deferred_admissions = 0;
    // What first-touch admission costs: an expert uploaded into a slot and
    // evicted again without a single route reading it. Hit rate cannot show
    // this -- a wasted upload and a never-attempted one look identical there.
    std::uint64_t expert_cache_unused_admissions = 0;
    std::uint64_t expert_residency_epochs = 0;
    std::uint64_t prefix_cache_hits = 0;
    std::uint64_t prefix_cache_misses = 0;
    std::uint64_t prefix_cache_reused_tokens = 0;
    std::uint64_t prefix_cache_reprefilled_tokens = 0;
    std::uint64_t prefix_cache_last_prompt_tokens = 0;
    std::uint64_t prefix_cache_last_reused_tokens = 0;
    std::uint64_t prefix_cache_last_lcp_live = 0;
    std::uint64_t prefix_cache_last_lcp_snapshot = 0;
    // KV occupancy against the reservation (phase 0 of plans/paged-kv-cache.md).
    // Every slot reserves state_bytes for the full context at prepare, whether
    // or not a conversation ever fills it; these are the high-water marks of
    // what the session actually touched, which is what a paged pool would have
    // had to hold. Sampled at request boundaries: inside a request `position`
    // only grows, so the boundary is that request's own maximum, and slot
    // recycling only lowers the sum.
    std::uint64_t kv_peak_live_bytes = 0;
    std::uint64_t kv_peak_tokens = 0;      // slot positions summed at that peak
    std::uint64_t kv_peak_tokens_max = 0;  // largest single-slot position seen
    std::uint64_t kv_occupancy_samples = 0;
    // Cross-slot prefix donations: prompts that shared a conversation's opening
    // without continuing it, and were given a slot seeded from it rather than
    // taking the slot over. See qwen_donate_prefix.
    std::uint64_t prefix_donations = 0;
    std::uint64_t prefix_donated_tokens = 0;
    std::uint64_t mtp_draft_tokens = 0;
    std::uint64_t mtp_accepted_tokens = 0;
    std::uint64_t mtp_rejected_tokens = 0;
    std::uint64_t mtp_draft_nanoseconds = 0;
    std::uint64_t mtp_verify_nanoseconds = 0;
    std::uint64_t mtp_rollback_nanoseconds = 0;
    std::uint64_t mtp_calibration_decode_nanoseconds = 0;
    std::uint64_t mtp_calibration_round_nanoseconds = 0;
    std::uint64_t mtp_calibration_round_tokens = 0;
    std::uint32_t mtp_calibration_decode_tokens = 0;
    std::uint32_t mtp_calibration_rounds = 0;
    std::uint32_t mtp_calibration_warmup_tokens = 0;
    bool mtp_calibration_draft_turn = false;
    bool mtp_calibration_done = false;
    bool mtp_adaptive_disabled = false;
    bool mtp_adaptive_reported = false;
    std::uint64_t mtp_target_hidden_offset = 0;
    std::uint64_t mtp_draft_hidden_offset = 0;
    std::uint64_t mtp_verified_hidden_offset = 0;
    std::uint64_t mtp_snapshot_offset = 0;
    std::uint64_t mtp_snapshot_bytes = 0;
    // MTP fold (ReplaySSM-style rejection rollback). `capture` is set only for
    // the duration of the batched verify pass and redirects each DeltaNet
    // layer's transition inputs into its retention arena; `valid` says the
    // arena holds the complete current batch (cleared if a partial span made
    // the capture unusable); `rows` is the batch row count the retention
    // layout was written with, which fixes the beta-logit offset during fold.
    bool mtp_fold_capture = false;
    bool mtp_fold_valid = false;
    std::uint32_t mtp_fold_rows = 0;
    bool mtp_delta_layers = false;
    std::uint64_t mtp_cache_tokens = 0;
    std::uint64_t decode_calls = 0;
    std::uint64_t decode_nanoseconds = 0;
    std::uint64_t route_wait_nanoseconds = 0;
    std::uint64_t expert_page_nanoseconds = 0;
    std::uint64_t tail_wait_nanoseconds = 0;
    std::uint64_t route_expert_sum = 0;
    std::uint64_t expert_compute_nanoseconds = 0;
    std::uint64_t prefill_cache_seeded_experts = 0;
    std::uint64_t prefill_cache_seed_nanoseconds = 0;
    std::uint64_t prefill_cache_seed_bytes = 0;
    std::uint64_t prefill_cache_seed_selected_experts = 0;
    std::uint64_t prefill_cache_seed_hits = 0;
    std::uint64_t prefill_cache_seed_avoided_misses = 0;
    std::uint64_t prefill_cache_seed_auto_skips = 0;
    std::uint64_t prefill_cache_seed_budget_stops = 0;
    std::uint64_t sampling_gpu_topk_calls = 0;
    std::uint64_t sampling_gpu_topk_bytes = 0;
    std::uint64_t sampling_full_download_bytes = 0;
    std::uint64_t sampling_nanoseconds = 0;
    // Tool-grammar accounting. `empty_candidate_sets` is the one to watch: it
    // counts the steps where the constraint had nothing to offer and the
    // sampler fell back, which is the grammar failing to do its job rather
    // than a malformed call.
    std::uint64_t grammar_constrained_steps = 0;
    std::uint64_t grammar_rejected_candidates = 0;
    std::uint64_t grammar_empty_candidate_sets = 0;
    std::uint64_t multi_decode_batches = 0;
    std::uint64_t multi_decode_tokens = 0;
    std::uint64_t prefill_streamed_bytes = 0;
    std::uint64_t paging_registration_nanoseconds = 0;
    std::uint64_t host_available_bytes = 0;
    std::uint64_t cpu_prefetch_experts = 0;
    std::uint64_t cpu_prefetch_bytes = 0;
    std::uint64_t cpu_prefetch_nanoseconds = 0;
    std::uint64_t cpu_prefetch_pages = 0;
    std::uint64_t cpu_prefetch_cold_pages = 0;
    std::uint64_t cpu_prefetch_loaded_pages = 0;
    std::uint64_t cpu_prefetch_auto_skips = 0;
    std::uint64_t cpu_prefetch_last_budget_bytes = 0;
    std::uint64_t prefill_calls = 0;
    std::uint64_t prefill_tokens = 0;
    std::uint64_t prefill_nanoseconds = 0;
    std::uint64_t prefill_route_wait_nanoseconds = 0;
    std::uint64_t prefill_expert_nanoseconds = 0;
    std::uint64_t prefill_gpu_core_nanoseconds = 0;
    std::uint64_t prefill_gpu_router_nanoseconds = 0;
    std::uint64_t prefill_gpu_transfer_nanoseconds = 0;
    // How many layers fed the three counters above. Zero distinguishes "not
    // measured" from "measured as zero" -- the pipelined driver cannot record
    // the event pairs, and a bare 0.00 there reads as though the GPU did no
    // work at all.
    std::uint64_t prefill_gpu_split_layers = 0;
    // The n-gram engram staging: hash, decode rows out of the 26.8 GiB
    // per_layer_token_embd mmap, upload. Host work, so it is measured in both
    // drivers -- and it is the largest single tensor in the checkpoint, so its
    // share rises as the expert phase falls.
    std::uint64_t prefill_ple_nanoseconds = 0;
    std::uint8_t cpu_prefetch_checksum = 0;
    void* host_staging = nullptr;
    std::uint64_t host_staging_bytes = 0;
    std::uint32_t forward_rows_capacity = 0;
    std::uint32_t prefill_rows = 0;
    std::vector<QwenPrefillSnapshot> prefill_snapshots{std::vector<QwenPrefillSnapshot>(2)};
    std::uint64_t prefill_snapshot_bytes = 0;
    std::uint32_t prefill_checkpoint_interval = 0; // tokens before the first mid-prefill checkpoint; 0 disables
    std::uint64_t prefill_snapshot_clock = 0;
    std::uint64_t stream = 0;
    std::uint64_t graph_stream = 0;
    std::uint64_t route_event = 0;
    // Second route event for the two-half prefill pipeline: each half syncs
    // only its own route downloads, so one half's wait never extends to the
    // other half's queued core work.
    std::uint64_t route_event_pipeline = 0;
    // Prefill expert-streaming fences, one pair per span. Uploads ride the
    // prefetch stream, so the compute stream waits on upload_ready before
    // the grouped kernels consume staged weights, and the prefetch stream
    // waits on stream_consumed before overwriting a span's slice with the
    // next layer's experts.
    std::uint64_t stream_upload_event = 0;
    std::uint64_t stream_upload_event_pipeline = 0;
    std::uint64_t stream_consumed_event = 0;
    std::uint64_t stream_consumed_event_pipeline = 0;
    std::uint64_t prefill_layer_start_event = 0;
    std::uint64_t prefill_core_end_event = 0;
    std::uint64_t prefill_router_end_event = 0;
    bool prefill_profile = false;
    bool cuda_profile = false;
    bool cuda_graphs = false;
    std::uint64_t cuda_graph_builds = 0;
    std::uint64_t cuda_graph_replays = 0;
    std::uint64_t cuda_graph_fallbacks = 0;
    // Host-side submission cost of the delta block, so a graph replay can be
    // compared against the eager launches it replaces. Trace-gated.
    std::uint64_t delta_host_ns = 0;
    std::uint64_t delta_host_calls = 0;
    bool fused_attention = true;
    // qwen4exp QSA selection. Resolved once per runtime at prepare rather than
    // memoized in a process-wide static: two runtimes in one process (the
    // parity tests, and any embedder holding more than one model) must be able
    // to disagree, and a latched static made the dense-fallback exactness test
    // compare a configuration against itself.
    bool qsa_enabled = false;  // opt-in: FLYWEIGHT_QSA=1
    // Interleaving two distant expert matrices hurts mmap/TLB locality on
    // memory-bound CPU MoE. Keep the experimental kernel opt-in until it can
    // demonstrate a win across representative hardware and expert routing.
    bool fused_moe_gate_up = false;
    std::vector<QwenCudaLayerProfile> cuda_layer_profiles;
    std::uint64_t cuda_tail_start = 0, cuda_lm_start = 0;
    std::uint64_t cuda_lm_end = 0, cuda_tail_end = 0;
    std::uint64_t position = 0;
    std::uint32_t last_output_token = 0;
    bool last_output_greedy = true;    // see QwenPrefillSnapshot::last_output_greedy
    std::vector<std::uint32_t> processed_tokens;
    // Parallel decode slots (see QwenSequence). sequences[active_sequence] owns
    // the arena that runtime.state currently points at; its bookkeeping is the
    // live runtime.{position,last_output_token,processed_tokens}. Default 1 slot
    // reproduces the legacy single-sequence runtime exactly.
    std::vector<QwenSequence> sequences;
    std::size_t active_sequence = 0;
    std::uint64_t sequence_clock = 0;
    std::uint32_t parallel_sequences = 1;
    // Slot geometries, one per distinct slot size; [0] is the full-context one
    // and every slot references one of these. A single entry means symmetric
    // slots, which is the legacy layout exactly.
    std::vector<QwenSlotGeometry> geometries;
    // What the slots reserve in total. A sum, not slots x state_bytes: slots
    // need not be the same size, and that difference is the point.
    std::uint64_t slots_state_bytes = 0;
    // Host-backed prompt cache (see QwenHostPrompt). limit=0 disables it.
    std::vector<QwenHostPrompt> host_prompts;
    std::uint64_t host_cache_limit_bytes = 0;
    std::uint64_t host_cache_used_bytes = 0;
    std::uint64_t host_cache_clock = 0;
    // Cooperative engine (see flyweight_v2_qwen_engine_step). engine_pending /
    // engine_cancel_requests / engine_next_task_id are guarded by engine_mutex
    // (submit/cancel arrive from request threads). engine_tasks elements are
    // touched only by the single engine-stepping thread, but its *size* is
    // read by submit's queue-full check, so every size change (admission
    // push, finished-task erase) happens under engine_mutex too. slot_owner
    // is engine-thread-only.
    std::vector<struct QwenEngineTask> engine_pending;
    std::vector<std::uint64_t> engine_cancel_requests;
    std::uint64_t engine_next_task_id = 1;
    std::vector<struct QwenEngineTask> engine_tasks;
    std::vector<long long> slot_owner;
    std::size_t engine_cursor = 0;
    std::mutex engine_mutex;
    // Multi-sequence decode (engine Phase B): per-slot route events let each
    // sequence's router readback complete independently; the staging event
    // fences reuse of the shared expert-paging staging area against its async
    // (stream-queued) consumers. Capacity is how many per-sequence workspace
    // slices fit; host block is one sequence's private host staging.
    std::vector<std::uint64_t> slot_events;
    std::uint64_t staging_event = 0;
    std::uint64_t prefetch_stream = 0;
    std::uint64_t prefetch_event = 0;
    bool prefetch_pending = false;
    std::uint32_t prefetch_target_layer =
        std::numeric_limits<std::uint32_t>::max();
    std::uint64_t decode_slice_bytes = 0;
    std::uint64_t decode_host_block_bytes = 0;
    std::uint32_t multi_decode_capacity = 1;
    bool cancelled = false;
    bool cache_admission_enabled = true;
    bool expert_residency_frozen = false;
    bool strict_cache_admission = true;
    bool mtp_has_target_hidden = false;
    bool cuda_ready = false;
    bool decode_ready = false;
    bool dma_paging = false;       // expert page-ins go straight from the registered mmap
    // The host ranges actually handed to cuMemHostRegister. Registration used to
    // be all-or-nothing over the whole mapping, which on a checkpoint bigger than
    // RAM meant it never happened and every cache miss paid a host memcpy; these
    // are the expert tensors (and the embedding when it fits) instead, so the
    // decision scales with what is actually paged rather than with file size.
    // Kept for unregistration and for the per-range membership test.
    std::vector<std::pair<const std::uint8_t*,std::uint64_t>> registered_ranges;
    bool embedding_dma = false;
    // Registration runs on a worker so decode does not wait ~20s for it. The
    // staged path stays correct for any layer not yet registered, so the only
    // coordination needed is publishing "layer N is now direct". The worker
    // appends finished layers under `registration_mutex` and bumps
    // `registration_ready`; the DECODE thread drains that into `expert_dma` at a
    // token boundary, so those flags are only ever written by the thread that
    // reads them and no per-layer atomics are needed.
    std::thread registration_thread;
    std::mutex registration_mutex;
    std::vector<std::uint32_t> registration_done;
    std::atomic<std::uint32_t> registration_ready{0};
    std::atomic<bool> registration_cancel{false};
    bool model_registered = false; // whether we cuMemHostRegister'd model->data
};

flyweight::v2::ExpertExecutionPolicy qwen_expert_policy(
    const FlyweightV2QwenRuntime& runtime,
    flyweight::v2::ExpertExecutionPhase phase
) {
    return {runtime.expert_mode,phase,runtime.cache_admission_enabled,
            runtime.options.hybrid_prefill_cpu!=0,
            runtime.expert_residency_frozen};
}

bool qwen_immutable_residency(const FlyweightV2QwenRuntime& runtime) {
    return runtime.options.immutable_residency!=0&&
        runtime.expert_mode==flyweight::v2::ExpertExecutionMode::hybrid;
}

bool qwen_freeze_expert_residency(FlyweightV2QwenRuntime& runtime) {
    if(!qwen_immutable_residency(runtime)||runtime.expert_residency_frozen)
        return false;
    runtime.expert_residency_frozen=true;
    ++runtime.expert_residency_epochs;
    return true;
}

void qwen_unfreeze_expert_residency(FlyweightV2QwenRuntime& runtime) {
    runtime.expert_residency_frozen=false;
}

struct QwenResidencyEpochGuard {
    FlyweightV2QwenRuntime& runtime;
    bool owned = false;
    explicit QwenResidencyEpochGuard(FlyweightV2QwenRuntime& value)
        :runtime(value),owned(qwen_freeze_expert_residency(value)){}
    ~QwenResidencyEpochGuard(){
        if(owned)qwen_unfreeze_expert_residency(runtime);
    }
};

std::size_t qwen_cache_layer_count(const FlyweightV2QwenRuntime& runtime) {
    // The MTP block's routed experts execute directly on the CPU and do
    // not consult expert_residency. Only target layers consume GPU cache slots
    // or persistent routing history.
    return runtime.layers.size();
}

constexpr std::size_t kNoExpertSlot = std::numeric_limits<std::size_t>::max();

// GGUF quantization block sizes (bytes per block). kQ8/kQ4K/kQ5K/kQ6K and
// kBlockElements live in src/qwen_kquant.h beside the decoders that use them.
constexpr std::uint32_t kQ4BlockSize = 18;   // Q4_0: 2-byte scale + 16 nibbles + 2 padding per 32 elements
constexpr std::uint32_t kTurbo3BlockSize = 14; // TurboQuant 3-bit: f16 scale + 32 packed indices
constexpr std::uint32_t kTurbo4BlockSize = 18; // TurboQuant 4-bit: f16 scale + 32 packed indices

// Bytes needed to hold `elements` KV slots at cache precision `t`
// (0=f32, 1=f16, 2=bf16, 3=q8_0, 4=turbo3, 5=turbo4).
inline std::uint64_t kv_type_bytes(std::uint64_t elements, int t) {
    if (t == 4) return (elements / 32) * kTurbo3BlockSize;
    if (t == 5) return (elements / 32) * kTurbo4BlockSize;
    return t == 3 ? (elements / 32) * kQ8BlockSize : elements * (t == 0 ? 4 : 2);
}
inline bool kv_type_is_turbo(int t) { return t == 4 || t == 5; }

// Point the layer plans at one slot geometry's offsets. This is the same trick
// runtime.state plays for the arena pointer: the dispatch sites read
// layer.state_first / layer.cache_capacity and pass them to kernels, so making
// those describe the ACTIVE slot is what lets slots differ in size without a
// single kernel or dispatch site learning about it.
static void qwen_apply_geometry(FlyweightV2QwenRuntime& runtime, std::size_t index) {
    if (index >= runtime.geometries.size()) return;
    const auto& g = runtime.geometries[index];
    auto apply_layer = [](QwenLayerPlan& layer, const QwenLayerOffsets& o) {
        layer.cache_capacity = o.cache_capacity;
        layer.state_first = o.state_first;
        layer.state_second = o.state_second;
        layer.state_third = o.state_third;
        layer.snapshot_first = o.snapshot_first;
        layer.snapshot_second = o.snapshot_second;
        layer.snapshot_third = o.snapshot_third;
        layer.qsa_blocks = o.qsa_blocks;
        layer.qsa_ring = o.qsa_ring;
        layer.mtp_retain_qkv = o.mtp_retain_qkv;
        layer.mtp_retain_logits = o.mtp_retain_logits;
    };
    for (std::size_t i = 0; i < runtime.layers.size() && i < g.layers.size(); ++i)
        apply_layer(runtime.layers[i], g.layers[i]);
    apply_layer(runtime.mtp_layer_plan, g.mtp_layer);
    runtime.mtp_target_hidden_offset = g.mtp_target_hidden_offset;
    runtime.mtp_draft_hidden_offset = g.mtp_draft_hidden_offset;
    runtime.mtp_verified_hidden_offset = g.mtp_verified_hidden_offset;
    runtime.mtp_snapshot_offset = g.mtp_snapshot_offset;
    runtime.mtp_snapshot_bytes = g.mtp_snapshot_bytes;
    runtime.state_bytes = g.state_bytes;
}

// Slot 0 keeps the full context; the rest take the scratch geometry when there
// is one. One rule, shared by the reservation estimate and the slots
// themselves, so the two cannot disagree about what was allocated.
static std::size_t qwen_slot_geometry_index(
        const FlyweightV2QwenRuntime& runtime, std::size_t slot) {
    return slot && runtime.geometries.size() > 1 ? 1 : 0;
}

// The context this slot can hold. Symmetric runtimes answer with the one limit.
static std::uint64_t qwen_slot_context(
        const FlyweightV2QwenRuntime& runtime, std::size_t slot) {
    if (slot >= runtime.sequences.size() || runtime.geometries.empty())
        return runtime.options.context_limit;
    const auto index = runtime.sequences[slot].geometry;
    return index < runtime.geometries.size()
        ? runtime.geometries[index].context_limit : runtime.options.context_limit;
}


constexpr std::uint32_t kQ2KBlockSize = kQ2KBlockBytes;   // Q2_K: 84 bytes per 256 elements
constexpr std::uint32_t kQ3KBlockSize = kQ3KBlockBytes;   // Q3_K: 110 bytes per 256 elements
constexpr std::uint32_t kIq2xxsBlockSize = kIq2xxsBlockBytes; // IQ2_XXS: 66 bytes per 256 elements
constexpr std::uint32_t kIq3xxsBlockSize = kIq3xxsBlockBytes; // IQ3_XXS: 98 bytes per 256 elements
// IQ1_S: d(2) + qs[32] + qh[8*2] = 50 bytes per 256 values, 1.5625 bits each.
constexpr std::uint32_t kIq1sBlockSize = kIq1sBlockBytes;
constexpr std::uint32_t kMxfp4BlockSize = 17;      // MXFP4: e[1] E8M0 scale + qs[16] nibbles
constexpr std::uint32_t kMxfp4BlockElements = 32;
// The FP4 codebook, doubled -- which is why the scale is halved to match.
constexpr float kMxfp4Lut[16] = {
    0.0f, 1.0f, 2.0f, 3.0f, 4.0f, 6.0f, 8.0f, 12.0f,
    0.0f, -1.0f, -2.0f, -3.0f, -4.0f, -6.0f, -8.0f, -12.0f,
};
// E8M0 exponent to float, halved: 2^(x-128). Values below 2 land in the
// denormal range and are built from bit patterns rather than shifted.
inline float mxfp4_scale(std::uint8_t exponent) {
    const std::uint32_t bits = exponent < 2
        ? (0x00200000u << exponent)
        : (static_cast<std::uint32_t>(exponent - 1) << 23);
    float value;
    std::memcpy(&value, &bits, sizeof(value));
    return value;
}
constexpr std::uint32_t kNvfp4BlockSize = 36;      // NVFP4: d[4] E4M3 scales + qs[32] nibbles
constexpr std::uint32_t kNvfp4BlockElements = 64;  // 4 sub-blocks of 16 elements
constexpr std::uint32_t kNvfp4SubBlock = 16;       // elements governed by one scale

// The AVX2/AVX-512 entry points decode anything they do not recognize with
// their Q8_0 path, so this has to be an allowlist of the formats that actually
// have a hand-written SIMD kernel. A blocklist silently mis-decodes every type
// nobody remembered to add to it. NVFP4 is dispatched separately, to AVX2, and
// deliberately so: qwen_cpu_avx512.cpp does carry a complete NVFP4 kernel set,
// but it is ~15% SLOWER than the AVX2 one at every working set from 72 KiB to
// 4.6 MiB (138.9 vs 159.2 ns/row, identical checksums), so routing type 40
// there costs throughput. Measured 2026-08-10 on Zen 5 against
// Qwen3.6-35B-Fast-NVFP4; do not "fix" this dispatch without re-measuring.
constexpr bool qwen_simd_quant_type(std::uint32_t type) {
    return type == 8 || type == 10 || type == 11 || type == 12 || type == 13 || type == 14;
}

// The multi-input entry points (pair/quad/oct) only have register-blocked
// kernels for a subset, so they take a narrower allowlist. Anything excluded
// still reaches SIMD one row at a time through the single-row fallback.
constexpr bool qwen_simd_multi_type(std::uint32_t type) {
    return type == 8 || type == 12 || type == 13 || type == 14;
}

constexpr char kExpertHistoryMagic[8] = {'C','O','L','H','I','S','T','1'};
constexpr std::uint32_t kExpertHistoryVersion = 1;

static std::uint64_t expert_history_hash_bytes(
    std::uint64_t hash, const void* data, std::size_t size
) {
    const auto* bytes = static_cast<const std::uint8_t*>(data);
    for (std::size_t index = 0; index < size; ++index) {
        hash ^= bytes[index];
        hash *= 1099511628211ULL;
    }
    return hash;
}

static std::uint64_t qwen_model_fingerprint(const FlyweightV2Model& model) {
    std::uint64_t hash = 1469598103934665603ULL;
    // Total mapped bytes, not the opened file's: a split checkpoint's first
    // shard is a few megabytes of metadata and would not distinguish models.
    // This equals `size` for a single file, so existing fingerprints hold.
    const std::uint64_t bytes = model.mapped_bytes();
    hash = expert_history_hash_bytes(hash, &bytes, sizeof(bytes));
    hash = expert_history_hash_bytes(
        hash, model.architecture.data(), model.architecture.size()
    );
    hash = expert_history_hash_bytes(hash, model.name.data(), model.name.size());
    for (const auto& tensor : model.tensors) {
        hash = expert_history_hash_bytes(hash, tensor.name.data(), tensor.name.size());
        hash = expert_history_hash_bytes(hash, &tensor.offset, sizeof(tensor.offset));
        hash = expert_history_hash_bytes(hash, &tensor.size, sizeof(tensor.size));
    }
    return hash;
}

static std::string qwen_expert_history_path(const FlyweightV2Model& model) {
    const char* setting = std::getenv("FLYWEIGHT_EXPERT_HISTORY");
    if (setting && (!std::strcmp(setting, "0") || !std::strcmp(setting, "off"))) {
        return {};
    }
    if (setting && *setting && std::strcmp(setting, "1") &&
        std::strcmp(setting, "auto")) {
        return setting;
    }
    return model.path.empty() ? std::string{} : model.path + ".expert-history";
}

template <typename T>
static bool expert_history_read(std::ifstream& input, T& value) {
    return static_cast<bool>(
        input.read(reinterpret_cast<char*>(&value), sizeof(value))
    );
}

template <typename T>
static bool expert_history_write(std::ofstream& output, const T& value) {
    return static_cast<bool>(
        output.write(reinterpret_cast<const char*>(&value), sizeof(value))
    );
}

static void qwen_load_expert_history(FlyweightV2QwenRuntime& runtime) {
    runtime.expert_history_path = qwen_expert_history_path(*runtime.model);
    runtime.expert_history_fingerprint = qwen_model_fingerprint(*runtime.model);
    if (runtime.expert_history_path.empty()) return;
    std::ifstream input(runtime.expert_history_path, std::ios::binary);
    if (!input) return;
    char magic[sizeof(kExpertHistoryMagic)]{};
    std::uint32_t version = 0, layers = 0, experts = 0;
    std::uint64_t fingerprint = 0, clock = 0, entries = 0;
    if (!input.read(magic, sizeof(magic)) ||
        std::memcmp(magic, kExpertHistoryMagic, sizeof(magic)) ||
        !expert_history_read(input, version) ||
        !expert_history_read(input, fingerprint) ||
        !expert_history_read(input, layers) ||
        !expert_history_read(input, experts) ||
        !expert_history_read(input, clock) ||
        !expert_history_read(input, entries) ||
        version != kExpertHistoryVersion ||
        fingerprint != runtime.expert_history_fingerprint ||
        layers != runtime.layers.size() ||
        experts != runtime.model->config.expert_count ||
        entries != runtime.expert_history.size()) {
        std::fprintf(
            stderr,
            "[flyweight] ignoring incompatible expert history: %s\n",
            runtime.expert_history_path.c_str()
        );
        return;
    }
    std::vector<QwenExpertHistory> loaded(runtime.expert_history.size());
    std::uint64_t loaded_entries = 0, maximum_last_used = 0;
    for (auto& item : loaded) {
        if (!expert_history_read(input, item.frequency) ||
            !expert_history_read(input, item.last_used)) {
            std::fprintf(
                stderr,
                "[flyweight] ignoring truncated expert history: %s\n",
                runtime.expert_history_path.c_str()
            );
            return;
        }
        // Age the prior process's workload once at startup while retaining at
        // least one observation for experts that were ever useful.
        if (item.frequency) item.frequency = std::max(1U, item.frequency / 2U);
        if (item.frequency) ++loaded_entries;
        maximum_last_used = std::max(maximum_last_used, item.last_used);
    }
    if (input.peek() != std::ifstream::traits_type::eof()) {
        std::fprintf(
            stderr,
            "[flyweight] ignoring oversized expert history: %s\n",
            runtime.expert_history_path.c_str()
        );
        return;
    }
    runtime.expert_history = std::move(loaded);
    runtime.expert_clock = std::max(clock, maximum_last_used);
    runtime.expert_history_loaded_entries = loaded_entries;
    std::fprintf(
        stderr,
        "[flyweight] restored %llu learned expert entries from %s\n",
        static_cast<unsigned long long>(loaded_entries),
        runtime.expert_history_path.c_str()
    );
}

static void qwen_save_expert_history(FlyweightV2QwenRuntime& runtime) {
    if (runtime.expert_history_path.empty() || runtime.expert_history.empty()) return;
    const std::string temporary = runtime.expert_history_path + ".tmp";
    std::ofstream output(temporary, std::ios::binary | std::ios::trunc);
    if (!output) return;
    const std::uint32_t layers = static_cast<std::uint32_t>(runtime.layers.size());
    const std::uint32_t experts = runtime.model->config.expert_count;
    const std::uint64_t entries = runtime.expert_history.size();
    output.write(kExpertHistoryMagic, sizeof(kExpertHistoryMagic));
    bool valid =
        expert_history_write(output, kExpertHistoryVersion) &&
        expert_history_write(output, runtime.expert_history_fingerprint) &&
        expert_history_write(output, layers) &&
        expert_history_write(output, experts) &&
        expert_history_write(output, runtime.expert_clock) &&
        expert_history_write(output, entries);
    for (const auto& item : runtime.expert_history) {
        valid = valid && expert_history_write(output, item.frequency);
        valid = valid && expert_history_write(output, item.last_used);
    }
    output.flush();
    valid = valid && static_cast<bool>(output);
    output.close();
    if (!valid || std::rename(temporary.c_str(), runtime.expert_history_path.c_str())) {
        std::remove(temporary.c_str());
        return;
    }
    ++runtime.expert_history_saves;
}

struct QwenExpertHistorySaveGuard {
    FlyweightV2QwenRuntime& runtime;
    ~QwenExpertHistorySaveGuard() { qwen_save_expert_history(runtime); }
};

static std::size_t select_expert_cache_slot(
    FlyweightV2QwenRuntime& runtime, std::uint32_t layer,
    std::uint32_t expert, bool allow_rejection, bool record_access = true
);
static void qwen_wait_for_prefetch_layer(
    FlyweightV2QwenRuntime& runtime, std::uint32_t layer
) {
    if (!runtime.prefetch_pending ||
        runtime.prefetch_target_layer != layer) return;
    if (runtime.prefetch_event &&
        flyweight_gpu_stream_wait_event(
            runtime.stream, runtime.prefetch_event
        ) != 0)
        throw std::runtime_error("native Qwen prefetch wait failed");
    runtime.prefetch_pending = false;
    runtime.prefetch_target_layer =
        std::numeric_limits<std::uint32_t>::max();
}

// Measures how much of a decode token's routing a *previous* token already
// told us, per layer. This is the cheap predictor the current design never
// consults: it is known before the token starts, for every layer at once,
// where the transition table only ever sees one layer of runway. Pure
// measurement -- no caching, prefetch, or execution decision reads these.
static void qwen_observe_route_recurrence(
    FlyweightV2QwenRuntime& runtime, QwenExpertPrefetchState& state,
    std::uint32_t layer, const std::int32_t* selected, int selected_count
) {
    // Off unless asked for: the distinct-count pass below is O(depth^2 * top_k)
    // per layer, which is measurement overhead nobody serving should pay.
    static const bool enabled = [] {
        const char* setting = std::getenv("FLYWEIGHT_ROUTE_RECURRENCE");
        return setting && setting[0] != '0';
    }();
    if (!enabled) return;
    const auto layers = runtime.layers.size();
    if (!layers || layer >= layers || selected_count <= 0) return;
    const auto stride = static_cast<std::size_t>(selected_count);
    // Route width can shrink mid-run (top-k / top-p pruning). Sizing the ring
    // to the model's trained top-k keeps one allocation valid for the session.
    const auto capacity = std::max<std::size_t>(
        stride, runtime.model->config.expert_used_count);
    if (state.recurrence_stride != capacity) {
        state.recurrence_stride = capacity;
        state.recurrence_history.assign(
            layers * kRouteRecurrenceDepth * capacity, -1);
        state.recurrence_counts.assign(layers * kRouteRecurrenceDepth, 0);
        state.recurrence_cursor.assign(layers, 0);
        state.recurrence_filled.assign(layers, 0);
    }
    const auto base = static_cast<std::size_t>(layer) * kRouteRecurrenceDepth;
    const auto filled = state.recurrence_filled[layer];
    if (filled) {
        ++runtime.route_recurrence_layer_samples;
        // Slot `cursor - 1` (mod depth) is the most recently written token.
        const auto newest =
            (state.recurrence_cursor[layer] + kRouteRecurrenceDepth - 1) %
            kRouteRecurrenceDepth;
        for (int rank = 0; rank < selected_count; ++rank) {
            const auto expert = selected[rank];
            if (expert < 0) continue;
            ++runtime.route_recurrence_observations;
            bool in_window = false;
            for (std::size_t depth = 0; depth < filled; ++depth) {
                const auto slot = base + (newest + kRouteRecurrenceDepth - depth) %
                    kRouteRecurrenceDepth;
                const auto count = state.recurrence_counts[slot];
                const auto* entries =
                    state.recurrence_history.data() + slot * capacity;
                const bool present =
                    std::find(entries, entries + count, expert) !=
                    entries + count;
                if (!present) continue;
                in_window = true;
                if (depth == 0) ++runtime.route_recurrence_prev_hits;
                break;
            }
            if (in_window) ++runtime.route_recurrence_window_hits;
            // Sizes the latency-hiding win. Residency is read before this
            // layer's own lookup and before the next-layer prefetch (which
            // only touches layer+1's slot range), so it is exactly what the
            // layer is about to see. A miss the window already knew about is
            // one a whole-token prefetch plan could have covered in advance;
            // a miss outside the window is genuinely cold.
            const auto key = (static_cast<std::uint64_t>(layer) << 32) |
                static_cast<std::uint32_t>(expert);
            if (runtime.expert_residency.find(key) !=
                runtime.expert_residency.end()) {
                ++runtime.route_recurrence_resident;
            } else if (in_window) {
                ++runtime.route_recurrence_miss_in_window;
            } else {
                ++runtime.route_recurrence_miss_cold;
            }
        }
        // Distinct size of the same window, counted once per layer sample.
        std::uint32_t distinct = 0;
        for (std::size_t depth = 0; depth < filled; ++depth) {
            const auto slot = base + (newest + kRouteRecurrenceDepth - depth) %
                kRouteRecurrenceDepth;
            const auto* entries =
                state.recurrence_history.data() + slot * capacity;
            for (std::size_t index = 0; index < state.recurrence_counts[slot];
                 ++index) {
                const auto expert = entries[index];
                bool seen = false;
                for (std::size_t earlier = 0; earlier < depth && !seen;
                     ++earlier) {
                    const auto other =
                        base + (newest + kRouteRecurrenceDepth - earlier) %
                            kRouteRecurrenceDepth;
                    const auto* prior =
                        state.recurrence_history.data() + other * capacity;
                    seen = std::find(
                        prior, prior + state.recurrence_counts[other], expert
                    ) != prior + state.recurrence_counts[other];
                }
                if (!seen) {
                    // Also dedupe against earlier ranks of this same slot.
                    seen = std::find(entries, entries + index, expert) !=
                        entries + index;
                }
                if (!seen) ++distinct;
            }
        }
        runtime.route_recurrence_window_experts += distinct;
    }
    const auto write = base + state.recurrence_cursor[layer];
    std::copy(selected, selected + selected_count,
              state.recurrence_history.begin() +
                  static_cast<std::ptrdiff_t>(write * capacity));
    state.recurrence_counts[write] = static_cast<std::uint8_t>(selected_count);
    state.recurrence_cursor[layer] =
        static_cast<std::uint8_t>((state.recurrence_cursor[layer] + 1) %
                                  kRouteRecurrenceDepth);
    if (filled < kRouteRecurrenceDepth)
        state.recurrence_filled[layer] = static_cast<std::uint8_t>(filled + 1);
}

static void qwen_observe_and_prefetch_next_layer(
    FlyweightV2QwenRuntime& runtime, QwenExpertPrefetchState& state,
    std::uint32_t layer,
    const std::int32_t* selected, int selected_count
) {
    const auto budget = runtime.options.next_layer_prefetch;
    const std::uint32_t experts = runtime.model->config.expert_count;
    const auto policy=qwen_expert_policy(
        runtime,flyweight::v2::ExpertExecutionPhase::decode);
    if (!budget || !experts || selected_count <= 0 ||
        runtime.expert_transitions.empty()) {
        return;
    }
    if (layer == 0) {
        // A completed token has no pending final-layer prediction. Clear any
        // stale bookkeeping defensively without carrying transition context
        // across token boundaries.
        for (const auto prediction : state.pending_predictions) {
            if (state.previous_route_layer ==
                std::numeric_limits<std::uint32_t>::max()) break;
            const auto target_layer = state.previous_route_layer + 1;
            const auto key =
                (static_cast<std::uint64_t>(target_layer) << 32) |
                static_cast<std::uint32_t>(prediction);
            const auto resident = runtime.expert_residency.find(key);
            if (resident != runtime.expert_residency.end()) {
                auto& slot = runtime.expert_slots[resident->second];
                if (slot.prefetch_pins) --slot.prefetch_pins;
            }
        }
        state.previous_layer_route.clear();
        state.pending_predictions.clear();
        state.previous_route_layer =
            std::numeric_limits<std::uint32_t>::max();
    }
    if (state.previous_route_layer !=
            std::numeric_limits<std::uint32_t>::max() &&
        state.previous_route_layer + 1 == layer) {
        for (const auto prediction : state.pending_predictions) {
            for (int rank = 0; rank < selected_count; ++rank) {
                if (selected[rank] == prediction) {
                    ++runtime.next_layer_prefetch_hits;
                    break;
                }
            }
            const auto key =
                (static_cast<std::uint64_t>(layer) << 32) |
                static_cast<std::uint32_t>(prediction);
            const auto resident = runtime.expert_residency.find(key);
            if (resident != runtime.expert_residency.end()) {
                auto& slot = runtime.expert_slots[resident->second];
                if (slot.prefetch_pins) --slot.prefetch_pins;
            }
        }
        const std::size_t boundary =
            static_cast<std::size_t>(layer - 1) * experts * experts;
        for (const auto source : state.previous_layer_route) {
            if (source < 0 || static_cast<std::uint32_t>(source) >= experts) continue;
            for (int rank = 0; rank < selected_count; ++rank) {
                const auto target = selected[rank];
                if (target < 0 || static_cast<std::uint32_t>(target) >= experts) continue;
                auto& count = runtime.expert_transitions[
                    boundary + static_cast<std::size_t>(source) * experts +
                    static_cast<std::uint32_t>(target)
                ];
                if (count != std::numeric_limits<std::uint16_t>::max()) ++count;
                ++runtime.next_layer_prefetch_trained_pairs;
            }
        }
    }
    state.previous_layer_route.assign(selected, selected + selected_count);
    state.previous_route_layer = layer;
    state.pending_predictions.clear();
    if (layer + 1 >= runtime.layers.size()) return;

    const std::size_t boundary =
        static_cast<std::size_t>(layer) * experts * experts;
    std::vector<std::pair<std::uint64_t, std::int32_t>> scores;
    scores.reserve(experts);
    for (std::uint32_t candidate = 0; candidate < experts; ++candidate) {
        std::uint64_t score = 0;
        for (const auto source : state.previous_layer_route) {
            if (source >= 0 && static_cast<std::uint32_t>(source) < experts) {
                score += runtime.expert_transitions[
                    boundary + static_cast<std::size_t>(source) * experts +
                    candidate
                ];
            }
        }
        if (score) scores.emplace_back(score, static_cast<std::int32_t>(candidate));
    }
    const auto count = std::min<std::size_t>(budget, scores.size());
    std::partial_sort(
        scores.begin(), scores.begin() + count, scores.end(),
        [](const auto& left, const auto& right) {
            return left.first != right.first
                ? left.first > right.first : left.second < right.second;
        }
    );
    const auto& next = runtime.layers[layer + 1];
    bool queued_gpu_upload = false;
    for (std::size_t index = 0; index < count; ++index) {
        const auto expert = scores[index].second;
        state.pending_predictions.push_back(expert);
        ++runtime.next_layer_prefetch_predictions;
        const auto key =
            (static_cast<std::uint64_t>(layer + 1) << 32) |
            static_cast<std::uint32_t>(expert);
        const auto already_resident = runtime.expert_residency.find(key);
        if (already_resident != runtime.expert_residency.end()) {
            if (policy.is_streamed_gpu())
                ++runtime.expert_slots[already_resident->second].prefetch_pins;
            continue;
        }
        // Faulting the mapping in is only worth it when the page-in still goes
        // through the host. Under direct paging the mmap is CUDA-registered, so
        // its pages are already pinned and resident, and this walk is 432 stray
        // reads per role per expert on the critical path for nothing.
        if (!runtime.layers[layer + 1].expert_dma) {
#if !defined(_WIN32)
        if (runtime.model->fd >= 0) {
            for (int role = 0; role < 3; ++role) {
                const auto& tensor =
                    runtime.model->tensors[next.expert_tensors[role]];
                const auto bytes = tensor.size / experts;
                const auto offset =
                    tensor.offset + static_cast<std::uint64_t>(expert) * bytes;
                (void)posix_fadvise(
                    runtime.model->fd, static_cast<off_t>(offset),
                    static_cast<off_t>(bytes), POSIX_FADV_WILLNEED
                );
                runtime.next_layer_prefetch_bytes += bytes;
            }
        }
#else
        for (int role = 0; role < 3; ++role) {
            const auto& tensor =
                runtime.model->tensors[next.expert_tensors[role]];
            const auto bytes = tensor.size / experts;
            const auto offset =
                tensor.offset + static_cast<std::uint64_t>(expert) * bytes;
            const auto* base = tensor_data(*runtime.model,tensor) +
                static_cast<std::uint64_t>(expert) * bytes;
            const std::size_t page_size = 4096;
            for (std::size_t pos = 0; pos < bytes; pos += page_size)
                (void)base[pos];
            runtime.next_layer_prefetch_bytes += bytes;
        }
#endif
        }
        if (policy.is_streamed_gpu() && runtime.layers[layer + 1].expert_dma &&
            !runtime.expert_slots.empty()) {
            const auto slot_index = select_expert_cache_slot(
                runtime, layer + 1, expert, true, false
            );
            if (slot_index == kNoExpertSlot) continue;
            auto& slot = runtime.expert_slots[slot_index];
            const auto slot_base =
                runtime.expert_cache + slot_index * runtime.expert_slot_bytes;
            std::uint64_t role_offset = 0;
            for (int role = 0; role < 3; ++role) {
                const auto& tensor =
                    runtime.model->tensors[next.expert_tensors[role]];
                const auto bytes = tensor.size / experts;
                const auto offset = static_cast<std::uint64_t>(expert) * bytes;
                if (flyweight_gpu_upload(
                        slot_base + role_offset,
                        tensor_data(*runtime.model,tensor) + offset,
                        bytes, runtime.prefetch_stream
                    ) != 0)
                    throw std::runtime_error(
                        "native Qwen GPU prefetch DMA upload failed"
                    );
                role_offset += bytes;
                runtime.next_layer_prefetch_bytes += bytes;
            }
            slot.key = key;
            slot.valid = true;
            slot.last_used = ++runtime.expert_clock;
            ++slot.prefetch_pins;
            runtime.expert_residency[key] = slot_index;
            queued_gpu_upload = true;
        }
    }
    if (queued_gpu_upload) {
        if (flyweight_gpu_event_record(
                runtime.prefetch_event, runtime.prefetch_stream
            ) != 0)
            throw std::runtime_error("native Qwen prefetch event record failed");
        runtime.prefetch_pending = true;
        runtime.prefetch_target_layer = layer + 1;
    }
}

QwenExpertHistory& record_expert_access(
    FlyweightV2QwenRuntime& runtime, std::uint32_t layer, std::uint32_t expert
) {
    const auto experts = runtime.model->config.expert_count;
    if (!runtime.cache_admission_enabled &&
        runtime.active_sequence < runtime.sequences.size()) {
        auto& sequence = runtime.sequences[runtime.active_sequence];
        const auto index = static_cast<std::size_t>(layer) * experts + expert;
        if (sequence.prompt_expert_frequency.size() ==
            static_cast<std::size_t>(runtime.layers.size()) * experts) {
            auto& frequency = sequence.prompt_expert_frequency[index];
            if (frequency != std::numeric_limits<std::uint32_t>::max())
                ++frequency;
            ++sequence.prompt_expert_observations;
        }
    }
    // Every route decision -- hit or miss, on every expert path -- passes through
    // here, so this is the one place a faithful trace can be tapped.
    if (expert_trace_file()) {
        const std::uint32_t record[2] = {layer, expert};
        std::fwrite(record, sizeof(record), 1, expert_trace_file());
    }
    auto& history = runtime.expert_history[
        static_cast<std::size_t>(layer) * experts + expert
    ];
    ++runtime.expert_clock;
    if (history.frequency != std::numeric_limits<std::uint32_t>::max()) {
        ++history.frequency;
    }
    history.last_used = runtime.expert_clock;
    if ((runtime.expert_clock & 32767U) == 0) {
        for (auto& item : runtime.expert_history) {
            if (item.frequency > 1) item.frequency = (item.frequency + 1) / 2;
        }
    }
    return history;
}

static void record_expert_cache_hit(
        FlyweightV2QwenRuntime& runtime, QwenExpertSlot& slot) {
    ++runtime.expert_cache_hits;
    slot.used_since_admission = true;
    if (runtime.cache_admission_enabled && slot.pinned) {
        ++runtime.prefill_cache_seed_hits;
        ++runtime.prefill_cache_seed_avoided_misses;
    }
}

std::size_t select_expert_cache_slot(
    FlyweightV2QwenRuntime& runtime, std::uint32_t layer,
    std::uint32_t expert, bool allow_rejection, bool record_access
) {
    if(runtime.expert_residency_frozen){
        if(record_access)record_expert_access(runtime,layer,expert);
        ++runtime.expert_cache_deferred_admissions;
        return kNoExpertSlot;
    }
    const auto policy=qwen_expert_policy(
        runtime,flyweight::v2::ExpertExecutionPhase::decode);
    if (!policy.misses_may_be_admitted()) {
        // Prompt prefill must not churn the device cache, but its routes are
        // valuable training data for a later bulk seed of the hottest experts.
        record_expert_access(runtime, layer, expert);
        ++runtime.expert_cache_prompt_bypasses;
        return kNoExpertSlot;
    }
    auto& candidate = runtime.expert_history[
        static_cast<std::size_t>(layer) *
            runtime.model->config.expert_count + expert
    ];
    if (record_access) record_expert_access(runtime, layer, expert);
    if(!runtime.whole_expert_layer_slots.empty()){
        if(layer>=runtime.whole_expert_layer_slots.size()||
           runtime.whole_expert_layer_slots[layer]<0){
            ++runtime.expert_cache_rejections;
            return kNoExpertSlot;
        }
        const auto slot=static_cast<std::size_t>(
            runtime.whole_expert_layer_slots[layer])+expert;
        if(slot>=runtime.expert_slots.size())
            throw std::runtime_error(
                "native Laguna whole-layer expert slot is out of range");
        return slot;
    }
    const auto cache_layers = qwen_cache_layer_count(runtime);
    const auto slot_begin = runtime.expert_slots.size() * layer / cache_layers;
    const auto slot_end = runtime.expert_slots.size() * (layer + 1) / cache_layers;
    auto begin = runtime.expert_slots.begin() + static_cast<std::ptrdiff_t>(slot_begin);
    auto end = runtime.expert_slots.begin() + static_cast<std::ptrdiff_t>(slot_end);
    if (begin == end) throw std::runtime_error("native Qwen layer has no expert cache slots");
    auto free_slot = std::find_if(begin, end, [](const QwenExpertSlot& slot) {
        return !slot.valid;
    });
    if (free_slot != end) {
        // No gate here: an empty slot displaces nothing, and refusing free
        // coverage is what made a threshold lose. See expert_admission_allowed.
        // The slot is about to hold a different expert, so any seed pin it
        // still carries describes the previous occupant.
        free_slot->pinned = false;
        free_slot->used_since_admission = false;
        ++runtime.expert_cache_admissions;
        return static_cast<std::size_t>(free_slot - runtime.expert_slots.begin());
    }
    auto victim = std::min_element(begin, end, [&](const QwenExpertSlot& left, const QwenExpertSlot& right) {
        const bool left_protected = left.pinned || left.prefetch_pins;
        const bool right_protected = right.pinned || right.prefetch_pins;
        if (left_protected != right_protected) return !left_protected;
        const auto left_expert = static_cast<std::uint32_t>(left.key);
        const auto right_expert = static_cast<std::uint32_t>(right.key);
        const auto& left_history = runtime.expert_history[static_cast<std::size_t>(layer) * runtime.model->config.expert_count + left_expert];
        const auto& right_history = runtime.expert_history[static_cast<std::size_t>(layer) * runtime.model->config.expert_count + right_expert];
        return left_history.frequency != right_history.frequency
            ? left_history.frequency < right_history.frequency
            : left.last_used < right.last_used;
    });
    // Only an in-flight prefetch is a hard block: its upload is still landing in
    // the slot. A seed pin is a placement preference, and the comparator above
    // already sorts pinned slots last, so a seeded expert is only ever the
    // victim once every unpinned slot in the layer is hotter than it. Treating
    // the seed pin as absolute left the cache unable to replace a single slot
    // after seeding -- evictions stayed at 0 for whole generations.
    if (victim->prefetch_pins) {
        ++runtime.expert_cache_rejections;
        return kNoExpertSlot;
    }
    const auto victim_expert = static_cast<std::uint32_t>(victim->key);
    const auto& victim_history = runtime.expert_history[
        static_cast<std::size_t>(layer) * runtime.model->config.expert_count + victim_expert
    ];
    // The legacy policy adapts quickly by admitting an equally frequent but
    // newer candidate. Strict admission avoids replacing a resident until the
    // candidate is demonstrably hotter.
    if (!flyweight::v2::expert_admission_allowed(
            candidate.frequency, victim_history.frequency, false,
            allow_rejection, runtime.strict_cache_admission)) {
        ++runtime.expert_cache_rejections;
        return kNoExpertSlot;
    }
    if (victim->valid && !victim->used_since_admission)
        ++runtime.expert_cache_unused_admissions;
    const auto slot = static_cast<std::size_t>(victim - runtime.expert_slots.begin());
    runtime.expert_residency.erase(victim->key);
    victim->native_valid = false;
    // The pin belonged to the expert being evicted, not to its replacement.
    // Leaving it set made every later occupant look seed-protected, which both
    // inflated the seed hit counters and flattened the eviction preference that
    // is supposed to keep the seeded set resident.
    victim->pinned = false;
    victim->used_since_admission = false;
    ++runtime.expert_cache_evictions;
    ++runtime.expert_cache_admissions;
    return slot;
}

std::uint64_t device_align(std::uint64_t bytes) {
    return flyweight::v2::workspace::align(bytes);
}

// Definition of the token-boundary promotion declared above.
void qwen_absorb_registration(FlyweightV2QwenRuntime& runtime) {
    if (runtime.registration_ready.load(std::memory_order_acquire) == 0) return;
    std::lock_guard<std::mutex> guard(runtime.registration_mutex);
    for (const auto layer : runtime.registration_done) {
        if (layer == std::numeric_limits<std::uint32_t>::max())
            runtime.embedding_dma = true;
        else if (layer < runtime.layers.size())
            runtime.layers[layer].expert_dma = true;
    }
    runtime.registration_done.clear();
    runtime.registration_ready.store(0, std::memory_order_release);
}

void release_qwen_device(FlyweightV2QwenRuntime& runtime) {
    // Stop the background registration before anything it writes to goes away.
    // It checks the flag between layers, so the wait is bounded by one layer's
    // registration (~0.4s) rather than by the whole checkpoint.
    if (runtime.registration_thread.joinable()) {
        runtime.registration_cancel.store(true, std::memory_order_release);
        runtime.registration_thread.join();
    }
    qwen_save_expert_history(runtime);
    if (runtime.stream) flyweight_gpu_stream_sync(runtime.stream);
    // An enqueued expert prefetch DMAs from the model mmap on its own stream,
    // and the main stream only waits for it at the consumption point -- which
    // may never come before teardown. Unregistering the mmap under an
    // in-flight prefetch is a use-after-unregister, so drain it here.
    if (runtime.prefetch_stream) flyweight_gpu_stream_sync(runtime.prefetch_stream);
    if (runtime.model_registered && runtime.model) {
        // Unregister exactly the ranges that were registered -- these are expert
        // tensor spans, not whole mappings, so the mapping list is not the
        // inverse of what prepare did.
        for (const auto& [base, bytes] : runtime.registered_ranges) {
            (void)bytes;
            flyweight_gpu_host_unregister(base);
        }
        runtime.registered_ranges.clear();
        runtime.model_registered = false;
        runtime.dma_paging = false;
    }
    flyweight_gpu_host_free(runtime.host_staging);
    flyweight_gpu_host_free(runtime.dense_host);
    runtime.dense_host = nullptr;
    runtime.dense_host_bytes = 0;
    runtime.host_ffn_stage = 0;
    runtime.host_ffn_stage_bytes = 0;
    runtime.host_ffn_q8.clear();
    runtime.host_ffn_q8_bytes = 0;
    if (runtime.embedding_event) flyweight_gpu_event_destroy(runtime.embedding_event);
    flyweight_gpu_host_free(runtime.embedding_host);
    flyweight_gpu_free(runtime.turbo_kv_stage);
    runtime.turbo_kv_stage = 0;
    runtime.turbo_kv_stage_bytes = 0;
    flyweight_gpu_free(runtime.embedding_stage);
    flyweight_gpu_free(runtime.embedding_row_index);
    runtime.embedding_event = 0;
    runtime.embedding_host = nullptr;
    runtime.embedding_stage = 0;
    runtime.embedding_stage_bytes = 0;
    runtime.embedding_row_index = 0;
    runtime.embeddings_host_resident = false;
    // The active slot's checkpoint pool is mirrored in runtime.prefill_snapshots;
    // inactive slots keep theirs in sequences[i]. Each buffer lives in exactly
    // one of the two, so freeing both frees every buffer once.
    for (auto& snapshot : runtime.prefill_snapshots) {
        flyweight_gpu_free(snapshot.device);
        snapshot = QwenPrefillSnapshot{};
    }
    runtime.prefill_snapshot_bytes = 0;
    flyweight_gpu_free(runtime.expert_cache);
    flyweight_gpu_free(runtime.expert_native_cache);
    flyweight_gpu_free(runtime.expert_staging);
    flyweight_gpu_free(runtime.prefill_stream_arena);
    runtime.prefill_stream_arena = 0;
    runtime.prefill_stream_bytes = 0;
    flyweight_gpu_free(runtime.prefill_stream_scratch);
    runtime.prefill_stream_scratch = 0;
    runtime.prefill_stream_scratch_bytes = 0;
    runtime.prefill_stream_scratch_span = 0;
    flyweight_gpu_free(runtime.imatrix_sums);
    runtime.imatrix_sums = 0;
    runtime.imatrix_elements = 0;
    runtime.imatrix_offsets.clear();
    runtime.imatrix_host.clear();
    runtime.imatrix_rows.clear();
    // runtime.state aliases sequences[active].state; free each slot's arena +
    // its checkpoint pool once.
    for (auto& seq : runtime.sequences) {
        for (auto& snapshot : seq.prefill_snapshots) flyweight_gpu_free(snapshot.device);
        flyweight_gpu_free(seq.state);
        seq.state = 0;
    }
    runtime.sequences.clear();
    runtime.active_sequence = 0;
    for (auto& e : runtime.host_prompts) {
        std::free(e.state);
        for (auto& snapshot : e.snapshots) std::free(snapshot.state);
    }
    runtime.host_prompts.clear();
    runtime.host_cache_used_bytes = 0;
    for (auto& layer : runtime.layers) {
        flyweight_gpu_graph_destroy(layer.shared_graph);
        layer.shared_graph = 0;
        layer.shared_graph_attempted = false;
        for (auto& graph : layer.delta_graphs) flyweight_gpu_graph_destroy(graph);
        layer.delta_graphs.clear();
        layer.delta_graph_attempted.clear();
        flyweight_gpu_graph_destroy(layer.dense_ffn_graph);
        layer.dense_ffn_graph = 0;
        layer.dense_ffn_graph_attempted = false;
        flyweight_gpu_graph_destroy(layer.gemma_ffn_graph);
        layer.gemma_ffn_graph = 0;
        layer.gemma_ffn_graph_attempted = false;
    }
    flyweight_gpu_free(runtime.workspace);
    flyweight_gpu_free(runtime.static_arena);
    for (auto& event : runtime.slot_events) flyweight_gpu_event_destroy(event);
    runtime.slot_events.clear();
    flyweight_gpu_event_destroy(runtime.staging_event);
    runtime.staging_event = 0;
    flyweight_gpu_event_destroy(runtime.prefetch_event);
    runtime.prefetch_event = 0;
    flyweight_gpu_event_destroy(runtime.route_event);
    flyweight_gpu_event_destroy(runtime.route_event_pipeline);
    flyweight_gpu_event_destroy(runtime.stream_upload_event);
    flyweight_gpu_event_destroy(runtime.stream_upload_event_pipeline);
    flyweight_gpu_event_destroy(runtime.stream_consumed_event);
    flyweight_gpu_event_destroy(runtime.stream_consumed_event_pipeline);
    runtime.stream_upload_event=runtime.stream_upload_event_pipeline=0;
    runtime.stream_consumed_event=runtime.stream_consumed_event_pipeline=0;
    flyweight_gpu_event_destroy(runtime.prefill_layer_start_event);
    flyweight_gpu_event_destroy(runtime.prefill_core_end_event);
    flyweight_gpu_event_destroy(runtime.prefill_router_end_event);
    for(auto&profile:runtime.cuda_layer_profiles){
        flyweight_gpu_event_destroy(profile.pre_start);
        flyweight_gpu_event_destroy(profile.pre_end);
        flyweight_gpu_event_destroy(profile.recurrent_start);
        flyweight_gpu_event_destroy(profile.recurrent_end);
        flyweight_gpu_event_destroy(profile.shared_start);
        flyweight_gpu_event_destroy(profile.shared_end);
        flyweight_gpu_event_destroy(profile.expert_start);
        flyweight_gpu_event_destroy(profile.expert_end);
    }
    runtime.cuda_layer_profiles.clear();
    flyweight_gpu_event_destroy(runtime.cuda_tail_start);
    flyweight_gpu_event_destroy(runtime.cuda_lm_start);
    flyweight_gpu_event_destroy(runtime.cuda_lm_end);
    flyweight_gpu_event_destroy(runtime.cuda_tail_end);
    runtime.cuda_tail_start=runtime.cuda_lm_start=0;
    runtime.cuda_lm_end=runtime.cuda_tail_end=0;
    runtime.cuda_profile=false;
    runtime.cuda_graphs=false;
    runtime.cuda_graph_builds=runtime.cuda_graph_replays=runtime.cuda_graph_fallbacks=0;
    runtime.delta_host_ns=runtime.delta_host_calls=0;
    flyweight_gpu_stream_destroy(runtime.prefetch_stream);
    runtime.prefetch_stream = 0;
    flyweight_gpu_stream_destroy(runtime.graph_stream);
    runtime.graph_stream = 0;
    flyweight_gpu_stream_destroy(runtime.stream);
    runtime.device_tensors.clear();
    runtime.host_staging = nullptr;
    runtime.expert_staging = runtime.state = runtime.workspace = 0;
    runtime.expert_cache = 0;
    runtime.expert_native_cache = 0;
    runtime.static_arena = runtime.stream = 0;
    runtime.route_event = 0;
    runtime.route_event_pipeline = 0;
    runtime.prefill_layer_start_event=runtime.prefill_core_end_event=
        runtime.prefill_router_end_event=0;
    runtime.prefill_profile=false;
    runtime.static_arena_bytes = runtime.workspace_bytes = 0;
    runtime.decode_workspace_layout = {};
    runtime.rows_workspace_layout = {};
    runtime.decode_host_layout = {};
    runtime.rows_host_layout = {};
    runtime.state_bytes = runtime.expert_staging_bytes = 0;
    runtime.host_staging_bytes = 0;
    runtime.forward_rows_capacity = 0;
    runtime.expert_cache_bytes = runtime.expert_native_cache_bytes =
        runtime.expert_slot_bytes = 0;
    runtime.expert_slots.clear();
    runtime.whole_expert_layer_slots.clear();
    runtime.expert_history.clear();
    runtime.expert_residency.clear();
    runtime.decode_ready = false;
}

uint64_t align_to(uint64_t n, uint32_t a) { return (n + a - 1) / a * a; }

std::uint64_t available_host_memory() {
#if defined(_WIN32)
    MEMORYSTATUSEX status{};
    status.dwLength=sizeof(status);
    return GlobalMemoryStatusEx(&status)?status.ullAvailPhys:0;
#elif defined(__linux__)
    // MemAvailable includes reclaimable page cache. That matters for mmap'd
    // GGUFs: after one pass most model bytes are cached, while AVPHYS counts
    // only free pages and would incorrectly disable direct paging.
    std::ifstream memory("/proc/meminfo");
    std::string key,unit;
    std::uint64_t kib=0;
    while(memory>>key>>kib>>unit)
        if(key=="MemAvailable:")return kib*1024;
    return 0;
#elif defined(_SC_AVPHYS_PAGES) && defined(_SC_PAGESIZE)
    const auto pages=sysconf(_SC_AVPHYS_PAGES),page_size=sysconf(_SC_PAGESIZE);
    return pages>0&&page_size>0?static_cast<std::uint64_t>(pages)*page_size:0;
#else
    return 0;
#endif
}
void copy_text(char* dst, size_t cap, const std::string& value) { if (!cap) return; std::strncpy(dst, value.c_str(), cap-1); dst[cap-1]=0; }

// Qwen3-Next GGUF files include the optional MTP draft block in ``block_count``.
// A block carrying ``nextn`` tensors is not part of the causal decoder stack and
// must not be executed before the final norm. Split checkpoints only see their
// full tensor list once the shards are merged, so this runs again after the
// merge rather than only at the end of the shard's own parse.
void detect_mtp_layer(FlyweightV2Model& m) {
    for (const auto& tensor : m.tensors) {
        const auto marker = tensor.name.find(".nextn.");
        if (marker == std::string::npos || tensor.name.rfind("blk.", 0) != 0) continue;
        try {
            const auto draft_layer = static_cast<uint32_t>(std::stoul(tensor.name.substr(4, marker - 4)));
            if (m.mtp_layer != std::numeric_limits<uint32_t>::max() &&
                m.mtp_layer != draft_layer) {
                throw std::runtime_error("multiple Qwen MTP draft layers are unsupported");
            }
            m.mtp_layer = draft_layer;
            if (draft_layer < m.config.layer_count) m.config.layer_count = draft_layer;
        } catch (const std::exception&) {
            throw std::runtime_error("invalid Qwen MTP tensor layer index");
        }
    }
}

// Tokenizer lookup tables, built once per model: rebuilding these ~150k-entry
// maps per tokenize call dominated short calls. Shared by the GGUF metadata
// path and the HF tokenizer.json path so the two cannot drift.
void build_tokenizer_tables(FlyweightV2Model& m) {
    m.merge_ranks.clear();
    m.vocabulary_ids.clear();
    m.control_tokens.clear();
    for(int rank=0;rank<static_cast<int>(m.merges.size());rank++)m.merge_ranks[m.merges[rank]]=rank;
    for(uint32_t id=0;id<static_cast<uint32_t>(m.vocabulary.size());id++)m.vocabulary_ids.emplace(m.vocabulary[id],id);
    // Longest first, so a control token that prefixes another still matches the
    // longer one when scanning input left to right.
    for(uint32_t id=0;id<static_cast<uint32_t>(m.vocabulary.size());id++){
        if(id>=m.token_types.size()||m.token_types[id]!=3)continue;
        if(m.vocabulary[id].empty())continue;
        m.control_tokens.emplace_back(m.vocabulary[id],id);
    }
    std::sort(m.control_tokens.begin(),m.control_tokens.end(),
        [](const auto& left,const auto& right){
            return left.first.size()>right.first.size();
        });
}

int parse(FlyweightV2Model& m) {
    if (m.size < 24 || std::memcmp(m.data,"GGUF",4)!=0) throw std::runtime_error("not a GGUF file");
    Reader r{m.data+4,m.data+m.size}; m.version=r.get<uint32_t>(); if (m.version<2 || m.version>3) throw std::runtime_error("unsupported GGUF version");
    uint64_t count=r.get<uint64_t>(); m.metadata=r.get<uint64_t>();
    auto read_uint = [&](uint32_t type)->uint64_t { if(type==4)return r.get<uint32_t>(); if(type==10)return r.get<uint64_t>(); throw std::runtime_error("GGUF architecture value is not an integer"); };
    auto is_integer = [](uint32_t type){ return type==0||type==1||type==2||type==3||type==4||type==5||type==10||type==11; };
    auto read_any_uint = [&](uint32_t type)->uint64_t {
        std::int64_t value=0;
        switch(type){
        case 0: value=r.get<std::uint8_t>(); break;
        case 1: value=r.get<std::int8_t>(); break;
        case 2: value=r.get<std::uint16_t>(); break;
        case 3: value=r.get<std::int16_t>(); break;
        case 4: value=r.get<std::uint32_t>(); break;
        case 5: value=r.get<std::int32_t>(); break;
        case 10: return r.get<std::uint64_t>();
        case 11: value=r.get<std::int64_t>(); break;
        default: throw std::runtime_error("GGUF value is not an integer");
        }
        if(value<0)throw std::runtime_error("GGUF integer value is negative");
        return static_cast<std::uint64_t>(value);
    };
    auto read_float = [&](uint32_t type)->float { if(type==6)return r.get<float>(); if(type==12)return static_cast<float>(r.get<double>()); throw std::runtime_error("GGUF architecture value is not a float"); };
    auto read_uint_array = [&](uint32_t type)->std::vector<std::uint32_t> {
        if(type!=9)throw std::runtime_error("GGUF architecture value is not an array");
        const auto element_type=r.get<uint32_t>();const auto count=r.get<uint64_t>();
        if(element_type!=4&&element_type!=5&&element_type!=10&&element_type!=11)
            throw std::runtime_error("GGUF architecture array is not integer");
        std::vector<std::uint32_t> values;values.reserve(static_cast<std::size_t>(count));
        for(std::uint64_t i=0;i<count;++i){
            std::int64_t value=0;
            if(element_type==4)value=r.get<std::uint32_t>();
            else if(element_type==5)value=r.get<std::int32_t>();
            else if(element_type==10)value=static_cast<std::int64_t>(r.get<std::uint64_t>());
            else value=r.get<std::int64_t>();
            if(value<0||static_cast<std::uint64_t>(value)>std::numeric_limits<std::uint32_t>::max())
                throw std::runtime_error("GGUF architecture array value is out of range");
            values.push_back(static_cast<std::uint32_t>(value));
        }
        return values;
    };
    auto read_u64_array = [&](uint32_t type)->std::vector<std::uint64_t> {
        // qwen4exp's PLE hash constants genuinely occupy the u64 range
        // (multipliers approach 2^63/vocab), so they cannot go through
        // read_uint_array's u32 clamp.
        if(type!=9)throw std::runtime_error("GGUF architecture value is not an array");
        const auto element_type=r.get<uint32_t>();const auto count=r.get<uint64_t>();
        if(element_type!=10&&element_type!=11&&element_type!=4&&element_type!=5)
            throw std::runtime_error("GGUF architecture array is not integer");
        std::vector<std::uint64_t> values;values.reserve(static_cast<std::size_t>(count));
        for(std::uint64_t i=0;i<count;++i){
            if(element_type==10)values.push_back(r.get<std::uint64_t>());
            else if(element_type==4)values.push_back(r.get<std::uint32_t>());
            else {
                const auto value=element_type==11
                    ?r.get<std::int64_t>()
                    :static_cast<std::int64_t>(r.get<std::int32_t>());
                if(value<0)throw std::runtime_error(
                    "GGUF architecture array value is negative");
                values.push_back(static_cast<std::uint64_t>(value));
            }
        }
        return values;
    };
    auto read_float_array = [&](uint32_t type)->std::vector<float> {
        if(type!=9)throw std::runtime_error("GGUF architecture value is not an array");
        const auto element_type=r.get<uint32_t>();const auto count=r.get<uint64_t>();
        if(element_type!=6&&element_type!=12)
            throw std::runtime_error("GGUF architecture array is not float");
        std::vector<float> values;values.reserve(static_cast<std::size_t>(count));
        for(std::uint64_t i=0;i<count;++i)
            values.push_back(element_type==6?r.get<float>():static_cast<float>(r.get<double>()));
        return values;
    };
    auto set_config = [&](const std::string& key, uint32_t type)->bool {
        if(type!=4 && type!=10)return false;
        auto suffix=[&](const char* text){size_t n=std::strlen(text);return key.size()>=n && key.compare(key.size()-n,n,text)==0;};
        uint32_t* target=nullptr;
        // DeepSeek-V4 scalars first: several of them end in strings that would
        // otherwise be caught by the shorter generic suffixes below.
        if(suffix(".attention.q_lora_rank"))target=&m.config.q_lora_rank;
        else if(suffix(".attention.kv_lora_rank"))target=&m.config.kv_lora_rank;
        // BailingMoE3: the per-head MLA widths, the declared KDA head width
        // (which is NOT hidden/heads on this family), and group-limited
        // routing. These must precede the generic suffixes below -- and note
        // `.expert_group_used_count` must be tested before `.expert_used_count`
        // could ever swallow it.
        else if(suffix(".attention.key_length_mla"))target=&m.config.key_length_mla;
        else if(suffix(".attention.value_length_mla"))target=&m.config.value_length_mla;
        else if(suffix(".kda.head_dim"))target=&m.config.attention_head_dim;
        else if(suffix(".ssm.conv_kernel"))target=&m.config.conv_kernel;
        else if(suffix(".expert_group_used_count"))target=&m.config.expert_group_used;
        else if(suffix(".expert_group_count"))target=&m.config.expert_group_count;
        else if(suffix(".attention.output_lora_rank"))target=&m.config.output_lora_rank;
        else if(suffix(".attention.output_group_count"))target=&m.config.output_group_count;
        else if(suffix(".attention.indexer.head_count"))target=&m.config.indexer_head_count;
        else if(suffix(".attention.indexer.key_length"))target=&m.config.indexer_key_length;
        else if(suffix(".attention.indexer.top_k"))target=&m.config.indexer_top_k;
        else if(suffix(".hyper_connection.sinkhorn_iterations"))target=&m.config.sinkhorn_iterations;
        else if(suffix(".hyper_connection.low_rank"))target=&m.config.hyper_connection_low_rank;
        else if(suffix(".hyper_connection.count"))target=&m.config.hyper_connection_count;
        // qwen4exp PLE scalars. `.ple.eos_token_id` must precede the generic
        // tokenizer keys only in spirit -- nothing else ends in these strings.
        else if(suffix(".ple.ngram_size"))target=&m.config.ple_ngram_size;
        else if(suffix(".ple.heads_per_ngram"))target=&m.config.ple_heads_per_ngram;
        else if(suffix(".ple.conv_kernel"))target=&m.config.ple_conv_kernel;
        else if(suffix(".ple.eos_token_id"))target=&m.config.ple_eos_token_id;
        else if(suffix(".expert_shared_count"))target=&m.config.expert_shared_count;
        else if(suffix(".hash_layer_count"))target=&m.config.hash_layer_count;
        else if(suffix(".block_size"))target=&m.config.draft_block_size;
        else if(suffix(".embedding_length"))target=&m.config.hidden_size;
        else if(suffix(".embedding_length_per_layer_input"))target=&m.config.per_layer_embedding_size;
        else if(suffix(".block_count"))target=&m.config.layer_count;
        else if(suffix(".attention.shared_kv_layers"))target=&m.config.shared_kv_layers;
        else if(suffix(".attention.head_count_kv"))target=&m.config.attention_kv_heads;
        else if(suffix(".attention.head_count"))target=&m.config.attention_heads;
        else if(suffix(".context_length"))target=&m.config.context_length;
        else if(suffix(".expert_shared_feed_forward_length"))target=&m.config.expert_shared_intermediate_size;
        else if(suffix(".expert_feed_forward_length"))target=&m.config.expert_intermediate_size;
        else if(suffix(".feed_forward_length"))target=&m.config.dense_intermediate_size;
        else if(suffix(".expert_count"))target=&m.config.expert_count;
        else if(suffix(".expert_used_count"))target=&m.config.expert_used_count;
        else if(suffix(".leading_dense_block_count"))target=&m.config.leading_dense_block_count;
        else if(suffix(".expert_gating_func"))target=&m.config.expert_gating_func;
        else if(suffix(".rope.scaling.original_context_length"))target=&m.config.rope_original_context_length;
        else if(key=="tokenizer.ggml.vocab_size")target=&m.config.vocabulary_size;
        if(!target)return false;
        *target=static_cast<uint32_t>(read_uint(type));
        return true;
    };
    for(uint64_t i=0;i<m.metadata;i++) { std::string key=r.str(); uint32_t type=r.get<uint32_t>();
        if (key=="general.alignment" && type==4) m.alignment=r.get<uint32_t>();
        else if (key=="general.architecture" && type==8) {m.architecture=r.str();m.config.architecture=m.architecture;}
        else if (key=="general.name" && type==8) m.name=r.str();
        // gguf-split writes the shard index and count as uint16 and the tensor
        // total as int32, so these do not go through read_uint.
        else if (key=="split.no" && is_integer(type)) m.split_no=static_cast<uint32_t>(read_any_uint(type));
        else if (key=="split.count" && is_integer(type)) m.split_count=static_cast<uint32_t>(read_any_uint(type));
        else if (key=="split.tensors.count" && is_integer(type)) m.split_tensors=read_any_uint(type);
        else if (key=="tokenizer.chat_template" && type==8) m.chat_template=r.str();
        else if (key=="tokenizer.ggml.tokens" && type==9) {uint32_t element_type=r.get<uint32_t>();uint64_t count_tokens=r.get<uint64_t>();m.config.vocabulary_size=static_cast<uint32_t>(count_tokens);m.vocabulary.reserve(static_cast<size_t>(count_tokens));for(uint64_t token_index=0;token_index<count_tokens;token_index++){if(element_type==8)m.vocabulary.push_back(r.str());else r.value(element_type);}}
        else if (key=="tokenizer.ggml.pre" && type==8) m.tokenizer_pre=r.str();
        else if (key=="tokenizer.ggml.eos_token_id" && (type==4||type==10)) m.config.eos_token_id=static_cast<uint32_t>(read_uint(type));
        else if (key=="tokenizer.ggml.eot_token_id" && (type==4||type==10)) m.config.eot_token_id=static_cast<uint32_t>(read_uint(type));
        else if (key=="tokenizer.ggml.bos_token_id" && (type==4||type==10)) m.config.bos_token_id=static_cast<uint32_t>(read_uint(type));
        else if (key=="tokenizer.ggml.mask_token_id" && is_integer(type)) m.config.mask_token_id=static_cast<uint32_t>(read_any_uint(type));
        else if (key=="tokenizer.ggml.token_type" && type==9) m.token_types=read_uint_array(type);
        else if (key=="tokenizer.ggml.merges" && type==9) {uint32_t element_type=r.get<uint32_t>();uint64_t count_merges=r.get<uint64_t>();m.merges.reserve(static_cast<size_t>(count_merges));for(uint64_t merge_index=0;merge_index<count_merges;merge_index++){if(element_type==8)m.merges.push_back(r.str());else r.value(element_type);}}
        else if (key.size()>=21 && key.compare(key.size()-21,21,".rope.dimension_count")==0 && (type==4 || type==10)) m.config.rotary_dimension=static_cast<uint32_t>(read_uint(type));
        else if (key.size()>=24 && key.compare(key.size()-24,24,".full_attention_interval")==0 && (type==4 || type==10)) m.config.full_attention_interval=static_cast<uint32_t>(read_uint(type));
        else if (key.size()>=24 && key.compare(key.size()-24,24,".attention.head_count_kv")==0 && type==9) m.config.attention_kv_heads_by_layer=read_uint_array(type);
        else if (key.size()>=21 && key.compare(key.size()-21,21,".attention.head_count")==0 && type==9) m.config.attention_heads_by_layer=read_uint_array(type);
        else if (key.size()>=25 && key.compare(key.size()-25,25,".attention.sliding_window")==0 && (type==4 || type==10)) m.config.sliding_window=static_cast<uint32_t>(read_uint(type));
        else if (key.size()>=33 && key.compare(key.size()-33,33,".attention.sliding_window_pattern")==0 && type==9) {
            const auto element_type=r.get<uint32_t>();
            const auto elements=r.get<uint64_t>();
            if(element_type!=7)throw std::runtime_error("GGUF sliding-window pattern must be boolean");
            m.config.sliding_window_pattern.reserve(static_cast<std::size_t>(elements));
            for(std::uint64_t element=0;element<elements;++element)m.config.sliding_window_pattern.push_back(r.get<std::uint8_t>()?1:0);
        }
        // Muse Glimmer writes the same key as a scalar period instead of a
        // per-layer array. The layout is expanded once the block count is
        // known, which is not guaranteed to have been read yet.
        else if (key.size()>=33 && key.compare(key.size()-33,33,".attention.sliding_window_pattern")==0 && (type==4 || type==10)) m.config.sliding_window_period=static_cast<uint32_t>(read_uint(type));
        else if (key.size()>=12 && key.compare(key.size()-12,12,".logit_scale")==0 && (type==6 || type==12)) m.config.logit_scale=read_float(type);
        else if (key.size()>=33 && key.compare(key.size()-33,33,".attention.layer_norm_rms_epsilon")==0 && (type==6 || type==12)) m.config.rms_norm_epsilon=read_float(type);
        else if (key.size()>=21 && key.compare(key.size()-21,21,".attention.key_length")==0 && (type==4 || type==10)) m.config.key_length=static_cast<uint32_t>(read_uint(type));
        else if (key.size()>=23 && key.compare(key.size()-23,23,".attention.value_length")==0 && (type==4 || type==10)) m.config.value_length=static_cast<uint32_t>(read_uint(type));
        else if (key.size()>=25 && key.compare(key.size()-25,25,".attention.key_length_swa")==0 && (type==4 || type==10)) m.config.key_length_swa=static_cast<uint32_t>(read_uint(type));
        else if (key.size()>=27 && key.compare(key.size()-27,27,".attention.value_length_swa")==0 && (type==4 || type==10)) m.config.value_length_swa=static_cast<uint32_t>(read_uint(type));
        else if (key.size()>=25 && key.compare(key.size()-25,25,".rope.dimension_count_swa")==0 && (type==4 || type==10)) m.config.rotary_dimension_swa=static_cast<uint32_t>(read_uint(type));
        else if (key.size()>=19 && key.compare(key.size()-19,19,".rope.freq_base_swa")==0 && (type==6 || type==12)) m.config.rope_freq_base_swa=read_float(type);
        else if (key.size()>=24 && key.compare(key.size()-24,24,".final_logit_softcapping")==0 && (type==6 || type==12)) m.config.final_logit_softcap=read_float(type);
        else if (key.size()>=15 && key.compare(key.size()-15,15,".rope.freq_base")==0 && (type==6 || type==12)) m.config.rope_freq_base=read_float(type);
        else if (key.size()>=20 && key.compare(key.size()-20,20,".rope.scaling.factor")==0 && (type==6 || type==12)) m.config.rope_scaling_factor=read_float(type);
        else if (key.size()>=30 && key.compare(key.size()-30,30,".rope.scaling.yarn_attn_factor")==0 && (type==6 || type==12)) m.config.yarn_attn_factor=read_float(type);
        else if (key.size()>=28 && key.compare(key.size()-28,28,".rope.scaling.yarn_beta_fast")==0 && (type==6 || type==12)) m.config.yarn_beta_fast=read_float(type);
        else if (key.size()>=28 && key.compare(key.size()-28,28,".rope.scaling.yarn_beta_slow")==0 && (type==6 || type==12)) m.config.yarn_beta_slow=read_float(type);
        else if (key.size()>=18 && key.compare(key.size()-18,18,".rope.scaling.type")==0 && type==8) m.config.rope_scaling_yarn=r.str()=="yarn";
        else if (key.size()>=21 && key.compare(key.size()-21,21,".expert_weights_scale")==0 && (type==6 || type==12)) m.config.expert_weights_scale=read_float(type);
        else if (key.size()>=20 && key.compare(key.size()-20,20,".expert_weights_norm")==0 && type==7) m.config.expert_weights_norm=r.get<std::uint8_t>()!=0;
        else if (key.size()>=26 && key.compare(key.size()-26,26,".attention.compress_ratios")==0 && type==9) m.config.compress_ratios=read_uint_array(type);
        else if (key.size()>=14 && key.compare(key.size()-14,14,".target_layers")==0 && type==9) m.config.target_layers=read_uint_array(type);
        // qwen4exp PLE arrays. `.ple.layers` cannot swallow the longer keys:
        // suffix matching compares whole tails and none of them end in it.
        else if (key.size()>=11 && key.compare(key.size()-11,11,".ple.layers")==0 && type==9) m.config.ple_layers=read_uint_array(type);
        else if (key.size()>=22 && key.compare(key.size()-22,22,".ple.layer_multipliers")==0 && type==9) m.config.ple_multipliers=read_u64_array(type);
        else if (key.size()>=17 && key.compare(key.size()-17,17,".ple.head_offsets")==0 && type==9) m.config.ple_head_offsets=read_u64_array(type);
        else if (key.size()>=21 && key.compare(key.size()-21,21,".ple.head_vocab_sizes")==0 && type==9) m.config.ple_head_vocab_sizes=read_u64_array(type);
        else if (key.size()>=34 && key.compare(key.size()-34,34,".attention.compress_rope_freq_base")==0 && (type==6 || type==12)) m.config.compress_rope_freq_base=read_float(type);
        else if (key.size()>=25 && key.compare(key.size()-25,25,".hyper_connection.epsilon")==0 && (type==6 || type==12)) m.config.sinkhorn_epsilon=read_float(type);
        else if (key.size()>=19 && key.compare(key.size()-19,19,".swiglu_clamp_shexp")==0 && type==9) m.config.swiglu_clamp_shexp=read_float_array(type);
        else if (key.size()>=17 && key.compare(key.size()-17,17,".swiglu_clamp_exp")==0 && type==9) m.config.swiglu_clamp_exp=read_float_array(type);
        else if (set_config(key,type)) {}
        else r.value(type);
    }
    m.tensors.reserve(static_cast<size_t>(count));
    for(uint64_t i=0;i<count;i++) { Tensor t; t.name=r.str(); auto dims=r.get<uint32_t>(); if(dims>4) throw std::runtime_error("GGUF rank exceeds v2 ABI"); for(uint32_t d=0;d<dims;d++) t.shape.push_back(r.get<uint64_t>()); t.type=r.get<uint32_t>(); t.offset=r.get<uint64_t>(); m.tensors.push_back(std::move(t)); }
    detect_mtp_layer(m);
    // An array-valued head count leaves the scalar unset. Workspaces and score
    // buffers are sized off the scalar, so it has to cover the widest layer.
    if(!m.config.attention_heads&&!m.config.attention_heads_by_layer.empty())
        m.config.attention_heads=*std::max_element(
            m.config.attention_heads_by_layer.begin(),
            m.config.attention_heads_by_layer.end());
    // Laguna does not ship a sliding-window pattern: the layout is implied by
    // the architecture as a period-4 cycle that starts with a full-attention
    // layer (12 full, 36 sliding for the 48-block S checkpoint).  The per-layer
    // head-count array is the independent witness of that layout, so cross-check
    // against it rather than trusting the period blindly.
    if(m.architecture=="laguna"&&m.config.sliding_window&&
       m.config.sliding_window_pattern.empty()&&m.config.layer_count){
        m.config.sliding_window_pattern.assign(m.config.layer_count,0);
        for(std::uint32_t layer=0;layer<m.config.layer_count;++layer)
            m.config.sliding_window_pattern[layer]=layer%4?1:0;
        const auto& heads=m.config.attention_heads_by_layer;
        if(heads.size()>=m.config.layer_count)
            for(std::uint32_t layer=0;layer<m.config.layer_count;++layer)
                if((heads[layer]==heads[0])!=(m.config.sliding_window_pattern[layer]==0))
                    throw std::runtime_error(
                        "Laguna sliding-window layout disagrees with the per-layer head count");
    }
    // A scalar sliding-window period expands to the per-layer layout the rest
    // of the runtime reads. The cycle ends on the full-attention layer, so for
    // period 4 layers 3, 7, 11 ... are full and the other three are sliding.
    if(m.config.sliding_window_period&&m.config.sliding_window_pattern.empty()&&
       m.config.layer_count){
        const auto period=m.config.sliding_window_period;
        m.config.sliding_window_pattern.assign(m.config.layer_count,0);
        for(std::uint32_t layer=0;layer<m.config.layer_count;++layer)
            m.config.sliding_window_pattern[layer]=layer%period+1<period?1:0;
    }
    // DeepSeek-V4 ships no explicit kv_lora_rank: `attention.key_length` is the
    // width of the compressed KV latent, and the decoupled RoPE half is carried
    // separately in `rope.dimension_count`. The shared expert likewise arrives
    // as a count of routed-expert widths rather than a width.
    if(m.architecture=="deepseek4"||m.architecture=="dflash"){
        if(!m.config.kv_lora_rank)m.config.kv_lora_rank=m.config.key_length;
        if(!m.config.expert_shared_intermediate_size&&m.config.expert_shared_count)
            m.config.expert_shared_intermediate_size=
                m.config.expert_intermediate_size*m.config.expert_shared_count;
        if(!m.config.compress_ratios.empty()&&
           m.config.compress_ratios.size()<m.config.layer_count)
            throw std::runtime_error(
                "deepseek4 compress-ratio array is shorter than the model layer count");
    }
    // BailingMoE3 GGUFs carry no full_attention_interval key: the cadence is
    // written as the per-layer KV-head array, heads on the MLA layers and 0 on
    // the KDA layers. The interval is the index+1 of the first full layer, and
    // the whole array is checked against layer_is_full_attention rather than
    // trusting one entry -- a cadence the runtime mis-derives produces fluent
    // wrong text, not an error.
    if(m.architecture=="bailingmoe3"){
        // `attention.key_length` in these GGUFs is the MQA cache-row width
        // (kv_lora + rope); the geometry wants the per-head widths.
        if(m.config.key_length_mla)m.config.key_length=m.config.key_length_mla;
        if(m.config.value_length_mla)m.config.value_length=m.config.value_length_mla;
        const auto& kv_heads=m.config.attention_kv_heads_by_layer;
        if(!m.config.full_attention_interval&&!kv_heads.empty()){
            std::uint32_t interval=0;
            for(std::size_t layer=0;layer<kv_heads.size();++layer)
                if(kv_heads[layer]){interval=static_cast<std::uint32_t>(layer)+1;break;}
            if(!interval)
                throw std::runtime_error(
                    "bailingmoe3 GGUF declares no full-attention layer in "
                    "attention.head_count_kv");
            for(std::size_t layer=0;layer<kv_heads.size();++layer){
                const bool full=flyweight::v2::bailing::layer_is_full_attention(
                    static_cast<std::uint32_t>(layer),
                    static_cast<std::uint32_t>(kv_heads.size()),interval);
                if((kv_heads[layer]!=0)!=full)
                    throw std::runtime_error(
                        "bailingmoe3 attention.head_count_kv disagrees with a "
                        "period-"+std::to_string(interval)+" cadence at layer "+
                        std::to_string(layer));
            }
            m.config.full_attention_interval=interval;
        }
        // Mirror the HF loader: the pattern marks the linear (KDA) layers.
        if(m.config.full_attention_interval&&
           m.config.sliding_window_pattern.empty()&&m.config.layer_count){
            m.config.sliding_window_pattern.assign(m.config.layer_count,0);
            for(std::uint32_t layer=0;layer<m.config.layer_count;++layer)
                m.config.sliding_window_pattern[layer]=
                    flyweight::v2::bailing::layer_is_full_attention(
                        layer,m.config.layer_count,
                        m.config.full_attention_interval)?0:1;
        }
    }
    // qwen4exp: the novel blocks are driven entirely by metadata, and a
    // missing array degrades to fluent-wrong output rather than an error
    // later, so the shape of the metadata is checked here once.
    if(m.architecture=="qwen4exp"){
        if(!m.config.hyper_connection_count||!m.config.hyper_connection_low_rank)
            throw std::runtime_error(
                "qwen4exp GGUF is missing the hyper_connection count/low_rank keys");
        if(!m.config.ple_layers.empty()){
            const auto heads=(m.config.ple_ngram_size-1)*m.config.ple_heads_per_ngram;
            if(m.config.ple_ngram_size<2||!m.config.ple_heads_per_ngram)
                throw std::runtime_error("qwen4exp PLE geometry keys are missing");
            if(m.config.ple_multipliers.size()<m.config.ple_ngram_size)
                throw std::runtime_error(
                    "qwen4exp ple.layer_multipliers is shorter than ngram_size");
            if(m.config.ple_head_offsets.size()<heads||
               m.config.ple_head_vocab_sizes.size()<heads)
                throw std::runtime_error(
                    "qwen4exp ple head offset/vocab arrays are shorter than the "
                    "head count");
            if(m.config.ple_eos_token_id==0xffffffffu)
                throw std::runtime_error("qwen4exp ple.eos_token_id is missing");
            if(!m.config.per_layer_embedding_size)
                throw std::runtime_error(
                    "qwen4exp embedding_length_per_layer_input is missing");
        }
        if(!m.config.compress_ratios.empty()&&
           m.config.compress_ratios.size()<m.config.layer_count)
            throw std::runtime_error(
                "qwen4exp compress-ratio array is shorter than the model layer count");
    }
    if(!m.config.sliding_window_pattern.empty()&&m.config.sliding_window_pattern.size()<m.config.layer_count)
        throw std::runtime_error("GGUF sliding-window pattern is shorter than the model layer count");
    if(!m.config.attention_kv_heads_by_layer.empty()&&m.config.attention_kv_heads_by_layer.size()<m.config.layer_count)
        throw std::runtime_error("GGUF per-layer KV-head array is shorter than the model layer count");
    if(!m.config.intermediate_size)m.config.intermediate_size=m.config.expert_intermediate_size?m.config.expert_intermediate_size:m.config.dense_intermediate_size;
    uint64_t data_offset=align_to(static_cast<uint64_t>(r.p-m.data),m.alignment ? m.alignment : 32); if(data_offset>m.size) throw std::runtime_error("GGUF tensor data is outside the file"); for(auto& t:m.tensors) { if(t.offset>m.size-data_offset) throw std::runtime_error("GGUF tensor offset out of bounds"); t.offset += data_offset; }
    for(size_t i=0;i<m.tensors.size();i++) { auto& t=m.tensors[i]; uint64_t next=m.size; for(auto const& other:m.tensors) if(other.offset>t.offset) next=std::min(next,other.offset); t.size=next-t.offset; }
    build_tokenizer_tables(m);
    return 0;
}

std::uint32_t attention_window(const FlyweightV2Model& model, std::uint32_t layer) {
    if(layer>=model.config.layer_count)throw std::runtime_error("attention layer index is out of range");
    if(!model.config.sliding_window)return 0;
    const auto& pattern=model.config.sliding_window_pattern;
    return pattern.empty()||pattern[layer]?model.config.sliding_window:0;
}

int fill(const Tensor& t, FlyweightV2TensorInfo& out) { std::memset(&out,0,sizeof(out)); out.dimensions=static_cast<uint32_t>(t.shape.size()); std::copy(t.shape.begin(),t.shape.end(),out.shape); out.ggml_type=t.type; out.offset=t.offset; out.size=t.size; copy_text(out.name,sizeof(out.name),t.name); return 0; }

std::uint64_t tensor_index(const FlyweightV2Model& model, const std::string& name) {
    for (std::uint64_t index = 0; index < model.tensors.size(); ++index) {
        if (model.tensors[index].name == name) return index;
    }
    throw std::runtime_error("required Qwen tensor is missing: " + name);
}

// Every extent multiplied out. Descriptors of the same bytes can differ in rank
// between formats -- torch's grouped conv weight keeps a singleton channel
// dimension the GGUF drops -- so anything sizing a buffer from a tensor wants
// this rather than a fixed pair of extents.
std::uint64_t tensor_elements(const Tensor& tensor) {
    std::uint64_t elements = 1;
    for (const auto extent : tensor.shape) elements *= extent;
    return elements;
}

bool has_tensor(const FlyweightV2Model& model, const std::string& name) {
    for (const auto& tensor : model.tensors) if (tensor.name == name) return true;
    return false;
}

std::uint64_t first_tensor_index(
    const FlyweightV2Model& model, std::initializer_list<const char*> names
) {
    for (const char* name : names) if (has_tensor(model, name)) return tensor_index(model, name);
    throw std::runtime_error("required Qwen model tensor is missing");
}

void add_static_tensor(
    FlyweightV2QwenRuntime& runtime, QwenLayerPlan& layer,
    const std::string& name
) {
    const auto index = tensor_index(*runtime.model, name);
    layer.static_tensors.push_back(index);
    runtime.static_tensor_bytes += runtime.model->tensors[index].size;
}

void build_qwen_plan(FlyweightV2QwenRuntime& runtime) {
    auto& model = *runtime.model;
    runtime.token_embeddings = first_tensor_index(
        model, {"token_embd.weight", "model.embed_tokens.weight", "embed_tokens.weight"}
    );
    runtime.final_norm = first_tensor_index(
        model, {"output_norm.weight", "model.norm.weight", "norm.weight"}
    );
    // Qwen permits tying the output projection to the token embedding table.
    // GGUF omits output.weight for that layout, just as the reference loader
    // does, so retain the embedding tensor as the executable LM head.
    runtime.lm_head = has_tensor(model, "output.weight")
        ? tensor_index(model, "output.weight")
        : (has_tensor(model, "lm_head.weight")
            ? tensor_index(model, "lm_head.weight")
            : runtime.token_embeddings);
    runtime.lm_head_type = model.tensors[runtime.lm_head].type;
    runtime.static_tensor_bytes += model.tensors[runtime.token_embeddings].size;
    runtime.static_tensor_bytes += model.tensors[runtime.final_norm].size;
    if (runtime.lm_head != runtime.token_embeddings)
        runtime.static_tensor_bytes += model.tensors[runtime.lm_head].size;
    runtime.layers.reserve(model.config.layer_count);
    for (std::uint32_t layer_index = 0; layer_index < model.config.layer_count; ++layer_index) {
        const std::string prefix = "blk." + std::to_string(layer_index) + ".";
        QwenLayerPlan layer;
        layer.attention = has_tensor(model, prefix + "attn_q.weight");
        if(layer.attention)layer.attention_window=attention_window(model,layer_index);
        add_static_tensor(runtime, layer, prefix + "attn_norm.weight");
        if (layer.attention) {
            for (const char* suffix : {
                     "attn_q.weight", "attn_k.weight", "attn_v.weight",
                     "attn_output.weight", "attn_q_norm.weight", "attn_k_norm.weight"
                 }) add_static_tensor(runtime, layer, prefix + suffix);
            layer.attention_heads=model.config.attention_heads;
            layer.kv_heads=model.config.attention_kv_heads;
            layer.head_dim=static_cast<std::uint32_t>(model.tensors[layer.static_tensors[2]].shape[1]/layer.kv_heads);
            // Clamped: rope.dimension_count comes straight from GGUF metadata,
            // and the RoPE kernels stage a head_dim-sized buffer that a wider
            // claimed span would read past.
            layer.rotary_dim=std::min<std::uint32_t>(model.config.rotary_dimension?model.config.rotary_dimension:layer.head_dim,layer.head_dim);
            layer.rope_theta=model.config.rope_freq_base?model.config.rope_freq_base:1000000.0f;
        } else {
            for (const char* suffix : {
                     "attn_qkv.weight", "attn_gate.weight", "ssm_out.weight",
                     "ssm_alpha.weight", "ssm_beta.weight", "ssm_conv1d.weight",
                     "ssm_dt.bias", "ssm_a", "ssm_norm.weight"
                 }) add_static_tensor(runtime, layer, prefix + suffix);
        }
        layer.dense_ffn = has_tensor(model, prefix + "ffn_gate.weight");
        if (layer.dense_ffn) {
            // Keeps post_attention_norm at the same slot the MoE layout uses,
            // so moe_base still addresses the feed-forward block.
            for (const char* suffix : {
                     "post_attention_norm.weight", "ffn_gate.weight",
                     "ffn_up.weight", "ffn_down.weight"
                 }) add_static_tensor(runtime, layer, prefix + suffix);
            runtime.layers.push_back(std::move(layer));
            continue;
        }
        for (const char* suffix : {
                 "post_attention_norm.weight", "ffn_gate_inp.weight",
                 "ffn_gate_shexp.weight", "ffn_up_shexp.weight",
                 "ffn_down_shexp.weight", "ffn_gate_inp_shexp.weight"
             }) add_static_tensor(runtime, layer, prefix + suffix);
        const std::array<std::string, 3> experts = {
            prefix + "ffn_gate_exps.weight",
            prefix + "ffn_up_exps.weight",
            prefix + "ffn_down_exps.weight",
        };
        for (std::size_t role = 0; role < experts.size(); ++role) {
            layer.expert_tensors[role] = tensor_index(model, experts[role]);
            runtime.expert_tensor_bytes += model.tensors[layer.expert_tensors[role]].size;
        }
        {
            if (has_tensor(model, prefix + "ffn_gate_exps.scale")) layer.expert_gate_scale = tensor_index(model, prefix + "ffn_gate_exps.scale");
            if (has_tensor(model, prefix + "ffn_up_exps.scale")) layer.expert_up_scale = tensor_index(model, prefix + "ffn_up_exps.scale");
            if (has_tensor(model, prefix + "ffn_down_exps.scale")) layer.expert_down_scale = tensor_index(model, prefix + "ffn_down_exps.scale");
        }
        {
            // Shared-expert weight_scale_2. NVFP4 stores it out-of-line as a
            // 1-element f32 tensor; without it the shared branch comes out
            // ~30000x too large and swamps the residual stream.
            auto scalar_scale = [&](const std::string& name) {
                if (!has_tensor(model, name)) return 1.0f;
                const auto& st = model.tensors[tensor_index(model, name)];
                float value = 1.0f;
                std::memcpy(&value, tensor_data(model,st), sizeof(float));
                return value;
            };
            layer.shared_gate_scale = scalar_scale(prefix + "ffn_gate_shexp.scale");
            layer.shared_up_scale = scalar_scale(prefix + "ffn_up_shexp.scale");
            layer.shared_down_scale = scalar_scale(prefix + "ffn_down_shexp.scale");
        }
        runtime.layers.push_back(std::move(layer));
    }
    if (model.mtp_layer != std::numeric_limits<std::uint32_t>::max()) {
        const auto draft_index = model.mtp_layer;
        const std::string prefix = "blk." + std::to_string(draft_index) + ".";
        QwenLayerPlan draft;
        draft.attention = true;
        auto add_mtp_static = [&](const std::string& name) {
            const auto index = tensor_index(model, name);
            draft.static_tensors.push_back(index);
            runtime.mtp_tensor_bytes += model.tensors[index].size;
        };
        add_mtp_static(prefix + "attn_norm.weight");
        for (const char* suffix : {
                 "attn_q.weight", "attn_k.weight", "attn_v.weight",
                 "attn_output.weight", "attn_q_norm.weight", "attn_k_norm.weight"
             }) add_mtp_static(prefix + suffix);
        draft.dense_ffn = has_tensor(model, prefix + "ffn_gate.weight");
        if (draft.dense_ffn) {
            for (const char* suffix : {
                     "post_attention_norm.weight", "ffn_gate.weight",
                     "ffn_up.weight", "ffn_down.weight"
                 }) add_mtp_static(prefix + suffix);
        } else {
        for (const char* suffix : {
                 "post_attention_norm.weight", "ffn_gate_inp.weight",
                 "ffn_gate_shexp.weight", "ffn_up_shexp.weight",
                 "ffn_down_shexp.weight", "ffn_gate_inp_shexp.weight"
             }) add_mtp_static(prefix + suffix);
        const std::array<std::string, 3> draft_experts = {
            prefix + "ffn_gate_exps.weight", prefix + "ffn_up_exps.weight",
            prefix + "ffn_down_exps.weight",
        };
        for (std::size_t role = 0; role < draft_experts.size(); ++role) {
            draft.expert_tensors[role] = tensor_index(model, draft_experts[role]);
            runtime.mtp_tensor_bytes += model.tensors[draft.expert_tensors[role]].size;
        }
        // NVFP4 weight_scale_2, exactly as for the ordinary blocks above; the
        // draft layer is quantized the same way and is just as unusable without it.
        if (has_tensor(model, prefix + "ffn_gate_exps.scale")) draft.expert_gate_scale = tensor_index(model, prefix + "ffn_gate_exps.scale");
        if (has_tensor(model, prefix + "ffn_up_exps.scale")) draft.expert_up_scale = tensor_index(model, prefix + "ffn_up_exps.scale");
        if (has_tensor(model, prefix + "ffn_down_exps.scale")) draft.expert_down_scale = tensor_index(model, prefix + "ffn_down_exps.scale");
        {
            auto draft_scalar_scale = [&](const std::string& name) {
                if (!has_tensor(model, name)) return 1.0f;
                const auto& st = model.tensors[tensor_index(model, name)];
                float value = 1.0f;
                std::memcpy(&value, tensor_data(model,st), sizeof(float));
                return value;
            };
            draft.shared_gate_scale = draft_scalar_scale(prefix + "ffn_gate_shexp.scale");
            draft.shared_up_scale = draft_scalar_scale(prefix + "ffn_up_shexp.scale");
            draft.shared_down_scale = draft_scalar_scale(prefix + "ffn_down_shexp.scale");
        }
        }
        const std::array<std::string, 3> special = {
            prefix + "nextn.eh_proj.weight", prefix + "nextn.enorm.weight",
            prefix + "nextn.hnorm.weight",
        };
        for (std::size_t role = 0; role < special.size(); ++role) {
            runtime.mtp_special_tensors[role] = tensor_index(model, special[role]);
            runtime.mtp_tensor_bytes += model.tensors[runtime.mtp_special_tensors[role]].size;
        }
        // Some valid Qwen NextN blocks share the trunk output norm instead of
        // carrying a dedicated draft-head norm.
        const auto head_norm = prefix + "nextn.shared_head_norm.weight";
        runtime.mtp_special_tensors[3] = has_tensor(model, head_norm)
            ? tensor_index(model, head_norm) : runtime.final_norm;
        if (runtime.mtp_special_tensors[3] != runtime.final_norm)
            runtime.mtp_tensor_bytes +=
                model.tensors[runtime.mtp_special_tensors[3]].size;
        runtime.mtp_layer_plan = std::move(draft);
        runtime.mtp_available = true;
    }
    for(const auto&layer:runtime.layers)for(auto index:layer.static_tensors){const auto&t=model.tensors[index];if(t.shape.size()==2)runtime.scratch_elements=std::max(runtime.scratch_elements,static_cast<std::uint32_t>(t.shape[1]));}
    if(runtime.mtp_available)for(auto index:runtime.mtp_layer_plan.static_tensors){const auto&t=model.tensors[index];if(t.shape.size()==2)runtime.scratch_elements=std::max(runtime.scratch_elements,static_cast<std::uint32_t>(t.shape[1]));}
    // The feed-forward width comes from the gate projection: the shared expert's
    // for MoE checkpoints, the block's own for dense ones. Both sit one slot
    // past post_attention_norm plus the router that only MoE layers carry.
    const auto& front = runtime.layers.front();
    const std::size_t ffn_base = front.attention ? 7 : 10;
    const auto& gate = model.tensors[front.static_tensors[ffn_base + (front.dense_ffn ? 1 : 2)]];
    runtime.moe_intermediate=static_cast<std::uint32_t>(gate.shape[1]);
    // The dense SwiGLU stages gate and up contiguously so silu_mul can read one
    // buffer, so one scratch slot has to hold both halves.
    if(front.dense_ffn)
        runtime.scratch_elements=std::max(runtime.scratch_elements,2u*runtime.moe_intermediate);
}

// qwen4exp (Qwen3.8-Flash-Next, "a preview of the Qwen4 architecture"): the
// qwen hybrid layout -- gated DeltaNet on three of every four blocks, gated
// GQA on the fourth, routed+shared MoE everywhere -- with three additions:
//
//   gated residual   hc_count=4 parallel residual streams; every block
//                    boundary norms the streams (grouped RMS), mixes them to
//                    one block input through a low-rank silu/sigmoid gate, and
//                    injects the block output back per stream. There are NO
//                    attn_norm / post_attention_norm / output_norm tensors:
//                    hc_attn_norm and hc_ffn_norm stand in their slots so the
//                    positional layout (and qwen_ffn_base) is unchanged, and
//                    the head collapse (output_hc_*) doubles as the final norm.
//   PLE              hashed n-gram embeddings gated into the streams at the
//                    blocks ple.layers names (blk.1 on the release model). The
//                    320M-row table stays in the mapping; a token reads 16 rows.
//   QSA indexer      per full-attention block, a 4-head scorer whose top-k
//                    mask makes attention sparse. Loaded here for residency
//                    accounting; the dense fallback ignores it (phase 3).
//
// Semantics: plans/qwen4exp-semantics.md, pinned by
// native/tools/qwen4exp_reference_check.py against transformers.
void build_qwen4exp_plan(FlyweightV2QwenRuntime& runtime) {
    auto& model = *runtime.model;
    runtime.token_embeddings = tensor_index(model, "token_embd.weight");
    // The head collapse ends in a grouped RMS over the streams, so the model
    // carries no output_norm; final_norm aliases the collapse norm for the
    // residency/accounting paths, and the decode bookend replaces the final
    // rms with the collapse when runtime.qwen4exp is set.
    runtime.output_hc_norm = tensor_index(model, "output_hc_norm.weight");
    runtime.output_hc_down = tensor_index(model, "output_hc_down.weight");
    runtime.output_hc_up = tensor_index(model, "output_hc_up.weight");
    runtime.final_norm = runtime.output_hc_norm;
    runtime.lm_head = has_tensor(model, "output.weight")
        ? tensor_index(model, "output.weight") : runtime.token_embeddings;
    runtime.lm_head_type = model.tensors[runtime.lm_head].type;
    runtime.static_tensor_bytes += model.tensors[runtime.token_embeddings].size;
    for (auto index : {runtime.output_hc_norm, runtime.output_hc_down,
                       runtime.output_hc_up})
        runtime.static_tensor_bytes += model.tensors[index].size;
    if (runtime.lm_head != runtime.token_embeddings)
        runtime.static_tensor_bytes += model.tensors[runtime.lm_head].size;
    // The n-gram table is never uploaded: it is read 16 rows per token on the
    // host, like host-resident embeddings, so it joins no byte accounting that
    // would push it toward the GPU budget.
    if (has_tensor(model, "per_layer_token_embd.weight"))
        runtime.ple_table = tensor_index(model, "per_layer_token_embd.weight");
    else if (!model.config.ple_layers.empty())
        throw std::runtime_error(
            "qwen4exp GGUF names PLE layers but carries no per_layer_token_embd");
    runtime.layers.reserve(model.config.layer_count);
    for (std::uint32_t layer_index = 0; layer_index < model.config.layer_count; ++layer_index) {
        const std::string prefix = "blk." + std::to_string(layer_index) + ".";
        QwenLayerPlan layer;
        layer.attention = has_tensor(model, prefix + "attn_q.weight");
        // Slot 0: the attention-boundary stream norm, standing where
        // attn_norm stands on qwen so every existing slot offset holds.
        add_static_tensor(runtime, layer, prefix + "hc_attn_norm.weight");
        if (layer.attention) {
            for (const char* suffix : {
                     "attn_q.weight", "attn_k.weight", "attn_v.weight",
                     "attn_output.weight", "attn_q_norm.weight", "attn_k_norm.weight"
                 }) add_static_tensor(runtime, layer, prefix + suffix);
            layer.attention_heads=model.config.attention_heads;
            layer.kv_heads=model.config.attention_kv_heads;
            layer.head_dim=static_cast<std::uint32_t>(model.tensors[layer.static_tensors[2]].shape[1]/layer.kv_heads);
            layer.rotary_dim=std::min<std::uint32_t>(model.config.rotary_dimension?model.config.rotary_dimension:layer.head_dim,layer.head_dim);
            layer.rope_theta=model.config.rope_freq_base?model.config.rope_freq_base:1000000.0f;
        } else {
            for (const char* suffix : {
                     "attn_qkv.weight", "attn_gate.weight", "ssm_out.weight",
                     "ssm_alpha.weight", "ssm_beta.weight", "ssm_conv1d.weight",
                     "ssm_dt.bias", "ssm_a", "ssm_norm.weight"
                 }) add_static_tensor(runtime, layer, prefix + suffix);
        }
        // The ffn-base slot: hc_ffn_norm standing where post_attention_norm
        // stands, then the identical MoE slot list.
        for (const char* suffix : {
                 "hc_ffn_norm.weight", "ffn_gate_inp.weight",
                 "ffn_gate_shexp.weight", "ffn_up_shexp.weight",
                 "ffn_down_shexp.weight", "ffn_gate_inp_shexp.weight"
             }) add_static_tensor(runtime, layer, prefix + suffix);
        // Everything below is appended PAST the positional slots so the qwen
        // feed-forward addressing is undisturbed; like Laguna's router_bias,
        // they still have to ride static_tensors to reach the device.
        auto add_extra = [&](const std::string& name, std::uint64_t& field) {
            add_static_tensor(runtime, layer, name);
            field = layer.static_tensors.back();
        };
        add_extra(prefix + "hc_attn_down.weight", layer.hc_attn_down);
        add_extra(prefix + "hc_attn_up.weight", layer.hc_attn_up);
        add_extra(prefix + "hc_attn_inject.weight", layer.hc_attn_inject);
        add_extra(prefix + "hc_ffn_down.weight", layer.hc_ffn_down);
        add_extra(prefix + "hc_ffn_up.weight", layer.hc_ffn_up);
        add_extra(prefix + "hc_ffn_inject.weight", layer.hc_ffn_inject);
        if (layer.attention) {
            // QSA indexer: resident from day one (phase 1), selecting since
            // phase 3. The per-layer compress ratio is the selection gate --
            // 4 on the full-attention layers of the real checkpoint, absent
            // or 0 means the layer stays dense forever.
            add_extra(prefix + "indexer.q_proj.weight", layer.indexer_q);
            add_extra(prefix + "indexer.k_proj.weight", layer.indexer_k);
            add_extra(prefix + "indexer.q_norm.weight", layer.indexer_q_norm);
            add_extra(prefix + "indexer.k_norm.weight", layer.indexer_k_norm);
            // `qsa_ratio` is the single source of truth for "this layer has
            // QSA state": the arena reservation, the snapshot accounting, the
            // snapshot copy and the host-spill ranges all key off it alone, so
            // every requirement of the indexer has to be settled HERE or those
            // four sites drift apart. A file that names a ratio but omits any
            // of the three indexer scalars keeps the layer dense.
            // QSA is OPT-IN (FLYWEIGHT_QSA=1). It is the attention the model was
            // trained with above 2048 tokens, so running dense past that is an
            // approximation -- but it measures 7-10% slower on decode (the
            // selection costs a host round-trip per selecting layer, and decode
            // here is expert-read bound, not attention bound), and its block-key
            // store costs 384 MiB per sequence slot at the native 262144 context,
            // straight out of the expert cache budget. Correctness above the
            // budget is available to anyone who asks for it; it is not worth
            // charging every short-context user for by default.
            //
            // Settled HERE and not only at the kernel gate, because the arena is
            // reserved from `qsa_ratio` alone -- gating just the execution left
            // the memory allocated for a feature that was switched off.
            const char* qsa_setting = std::getenv("FLYWEIGHT_QSA");
            if (qsa_setting && qsa_setting[0] == '1'
                && layer_index < model.config.compress_ratios.size()
                && model.config.indexer_head_count
                && model.config.indexer_key_length && model.config.indexer_top_k)
                layer.qsa_ratio = model.config.compress_ratios[layer_index];
        }
        const auto& ple_layers = model.config.ple_layers;
        if (std::find(ple_layers.begin(), ple_layers.end(), layer_index)
            != ple_layers.end()) {
            add_extra(prefix + "ple_conv1d.weight", layer.ple_conv);
            add_extra(prefix + "ple_key.weight", layer.ple_key);
            add_extra(prefix + "ple_value.weight", layer.ple_value);
            add_extra(prefix + "ple_norm_conv.weight", layer.ple_norm_conv);
            add_extra(prefix + "ple_norm_key.weight", layer.ple_norm_key);
            add_extra(prefix + "ple_norm_query.weight", layer.ple_norm_query);
        }
        const std::array<std::string, 3> experts = {
            prefix + "ffn_gate_exps.weight",
            prefix + "ffn_up_exps.weight",
            prefix + "ffn_down_exps.weight",
        };
        for (std::size_t role = 0; role < experts.size(); ++role) {
            layer.expert_tensors[role] = tensor_index(model, experts[role]);
            runtime.expert_tensor_bytes += model.tensors[layer.expert_tensors[role]].size;
        }
        runtime.layers.push_back(std::move(layer));
    }
    // The nextn draft block, one past the executed stack. Present in the
    // Q4_K_XL release and in the standalone MTP file, absent from UD-IQ1_S --
    // so this is keyed off the tensors, never off the architecture.
    //
    // The slot order below MIRRORS the ordinary layer above exactly, because
    // the draft forward indexes a QwenLayerPlan the same way the stack does.
    // Two deliberate omissions (plans/qwen4exp-semantics.md, MTP section):
    // the draft has no PLE, and its attention runs DENSE -- the indexer
    // tensors exist in the checkpoint but llama.cpp loads and ignores them,
    // so leaving `qsa_ratio` at 0 both matches the reference and spares the
    // draft a per-sequence block-key store.
    if (model.mtp_layer != std::numeric_limits<std::uint32_t>::max()) {
        const std::string prefix = "blk." + std::to_string(model.mtp_layer) + ".";
        QwenLayerPlan draft;
        draft.attention = true;
        auto add_mtp_static = [&](const std::string& name) {
            const auto index = tensor_index(model, name);
            draft.static_tensors.push_back(index);
            runtime.mtp_tensor_bytes += model.tensors[index].size;
        };
        auto add_mtp_extra = [&](const std::string& name, std::uint64_t& field) {
            add_mtp_static(name);
            field = draft.static_tensors.back();
        };
        add_mtp_static(prefix + "hc_attn_norm.weight");           // slot 0
        for (const char* suffix : {
                 "attn_q.weight", "attn_k.weight", "attn_v.weight",
                 "attn_output.weight", "attn_q_norm.weight", "attn_k_norm.weight"
             }) add_mtp_static(prefix + suffix);                  // slots 1-6
        for (const char* suffix : {
                 "hc_ffn_norm.weight", "ffn_gate_inp.weight",
                 "ffn_gate_shexp.weight", "ffn_up_shexp.weight",
                 "ffn_down_shexp.weight", "ffn_gate_inp_shexp.weight"
             }) add_mtp_static(prefix + suffix);                  // slots 7-12
        add_mtp_extra(prefix + "hc_attn_down.weight", draft.hc_attn_down);
        add_mtp_extra(prefix + "hc_attn_up.weight", draft.hc_attn_up);
        add_mtp_extra(prefix + "hc_attn_inject.weight", draft.hc_attn_inject);
        add_mtp_extra(prefix + "hc_ffn_down.weight", draft.hc_ffn_down);
        add_mtp_extra(prefix + "hc_ffn_up.weight", draft.hc_ffn_up);
        add_mtp_extra(prefix + "hc_ffn_inject.weight", draft.hc_ffn_inject);
        draft.attention_heads = model.config.attention_heads;
        draft.kv_heads = model.config.attention_kv_heads;
        draft.head_dim = static_cast<std::uint32_t>(
            model.tensors[draft.static_tensors[2]].shape[1] / draft.kv_heads);
        draft.rotary_dim = std::min<std::uint32_t>(
            model.config.rotary_dimension ? model.config.rotary_dimension
                                          : draft.head_dim, draft.head_dim);
        draft.rope_theta = model.config.rope_freq_base
            ? model.config.rope_freq_base : 1000000.0f;
        const std::array<std::string, 3> draft_experts = {
            prefix + "ffn_gate_exps.weight", prefix + "ffn_up_exps.weight",
            prefix + "ffn_down_exps.weight",
        };
        for (std::size_t role = 0; role < draft_experts.size(); ++role) {
            draft.expert_tensors[role] = tensor_index(model, draft_experts[role]);
            runtime.mtp_tensor_bytes += model.tensors[draft.expert_tensors[role]].size;
        }
        const std::array<std::string, 3> special = {
            prefix + "nextn.eh_proj.weight", prefix + "nextn.enorm.weight",
            prefix + "nextn.hnorm.weight",
        };
        for (std::size_t role = 0; role < special.size(); ++role) {
            runtime.mtp_special_tensors[role] = tensor_index(model, special[role]);
            runtime.mtp_tensor_bytes += model.tensors[runtime.mtp_special_tensors[role]].size;
        }
        // qwen4exp has no output_norm: the head collapse IS the final norm, and
        // the draft reuses it (llama.cpp runs build_hc_pre with model.hc_mix_*
        // on the draft's own output). `final_norm` holds output_hc_norm here.
        runtime.mtp_special_tensors[3] = has_tensor(model, prefix + "nextn.shared_head_norm.weight")
            ? tensor_index(model, prefix + "nextn.shared_head_norm.weight")
            : runtime.final_norm;
        if (runtime.mtp_special_tensors[3] != runtime.final_norm)
            runtime.mtp_tensor_bytes +=
                model.tensors[runtime.mtp_special_tensors[3]].size;
        runtime.mtp_layer_plan = std::move(draft);
        runtime.mtp_available = true;
    }
    for(const auto&layer:runtime.layers)for(auto index:layer.static_tensors){const auto&t=model.tensors[index];if(t.shape.size()==2)runtime.scratch_elements=std::max(runtime.scratch_elements,static_cast<std::uint32_t>(t.shape[1]));}
    if(runtime.mtp_available)for(auto index:runtime.mtp_layer_plan.static_tensors){const auto&t=model.tensors[index];if(t.shape.size()==2)runtime.scratch_elements=std::max(runtime.scratch_elements,static_cast<std::uint32_t>(t.shape[1]));}
    const auto& front = runtime.layers.front();
    const std::size_t ffn_base = front.attention ? 7 : 10;
    const auto& gate = model.tensors[front.static_tensors[ffn_base + 2]];
    runtime.moe_intermediate=static_cast<std::uint32_t>(gate.shape[1]);
}

// YaRN correction band. `beta_fast` and `beta_slow` are expressed as numbers of
// full rotations across the trained context; converting each to the rotary pair
// index that completes that many rotations gives the band over which the kernel
// blends extrapolation into interpolation.
void qwen_yarn_correction_dims(
    int rotary_dim, std::uint32_t original_context, float theta,
    float beta_fast, float beta_slow, float& low, float& high
) {
    if(rotary_dim<=0||!original_context||theta<=1.0f){low=0.0f;high=0.0f;return;}
    const auto dimension=[&](float rotations){
        if(rotations<=0.0f)return 0.0f;
        return static_cast<float>(rotary_dim)*
            std::log(static_cast<float>(original_context)/
                     (rotations*2.0f*3.14159265358979323846f))/
            (2.0f*std::log(theta));
    };
    low=std::max(0.0f,std::floor(dimension(beta_fast)));
    high=std::min(static_cast<float>(rotary_dim-1),std::ceil(dimension(beta_slow)));
    if(high<low)high=low;
}

// Slot of the block's feed-forward norm within static_tensors, which every
// later feed-forward tensor is addressed relative to. Laguna's attention blocks
// carry an extra gate projection ahead of it; Qwen's DeltaNet blocks carry the
// wider recurrent set.
std::size_t qwen_ffn_base(const FlyweightV2QwenRuntime& runtime, const QwenLayerPlan& layer) {
    if(runtime.laguna)return 8;
    // Muse Glimmer carries a post-attention norm that the other layouts do not,
    // so its pre-FFN norm sits one slot later and gate/up/down at 10/11/12.
    if(runtime.muse)return 9;
    return layer.attention?7:10;
}

// Laguna (poolside): every block is full attention or sliding-window attention
// with a softplus per-head output gate, a single pre-FFN norm (no post-attention
// or post-FFN norm), a leading run of dense blocks, and sigmoid-routed MoE with
// a score-correction bias and one always-on ungated shared expert.
void build_laguna_plan(FlyweightV2QwenRuntime& runtime) {
    auto& model=*runtime.model;
    runtime.laguna=true;
    runtime.token_embeddings=tensor_index(model,"token_embd.weight");
    runtime.final_norm=tensor_index(model,"output_norm.weight");
    // Laguna ships an untied head, but fall back to the table if a future
    // checkpoint ties them.
    runtime.lm_head=has_tensor(model,"output.weight")
        ?tensor_index(model,"output.weight"):runtime.token_embeddings;
    runtime.lm_head_type=model.tensors[runtime.lm_head].type;
    runtime.static_tensor_bytes+=model.tensors[runtime.token_embeddings].size;
    runtime.static_tensor_bytes+=model.tensors[runtime.final_norm].size;
    if(runtime.lm_head!=runtime.token_embeddings)
        runtime.static_tensor_bytes+=model.tensors[runtime.lm_head].size;
    const auto head_dim=model.config.key_length?model.config.key_length:128u;
    runtime.layers.reserve(model.config.layer_count);
    for(std::uint32_t layer_index=0;layer_index<model.config.layer_count;++layer_index){
        const std::string prefix="blk."+std::to_string(layer_index)+".";
        QwenLayerPlan layer;layer.attention=true;
        layer.attention_window=attention_window(model,layer_index);
        layer.attention_heads=model.config.attention_heads_by_layer.empty()
            ?model.config.attention_heads
            :model.config.attention_heads_by_layer[layer_index];
        layer.kv_heads=model.config.attention_kv_heads;
        layer.head_dim=head_dim;
        // Full-attention layers run YaRN over a partial rotary span with the
        // long-context theta; sliding-window layers run plain RoPE over the
        // whole head with the short theta.
        layer.rotary_dim=std::min<std::uint32_t>(layer.attention_window
            ?(model.config.rotary_dimension_swa?model.config.rotary_dimension_swa:head_dim)
            :(model.config.rotary_dimension?model.config.rotary_dimension:head_dim),head_dim);
        layer.rope_theta=layer.attention_window
            ?(model.config.rope_freq_base_swa?model.config.rope_freq_base_swa:10000.0f)
            :(model.config.rope_freq_base?model.config.rope_freq_base:500000.0f);
        if(!layer.attention_window&&model.config.rope_scaling_yarn&&
           model.config.rope_scaling_factor>1.0f){
            layer.rope_freq_scale=1.0f/model.config.rope_scaling_factor;
            layer.rope_ext_factor=1.0f;
            layer.rope_attn_factor=model.config.yarn_attn_factor;
            layer.rope_beta_fast=model.config.yarn_beta_fast;
            layer.rope_beta_slow=model.config.yarn_beta_slow;
            layer.rope_orig_context=model.config.rope_original_context_length
                ?model.config.rope_original_context_length:model.config.context_length;
        }
        for(const char* suffix:{
            "attn_norm.weight","attn_q.weight","attn_k.weight","attn_v.weight",
            "attn_output.weight","attn_q_norm.weight","attn_k_norm.weight",
            "attn_gate.weight","ffn_norm.weight"
        })add_static_tensor(runtime,layer,prefix+suffix);
        // The gate is per-head (one scalar broadcast over head_dim). The
        // per-element variant the larger Laguna checkpoints use would need a
        // different apply kernel, so reject it rather than silently mis-scale.
        const auto gate_width=model.tensors[layer.static_tensors[7]].shape[1];
        if(gate_width!=layer.attention_heads)
            throw std::runtime_error(
                "native Laguna supports only the per-head attention gate");
        layer.dense_ffn=has_tensor(model,prefix+"ffn_gate.weight");
        if(layer.dense_ffn){
            for(const char* suffix:{"ffn_gate.weight","ffn_up.weight","ffn_down.weight"})
                add_static_tensor(runtime,layer,prefix+suffix);
        }else{
            // Same slot order the Qwen MoE block uses (router, then the shared
            // expert's gate/up/down) so the shared feed-forward code addresses
            // both architectures off moe_base. Laguna's shared expert has no
            // gate tensor, and its router bias hangs off the plan instead.
            for(const char* suffix:{
                "ffn_gate_inp.weight","ffn_gate_shexp.weight",
                "ffn_up_shexp.weight","ffn_down_shexp.weight"
            })add_static_tensor(runtime,layer,prefix+suffix);
            // Appended last so it lands past the shared expert's slots and the
            // Qwen feed-forward addressing is undisturbed; it still has to be a
            // static tensor to reach the device.
            add_static_tensor(runtime,layer,prefix+"exp_probs_b.bias");
            layer.router_bias=layer.static_tensors.back();
            const std::array<std::string,3> experts={
                prefix+"ffn_gate_exps.weight",
                prefix+"ffn_up_exps.weight",
                prefix+"ffn_down_exps.weight",
            };
            for(std::size_t role=0;role<experts.size();++role){
                layer.expert_tensors[role]=tensor_index(model,experts[role]);
                runtime.expert_tensor_bytes+=model.tensors[layer.expert_tensors[role]].size;
            }
        }
        for(auto index:layer.static_tensors){const auto&t=model.tensors[index];if(t.shape.size()==2)runtime.scratch_elements=std::max(runtime.scratch_elements,static_cast<std::uint32_t>(t.shape[1]));}
        runtime.layers.push_back(std::move(layer));
    }
    runtime.moe_intermediate=model.config.expert_intermediate_size;
    // The shared expert and the routed experts may be sized differently; both
    // SwiGLU stages write gate and up contiguously, so reserve the wider pair.
    const auto shared_intermediate=model.config.expert_shared_intermediate_size
        ?model.config.expert_shared_intermediate_size:runtime.moe_intermediate;
    runtime.scratch_elements=std::max(
        runtime.scratch_elements,
        2u*std::max({runtime.moe_intermediate,shared_intermediate,
                     model.config.dense_intermediate_size}));
}

// Muse Glimmer is dense throughout: no router, no experts, no expert paging.
// Every layer carries the same fourteen tensors, so the slot layout is fixed
// and `moe_base` lands on the pre-FFN norm exactly as the Qwen dense block
// expects.
//
// Slots 8 and 13 are the two post-norms that make this architecture different
// from Laguna: attention output and feed-forward output are each normalized
// *before* rejoining the residual stream, rather than added raw.
void build_muse_glimmer_plan(FlyweightV2QwenRuntime& runtime) {
    auto& model=*runtime.model;
    runtime.muse=true;
    runtime.token_embeddings=tensor_index(model,"token_embd.weight");
    runtime.final_norm=tensor_index(model,"output_norm.weight");
    // The 202k-entry head is untied and is the single largest tensor in the
    // file, so fall back to the table only if a future checkpoint ties them.
    runtime.lm_head=has_tensor(model,"output.weight")
        ?tensor_index(model,"output.weight"):runtime.token_embeddings;
    runtime.lm_head_type=model.tensors[runtime.lm_head].type;
    runtime.static_tensor_bytes+=model.tensors[runtime.token_embeddings].size;
    runtime.static_tensor_bytes+=model.tensors[runtime.final_norm].size;
    if(runtime.lm_head!=runtime.token_embeddings)
        runtime.static_tensor_bytes+=model.tensors[runtime.lm_head].size;
    const auto head_dim=model.config.key_length?model.config.key_length:128u;
    runtime.layers.reserve(model.config.layer_count);
    for(std::uint32_t layer_index=0;layer_index<model.config.layer_count;++layer_index){
        const std::string prefix="blk."+std::to_string(layer_index)+".";
        QwenLayerPlan layer;layer.attention=true;
        layer.attention_window=attention_window(model,layer_index);
        layer.attention_heads=model.config.attention_heads;
        layer.kv_heads=model.config.attention_kv_heads;
        layer.head_dim=head_dim;
        // RoPE runs on the sliding-window layers and the full-attention layers
        // run NoPE. A zero rotary span is how that reaches the kernel, and it
        // is the reverse of the usual arrangement -- the global layers here
        // carry no positional signal of their own at all.
        layer.rotary_dim=layer.attention_window
            ?std::min<std::uint32_t>(model.config.rotary_dimension?model.config.rotary_dimension:head_dim,head_dim)
            :0;
        layer.rope_theta=model.config.rope_freq_base?model.config.rope_freq_base:500000.0f;
        for(const char* suffix:{
            "attn_norm.weight","attn_q.weight","attn_k.weight","attn_v.weight",
            "attn_output.weight","attn_q_norm.weight","attn_k_norm.weight",
            "attn_gate.weight","post_attention_norm.weight","ffn_norm.weight",
            "ffn_gate.weight","ffn_up.weight","ffn_down.weight",
            "post_ffw_norm.weight"
        })add_static_tensor(runtime,layer,prefix+suffix);
        // The gate is one value per query channel, not per head: it multiplies
        // the attention output elementwise before the output projection. A
        // per-head checkpoint would silently broadcast wrong, so check.
        const auto gate_width=model.tensors[layer.static_tensors[7]].shape[1];
        if(gate_width!=static_cast<std::uint64_t>(layer.attention_heads)*head_dim)
            throw std::runtime_error(
                "native Muse Glimmer expects a per-channel attention gate");
        layer.dense_ffn=true;
        for(auto index:layer.static_tensors){
            const auto&t=model.tensors[index];
            if(t.shape.size()==2)runtime.scratch_elements=std::max(
                runtime.scratch_elements,static_cast<std::uint32_t>(t.shape[1]));
        }
        runtime.layers.push_back(std::move(layer));
    }
    // The dense block writes gate and up contiguously before silu_mul reads the
    // pair, so the scratch has to hold both halves at once.
    runtime.moe_intermediate=model.config.dense_intermediate_size;
    runtime.scratch_elements=std::max(
        runtime.scratch_elements,2u*model.config.dense_intermediate_size);
}

void build_gemma4_plan(FlyweightV2QwenRuntime& runtime) {
    auto& model=*runtime.model;
    runtime.gemma4=true;
    runtime.token_embeddings=tensor_index(model,"token_embd.weight");
    runtime.final_norm=tensor_index(model,"output_norm.weight");
    runtime.lm_head=runtime.token_embeddings;
    runtime.static_tensor_bytes+=model.tensors[runtime.token_embeddings].size;
    runtime.static_tensor_bytes+=model.tensors[runtime.final_norm].size;
    if(has_tensor(model,"rope_freqs.weight")){
        runtime.rope_factors=tensor_index(model,"rope_freqs.weight");
        runtime.static_tensor_bytes+=model.tensors[runtime.rope_factors].size;
    }
    runtime.layers.reserve(model.config.layer_count);
    for(std::uint32_t layer_index=0;layer_index<model.config.layer_count;++layer_index){
        const std::string prefix="blk."+std::to_string(layer_index)+".";
        QwenLayerPlan layer;layer.attention=true;
        layer.attention_window=attention_window(model,layer_index);
        layer.attention_heads=model.config.attention_heads;
        layer.kv_heads=model.config.attention_kv_heads_by_layer.empty()
            ?model.config.attention_kv_heads:model.config.attention_kv_heads_by_layer[layer_index];
        layer.head_dim=layer.attention_window
            ?(model.config.key_length_swa?model.config.key_length_swa:model.config.rotary_dimension_swa)
            :(model.config.key_length?model.config.key_length:model.config.rotary_dimension);
        layer.rotary_dim=std::min<std::uint32_t>(layer.attention_window
            ?(model.config.rotary_dimension_swa?model.config.rotary_dimension_swa:layer.head_dim)
            :(model.config.rotary_dimension?model.config.rotary_dimension:layer.head_dim),layer.head_dim);
        layer.rope_theta=layer.attention_window
            ?(model.config.rope_freq_base_swa?model.config.rope_freq_base_swa:10000.0f)
            :(model.config.rope_freq_base?model.config.rope_freq_base:1000000.0f);
        for(const char* suffix:{"attn_norm.weight","attn_q.weight","attn_k.weight"})
            add_static_tensor(runtime,layer,prefix+suffix);
        // Gemma 4 global attention can be configured with K == V. Those
        // checkpoints intentionally omit attn_v.weight; re-projecting with K
        // gives the unnormalized value states before K takes its learned norm.
        if(has_tensor(model,prefix+"attn_v.weight"))
            add_static_tensor(runtime,layer,prefix+"attn_v.weight");
        else
            layer.static_tensors.push_back(layer.static_tensors[2]);
        for(const char* suffix:{
            "attn_output.weight","attn_q_norm.weight","attn_k_norm.weight",
            "post_attention_norm.weight","ffn_norm.weight","ffn_gate.weight",
            "ffn_up.weight","ffn_down.weight","post_ffw_norm_1.weight",
            "ffn_gate_inp.scale","ffn_gate_inp.weight","pre_ffw_norm_2.weight",
            "post_ffw_norm_2.weight","post_ffw_norm.weight","layer_output_scale.weight"
        })add_static_tensor(runtime,layer,prefix+suffix);
        layer.expert_tensors[0]=tensor_index(model,prefix+"ffn_gate_up_exps.weight");
        layer.expert_tensors[1]=tensor_index(model,prefix+"ffn_down_exps.weight");
        layer.expert_tensors[2]=tensor_index(model,prefix+"ffn_down_exps.scale");
        layer.expert_tensor_count=3;
        for(auto index:layer.expert_tensors)runtime.expert_tensor_bytes+=model.tensors[index].size;
        for(auto index:layer.static_tensors){const auto&t=model.tensors[index];if(t.shape.size()==2)runtime.scratch_elements=std::max(runtime.scratch_elements,static_cast<std::uint32_t>(t.shape[1]));}
        runtime.layers.push_back(std::move(layer));
    }
    runtime.moe_intermediate=model.config.expert_intermediate_size;
}

// qwen_half_value now lives in src/qwen_kquant.h so the contract tests can
// decode super-blocks without linking the whole runtime.

// bf16 is just the top half of an f32, so widening is a shift.
float qwen_bf16_value(std::uint16_t bits) {
    const std::uint32_t widened=static_cast<std::uint32_t>(bits)<<16;
    float value;std::memcpy(&value,&widened,sizeof(value));return value;
}

// qwen_half_bits now lives in src/qwen_kquant.h beside qwen_half_value, so the
// K-quant packers can share one definition with the decoders.

// Pack `count` f32 values (a multiple of 32) into Q8_0 blocks: one f16 scale
// followed by 32 int8 codes. Matches qwen_q8_value and the q8 CUDA kernels.
using qwen_kpack::pack_q8_0;
using qwen_kpack::pack_q2_k;
using qwen_kpack::pack_q3_k;

// Q2_K and Q3_K element decoders are shared with the SIMD kernels and the
// contract tests; see src/qwen_kquant.h for the block layouts.
// Q4_K super-block (GGML type 12): 144 bytes per 256 values -> d(2) dmin(2)
// scales[12] (same 6-bit packing as Q5_K) qs[128] (4-bit). Like Q5_K but with
// no 5th-bit array; value = d*scale*q - dmin*min.
// NVFP4 (GGML type 40): E2M1 4-bit float (1 sign + 2 exp + 1 mantissa, bias=1).
// Verified against a real NVFP4 checkpoint (Qwen3.6-35B-Fast-NVFP4.gguf):
// 36 bytes per 64 elements -- d[4] E4M3 scales then qs[32] packed nibbles, with
// scale i governing bytes 4+8i..11+8i (16 elements). Nibbles are split-half like
// q4_0/q4_K: qs[lane] holds element `lane` in the low nibble and `lane+8` in the
// high nibble, relative to the sub-block. (Byte-position entropy pins the scales
// to bytes 0-3; every sub-block's largest magnitude decodes to exactly 6.0,
// confirming the grouping.)
// This layout must stay in lockstep with nvfp4_value() in
// flyweight_v2_qwen_kernels.hpp -- the CPU and GPU expert paths decode the very
// same bytes, and the expert-scale folding in the MoE staging path rewrites the
// four scale bytes of every 36-byte block in place.
// E2M1 LUT: 0, 0.5, 1.0, 1.5, 2.0, 3.0, 4.0, 6.0 (and negatives).
constexpr float kNvfp4Lut[16]={
    0.0f,0.5f,1.0f,1.5f,2.0f,3.0f,4.0f,6.0f,
    0.0f,-0.5f,-1.0f,-1.5f,-2.0f,-3.0f,-4.0f,-6.0f};
// OCP "FN" E4M3, which is what NVFP4 block scales use: e==0xF is FINITE (up to
// 448 at 0x7E) and only 0x7F/0xFF are NaN -- there are no infinities. Real
// checkpoints do reach e==0xF (0.34% of scales in Qwen3.6-35B-Fast-NVFP4), so
// decoding that exponent as infinity poisons whole blocks with NaN.
float ue4m3_to_float(std::uint8_t bits){
    const int s=(bits>>7)&1;const int e=(bits>>3)&0xF;const int m=bits&7;
    float val;
    if(e==0xF&&m==7){val=std::numeric_limits<float>::quiet_NaN();}
    else if(e==0){val=(m/8.0f)*std::exp2(-6.0f);}
    else{val=std::exp2(static_cast<float>(e-7))*(1.0f+m/8.0f);}
    return s?-val:val;
}
// NOTE: there is deliberately no float_to_ue4m3 here. NVFP4's per-tensor
// weight_scale_2 runs ~3e-5, far below E4M3's smallest subnormal (2^-9), so
// folding it back into the block scales flushes ~56% of them to zero. The scale
// is carried in f32 to the kernels instead.
float qwen_nvfp4_value(const std::uint8_t*packed,std::uint64_t absolute){
    const std::uint64_t block=absolute/kNvfp4BlockElements;
    const int offset=static_cast<int>(absolute%kNvfp4BlockElements);
    const int sub=offset/kNvfp4SubBlock,within=offset%kNvfp4SubBlock;
    const auto*base=packed+block*kNvfp4BlockSize;
    const float scale=ue4m3_to_float(base[sub]);
    const auto byte=base[4+sub*8+(within&7)];
    const int val=(within<8)?(byte&0x0F):(byte>>4);
    return scale*kNvfp4Lut[val];
}
// Cached once: these dot functions run per row (x top_k x layers) on
// CPU-expert decode, and glibc's getenv scans environ linearly -- read raw,
// the switch cost 10^4-10^5 environ scans per token.
bool qwen_iq_avx512_enabled(){
    static const bool enabled=[]{
        const char*setting=std::getenv("FLYWEIGHT_IQ_AVX512");
        return !setting||setting[0]!='0';
    }();
    return enabled;
}
// Same treatment: the diagnostic gate sat as a raw getenv inside per-layer
// decode code, costing an environ scan per layer per token when it was off.
bool qwen_lm_diag_enabled(){
    static const bool enabled=std::getenv("FLYWEIGHT_LM_DIAG")!=nullptr;
    return enabled;
}
float qwen_quant_dot(const std::uint8_t*packed,std::uint32_t type,const float*input,int elements,std::uint64_t row){
    if(qwen_simd_quant_type(type)&&(flyweight_cpu_features()&2u)!=0&&elements%kBlockElements==0)return qwen_quant_dot_avx512(packed,type,input,elements,row);
    if(type==17&&(flyweight_cpu_features()&2u)!=0&&elements%256==0&&
       qwen_iq_avx512_enabled())
        return qwen_quant_dot_avx512(packed,type,input,elements,row);
    // Q4_0's native block is 32 elements, so it takes its own admission: the
    // K-quant super-block gate would exclude Gemma 4's 704-wide expert rows
    // (704 % 256 != 0), which is exactly where this type dominates decode.
    if(type==2&&(flyweight_cpu_features()&2u)!=0&&elements%32==0)
        return qwen_quant_dot_avx512(packed,type,input,elements,row);
    if(type==2&&(flyweight_cpu_features()&1u)!=0&&elements%32==0)
        return qwen_quant_dot_avx2(packed,type,input,elements,row);
    if(type==40&&(flyweight_cpu_features()&1u)!=0&&elements%kNvfp4BlockElements==0)return qwen_quant_dot_avx2(packed,type,input,elements,row);
    // The IQ codebook formats decode a branch per weight in scalar form, which
    // is what made low-bit MoE decode compute-bound rather than bandwidth-bound.
    if((type==16||type==17||type==18||type==19||type==22||type==23)&&(flyweight_cpu_features()&1u)!=0&&elements%256==0)
        return qwen_quant_dot_avx2(packed,type,input,elements,row);
    // IQ4_NL's native block is 32 elements (no super-block), so like Q4_0 it
    // takes its own admission: qwen4exp's 640-wide expert down rows fail the
    // 256 gate but are exactly where this type dominates decode. AVX-512 first
    // -- prefill on a low-bit MoE is expert-compute bound, and this type is the
    // down projection of every layer.
    if(type==20&&(flyweight_cpu_features()&2u)!=0&&elements%32==0&&
       qwen_iq_avx512_enabled())
        return qwen_quant_dot_avx512(packed,type,input,elements,row);
    if(type==20&&(flyweight_cpu_features()&1u)!=0&&elements%32==0)
        return qwen_quant_dot_avx2(packed,type,input,elements,row);
    if(qwen_simd_quant_type(type)&&(flyweight_cpu_features()&1u)!=0&&elements%kBlockElements==0)return qwen_quant_dot_avx2(packed,type,input,elements,row);
    float result=0.0f;
    if(type==2){
        const auto*row_data=packed+row*static_cast<std::uint64_t>(elements/32)*18;
        for(int block=0;block<elements/32;++block){const auto*base=row_data+block*18;std::uint16_t scale_bits=0;std::memcpy(&scale_bits,base,2);const float scale=qwen_half_value(scale_bits);const auto*vector=input+block*32;for(int lane=0;lane<16;++lane){const auto byte=base[2+lane];result+=scale*((byte&15)-8)*vector[lane];result+=scale*((byte>>4)-8)*vector[lane+16];}}
    }else if(type==13){
        const auto*row_data=packed+row*static_cast<std::uint64_t>(elements/kBlockElements)*kQ5KBlockSize;
        for(int block=0;block<elements/256;++block){
            const auto*base=row_data+block*kQ5KBlockSize;std::uint16_t d_bits=0,dmin_bits=0;
            std::memcpy(&d_bits,base,2);std::memcpy(&dmin_bits,base+2,2);
            const float d=qwen_half_value(d_bits),dmin=qwen_half_value(dmin_bits);
            const auto*scales=base+4;const auto*high=base+16;const auto*low=base+48;
            const auto*vector=input+block*256;
            for(int group=0;group<4;++group)for(int sub=0;sub<2;++sub){
                const int scale_index=group*2+sub;int scale=0,minimum=0;
                if(scale_index<4){scale=scales[scale_index]&63;minimum=scales[scale_index+4]&63;}
                else{scale=(scales[scale_index+4]&15)|((scales[scale_index-4]>>6)<<4);minimum=(scales[scale_index+4]>>4)|((scales[scale_index]>>6)<<4);}
                const float ds=d*scale,dm=dmin*minimum;const int shift=2*group+sub;
                const auto*values=vector+group*64+sub*32;const auto*quants=low+group*32;
                for(int lane=0;lane<32;++lane){const int quant=((sub==0)?(quants[lane]&15):(quants[lane]>>4))+16*((high[lane]>>shift)&1);result+=(ds*quant-dm)*values[lane];}
            }
        }
    }else if(type==14){
        const auto*row_data=packed+row*static_cast<std::uint64_t>(elements/kBlockElements)*kQ6KBlockSize;
        for(int block=0;block<elements/256;++block){
            const auto*base=row_data+block*kQ6KBlockSize;const auto*ql=base;const auto*qh=base+128;
            const auto*scales=reinterpret_cast<const std::int8_t*>(base+192);std::uint16_t d_bits=0;
            std::memcpy(&d_bits,base+208,2);const float d=qwen_half_value(d_bits);const auto*vector=input+block*256;
            for(int half=0;half<2;++half)for(int segment=0;segment<4;++segment){
                const int q_offset=(segment==0||segment==2)?0:32;const int shift=segment*2;
                const auto*values=vector+half*128+segment*32;
                for(int lane=0;lane<32;++lane){const auto qbyte=ql[half*64+q_offset+lane];const int nibble=(segment<2)?(qbyte&15):(qbyte>>4);const int quant=(nibble|(((qh[half*32+lane]>>shift)&3)<<4))-32;const int scale_index=half*8+(lane/16)+segment*2;result+=d*scales[scale_index]*quant*values[lane];}
            }
        }
    }else if(type==8){
        const auto*row_data=packed+row*static_cast<std::uint64_t>(elements/32)*kQ8BlockSize;
        for(int block=0;block<elements/32;++block){const auto*base=row_data+block*kQ8BlockSize;std::uint16_t scale_bits=0;std::memcpy(&scale_bits,base,2);const float scale=qwen_half_value(scale_bits);const auto*values=reinterpret_cast<const std::int8_t*>(base+2);const auto*vector=input+block*32;for(int lane=0;lane<32;++lane)result+=scale*values[lane]*vector[lane];}
    }else if(type==12){
        const auto*row_data=packed+row*static_cast<std::uint64_t>(elements/kBlockElements)*kQ4KBlockSize;
        for(int block=0;block<elements/256;++block){
            const auto*base=row_data+block*kQ4KBlockSize;std::uint16_t d_bits=0,dmin_bits=0;
            std::memcpy(&d_bits,base,2);std::memcpy(&dmin_bits,base+2,2);
            const float d=qwen_half_value(d_bits),dmin=qwen_half_value(dmin_bits);
            const auto*scales=base+4;const auto*low=base+16;
            const auto*vector=input+block*256;
            for(int group=0;group<4;++group)for(int sub=0;sub<2;++sub){
                const int scale_index=group*2+sub;int scale=0,minimum=0;
                if(scale_index<4){scale=scales[scale_index]&63;minimum=scales[scale_index+4]&63;}
                else{scale=(scales[scale_index+4]&15)|((scales[scale_index-4]>>6)<<4);minimum=(scales[scale_index+4]>>4)|((scales[scale_index]>>6)<<4);}
                const float ds=d*scale,dm=dmin*minimum;
                const auto*values=vector+group*64+sub*32;const auto*quants=low+group*32;
                for(int lane=0;lane<32;++lane){const int quant=(sub==0)?(quants[lane]&15):(quants[lane]>>4);result+=(ds*quant-dm)*values[lane];}
            }
        }
    }else if(type==10){
        result+=qwen_q2k_dot_row(packed,input,elements,row);
    }else if(type==11){
        result+=qwen_q3k_dot_row(packed,input,elements,row);
    }else if(type==16){
        result+=qwen_iq2xxs_dot_row(packed,input,elements,row);
    }else if(type==18){
        result+=qwen_iq3xxs_dot_row(packed,input,elements,row);
    }else if(type==22){
        result+=qwen_iq2s_dot_row(packed,input,elements,row);
    }else if(type==21){
        result+=qwen_iq3s_dot_row(packed,input,elements,row);
    }else if(type==17){
        result+=qwen_iq2xs_dot_row(packed,input,elements,row);
    }else if(type==23){
        result+=qwen_iq4xs_dot_row(packed,input,elements,row);
    }else if(type==20){
        result+=qwen_iq4nl_dot_row(packed,input,elements,row);
    }else if(type==19){
        result+=qwen_iq1s_dot_row(packed,input,elements,row);
    }else if(type==29){
        result+=qwen_iq1m_dot_row(packed,input,elements,row);
    }else if(type==39){
        // MXFP4: one E8M0 exponent per 32 values, then 16 packed nibbles where
        // byte j holds element j in the low half and element j+16 in the high.
        const int blocks=elements/kMxfp4BlockElements;
        const std::uint64_t row_offset=static_cast<std::uint64_t>(row)*blocks*kMxfp4BlockSize;
        for(int block=0;block<blocks;++block){
            const auto*base=packed+row_offset+block*kMxfp4BlockSize;
            const float scale=mxfp4_scale(base[0]);
            const auto*vector=input+block*kMxfp4BlockElements;
            float partial=0.0f;
            for(int lane=0;lane<16;++lane){
                const auto byte=base[1+lane];
                partial+=kMxfp4Lut[byte&15]*vector[lane]+kMxfp4Lut[byte>>4]*vector[lane+16];
            }
            result+=scale*partial;
        }
    }else if(type==40){
        const int blocks=elements/kNvfp4BlockElements;
        const std::uint64_t row_offset=static_cast<std::uint64_t>(row)*blocks*kNvfp4BlockSize;
        for(int block=0;block<blocks;++block){
            const auto*base=packed+row_offset+block*kNvfp4BlockSize;
            for(int sub=0;sub<4;++sub){
                const float scale=ue4m3_to_float(base[sub]);
                const auto*vector=input+block*kNvfp4BlockElements+sub*kNvfp4SubBlock;
                float partial=0.0f;
                // qs[lane] holds element `lane` in the low nibble, `lane+8` in the high.
                for(int lane=0;lane<8;++lane){const auto byte=base[4+sub*8+lane];partial+=kNvfp4Lut[byte&15]*vector[lane]+kNvfp4Lut[byte>>4]*vector[lane+8];}
                result+=scale*partial;
            }
        }
    }else if(type==30){
        // bf16 experts, as the Qwen3.6 NVFP4 checkpoints ship the nextn (MTP) block.
        if((flyweight_cpu_features()&2u)!=0)return qwen_quant_dot_avx512(packed,type,input,elements,row);
        if((flyweight_cpu_features()&1u)!=0)return qwen_quant_dot_avx2(packed,type,input,elements,row);
        const auto*row_data=reinterpret_cast<const std::uint16_t*>(packed)
            +row*static_cast<std::uint64_t>(elements);
        for(int index=0;index<elements;++index)
            result+=qwen_bf16_value(row_data[index])*input[index];
    }else if(type==1){
        // f16 rows, as an unquantized GGUF conversion stores every matrix.
        if((flyweight_cpu_features()&2u)!=0)return qwen_quant_dot_avx512(packed,type,input,elements,row);
        if((flyweight_cpu_features()&1u)!=0)return qwen_quant_dot_avx2(packed,type,input,elements,row);
        const auto*row_data=packed+row*static_cast<std::uint64_t>(elements)*2;
        for(int index=0;index<elements;++index){
            std::uint16_t bits=0;std::memcpy(&bits,row_data+index*2,2);
            result+=qwen_half_value(bits)*input[index];
        }
    }else if(type==0){
        // Unquantized weights, as the dense host feed-forward can be handed.
        // qwen_f32_dot_multi already has the vectorized single-row case.
        const auto*row_data=reinterpret_cast<const float*>(packed)
            +row*static_cast<std::uint64_t>(elements);
        if((flyweight_cpu_features()&2u)!=0&&elements%32==0){
            float value=0.0f;qwen_f32_dot_multi_avx512(row_data,&input,1,elements,&value);return value;
        }
        if((flyweight_cpu_features()&1u)!=0&&elements%16==0){
            float value=0.0f;qwen_f32_dot_multi_avx2(row_data,&input,1,elements,&value);return value;
        }
        for(int index=0;index<elements;++index)result+=row_data[index]*input[index];
    }else throw std::runtime_error(
        "unsupported native CPU expert quantization: "+std::to_string(type));
    return result;
}

void qwen_quant_dot_rows(
    const std::uint8_t* packed, std::uint32_t type, const float* input,
    int elements, std::uint64_t first_row, int row_count, float* outputs
) {
    if (type == 12 && elements % kBlockElements == 0
        && row_count >= 1 && row_count <= 4) {
        const auto features = flyweight_cpu_features();
        if ((features & 2u) != 0) {
            qwen_quant_dot_rows_avx512(
                packed, type, input, elements, first_row, row_count, outputs);
            return;
        }
        if ((features & 1u) != 0) {
            qwen_quant_dot_rows_avx2(
                packed, type, input, elements, first_row, row_count, outputs);
            return;
        }
    }
    for (int row = 0; row < row_count; ++row)
        outputs[row] = qwen_quant_dot(
            packed, type, input, elements, first_row + row);
}

void qwen_quant_dot_pair(const std::uint8_t*packed,std::uint32_t type,const float*first,const float*second,int elements,std::uint64_t row,float&first_output,float&second_output){
    if(type==17&&(flyweight_cpu_features()&2u)!=0&&elements%256==0&&
       qwen_iq_avx512_enabled()){
        qwen_quant_dot_pair_avx512(
            packed,type,first,second,elements,row,&first_output,&second_output);
        return;
    }
    if(qwen_simd_multi_type(type)&&(flyweight_cpu_features()&2u)!=0&&elements%kBlockElements==0){qwen_quant_dot_pair_avx512(packed,type,first,second,elements,row,&first_output,&second_output);return;}
    first_output=qwen_quant_dot(packed,type,first,elements,row);second_output=qwen_quant_dot(packed,type,second,elements,row);
}

// Weight types the grouped GPU expert kernels can execute. The IQ codebook
// formats have no grouped kernel: they have to stay on the CPU expert path,
// which decodes every type qwen_quant_dot supports.
// Kernel-name prefix for the IQ codebook formats, which share one generated
// family of grouped expert kernels. Null for everything else.
const char* qwen_iq_kernel_prefix(std::uint32_t type) {
    // Only the formats with a device octet decoder. IQ2_S and IQ3_S pack
    // their signs and grid indices differently and have no grouped kernel,
    // so models using them still route experts to the CPU.
    const auto* format = flyweight::v2::qwen_format(type);
    return format ? format->iq_expert_prefix : nullptr;
}

// Grouped kernel name for an IQ type, empty when the type is not one.
std::string qwen_iq_grouped_kernel(std::uint32_t type, const char* suffix) {
    const char* prefix=qwen_iq_kernel_prefix(type);
    return prefix?std::string(prefix)+suffix:std::string();
}

bool qwen_gpu_expert_type_supported(std::uint32_t type) {
    return type==8||type==12||type==13||type==14||type==40||
           qwen_iq_kernel_prefix(type)!=nullptr;
}

// Grouped SwiGLU kernel for the routed experts' gate/up type. The trailing
// k-quant case is the historical default and stays reachable only for types
// this build actually decodes, because prepare rejects anything else.
// The message for a type qwen_gpu_expert_type_supported rejects. The two name
// lookups below used to answer that case by falling through to Q5_K/Q6_K and
// decoding the bytes as a format they are not; prepare routes such models to
// the CPU, and this makes the silent path unreachable if that stops holding.
std::string qwen_grouped_expert_unsupported(std::uint32_t type) {
    return "no routed-expert kernel decodes weight type "+std::to_string(type)+
        "; the expert stacks have to be Q4_K, Q5_K, Q6_K, Q8_0, NVFP4 or an IQ "
        "codebook type";
}

std::string qwen_grouped_swiglu_name(std::uint32_t type, bool nvfp4_tiled, bool rows) {
    const char* suffix=rows?"_grouped_swiglu_rows":"_grouped_swiglu";
    auto iq=qwen_iq_grouped_kernel(type,suffix);
    if(!iq.empty())return iq;
    if(type==40)return rows?"nvfp4_grouped_swiglu_rows"
        :(nvfp4_tiled?"nvfp4_grouped_swiglu_tiled":"nvfp4_grouped_swiglu");
    if(type==8)return rows?"q8_grouped_swiglu_rows":"q8_grouped_swiglu";
    if(type==14)return rows?"q6k_grouped_swiglu_rows":"q6k_grouped_swiglu";
    if(type==12)return rows?"q4k_grouped_swiglu_rows":"q4k_grouped_swiglu";
    if(type!=13)throw std::runtime_error(qwen_grouped_expert_unsupported(type));
    return rows?"q5k_grouped_swiglu_rows":"q5k_grouped_swiglu";
}

// Row-batched grouped accumulate, which the prefill path launches by name for
// every weight type rather than through a driver entry point.
std::string qwen_grouped_accumulate_rows_name(std::uint32_t type) {
    auto iq=qwen_iq_grouped_kernel(type,"_grouped_accumulate_rows");
    if(!iq.empty())return iq;
    if(type==8)return "q8_grouped_accumulate_rows";
    if(type==40)return "nvfp4_grouped_accumulate_rows";
    if(type==12)return "q4k_grouped_accumulate_rows";
    if(type==13)return "q5k_grouped_accumulate_rows";
    if(type!=14)throw std::runtime_error(qwen_grouped_expert_unsupported(type));
    return "q6k_grouped_accumulate_rows";
}

// The k-quant and NVFP4 accumulates go through their own driver entry points;
// the IQ family has no such wrapper and launches by name with the identical
// grid, one block per output row.
int qwen_launch_grouped_accumulate(
    std::uint64_t stream, std::uint32_t down_type, std::uint64_t down_table,
    std::uint64_t activated, std::uint64_t output, std::uint64_t weights,
    int intermediate, int hidden_size, int count
) {
    const auto iq=qwen_iq_grouped_kernel(down_type,"_grouped_accumulate");
    if(!iq.empty()){
        void* args[]={&down_table,&activated,&output,&weights,
                      &intermediate,&hidden_size,&count};
        return flyweight_gpu_launch_named(
            iq.c_str(),static_cast<std::uint32_t>(hidden_size),1,256,0,stream,args);
    }
    switch(down_type){
        case 8:return flyweight_gpu_q8_grouped_accumulate(down_table,activated,output,weights,intermediate,hidden_size,count,stream);
        case 40:return flyweight_gpu_nvfp4_grouped_accumulate(down_table,activated,output,weights,intermediate,hidden_size,count,stream);
        case 12:return flyweight_gpu_q4k_grouped_accumulate(down_table,activated,output,weights,intermediate,hidden_size,count,stream);
        case 13:return flyweight_gpu_q5k_grouped_accumulate(down_table,activated,output,weights,intermediate,hidden_size,count,stream);
        case 14:return flyweight_gpu_q6_grouped_accumulate(down_table,activated,output,weights,intermediate,hidden_size,count,stream);
        default:throw std::runtime_error(qwen_grouped_expert_unsupported(down_type));
    }
}

// True when any routed expert tensor uses an IQ codebook format.
bool qwen_model_has_iq_experts(const FlyweightV2QwenRuntime& runtime) {
    for(const auto& layer:runtime.layers){
        if(layer.dense_ffn||!layer.expert_tensors[0])continue;
        for(const auto index:layer.expert_tensors)
            if(qwen_iq_kernel_prefix(runtime.model->tensors[index].type))return true;
    }
    return false;
}

// True when every routed expert tensor in the model can run on the GPU.
bool qwen_gpu_experts_executable(const FlyweightV2QwenRuntime& runtime) {
    // Gemma 4's hybrid decode has its own grouped kernels for the fused Q4_0
    // gate_up bundle and its f32 expert scales; the generic grouped dispatch
    // knows neither, so testing its type list here parked a cache the decode
    // path could actually serve.
    if(runtime.gemma4){
        for(const auto& layer:runtime.layers){
            if(layer.dense_ffn||!layer.expert_tensors[0])continue;
            if(runtime.model->tensors[layer.expert_tensors[0]].type!=2||
               runtime.model->tensors[layer.expert_tensors[1]].type!=2||
               runtime.model->tensors[layer.expert_tensors[2]].type!=0)
                return false;
        }
        return true;
    }
    for(const auto& layer:runtime.layers){
        if(layer.dense_ffn||!layer.expert_tensors[0])continue;
        for(const auto index:layer.expert_tensors)
            if(!qwen_gpu_expert_type_supported(runtime.model->tensors[index].type))
                return false;
    }
    return true;
}

// Weight types the CPU expert path can execute, which is exactly what
// qwen_quant_dot decodes. The IQ codebook formats matter for the published
// low-bit MoE checkpoints: their routed experts are IQ2/IQ3/IQ4 even when the
// dense projections are k-quants.
bool qwen_cpu_expert_type_supported(std::uint32_t type) {
    const auto* format = flyweight::v2::qwen_format(type);
    return format && format->cpu_expert;
}

void qwen_quant_dot_two_rows(
    const std::uint8_t* first_matrix,
    const std::uint8_t* second_matrix,
    std::uint32_t type,
    const float* input,
    int elements,
    std::uint64_t row,
    float& first_output,
    float& second_output
) {
    std::uint64_t row_bytes = 0;
    if (type == 12) {
        row_bytes = static_cast<std::uint64_t>(elements / kBlockElements) * kQ4KBlockSize;
    } else if (type == 13) {
        row_bytes = static_cast<std::uint64_t>(elements / kBlockElements) * kQ5KBlockSize;
    } else if (type == 14) {
        row_bytes = static_cast<std::uint64_t>(elements / kBlockElements) * kQ6KBlockSize;
    } else if (type == 17) {
        row_bytes = static_cast<std::uint64_t>(elements / 256) * kIq2xsBlockBytes;
    } else if (type == 8) {
        row_bytes = static_cast<std::uint64_t>(elements / 32) * kQ8BlockSize;
    } else if (type == 40) {
        row_bytes = static_cast<std::uint64_t>(elements / kNvfp4BlockElements) * kNvfp4BlockSize;
    } else if (type == 30) {
        row_bytes = static_cast<std::uint64_t>(elements) * 2;
    }
    const auto* first_row = first_matrix + row * row_bytes;
    const auto* second_row = second_matrix + row * row_bytes;
    if ((flyweight_cpu_features() & 2u) != 0) {
        const bool supported =
            (type == 8 && elements % 32 == 0)
            || (type == 17 && qwen_iq_avx512_enabled() && elements % 256 == 0)
            || ((type == 12 || type == 13 || type == 14) && elements % 256 == 0);
        if (supported) {
            qwen_quant_dot_two_rows_avx512(
                first_row, second_row, type, input, elements,
                &first_output, &second_output
            );
            return;
        }
    }
    first_output = qwen_quant_dot(first_matrix, type, input, elements, row);
    second_output = qwen_quant_dot(second_matrix, type, input, elements, row);
}

#if defined(_OPENMP)
int qwen_cpu_thread_count(const FlyweightV2QwenRuntime& runtime) {
    if (runtime.options.cpu_threads)
        return std::min<int>(runtime.options.cpu_threads, omp_get_num_procs());
    int team = omp_get_max_threads();
    if (std::getenv("OMP_NUM_THREADS") == nullptr) {
        const int physical = omp_get_num_procs() / 2;
        if (physical >= 1 && team > physical) team = physical;
    }
    return team;
}
#endif

// Q8 activations for the Q4_0 expert dots: quantize the shared input once and
// run integer VNNI dots against the raw nibbles. Off-switch because the
// activation quantization changes the numerics (the checkpoint is QAT'd for
// Q4_0 weights, and the measured output stays intact -- see the A/B before
// this landed).
bool gemma_q8_activations_enabled(){
    static const bool enabled=[]{
        const char*setting=std::getenv("FLYWEIGHT_GEMMA_Q8_ACTIVATIONS");
        return (flyweight_cpu_features()&4u)!=0&&(!setting||setting[0]!='0');
    }();
    return enabled;
}

// Expert GEMM for the chunked prefill: repacked Q4_0 weights and 8 output
// rows per dpbusd instead of the 8-token multi dots. Opt-in while the A/B
// coverage grows; requires AVX512-VNNI (feature bit 3). Same quantized
// activations as the q8 path, so the only numeric difference is float
// summation order inside a block dot.
bool gemma_expert_gemm_enabled(){
    static const bool enabled=[]{
        const char*setting=std::getenv("FLYWEIGHT_GEMMA_EXPERT_GEMM");
        return setting&&setting[0]=='1'&&(flyweight_cpu_features()&8u)!=0;
    }();
    return enabled;
}

void gemma_cpu_moe(const FlyweightV2QwenRuntime&runtime,const QwenLayerPlan&layer,
        const std::int32_t*selected,const float*weights,int routed_count,const float*input,
        float*activated,float*output){
    const int experts=runtime.model->config.expert_count;
    const int hidden=runtime.model->config.hidden_size,intermediate=runtime.moe_intermediate;
    const auto&gate_up_tensor=runtime.model->tensors[layer.expert_tensors[0]];
    const auto&down_tensor=runtime.model->tensors[layer.expert_tensors[1]];
    const auto&scale_tensor=runtime.model->tensors[layer.expert_tensors[2]];
    if(gate_up_tensor.type!=2||down_tensor.type!=2||scale_tensor.type!=0)
        throw std::runtime_error("native Gemma 4 expects Q4_0 experts and f32 expert scales");
    const auto gate_up_bytes=gate_up_tensor.size/experts,down_bytes=down_tensor.size/experts;
    const auto*expert_scales=reinterpret_cast<const float*>(tensor_data(*runtime.model,scale_tensor));
    const bool use_q8=gemma_q8_activations_enabled()&&hidden%32==0&&intermediate%32==0;
    thread_local std::vector<QwenQ80Block> input_q8,activated_q8;
    if(use_q8){
        input_q8.resize(hidden/32);
        qwen_quantize_q8_0(input,hidden,input_q8.data());
    }
    const auto*input_q8_data=input_q8.data();
    // Every task is the same two fixed-width dots, so static scheduling costs
    // nothing in balance and removes the chunk-queue contention that made this
    // loop run SLOWER with more threads (measured 40ms/token at 16 threads
    // against 21ms at 8 with dynamic,4 on a 16-core Zen 5).
    #pragma omp parallel for schedule(static) num_threads(qwen_cpu_thread_count(runtime))
    for(int task=0;task<routed_count*intermediate;++task){
        const int rank=task/intermediate,row=task%intermediate,expert=selected[rank];
        if(expert<0||expert>=experts)continue;
        const auto*gate_up=tensor_data(*runtime.model,gate_up_tensor)+static_cast<std::uint64_t>(expert)*gate_up_bytes;
        const float gate=use_q8
            ?qwen_quant_dot_q4_0_q8_0_vnni(gate_up,input_q8_data,hidden,row)
            :qwen_quant_dot(gate_up,2,input,hidden,row);
        const float up=use_q8
            ?qwen_quant_dot_q4_0_q8_0_vnni(gate_up,input_q8_data,hidden,row+intermediate)
            :qwen_quant_dot(gate_up,2,input,hidden,row+intermediate);
        const float cubic=gate*gate*gate;
        const float gelu=0.5f*gate*(1.0f+std::tanh(0.7978845608028654f*(gate+0.044715f*cubic)));
        activated[task]=gelu*up;
    }
    if(use_q8){
        activated_q8.resize(static_cast<std::size_t>(routed_count)*(intermediate/32));
        for(int rank=0;rank<routed_count;++rank)
            qwen_quantize_q8_0(activated+rank*intermediate,intermediate,
                               activated_q8.data()+static_cast<std::size_t>(rank)*(intermediate/32));
    }
    const auto*activated_q8_data=activated_q8.data();
    #pragma omp parallel for schedule(static) num_threads(qwen_cpu_thread_count(runtime))
    for(int row=0;row<hidden;++row){
        float sum=0.0f;
        for(int rank=0;rank<routed_count;++rank){
            const int expert=selected[rank];
            if(expert<0||expert>=experts)continue;
            const auto*down=tensor_data(*runtime.model,down_tensor)+static_cast<std::uint64_t>(expert)*down_bytes;
            const float expert_value=use_q8
                ?qwen_quant_dot_q4_0_q8_0_vnni(down,
                    activated_q8_data+static_cast<std::size_t>(rank)*(intermediate/32),
                    intermediate,row)
                :qwen_quant_dot(down,2,activated+rank*intermediate,intermediate,row);
            sum+=weights[rank]*expert_scales[expert]*expert_value;
        }
        output[row]=sum;
    }
}

// The same expert math over a whole prefill chunk, gathered by expert so each
// routed expert's weights stream from RAM once per chunk instead of once per
// token. Weight-row outer, token inner: the 1.6 KB quantized row stays in L1
// across every token routed to it, which is where the chunk's bandwidth win
// actually comes from. `selected`/`weights`/`inputs`/`outputs` are the pinned
// rows staging regions; the hot loops re-read only cacheable mirrors because
// repeated reads against page-locked memory measure ~5x slower (see
// qwen_cpu_dense_ffn).
void gemma_cpu_moe_rows(const FlyweightV2QwenRuntime&runtime,const QwenLayerPlan&layer,
        int rows,const std::int32_t*selected,const float*weights,
        const float*inputs,float*outputs){
    const int experts=runtime.model->config.expert_count;
    const int top_k=static_cast<int>(runtime.model->config.expert_used_count);
    const int hidden=runtime.model->config.hidden_size,intermediate=runtime.moe_intermediate;
    const auto&gate_up_tensor=runtime.model->tensors[layer.expert_tensors[0]];
    const auto&down_tensor=runtime.model->tensors[layer.expert_tensors[1]];
    const auto&scale_tensor=runtime.model->tensors[layer.expert_tensors[2]];
    if(gate_up_tensor.type!=2||down_tensor.type!=2||scale_tensor.type!=0)
        throw std::runtime_error("native Gemma 4 expects Q4_0 experts and f32 expert scales");
    const auto gate_up_bytes=gate_up_tensor.size/experts,down_bytes=down_tensor.size/experts;
    const auto*expert_scales=reinterpret_cast<const float*>(tensor_data(*runtime.model,scale_tensor));
    std::vector<std::vector<std::pair<int,int>>> gathered(experts);
    for(int row=0;row<rows;++row)for(int rank=0;rank<top_k;++rank){
        const int expert=selected[row*top_k+rank];
        if(expert>=0&&expert<experts)gathered[expert].push_back({row,rank});
    }
    std::vector<int> used;
    for(int expert=0;expert<experts;++expert)
        if(!gathered[expert].empty())used.push_back(expert);
    auto&scratch=const_cast<FlyweightV2QwenRuntime&>(runtime).dense_scratch;
    const std::size_t input_floats=static_cast<std::size_t>(rows)*hidden;
    const std::size_t activated_floats=static_cast<std::size_t>(rows)*top_k*intermediate;
    const std::size_t partial_floats=static_cast<std::size_t>(rows)*top_k*hidden;
    scratch.resize(input_floats+activated_floats+partial_floats);
    float*local_inputs=scratch.data();
    float*activated=local_inputs+input_floats;
    float*partial=activated+activated_floats;
    std::memcpy(local_inputs,inputs,input_floats*sizeof(float));
    // A routing gap (an invalid expert) would leave its partial slice unread
    // by the expert loop but still summed by the reduction below.
    std::fill(partial,partial+partial_floats,0.0f);
    const bool use_q8=gemma_q8_activations_enabled()&&hidden%32==0&&intermediate%32==0;
    const std::size_t input_blocks=static_cast<std::size_t>(hidden)/32;
    const std::size_t activated_blocks=static_cast<std::size_t>(intermediate)/32;
    const bool use_gemm=use_q8&&gemma_expert_gemm_enabled()&&
        (2*intermediate)%8==0&&hidden%8==0;
    const std::uint8_t*gate_up_repacked=nullptr,*down_repacked=nullptr;
    thread_local std::vector<std::int8_t> gemm_in_values,gemm_act_values;
    thread_local std::vector<float> gemm_in_scales,gemm_act_scales;
    thread_local std::vector<std::int32_t> gemm_in_bsums,gemm_act_bsums;
    if(use_gemm){
        // Lazy per-tensor repack: first chunk through a layer pays one pass
        // over the expert bytes, every later chunk reads the repacked copy.
        // This runs on the prefill driver thread, before the parallel region.
        auto&cache=const_cast<FlyweightV2QwenRuntime&>(runtime).q4_gemm_repack;
        auto repack=[&](std::size_t tensor_index,int weight_rows,int weight_elements)
                ->const std::uint8_t*{
            const auto&tensor=runtime.model->tensors[tensor_index];
            auto&buffer=cache[tensor_index];
            if(buffer.empty()){
                buffer.resize(tensor.size);
                const auto per_expert=tensor.size/experts;
                const auto*source=tensor_data(*runtime.model,tensor);
                #pragma omp parallel for schedule(static) num_threads(qwen_cpu_thread_count(runtime))
                for(int expert=0;expert<experts;++expert)
                    qwen_q4_0_repack_x8(
                        source+static_cast<std::uint64_t>(expert)*per_expert,
                        weight_rows,weight_elements,
                        buffer.data()+static_cast<std::uint64_t>(expert)*per_expert);
            }
            return buffer.data();
        };
        gate_up_repacked=repack(layer.expert_tensors[0],2*intermediate,hidden);
        down_repacked=repack(layer.expert_tensors[1],hidden,intermediate);
        gemm_in_values.resize(static_cast<std::size_t>(rows)*hidden);
        gemm_in_scales.resize(static_cast<std::size_t>(rows)*input_blocks);
        gemm_in_bsums.resize(static_cast<std::size_t>(rows)*input_blocks);
        gemm_act_values.resize(static_cast<std::size_t>(rows)*top_k*intermediate);
        gemm_act_scales.resize(static_cast<std::size_t>(rows)*top_k*activated_blocks);
        gemm_act_bsums.resize(static_cast<std::size_t>(rows)*top_k*activated_blocks);
    }
    // Raw views taken on the driver thread: the vectors above are
    // thread_local, so the parallel workers must reach the storage through
    // these shared pointers rather than the names.
    std::int8_t*in_values=gemm_in_values.data();
    std::int8_t*act_values=gemm_act_values.data();
    float*in_scales=gemm_in_scales.data();
    float*act_scales=gemm_act_scales.data();
    std::int32_t*in_bsums=gemm_in_bsums.data();
    std::int32_t*act_bsums=gemm_act_bsums.data();
    std::vector<QwenQ80Block> input_q8,activated_q8;
    if(use_q8&&!use_gemm){
        input_q8.resize(static_cast<std::size_t>(rows)*input_blocks);
        activated_q8.resize(static_cast<std::size_t>(rows)*top_k*activated_blocks);
    }
    auto*input_q8_data=input_q8.data();
    auto*activated_q8_data=activated_q8.data();
    #pragma omp parallel num_threads(qwen_cpu_thread_count(runtime))
    {
        if(use_gemm){
            #pragma omp for schedule(static)
            for(int row=0;row<rows;++row)
                qwen_quantize_q8_gemm(
                    local_inputs+static_cast<std::size_t>(row)*hidden,hidden,
                    in_values+static_cast<std::size_t>(row)*hidden,
                    in_scales+row*input_blocks,in_bsums+row*input_blocks);
        }else if(use_q8){
            // Each row's input quantizes once and serves every expert routed
            // to it; the implicit barrier orders this before the expert loop.
            #pragma omp for schedule(static)
            for(int row=0;row<rows;++row)
                qwen_quantize_q8_0(local_inputs+static_cast<std::size_t>(row)*hidden,
                                   hidden,input_q8_data+row*input_blocks);
        }
        // Experts carry work proportional to how many rows routed to them, so
        // this one stays dynamic; the uniform reduction below is static.
        #pragma omp for schedule(dynamic,1)
        for(std::size_t index=0;index<used.size();++index){
            const int expert=used[index];
            const auto*gate_up=tensor_data(*runtime.model,gate_up_tensor)+
                static_cast<std::uint64_t>(expert)*gate_up_bytes;
            const auto*down=tensor_data(*runtime.model,down_tensor)+
                static_cast<std::uint64_t>(expert)*down_bytes;
            // The q8 branch batches up to 8 routed tokens through one weight
            // row so the nibble decode amortizes and the accumulator chains
            // stay independent; the f32 branch keeps the original per-pair
            // dots.
            const auto&pairs=gathered[expert];
            std::array<const QwenQ80Block*,8> streams{};
            std::array<float,8> gate_values{},up_values{};
            if(use_gemm){
                const int tokens=static_cast<int>(pairs.size());
                thread_local std::vector<const std::int8_t*> value_pointers;
                thread_local std::vector<const float*> scale_pointers;
                thread_local std::vector<const std::int32_t*> bsum_pointers;
                thread_local std::vector<float*> output_pointers;
                thread_local std::vector<float> gate_up_out;
                value_pointers.resize(tokens);scale_pointers.resize(tokens);
                bsum_pointers.resize(tokens);output_pointers.resize(tokens);
                gate_up_out.resize(static_cast<std::size_t>(tokens)*2*intermediate);
                for(int t=0;t<tokens;++t){
                    const std::size_t row=pairs[t].first;
                    value_pointers[t]=in_values+row*hidden;
                    scale_pointers[t]=in_scales+row*input_blocks;
                    bsum_pointers[t]=in_bsums+row*input_blocks;
                    output_pointers[t]=gate_up_out.data()+
                        static_cast<std::size_t>(t)*2*intermediate;
                }
                qwen_q4_0x8_q8_gemm_vnni512(
                    gate_up_repacked+static_cast<std::uint64_t>(expert)*gate_up_bytes,
                    2*intermediate,hidden,value_pointers.data(),
                    scale_pointers.data(),bsum_pointers.data(),tokens,
                    output_pointers.data());
                for(int t=0;t<tokens;++t){
                    const float*out=output_pointers[t];
                    const std::size_t slice=static_cast<std::size_t>(
                        pairs[t].first)*top_k+pairs[t].second;
                    float*slot=activated+slice*intermediate;
                    for(int g=0;g<intermediate;++g){
                        const float gate=out[g],up=out[g+intermediate];
                        const float cubic=gate*gate*gate;
                        const float gelu=0.5f*gate*(1.0f+std::tanh(
                            0.7978845608028654f*(gate+0.044715f*cubic)));
                        slot[g]=gelu*up;
                    }
                    qwen_quantize_q8_gemm(slot,intermediate,
                        act_values+slice*intermediate,
                        act_scales+slice*activated_blocks,
                        act_bsums+slice*activated_blocks);
                    value_pointers[t]=act_values+slice*intermediate;
                    scale_pointers[t]=act_scales+slice*activated_blocks;
                    bsum_pointers[t]=act_bsums+slice*activated_blocks;
                    output_pointers[t]=partial+slice*hidden;
                }
                qwen_q4_0x8_q8_gemm_vnni512(
                    down_repacked+static_cast<std::uint64_t>(expert)*down_bytes,
                    hidden,intermediate,value_pointers.data(),
                    scale_pointers.data(),bsum_pointers.data(),tokens,
                    output_pointers.data());
            }else if(use_q8){
                for(std::size_t first=0;first<pairs.size();first+=8){
                    const int batch=static_cast<int>(
                        std::min<std::size_t>(8,pairs.size()-first));
                    for(int t=0;t<batch;++t)
                        streams[t]=input_q8_data+static_cast<std::size_t>(
                            pairs[first+t].first)*input_blocks;
                    for(int g=0;g<intermediate;++g){
                        qwen_quant_dot_q4_0_q8_0_multi_vnni(
                            gate_up,streams.data(),batch,hidden,g,gate_values.data());
                        qwen_quant_dot_q4_0_q8_0_multi_vnni(
                            gate_up,streams.data(),batch,hidden,g+intermediate,up_values.data());
                        for(int t=0;t<batch;++t){
                            const float gate=gate_values[t],up=up_values[t];
                            const float cubic=gate*gate*gate;
                            const float gelu=0.5f*gate*(1.0f+std::tanh(
                                0.7978845608028654f*(gate+0.044715f*cubic)));
                            const auto&pair=pairs[first+t];
                            activated[(static_cast<std::size_t>(pair.first)*top_k+
                                       pair.second)*intermediate+g]=gelu*up;
                        }
                    }
                }
                // A (row, rank) pair belongs to exactly one expert, so these
                // slices are complete here and disjoint across the parallel
                // loop.
                for(const auto&pair:pairs){
                    const std::size_t slice=static_cast<std::size_t>(pair.first)*top_k+pair.second;
                    qwen_quantize_q8_0(activated+slice*intermediate,intermediate,
                                       activated_q8_data+slice*activated_blocks);
                }
                for(std::size_t first=0;first<pairs.size();first+=8){
                    const int batch=static_cast<int>(
                        std::min<std::size_t>(8,pairs.size()-first));
                    for(int t=0;t<batch;++t)
                        streams[t]=activated_q8_data+(static_cast<std::size_t>(
                            pairs[first+t].first)*top_k+pairs[first+t].second)*activated_blocks;
                    for(int h=0;h<hidden;++h){
                        qwen_quant_dot_q4_0_q8_0_multi_vnni(
                            down,streams.data(),batch,intermediate,h,gate_values.data());
                        for(int t=0;t<batch;++t){
                            const auto&pair=pairs[first+t];
                            partial[(static_cast<std::size_t>(pair.first)*top_k+
                                     pair.second)*hidden+h]=gate_values[t];
                        }
                    }
                }
            }else{
                for(int g=0;g<intermediate;++g){
                    for(const auto&pair:pairs){
                        const float*input=local_inputs+
                            static_cast<std::size_t>(pair.first)*hidden;
                        const float gate=qwen_quant_dot(gate_up,2,input,hidden,g);
                        const float up=qwen_quant_dot(gate_up,2,input,hidden,g+intermediate);
                        const float cubic=gate*gate*gate;
                        const float gelu=0.5f*gate*(1.0f+std::tanh(
                            0.7978845608028654f*(gate+0.044715f*cubic)));
                        activated[(static_cast<std::size_t>(pair.first)*top_k+
                                   pair.second)*intermediate+g]=gelu*up;
                    }
                }
                for(int h=0;h<hidden;++h){
                    for(const auto&pair:pairs){
                        const float*acts=activated+(static_cast<std::size_t>(
                            pair.first)*top_k+pair.second)*intermediate;
                        partial[(static_cast<std::size_t>(pair.first)*top_k+
                                 pair.second)*hidden+h]=
                            qwen_quant_dot(down,2,acts,intermediate,h);
                    }
                }
            }
            // The routing weight and expert scale fold in afterwards, once per
            // element instead of once per dot, and reading the pinned weights
            // staging only here rather than from the hot loop above.
            for(const auto&pair:gathered[expert]){
                const float folded=weights[pair.first*top_k+pair.second]*
                    expert_scales[expert];
                float*slice=partial+(static_cast<std::size_t>(pair.first)*top_k+
                                     pair.second)*hidden;
                for(int h=0;h<hidden;++h)slice[h]*=folded;
            }
        }
        #pragma omp for schedule(static)
        for(int row=0;row<rows;++row){
            float*output=outputs+static_cast<std::size_t>(row)*hidden;
            for(int h=0;h<hidden;++h){
                float sum=0.0f;
                for(int rank=0;rank<top_k;++rank)
                    sum+=partial[(static_cast<std::size_t>(row)*top_k+rank)*hidden+h];
                output[h]=sum;
            }
        }
    }
}

// Host-side dense SwiGLU for a block whose feed-forward weights stayed in the
// mapping instead of the GPU arena. Reads the quantized bytes directly, so it
// costs no VRAM at all -- this is what lets a dense model exceed the card.
// Weights for a spilled block's feed-forward as the host should read them:
// the Q8_0 re-encoding when prepare made one, otherwise the checkpoint bytes.
const std::uint8_t* qwen_host_ffn_weights(
    const FlyweightV2QwenRuntime& runtime, std::size_t index, std::uint32_t& type
) {
    const auto found = runtime.host_ffn_q8.find(index);
    if (found != runtime.host_ffn_q8.end()) {
        type = runtime.host_ffn_q8_type;
        return found->second.data();
    }
    const auto& tensor = runtime.model->tensors[index];
    type = tensor.type;
    return tensor_data(*runtime.model, tensor);
}

void qwen_cpu_dense_ffn(
    FlyweightV2QwenRuntime& runtime, const QwenLayerPlan& layer,
    const float* input, float* output
) {
    const int hidden=runtime.model->config.hidden_size;
    const int intermediate=static_cast<int>(runtime.moe_intermediate);
    const std::size_t ffn_base=qwen_ffn_base(runtime,layer);
    std::uint32_t gate_type=0,up_type=0,down_type=0;
    const auto*gate_data=qwen_host_ffn_weights(
        runtime,layer.static_tensors[ffn_base+1],gate_type);
    const auto*up_data=qwen_host_ffn_weights(
        runtime,layer.static_tensors[ffn_base+2],up_type);
    const auto*down_data=qwen_host_ffn_weights(
        runtime,layer.static_tensors[ffn_base+3],down_type);
    // The pinned buffers the caller hands over are DMA staging, and every
    // output row re-reads the whole activation vector -- roughly 700 MiB of
    // re-reads per block. Doing that against page-locked memory costs ~5x, so
    // the vectors are mirrored into ordinary cacheable scratch first.
    runtime.dense_scratch.resize(static_cast<std::size_t>(hidden)+intermediate);
    float*local_input=runtime.dense_scratch.data();
    float*activated=local_input+hidden;
    std::memcpy(local_input,input,static_cast<std::size_t>(hidden)*sizeof(float));
    #pragma omp parallel for schedule(static) num_threads(qwen_cpu_thread_count(runtime))
    for(int row=0;row<intermediate;++row){
        const float gate=qwen_quant_dot(gate_data,gate_type,local_input,hidden,row);
        const float up=qwen_quant_dot(up_data,up_type,local_input,hidden,row);
        activated[row]=gate/(1.0f+std::exp(-std::min(80.0f,std::max(-80.0f,gate))))*up;
    }
    #pragma omp parallel for schedule(static) num_threads(qwen_cpu_thread_count(runtime))
    for(int row=0;row<hidden;++row)
        output[row]=qwen_quant_dot(down_data,down_type,activated,intermediate,row);
}

void qwen_dequant_row(const std::uint8_t*packed,std::uint32_t type,int elements,
                      std::uint64_t row,float*output);

// The same SwiGLU over a whole prefill batch, decoding each weight row once.
//
// The single-row form above is right for decode, where there is one token and
// nothing to amortize. Prefill is the opposite: the device path reads each
// weight once per 64-token tile, and calling the single-row form in a loop left
// a spilled block re-reading all 88 MB of its weights *per token*. That is the
// whole reason a spilled block costs ~3.4 ms/token against 0.003 ms for a
// resident one -- a factor of a thousand, from batching alone, on top of the
// ~8x the host's 51 GB/s gives up against the device's 391 GB/s.
//
// Here the loop is inverted: a weight row is dequantized once into per-thread
// scratch and then dotted against every token in the batch. The weight traffic
// drops by the batch size and the arithmetic turns into a GEMM, which the host
// is far better at than a stream of GEMVs.
void qwen_cpu_dense_ffn_rows(
    FlyweightV2QwenRuntime& runtime, const QwenLayerPlan& layer,
    const float* input, float* output, int rows
) {
    if(rows<=0)return;
    const int hidden=runtime.model->config.hidden_size;
    const int intermediate=static_cast<int>(runtime.moe_intermediate);
    const std::size_t ffn_base=qwen_ffn_base(runtime,layer);
    std::uint32_t gate_type=0,up_type=0,down_type=0;
    const auto*gate_data=qwen_host_ffn_weights(
        runtime,layer.static_tensors[ffn_base+1],gate_type);
    const auto*up_data=qwen_host_ffn_weights(
        runtime,layer.static_tensors[ffn_base+2],up_type);
    const auto*down_data=qwen_host_ffn_weights(
        runtime,layer.static_tensors[ffn_base+3],down_type);
    const std::size_t batch=static_cast<std::size_t>(rows);

    // Batching pays only where a weight row can be *decoded* quickly. The
    // K-quants have a SIMD dequantizer; the IQ codebook formats do not, and
    // their per-element accessor recomputes the block base, the scale word and
    // the sign lookup for every value -- an order of magnitude worse than
    // qwen_quant_dot, which reaches the fused row kernel in qwen_kquant.h.
    // Decoding once per batch is no bargain when the decode is that much
    // dearer, so those formats keep the row-at-a-time path. Measured on a
    // 14-block spill at IQ3_XXS: 903 ms/token through the batched form against
    // 72 ms ordinary.
    if(!qwen_simd_quant_type(gate_type)||
       !qwen_simd_quant_type(up_type)||
       !qwen_simd_quant_type(down_type)){
        for(std::size_t token=0;token<batch;++token)
            qwen_cpu_dense_ffn(runtime,layer,
                               input+token*hidden,output+token*hidden);
        return;
    }

    // Mirrored out of the pinned staging buffer for the same reason the
    // single-row path does it: every weight row re-reads the activations, and
    // page-locked memory is ~5x slower to read repeatedly.
    runtime.dense_scratch.resize(batch*(static_cast<std::size_t>(hidden)+intermediate));
    float*local_input=runtime.dense_scratch.data();
    float*activated=local_input+batch*hidden;
    std::memcpy(local_input,input,batch*hidden*sizeof(float));

    // The decoded row is dotted against every token through the same SIMD
    // helper the single-row path uses, which keeps four token vectors in flight
    // per weight load. A scalar inner loop here measured *slower* than the
    // unbatched path it replaces: this translation unit is built for baseline
    // x86-64, so the saving from decoding once is easily lost to a dot product
    // that is not vectorized.
    const bool avx512=(flyweight_cpu_features()&2u)!=0;
    const bool avx2=(flyweight_cpu_features()&1u)!=0;
    auto dot_multi=[&](const float*row,const float*const*inputs,int count,
                       int elements,float*outputs){
        if(avx512&&elements%32==0){
            qwen_f32_dot_multi_avx512(row,inputs,count,elements,outputs);return;
        }
        if(avx2&&elements%16==0){
            qwen_f32_dot_multi_avx2(row,inputs,count,elements,outputs);return;
        }
        for(int token=0;token<count;++token){
            const float*vector=inputs[token];
            float sum=0.0f;
            for(int index=0;index<elements;++index)sum+=row[index]*vector[index];
            outputs[token]=sum;
        }
    };

    std::vector<const float*> input_rows(batch),activated_rows(batch);
    for(std::size_t token=0;token<batch;++token)
        input_rows[token]=local_input+token*hidden;

    const int threads=qwen_cpu_thread_count(runtime);
    #pragma omp parallel num_threads(threads)
    {
        // One decoded row per thread, reused across the batch, plus the dots it
        // produces. Sized to the wider projection so both loops share them.
        std::vector<float> decoded(static_cast<std::size_t>(
            std::max(hidden,intermediate)));
        std::vector<float> gate_dots(batch),up_dots(batch);
        #pragma omp for schedule(static)
        for(int row=0;row<intermediate;++row){
            qwen_dequant_row(gate_data,gate_type,hidden,
                             static_cast<std::uint64_t>(row),decoded.data());
            dot_multi(decoded.data(),input_rows.data(),
                      static_cast<int>(batch),hidden,gate_dots.data());
            qwen_dequant_row(up_data,up_type,hidden,
                             static_cast<std::uint64_t>(row),decoded.data());
            dot_multi(decoded.data(),input_rows.data(),
                      static_cast<int>(batch),hidden,up_dots.data());
            for(std::size_t token=0;token<batch;++token){
                const float gate=gate_dots[token];
                activated[token*intermediate+row]=
                    gate/(1.0f+std::exp(-std::min(80.0f,std::max(-80.0f,gate))))
                    *up_dots[token];
            }
        }
    }
    for(std::size_t token=0;token<batch;++token)
        activated_rows[token]=activated+token*intermediate;
    #pragma omp parallel num_threads(threads)
    {
        std::vector<float> decoded(static_cast<std::size_t>(intermediate));
        std::vector<float> dots(batch);
        #pragma omp for schedule(static)
        for(int row=0;row<hidden;++row){
            qwen_dequant_row(down_data,down_type,intermediate,
                             static_cast<std::uint64_t>(row),decoded.data());
            dot_multi(decoded.data(),activated_rows.data(),
                      static_cast<int>(batch),intermediate,dots.data());
            for(std::size_t token=0;token<batch;++token)
                output[token*hidden+row]=dots[token];
        }
    }
}

// Weight types that fell through every vectorized decoder below to the
// per-element form, one bit each. A set rather than a count because the useful
// question is which format is paying, not how often -- the batched MoE reads it
// to name the offender among a layer's three.
std::atomic<std::uint64_t> g_scalar_dequant_types{0};
constexpr std::uint32_t kScalarDequantTypeLimit = 64;

// Dequantize one weight row to f32 so it can be reused across every token
// routed to the same expert within a batch: the quantized bytes are decoded
// once per batch instead of once per token.
void qwen_dequant_row(const std::uint8_t*packed,std::uint32_t type,int elements,std::uint64_t row,float*output){
    if(qwen_simd_quant_type(type)&&(flyweight_cpu_features()&2u)!=0&&elements%kBlockElements==0){qwen_dequant_row_avx512(packed,type,elements,row,output);return;}
    if(type==40&&(flyweight_cpu_features()&1u)!=0&&elements%kNvfp4BlockElements==0){qwen_dequant_row_avx2(packed,type,elements,row,output);return;}
    // qwen4exp's expert formats: IQ1_S super-blocks, IQ4_NL 32-wide blocks
    // (the 640-wide expert down rows fail a 256 gate). Chunked prefill decodes
    // every routed expert's rows through here once per chunk, so the scalar
    // form was the whole prefill wall.
    // IQ2_XXS belongs here too, and its absence was the wall that remained:
    // the unsloth UD mix puts it on the gate and up of 14 of 48 layers, and
    // those alone were 80% of the CPU expert phase's thread time.
    // 17 and 18 were the same omission one model over: measured 44 GMAC/s
    // against 623-768 for the formats that had a decoder, which is the expert
    // layout Laguna ships.
    if((type==16||type==17||type==18||type==19)&&
       (flyweight_cpu_features()&1u)!=0&&elements%256==0){
        qwen_dequant_row_avx2(packed,type,elements,row,output);return;}
    if(type==20&&(flyweight_cpu_features()&1u)!=0&&elements%32==0){qwen_dequant_row_avx2(packed,type,elements,row,output);return;}
    if(qwen_simd_quant_type(type)&&(flyweight_cpu_features()&1u)!=0&&elements%kBlockElements==0){qwen_dequant_row_avx2(packed,type,elements,row,output);return;}
    // Everything past here re-derives a weight's block, group, scale and grid
    // entry per element. It is correct and roughly 10x slower, and nothing
    // said so: IQ2_XXS sat here for two sessions on 14 of qwen4exp's 48 layers
    // while the prefill work looked for the cost in kernels and dispatch. The
    // counter is read once per batched MoE call, off this path, so a
    // configuration that lands here announces itself instead of just being
    // slow. Relaxed ordering: it is a tripwire, not a synchronizer.
    if(type<kScalarDequantTypeLimit)
        g_scalar_dequant_types.fetch_or(1ull<<type,std::memory_order_relaxed);
    const auto base=row*static_cast<std::uint64_t>(elements);
    if(type==13)for(int index=0;index<elements;++index)output[index]=qwen_q5_value(packed,base+index);
    else if(type==14)for(int index=0;index<elements;++index)output[index]=qwen_q6_value(packed,base+index);
    else if(type==8)for(int index=0;index<elements;++index)output[index]=qwen_q8_value(packed,base+index);
    else if(type==12)for(int index=0;index<elements;++index)output[index]=qwen_q4k_value(packed,base+index);
    else if(type==10)for(int index=0;index<elements;++index)output[index]=qwen_q2k_value(packed,base+index);
    else if(type==11)for(int index=0;index<elements;++index)output[index]=qwen_q3k_value(packed,base+index);
    else if(type==16)for(int index=0;index<elements;++index)output[index]=qwen_iq2xxs_value(packed,base+index);
    else if(type==18)for(int index=0;index<elements;++index)output[index]=qwen_iq3xxs_value(packed,base+index);
    else if(type==22)for(int index=0;index<elements;++index)output[index]=qwen_iq2s_value(packed,base+index);
    else if(type==21)for(int index=0;index<elements;++index)output[index]=qwen_iq3s_value(packed,base+index);
    else if(type==17)for(int index=0;index<elements;++index)output[index]=qwen_iq2xs_value(packed,base+index);
    else if(type==23)for(int index=0;index<elements;++index)output[index]=qwen_iq4xs_value(packed,base+index);
    else if(type==20)for(int index=0;index<elements;++index)output[index]=qwen_iq4nl_value(packed,base+index);
    else if(type==19)for(int index=0;index<elements;++index)output[index]=qwen_iq1s_value(packed,base+index);
    else if(type==40)for(int index=0;index<elements;++index)output[index]=qwen_nvfp4_value(packed,base+index);
    else if(type==30){const auto*row_data=reinterpret_cast<const std::uint16_t*>(packed)+base;for(int index=0;index<elements;++index)output[index]=qwen_bf16_value(row_data[index]);}
    else if(type==1){for(int index=0;index<elements;++index){std::uint16_t bits=0;std::memcpy(&bits,packed+(base+index)*2,2);output[index]=qwen_half_value(bits);}}
    else if(type==0){const auto*row_data=reinterpret_cast<const float*>(packed)+base;for(int index=0;index<elements;++index)output[index]=row_data[index];}
    else throw std::runtime_error("unsupported native CPU expert quantization");
}

void qwen_f32_dot_multi(const float*row,const float*const*inputs,int count,int elements,float*outputs){
    if((flyweight_cpu_features()&2u)!=0&&elements%32==0){qwen_f32_dot_multi_avx512(row,inputs,count,elements,outputs);return;}
    if((flyweight_cpu_features()&1u)!=0&&elements%16==0){qwen_f32_dot_multi_avx2(row,inputs,count,elements,outputs);return;}
    for(int token=0;token<count;++token){
        const float*vector=inputs[token];
        float sum=0.0f;
        for(int index=0;index<elements;++index)sum+=row[index]*vector[index];
        outputs[token]=sum;
    }
}

// Register-blocked expert GEMM over a block of <=4 weight rows: out[i*count+j].
void qwen_f32_gemm_rows(const float*weights,int mr,const float*const*inputs,int count,int elements,float*out){
    if((flyweight_cpu_features()&2u)!=0&&elements%32==0){qwen_f32_gemm_rows_avx512(weights,mr,inputs,count,elements,out);return;}
    if((flyweight_cpu_features()&1u)!=0&&elements%16==0){qwen_f32_gemm_rows_avx2(weights,mr,inputs,count,elements,out);return;}
    for(int i=0;i<mr;++i){
        const float*row=weights+static_cast<std::size_t>(i)*elements;float*o=out+static_cast<std::size_t>(i)*count;
        for(int j=0;j<count;++j){const float*v=inputs[j];float sum=0.0f;for(int k=0;k<elements;++k)sum+=row[k]*v[k];o[j]=sum;}
    }
}

void qwen_quant_dot_quad(
    const std::uint8_t*packed,std::uint32_t type,const float*const inputs[4],
    int elements,std::uint64_t row,float outputs[4]
){
    if(qwen_simd_multi_type(type)&&(flyweight_cpu_features()&2u)!=0&&elements%256==0){
        qwen_quant_dot_quad_avx512(packed,type,inputs,elements,row,outputs);return;
    }
    if(type==40&&(flyweight_cpu_features()&1u)!=0&&elements%64==0){
        qwen_quant_dot_quad_avx2(packed,type,inputs,elements,row,outputs);return;
    }
    if(qwen_simd_multi_type(type)&&(flyweight_cpu_features()&1u)!=0&&elements%256==0){
        qwen_quant_dot_quad_avx2(packed,type,inputs,elements,row,outputs);return;
    }
    if((flyweight_cpu_features()&1u)!=0&&
       qwen_quant_dot_iq_multi_avx2(packed,type,inputs,4,elements,row,outputs))return;
    qwen_quant_dot_pair(packed,type,inputs[0],inputs[1],elements,row,outputs[0],outputs[1]);
    qwen_quant_dot_pair(packed,type,inputs[2],inputs[3],elements,row,outputs[2],outputs[3]);
}

void qwen_quant_dot_oct(
    const std::uint8_t*packed,std::uint32_t type,const float*const inputs[8],
    int elements,std::uint64_t row,float outputs[8]
){
    if(!qwen_simd_multi_type(type)){
        // The IQ formats have an eight-token AVX2 kernel; taking it here rather
        // than as two quads halves the codebook decodes.
        if((flyweight_cpu_features()&1u)!=0&&
           qwen_quant_dot_iq_multi_avx2(packed,type,inputs,8,elements,row,outputs))return;
        // NVFP4 uses two register-blocked AVX2 quads; the formats without an
        // AVX-512 oct kernel fall back through quad to the ordinary pair path,
        // because the oct entry point would otherwise decode them as Q8_0.
        qwen_quant_dot_quad(packed,type,inputs,elements,row,outputs);
        qwen_quant_dot_quad(packed,type,inputs+4,elements,row,outputs+4);
        return;
    }
    qwen_quant_dot_oct_avx512(packed,type,inputs,elements,row,outputs);
}

// The embedding table is indexed, not multiplied, so nothing downstream can
// detect a wrong decode -- picking the kernel by tensor type is the only guard.
// Throwing beats defaulting to Q8_0: an unsupported table silently produces
// ~100x-magnitude noise that drowns the residual stream instead of failing.
// Picking the LM-head kernel by tensor type, and refusing anything without a
// kernel, rather than defaulting to Q8_0: a mismatched head decodes to noise
// and pins the argmax to one token, which looks like a model bug, not a
// dispatch bug.
// One dispatch point from tensor type to the device matvec kernels, so the
// batched prefill path can fall back on them for any quantization that has no
// batched kernel of its own. Returns nonzero when the type has no kernel.
int qwen_gpu_matvec_by_type(
    std::uint32_t type, std::uint64_t matrix, std::uint64_t input,
    std::uint64_t output, int input_size, int output_size, std::uint64_t stream
) {
    switch (type) {
        // F32 matters to the sampler: the greedy path has a fused f32 argmax,
        // but a sampled request projects the full logits through this dispatch
        // and used to throw on an f32 LM head while greedy worked.
        case 0: {
            void* args[] = {&matrix, &input, &output, &input_size, &output_size};
            return flyweight_gpu_launch_named(
                "qwen_f32_matvec_warp", (output_size + 7) / 8, 1, 256, 0,
                stream, args);
        }
        case 1: {
            void* args[] = {&matrix, &input, &output, &input_size, &output_size};
            return flyweight_gpu_launch_named(
                "qwen_f16_matvec_warp", (output_size + 7) / 8, 1, 256, 0,
                stream, args);
        }
        // bf16_matvec_warp declares (rows, columns), so the extents swap.
        case 30: {
            void* args[] = {&matrix, &input, &output, &output_size, &input_size};
            return flyweight_gpu_launch_named(
                "bf16_matvec_warp", (output_size + 7) / 8, 1, 256, 0,
                stream, args);
        }
        case 8: return flyweight_gpu_q8_matvec_transposed(matrix, input, output, input_size, output_size, stream);
        case 10: return flyweight_gpu_q2k_matvec_transposed(matrix, input, output, input_size, output_size, stream);
        case 11: return flyweight_gpu_q3k_matvec_transposed(matrix, input, output, input_size, output_size, stream);
        // Q4_K has two kernels: a sub-block one that unpacks each scale once
        // and the generic per-element one. Prefer the fast path and fall back
        // when it declines (kernel absent, or a row that is not a whole number
        // of sub-blocks) rather than assuming it is always there.
        case 12: {
            const int fast = flyweight_gpu_q4k_matvec_subblock(
                matrix, input, output, input_size, output_size, stream);
            if (fast == 0) return 0;
            return flyweight_gpu_q4k_matvec_transposed(matrix, input, output, input_size, output_size, stream);
        }
        case 13: return flyweight_gpu_q5k_matvec_transposed(matrix, input, output, input_size, output_size, stream);
        case 14: return flyweight_gpu_q6k_matvec_transposed(matrix, input, output, input_size, output_size, stream);
        case 16: return flyweight_gpu_iq2xxs_matvec_transposed(matrix, input, output, input_size, output_size, stream);
        case 18: return flyweight_gpu_iq3xxs_matvec_transposed(matrix, input, output, input_size, output_size, stream);
        case 21: return flyweight_gpu_iq3s_matvec_transposed(matrix, input, output, input_size, output_size, stream);
        case 17: return flyweight_gpu_iq2xs_matvec_transposed(matrix, input, output, input_size, output_size, stream);
        case 22: return flyweight_gpu_iq2s_matvec_transposed(matrix, input, output, input_size, output_size, stream);
        case 23: return flyweight_gpu_iq4xs_matvec_transposed(matrix, input, output, input_size, output_size, stream);
        case 19: return flyweight_gpu_iq1s_matvec_transposed(matrix, input, output, input_size, output_size, stream);
        case 29: return flyweight_gpu_iq1m_matvec_transposed(matrix, input, output, input_size, output_size, stream);
        default: break;
    }
    return -1;
}

// The eager per-element driver matvec the decode paths fall back to when the
// Q8-activation fast path declines. Distinct from qwen_gpu_matvec_by_type
// above in exactly one way: Q4_K stays on the plain transposed kernel here,
// because that is what single decode, multi-decode and MTP verification have
// always launched -- the subblock preference belongs to by_type's callers
// (the sampler and the rows-forward compatibility fallback) alone.
//
// Returns kQwenMatvecUnsupported for a type with no driver matvec at all, so
// callers can tell "unknown format" from "the launch failed" -- the driver's
// own failure codes are not distinguishable from any fixed sentinel.
constexpr int kQwenMatvecUnsupported = -1000000;
// Types qwen_matvec_driver below can actually launch. Kept adjacent to the
// switch it mirrors -- the two drift apart silently otherwise, and the symptom
// is a load-time "type is unsupported" naming a number and nothing else.
bool qwen_matvec_supported(std::uint32_t type) {
    switch (type) {
        case 1: case 8: case 10: case 11: case 12: case 13: case 14:
        case 16: case 17: case 18: case 19: case 21: case 22: case 23:
        case 29: case 30:
            return true;
        default:
            return false;
    }
}

int qwen_matvec_driver(
    std::uint32_t type, std::uint64_t matrix, std::uint64_t input,
    std::uint64_t output, int input_size, int output_size, std::uint64_t stream
) {
    switch (type) {
        case 1: {
            void* args[] = {&matrix, &input, &output, &input_size, &output_size};
            return flyweight_gpu_launch_named(
                "qwen_f16_matvec_warp", (output_size + 7) / 8, 1, 256, 0,
                stream, args);
        }
        // bf16_matvec_warp declares (rows, columns), so the extents swap.
        case 30: {
            void* args[] = {&matrix, &input, &output, &output_size, &input_size};
            return flyweight_gpu_launch_named(
                "bf16_matvec_warp", (output_size + 7) / 8, 1, 256, 0,
                stream, args);
        }
        case 8: return flyweight_gpu_q8_matvec_transposed(matrix, input, output, input_size, output_size, stream);
        case 10: return flyweight_gpu_q2k_matvec_transposed(matrix, input, output, input_size, output_size, stream);
        case 11: return flyweight_gpu_q3k_matvec_transposed(matrix, input, output, input_size, output_size, stream);
        case 12: return flyweight_gpu_q4k_matvec_transposed(matrix, input, output, input_size, output_size, stream);
        case 13: return flyweight_gpu_q5k_matvec_transposed(matrix, input, output, input_size, output_size, stream);
        case 14: return flyweight_gpu_q6k_matvec_transposed(matrix, input, output, input_size, output_size, stream);
        case 16: return flyweight_gpu_iq2xxs_matvec_transposed(matrix, input, output, input_size, output_size, stream);
        case 17: return flyweight_gpu_iq2xs_matvec_transposed(matrix, input, output, input_size, output_size, stream);
        case 18: return flyweight_gpu_iq3xxs_matvec_transposed(matrix, input, output, input_size, output_size, stream);
        case 19: return flyweight_gpu_iq1s_matvec_transposed(matrix, input, output, input_size, output_size, stream);
        case 21: return flyweight_gpu_iq3s_matvec_transposed(matrix, input, output, input_size, output_size, stream);
        case 22: return flyweight_gpu_iq2s_matvec_transposed(matrix, input, output, input_size, output_size, stream);
        case 23: return flyweight_gpu_iq4xs_matvec_transposed(matrix, input, output, input_size, output_size, stream);
        case 29: return flyweight_gpu_iq1m_matvec_transposed(matrix, input, output, input_size, output_size, stream);
        default: break;
    }
    return kQwenMatvecUnsupported;
}

// Q8-activation group-decode LM head, or null where the type has no such
// kernel and the per-element fused head has to stand in.
const char* qwen_q8_lm_head_kernel(std::uint32_t type) {
    const auto* format = flyweight::v2::qwen_format(type);
    return format ? format->lm_head_argmax_q8 : nullptr;
}

// Q8-activation group-decode matvec, or null where the type has none. This is
// the full-logits sibling of qwen_q8_lm_head_kernel above: the fused head only
// returns an argmax, so anything that needs the distribution itself -- the
// temperature path -- has to project with one of these instead.
const char* qwen_q8_matvec_kernel(std::uint32_t type) {
    const auto* format = flyweight::v2::qwen_format(type);
    return format ? format->matvec_q8_warp : nullptr;
}

const char* qwen_lm_head_argmax_kernel(std::uint32_t type) {
    const auto* format = flyweight::v2::qwen_format(type);
    if (format && format->lm_head_argmax) return format->lm_head_argmax;
    throw std::runtime_error(
        "native Qwen LM-head type is unsupported: " + std::to_string(type));
}

// Effective device type for a tensor: what actually got uploaded, which is the
// checkpoint type unless prepare requantized it.
std::uint32_t qwen_device_type(
    const FlyweightV2QwenRuntime& runtime, std::uint64_t index
) {
    if (index < runtime.device_tensor_types.size())
        return runtime.device_tensor_types[index];
    return runtime.model->tensors[index].type;
}

// Bytes the tensor occupies in the static arena under its effective type.
std::uint64_t qwen_device_tensor_size(
    const FlyweightV2QwenRuntime& runtime, std::uint64_t index
) {
    const auto& tensor = runtime.model->tensors[index];
    if (qwen_device_type(runtime, index) == 8 && tensor.type != 8) {
        std::uint64_t elements = 1;
        for (auto dimension : tensor.shape) elements *= dimension;
        return (elements / 32) * kQ8BlockSize;
    }
    return tensor.size;
}

// Host pointer a static tensor can be used from directly, or 0 if it has to be
// copied into the device arena.
//
// On the CPU backend "device memory" is ordinary host memory, and the weights
// are already resident in the GGUF mapping -- so uploading them allocates and
// fills a second full copy of the model. For a large checkpoint that is the
// difference between running and exhausting RAM, and it buys nothing: the
// mapping is MAP_PRIVATE and no kernel writes to weights.
//
// Aliasing is refused whenever the device representation is not byte-identical
// to the file (the bf16 -> q8_0 requantization path genuinely transforms the
// data) or the mapped address is not aligned enough for the 128-bit vector
// loads the kernels use.
const std::uint8_t* qwen_alias_static_tensor(
    const FlyweightV2QwenRuntime& runtime, std::uint64_t index
) {
    if (!flyweight_backend_is_cpu()) return nullptr;
    // Escape hatch: forces the copying path so a suspected aliasing problem can
    // be confirmed or ruled out without a rebuild.
    static const bool disabled = [] {
        const char* setting = std::getenv("FLYWEIGHT_CPU_NO_ALIAS");
        return setting != nullptr && setting[0] == '1';
    }();
    if (disabled) return nullptr;
    const auto& tensor = runtime.model->tensors[index];
    if (qwen_device_type(runtime, index) != tensor.type) return nullptr;
    if (qwen_device_tensor_size(runtime, index) != tensor.size) return nullptr;
    const std::uint8_t* data = tensor_data(*runtime.model, tensor);
    if (data == nullptr) return nullptr;
    constexpr std::uintptr_t kVectorAlignment = 16;
    if (reinterpret_cast<std::uintptr_t>(data) % kVectorAlignment != 0)
        return nullptr;
    return data;
}

const char* qwen_embedding_kernel(std::uint32_t type, bool rows) {
    const auto* format = flyweight::v2::qwen_format(type);
    if (format && format->embedding)
        return rows ? format->embedding_rows : format->embedding;
    throw std::runtime_error(
        "native Qwen embedding table type is unsupported: " + std::to_string(type));
}

// Stage `count` embedding rows from the host mapping into device scratch and
// return the base pointer for the embedding kernels. Those kernels address a
// row as token*hidden, so the staged copy is indexed 0..count-1 rather than by
// the real token id -- callers pass 0 (single token) or embedding_row_index
// (rows variant) instead of the token itself.
//
// Returns the resident table unchanged when the embeddings were kept in VRAM,
// so every call site is identical in both modes.
std::uint64_t qwen_stage_embedding_rows(
    FlyweightV2QwenRuntime& runtime, const std::uint32_t* tokens, int count
) {
    if (!runtime.embeddings_host_resident)
        return runtime.device_tensors[runtime.token_embeddings];
    const auto& table = runtime.model->tensors[runtime.token_embeddings];
    const auto row_bytes = runtime.embedding_row_bytes;
    const auto vocabulary = table.size / row_bytes;
    const auto* source = tensor_data(*runtime.model,table);
    // Single-token decode is the hot case. When the mapping is registered the
    // row can be DMA'd straight out of it, which drops both the host memcpy and
    // the pinned-buffer reuse hazard from the per-token path. A rows chunk
    // gathers scattered rows, so it stays on the pack-then-one-upload path
    // rather than issuing one small DMA per row.
    if (count == 1 && runtime.embedding_dma) {
        if (tokens[0] >= vocabulary)
            throw std::runtime_error(
                "native Qwen embedding token is out of range: "
                + std::to_string(tokens[0]));
        if (flyweight_gpu_upload(
                runtime.embedding_stage,
                source + static_cast<std::uint64_t>(tokens[0]) * row_bytes,
                row_bytes, runtime.stream) != 0)
            throw std::runtime_error("native Qwen embedding row staging failed");
        return runtime.embedding_stage;
    }
    // The pinned mirror is refilled every token, so the previous upload has to
    // have drained first. The event is recorded immediately after each upload,
    // so by the time the next token reaches here the wait is already satisfied.
    if (runtime.embedding_event) flyweight_gpu_event_sync(runtime.embedding_event);
    auto* host = static_cast<std::uint8_t*>(runtime.embedding_host);
    for (int row = 0; row < count; ++row) {
        if (tokens[row] >= vocabulary)
            throw std::runtime_error(
                "native Qwen embedding token is out of range: "
                + std::to_string(tokens[row]));
        std::memcpy(
            host + static_cast<std::uint64_t>(row) * row_bytes,
            source + static_cast<std::uint64_t>(tokens[row]) * row_bytes,
            row_bytes);
    }
    if (flyweight_gpu_upload(
            runtime.embedding_stage, host,
            static_cast<std::uint64_t>(count) * row_bytes, runtime.stream) != 0)
        throw std::runtime_error("native Qwen embedding row staging failed");
    if (runtime.embedding_event)
        flyweight_gpu_event_record(runtime.embedding_event, runtime.stream);
    return runtime.embedding_stage;
}

// qwen4exp: gather the current token's hashed n-gram embedding (16 rows of the
// host-resident table, concatenated to one hidden-wide vector) and upload it to
// the decode workspace. History is EOS-segmented exactly as the reference does
// it: walking back from the current token, the first eos (or the sequence
// start) makes every further lookback read eos itself. The hash constants come
// from GGUF metadata verbatim; all arithmetic is u64 with wraparound, which
// matches the reference's int64 because the products are bounded below 2^63
// and xor cannot set the sign bit (plans/qwen4exp-semantics.md).
// Tell the kernel how the big host-resident tensors are actually read.
//
// The mapping is opened POSIX_FADV_SEQUENTIAL because the loader walks tensors
// in order and the weights are then read densely. The n-gram table is the
// exception and it is a large one: 26.8 GiB, 40% of a qwen4exp checkpoint, and
// qwen4_stage_ple_embed gathers a handful of 90-byte rows out of 320 million by
// hash. Sequential readahead into that is pure waste -- the kernel faults in a
// readahead window to use ninety bytes of it, and evicts expert weights that
// are read densely to make room.
//
// This is llama.cpp's lazy-range idea (llama-mmap.cpp: tensors it will not
// prefetch get POSIX_MADV_RANDOM, and MAP_POPULATE is dropped entirely when any
// exist, because POPULATE cannot be selective). The policy has to be per range,
// not per file.
void qwen_advise_access_pattern(const FlyweightV2QwenRuntime& runtime) {
#if !defined(_WIN32) && defined(MADV_RANDOM)
    // Kill switch: this is a hint whose effect is invisible in correctness and
    // only shows up in I/O, so it needs to be switchable to be measurable.
    static const char* setting = std::getenv("FLYWEIGHT_ADVISE_RANDOM");
    if (setting && setting[0] == '0') return;
    if (runtime.ple_table == std::numeric_limits<std::uint64_t>::max()) return;
    const auto& table = runtime.model->tensors[runtime.ple_table];
    const auto* address = tensor_data(*runtime.model, table);
    if (!address || !table.size) return;
    const auto page = static_cast<std::uintptr_t>(sysconf(_SC_PAGESIZE));
    const auto begin = reinterpret_cast<std::uintptr_t>(address) & ~(page - 1);
    const auto end = (reinterpret_cast<std::uintptr_t>(address) + table.size
                      + page - 1) & ~(page - 1);
    // Advisory: a failure here costs readahead, not correctness.
    (void)madvise(reinterpret_cast<void*>(begin),
                  static_cast<std::size_t>(end - begin), MADV_RANDOM);
#else
    (void)runtime;
#endif
}

void qwen4_stage_ple_embed(
    FlyweightV2QwenRuntime& runtime, std::uint32_t token,
    const std::vector<std::uint32_t>& history, std::uint64_t destination
) {
    const auto& config = runtime.model->config;
    const std::uint64_t eos = config.ple_eos_token_id;
    const std::uint64_t t0 = token;
    std::uint64_t t1 = history.empty() ? eos : history.back();
    std::uint64_t t2 = (t1 == eos || history.size() < 2)
        ? eos : history[history.size() - 2];
    const auto& multipliers = config.ple_multipliers;
    const std::uint32_t heads_per = config.ple_heads_per_ngram;
    const std::uint32_t head_count = (config.ple_ngram_size - 1) * heads_per;
    const auto& table = runtime.model->tensors[runtime.ple_table];
    const std::uint64_t row_width = config.per_layer_embedding_size;
    if (table.type != 0 && table.type != 20)
        throw std::runtime_error(
            "native qwen4exp PLE table type is not implemented yet: "
            + std::to_string(table.type));
    if (table.type == 20 && row_width % kIq4nlBlockElements != 0)
        throw std::runtime_error(
            "native qwen4exp IQ4_NL PLE rows must be a multiple of 32 wide");
    const auto* source = tensor_data(*runtime.model, table);
    const std::uint64_t rows_total = table.shape[1];  // GGUF shape [width, rows]
    const std::uint64_t row_bytes = table.type == 20
        ? row_width / kIq4nlBlockElements * kIq4nlBlockBytes
        : row_width * sizeof(float);
    // Pageable source is fine here: an async upload from pageable memory
    // stages internally before returning, so the buffer can be reused.
    static thread_local std::vector<float> gathered;
    gathered.resize(static_cast<std::size_t>(head_count) * row_width);
    const std::uint64_t bigram = t0 * multipliers[0] ^ t1 * multipliers[1];
    const std::uint64_t trigram = bigram ^ t2 * multipliers[2];
    for (std::uint32_t head = 0; head < head_count; ++head) {
        const std::uint64_t mixed = head < heads_per ? bigram : trigram;
        const std::uint64_t row = mixed % config.ple_head_vocab_sizes[head]
            + config.ple_head_offsets[head];
        if (row >= rows_total)
            throw std::runtime_error(
                "native qwen4exp PLE row is out of range");
        float* destination =
            gathered.data() + static_cast<std::size_t>(head) * row_width;
        const auto* row_data = source + row * row_bytes;
        if (table.type == 20) {
            for (std::uint64_t block = 0; block * kIq4nlBlockElements < row_width;
                 ++block) {
                const auto* base = row_data + block * kIq4nlBlockBytes;
                std::uint16_t d_bits = 0;
                std::memcpy(&d_bits, base, 2);
                const float scale = qwen_half_value(d_bits);
                float* out = destination + block * kIq4nlBlockElements;
                for (int element = 0; element < 16; ++element) {
                    const std::uint8_t byte = base[2 + element];
                    out[element] = scale * kIq4nlValues[byte & 15];
                    out[element + 16] = scale * kIq4nlValues[byte >> 4];
                }
            }
        } else {
            std::memcpy(destination, row_data, row_width * sizeof(float));
        }
    }
    if (flyweight_gpu_upload(destination, gathered.data(),
            gathered.size() * sizeof(float), runtime.stream) != 0)
        throw std::runtime_error("native qwen4exp PLE staging failed");
}

// Rows form: the whole chunk's n-gram gathers, one upload. In-chunk history
// comes from the chunk itself; the boundary rows read the sequence tail from
// processed_tokens, which at this point holds exactly the tokens BEFORE the
// chunk (qwen_prefill_rows appends after the forward). Same EOS segmentation
// as the single-token stager.
void qwen4_stage_ple_embed_rows(
    FlyweightV2QwenRuntime& runtime, const std::uint32_t* tokens, int count,
    std::uint64_t destination
) {
    const auto& config = runtime.model->config;
    const std::uint64_t eos = config.ple_eos_token_id;
    const auto& history = runtime.processed_tokens;
    const auto& multipliers = config.ple_multipliers;
    const std::uint32_t heads_per = config.ple_heads_per_ngram;
    const std::uint32_t head_count = (config.ple_ngram_size - 1) * heads_per;
    const auto& table = runtime.model->tensors[runtime.ple_table];
    const std::uint64_t row_width = config.per_layer_embedding_size;
    if (table.type != 0 && table.type != 20)
        throw std::runtime_error(
            "native qwen4exp PLE table type is not implemented yet: "
            + std::to_string(table.type));
    const auto* source = tensor_data(*runtime.model, table);
    const std::uint64_t rows_total = table.shape[1];
    const std::uint64_t row_bytes = table.type == 20
        ? row_width / kIq4nlBlockElements * kIq4nlBlockBytes
        : row_width * sizeof(float);
    static thread_local std::vector<float> gathered;
    gathered.resize(static_cast<std::size_t>(count) * head_count * row_width);
    auto back = [&](int position, int shift) -> std::uint64_t {
        // Token `shift` places before chunk position `position`, stopping at
        // the first eos (or the sequence start) on the way back.
        std::uint64_t value = tokens[position];
        for (int step = 1; step <= shift; ++step) {
            const int at = position - step;
            const std::uint64_t token = at >= 0
                ? tokens[at]
                : (history.size() >= static_cast<std::size_t>(-at)
                       ? history[history.size() + at] : eos);
            if (token == eos && step < shift) return eos;
            value = token;
        }
        return value;
    };
    for (int position = 0; position < count; ++position) {
        const std::uint64_t t0 = tokens[position];
        std::uint64_t t1 = back(position, 1);
        std::uint64_t t2 = t1 == eos ? eos : back(position, 2);
        const std::uint64_t bigram = t0 * multipliers[0] ^ t1 * multipliers[1];
        const std::uint64_t trigram = bigram ^ t2 * multipliers[2];
        float* row_out = gathered.data()
            + static_cast<std::size_t>(position) * head_count * row_width;
        for (std::uint32_t head = 0; head < head_count; ++head) {
            const std::uint64_t mixed = head < heads_per ? bigram : trigram;
            const std::uint64_t row = mixed % config.ple_head_vocab_sizes[head]
                + config.ple_head_offsets[head];
            if (row >= rows_total)
                throw std::runtime_error("native qwen4exp PLE row is out of range");
            float* destination_row =
                row_out + static_cast<std::size_t>(head) * row_width;
            const auto* row_data = source + row * row_bytes;
            if (table.type == 20) {
                for (std::uint64_t block = 0;
                     block * kIq4nlBlockElements < row_width; ++block) {
                    const auto* base = row_data + block * kIq4nlBlockBytes;
                    std::uint16_t d_bits = 0;
                    std::memcpy(&d_bits, base, 2);
                    const float scale = qwen_half_value(d_bits);
                    float* out = destination_row + block * kIq4nlBlockElements;
                    for (int element = 0; element < 16; ++element) {
                        const std::uint8_t byte = base[2 + element];
                        out[element] = scale * kIq4nlValues[byte & 15];
                        out[element + 16] = scale * kIq4nlValues[byte >> 4];
                    }
                }
            } else {
                std::memcpy(destination_row, row_data, row_width * sizeof(float));
            }
        }
    }
    if (flyweight_gpu_upload(destination, gathered.data(),
            gathered.size() * sizeof(float), runtime.stream) != 0)
        throw std::runtime_error("native qwen4exp PLE rows staging failed");
}

// NVFP4 checkpoints carry an optional per-expert f32 scale tensor alongside each
// expert role (ffn_{gate,up,down}_exps.scale). Returns 1.0 when the model has none.
float qwen_expert_role_scale(
    const FlyweightV2QwenRuntime& runtime, std::uint64_t scale_tensor, int expert
) {
    if (scale_tensor == std::numeric_limits<std::uint64_t>::max()) return 1.0f;
    const auto& st = runtime.model->tensors[scale_tensor];
    const auto scale_bytes = st.size / runtime.model->config.expert_count;
    float value = 1.0f;
    std::memcpy(
        &value,
        tensor_data(*runtime.model,st)
            + static_cast<std::uint64_t>(expert) * scale_bytes,
        sizeof(float)
    );
    return value;
}

// Per-phase MoE timing. The expert path does not go through launch_named, so
// FLYWEIGHT_CPU_PROFILE cannot see inside it; this is the only view of where the
// ~43% of decode that lands here actually goes. Off unless FLYWEIGHT_MOE_PROFILE=1.
std::atomic<std::uint64_t> g_moe_gate_up_ns{0}, g_moe_quant_ns{0}, g_moe_down_ns{0};
std::atomic<std::uint64_t> g_moe_calls{0};

bool moe_profiling() {
    static const bool on = [] {
        const char* s = std::getenv("FLYWEIGHT_MOE_PROFILE");
        return s && s[0] == '1';
    }();
    return on;
}

extern "C" FLYWEIGHT_BACKEND_API void flyweight_moe_profile_dump() {
    if (!moe_profiling()) return;
    const double gate_up = g_moe_gate_up_ns.load() / 1e6;
    const double quant = g_moe_quant_ns.load() / 1e6;
    const double down = g_moe_down_ns.load() / 1e6;
    const double total = gate_up + quant + down;
    if (total <= 0.0) return;
    std::fprintf(stderr,
        "\n[flyweight-moe] %llu calls, %.1f ms total\n"
        "  gate+up+swiglu %8.1f ms  %5.1f%%\n"
        "  activation q8  %8.1f ms  %5.1f%%\n"
        "  down           %8.1f ms  %5.1f%%\n",
        static_cast<unsigned long long>(g_moe_calls.load()), total,
        gate_up, 100*gate_up/total, quant, 100*quant/total,
        down, 100*down/total);
}

void qwen_cpu_moe(
    const FlyweightV2QwenRuntime& runtime,
    const QwenLayerPlan& layer,
    const std::int32_t* selected,
    const float* weights,
    int routed_count,
    const float* input,
    float* activated,
    float* output
) {
    const int experts = runtime.model->config.expert_count;
    const int hidden = runtime.model->config.hidden_size;
    const int intermediate = runtime.moe_intermediate;
    if (routed_count < 0 || routed_count > 256) {
        throw std::runtime_error("native CPU MoE routed count is unsupported");
    }
    for (int role = 0; role < 3; ++role) {
        const auto type = runtime.model->tensors[layer.expert_tensors[role]].type;
        if (!qwen_cpu_expert_type_supported(type)) {
            throw std::runtime_error(
                "unsupported native CPU expert quantization: " + std::to_string(type));
        }
    }
    std::array<const std::uint8_t*, 256> gate{}, up{}, down{};
    // The GPU path folds the per-expert gate/up scales into the staged block
    // scales; here the weights are read straight from the mmap, so apply them to
    // the dot products instead. That is exact (the scale is a plain per-expert
    // factor) and has to happen before the SwiGLU, which is non-linear. The down
    // scale is already folded into `weights` by the caller.
    std::array<float, 256> gate_scale{}, up_scale{};
    for (int rank = 0; rank < routed_count; ++rank) {
        if (selected[rank] < 0 || selected[rank] >= experts) {
            // The raw value, in hex, distinguishes the two ways this fires: a
            // routing bug puts a plausible integer here, where a route buffer
            // the router never wrote carries whatever the allocator left --
            // recognisably 0xabababab under FLYWEIGHT_CPU_POISON.
            throw std::runtime_error(
                "native CPU MoE selected an invalid expert: rank " +
                std::to_string(rank) + " of " + std::to_string(routed_count) +
                " is " + std::to_string(selected[rank]) + " (0x" +
                [&]{ char hex[16];
                     std::snprintf(hex, sizeof(hex), "%08x",
                                   static_cast<unsigned>(selected[rank]));
                     return std::string(hex); }() + ")");
        }
        for (int role = 0; role < 3; ++role) {
            const auto& t = runtime.model->tensors[layer.expert_tensors[role]];
            const auto bytes = t.size / experts;
            const auto* pointer = tensor_data(*runtime.model,t)
                + static_cast<std::uint64_t>(selected[rank]) * bytes;
            if (role == 0) gate[rank] = pointer;
            else if (role == 1) up[rank] = pointer;
            else down[rank] = pointer;
        }
        gate_scale[rank] =
            qwen_expert_role_scale(runtime, layer.expert_gate_scale, selected[rank]);
        up_scale[rank] =
            qwen_expert_role_scale(runtime, layer.expert_up_scale, selected[rank]);
    }
    const auto gate_type = runtime.model->tensors[layer.expert_tensors[0]].type;
    const auto up_type = runtime.model->tensors[layer.expert_tensors[1]].type;
    const auto down_type = runtime.model->tensors[layer.expert_tensors[2]].type;
    static const char* q8_setting = std::getenv("FLYWEIGHT_Q8_ACTIVATIONS");
    const auto cpu_features = flyweight_cpu_features();
    // Q8-K activations only pay off, and are only decoded, for these quants.
    // Q4_K, NVFP4 and bf16 are faster on the vectorized f32 path (their small
    // codes do not amortize the activation quantization). IQ2_XS and IQ3_XXS
    // use the same integer codebook matvec strategy as llama.cpp.
    const auto q8_activations_ok = [](std::uint32_t type) {
        return type == 8 || type == 13 || type == 14 || type == 17 || type == 18;
    };
    const bool laguna_iq_q8 = (cpu_features & 4u) != 0
        && gate_type == 17 && up_type == 17 && down_type == 18
        && (!q8_setting || q8_setting[0] != '0');
    // On the CPU backend, default the Q8-K activation path on for any expert
    // quantization q8_activations_ok() accepts. It was previously reachable for
    // Q8_0 only through FLYWEIGHT_Q8_ACTIVATIONS=1, even though the type is on
    // that list -- measured worth ~14% on a 35B Q8_0 MoE (medians 7.53/7.16
    // without against 8.33/8.42 with, two replications, non-overlapping in the
    // second). Scoped to the CPU backend because that is the configuration the
    // measurement covers; a GPU machine running hybrid or cpu expert placement
    // keeps the old behaviour until someone measures it there.
    const bool q8_default_on = flyweight_backend_is_cpu()
        && !(q8_setting && q8_setting[0] == '0');
    const bool use_q8 = (cpu_features & 1u) != 0 && hidden % 256 == 0
        && intermediate % 256 == 0
        && ((q8_setting && q8_setting[0] == '1') || laguna_iq_q8 || q8_default_on)
        && q8_activations_ok(gate_type) && q8_activations_ok(up_type)
        && q8_activations_ok(down_type);
    static constexpr char q4_tile_name[] = {
        'C','O','L','I','B','R','I','_','Q','4','_','R','O','W','_','T','I','L','E','S','\0'
    };
    static const char* q4_tile_setting = std::getenv(q4_tile_name);
    const bool q4_tiled = !use_q8 && (cpu_features & 3u) != 0
        && hidden % 256 == 0 && intermediate % 256 == 0
        && gate_type == 12 && up_type == 12 && down_type == 12
        && (!q4_tile_setting || q4_tile_setting[0] != '0');
    thread_local std::vector<QwenQ8KBlock> input_q8, activated_q8;
    if (use_q8) {
        input_q8.resize(hidden / 256);
        activated_q8.resize(static_cast<std::size_t>(routed_count) * (intermediate / 256));
        qwen_quantize_q8_k_avx2(input, hidden, input_q8.data());
    }
#if defined(_OPENMP)
    // Decode is bandwidth-bound on expert weights. SMT siblings contend for the
    // same load ports and memory bandwidth, so use physical cores by default.
    // Keep an explicit OMP_NUM_THREADS override for machine-specific tuning.
    const int team = qwen_cpu_thread_count(runtime);
#endif
    const auto* input_q8_data = input_q8.data();
    auto* activated_q8_data = activated_q8.data();
    const auto q8_dot = [cpu_features](
        const std::uint8_t* packed, std::uint32_t type,
        const QwenQ8KBlock* input, int elements, std::uint64_t row
    ) {
        return (cpu_features & 4u) != 0
            ? qwen_quant_dot_q8_k_avx_vnni(packed,type,input,elements,row)
            : qwen_quant_dot_q8_k_avx2(packed,type,input,elements,row);
    };
    static const char* fused_gate_up_setting = std::getenv("FLYWEIGHT_FUSED_MOE_GATE_UP");
    static const char* iq_avx512_setting = std::getenv("FLYWEIGHT_IQ_AVX512");
    const bool auto_fused_iq2xs = gate_type == 17
        && (flyweight_cpu_features() & 2u) != 0
        && (!iq_avx512_setting || iq_avx512_setting[0] != '0')
        && (!fused_gate_up_setting || fused_gate_up_setting[0] != '0');
    const auto gate_up_task = [&](int task) {
        const int rank = task / intermediate;
        const int row = task % intermediate;
        float gate_value = 0.0f;
        float up_value = 0.0f;
        if (use_q8) {
            gate_value = q8_dot(
                gate[rank], gate_type, input_q8_data, hidden, row
            );
            up_value = q8_dot(
                up[rank], up_type, input_q8_data, hidden, row
            );
        } else if ((runtime.fused_moe_gate_up || auto_fused_iq2xs)
                   && gate_type == up_type) {
            qwen_quant_dot_two_rows(
                gate[rank], up[rank], gate_type, input, hidden,
                row, gate_value, up_value
            );
        } else {
            gate_value = qwen_quant_dot(gate[rank], gate_type, input, hidden, row);
            up_value = qwen_quant_dot(up[rank], up_type, input, hidden, row);
        }
        gate_value *= gate_scale[rank];
        up_value *= up_scale[rank];
        const float clipped = std::max(-80.0f, std::min(80.0f, gate_value));
        activated[task] = gate_value / (1.0f + std::exp(-clipped)) * up_value;
    };
    const auto down_row = [&](int row) {
        float value = 0.0f;
        for (int rank = 0; rank < routed_count; ++rank) {
            const float expert_value = use_q8
                ? q8_dot(
                    down[rank], down_type,
                    activated_q8_data + static_cast<std::size_t>(rank) * (intermediate / 256),
                    intermediate, row
                )
                : qwen_quant_dot(down[rank], down_type, activated + rank * intermediate, intermediate, row);
            value += weights[rank] * expert_value;
        }
        output[row] = value;
    };
    const auto gate_up_tile = [&](int task) {
        constexpr int tile_rows = 4;
        const int tiles_per_expert = (intermediate + tile_rows - 1) / tile_rows;
        const int rank = task / tiles_per_expert;
        const int first_row = (task % tiles_per_expert) * tile_rows;
        const int count = std::min(tile_rows, intermediate - first_row);
        float gate_values[tile_rows]{}, up_values[tile_rows]{};
        qwen_quant_dot_rows(
            gate[rank], gate_type, input, hidden,
            first_row, count, gate_values);
        qwen_quant_dot_rows(
            up[rank], up_type, input, hidden,
            first_row, count, up_values);
        for (int lane = 0; lane < count; ++lane) {
            const float gate_value = gate_values[lane] * gate_scale[rank];
            const float up_value = up_values[lane] * up_scale[rank];
            const float clipped = std::max(-80.0f, std::min(80.0f, gate_value));
            activated[rank * intermediate + first_row + lane] =
                gate_value / (1.0f + std::exp(-clipped)) * up_value;
        }
    };
    const auto down_tile = [&](int first_row) {
        constexpr int tile_rows = 4;
        const int count = std::min(tile_rows, hidden - first_row);
        float sums[tile_rows]{};
        for (int rank = 0; rank < routed_count; ++rank) {
            float values[tile_rows]{};
            qwen_quant_dot_rows(
                down[rank], down_type, activated + rank * intermediate,
                intermediate, first_row, count, values);
            for (int lane = 0; lane < count; ++lane)
                sums[lane] += weights[rank] * values[lane];
        }
        for (int lane = 0; lane < count; ++lane)
            output[first_row + lane] = sums[lane];
    };
    if (use_q8) {
#if defined(_OPENMP)
#pragma omp parallel num_threads(team)
#endif
        {
            // `omp for` carries an implicit barrier, so a timestamp taken by one
            // thread between them is a true phase boundary rather than a sample
            // of whichever thread got there first.
            const bool profile = moe_profiling();
            auto mark = std::chrono::steady_clock::time_point{};
            if (profile) mark = std::chrono::steady_clock::now();
#if defined(_OPENMP)
#pragma omp for schedule(static)
#endif
            for (int task = 0; task < routed_count * intermediate; ++task)
                gate_up_task(task);
#if defined(_OPENMP)
#pragma omp master
#endif
            if (profile) {
                const auto now = std::chrono::steady_clock::now();
                g_moe_gate_up_ns += static_cast<std::uint64_t>(
                    std::chrono::duration_cast<std::chrono::nanoseconds>(now-mark).count());
                mark = now;
            }
#if defined(_OPENMP)
#pragma omp for schedule(static)
#endif
            for (int rank = 0; rank < routed_count; ++rank) {
                qwen_quantize_q8_k_avx2(
                    activated + static_cast<std::size_t>(rank) * intermediate,
                    intermediate,
                    activated_q8_data + static_cast<std::size_t>(rank) * (intermediate / 256)
                );
            }
#if defined(_OPENMP)
#pragma omp master
#endif
            if (profile) {
                const auto now = std::chrono::steady_clock::now();
                g_moe_quant_ns += static_cast<std::uint64_t>(
                    std::chrono::duration_cast<std::chrono::nanoseconds>(now-mark).count());
                mark = now;
            }
#if defined(_OPENMP)
#pragma omp for schedule(static)
#endif
            for (int row = 0; row < hidden; ++row) down_row(row);
#if defined(_OPENMP)
#pragma omp master
#endif
            if (profile) {
                g_moe_down_ns += static_cast<std::uint64_t>(
                    std::chrono::duration_cast<std::chrono::nanoseconds>(
                        std::chrono::steady_clock::now()-mark).count());
                ++g_moe_calls;
            }
        }
    } else {
        // One team covers both dependent phases. The implicit barrier after the
        // first loop replaces a second parallel-region launch on every layer.
#if defined(_OPENMP)
#pragma omp parallel num_threads(team)
#endif
        {
            if (q4_tiled) {
#if defined(_OPENMP)
#pragma omp for schedule(static)
#endif
                for (int task = 0;
                     task < routed_count * ((intermediate + 3) / 4); ++task)
                    gate_up_tile(task);
#if defined(_OPENMP)
#pragma omp for schedule(static)
#endif
                for (int row = 0; row < hidden; row += 4) down_tile(row);
            } else {
#if defined(_OPENMP)
#pragma omp for schedule(static)
#endif
                for (int task = 0; task < routed_count * intermediate; ++task)
                    gate_up_task(task);
#if defined(_OPENMP)
#pragma omp for schedule(static)
#endif
                for (int row = 0; row < hidden; ++row) down_row(row);
            }
        }
    }
    if (std::getenv("FLYWEIGHT_MOE_DEBUG")) {
        static int calls = 0;
        int n = calls++;
        if (n < 45) {
            double s = 0; for (int i = 0; i < hidden; ++i) s += std::abs(output[i]);
            std::fprintf(stderr, "[moe-dbg] call=%02d sel0=%d w0=%.4f sum|out|=%.4f\n",
                n, selected[0], weights[0], s);
        }
    }
}

bool qwen_prefill_direct_quant_enabled(
    const FlyweightV2QwenRuntime& runtime
) {
    if ((flyweight_cpu_features() & 3u) == 0) return false;
    if (const char* setting = std::getenv("FLYWEIGHT_PREFILL_DIRECT_QUANT"))
        return setting[0] == '1';
    if (!runtime.laguna) return false;
    bool found = false;
    for (const auto& layer : runtime.layers) {
        if (layer.dense_ffn || !layer.expert_tensors[0]) continue;
        for (const auto tensor : layer.expert_tensors) {
            const auto type = runtime.model->tensors[tensor].type;
            if (type != 17 && type != 18 && type != 23) return false;
            found = true;
        }
    }
    return found;
}

// Region timers for the batched CPU MoE. `qwen_cpu_moe` is the largest block of
// prefill (58% of wall on the 35B-A3B) and ~3.5x off its roofline, but it does
// not go through `launch_named`, so FLYWEIGHT_CPU_PROFILE never sees it. Enable
// with FLYWEIGHT_MOE_PROFILE=1; the dump prints once at exit. Off by default --
// the clock reads sit inside the OpenMP task loop.
struct QwenCpuMoeProfile {
    std::atomic<std::uint64_t> setup{0};
    std::atomic<std::uint64_t> gate_dequant{0};
    std::atomic<std::uint64_t> gate_gemm{0};
    std::atomic<std::uint64_t> gate_activate{0};
    std::atomic<std::uint64_t> down_dequant{0};
    std::atomic<std::uint64_t> down_gemm{0};
    std::atomic<std::uint64_t> down_store{0};
    std::atomic<std::uint64_t> combine{0};
    std::atomic<std::uint64_t> calls{0};
    std::atomic<std::uint64_t> tasks_gate{0};
    std::atomic<std::uint64_t> tasks_down{0};

    ~QwenCpuMoeProfile() {
        if (!calls.load()) return;
        const double ms = 1.0e6;
        std::fprintf(stderr,
            "[moe] calls=%llu gate{dequant=%.1fms gemm=%.1fms act=%.1fms} "
            "down{dequant=%.1fms gemm=%.1fms store=%.1fms} setup=%.1fms "
            "combine=%.1fms tasks{gate=%llu down=%llu}\n",
            (unsigned long long)calls.load(),
            gate_dequant.load()/ms, gate_gemm.load()/ms, gate_activate.load()/ms,
            down_dequant.load()/ms, down_gemm.load()/ms, down_store.load()/ms,
            setup.load()/ms, combine.load()/ms,
            (unsigned long long)tasks_gate.load(),
            (unsigned long long)tasks_down.load());
    }
};
QwenCpuMoeProfile g_cpu_moe_profile;
bool qwen_cpu_moe_profile_enabled() {
    static const bool on = [] {
        const char* v = std::getenv("FLYWEIGHT_MOE_PROFILE");
        return v && v[0] != '0';
    }();
    return on;
}
inline std::uint64_t qwen_moe_now() {
    return static_cast<std::uint64_t>(
        std::chrono::steady_clock::now().time_since_epoch().count());
}

// Importance-matrix capture for the CPU expert path: the per-expert slice of
// a stacked tensor accumulates the rows routed to that expert. Host-side --
// these activations never touch the device -- and merged with the device
// arena when the matrix is read back. `runtime` arrives const in the callers,
// but the accumulators are capture state, not model state.
static void qwen_imatrix_note_expert(
    const FlyweightV2QwenRuntime& runtime, std::uint64_t tensor_index,
    int expert, const float* const* vectors, int count, int width
) {
    auto& capture=const_cast<FlyweightV2QwenRuntime&>(runtime);
    if(capture.imatrix_offsets.empty())return;
    const auto slot=capture.imatrix_offsets[tensor_index];
    if(slot==~0ull)return;
    float* sums=capture.imatrix_host.data()+slot
        +static_cast<std::uint64_t>(expert)*static_cast<std::uint64_t>(width);
    for(int row=0;row<count;++row){
        const float* values=vectors[row];
        for(int column=0;column<width;++column)
            sums[column]+=values[column]*values[column];
    }
    capture.imatrix_rows[tensor_index]+=static_cast<std::uint64_t>(count);
}

void qwen_cpu_moe_rows(
    const FlyweightV2QwenRuntime& runtime, const QwenLayerPlan& layer,
    const std::int32_t* selected, const float* weights, int rows,
    int routed_count, const float* input, float* activated,
    float* down_values, float* output
) {
    const int experts=runtime.model->config.expert_count;
    const int hidden=runtime.model->config.hidden_size;
    const int intermediate=runtime.moe_intermediate;
    const bool direct_quant=qwen_prefill_direct_quant_enabled(runtime);
    const bool direct_oct=direct_quant&&(flyweight_cpu_features()&2u)!=0;
    if(rows<=0||rows>4096||routed_count<=0||routed_count>256)
        throw std::runtime_error("native CPU batched MoE shape is unsupported");
    const auto gate_type=runtime.model->tensors[layer.expert_tensors[0]].type;
    const auto up_type=runtime.model->tensors[layer.expert_tensors[1]].type;
    const auto down_type=runtime.model->tensors[layer.expert_tensors[2]].type;
    static const char* fused_gate_up_setting=std::getenv("FLYWEIGHT_FUSED_MOE_GATE_UP");
    static const char* iq_avx512_setting=std::getenv("FLYWEIGHT_IQ_AVX512");
    const bool auto_fused_iq2xs=gate_type==17
        &&(flyweight_cpu_features()&2u)!=0
        &&(!iq_avx512_setting||iq_avx512_setting[0]!='0')
        &&(!fused_gate_up_setting||fused_gate_up_setting[0]!='0');
    for(const auto type:{gate_type,up_type,down_type})
        if(!qwen_cpu_expert_type_supported(type))
            throw std::runtime_error(
                "unsupported native CPU expert quantization: "+std::to_string(type));
    auto expert_data=[&](int role,int expert){
        const auto&t=runtime.model->tensors[layer.expert_tensors[role]];
        return tensor_data(*runtime.model,t)+static_cast<std::uint64_t>(expert)*(t.size/experts);
    };
    // Group routes by expert (CSR over token_rank slots) so each expert's
    // weight rows are decoded once per batch and dotted with every token
    // routed to it. Routes with weight 0 were already claimed by the GPU
    // expert cache (or pruned) and are skipped everywhere below.
    const bool moe_profile=qwen_cpu_moe_profile_enabled();
    const std::uint64_t t_setup0=moe_profile?qwen_moe_now():0;
    const int total=rows*routed_count;
    // Reuse heap allocations across calls via static vectors.  These are
    // filled sequentially before the OpenMP parallel region and only read
    // inside it, so there is no data race.
    static std::vector<int> counts;
    static std::vector<int> offsets;
    static std::vector<int> occurrences;
    static std::vector<const float*> vectors;
    counts.assign(experts,0);
    for(int route=0;route<total;++route){
        if(weights[route]==0.0f)continue;
        const int expert=selected[route];
        if(expert<0||expert>=experts)
            throw std::runtime_error("native CPU batched MoE selected an invalid expert");
        ++counts[expert];
    }
    offsets.resize(experts+1);
    offsets[0]=0;
    for(int expert=0;expert<experts;++expert)offsets[expert+1]=offsets[expert]+counts[expert];
    occurrences.resize(offsets[experts]);
    vectors.resize(offsets[experts]);
    {
        static std::vector<int> cursor;
        cursor.assign(offsets.begin(),offsets.end()-1);
        for(int route=0;route<total;++route){
            if(weights[route]==0.0f)continue;
            const int slot=cursor[selected[route]]++;
            occurrences[slot]=route;
            vectors[slot]=input+static_cast<std::size_t>(route/routed_count)*hidden;
        }
    }
    static std::vector<int> group_experts;
    group_experts.clear();
    group_experts.reserve(256);
    for(int expert=0;expert<experts;++expert)if(counts[expert])group_experts.push_back(expert);
    const int group_count=static_cast<int>(group_experts.size());
    if(!runtime.imatrix_offsets.empty())
        for(const int expert:group_experts){
            // Gate and up read the same activations; each keeps its own entry
            // so the written matrix matches llama.cpp's per-tensor layout.
            qwen_imatrix_note_expert(runtime,layer.expert_tensors[0],expert,
                                     &vectors[offsets[expert]],counts[expert],hidden);
            qwen_imatrix_note_expert(runtime,layer.expert_tensors[1],expert,
                                     &vectors[offsets[expert]],counts[expert],hidden);
        }
    if(moe_profile){g_cpu_moe_profile.setup+=qwen_moe_now()-t_setup0;
        ++g_cpu_moe_profile.calls;}
    // A scalar decoder here costs ~10x, and the only previous symptom was a
    // slow prefill with no attribution. One line, once per process, naming the
    // types that are paying: the phase counters then read as expected instead
    // of pointing at the GEMM they feed.
    const auto scalar_types_before=
        g_scalar_dequant_types.load(std::memory_order_relaxed);
    constexpr int kRowBlock=4;
    // Direct IQ rows are already grouped by expert and have thousands of tasks
    // available. Larger hand-out chunks avoid making OpenMP's dynamic scheduler
    // a measurable part of short prompts; keep the finer balance for the f32
    // fallback, whose per-task cost varies much more with route count.
    const int schedule_chunk=direct_quant?32:4;
    const int gate_blocks=(intermediate+kRowBlock-1)/kRowBlock;
#pragma omp parallel for schedule(dynamic,schedule_chunk) num_threads(qwen_cpu_thread_count(runtime))
    for(int task=0;task<group_count*gate_blocks;++task){
        const int group=task/gate_blocks;const int row0=(task%gate_blocks)*kRowBlock;
        const int mr=std::min(kRowBlock,intermediate-row0);
        const int expert=group_experts[group];
        const int begin=offsets[expert],count=counts[expert];
        const auto*gate_data=expert_data(0,expert);
        const auto*up_data=expert_data(1,expert);
        // Per-expert gate/up scales, applied before the (non-linear) SwiGLU. See
        // the matching comment in qwen_cpu_moe.
        const float gate_scale=qwen_expert_role_scale(runtime,layer.expert_gate_scale,expert);
        const float up_scale=qwen_expert_role_scale(runtime,layer.expert_up_scale,expert);
        if(count<=2){
            for(int i=0;i<mr;++i){
                float gate_values[2]{},up_values[2]{};
                if((runtime.fused_moe_gate_up||auto_fused_iq2xs)&&gate_type==up_type){
                    if(count==1){
                        qwen_quant_dot_two_rows(
                            gate_data, up_data, gate_type, vectors[begin],
                            hidden, row0+i, gate_values[0], up_values[0]
                        );
                    }else{
                        qwen_quant_dot_two_rows(
                            gate_data, up_data, gate_type, vectors[begin],
                            hidden, row0+i, gate_values[0], up_values[0]
                        );
                        qwen_quant_dot_two_rows(
                            gate_data, up_data, gate_type, vectors[begin+1],
                            hidden, row0+i, gate_values[1], up_values[1]
                        );
                    }
                }else{
                    if(count==1){
                        gate_values[0]=qwen_quant_dot(gate_data,gate_type,vectors[begin],hidden,row0+i);
                        up_values[0]=qwen_quant_dot(up_data,up_type,vectors[begin],hidden,row0+i);
                    }else{
                        qwen_quant_dot_pair(gate_data,gate_type,vectors[begin],vectors[begin+1],hidden,row0+i,gate_values[0],gate_values[1]);
                        qwen_quant_dot_pair(up_data,up_type,vectors[begin],vectors[begin+1],hidden,row0+i,up_values[0],up_values[1]);
                    }
                }
                for(int occurrence=0;occurrence<count;++occurrence){const int token_rank=occurrences[begin+occurrence];const float gate_value=gate_values[occurrence]*gate_scale,clipped=std::max(-80.0f,std::min(80.0f,gate_value));activated[static_cast<std::size_t>(token_rank)*intermediate+(row0+i)]=gate_value/(1.0f+std::exp(-clipped))*up_values[occurrence]*up_scale;}
            }
            continue;
        }
        if(direct_quant){
            for(int i=0;i<mr;++i)for(int occurrence=0;occurrence<count;){
                const int remaining=count-occurrence;
                const int tile=direct_oct&&remaining>=8?8:std::min(4,remaining);
                float gate_values[8]{},up_values[8]{};
                if(tile==8){
                    const float*tile_vectors[8]={vectors[begin+occurrence],vectors[begin+occurrence+1],vectors[begin+occurrence+2],vectors[begin+occurrence+3],vectors[begin+occurrence+4],vectors[begin+occurrence+5],vectors[begin+occurrence+6],vectors[begin+occurrence+7]};
                    qwen_quant_dot_oct(gate_data,gate_type,tile_vectors,hidden,row0+i,gate_values);
                    qwen_quant_dot_oct(up_data,up_type,tile_vectors,hidden,row0+i,up_values);
                }else if(tile==4){
                    const float*tile_vectors[4]={vectors[begin+occurrence],vectors[begin+occurrence+1],vectors[begin+occurrence+2],vectors[begin+occurrence+3]};
                    qwen_quant_dot_quad(gate_data,gate_type,tile_vectors,hidden,row0+i,gate_values);
                    qwen_quant_dot_quad(up_data,up_type,tile_vectors,hidden,row0+i,up_values);
                }else for(int token=0;token<tile;++token){
                    gate_values[token]=qwen_quant_dot(gate_data,gate_type,vectors[begin+occurrence+token],hidden,row0+i);
                    up_values[token]=qwen_quant_dot(up_data,up_type,vectors[begin+occurrence+token],hidden,row0+i);
                }
                for(int token=0;token<tile;++token){const int token_rank=occurrences[begin+occurrence+token];const float gate_value=gate_values[token]*gate_scale,clipped=std::max(-80.0f,std::min(80.0f,gate_value));activated[static_cast<std::size_t>(token_rank)*intermediate+(row0+i)]=gate_value/(1.0f+std::exp(-clipped))*up_values[token]*up_scale;}
                occurrence+=tile;
            }
            continue;
        }
        thread_local std::vector<float> gate_block,up_block,gate_values,up_values;
        gate_block.resize(static_cast<std::size_t>(kRowBlock)*hidden);up_block.resize(static_cast<std::size_t>(kRowBlock)*hidden);
        gate_values.resize(static_cast<std::size_t>(kRowBlock)*count);up_values.resize(static_cast<std::size_t>(kRowBlock)*count);
        const std::uint64_t t_dq0=moe_profile?qwen_moe_now():0;
        for(int i=0;i<mr;++i){
            qwen_dequant_row(gate_data,gate_type,hidden,row0+i,gate_block.data()+static_cast<std::size_t>(i)*hidden);
            qwen_dequant_row(up_data,up_type,hidden,row0+i,up_block.data()+static_cast<std::size_t>(i)*hidden);
        }
        const std::uint64_t t_gemm0=moe_profile?qwen_moe_now():0;
        qwen_f32_gemm_rows(gate_block.data(),mr,&vectors[begin],count,hidden,gate_values.data());
        qwen_f32_gemm_rows(up_block.data(),mr,&vectors[begin],count,hidden,up_values.data());
        const std::uint64_t t_act0=moe_profile?qwen_moe_now():0;
        if(moe_profile){g_cpu_moe_profile.gate_dequant+=t_gemm0-t_dq0;
            g_cpu_moe_profile.gate_gemm+=t_act0-t_gemm0;
            ++g_cpu_moe_profile.tasks_gate;}
        for(int occurrence=0;occurrence<count;++occurrence){
            const int token_rank=occurrences[begin+occurrence];
            float*dst=activated+static_cast<std::size_t>(token_rank)*intermediate+row0;
            for(int i=0;i<mr;++i){
                const float gate_value=gate_values[static_cast<std::size_t>(i)*count+occurrence]*gate_scale;
                const float clipped=std::max(-80.0f,std::min(80.0f,gate_value));
                dst[i]=gate_value/(1.0f+std::exp(-clipped))*up_values[static_cast<std::size_t>(i)*count+occurrence]*up_scale;
            }
        }
        if(moe_profile)g_cpu_moe_profile.gate_activate+=qwen_moe_now()-t_act0;
    }
    static std::vector<const float*> activated_vectors;
    activated_vectors.resize(offsets[experts]);
    for(int slot=0;slot<offsets[experts];++slot)
        activated_vectors[slot]=activated+static_cast<std::size_t>(occurrences[slot])*intermediate;
    if(!runtime.imatrix_offsets.empty())
        for(const int expert:group_experts)
            qwen_imatrix_note_expert(runtime,layer.expert_tensors[2],expert,
                                     &activated_vectors[offsets[expert]],
                                     counts[expert],intermediate);
    const int down_blocks=(hidden+kRowBlock-1)/kRowBlock;
#pragma omp parallel for schedule(dynamic,schedule_chunk) num_threads(qwen_cpu_thread_count(runtime))
    for(int task=0;task<group_count*down_blocks;++task){
        const int group=task/down_blocks;const int row0=(task%down_blocks)*kRowBlock;
        const int mr=std::min(kRowBlock,hidden-row0);
        const int expert=group_experts[group];
        const int begin=offsets[expert],count=counts[expert];
        const auto*down_data=expert_data(2,expert);
        // The down projection is linear, so its per-expert scale rides on the
        // routing weight. Both callers pass the router weights unscaled, unlike
        // the single-token path which folds it in before calling qwen_cpu_moe.
        const float down_scale=qwen_expert_role_scale(runtime,layer.expert_down_scale,expert);
        if(count<=2){
            for(int i=0;i<mr;++i){
                float values[2]{};
                if(count==1)values[0]=qwen_quant_dot(down_data,down_type,activated_vectors[begin],intermediate,row0+i);
                else qwen_quant_dot_pair(down_data,down_type,activated_vectors[begin],activated_vectors[begin+1],intermediate,row0+i,values[0],values[1]);
                for(int occurrence=0;occurrence<count;++occurrence){const int token_rank=occurrences[begin+occurrence];down_values[static_cast<std::size_t>(token_rank)*hidden+(row0+i)]=weights[token_rank]*down_scale*values[occurrence];}
            }
            continue;
        }
        if(direct_quant){
            for(int i=0;i<mr;++i)for(int occurrence=0;occurrence<count;){
                const int remaining=count-occurrence;
                const int tile=direct_oct&&remaining>=8?8:std::min(4,remaining);
                float values[8]{};
                if(tile==8){
                    const float*tile_vectors[8]={activated_vectors[begin+occurrence],activated_vectors[begin+occurrence+1],activated_vectors[begin+occurrence+2],activated_vectors[begin+occurrence+3],activated_vectors[begin+occurrence+4],activated_vectors[begin+occurrence+5],activated_vectors[begin+occurrence+6],activated_vectors[begin+occurrence+7]};
                    qwen_quant_dot_oct(down_data,down_type,tile_vectors,intermediate,row0+i,values);
                }else if(tile==4){
                    const float*tile_vectors[4]={activated_vectors[begin+occurrence],activated_vectors[begin+occurrence+1],activated_vectors[begin+occurrence+2],activated_vectors[begin+occurrence+3]};
                    qwen_quant_dot_quad(down_data,down_type,tile_vectors,intermediate,row0+i,values);
                }else for(int token=0;token<tile;++token)
                    values[token]=qwen_quant_dot(down_data,down_type,activated_vectors[begin+occurrence+token],intermediate,row0+i);
                for(int token=0;token<tile;++token){const int token_rank=occurrences[begin+occurrence+token];down_values[static_cast<std::size_t>(token_rank)*hidden+(row0+i)]=weights[token_rank]*down_scale*values[token];}
                occurrence+=tile;
            }
            continue;
        }
        thread_local std::vector<float> down_block,values;
        down_block.resize(static_cast<std::size_t>(kRowBlock)*intermediate);values.resize(static_cast<std::size_t>(kRowBlock)*count);
        const std::uint64_t t_ddq0=moe_profile?qwen_moe_now():0;
        for(int i=0;i<mr;++i)qwen_dequant_row(down_data,down_type,intermediate,row0+i,down_block.data()+static_cast<std::size_t>(i)*intermediate);
        const std::uint64_t t_dgemm0=moe_profile?qwen_moe_now():0;
        qwen_f32_gemm_rows(down_block.data(),mr,&activated_vectors[begin],count,intermediate,values.data());
        const std::uint64_t t_dst0=moe_profile?qwen_moe_now():0;
        if(moe_profile){g_cpu_moe_profile.down_dequant+=t_dgemm0-t_ddq0;
            g_cpu_moe_profile.down_gemm+=t_dst0-t_dgemm0;
            ++g_cpu_moe_profile.tasks_down;}
        // Token-outermost so each token's mr results land contiguously. The
        // transposed order wrote one 4-byte value per 8 KB-strided cache line
        // of a 67 MB buffer -- a full line fetched, and read-for-owned, per
        // store. `values` is only kRowBlock*count floats, so the strided read
        // it leaves behind stays in L1. Arithmetic is unchanged: the original
        // already grouped as (weights*down_scale)*values, left to right.
        for(int occurrence=0;occurrence<count;++occurrence){
            const int token_rank=occurrences[begin+occurrence];
            const float scale=weights[token_rank]*down_scale;
            float*dst=down_values+static_cast<std::size_t>(token_rank)*hidden+row0;
            for(int i=0;i<mr;++i)
                dst[i]=scale*values[static_cast<std::size_t>(i)*count+occurrence];
        }
        if(moe_profile)g_cpu_moe_profile.down_store+=qwen_moe_now()-t_dst0;
    }
    const std::uint64_t t_comb0=moe_profile?qwen_moe_now():0;
#pragma omp parallel for schedule(static) num_threads(qwen_cpu_thread_count(runtime))
    for(int task=0;task<rows*hidden;++task){
        const int token=task/hidden,row=task%hidden;
        float value=0.0f;
        for(int rank=0;rank<routed_count;++rank){
            const int token_rank=token*routed_count+rank;
            if(weights[token_rank]!=0.0f)
                value+=down_values[static_cast<std::size_t>(token_rank)*hidden+row];
        }
        output[task]=value;
    }
    if(moe_profile)g_cpu_moe_profile.combine+=qwen_moe_now()-t_comb0;
    const auto scalar_types_after=
        g_scalar_dequant_types.load(std::memory_order_relaxed);
    if(scalar_types_after!=scalar_types_before){
        static std::once_flag once;
        std::call_once(once,[&]{
            std::string offenders;
            std::uint64_t listed=0;
            for(const auto type:{gate_type,up_type,down_type}){
                if(type>=kScalarDequantTypeLimit)continue;
                const auto bit=1ull<<type;
                if((scalar_types_after&bit)==0||(listed&bit)!=0)continue;
                listed|=bit;
                if(!offenders.empty())offenders+=", ";
                offenders+=std::to_string(type);
            }
            std::fprintf(stderr,
                "[flyweight] expert weight type(s) %s decode a row at a time "
                "on this CPU -- no vectorized decoder is admitted for them at "
                "widths %d/%d, which costs the batched MoE several times its "
                "speed on the affected layers\n",
                offenders.c_str(),hidden,intermediate);
        });
    }
    if (std::getenv("FLYWEIGHT_MOE_DEBUG")) {
        static int rcalls = 0;
        int n = rcalls++;
        if (n < 6) {
            double s = 0; for (int i = 0; i < rows * hidden; ++i) s += std::abs(output[i]);
            std::fprintf(stderr, "[rows-dbg] call=%d rows=%d g/u/d=%u/%u/%u sum|out|=%.4f\n",
                n, rows, gate_type, up_type, down_type, s);
        }
    }
}

int gpu_probe(FlyweightV2GpuInfo& out, int device) {
    std::memset(&out, 0, sizeof(out)); out.device = device;
#if defined(_WIN32)
    HMODULE lib = LoadLibraryW(L"nvcuda.dll");
    if (!lib) return 0;
    using Init = int (*)(unsigned int); using Retain = int (*)(void**, int); using Set = int (*)(void*); using SetFlags = int (*)(int, unsigned int); using Attr = int (*)(int*, int, int); using Mem = int (*)(size_t*, size_t*);
    auto init=reinterpret_cast<Init>(GetProcAddress(lib,"cuInit")); auto retain=reinterpret_cast<Retain>(GetProcAddress(lib,"cuDevicePrimaryCtxRetain")); auto set=reinterpret_cast<Set>(GetProcAddress(lib,"cuCtxSetCurrent")); auto set_flags=reinterpret_cast<SetFlags>(GetProcAddress(lib,"cuDevicePrimaryCtxSetFlags")); auto attr=reinterpret_cast<Attr>(GetProcAddress(lib,"cuDeviceGetAttribute")); auto mem=reinterpret_cast<Mem>(GetProcAddress(lib,"cuMemGetInfo_v2"));
    if (!init || !retain || !set || !attr || init(0)!=0) { FreeLibrary(lib); return 0; }
    if(set_flags&&(!std::getenv("FLYWEIGHT_CUDA_SPIN_WAIT")||std::getenv("FLYWEIGHT_CUDA_SPIN_WAIT")[0]!='1'))(void)set_flags(device,0x04);
    void* context=nullptr; if(retain(&context,device)!=0 || set(context)!=0) { FreeLibrary(lib); return 0; }
    int major=0,minor=0; if(attr(&major,75,device)!=0 || attr(&minor,76,device)!=0) { FreeLibrary(lib); return 0; } out.available=1; out.compute_major=major; out.compute_minor=minor; if(mem) { size_t free_bytes=0,total_bytes=0; if(mem(&free_bytes,&total_bytes)==0) { out.free_memory=free_bytes; out.total_memory=total_bytes; } } FreeLibrary(lib); return 0;
#else
    void* lib = dlopen("libcuda.so.1", RTLD_NOW | RTLD_LOCAL); if (!lib) lib = dlopen("libcuda.so", RTLD_NOW | RTLD_LOCAL); if (!lib) return 0;
    using Init = int (*)(unsigned int); using Retain = int (*)(void**, int); using Set = int (*)(void*); using SetFlags = int (*)(int, unsigned int); using Attr = int (*)(int*, int, int); using Mem = int (*)(size_t*, size_t*);
    auto init=reinterpret_cast<Init>(dlsym(lib,"cuInit")); auto retain=reinterpret_cast<Retain>(dlsym(lib,"cuDevicePrimaryCtxRetain")); auto set=reinterpret_cast<Set>(dlsym(lib,"cuCtxSetCurrent")); auto set_flags=reinterpret_cast<SetFlags>(dlsym(lib,"cuDevicePrimaryCtxSetFlags")); auto attr=reinterpret_cast<Attr>(dlsym(lib,"cuDeviceGetAttribute")); auto mem=reinterpret_cast<Mem>(dlsym(lib,"cuMemGetInfo_v2"));
    if (!init || !retain || !set || !attr || init(0)!=0) { dlclose(lib); return 0; }
    if(set_flags&&(!std::getenv("FLYWEIGHT_CUDA_SPIN_WAIT")||std::getenv("FLYWEIGHT_CUDA_SPIN_WAIT")[0]!='1'))(void)set_flags(device,0x04);
    void* context=nullptr; if(retain(&context,device)!=0 || set(context)!=0) { dlclose(lib); return 0; }
    int major=0,minor=0; if(attr(&major,75,device)!=0 || attr(&minor,76,device)!=0) { dlclose(lib); return 0; } out.available=1; out.compute_major=major; out.compute_minor=minor; if(mem) { size_t free_bytes=0,total_bytes=0; if(mem(&free_bytes,&total_bytes)==0) { out.free_memory=free_bytes; out.total_memory=total_bytes; } } dlclose(lib); return 0;
#endif
}

int plan_memory(FlyweightV2MemoryPlan& out, uint64_t budget, uint64_t static_weights, uint64_t kv_state, uint64_t workspace, uint64_t active, uint64_t staging) {
    if (static_weights > budget || kv_state > budget-static_weights || workspace > budget-static_weights-kv_state) { fail("persistent v2 GPU allocations exceed budget"); return -1; }
    out={}; out.budget=budget; out.static_weights=static_weights; out.kv_state=kv_state; out.workspace=workspace; uint64_t left=budget-static_weights-kv_state-workspace; out.active_experts=std::min(active,left); left-=out.active_experts; out.staging=std::min(staging,left); out.unused=left-out.staging; return 0;
}

extern "C" {
uint32_t flyweight_v2_version() { return 5; }
uint64_t flyweight_v2_runtime_options_size() { return sizeof(FlyweightV2QwenRuntimeOptions); }
uint64_t flyweight_v2_runtime_info_size() { return sizeof(FlyweightV2QwenRuntimeInfo); }
const char* flyweight_v2_last_error() { return error.c_str(); }
int flyweight_v2_gpu_probe(int32_t device, FlyweightV2GpuInfo* out) { return guarded([&]{ if(!out||device<0) throw std::runtime_error("invalid GPU probe arguments"); return gpu_probe(*out,device); }); }
int flyweight_v2_memory_plan(uint64_t budget,uint64_t static_weights,uint64_t kv_state,uint64_t workspace,uint64_t active,uint64_t staging,FlyweightV2MemoryPlan*out){return guarded([&]{if(!out)throw std::runtime_error("memory plan output is required");return plan_memory(*out,budget,static_weights,kv_state,workspace,active,staging);});}
int flyweight_v2_gpu_available(){return flyweight_gpu_available();}
int flyweight_v2_gpu_init(int32_t device){return flyweight_gpu_init(device);}
int flyweight_v2_gpu_compile(const char* source,const char*const*options,int32_t count,int32_t device,char*log,int32_t capacity){return flyweight_gpu_compile(source,options,count,device,log,capacity);}
int flyweight_v2_gpu_rms_norm(uint64_t input,uint64_t weights,uint64_t output,int32_t size,float epsilon,int32_t one_centered){return flyweight_gpu_rms_norm(input,weights,output,size,epsilon,one_centered);}
int flyweight_v2_gpu_q4_matvec(uint64_t packed,uint64_t scales,uint64_t input,uint64_t output,uint64_t stream,int32_t rows,int32_t columns){return flyweight_gpu_q4_matvec(packed,scales,input,output,stream,rows,columns);}
int flyweight_v2_gpu_dense_projection(uint64_t input,uint64_t norm_weights,uint64_t normalized,uint64_t packed,uint64_t scales,uint64_t projection,int32_t rows,int32_t columns,float epsilon,int32_t one_centered){int status=flyweight_gpu_rms_norm(input,norm_weights,normalized,columns,epsilon,one_centered);if(status)return status;return flyweight_gpu_q4_matvec(packed,scales,normalized,projection,0,rows,columns);}
int flyweight_v2_gpu_dense_residual(uint64_t input,uint64_t norm_weights,uint64_t normalized,uint64_t packed,uint64_t scales,uint64_t output,int32_t rows,int32_t columns,float epsilon,int32_t one_centered){if(rows!=columns)return -3;int status=flyweight_gpu_rms_norm(input,norm_weights,normalized,columns,epsilon,one_centered);if(status)return status;status=flyweight_gpu_q4_matvec(packed,scales,normalized,output,0,rows,columns);if(status)return status;return flyweight_gpu_scaled_add(output,input,1.0f,rows);}
int flyweight_v2_gpu_attention(uint64_t query,uint64_t keys,uint64_t values,uint64_t output,int32_t heads,int32_t kv_heads,int32_t head_dim,int32_t tokens,float scale){return flyweight_gpu_attention(query,keys,values,output,heads,kv_heads,head_dim,tokens,scale);}
int flyweight_v2_gpu_decoder_attention_step(uint64_t input,uint64_t norm_weights,uint64_t normalized,uint64_t qkv_packed,uint64_t qkv_scales,uint64_t qkv,uint64_t cache_keys,uint64_t cache_values,uint64_t attention_output,uint64_t out_packed,uint64_t out_scales,uint64_t output,int32_t hidden_size,int32_t heads,int32_t kv_heads,int32_t head_dim,int32_t position,int32_t capacity,float epsilon,int32_t one_centered){if(heads<=0||kv_heads<=0||heads%kv_heads!=0||head_dim<=0||position<0||position>=capacity)return -3;int status=flyweight_gpu_rms_norm(input,norm_weights,normalized,hidden_size,epsilon,one_centered);if(status)return status;const int q_rows=heads*head_dim;const int kv_rows=kv_heads*head_dim;status=flyweight_gpu_q4_matvec(qkv_packed,qkv_scales,normalized,qkv,0,q_rows+2*kv_rows,hidden_size);if(status)return status;status=flyweight_gpu_kv_append(qkv+q_rows*sizeof(float),qkv+(q_rows+kv_rows)*sizeof(float),cache_keys,cache_values,kv_heads,head_dim,position,capacity);if(status)return status;status=flyweight_gpu_attention_cache(qkv,cache_keys,cache_values,attention_output,heads,kv_heads,head_dim,position+1,capacity,1.0f/sqrtf(static_cast<float>(head_dim)));if(status)return status;status=flyweight_gpu_q4_matvec(out_packed,out_scales,attention_output,output,0,hidden_size,heads*head_dim);if(status)return status;return flyweight_gpu_scaled_add(output,input,1.0f,hidden_size);}
int flyweight_v2_kv_cache_create(uint64_t keys,uint64_t values,int32_t capacity,int32_t kv_heads,int32_t head_dim,FlyweightV2KvCache**out){return guarded([&]{if(!out||!keys||!values||capacity<=0||kv_heads<=0||head_dim<=0)throw std::runtime_error("invalid KV cache arguments");*out=new FlyweightV2KvCache{keys,values,capacity,kv_heads,head_dim,0};return 0;});}
void flyweight_v2_kv_cache_destroy(FlyweightV2KvCache*cache){try{delete cache;}catch(...){}}
int flyweight_v2_kv_cache_reset(FlyweightV2KvCache*cache){return guarded([&]{if(!cache){fail("invalid KV cache");return -1;}cache->position=0;return 0;});}
int flyweight_v2_kv_cache_position(const FlyweightV2KvCache*cache,int32_t*out){return guarded([&]{if(!cache||!out){fail("invalid KV cache position");return -1;}*out=cache->position;return 0;});}
int flyweight_v2_gpu_decoder_attention_cached(FlyweightV2KvCache*cache,uint64_t input,uint64_t norm_weights,uint64_t normalized,uint64_t qkv_packed,uint64_t qkv_scales,uint64_t qkv,uint64_t attention_output,uint64_t out_packed,uint64_t out_scales,uint64_t output,int32_t hidden_size,int32_t heads,float epsilon,int32_t one_centered){return guarded([&]{if(!cache){fail("invalid KV cache");return -1;}int status=flyweight_v2_gpu_decoder_attention_step(input,norm_weights,normalized,qkv_packed,qkv_scales,qkv,cache->keys,cache->values,attention_output,out_packed,out_scales,output,hidden_size,heads,cache->kv_heads,cache->head_dim,cache->position,cache->capacity,epsilon,one_centered);if(status)return status;++cache->position;return 0;});}
// Map one file into `model`, without interpreting a byte of it. GGUF shards and
// safetensors shards both come through here; only the caller knows which.
void map_file(const char* path, FlyweightV2Model& model) { FlyweightV2Model* m=&model; m->path=path;
#if !defined(_WIN32)
    m->fd=open(path,O_RDONLY); if(m->fd<0) throw std::runtime_error("cannot open GGUF"); struct stat st{}; if(fstat(m->fd,&st)!=0) throw std::runtime_error("cannot stat GGUF"); m->size=static_cast<size_t>(st.st_size);
#if defined(POSIX_FADV_SEQUENTIAL)
    // Weights are read front to back as the loader walks tensors, so tell the
    // kernel to read ahead accordingly. Advisory and free. The one region this
    // is wrong for -- the hashed n-gram table, which is gathered a row at a
    // time -- is marked back to random once the tensor map says where it is;
    // see qwen_advise_access_pattern.
    (void)posix_fadvise(m->fd,0,0,POSIX_FADV_SEQUENTIAL);
#endif
    const char* lock_env=std::getenv("FLYWEIGHT_V2_MLOCK"); const bool lock_model=lock_env&&lock_env[0]=='1';
    int map_flags=MAP_PRIVATE;
#ifdef MAP_POPULATE
    if(lock_model) map_flags|=MAP_POPULATE; // prefault every page so decode never page-faults on a cold expert
#endif
    // MAP_PRIVATE keeps the GGUF immutable on disk, while PROT_WRITE makes the
    // mapping eligible for CUDA host registration selected later by runtime
    // options. Model open necessarily happens before `--expert-paging` is
    // applied, so mapping permissions cannot depend on that option.
    m->data=static_cast<const uint8_t*>(mmap(nullptr,m->size,PROT_READ|PROT_WRITE,map_flags,m->fd,0)); if(m->data==MAP_FAILED) throw std::runtime_error("cannot map GGUF");
#if defined(MADV_HUGEPAGE)
    // Ask for 2 MiB pages. On the CPU backend the weights are read straight out
    // of this mapping (see qwen_alias_static_tensor), so a decode walks the
    // whole non-expert weight set every token -- 2.5 GiB for a 35B MoE, which
    // is ~650k pages at 4 KiB.
    //
    // Measured no effect on the machine this was written on: smaps_rollup
    // reported FilePmdMapped = 0 afterwards, i.e. the kernel declined to back a
    // file mapping with large folios despite THP being set to `always`. It is
    // kept because it is advisory, free, and correct where file THP is
    // supported -- but do not assume it is doing anything without checking
    // FilePmdMapped, and do not attribute a speedup to it on that assumption.
    (void)madvise(const_cast<uint8_t*>(m->data),m->size,MADV_HUGEPAGE);
#endif
    if(lock_model&&mlock(m->data,m->size)!=0) std::fprintf(stderr,"flyweight_v2: mlock(%zu bytes) failed: %s; continuing without pinning (raise RLIMIT_MEMLOCK to pin)\n",m->size,std::strerror(errno));
#else
    m->file=CreateFileA(path,GENERIC_READ,FILE_SHARE_READ,nullptr,OPEN_EXISTING,FILE_ATTRIBUTE_NORMAL,nullptr);
    if(m->file==INVALID_HANDLE_VALUE){m->file=nullptr;throw std::runtime_error("cannot open GGUF");}
    LARGE_INTEGER file_size{}; if(!GetFileSizeEx(m->file,&file_size)) throw std::runtime_error("cannot stat GGUF");
    m->size=static_cast<size_t>(file_size.QuadPart);
    // Read-only mapping: the GGUF is immutable (m->data is const and nothing
    // writes through it). Unlike PAGE_WRITECOPY, PAGE_READONLY is file-backed and
    // charges no pagefile commit, so mapping a 20+ GiB model does not fail under
    // commit pressure. CUDA host registration uses the READ_ONLY flag (0x08).
    m->mapping=CreateFileMappingW(m->file,nullptr,PAGE_READONLY,0,0,nullptr);
    if(!m->mapping) throw std::runtime_error("cannot create GGUF mapping");
    m->data=static_cast<const uint8_t*>(MapViewOfFile(m->mapping,FILE_MAP_READ,0,0,0));
    if(!m->data) throw std::runtime_error("cannot map GGUF");
    const char* lock_env=std::getenv("FLYWEIGHT_V2_MLOCK"); const bool lock_model=lock_env&&lock_env[0]=='1';
    if(lock_model){
        // Touch every page to prefault (MAP_POPULATE equivalent on Windows)
        volatile uint8_t sink=0; const size_t page_size=4096;
        for(size_t offset=0;offset<m->size;offset+=page_size) sink^=m->data[offset];
        // Attempt to enable SeLockMemoryPrivilege in the process token so
        // VirtualLock can pin the full working set.  This avoids requiring
        // the user to configure Local Security Policy manually.
        {
            HANDLE token=nullptr;
            if(OpenProcessToken(GetCurrentProcess(),TOKEN_ADJUST_PRIVILEGES|TOKEN_QUERY,&token)){
                TOKEN_PRIVILEGES tp{}; tp.PrivilegeCount=1;
                tp.Privileges[0].Attributes=SE_PRIVILEGE_ENABLED;
                if(LookupPrivilegeValueW(nullptr,L"SeLockMemoryPrivilege",&tp.Privileges[0].Luid))
                    AdjustTokenPrivileges(token,FALSE,&tp,0,nullptr,nullptr);
                CloseHandle(token);
            }
        }
        const size_t chunk=1024ULL*1024ULL*1024ULL; size_t locked=0;
        while(locked<m->size){
            size_t len=m->size-locked; if(len>chunk) len=chunk;
            if(VirtualLock(const_cast<uint8_t*>(m->data+locked),len)!=0) locked+=len;
            else break;
        }
        if(locked==0)
            std::fprintf(stderr,"flyweight_v2: VirtualLock failed (error %lu) — pages prefaulted "
                "but not pinned; enable SeLockMemoryPrivilege for pinning\n",GetLastError());
        else if(locked<m->size)
            std::fprintf(stderr,"flyweight_v2: pinned %zu/%zu bytes\n",locked,m->size);
        else
            std::fprintf(stderr,"flyweight_v2: pinned all %zu bytes\n",locked);
    }
#endif
}

// Map one GGUF file and parse its header. Shards of a split checkpoint come
// through here too, which is why it does not attach shards itself.
void map_and_parse(const char* path, FlyweightV2Model& model) {
    map_file(path,model);
    parse(model);
}

// gguf-split names shards "<prefix>-00001-of-00004.gguf" with a 1-based file
// index, while GGUF's own split.no is 0-based.
std::string split_shard_path(const std::string& path, std::uint32_t index, std::uint32_t count) {
    constexpr std::size_t kSuffix = 20; // "-00001-of-00004.gguf"
    const auto shaped=[&]{
        if(path.size()<=kSuffix)return false;
        const char* tail=path.c_str()+path.size()-kSuffix;
        if(tail[0]!='-'||std::memcmp(tail+6,"-of-",4)!=0||std::memcmp(tail+15,".gguf",5)!=0)return false;
        for(int digit=0;digit<5;++digit)
            if(!std::isdigit(static_cast<unsigned char>(tail[1+digit]))||
               !std::isdigit(static_cast<unsigned char>(tail[10+digit])))return false;
        return true;
    }();
    if(!shaped)throw std::runtime_error(
        "split GGUF path does not carry a \"-00001-of-0000N.gguf\" suffix: "+path);
    char suffix[32];
    std::snprintf(suffix,sizeof(suffix),"-%05u-of-%05u.gguf",index+1,count);
    return path.substr(0,path.size()-kSuffix)+suffix;
}

// Merge the remaining shards of a split checkpoint into `m`. Their descriptors
// keep pointing at their own mapping through Tensor::source, so downstream code
// never learns the model arrived in pieces.
void attach_split_shards(FlyweightV2Model& m) {
    if(m.split_count<=1)return;
    if(m.split_no>=m.split_count)
        throw std::runtime_error("split GGUF shard index is outside its own split count");
    m.shards.reserve(m.split_count-1);
    for(std::uint32_t index=0;index<m.split_count;++index){
        if(index==m.split_no)continue;
        const auto path=split_shard_path(m.path,index,m.split_count);
        auto shard=std::make_unique<FlyweightV2Model>();
        map_and_parse(path.c_str(),*shard);
        if(shard->split_count!=m.split_count||shard->split_no!=index)
            throw std::runtime_error("split GGUF shard disagrees about its place in the split: "+path);
        for(const auto& tensor:shard->tensors){
            auto merged=tensor;
            merged.source=shard->data;
            m.tensors.push_back(std::move(merged));
        }
        m.shards.push_back(std::move(shard));
    }
    if(m.split_tensors&&m.tensors.size()!=m.split_tensors)
        throw std::runtime_error("split GGUF holds "+std::to_string(m.tensors.size())+
            " tensors but declares "+std::to_string(m.split_tensors));
    // The draft block can live in any shard, so this only becomes decidable
    // once every descriptor is present.
    detect_mtp_layer(m);
}

// Resolve every tensor the layer plan calls for, and fail on the first one
// missing. Runs at open, which is the earliest point a misnamed tensor can be
// caught: the loader's own gate only proves descriptors point at the right
// bytes, never that they were given the right meaning.
void bailing_validate_plan(const FlyweightV2Model& m) {
    namespace bailing = flyweight::v2::bailing;
    const auto& config = m.config;
    if(!config.layer_count)throw std::runtime_error("bailingmoe3 config has no layers");
    std::unordered_set<std::string> present;
    present.reserve(m.tensors.size() * 2);
    for(const auto& tensor:m.tensors)present.insert(tensor.name);

    for(const char* name:{"token_embd.weight","output_norm.weight","output.weight"})
        if(!present.count(name))
            throw std::runtime_error(std::string("bailingmoe3 checkpoint is missing ")+name);

    std::uint32_t full_layers=0,linear_layers=0;
    for(std::uint32_t layer=0;layer<config.layer_count;++layer){
        const bool full=bailing::layer_is_full_attention(
            layer,config.layer_count,config.full_attention_interval);
        // The cadence is derived twice from the same config -- once by the
        // parser into sliding_window_pattern, once here -- so a disagreement
        // means one of the two transcriptions is wrong. A nonzero pattern entry
        // marks the linear (KDA) layers.
        if(layer<config.sliding_window_pattern.size()){
            const bool pattern_says_linear=config.sliding_window_pattern[layer]!=0;
            if(full==pattern_says_linear)
                throw std::runtime_error(
                    "bailingmoe3 layer cadence disagrees with the parsed pattern at layer "+
                    std::to_string(layer));
        }
        full?++full_layers:++linear_layers;
        const bool routed=config.expert_count&&layer>=config.leading_dense_block_count;
        const auto plan=bailing::layer_requirements(
            full,routed,config.expert_shared_count!=0,config.q_lora_rank!=0);
        const std::string prefix="blk."+std::to_string(layer)+".";
        for(const char* suffix:plan.names)
            if(!present.count(prefix+suffix))
                throw std::runtime_error(
                    "bailingmoe3 layer "+std::to_string(layer)+" is missing "+
                    prefix+suffix);
    }
    // The clamp counts are reported because they are otherwise invisible: a
    // checkpoint that carries them and a runtime that drops them differ only
    // in the tails of the last few layers' activations.
    const auto clamped=[](const std::vector<float>& limits){
        std::uint32_t count=0;
        for(const float limit:limits)if(limit>0.0f)++count;
        return count;
    };
    std::fprintf(stderr,
        "[flyweight] bailingmoe3: %u layers (%u MLA, %u KDA), %u experts "
        "top-%u in %u groups keeping %u, %u shared, query %s, "
        "swiglu clamp on %u routed / %u shared layers\n",
        config.layer_count,full_layers,linear_layers,config.expert_count,
        config.expert_used_count,config.expert_group_count,
        config.expert_group_used,config.expert_shared_count,
        config.q_lora_rank?"low-rank":"un-factored",
        clamped(config.swiglu_clamp_exp),clamped(config.swiglu_clamp_shexp));
}

// A llama.cpp-style bailingmoe3 GGUF is not this runtime's internal layout
// under different names: the conversion renames the KDA projections, stores A
// rather than A_log, and splits the MLA kv_b projection in two with the key
// half transposed per head. Adapt the descriptors in place so everything
// downstream -- the layer plan, both eval paths, the GPU upload -- sees exactly
// what the HF loader produces. Runs only on the GGUF path; the HF path already
// carries the internal layout.
//
// Two tensors differ in content rather than name, so their bytes are
// synthesized into `owned_arena`, which the model keeps alive for its lifetime
// (the same indirection the HF loader and split shards already use):
//   * ssm_a: the GGUF stores +exp(A_log); the kernels here consume A_log
//     itself and apply -exp() per step (flyweight_v2_bailing.hpp:905 and the
//     device kernel alike), so the load takes log() -- NOT a negation.
//   * attn_kv_b.weight: rebuilt from attn_k_b (stored [heads][kv_lora][qk_nope],
//     i.e. transposed per head for llama.cpp's absorbed matmul) and attn_v_b
//     ([heads][v_head][kv_lora]), un-transposing the key half so the merged
//     rows match kv_b_proj byte-for-byte with what the HF path yields.
void bailing_adapt_gguf(FlyweightV2Model& m) {
    namespace bailing = flyweight::v2::bailing;
    const auto& config = m.config;
    if(!config.layer_count)return;

    const auto find=[&m](const std::string& name)->Tensor*{
        for(auto& tensor:m.tensors)if(tensor.name==name)return &tensor;
        return nullptr;
    };

    struct Pending {
        std::string name;               // final (internal) tensor name
        std::vector<std::uint64_t> shape;  // GGUF order; empty = keep existing
        std::uint64_t offset=0, size=0; // into the synthesized arena
        bool replace=false;             // patch the descriptor of `name` in place
    };
    std::vector<Pending> pending;
    std::vector<std::uint8_t> arena;
    const auto reserve=[&arena](std::uint64_t bytes)->std::uint64_t{
        const auto at=arena.size();
        arena.resize(at+bytes);
        return at;
    };

    static const std::pair<const char*,const char*> kda_renames[]={
        {"attn_q.weight","ssm_q.weight"},
        {"attn_k.weight","ssm_k.weight"},
        {"attn_v.weight","ssm_v.weight"},
        {"attn_output.weight","ssm_out.weight"},
        {"ssm_conv1d_q.weight","ssm_q_conv1d.weight"},
        {"ssm_conv1d_k.weight","ssm_k_conv1d.weight"},
        {"ssm_conv1d_v.weight","ssm_v_conv1d.weight"},
        {"ssm_f_a.weight","ssm_f.weight"},
        {"ssm_beta.weight","ssm_b.weight"},
        {"ssm_g_a.weight","ssm_g.weight"},
    };

    std::vector<std::string> dropped;
    // One index past the stack when a nextn draft block rides along: its MLA
    // half ships the same split attn_k_b/attn_v_b as every full-attention
    // layer and needs the same re-merge before the draft can run.
    const std::uint32_t adapted_layers=config.layer_count+
        (m.mtp_layer!=std::numeric_limits<std::uint32_t>::max()?1u:0u);
    for(std::uint32_t layer=0;layer<adapted_layers;++layer){
        const std::string prefix="blk."+std::to_string(layer)+".";
        const bool full=layer>=config.layer_count||
            bailing::layer_is_full_attention(
                layer,config.layer_count,config.full_attention_interval);
        if(!full){
            // The KDA projections arrive under ordinary attention names, which
            // mean different weights on an MLA layer -- hence per-layer rather
            // than a blanket rename.
            for(const auto& [from,to]:kda_renames)
                if(auto* tensor=find(prefix+from);tensor&&!find(prefix+to))
                    tensor->name=prefix+to;
            if(auto* a=find(prefix+"ssm_a")){
                if(a->type!=0)
                    throw std::runtime_error(
                        "bailingmoe3 expects "+a->name+" to be f32 but it is "
                        "type "+std::to_string(a->type));
                const auto elements=tensor_elements(*a);
                const auto* values=
                    reinterpret_cast<const float*>(tensor_data(m,*a));
                const auto offset=reserve(elements*sizeof(float));
                auto* out=reinterpret_cast<float*>(arena.data()+offset);
                for(std::uint64_t i=0;i<elements;++i){
                    if(!(values[i]>0.0f))
                        throw std::runtime_error(
                            "bailingmoe3 "+a->name+" is not the positive "
                            "exp(A_log) a GGUF conversion writes");
                    out[i]=std::log(values[i]);
                }
                pending.push_back({a->name,{},offset,elements*sizeof(float),true});
            }
            continue;
        }
        if(find(prefix+"attn_kv_b.weight"))continue;
        auto* k_b=find(prefix+"attn_k_b.weight");
        auto* v_b=find(prefix+"attn_v_b.weight");
        if(!k_b||!v_b)continue;  // the layer plan will name what is missing
        const std::uint64_t heads=config.attention_heads;
        const std::uint64_t qk_nope=config.key_length-config.rotary_dimension;
        const std::uint64_t v_head=config.value_length;
        const std::uint64_t kv_lora=config.kv_lora_rank;
        // GGUF extents run fastest-first.
        const std::vector<std::uint64_t> k_shape{qk_nope,kv_lora,heads};
        const std::vector<std::uint64_t> v_shape{kv_lora,v_head,heads};
        if(k_b->shape!=k_shape||v_b->shape!=v_shape)
            throw std::runtime_error(
                "bailingmoe3 "+prefix+"attn_k_b/attn_v_b shapes disagree with "
                "the declared MLA geometry");
        std::vector<float> keys(heads*kv_lora*qk_nope);
        std::vector<float> values(heads*v_head*kv_lora);
        bailing::matrix_decode({tensor_data(m,*k_b),k_b->type},
                               qk_nope,heads*kv_lora,keys.data());
        bailing::matrix_decode({tensor_data(m,*v_b),v_b->type},
                               kv_lora,heads*v_head,values.data());
        const std::uint64_t merged_rows=heads*(qk_nope+v_head);
        const auto offset=reserve(merged_rows*kv_lora*sizeof(float));
        auto* out=reinterpret_cast<float*>(arena.data()+offset);
        for(std::uint64_t head=0;head<heads;++head){
            float* block=out+head*(qk_nope+v_head)*kv_lora;
            const float* k=keys.data()+head*kv_lora*qk_nope;
            for(std::uint64_t row=0;row<qk_nope;++row)
                for(std::uint64_t column=0;column<kv_lora;++column)
                    block[row*kv_lora+column]=k[column*qk_nope+row];
            std::memcpy(block+qk_nope*kv_lora,
                        values.data()+head*v_head*kv_lora,
                        v_head*kv_lora*sizeof(float));
        }
        pending.push_back({prefix+"attn_kv_b.weight",{kv_lora,merged_rows},
                           offset,merged_rows*kv_lora*sizeof(float),false});
        dropped.push_back(k_b->name);
        dropped.push_back(v_b->name);
    }

    // The arena must stop moving before any descriptor points into it.
    m.owned_arena=std::move(arena);
    for(const auto& entry:pending){
        if(entry.replace){
            auto* tensor=find(entry.name);
            tensor->type=0;
            tensor->offset=entry.offset;
            tensor->size=entry.size;
            tensor->source=m.owned_arena.data();
            continue;
        }
        Tensor tensor;
        tensor.name=entry.name;
        tensor.shape=entry.shape;
        tensor.type=0;
        tensor.offset=entry.offset;
        tensor.size=entry.size;
        tensor.source=m.owned_arena.data();
        m.tensors.push_back(std::move(tensor));
    }
    // The split halves have no consumer once the merge exists; leaving them
    // would make the GGUF model's tensor surface differ from the HF one.
    if(!dropped.empty())
        m.tensors.erase(
            std::remove_if(m.tensors.begin(),m.tensors.end(),
                [&dropped](const Tensor& tensor){
                    return std::find(dropped.begin(),dropped.end(),tensor.name)
                        !=dropped.end();
                }),
            m.tensors.end());
}

// An HF checkpoint is a directory: config.json plus one or more safetensors
// shards. A GGUF is a single file. That is the whole detection rule.
bool is_hf_directory(const char* path) {
    struct stat st{};
    if(stat(path,&st)!=0||!S_ISDIR(st.st_mode))return false;
    const std::string config=std::string(path)+"/config.json";
    return stat(config.c_str(),&st)==0;
}

std::string read_text_file(const std::string& path) {
    std::ifstream file(path,std::ios::binary);
    if(!file)return {};
    std::ostringstream buffer;
    buffer<<file.rdbuf();
    return buffer.str();
}

// Modification time in nanoseconds, for the cache fingerprint. The three
// spellings of `st_mtim` are a portability wart, not a design decision.
std::int64_t modified_ns(const struct stat& st) {
#if defined(__APPLE__)
    return static_cast<std::int64_t>(st.st_mtimespec.tv_sec)*1000000000+st.st_mtimespec.tv_nsec;
#elif defined(_WIN32)
    return static_cast<std::int64_t>(st.st_mtime)*1000000000;
#else
    return static_cast<std::int64_t>(st.st_mtim.tv_sec)*1000000000+st.st_mtim.tv_nsec;
#endif
}

// Where the quantized arena may live, most preferred first.
//
// Beside the checkpoint is the useful place: it survives cache eviction, it is
// obvious to anyone wondering what the extra file is, and deleting the model
// deletes it. But checkpoint directories are often read-only (a shared mount, a
// hub cache), so the user cache directory stands behind it. The file name
// carries the fingerprint, so a directory holding caches for several models --
// or several quantizations of one -- never collides.
std::vector<std::string> hf_cache_candidates(const std::string& directory,
                                             std::uint64_t print) {
    char name[64];
    std::snprintf(name,sizeof(name),"flyweight-%016llx.cache",
                  static_cast<unsigned long long>(print));

    const char* configured=std::getenv("FLYWEIGHT_HF_CACHE");
    if(configured){
        const std::string value=configured;
        // An explicit off switch matters for benchmarking the packers and for
        // machines where the extra 4 GB on disk is not wanted.
        if(value=="0"||value=="off"||value.empty())return {};
        return {value+"/"+name};
    }

    std::vector<std::string> out{directory+"/"+name};
    std::string base;
    if(const char* xdg=std::getenv("XDG_CACHE_HOME");xdg&&xdg[0])base=xdg;
    else if(const char* home=std::getenv("HOME");home&&home[0])base=std::string(home)+"/.cache";
#if defined(_WIN32)
    else if(const char* appdata=std::getenv("LOCALAPPDATA");appdata&&appdata[0])base=appdata;
#endif
    if(!base.empty()){
        const std::string folder=base+"/flyweight";
#if defined(_WIN32)
        CreateDirectoryA(folder.c_str(),nullptr);
#else
        ::mkdir(folder.c_str(),0755);
#endif
        out.push_back(folder+"/"+name);
    }
    return out;
}

namespace hf = flyweight::v2::hf;

// The quantization policy a name selects, or false when the name is not one.
// Shared by the loader and by the pre-load preview, so what a caller is offered
// and what the loader then does cannot drift apart.
bool hf_policy_for(const std::string& name, hf::Policy& policy) {
    hf::Target weights;
    if(name=="IQ2_XS")weights=hf::Target::IQ2_XS;
    else if(name=="Q2_K")weights=hf::Target::Q2_K;
    else if(name=="IQ3_XXS")weights=hf::Target::IQ3_XXS;
    else if(name=="Q3_K")weights=hf::Target::Q3_K;
    else if(name=="IQ4_XS")weights=hf::Target::IQ4_XS;
    else if(name=="Q4_K")weights=hf::Target::Q4_K;
    else if(name=="Q5_K")weights=hf::Target::Q5_K;
    else if(name=="Q6_K")weights=hf::Target::Q6_K;
    else if(name=="Q8_0")weights=hf::Target::Q8_0;
    else if(name=="F32")weights=hf::Target::F32;
    else return false;
    policy=hf::policy_for_weights(weights);
    return true;
}

// The targets whose packer reads an importance matrix. Everything else must
// not hash one into its cache fingerprint: a Q6_K cache going stale over a
// file only the IQ searches read would repack gigabytes for nothing.
bool hf_policy_reads_imatrix(const hf::Policy& policy) {
    return policy.weights==hf::Target::IQ3_XXS
        ||policy.weights==hf::Target::IQ4_XS
        ||policy.weights==hf::Target::IQ2_XS;
}

// IQ2_XS is the one target the matrix is mandatory for, not merely useful:
// at 2.31 bits the unweighted search cannot tell which channels can afford
// to be wrong, and llama.cpp refuses the format without a matrix for the
// same reason. Shared by the loader and the menu so an offer and a refusal
// cannot drift apart.
std::string hf_policy_needs_missing_imatrix(const hf::Policy& policy,
                                            bool imatrix_present) {
    if(policy.weights!=hf::Target::IQ2_XS||imatrix_present)return {};
    return "needs an importance matrix: place imatrix.dat beside the "
           "checkpoint, pass --imatrix, or gather one with "
           "`flyweight imatrix`";
}

// Empty when `policy` can be loaded for this checkpoint, else why it cannot.
//
// The dense path decodes more types than the routed-expert path does: Q2_K and
// Q3_K have a matvec, a tiled row GEMM and an embedding kernel, but no grouped
// expert kernel at all. Rather than pack a mixture-of-experts checkpoint into
// something that will fail at the first routed layer -- or, before
// qwen_grouped_expert_type_supported existed, decode as the wrong format --
// the combination is refused where it is chosen.
std::string hf_policy_unavailable(const hf::Policy& policy,
                                  const flyweight::v2::ModelConfig& config) {
    const auto type=static_cast<std::uint32_t>(policy.weights);
    if(config.expert_count&&(type==10||type==11))
        return "no GPU routed-expert kernel decodes it, which would leave "
               "every routed layer on the CPU; Q4_K is the smallest a "
               "mixture-of-experts checkpoint can be packed to";
    return {};
}

// Every .safetensors file in the directory, sorted. Shard order must not affect
// the result -- experts are placed by parsed index, not encounter order -- but
// sorting keeps logs reproducible and keeps the cache fingerprint off readdir
// order.
std::vector<std::string> hf_shard_files(const std::string& directory) {
    std::vector<std::string> files;
#if defined(_WIN32)
    {
        WIN32_FIND_DATAA find_data;
        HANDLE hfind = FindFirstFileA((directory+"\\*").c_str(), &find_data);
        if(hfind != INVALID_HANDLE_VALUE){
            do {
                const std::string name=find_data.cFileName;
                static const std::string suffix=".safetensors";
                if(name.size()>suffix.size()&&
                   name.compare(name.size()-suffix.size(),suffix.size(),suffix)==0)
                    files.push_back(directory+"/"+name);
            } while(FindNextFileA(hfind, &find_data));
            FindClose(hfind);
        }
    }
#else
    if(DIR* handle=opendir(directory.c_str())){
        while(dirent* entry=readdir(handle)){
            const std::string name=entry->d_name;
            static const std::string suffix=".safetensors";
            if(name.size()>suffix.size()&&
               name.compare(name.size()-suffix.size(),suffix.size(),suffix)==0)
                files.push_back(directory+"/"+name);
        }
        closedir(handle);
    }
#endif
    if(files.empty())throw std::runtime_error("no .safetensors files in "+directory);
    std::sort(files.begin(),files.end());
    return files;
}

// Fingerprint inputs from metadata alone: every shard is stat'd, none is read.
// Hashing 15.8 GB of content to decide whether to avoid re-quantizing it would
// cost more than the packing it saves.
std::vector<hf::cache::SourceFile> hf_cache_sources(
    const std::vector<std::string>& files
) {
    std::vector<hf::cache::SourceFile> sources;
    sources.reserve(files.size());
    for(const auto& file:files){
        struct stat st{};
        if(stat(file.c_str(),&st)!=0)throw std::runtime_error("cannot stat "+file);
        const auto cut=file.find_last_of('/');
        sources.push_back({cut==std::string::npos?file:file.substr(cut+1),
                           static_cast<std::uint64_t>(st.st_size),modified_ns(st)});
    }
    return sources;
}

// The importance matrix a quantization would read: FLYWEIGHT_HF_IMATRIX names
// one (or switches it off), and otherwise an `imatrix.dat` beside the
// checkpoint is picked up on its own -- that is where the ecosystem publishes
// them. Returns the path, or empty when there is none. `identity` is filled
// for the cache fingerprint; only a policy whose target consumes the matrix
// should hash it, so a Q6_K cache does not go stale over a file only IQ3_XXS
// reads.
std::string hf_imatrix_path(const std::string& directory,
                            hf::cache::SourceFile& identity) {
    std::string path;
    if(const char* configured=std::getenv("FLYWEIGHT_HF_IMATRIX")){
        const std::string value=configured;
        if(value=="0"||value=="off"||value.empty())return {};
        path=value;
    }else{
        path=directory+"/imatrix.dat";
    }
    struct stat st{};
    if(stat(path.c_str(),&st)!=0||!S_ISREG(st.st_mode)){
        // A configured path that does not exist is an error worth raising --
        // the user asked for weighted packing and silence would pack without
        // it. The automatic probe simply found nothing.
        if(std::getenv("FLYWEIGHT_HF_IMATRIX"))
            throw std::runtime_error("FLYWEIGHT_HF_IMATRIX points at "+path+
                                     ", which cannot be read");
        return {};
    }
    const auto cut=path.find_last_of('/');
    identity={cut==std::string::npos?path:path.substr(cut+1),
              static_cast<std::uint64_t>(st.st_size),modified_ns(st)};
    return path;
}

// Load an HF checkpoint: map every shard, translate the descriptors, quantize
// into an owned arena, then drop the mappings.
//
// Dropping them is the point of the ordering. The source is bf16 -- 15.8 GB for
// Ling-3.0-tiny -- and the arena is a quarter of that; keeping both alive would
// waste the difference for the whole process lifetime when nothing reads the
// originals again.
//
// Quantizing is hundreds of core-seconds, and its inputs are files that do not
// change, so the arena is written to a sidecar on the way out and mapped on the
// way back in. A hit skips the shard mappings entirely -- the 15.8 GB of bf16
// is never opened, let alone read.
void load_hf(const char* path, FlyweightV2Model& m) {
    namespace hf = flyweight::v2::hf;
    m.path=path;
    m.format_name="safetensors";

    // Phase timings under FLYWEIGHT_HF_PROFILE=1. "Open is slow" has at least
    // four candidate causes here -- JSON parsing, packing, disk, page faults --
    // and they take different fixes, so guessing between them is not useful.
    const bool profile=[]{
        const char* value=std::getenv("FLYWEIGHT_HF_PROFILE");
        return value&&value[0]=='1';
    }();
    auto mark=std::chrono::steady_clock::now();
    const auto phase=[&profile,&mark](const char* name){
        if(!profile)return;
        const auto now=std::chrono::steady_clock::now();
        std::fprintf(stderr,"[flyweight]   %-20s %7.2fs\n",name,
            std::chrono::duration<double>(now-mark).count());
        mark=now;
    };

    const std::string directory=path;
    const auto config_text=read_text_file(directory+"/config.json");
    if(config_text.empty())throw std::runtime_error("cannot read config.json");
    m.config=hf::config_from_json(flyweight::v2::json::parse(config_text));
    m.architecture=m.config.architecture;
    // The directory name is the closest thing an HF checkpoint has to a model
    // name; config.json carries no equivalent of general.name.
    const auto slash=directory.find_last_of('/');
    m.name=slash==std::string::npos?directory:directory.substr(slash+1);
    m.chat_template=read_text_file(directory+"/chat_template.jinja");
    // Checkpoints published before chat_template.jinja existed keep the
    // template in tokenizer_config.json -- one string, or a list of named
    // templates of which "default" is the conversational one. Reading only
    // the file left those checkpoints on the runtime's generic fallback
    // markup, silently.
    if(m.chat_template.empty()){
        if(const auto text=read_text_file(directory+"/tokenizer_config.json");
           !text.empty()){
            const auto parsed=flyweight::v2::json::parse(text);
            const auto& configured=parsed["chat_template"];
            if(configured.kind==flyweight::v2::json::Kind::String)
                m.chat_template=configured.string;
            else if(configured.kind==flyweight::v2::json::Kind::Array)
                for(const auto& entry:configured.array)
                    if(entry["name"].as_string()=="default"){
                        m.chat_template=entry["template"].as_string();
                        break;
                    }
        }
    }
    // config.json's eos is end-of-document; the turn ends on a different token
    // that only generation_config.json names, and it lists that one first.
    // Getting this wrong does not fail loudly -- generation simply never stops.
    if(const auto generation=read_text_file(directory+"/generation_config.json");
       !generation.empty()){
        std::uint32_t eos=0;
        if(hf::hf_generation_eos(flyweight::v2::json::parse(generation),eos))
            m.config.eos_token_id=eos;
    }
    phase("config.json");

    // tokenizer.json is ~12 MB of JSON on this checkpoint, most of it the
    // 157k-entry vocabulary, and it is parsed once at open.
    const auto tokenizer_text=read_text_file(directory+"/tokenizer.json");
    if(tokenizer_text.empty())
        throw std::runtime_error("cannot read tokenizer.json");
    phase("tokenizer.json read");
    auto tokenizer=hf::tokenizer_from_json(
        flyweight::v2::json::parse(tokenizer_text),m.config.vocabulary_size);
    phase("tokenizer.json parse");
    if(tokenizer.pre.empty())
        throw std::runtime_error(
            "tokenizer.json uses a pre-tokenizer pattern this runtime has not "
            "transcribed; refusing to tokenize with the wrong splitter");
    m.vocabulary=std::move(tokenizer.vocabulary);
    m.merges=std::move(tokenizer.merges);
    m.token_types=std::move(tokenizer.token_types);
    m.tokenizer_pre=std::move(tokenizer.pre);
    build_tokenizer_tables(m);
    phase("tokenizer tables");

    const auto files=hf_shard_files(directory);

    hf::Policy policy;
    if(const char* requested=std::getenv("FLYWEIGHT_HF_QUANT");
       requested&&!hf_policy_for(requested,policy))
        throw std::runtime_error("FLYWEIGHT_HF_QUANT must be one of "
            "IQ2_XS, Q2_K, IQ3_XXS, Q3_K, IQ4_XS, Q4_K, Q5_K, Q6_K, Q8_0, "
            "F32");

    if(const auto refusal=hf_policy_unavailable(policy,m.config);!refusal.empty())
        throw std::runtime_error(
            std::string("this checkpoint cannot be quantized as requested: ")+refusal);

    hf::cache::SourceFile imatrix_identity;
    const auto imatrix_path=hf_imatrix_path(directory,imatrix_identity);
    const bool imatrix_applies=
        !imatrix_path.empty()&&hf_policy_reads_imatrix(policy);
    if(const auto missing=
           hf_policy_needs_missing_imatrix(policy,!imatrix_path.empty());
       !missing.empty())
        throw std::runtime_error("IQ2_XS "+missing);

    const auto print=hf::cache::fingerprint(
        config_text,hf_cache_sources(files),policy,
        imatrix_applies?&imatrix_identity:nullptr);
    const auto candidates=hf_cache_candidates(directory,print);

    // Publish descriptors against whichever arena we end up with. `base` is
    // borrowed -- it is either a mapping this model keeps alive in `shards` or
    // the owned buffer -- which is the same indirection split GGUF already uses.
    const auto install=[&m](const std::vector<hf::QuantizedTensor>& source,
                            const std::uint8_t* base,std::uint64_t arena_offset){
        m.tensors.clear();
        m.tensors.reserve(source.size());
        for(const auto& entry:source){
            Tensor tensor;
            tensor.name=entry.name;
            tensor.shape=entry.shape;
            tensor.type=entry.type;
            tensor.offset=arena_offset+entry.offset;
            tensor.size=entry.size;
            tensor.source=base;
            m.tensors.push_back(std::move(tensor));
        }
        // The same scan the GGUF parse runs. Without it an HF checkpoint whose
        // `mtp.` tensors were translated to `blk.<layers>.nextn.*` still
        // reported no draft block, so MTP -- the one lever a bandwidth-bound
        // decode has -- was unavailable on exactly the checkpoints that carry
        // one. Here rather than at the two call sites because both the cache
        // hit and the freshly quantized path go through it.
        detect_mtp_layer(m);
    };

    const auto started=std::chrono::steady_clock::now();

    // Cache hit: the shards are never opened. Anything a candidate gets wrong
    // -- absent, unreadable, stale, truncated -- is a miss, so the loop just
    // moves on rather than distinguishing failures it would treat alike.
    for(const auto& candidate:candidates){
        auto mapping=std::make_unique<FlyweightV2Model>();
        try{
            map_file(candidate.c_str(),*mapping);
        }catch(const std::exception&){
            continue;
        }
        hf::cache::Contents contents;
        if(!hf::cache::decode(mapping->data,mapping->size,print,contents))continue;
        install(contents.tensors,mapping->data,contents.arena_offset);
        m.shards.push_back(std::move(mapping));
        phase("cache map");
        if(m.architecture=="bailingmoe3")bailing_validate_plan(m);
        std::fprintf(stderr,
            "[flyweight] safetensors: %llu tensors, %llu MiB mapped from %s "
            "in %.2fs\n",
            static_cast<unsigned long long>(m.tensors.size()),
            static_cast<unsigned long long>(contents.arena_bytes/(1024ull*1024)),
            candidate.c_str(),
            std::chrono::duration<double>(
                std::chrono::steady_clock::now()-started).count());
        return;
    }

    std::vector<hf::Shard> parsed;
    parsed.reserve(files.size());
    for(const auto& file:files){
        auto shard=std::make_unique<FlyweightV2Model>();
        map_file(file.c_str(),*shard);
        parsed.push_back({flyweight::v2::safetensors::parse_header(shard->data,shard->size),
                          shard->data});
        m.shards.push_back(std::move(shard));
    }
    phase("shard mmap");

    const auto tensors=hf::build_tensors(parsed,m.config);
    phase("build_tensors");
    // Loaded on the miss path only: a cache hit never packs, so it never needs
    // the matrix the pack would have read.
    hf::ImportanceMatrix imatrix;
    if(imatrix_applies){
        imatrix=hf::load_imatrix(imatrix_path);
        std::fprintf(stderr,
            "[flyweight] safetensors: weighting the pack with %s "
            "(%zu tensors)\n",
            imatrix_path.c_str(),imatrix.entries.size());
    }
    phase("imatrix");
    auto quantized=hf::quantize(tensors,policy,
                                imatrix_applies?&imatrix:nullptr);
    phase("quantize");
    const double seconds=std::chrono::duration<double>(
        std::chrono::steady_clock::now()-started).count();

    // Write before the arena moves into the model, while the source bytes are
    // still mapped -- a failure here costs nothing but the next open's time,
    // so it is reported and stepped over rather than raised.
    std::string cached;
    for(const auto& candidate:candidates)
        if(hf::cache::write(candidate,quantized,print)){cached=candidate;break;}
    phase("cache write");
    if(!candidates.empty()&&cached.empty())
        std::fprintf(stderr,
            "[flyweight] safetensors: could not write the quantized cache to "
            "%s; the next open will quantize again\n",candidates.back().c_str());

    m.owned_arena=std::move(quantized.arena);
    install(quantized.tensors,m.owned_arena.data(),0);
    // Every byte now lives in the arena, so the source mappings are dead
    // weight. Releasing them here also releases their file descriptors.
    m.shards.clear();

    if(m.architecture=="bailingmoe3")bailing_validate_plan(m);

    std::fprintf(stderr,
        "[flyweight] safetensors: %llu tensors, %llu MiB bf16 -> %llu MiB "
        "quantized in %.1fs%s%s\n",
        static_cast<unsigned long long>(m.tensors.size()),
        static_cast<unsigned long long>(quantized.source_bytes/(1024ull*1024)),
        static_cast<unsigned long long>(m.owned_arena.size()/(1024ull*1024)),
        seconds,
        cached.empty()?"":"; cached to ",cached.c_str());
}


// ---------------------------------------------------------------------------
// BailingMoE3 GPU decode
// ---------------------------------------------------------------------------
//
// Decode only, for now: one token at a time, which is the latency path and the
// one where the CPU is slowest. Prefill stays on the host because MoE routing
// would need either a device-side top-k or a sync per token, and neither is
// worth building before the decode path proves the wiring.
//
// Everything the layer needs already existed: `qwen_gpu_matvec_by_type`
// dispatches the quantized matvecs, `flyweight_gpu_rms_norm` the norms, and the
// two new kernels cover KDA and MLA. What is added here is residency and
// sequencing, not numerics.

struct BailingGpuMatrix {
    std::uint64_t data = 0;
    std::uint32_t type = 0;
};

struct BailingGpuLayer {
    // `q` and the q_a/q_b pair are alternatives, not both: see MlaWeights.
    BailingGpuMatrix q, q_a, q_b, kv_a_mqa, attn_gate, attn_output;
    std::uint64_t q_a_norm = 0, kv_a_norm = 0, kv_b = 0;
    BailingGpuMatrix ssm_q, ssm_k, ssm_v, ssm_f, ssm_b, ssm_g, ssm_out;
    std::uint64_t ssm_q_conv = 0, ssm_k_conv = 0, ssm_v_conv = 0;
    std::uint64_t ssm_a = 0, ssm_dt = 0, ssm_norm = 0;
    BailingGpuMatrix router, gate_exps, up_exps, down_exps;
    BailingGpuMatrix shared_gate, shared_up, shared_down;
    std::uint64_t router_bias = 0;
    std::uint64_t attn_norm = 0, ffn_norm = 0;
};

// The per-layer state that must persist across tokens -- and therefore the
// only thing on the device that belongs to ONE sequence rather than to the
// model. Everything in BailingGpuLayer above is a weight and everything in
// BailingGpu below is scratch that lives inside a single token's forward, so
// this struct is exactly the slot boundary.
struct BailingGpuSlotLayer {
    std::uint64_t latents = 0, rope_keys = 0;   // MLA cache
    std::uint64_t state = 0;                    // KDA recurrent state
    std::uint64_t conv_windows = 0;             // three windows, packed
};

struct BailingGpuSlot {
    std::vector<BailingGpuSlotLayer> layers;
};

struct BailingGpu {
    bool ready = false;
    std::uint64_t stream = 0;
    std::vector<BailingGpuLayer> layers;
    // One entry per sequence slot, each with one entry per layer.
    std::vector<BailingGpuSlot> slots;
    BailingGpuMatrix embedding, head;
    std::uint64_t output_norm = 0;
    // Scratch, sized once for the widest use.
    std::uint64_t hidden_a = 0, hidden_b = 0, normalized = 0, branch = 0;
    std::uint64_t wide_a = 0, wide_b = 0, wide_c = 0, wide_d = 0;
    std::uint64_t scores = 0, logits = 0, small = 0;
    std::uint64_t mla_projected = 0, mla_accumulated = 0, mla_partials = 0;
    // Grouped MoE: device-side arrays of the chosen experts' weight pointers.
    std::uint64_t expert_gate_ptrs = 0, expert_up_ptrs = 0, expert_down_ptrs = 0;
    std::uint64_t expert_weights = 0, expert_activated = 0;
    // Complete row-batched prefill workspace. The default 128-row MLA score
    // tile remains bounded enough for a 128K context on a 12 GiB card.
    std::uint32_t prefill_rows = 128;
    std::uint64_t prefill_tokens = 0;
    std::uint64_t prefill_hidden_a = 0, prefill_hidden_b = 0;
    std::uint64_t prefill_normalized = 0, prefill_branch = 0;
    std::uint64_t prefill_wide_a = 0, prefill_wide_b = 0;
    std::uint64_t prefill_wide_c = 0, prefill_wide_d = 0;
    std::uint64_t prefill_wide_e = 0;
    std::uint64_t prefill_scores = 0;
    std::uint64_t prefill_projected = 0, prefill_accumulated = 0;
    std::uint64_t prefill_router_logits = 0, prefill_selected = 0;
    std::uint64_t prefill_weights = 0, prefill_activated = 0;
    std::uint64_t prefill_expert_counts = 0, prefill_expert_routes = 0;
    std::uint64_t prefill_q8 = 0, prefill_q8_scales = 0;
    std::vector<std::uint64_t> owned;
};

// BailingMoE3 host execution.
//
// Resolves every layer's weights once, then runs tokens through
// bailing::decoder_layer. Deliberately plain: this is the correctness path and
// the oracle the eventual GPU kernels are measured against, so it favours being
// obviously right over being fast.
// One independent sequence. Slots run interleaved -- one forward pass at a
// time, a token at a time, round-robined by the caller -- so what a slot owns
// is state that PERSISTS between tokens, and nothing else. The scratch every
// forward pass writes and reads within one token stays shared.
struct BailingSlot {
    std::vector<flyweight::v2::bailing::LayerCache> caches;
    std::uint32_t position = 0;
    // MTP: the draft layer's own cache, the target's pre-norm hidden for the
    // last forwarded token (what the draft block conditions on), and the
    // per-KDA-layer scratch the fold rollback needs -- the state/window
    // snapshot taken before a verify and the retained transition inputs the
    // verify recorded. Persistent per slot so a round allocates nothing.
    flyweight::v2::bailing::LayerCache draft_cache;
    std::vector<float> target_hidden;
    bool has_target_hidden = false;
    std::vector<std::vector<float>> mtp_state, mtp_qwin, mtp_kwin, mtp_vwin;
    std::vector<std::vector<float>> mtp_q, mtp_k, mtp_v, mtp_decay, mtp_beta;
    // Which copy of this slot's per-layer cache is current. The host and device
    // each keep their own, and the two paths play to different strengths -- the
    // host has a batched prefill, the device has the faster decode -- so eval
    // uses whichever suits the call and moves the cache across when it has to.
    // False means the host copy is authoritative, the state after a reset.
    //
    // Per slot rather than per runtime: with several sequences live, one of
    // them taking a host prefill says nothing about where another one's cache
    // is, and a single shared flag would have each slot answering for the last
    // call any slot made.
    bool cache_on_device = false;
};

struct FlyweightV2BailingRuntime {
    const FlyweightV2Model* model = nullptr;
    flyweight::v2::bailing::Geometry geometry;
    std::vector<flyweight::v2::bailing::LayerWeights> layers;
    flyweight::v2::bailing::Matrix embedding_matrix;
    const float* output_norm = nullptr;
    flyweight::v2::bailing::Matrix head;
    std::uint32_t capacity = 0;
    // At least one, always; slot 0 is the sequence a single-sequence caller
    // gets without knowing slots exist.
    std::vector<BailingSlot> slots;
    // kv_b decoded to f32 per MLA layer; see MlaWeights::kv_b for why.
    std::vector<std::vector<float>> decoded_kv_b;
    // The nextn draft block, when the checkpoint carries one: a full MLA+MoE
    // decoder layer of its own plus the enorm/hnorm/eh_proj merge and a
    // dedicated head norm. Loaded host-side; unused unless a caller drives
    // flyweight_v2_bailing_mtp_round.
    struct {
        bool available = false;
        flyweight::v2::bailing::LayerWeights layer;
        std::vector<float> decoded_kv_b;
        const float* enorm = nullptr;
        const float* hnorm = nullptr;
        flyweight::v2::bailing::Matrix eh_proj;
        const float* head_norm = nullptr;
    } draft;
    std::uint64_t mtp_drafted = 0, mtp_accepted = 0, mtp_rejected = 0;
    std::unique_ptr<BailingGpu> gpu;
    // Checked, not assumed. Every entry point that names a slot comes through
    // here, so an out-of-range index is an error the caller sees rather than a
    // read past the end of the vector.
    BailingSlot& slot(std::uint32_t index) {
        if (index >= slots.size())
            throw std::runtime_error(
                "bailing slot " + std::to_string(index) +
                " is out of range; this runtime has " +
                std::to_string(slots.size()) +
                (slots.size() == 1 ? " slot" : " slots"));
        return slots[index];
    }
    // Optional watcher for long prompts; see flyweight_v2_bailing_set_progress.
    // `reported` and `reporting` are per-eval scratch, not configuration: the
    // internal paths report at whatever granularity is natural for them, and
    // deduplicating here is what makes the sequence the watcher sees strictly
    // increasing regardless of which path ran.
    int (*progress)(void*, std::uint32_t, std::uint32_t) = nullptr;
    void* progress_user = nullptr;
    std::uint32_t reported = 0;
    bool reporting = false;
};

// Tell the watcher how far a prompt has got, and let it stop the call.
//
// Cheap enough to sit in a per-tile or per-layer loop and nowhere near a
// per-element one: a null callback is a predictable branch, and a live one is
// one indirect call per ~128 tokens.
//
// Cancellation is an exception rather than a status because every caller of
// this is several frames deep inside a prefill; unwinding is the only way out
// that does not thread a "stop" return through the whole device path. What it
// leaves behind is a sequence advanced by however many tokens completed, which
// is consistent but not what the caller asked for -- hence the documented
// requirement to reset.
void bailing_progress(FlyweightV2BailingRuntime& runtime, std::uint32_t processed,
                      std::uint32_t total) {
    if (!runtime.progress) return;
    if (runtime.reporting && processed <= runtime.reported) return;
    runtime.reported = processed;
    runtime.reporting = true;
    if (runtime.progress(runtime.progress_user, processed, total) != 0)
        throw std::runtime_error("bailing prompt evaluation was cancelled");
}

// A weight in whatever storage the checkpoint used.
flyweight::v2::bailing::Matrix bailing_matrix(const FlyweightV2Model& m,
                                            const std::string& name,
                                            bool required = true) {
    for (const auto& tensor : m.tensors) {
        if (tensor.name != name) continue;
        return {tensor_data(m, tensor), tensor.type};
    }
    if (required) throw std::runtime_error("bailingmoe3 is missing " + name);
    return {};
}

// Small tensors the policy always keeps f32: norms, biases, A_log, dt_bias and
// the convolution taps, all of which are read as vectors rather than matvecs.
const float* bailing_f32(const FlyweightV2Model& m, const std::string& name,
                         bool required = true) {
    for (const auto& tensor : m.tensors) {
        if (tensor.name != name) continue;
        if (tensor.type != 0)
            throw std::runtime_error(
                "bailingmoe3 expects " + name + " to be f32 but it is type " +
                std::to_string(tensor.type));
        return reinterpret_cast<const float*>(tensor_data(m, tensor));
    }
    if (required) throw std::runtime_error("bailingmoe3 is missing " + name);
    return nullptr;
}

// Upload one tensor and remember it for release.
std::uint64_t bailing_gpu_upload(BailingGpu& gpu, const FlyweightV2Model& m,
                                 const std::string& name, bool required = true) {
    for (const auto& tensor : m.tensors) {
        if (tensor.name != name) continue;
        std::uint64_t pointer = 0;
        if (flyweight_gpu_alloc(tensor.size, &pointer) != 0)
            throw std::runtime_error("out of device memory uploading " + name);
        if (flyweight_gpu_upload_sync(pointer, tensor_data(m, tensor), tensor.size) != 0)
            throw std::runtime_error("failed to upload " + name);
        gpu.owned.push_back(pointer);
        return pointer;
    }
    if (required) throw std::runtime_error("bailingmoe3 is missing " + name);
    return 0;
}

BailingGpuMatrix bailing_gpu_matrix(BailingGpu& gpu, const FlyweightV2Model& m,
                                    const std::string& name, bool required = true) {
    for (const auto& tensor : m.tensors)
        if (tensor.name == name)
            return {bailing_gpu_upload(gpu, m, name, required), tensor.type};
    if (required) throw std::runtime_error("bailingmoe3 is missing " + name);
    return {};
}

std::uint64_t bailing_gpu_scratch(BailingGpu& gpu, std::size_t bytes) {
    std::uint64_t pointer = 0;
    if (flyweight_gpu_alloc(bytes, &pointer) != 0)
        throw std::runtime_error("out of device memory for bailing scratch");
    flyweight_gpu_memset(pointer, 0, bytes, 0);
    gpu.owned.push_back(pointer);
    return pointer;
}

// What one sequence slot costs on the device: the MLA caches sized to the full
// capacity, plus the fixed KDA recurrent state and convolution windows.
//
// Computed rather than discovered, because discovering it means allocating
// until the driver refuses -- which leaves a half-built runtime and an error
// naming a scratch buffer instead of the decision that caused it.
std::uint64_t bailing_slot_device_bytes(const FlyweightV2BailingRuntime& runtime) {
    const auto& g = runtime.geometry;
    const std::uint64_t channels = static_cast<std::uint64_t>(g.heads) * g.head_dim;
    const std::uint64_t history = g.conv_width ? g.conv_width - 1 : 0;
    std::uint64_t bytes = 0;
    for (const auto& layer : runtime.layers)
        bytes += layer.full_attention
            ? static_cast<std::uint64_t>(runtime.capacity) *
                  (g.kv_lora + g.qk_rope) * 4
            : (channels * g.head_dim + 3 * channels * history) * 4;
    return bytes;
}

// Whether a tensor belongs to a `blk.N` past the end of the decoder stack --
// the MTP draft block, and anything else a converter appends. `layer_count` is
// already trimmed to the executed stack by detect_mtp_layer, so the index is
// the whole test.
bool bailing_tensor_is_unexecuted_block(const FlyweightV2Model& m,
                                        const std::string& name) {
    if (name.rfind("blk.", 0) != 0) return false;
    const auto dot = name.find('.', 4);
    if (dot == std::string::npos) return false;
    std::uint64_t index = 0;
    for (std::size_t i = 4; i < dot; ++i) {
        if (name[i] < '0' || name[i] > '9') return false;
        index = index * 10 + static_cast<std::uint64_t>(name[i] - '0');
    }
    return index >= m.config.layer_count;
}

// Whether the bailing decode path has a device matvec for this storage --
// exactly the case list of qwen_gpu_matvec_by_type. Checked before anything is
// uploaded, because the alternative is discovering the gap as a throw out of
// the first forward pass, mid-generation, instead of a clean fall back to the
// host path at load.
bool bailing_gpu_weight_type(std::uint32_t type) {
    switch (type) {
        case 0: case 1: case 8: case 10: case 11: case 12: case 13: case 14:
        case 16: case 17: case 18: case 19: case 21: case 22: case 23:
        case 29: case 30:
            return true;
        default:
            return false;
    }
}

// Bring the whole model onto the device. Q4_K is 4.4 GB, which fits the 12 GB
// card with room for the caches and scratch.
void bailing_gpu_prepare(FlyweightV2BailingRuntime& runtime) {
    namespace bailing = flyweight::v2::bailing;
    auto& gpu = *runtime.gpu;
    const auto& m = *runtime.model;
    const auto& c = m.config;
    const auto& g = runtime.geometry;

    // Only what the decoder stack actually runs gets a vote here. A GGUF can
    // carry blocks past `layer_count` -- the MTP draft block is one, and
    // quantizers give it its own storage (Ling 3.0 Flash writes the whole
    // block at Q4_0) -- and nothing uploads or executes them. Letting such a
    // tensor veto the device path costs the whole model its GPU over bytes
    // that are never read.
    for (const auto& tensor : m.tensors) {
        if (tensor.shape.size() < 2) continue;
        if (bailing_tensor_is_unexecuted_block(m, tensor.name)) continue;
        if (!bailing_gpu_weight_type(tensor.type))
            throw std::runtime_error(
                "bailingmoe3 " + tensor.name + " is stored as type " +
                std::to_string(tensor.type) +
                ", which has no GPU kernel; running on the host");
    }
    // The embedding gather dispatches through the format table, which covers
    // a narrower set than the matvecs.
    for (const auto& tensor : m.tensors)
        if (tensor.name == "token_embd.weight" &&
            !(flyweight::v2::qwen_format(tensor.type) &&
              flyweight::v2::qwen_format(tensor.type)->embedding))
            throw std::runtime_error(
                "bailingmoe3 token_embd.weight is stored as type " +
                std::to_string(tensor.type) +
                ", which has no GPU embedding kernel; running on the host");

    if (flyweight_gpu_init(0) != 0) throw std::runtime_error("failed to initialize CUDA");
    std::vector<std::string> option_storage;
#if !defined(_WIN32)
    for (const char* path : {"/opt/cuda/include", "/usr/local/cuda/include", "/usr/include"})
        if (access((std::string(path) + "/cuda_fp16.h").c_str(), R_OK) == 0) {
            option_storage.push_back(std::string("-I") + path);
            if (access((std::string(path) + "/cccl/cub/config.cuh").c_str(), R_OK) == 0)
                option_storage.push_back(std::string("-I") + path + "/cccl");
        }
#endif
    std::vector<const char*> options;
    for (const auto& option : option_storage) options.push_back(option.c_str());
    std::array<char, 16384> log{};
    const std::string source = std::string(flyweight::v2::qwen_cuda_source) +
                               flyweight::v2::qwen_native_cuda_source;
    if (flyweight_gpu_compile(source.c_str(), options.data(),
                            static_cast<std::int32_t>(options.size()), 0,
                            log.data(), log.size()) != 0)
        throw std::runtime_error(std::string("failed to compile CUDA kernels: ") + log.data());
    if (flyweight_gpu_stream_create(&gpu.stream) != 0)
        throw std::runtime_error("failed to create CUDA stream");

    gpu.embedding = bailing_gpu_matrix(gpu, m, "token_embd.weight");
    gpu.head = bailing_gpu_matrix(gpu, m, "output.weight");
    gpu.output_norm = bailing_gpu_upload(gpu, m, "output_norm.weight");

    const std::size_t channels = g.heads * g.head_dim;
    const std::size_t qk = g.qk_nope + g.qk_rope;
    gpu.layers.resize(c.layer_count);
    for (std::uint32_t index = 0; index < c.layer_count; ++index) {
        auto& L = gpu.layers[index];
        const std::string prefix = "blk." + std::to_string(index) + ".";
        const auto& weights = runtime.layers[index];
        L.attn_norm = bailing_gpu_upload(gpu, m, prefix + "attn_norm.weight");
        L.ffn_norm = bailing_gpu_upload(gpu, m, prefix + "ffn_norm.weight");
        if (weights.full_attention) {
            if (g.q_lora) {
                L.q_a = bailing_gpu_matrix(gpu, m, prefix + "attn_q_a.weight");
                L.q_a_norm = bailing_gpu_upload(gpu, m, prefix + "attn_q_a_norm.weight");
                L.q_b = bailing_gpu_matrix(gpu, m, prefix + "attn_q_b.weight");
            } else {
                L.q = bailing_gpu_matrix(gpu, m, prefix + "attn_q.weight");
            }
            L.kv_a_mqa = bailing_gpu_matrix(gpu, m, prefix + "attn_kv_a_mqa.weight");
            L.kv_a_norm = bailing_gpu_upload(gpu, m, prefix + "attn_kv_a_norm.weight");
            L.attn_gate = bailing_gpu_matrix(gpu, m, prefix + "attn_gate.weight");
            L.attn_output = bailing_gpu_matrix(gpu, m, prefix + "attn_output.weight");
            // kv_b goes up already decoded: the absorbed attention walks along
            // its rows, and the host runtime decoded it for the same reason.
            const auto& decoded = runtime.decoded_kv_b[index];
            if (flyweight_gpu_alloc(decoded.size() * 4, &L.kv_b) != 0)
                throw std::runtime_error("out of device memory for kv_b");
            flyweight_gpu_upload_sync(L.kv_b, decoded.data(), decoded.size() * 4);
            gpu.owned.push_back(L.kv_b);
        } else {
            L.ssm_q = bailing_gpu_matrix(gpu, m, prefix + "ssm_q.weight");
            L.ssm_k = bailing_gpu_matrix(gpu, m, prefix + "ssm_k.weight");
            L.ssm_v = bailing_gpu_matrix(gpu, m, prefix + "ssm_v.weight");
            L.ssm_f = bailing_gpu_matrix(gpu, m, prefix + "ssm_f.weight");
            L.ssm_b = bailing_gpu_matrix(gpu, m, prefix + "ssm_b.weight");
            L.ssm_g = bailing_gpu_matrix(gpu, m, prefix + "ssm_g.weight");
            L.ssm_out = bailing_gpu_matrix(gpu, m, prefix + "ssm_out.weight");
            L.ssm_q_conv = bailing_gpu_upload(gpu, m, prefix + "ssm_q_conv1d.weight");
            L.ssm_k_conv = bailing_gpu_upload(gpu, m, prefix + "ssm_k_conv1d.weight");
            L.ssm_v_conv = bailing_gpu_upload(gpu, m, prefix + "ssm_v_conv1d.weight");
            L.ssm_a = bailing_gpu_upload(gpu, m, prefix + "ssm_a");
            L.ssm_dt = bailing_gpu_upload(gpu, m, prefix + "ssm_dt.bias");
            L.ssm_norm = bailing_gpu_upload(gpu, m, prefix + "ssm_norm.weight");
        }
        if (weights.routed) {
            L.router = bailing_gpu_matrix(gpu, m, prefix + "ffn_gate_inp.weight");
            L.router_bias = bailing_gpu_upload(gpu, m, prefix + "exp_probs_b.bias", false);
            L.gate_exps = bailing_gpu_matrix(gpu, m, prefix + "ffn_gate_exps.weight");
            L.up_exps = bailing_gpu_matrix(gpu, m, prefix + "ffn_up_exps.weight");
            L.down_exps = bailing_gpu_matrix(gpu, m, prefix + "ffn_down_exps.weight");
            L.shared_gate = bailing_gpu_matrix(gpu, m, prefix + "ffn_gate_shexp.weight", false);
            L.shared_up = bailing_gpu_matrix(gpu, m, prefix + "ffn_up_shexp.weight", false);
            L.shared_down = bailing_gpu_matrix(gpu, m, prefix + "ffn_down_shexp.weight", false);
        } else {
            L.gate_exps = bailing_gpu_matrix(gpu, m, prefix + "ffn_gate.weight");
            L.up_exps = bailing_gpu_matrix(gpu, m, prefix + "ffn_up.weight");
            L.down_exps = bailing_gpu_matrix(gpu, m, prefix + "ffn_down.weight");
        }
    }

    const std::size_t widest = std::max({channels, g.heads * qk, (std::size_t)g.dense_size,
                                         g.kv_lora + g.qk_rope, (std::size_t)g.experts});
    gpu.hidden_a = bailing_gpu_scratch(gpu, g.hidden * 4);
    gpu.hidden_b = bailing_gpu_scratch(gpu, g.hidden * 4);
    gpu.normalized = bailing_gpu_scratch(gpu, g.hidden * 4);
    gpu.branch = bailing_gpu_scratch(gpu, g.hidden * 4);
    gpu.wide_a = bailing_gpu_scratch(gpu, widest * 4);
    gpu.wide_b = bailing_gpu_scratch(gpu, widest * 4);
    gpu.wide_c = bailing_gpu_scratch(gpu, widest * 4);
    gpu.wide_d = bailing_gpu_scratch(gpu, widest * 4);
    gpu.scores = bailing_gpu_scratch(gpu, (std::size_t)g.heads * runtime.capacity * 4);
    gpu.mla_projected = bailing_gpu_scratch(gpu, (std::size_t)g.heads * g.kv_lora * 4);
    gpu.mla_accumulated = bailing_gpu_scratch(gpu, (std::size_t)g.heads * g.kv_lora * 4);
    // Partial sums for the position-split accumulate: one [heads, kv_lora]
    // slab per split, 64 splits at most.
    gpu.mla_partials = bailing_gpu_scratch(gpu, (std::size_t)64 * g.heads * g.kv_lora * 4);
    gpu.logits = bailing_gpu_scratch(gpu, (std::size_t)c.vocabulary_size * 4);
    gpu.small = bailing_gpu_scratch(gpu, widest * 4);
    // One allocation holding gate, up and down pointer arrays followed by the
    // weights, so the per-layer update is a single transfer.
    gpu.expert_gate_ptrs = bailing_gpu_scratch(gpu, g.experts_used * 28);
    gpu.expert_up_ptrs = gpu.expert_gate_ptrs + g.experts_used * 8;
    gpu.expert_down_ptrs = gpu.expert_gate_ptrs + g.experts_used * 16;
    gpu.expert_weights = gpu.expert_gate_ptrs + g.experts_used * 24;
    gpu.expert_activated = bailing_gpu_scratch(gpu, g.experts_used * g.expert_size * 4);
    if (const char* setting = std::getenv("FLYWEIGHT_BAILING_PREFILL_ROWS")) {
        const long requested = std::strtol(setting, nullptr, 10);
        if (requested >= 1 && requested <= 512)
            gpu.prefill_rows = static_cast<std::uint32_t>(requested);
    }
    const std::size_t batch = gpu.prefill_rows;
    gpu.prefill_tokens = bailing_gpu_scratch(gpu, batch * sizeof(std::uint32_t));
    gpu.prefill_hidden_a = bailing_gpu_scratch(gpu, batch * g.hidden * 4);
    gpu.prefill_hidden_b = bailing_gpu_scratch(gpu, batch * g.hidden * 4);
    gpu.prefill_normalized = bailing_gpu_scratch(gpu, batch * g.hidden * 4);
    gpu.prefill_branch = bailing_gpu_scratch(gpu, batch * g.hidden * 4);
    gpu.prefill_wide_a = bailing_gpu_scratch(gpu, batch * widest * 4);
    gpu.prefill_wide_b = bailing_gpu_scratch(gpu, batch * widest * 4);
    gpu.prefill_wide_c = bailing_gpu_scratch(gpu, batch * widest * 4);
    gpu.prefill_wide_d = bailing_gpu_scratch(gpu, batch * widest * 4);
    gpu.prefill_wide_e = bailing_gpu_scratch(gpu, batch * widest * 4);
    const char* fused_mla_setting = std::getenv("FLYWEIGHT_BAILING_FUSED_MLA");
    if (!fused_mla_setting || fused_mla_setting[0] != '1')
        gpu.prefill_scores = bailing_gpu_scratch(
            gpu, batch * g.heads * runtime.capacity * 4);
    gpu.prefill_projected = bailing_gpu_scratch(
        gpu, batch * g.heads * g.kv_lora * 4);
    gpu.prefill_accumulated = bailing_gpu_scratch(
        gpu, batch * g.heads * g.kv_lora * 4);
    gpu.prefill_router_logits = bailing_gpu_scratch(gpu, batch * g.experts * 4);
    gpu.prefill_selected = bailing_gpu_scratch(
        gpu, batch * g.experts_used * sizeof(std::int32_t));
    gpu.prefill_weights = bailing_gpu_scratch(gpu, batch * g.experts_used * 4);
    gpu.prefill_expert_counts = bailing_gpu_scratch(
        gpu, g.experts * sizeof(std::int32_t));
    gpu.prefill_expert_routes = bailing_gpu_scratch(
        gpu, g.experts * batch * g.experts_used * sizeof(std::int32_t));
    gpu.prefill_activated = bailing_gpu_scratch(
        gpu, batch * g.experts_used * g.expert_size * 4);
    const std::size_t q8_elements = batch * widest;
    gpu.prefill_q8 = bailing_gpu_scratch(gpu, q8_elements);
    gpu.prefill_q8_scales = bailing_gpu_scratch(
        gpu, ((q8_elements + 31) / 32) * sizeof(std::uint16_t));

    // The per-slot caches go last, once the weights and the shared scratch are
    // already resident, so the free-memory reading below is what is actually
    // left for them rather than a guess with the rest subtracted by hand.
    //
    // A slot is the expensive part of a second sequence -- ~918 MiB on
    // Ling-3.0-tiny at a 65k capacity -- so a caller who asks for more than
    // fits deserves to be told the number they should have asked for, not a
    // "cuMemAlloc failed" from whichever layer happened to be next.
    const std::uint64_t per_slot = bailing_slot_device_bytes(runtime);
    const std::uint64_t wanted = per_slot * runtime.slots.size();
    FlyweightV2GpuInfo memory{};
    if (gpu_probe(memory, 0) == 0 && memory.available && memory.free_memory) {
        // A margin, not a rounding error: the driver keeps its own allocations
        // in this pool and a slot that fits with nothing to spare would leave
        // the first kernel launch to fail instead.
        const std::uint64_t margin = 64ull << 20;
        if (wanted + margin > memory.free_memory) {
            const std::uint64_t available =
                memory.free_memory > margin ? memory.free_memory - margin : 0;
            const auto mib = [](std::uint64_t bytes) {
                return std::to_string((bytes + (1ull << 20) - 1) >> 20);
            };
            const std::uint64_t fits = available / (per_slot ? per_slot : 1);
            throw std::runtime_error(
                "bailingmoe3 cannot fit " + std::to_string(runtime.slots.size()) +
                " sequence slot" + (runtime.slots.size() == 1 ? "" : "s") +
                " at a capacity of " + std::to_string(runtime.capacity) +
                " tokens: each slot needs " + mib(per_slot) + " MiB of device "
                "memory, " + std::to_string(runtime.slots.size()) + " need " +
                mib(wanted) + " MiB, and only " + mib(available) + " MiB is "
                "free after the weights and scratch -- short by " +
                mib(wanted + margin - memory.free_memory) + " MiB. Ask for " +
                std::to_string(fits) + " slot" + (fits == 1 ? "" : "s") +
                " at this capacity, or reduce the capacity.");
        }
    }
    gpu.slots.resize(runtime.slots.size());
    for (auto& slot : gpu.slots) {
        slot.layers.resize(c.layer_count);
        for (std::uint32_t index = 0; index < c.layer_count; ++index) {
            auto& S = slot.layers[index];
            if (runtime.layers[index].full_attention) {
                S.latents = bailing_gpu_scratch(
                    gpu, (std::size_t)runtime.capacity * g.kv_lora * 4);
                S.rope_keys = bailing_gpu_scratch(
                    gpu, (std::size_t)runtime.capacity * g.qk_rope * 4);
            } else {
                S.state = bailing_gpu_scratch(gpu, channels * g.head_dim * 4);
                S.conv_windows = bailing_gpu_scratch(
                    gpu, 3 * channels * (g.conv_width - 1) * 4);
            }
        }
    }
    gpu.ready = true;
    std::fprintf(stderr,
                 "[flyweight] bailingmoe3: %zu tensors resident on device, "
                 "%zu sequence slot(s) at %llu MiB each\n",
                 gpu.owned.size(), gpu.slots.size(),
                 static_cast<unsigned long long>((per_slot + (1ull << 20) - 1) >> 20));
}

// One token through the whole model, on the device.
//
// Routing is the one thing that comes back to the host: the router's 128 logits
// are downloaded, `bailing::moe_router` picks the experts, and the chosen
// expert matvecs are launched. That is one small sync per MoE layer, which is
// affordable for decode and would not be for prefill -- hence decode only.
// Bind this thread to the primary CUDA context.
//
// Driver contexts are per thread. The weights were uploaded on whichever thread
// built the runtime, but a server runs generation on an engine worker, which
// starts with no current context -- and there every launch, upload and download
// fails. Binding costs nothing after the first call on a thread.
//
// EVERY entry point that touches the device has to do this, not just the one
// that runs kernels. `bailing_cache_transfer` did not, and its uploads failed
// on the server's worker with "bailing cache transfer failed" while every
// single-threaded test passed.
void bailing_gpu_bind_thread() {
    static thread_local bool bound = false;
    if (bound) return;
    if (flyweight_gpu_init(0) != 0)
        throw std::runtime_error("failed to bind CUDA context to this thread");
    bound = true;
}

// Move the per-layer cache between the host and the device.
//
// The two copies have identical layouts -- the same row strides, the same
// [position][channel] order, the three conv windows in the same q/k/v order --
// because the device path was written against the host one. So this is a
// straight transfer, not a conversion, and the only thing to get right is how
// much of it is live: only `position` rows of the MLA caches hold anything, and
// copying `capacity` rows would move megabytes of zeros on every switch.
//
// Cost is small enough not to need managing: ~1 MB of MLA cache per 512
// positions plus a fixed ~1 MB of recurrent state per KDA layer, once per
// prefill/decode transition rather than per token.
//
// One slot's cache, never all of them: with several sequences live, moving
// somebody else's cache across would at best waste the bus and at worst
// overwrite a sequence that is only paused.
void bailing_cache_transfer(FlyweightV2BailingRuntime& runtime,
                            std::uint32_t slot_index, bool to_device) {
    if (!runtime.gpu || !runtime.gpu->ready) return;
    bailing_gpu_bind_thread();
    auto& slot = runtime.slot(slot_index);
    auto& device_slot = runtime.gpu->slots[slot_index];
    const auto& g = runtime.geometry;
    const std::size_t position = slot.position;
    const std::size_t channels = g.heads * g.head_dim;
    const std::size_t history = g.conv_width - 1;

    auto move = [&](const char* what, std::size_t layer, std::uint64_t device,
                    float* host, std::size_t elements) {
        if (!device || !elements) return;
        const std::uint64_t bytes = static_cast<std::uint64_t>(elements) * 4;
        const int status = to_device ? flyweight_gpu_upload(device, host, bytes, 0)
                                     : flyweight_gpu_download(host, device, bytes, 0);
        if (status != 0)
            throw std::runtime_error(
                std::string("bailing cache ") + (to_device ? "upload" : "download") +
                " failed for " + what + " of layer " + std::to_string(layer) +
                " (" + std::to_string(bytes) + " bytes, status " +
                std::to_string(status) + ")");
    };

    for (std::size_t index = 0; index < runtime.layers.size(); ++index) {
        auto& S = device_slot.layers[index];
        auto& cache = slot.caches[index];
        if (runtime.layers[index].full_attention) {
            move("latents", index, S.latents, cache.latents.data(),
                 position * g.kv_lora);
            move("rope keys", index, S.rope_keys, cache.rope_keys.data(),
                 position * g.qk_rope);
        } else {
            move("state", index, S.state, cache.state.data(), channels * g.head_dim);
            const std::size_t window = channels * history;
            move("query window", index, S.conv_windows,
                 cache.query_window.data(), window);
            move("key window", index, S.conv_windows + window * 4,
                 cache.key_window.data(), window);
            move("value window", index, S.conv_windows + window * 8,
                 cache.value_window.data(), window);
        }
    }
    if (flyweight_gpu_sync() != 0)
        throw std::runtime_error("bailing cache transfer failed to synchronize");
    slot.cache_on_device = to_device;
}

void bailing_gpu_step(FlyweightV2BailingRuntime& runtime, std::uint32_t slot_index,
                      std::uint32_t token, float* logits) {
    namespace bailing = flyweight::v2::bailing;
    bailing_gpu_bind_thread();
    auto& gpu = *runtime.gpu;
    auto& slot = runtime.slot(slot_index);
    auto& device_slot = gpu.slots[slot_index];
    const auto& g = runtime.geometry;
    const auto& c = runtime.model->config;
    // Everything on the default stream. flyweight_gpu_rms_norm and
    // flyweight_gpu_scaled_add take no stream argument and use the default one,
    // so issuing the matvecs and custom kernels on gpu.stream left the two
    // halves of every layer unordered with respect to each other -- which
    // produced finite but uncorrelated logits, the classic symptom.
    const std::uint64_t stream = 0;
    const std::size_t channels = g.heads * g.head_dim;
    const std::size_t qk = g.qk_nope + g.qk_rope;
    const std::size_t position = slot.position;
    const char* grouped_q6_env =
        std::getenv("FLYWEIGHT_BAILING_GROUPED_Q6_DECODE");
    const bool grouped_q6 = !grouped_q6_env || grouped_q6_env[0] != '0';

    auto matvec = [&](const BailingGpuMatrix& w, std::uint64_t in, std::uint64_t out,
                      std::size_t inputs, std::size_t outputs) {
        if (qwen_gpu_matvec_by_type(w.type, w.data, in, out,
                                    static_cast<int>(inputs),
                                    static_cast<int>(outputs), stream) != 0)
            throw std::runtime_error("bailing GPU matvec failed for type " +
                                     std::to_string(w.type));
    };
    // Phase timing for the device path. Each stop syncs, which perturbs by
    // ~16us a call -- measured, and negligible against a 20ms token.
    const bool timing = bailing::profiling();
    auto clock_now = []{ return std::chrono::duration<double>(
        std::chrono::steady_clock::now().time_since_epoch()).count(); };
    double mark = timing ? (flyweight_gpu_sync(), clock_now()) : 0.0;
    auto stop = [&](double& bucket) {
        if (!timing) return;
        flyweight_gpu_sync();
        const double now = clock_now();
        bucket += now - mark;
        mark = now;
    };
    auto launch = [&](const char* name, std::uint32_t grid, std::uint32_t block,
                      void** arguments) {
        if (flyweight_gpu_launch_named(name, grid, 1, block, 0, stream, arguments) != 0)
            throw std::runtime_error(std::string("bailing GPU launch failed: ") + name);
    };
    auto launch_2d = [&](const char* name, std::uint32_t grid_x,
                         std::uint32_t grid_y, std::uint32_t block,
                         void** arguments) {
        if (flyweight_gpu_launch_named(name, grid_x, grid_y, block, 0, stream,
                                     arguments) != 0)
            throw std::runtime_error(std::string("bailing GPU launch failed: ") + name);
    };

    // Embedding row -> hidden_a.
    {
        int token_arg = static_cast<int>(token), hidden = static_cast<int>(g.hidden);
        std::uint64_t packed = gpu.embedding.data, out = gpu.hidden_a;
        void* arguments[] = {&packed, &out, &token_arg, &hidden};
        launch(qwen_embedding_kernel(gpu.embedding.type, false), 32, 256, arguments);
    }
    stop(bailing::profile().norms);

    std::uint64_t current = gpu.hidden_a, next = gpu.hidden_b;
    for (std::size_t index = 0; index < runtime.layers.size(); ++index) {
        const auto& weights = runtime.layers[index];
        auto& L = gpu.layers[index];
        auto& S = device_slot.layers[index];
        flyweight_gpu_rms_norm(current, L.attn_norm, gpu.normalized,
                             static_cast<int>(g.hidden), g.epsilon, 0);
        if (weights.full_attention) {
            if (g.q_lora) {
                matvec(L.q_a, gpu.normalized, gpu.wide_a, g.hidden, g.q_lora);
                flyweight_gpu_rms_norm(gpu.wide_a, L.q_a_norm, gpu.wide_b,
                                     static_cast<int>(g.q_lora), g.epsilon, 0);
                matvec(L.q_b, gpu.wide_b, gpu.wide_c, g.q_lora, g.heads * qk);
            } else {
                matvec(L.q, gpu.normalized, gpu.wide_c, g.hidden, g.heads * qk);
            }
            matvec(L.kv_a_mqa, gpu.normalized, gpu.wide_d, g.hidden,
                   g.kv_lora + g.qk_rope);
            flyweight_gpu_rms_norm(gpu.wide_d, L.kv_a_norm,
                                 S.latents + position * g.kv_lora * 4,
                                 static_cast<int>(g.kv_lora), g.epsilon, 0);
            {
                // rope the shared key half, in place at its cache slot.
                std::uint64_t source = gpu.wide_d + g.kv_lora * 4;
                std::uint64_t target = S.rope_keys + position * g.qk_rope * 4;
                int count = static_cast<int>(g.qk_rope);
                void* copy_args[] = {&target, &source, &count};
                launch("bailing_copy", 1, 64, copy_args);
                int head_dim = static_cast<int>(g.qk_rope);
                int rope_dim = static_cast<int>(g.qk_rope);
                int pos = static_cast<int>(position), heads_arg = 1;
                float theta = g.rope_theta;
                void* arguments[] = {&target, &heads_arg, &head_dim, &rope_dim,
                                     &pos, &theta};
                launch("bailing_partial_rope", 1, 64, arguments);
            }
            {
                int heads_arg = static_cast<int>(g.heads);
                int head_dim = static_cast<int>(qk), rope_dim = static_cast<int>(g.qk_rope);
                int pos = static_cast<int>(position);
                float theta = g.rope_theta;
                void* arguments[] = {&gpu.wide_c, &heads_arg, &head_dim, &rope_dim,
                                     &pos, &theta};
                launch("bailing_partial_rope", static_cast<std::uint32_t>(g.heads),
                       64, arguments);
            }
            {
                // Split the query into its nope and rope halves for the kernel.
                int heads_arg = static_cast<int>(g.heads);
                int nope = static_cast<int>(g.qk_nope), rope = static_cast<int>(g.qk_rope);
                void* arguments[] = {&gpu.wide_c, &gpu.wide_a, &gpu.wide_b,
                                     &heads_arg, &nope, &rope};
                launch("bailing_split_query", static_cast<std::uint32_t>(g.heads),
                       128, arguments);
            }
            {
                int positions = static_cast<int>(position + 1);
                int heads_arg = static_cast<int>(g.heads);
                int nope = static_cast<int>(g.qk_nope), rope = static_cast<int>(g.qk_rope);
                int value_dim = static_cast<int>(g.v_head_dim);
                int lora = static_cast<int>(g.kv_lora);
                const char* tiled_env = std::getenv("FLYWEIGHT_BAILING_MLA_TILED");
                const bool tiled = !tiled_env || tiled_env[0] != '0';
                // Fused kernels read each latent cache row once for all heads
                // instead of once per head; the per-head kernels remain the
                // A/B fallback. "0" disables both, "s" fuses only the score
                // phase, "a" only the accumulate phase, anything else both.
                const char* fused_env = std::getenv("FLYWEIGHT_BAILING_MLA_FUSED");
                const bool fused_scores =
                    !fused_env || (fused_env[0] != '0' && fused_env[0] != 'a');
                const bool fused_accumulate =
                    !fused_env || (fused_env[0] != '0' && fused_env[0] != 's');
                const bool fused_any = fused_scores || fused_accumulate;
                if (tiled) {
                    void* project_args[] = {&gpu.wide_a, &L.kv_b,
                        &gpu.mla_projected, &heads_arg, &nope, &value_dim, &lora};
                    if (fused_any)
                        launch_2d("bailing_mla_project_fused",
                                  static_cast<std::uint32_t>(g.heads),
                                  static_cast<std::uint32_t>((lora + 127) / 128),
                                  128, project_args);
                    else
                        launch("bailing_mla_project",
                               static_cast<std::uint32_t>(g.heads), 128,
                               project_args);
                    void* score_args[] = {&gpu.mla_projected, &gpu.wide_b,
                        &S.latents, &S.rope_keys, &gpu.scores, &positions,
                        &heads_arg, &nope, &rope, &lora};
                    if (fused_scores)
                        launch("bailing_mla_scores_fused",
                               static_cast<std::uint32_t>((positions + 7) / 8),
                               256, score_args);
                    else
                        launch_2d("bailing_mla_scores",
                                  static_cast<std::uint32_t>(g.heads),
                                  static_cast<std::uint32_t>((positions + 7) / 8),
                                  256, score_args);
                    void* softmax_args[] = {&gpu.scores, &positions, &heads_arg};
                    launch("bailing_mla_softmax",
                           static_cast<std::uint32_t>(g.heads), 128, softmax_args);
                    if (fused_accumulate) {
                        int splits = positions / 128;
                        if (splits < 1) splits = 1;
                        if (splits > 64) splits = 64;
                        std::uint64_t target =
                            splits > 1 ? gpu.mla_partials : gpu.mla_accumulated;
                        void* accumulate_args[] = {&gpu.scores, &S.latents,
                            &target, &positions, &heads_arg, &lora, &splits};
                        launch_2d("bailing_mla_accumulate_fused",
                                  static_cast<std::uint32_t>(splits),
                                  static_cast<std::uint32_t>((lora + 127) / 128),
                                  128, accumulate_args);
                        if (splits > 1) {
                            void* reduce_args[] = {&gpu.mla_partials,
                                &gpu.mla_accumulated, &heads_arg, &lora, &splits};
                            launch_2d("bailing_mla_accumulate_reduce",
                                      static_cast<std::uint32_t>(g.heads),
                                      static_cast<std::uint32_t>((lora + 127) / 128),
                                      128, reduce_args);
                        }
                    } else {
                        void* accumulate_args[] = {&gpu.scores, &S.latents,
                            &gpu.mla_accumulated, &positions, &heads_arg, &lora};
                        launch_2d("bailing_mla_accumulate",
                                  static_cast<std::uint32_t>(g.heads),
                                  static_cast<std::uint32_t>((lora + 127) / 128),
                                  128, accumulate_args);
                    }
                    void* output_args[] = {&gpu.mla_accumulated, &L.kv_b,
                        &gpu.wide_d, &heads_arg, &nope, &value_dim, &lora};
                    if (fused_any)
                        launch_2d("bailing_mla_output_fused",
                                  static_cast<std::uint32_t>(g.heads),
                                  static_cast<std::uint32_t>((value_dim + 7) / 8),
                                  256, output_args);
                    else
                        launch("bailing_mla_output",
                               static_cast<std::uint32_t>(g.heads), 128,
                               output_args);
                } else {
                    void* arguments[] = {&gpu.wide_a, &gpu.wide_b, &L.kv_b,
                        &S.latents, &S.rope_keys, &gpu.scores, &gpu.wide_d,
                        &positions, &heads_arg, &nope, &rope, &value_dim, &lora};
                    launch("bailing_mla_attention",
                           static_cast<std::uint32_t>(g.heads), 128, arguments);
                }
            }
            matvec(L.attn_gate, gpu.normalized, gpu.small, g.hidden, g.heads);
            {
                int heads_arg = static_cast<int>(g.heads);
                int value_dim = static_cast<int>(g.v_head_dim);
                void* arguments[] = {&gpu.small, &heads_arg, &value_dim, &gpu.wide_d};
                launch("bailing_head_gate", static_cast<std::uint32_t>(g.heads),
                       128, arguments);
            }
            matvec(L.attn_output, gpu.wide_d, gpu.branch, g.heads * g.v_head_dim, g.hidden);
            stop(bailing::profile().mla);
        } else {
            matvec(L.ssm_q, gpu.normalized, gpu.wide_a, g.hidden, channels);
            matvec(L.ssm_k, gpu.normalized, gpu.wide_b, g.hidden, channels);
            matvec(L.ssm_v, gpu.normalized, gpu.wide_c, g.hidden, channels);
            {
                int channels_arg = static_cast<int>(channels);
                int width = static_cast<int>(g.conv_width);
                std::uint64_t wq = S.conv_windows;
                std::uint64_t wk = S.conv_windows + channels * (g.conv_width - 1) * 4;
                std::uint64_t wv = wk + channels * (g.conv_width - 1) * 4;
                void* a1[] = {&gpu.wide_a, &L.ssm_q_conv, &wq, &channels_arg, &width};
                launch("bailing_short_conv", 32, 256, a1);
                void* a2[] = {&gpu.wide_b, &L.ssm_k_conv, &wk, &channels_arg, &width};
                launch("bailing_short_conv", 32, 256, a2);
                void* a3[] = {&gpu.wide_c, &L.ssm_v_conv, &wv, &channels_arg, &width};
                launch("bailing_short_conv", 32, 256, a3);
            }
            matvec(L.ssm_f, gpu.normalized, gpu.wide_d, g.hidden, channels);
            matvec(L.ssm_b, gpu.normalized, gpu.small, g.hidden, g.heads);
            {
                int rows = 1, heads_arg = static_cast<int>(g.heads);
                int head_dim = static_cast<int>(g.head_dim);
                float epsilon = g.epsilon;
                std::uint64_t out = gpu.wide_a;   // reuse: queries are consumed
                void* arguments[] = {&gpu.wide_a, &gpu.wide_b, &gpu.wide_c,
                                     &gpu.wide_d, &gpu.small, &L.ssm_a, &L.ssm_dt,
                                     &S.state, &out, &rows, &heads_arg, &head_dim,
                                     &epsilon};
                launch("bailing_kda_recurrent_chunk",
                       static_cast<std::uint32_t>(g.heads), 128, arguments);
            }
            matvec(L.ssm_g, gpu.normalized, gpu.wide_b, g.hidden, channels);
            {
                int heads_arg = static_cast<int>(g.heads);
                int head_dim = static_cast<int>(g.head_dim);
                float epsilon = g.epsilon;
                void* arguments[] = {&gpu.wide_a, &L.ssm_norm, &gpu.wide_b,
                                     &heads_arg, &head_dim, &epsilon};
                launch("bailing_gated_head_norm",
                       static_cast<std::uint32_t>(g.heads), 128, arguments);
            }
            matvec(L.ssm_out, gpu.wide_a, gpu.branch, channels, g.hidden);
            stop(bailing::profile().kda);
        }
        flyweight_gpu_scaled_add(gpu.branch, current, 1.0f, static_cast<int>(g.hidden));
        flyweight_gpu_rms_norm(gpu.branch, L.ffn_norm, gpu.normalized,
                             static_cast<int>(g.hidden), g.epsilon, 0);

        if (weights.routed) {
            matvec(L.router, gpu.normalized, gpu.small, g.hidden, g.experts);
            std::vector<float> router_logits(g.experts);
            flyweight_gpu_sync();
            flyweight_gpu_download(router_logits.data(), gpu.small, g.experts * 4, 0);
            std::vector<std::int32_t> chosen(g.experts_used);
            std::vector<float> expert_weights(g.experts_used);
            bailing::moe_router(router_logits.data(),
                                runtime.layers[index].ffn.router_bias,
                                g.experts, g.experts_used, g.groups, g.groups_used,
                                g.weight_scale, g.normalize_weights,
                                chosen.data(), expert_weights.data());
            // One launch for all chosen experts, not one per expert. The
            // per-expert loop issued 8 x (3 matvecs + swiglu) = 32 tiny kernels
            // per MoE layer, 874 of the 1168 launches per token, each giving
            // the GPU only 512 blocks of work. These grouped kernels grid over
            // (rows, experts) instead, so the card sees 4096 blocks and the
            // launch count drops to 2.
            const auto narrow = g.expert_size *
                bailing::row_bytes(L.gate_exps.type, g.hidden);
            const auto wide = g.hidden *
                bailing::row_bytes(L.down_exps.type, g.expert_size);
            const auto expert_type = L.gate_exps.type;
            const bool grouped = (expert_type == 12 ||
                    (expert_type == 14 && grouped_q6)) &&
                L.up_exps.type == expert_type &&
                L.down_exps.type == expert_type;
            // q4k_grouped_accumulate does `output[row] += partial`, so the
            // target has to start at zero. Both paths need this.
            flyweight_gpu_memset(gpu.wide_d, 0, g.hidden * 4, stream);
            if (grouped) {
                const bool q6_grouped = expert_type == 14;
                std::vector<std::uint64_t> gate_ptrs(g.experts_used),
                    up_ptrs(g.experts_used), down_ptrs(g.experts_used);
                for (std::size_t slot = 0; slot < g.experts_used; ++slot) {
                    const auto expert = static_cast<std::size_t>(chosen[slot]);
                    gate_ptrs[slot] = L.gate_exps.data + expert * narrow;
                    up_ptrs[slot] = L.up_exps.data + expert * narrow;
                    down_ptrs[slot] = L.down_exps.data + expert * wide;
                }
                // One upload, not four. Each flyweight_gpu_upload_sync blocks,
                // and four per MoE layer is 92 blocking transfers per token --
                // measured at ~15us each, which was more than the expert
                // matmuls they were feeding. The three pointer arrays and the
                // weights share one device buffer and one copy.
                const std::size_t pointer_bytes = g.experts_used * 8;
                std::vector<std::uint8_t> packed(pointer_bytes * 3 +
                                                 g.experts_used * 4);
                std::memcpy(packed.data(), gate_ptrs.data(), pointer_bytes);
                std::memcpy(packed.data() + pointer_bytes, up_ptrs.data(), pointer_bytes);
                std::memcpy(packed.data() + pointer_bytes * 2, down_ptrs.data(),
                            pointer_bytes);
                std::memcpy(packed.data() + pointer_bytes * 3, expert_weights.data(),
                            g.experts_used * 4);
                flyweight_gpu_upload_sync(gpu.expert_gate_ptrs, packed.data(),
                                        packed.size());
                int inputs = static_cast<int>(g.hidden);
                int outputs = static_cast<int>(g.expert_size);
                int used = static_cast<int>(g.experts_used);
                void* swiglu_args[] = {&gpu.expert_gate_ptrs, &gpu.expert_up_ptrs,
                                       &gpu.normalized, &gpu.expert_activated,
                                       &inputs, &outputs, &used};
                const char* swiglu_kernel = q6_grouped
                    ? "bailing_q6_grouped_swiglu_warp"
                    : "q4k_grouped_swiglu";
                if (flyweight_gpu_launch_named(swiglu_kernel,
                        static_cast<std::uint32_t>(q6_grouped
                            ? (g.expert_size + 7) / 8 : g.expert_size),
                        static_cast<std::uint32_t>(g.experts_used),
                        q6_grouped ? 256 : 128, 0,
                        stream, swiglu_args) != 0)
                    throw std::runtime_error(std::string(swiglu_kernel) +
                                             " launch failed");
                int down_inputs = static_cast<int>(g.expert_size);
                int down_outputs = static_cast<int>(g.hidden);
                void* accumulate_args[] = {&gpu.expert_down_ptrs, &gpu.expert_activated,
                                           &gpu.wide_d, &gpu.expert_weights,
                                           &down_inputs, &down_outputs, &used};
                const char* accumulate_kernel = q6_grouped
                    ? "bailing_q6_grouped_accumulate_warp"
                    : "q4k_grouped_accumulate";
                if (flyweight_gpu_launch_named(accumulate_kernel,
                        static_cast<std::uint32_t>(q6_grouped
                            ? (g.hidden + 7) / 8 : g.hidden),
                        1, q6_grouped ? 256 : 128, 0, stream,
                        accumulate_args) != 0)
                    throw std::runtime_error(std::string(accumulate_kernel) +
                                             " launch failed");
            } else {
            for (std::size_t slot = 0; slot < g.experts_used; ++slot) {
                const auto expert = static_cast<std::size_t>(chosen[slot]);
                BailingGpuMatrix eg{L.gate_exps.data + expert * narrow, L.gate_exps.type};
                BailingGpuMatrix eu{L.up_exps.data + expert * narrow, L.up_exps.type};
                BailingGpuMatrix ed{L.down_exps.data + expert * wide, L.down_exps.type};
                matvec(eg, gpu.normalized, gpu.wide_a, g.hidden, g.expert_size);
                matvec(eu, gpu.normalized, gpu.wide_b, g.hidden, g.expert_size);
                {
                    int size = static_cast<int>(g.expert_size);
                    float limit = weights.swiglu_limit_exp;
                    void* arguments[] = {&gpu.wide_a, &gpu.wide_b, &size, &limit,
                                         &gpu.wide_c};
                    launch("bailing_swiglu", 32, 256, arguments);
                }
                matvec(ed, gpu.wide_c, gpu.wide_b, g.expert_size, g.hidden);
                flyweight_gpu_scaled_add(gpu.wide_d, gpu.wide_b, expert_weights[slot],
                                       static_cast<int>(g.hidden));
            }
            }
            if (L.shared_gate.data) {
                matvec(L.shared_gate, gpu.normalized, gpu.wide_a, g.hidden, g.shared_size);
                matvec(L.shared_up, gpu.normalized, gpu.wide_b, g.hidden, g.shared_size);
                int size = static_cast<int>(g.shared_size);
                float limit = weights.swiglu_limit_shexp;
                void* arguments[] = {&gpu.wide_a, &gpu.wide_b, &size, &limit, &gpu.wide_c};
                launch("bailing_swiglu", 32, 256, arguments);
                matvec(L.shared_down, gpu.wide_c, gpu.wide_b, g.shared_size, g.hidden);
                flyweight_gpu_scaled_add(gpu.wide_d, gpu.wide_b, 1.0f,
                                       static_cast<int>(g.hidden));
            }
            int count = static_cast<int>(g.hidden);
            void* copy_args[] = {&next, &gpu.wide_d, &count};
            launch("bailing_copy", 32, 256, copy_args);
        } else {
            matvec(L.gate_exps, gpu.normalized, gpu.wide_a, g.hidden, g.dense_size);
            matvec(L.up_exps, gpu.normalized, gpu.wide_b, g.hidden, g.dense_size);
            int size = static_cast<int>(g.dense_size);
            float limit = weights.swiglu_limit_exp;
            void* arguments[] = {&gpu.wide_a, &gpu.wide_b, &size, &limit, &gpu.wide_c};
            launch("bailing_swiglu", 32, 256, arguments);
            matvec(L.down_exps, gpu.wide_c, next, g.dense_size, g.hidden);
        }
        flyweight_gpu_scaled_add(next, gpu.branch, 1.0f, static_cast<int>(g.hidden));
        stop(bailing::profile().moe);
        std::swap(current, next);
    }

    flyweight_gpu_rms_norm(current, gpu.output_norm, gpu.normalized,
                         static_cast<int>(g.hidden), g.epsilon, 0);
    matvec(gpu.head, gpu.normalized, gpu.logits, g.hidden, c.vocabulary_size);
    stop(bailing::profile().projections);   // reused bucket: the lm head
    flyweight_gpu_sync();
    flyweight_gpu_download(logits, gpu.logits, (std::size_t)c.vocabulary_size * 4, 0);
    if (timing) bailing::profile().tokens += 1;
    ++slot.position;
}

bool bailing_gpu_prefill_tiled_supported(const FlyweightV2BailingRuntime& runtime) {
    if (!runtime.gpu || !runtime.gpu->ready) return false;
    // The direct expert kernels currently decode Q6_K. Dense row kernels exist
    // for the other formats, but mixing in a host MoE would defeat the purpose
    // of a fully device-resident prompt tile.
    for (std::size_t index = 0; index < runtime.layers.size(); ++index) {
        const auto& weights = runtime.layers[index];
        const auto& layer = runtime.gpu->layers[index];
        if (weights.routed && (layer.gate_exps.type != 14 ||
                               layer.up_exps.type != 14 ||
                               layer.down_exps.type != 14)) return false;
    }
    return true;
}

void bailing_gpu_prefill_tiled(
    FlyweightV2BailingRuntime& runtime, std::uint32_t slot_index,
    const std::uint32_t* tokens, std::uint32_t count, float* logits
) {
    namespace bailing = flyweight::v2::bailing;
    bailing_gpu_bind_thread();
    auto& gpu = *runtime.gpu;
    auto& slot = runtime.slot(slot_index);
    auto& device_slot = gpu.slots[slot_index];
    const auto& g = runtime.geometry;
    const auto& c = runtime.model->config;
    const std::uint64_t stream = 0;
    const int hidden = static_cast<int>(g.hidden);
    const int heads = static_cast<int>(g.heads);
    const int channels = static_cast<int>(g.heads * g.head_dim);
    const int head_dim = static_cast<int>(g.head_dim);
    const int qk_nope = static_cast<int>(g.qk_nope);
    const int qk_rope = static_cast<int>(g.qk_rope);
    const int qk = qk_nope + qk_rope;
    const int value_dim = static_cast<int>(g.v_head_dim);
    const int lora = static_cast<int>(g.kv_lora);
    const int top_k = static_cast<int>(g.experts_used);
    const int experts = static_cast<int>(g.experts);
    const int groups = static_cast<int>(g.groups);
    const int groups_used = static_cast<int>(g.groups_used);
    const int expert_size = static_cast<int>(g.expert_size);
    const int dense_size = static_cast<int>(g.dense_size);
    const float epsilon = g.epsilon;
    const bool phase_profile = std::getenv("FLYWEIGHT_BAILING_TILED_PROFILE") != nullptr;
    double mla_seconds = 0.0, kda_seconds = 0.0;
    double mla_proj_seconds = 0.0, mla_scores_seconds = 0.0,
           mla_softmax_seconds = 0.0, mla_accumulate_seconds = 0.0,
           mla_out_seconds = 0.0;
    double routed_seconds = 0.0, dense_seconds = 0.0;
    double router_projection_seconds = 0.0, router_selection_seconds = 0.0;
    double expert_gate_seconds = 0.0, expert_down_seconds = 0.0;
    double shared_expert_seconds = 0.0;

    auto launch = [&](const char* name, std::uint32_t gx, std::uint32_t gy,
                      std::uint32_t block, void** args,
                      std::uint32_t shared = 0) {
        if (flyweight_gpu_launch_named(name, gx, gy, block, shared, stream, args) != 0)
            throw std::runtime_error(std::string("bailing tiled prefill kernel failed: ") + name);
    };
    auto rows_kernel = [](std::uint32_t type) -> const char* {
        switch (type) {
            case 8: return "q8_matmul_tiled";
            case 10: return "q2k_matmul_rows";
            case 11: return "q3k_matmul_rows";
            case 12: return "q4k_matmul_rows";
            case 13: return "q5k_matmul_rows";
            case 14: return "q6k_matmul_rows";
            case 16: return "iq2xxs_matmul_rows";
            case 17: return "iq2xs_matmul_rows";
            case 18: return "iq3xxs_matmul_rows";
            case 21: return "iq3s_matmul_rows";
            case 22: return "iq2s_matmul_rows";
            case 23: return "iq4xs_matmul_rows";
            case 19: return "iq1s_matmul_rows";
            case 29: return "iq1m_matmul_rows";
            default: return nullptr;
        }
    };
    const char* q8_prefill_env = std::getenv("FLYWEIGHT_BAILING_Q8_PREFILL");
    const bool q8_prefill = !q8_prefill_env || q8_prefill_env[0] != '0';
    const char* mmq_prefill_env = std::getenv("FLYWEIGHT_BAILING_MMQ_PREFILL");
    const bool mmq_prefill = !mmq_prefill_env || mmq_prefill_env[0] != '0';
    const char* fused_mla_env = std::getenv("FLYWEIGHT_BAILING_FUSED_MLA");
    const bool fused_mla = fused_mla_env && fused_mla_env[0] == '1';
    // Head-fused quadratic phase: each latent row is read once for all heads
    // instead of once per head. Same switch as the decode-side fusion.
    const char* fused_heads_env = std::getenv("FLYWEIGHT_BAILING_MLA_FUSED");
    const bool fused_heads = !fused_heads_env || fused_heads_env[0] != '0';
    const char* grouped_moe_env = std::getenv("FLYWEIGHT_BAILING_GROUPED_PREFILL");
    const bool grouped_moe = !grouped_moe_env || grouped_moe_env[0] != '0';
    const char* q8_expert_env = std::getenv("FLYWEIGHT_BAILING_Q8_EXPERT_PREFILL");
    const bool q8_expert_prefill = !q8_expert_env || q8_expert_env[0] != '0';
    const char* mmq_expert_env = std::getenv("FLYWEIGHT_BAILING_MMQ_EXPERT_PREFILL");
    const bool mmq_expert_prefill =
        !mmq_expert_env || mmq_expert_env[0] != '0';
    std::uint64_t q8_cached_input = 0;
    int q8_cached_inputs = 0, q8_cached_rows = 0;
    auto matmul = [&](const BailingGpuMatrix& matrix, std::uint64_t input,
                      std::uint64_t output, int inputs, int outputs, int rows) {
        if (q8_prefill && matrix.type == 14 && !(inputs & 255)) {
            if (q8_cached_input != input || q8_cached_inputs != inputs ||
                q8_cached_rows != rows) {
                void* quant_args[] = {&input, &gpu.prefill_q8,
                    &gpu.prefill_q8_scales, &rows, &inputs};
                launch("bailing_quantize_q8_rows", inputs / 32, rows, 32,
                       quant_args);
                q8_cached_input = input;
                q8_cached_inputs = inputs;
                q8_cached_rows = rows;
            }
            void* q8_args[] = {const_cast<std::uint64_t*>(&matrix.data),
                &gpu.prefill_q8, &gpu.prefill_q8_scales, &output,
                &inputs, &outputs, &rows};
            if (mmq_prefill)
                launch("bailing_q6_q8_mmq_rows", (outputs + 31) / 32,
                       (rows + 3) / 4, 128, q8_args);
            else
                launch("bailing_q6_q8_matmul_rows", outputs, (rows + 3) / 4,
                       128, q8_args);
            if (output == q8_cached_input) q8_cached_input = 0;
            return;
        }
        const char* name = rows_kernel(matrix.type);
        if (!name) throw std::runtime_error(
            "bailing tiled prefill has no row kernel for type " +
            std::to_string(matrix.type));
        void* args[] = {const_cast<std::uint64_t*>(&matrix.data), &input, &output,
                        &inputs, &outputs, &rows};
        if (matrix.type == 8)
            launch(name, (outputs + 31) / 32, (rows + 31) / 32, 256, args);
        else if (matrix.type == 14)
            launch("bailing_q6_matmul_rows_16", outputs, (rows + 15) / 16,
                   256, args);
        else
            launch(name, outputs, (rows + 3) / 4, 256, args);
    };
    auto rms = [&](std::uint64_t input, std::uint64_t weights,
                   std::uint64_t output, int rows, int columns) {
        int one_centered = 0;
        void* args[] = {&input, &weights, &output, &rows, &columns,
                        const_cast<float*>(&epsilon), &one_centered};
        launch("rms_norm_rows", rows, 1, 256, args);
    };
    auto add = [&](std::uint64_t target, std::uint64_t source, int elements) {
        if (flyweight_gpu_scaled_add(target, source, 1.0f, elements) != 0)
            throw std::runtime_error("bailing tiled prefill residual add failed");
    };

    for (std::uint32_t offset = 0; offset < count; offset += gpu.prefill_rows) {
        const int rows = static_cast<int>(std::min<std::uint32_t>(
            gpu.prefill_rows, count - offset));
        const int base_position = static_cast<int>(slot.position);
        if (flyweight_gpu_upload_sync(gpu.prefill_tokens, tokens + offset,
                                    rows * sizeof(std::uint32_t)) != 0)
            throw std::runtime_error("bailing tiled prefill token upload failed");
        {
            int grid = (hidden + 255) / 256;
            void* args[] = {&gpu.embedding.data, &gpu.prefill_tokens,
                            &gpu.prefill_hidden_a, const_cast<int*>(&rows),
                            const_cast<int*>(&hidden)};
            launch(qwen_embedding_kernel(gpu.embedding.type, true), grid, rows,
                   256, args);
        }
        std::uint64_t current = gpu.prefill_hidden_a;
        std::uint64_t next = gpu.prefill_hidden_b;
        for (std::size_t layer_index = 0; layer_index < runtime.layers.size();
             ++layer_index) {
            const auto& weights = runtime.layers[layer_index];
            auto& layer = gpu.layers[layer_index];
            auto& cache = device_slot.layers[layer_index];
            const auto attention_start = std::chrono::steady_clock::now();
            rms(current, layer.attn_norm, gpu.prefill_normalized, rows, hidden);
            q8_cached_input = 0;
            if (weights.full_attention) {
                if (g.q_lora) {
                    matmul(layer.q_a, gpu.prefill_normalized, gpu.prefill_wide_a,
                           hidden, static_cast<int>(g.q_lora), rows);
                    rms(gpu.prefill_wide_a, layer.q_a_norm, gpu.prefill_wide_b,
                        rows, static_cast<int>(g.q_lora));
                    matmul(layer.q_b, gpu.prefill_wide_b, gpu.prefill_wide_c,
                           static_cast<int>(g.q_lora), heads * qk, rows);
                } else {
                    matmul(layer.q, gpu.prefill_normalized, gpu.prefill_wide_c,
                           hidden, heads * qk, rows);
                }
                matmul(layer.kv_a_mqa, gpu.prefill_normalized, gpu.prefill_wide_d,
                       hidden, lora + qk_rope, rows);
                matmul(layer.attn_gate, gpu.prefill_normalized, gpu.prefill_wide_a,
                       hidden, heads, rows);
                void* prepare_args[] = {&gpu.prefill_wide_d, &gpu.prefill_wide_c,
                    &layer.kv_a_norm, &cache.latents, &cache.rope_keys,
                    &gpu.prefill_wide_b, &gpu.prefill_normalized,
                    const_cast<int*>(&rows), const_cast<int*>(&base_position),
                    const_cast<int*>(&heads), const_cast<int*>(&qk_nope),
                    const_cast<int*>(&qk_rope), const_cast<int*>(&lora),
                    const_cast<float*>(&epsilon), const_cast<float*>(&g.rope_theta)};
                launch("bailing_mla_prepare_rows", rows, 1, 256, prepare_args);
                void* project_args[] = {&gpu.prefill_wide_b, &layer.kv_b,
                    &gpu.prefill_projected, const_cast<int*>(&rows),
                    const_cast<int*>(&heads), const_cast<int*>(&qk_nope),
                    const_cast<int*>(&value_dim), const_cast<int*>(&lora)};
                launch("bailing_mla_project_rows", rows * heads, 1, 128,
                       project_args);
                auto mla_mark = std::chrono::steady_clock::now();
                auto mla_lap = [&](double& bucket) {
                    if (!phase_profile) return;
                    flyweight_gpu_sync();
                    const auto now = std::chrono::steady_clock::now();
                    bucket += std::chrono::duration<double>(now - mla_mark).count();
                    mla_mark = now;
                };
                mla_lap(mla_proj_seconds);
                if (fused_mla) {
                    void* fused_args[] = {&gpu.prefill_projected,
                        &gpu.prefill_normalized, &cache.latents, &cache.rope_keys,
                        &gpu.prefill_accumulated, const_cast<int*>(&rows),
                        const_cast<int*>(&base_position), const_cast<int*>(&heads),
                        const_cast<int*>(&qk_nope), const_cast<int*>(&qk_rope),
                        const_cast<int*>(&lora)};
                    launch("bailing_mla_fused_rows", rows * heads, 1, 512,
                           fused_args);
                } else {
                    void* score_args[] = {&gpu.prefill_projected,
                        &gpu.prefill_normalized, &cache.latents, &cache.rope_keys,
                        &gpu.prefill_scores, const_cast<int*>(&rows),
                        const_cast<int*>(&base_position), &runtime.capacity,
                        const_cast<int*>(&heads), const_cast<int*>(&qk_nope),
                        const_cast<int*>(&qk_rope), const_cast<int*>(&lora)};
                    if (fused_heads)
                        launch("bailing_mla_scores_rows_gemm",
                               (rows * heads + 63) / 64,
                               (base_position + rows + 63) / 64, 256,
                               score_args);
                    else
                        launch("bailing_mla_scores_rows", rows * heads,
                               (base_position + rows + 7) / 8, 256, score_args);
                    mla_lap(mla_scores_seconds);
                    void* softmax_args[] = {&gpu.prefill_scores,
                        const_cast<int*>(&rows), const_cast<int*>(&base_position),
                        &runtime.capacity, const_cast<int*>(&heads)};
                    launch("bailing_mla_softmax_rows", rows * heads, 1, 128,
                           softmax_args);
                    mla_lap(mla_softmax_seconds);
                    void* accumulate_args[] = {&gpu.prefill_scores, &cache.latents,
                        &gpu.prefill_accumulated, const_cast<int*>(&rows),
                        const_cast<int*>(&base_position), &runtime.capacity,
                        const_cast<int*>(&heads), const_cast<int*>(&lora)};
                    if (fused_heads)
                        launch("bailing_mla_accumulate_rows_gemm",
                               (rows * heads + 63) / 64, (lora + 63) / 64,
                               256, accumulate_args);
                    else
                        launch("bailing_mla_accumulate_rows", rows * heads,
                               (lora + 127) / 128, 128, accumulate_args);
                }
                mla_lap(mla_accumulate_seconds);
                void* output_args[] = {&gpu.prefill_accumulated, &layer.kv_b,
                    &gpu.prefill_wide_a, &gpu.prefill_wide_c,
                    const_cast<int*>(&rows), const_cast<int*>(&heads),
                    const_cast<int*>(&qk_nope), const_cast<int*>(&value_dim),
                    const_cast<int*>(&lora)};
                launch("bailing_mla_output_rows", rows * heads, 1, 128,
                       output_args);
                matmul(layer.attn_output, gpu.prefill_wide_c, gpu.prefill_branch,
                       heads * value_dim, hidden, rows);
                mla_lap(mla_out_seconds);
            } else {
                matmul(layer.ssm_q, gpu.prefill_normalized, gpu.prefill_wide_a,
                       hidden, channels, rows);
                matmul(layer.ssm_k, gpu.prefill_normalized, gpu.prefill_wide_b,
                       hidden, channels, rows);
                matmul(layer.ssm_v, gpu.prefill_normalized, gpu.prefill_wide_c,
                       hidden, channels, rows);
                matmul(layer.ssm_f, gpu.prefill_normalized, gpu.prefill_wide_d,
                       hidden, channels, rows);
                matmul(layer.ssm_b, gpu.prefill_normalized, gpu.prefill_branch,
                       hidden, heads, rows);
                matmul(layer.ssm_g, gpu.prefill_normalized,
                       gpu.prefill_wide_e, hidden, channels, rows);
                int width = static_cast<int>(g.conv_width);
                std::uint64_t windows[] = {
                    cache.conv_windows,
                    cache.conv_windows + static_cast<std::uint64_t>(channels) * (width - 1) * 4,
                    cache.conv_windows + static_cast<std::uint64_t>(channels) * (width - 1) * 8};
                std::uint64_t values[] = {gpu.prefill_wide_a, gpu.prefill_wide_b,
                                          gpu.prefill_wide_c};
                std::uint64_t conv_weights[] = {layer.ssm_q_conv, layer.ssm_k_conv,
                                                layer.ssm_v_conv};
                for (int part = 0; part < 3; ++part) {
                    void* conv_args[] = {&values[part], &conv_weights[part],
                        &windows[part], const_cast<int*>(&rows),
                        const_cast<int*>(&channels), &width};
                    launch("bailing_short_conv_rows", 32, 1, 256, conv_args);
                }
                void* recurrent_args[] = {&gpu.prefill_wide_a,
                    &gpu.prefill_wide_b, &gpu.prefill_wide_c,
                    &gpu.prefill_wide_d, &gpu.prefill_branch, &layer.ssm_a,
                    &layer.ssm_dt, &cache.state, &gpu.prefill_wide_a,
                    const_cast<int*>(&rows), const_cast<int*>(&heads),
                    const_cast<int*>(&head_dim), const_cast<float*>(&epsilon)};
                launch("bailing_kda_recurrent_chunk", heads, 1, 128,
                       recurrent_args);
                void* norm_args[] = {&gpu.prefill_wide_a, &layer.ssm_norm,
                    &gpu.prefill_wide_e, const_cast<int*>(&rows),
                    const_cast<int*>(&heads), const_cast<int*>(&head_dim),
                    const_cast<float*>(&epsilon)};
                launch("bailing_gated_head_norm_rows", rows * heads, 1, 128,
                       norm_args);
                matmul(layer.ssm_out, gpu.prefill_wide_a, gpu.prefill_branch,
                       channels, hidden, rows);
            }
            add(gpu.prefill_branch, current, rows * hidden);
            if (phase_profile) {
                flyweight_gpu_sync();
                const double seconds = std::chrono::duration<double>(
                    std::chrono::steady_clock::now() - attention_start).count();
                (weights.full_attention ? mla_seconds : kda_seconds) += seconds;
            }
            const auto ffn_start = std::chrono::steady_clock::now();
            rms(gpu.prefill_branch, layer.ffn_norm, gpu.prefill_normalized,
                rows, hidden);
            q8_cached_input = 0;
            if (weights.routed) {
                auto moe_mark = std::chrono::steady_clock::now();
                matmul(layer.router, gpu.prefill_normalized,
                       gpu.prefill_router_logits, hidden, experts, rows);
                if (phase_profile) {
                    flyweight_gpu_sync();
                    const auto now = std::chrono::steady_clock::now();
                    router_projection_seconds +=
                        std::chrono::duration<double>(now - moe_mark).count();
                    moe_mark = now;
                }
                int normalize = g.normalize_weights ? 1 : 0;
                const int max_routes = rows * top_k;
                if (flyweight_gpu_memset(gpu.prefill_expert_counts, 0,
                        static_cast<std::size_t>(experts) * sizeof(std::int32_t),
                        stream) != 0)
                    throw std::runtime_error(
                        "bailing tiled prefill route-count reset failed");
                void* route_args[] = {&gpu.prefill_router_logits,
                    &layer.router_bias, &gpu.prefill_selected,
                    &gpu.prefill_weights, &gpu.prefill_expert_counts,
                    &gpu.prefill_expert_routes, const_cast<int*>(&max_routes),
                    const_cast<int*>(&rows),
                    const_cast<int*>(&experts), const_cast<int*>(&top_k),
                    const_cast<int*>(&groups), const_cast<int*>(&groups_used),
                    &normalize, const_cast<float*>(&g.weight_scale)};
                launch("bailing_route_rows", rows, 1, 256, route_args);
                if (phase_profile) {
                    flyweight_gpu_sync();
                    const auto now = std::chrono::steady_clock::now();
                    router_selection_seconds +=
                        std::chrono::duration<double>(now - moe_mark).count();
                    moe_mark = now;
                }
                const auto narrow = static_cast<std::uint64_t>(g.expert_size) *
                    bailing::row_bytes(layer.gate_exps.type, g.hidden);
                const auto wide = static_cast<std::uint64_t>(g.hidden) *
                    bailing::row_bytes(layer.down_exps.type, g.expert_size);
                if (grouped_moe) {
                    const bool q8_expert = q8_expert_prefill &&
                        layer.gate_exps.type == 14 &&
                        layer.up_exps.type == 14 && !(hidden & 255);
                    const bool mmq_expert = mmq_expert_prefill && q8_expert &&
                        layer.down_exps.type == 14;
                    if (q8_expert) {
                        void* quant_args[] = {&gpu.prefill_normalized,
                            &gpu.prefill_q8, &gpu.prefill_q8_scales,
                            const_cast<int*>(&rows), const_cast<int*>(&hidden)};
                        launch("bailing_quantize_q8_rows", hidden / 32, rows,
                               32, quant_args);
                        void* gate_args[] = {&layer.gate_exps.data,
                            &layer.up_exps.data, &gpu.prefill_expert_counts,
                            &gpu.prefill_expert_routes, &gpu.prefill_q8,
                            &gpu.prefill_q8_scales, &gpu.prefill_activated,
                            const_cast<std::uint64_t*>(&narrow),
                            const_cast<int*>(&max_routes),
                            const_cast<int*>(&top_k), const_cast<int*>(&hidden),
                            const_cast<int*>(&expert_size),
                            const_cast<float*>(&weights.swiglu_limit_exp)};
                        if (mmq_expert)
                            launch("bailing_q6_q8_expert_swiglu_mmq_rows",
                                   (expert_size + 31) / 32, experts, 128,
                                   gate_args);
                        else
                            launch("bailing_q6_q8_expert_swiglu_grouped_rows",
                                   expert_size, experts, 256, gate_args);
                    } else {
                        void* gate_args[] = {&layer.gate_exps.data,
                            &layer.up_exps.data, &gpu.prefill_expert_counts,
                            &gpu.prefill_expert_routes, &gpu.prefill_normalized,
                            &gpu.prefill_activated,
                            const_cast<std::uint64_t*>(&narrow),
                            const_cast<int*>(&max_routes),
                            const_cast<int*>(&top_k), const_cast<int*>(&hidden),
                            const_cast<int*>(&expert_size),
                            const_cast<float*>(&weights.swiglu_limit_exp)};
                        launch("bailing_q6_expert_swiglu_grouped_rows",
                               expert_size, experts, 256, gate_args);
                    }
                    if (phase_profile) {
                        flyweight_gpu_sync();
                        const auto now = std::chrono::steady_clock::now();
                        expert_gate_seconds +=
                            std::chrono::duration<double>(now - moe_mark).count();
                        moe_mark = now;
                    }
                    if (flyweight_gpu_memset(next, 0,
                            static_cast<std::size_t>(rows) * hidden * 4,
                            stream) != 0)
                        throw std::runtime_error(
                            "bailing tiled prefill expert-output reset failed");
                    void* down_args[] = {&layer.down_exps.data,
                        &gpu.prefill_expert_counts,
                        &gpu.prefill_expert_routes,
                        &gpu.prefill_activated, &gpu.prefill_weights, &next,
                        const_cast<std::uint64_t*>(&wide),
                        const_cast<int*>(&max_routes),
                        const_cast<int*>(&top_k),
                        const_cast<int*>(&expert_size),
                        const_cast<int*>(&hidden)};
                    if (mmq_expert)
                        launch("bailing_q6_f32_expert_accumulate_mmq_rows",
                               (hidden + 31) / 32, experts, 128, down_args);
                    else
                        launch("bailing_q6_expert_accumulate_grouped_rows",
                               hidden, experts, 256, down_args);
                    if (phase_profile) {
                        flyweight_gpu_sync();
                        const auto now = std::chrono::steady_clock::now();
                        expert_down_seconds +=
                            std::chrono::duration<double>(now - moe_mark).count();
                        moe_mark = now;
                    }
                } else {
                    void* gate_args[] = {&layer.gate_exps.data,
                        &layer.up_exps.data, &gpu.prefill_selected,
                        &gpu.prefill_normalized, &gpu.prefill_activated,
                        const_cast<std::uint64_t*>(&narrow),
                        const_cast<int*>(&rows), const_cast<int*>(&top_k),
                        const_cast<int*>(&hidden),
                        const_cast<int*>(&expert_size),
                        const_cast<float*>(&weights.swiglu_limit_exp)};
                    launch("bailing_q6_expert_swiglu_rows", expert_size,
                           rows * top_k, 256, gate_args);
                    void* down_args[] = {&layer.down_exps.data,
                        &gpu.prefill_selected, &gpu.prefill_activated,
                        &gpu.prefill_weights, &next,
                        const_cast<std::uint64_t*>(&wide),
                        const_cast<int*>(&rows), const_cast<int*>(&top_k),
                        const_cast<int*>(&expert_size),
                        const_cast<int*>(&hidden)};
                    launch("bailing_q6_expert_accumulate_rows", hidden, rows,
                           256, down_args);
                }
                if (layer.shared_gate.data && layer.shared_up.data &&
                    layer.shared_down.data) {
                    matmul(layer.shared_gate, gpu.prefill_normalized,
                           gpu.prefill_wide_a, hidden,
                           static_cast<int>(g.shared_size), rows);
                    matmul(layer.shared_up, gpu.prefill_normalized,
                           gpu.prefill_wide_b, hidden,
                           static_cast<int>(g.shared_size), rows);
                    int elements = rows * static_cast<int>(g.shared_size);
                    void* swiglu_args[] = {&gpu.prefill_wide_a,
                        &gpu.prefill_wide_b, &elements,
                        const_cast<float*>(&weights.swiglu_limit_shexp),
                        &gpu.prefill_wide_c};
                    launch("bailing_swiglu", (elements + 255) / 256, 1, 256,
                           swiglu_args);
                    matmul(layer.shared_down, gpu.prefill_wide_c,
                           gpu.prefill_wide_a, static_cast<int>(g.shared_size),
                           hidden, rows);
                    add(next, gpu.prefill_wide_a, rows * hidden);
                    if (phase_profile) {
                        flyweight_gpu_sync();
                        const auto now = std::chrono::steady_clock::now();
                        shared_expert_seconds +=
                            std::chrono::duration<double>(now - moe_mark).count();
                    }
                }
            } else {
                matmul(layer.gate_exps, gpu.prefill_normalized,
                       gpu.prefill_wide_a, hidden, dense_size, rows);
                matmul(layer.up_exps, gpu.prefill_normalized,
                       gpu.prefill_wide_b, hidden, dense_size, rows);
                int elements = rows * dense_size;
                void* swiglu_args[] = {&gpu.prefill_wide_a,
                    &gpu.prefill_wide_b, &elements,
                    const_cast<float*>(&weights.swiglu_limit_exp),
                    &gpu.prefill_wide_c};
                launch("bailing_swiglu", (elements + 255) / 256, 1, 256,
                       swiglu_args);
                matmul(layer.down_exps, gpu.prefill_wide_c, next,
                       dense_size, hidden, rows);
            }
            add(next, gpu.prefill_branch, rows * hidden);
            if (phase_profile) {
                flyweight_gpu_sync();
                const double seconds = std::chrono::duration<double>(
                    std::chrono::steady_clock::now() - ffn_start).count();
                (weights.routed ? routed_seconds : dense_seconds) += seconds;
            }
            std::swap(current, next);
        }
        slot.position += rows;
        if (offset + rows == count) {
            std::uint64_t last = current +
                static_cast<std::uint64_t>(rows - 1) * g.hidden * 4;
            if (flyweight_gpu_rms_norm(last, gpu.output_norm, gpu.normalized,
                                     hidden, epsilon, 0) != 0)
                throw std::runtime_error("bailing tiled prefill output norm failed");
            if (qwen_gpu_matvec_by_type(gpu.head.type, gpu.head.data,
                    gpu.normalized, gpu.logits, hidden,
                    static_cast<int>(c.vocabulary_size), stream) != 0)
                throw std::runtime_error("bailing tiled prefill LM head failed");
            flyweight_gpu_sync();
            if (flyweight_gpu_download(logits, gpu.logits,
                    static_cast<std::size_t>(c.vocabulary_size) * 4, stream) != 0)
                throw std::runtime_error("bailing tiled prefill logits download failed");
        }
        // A tile is the natural report and the natural cancellation point: the
        // device is idle here, the cache is coherent, and `position` already
        // counts the rows that landed. Cancelling leaves the sequence short but
        // not corrupt.
        if (offset + rows < count) bailing_progress(runtime, offset + rows, count);
    }
    if (phase_profile) std::fprintf(stderr,
        "[bailing-tiled-profile] mla=%.3fs kda=%.3fs routed=%.3fs dense=%.3fs "
        "router=%.3fs select=%.3fs gate=%.3fs down=%.3fs shared=%.3fs\n"
        "[bailing-tiled-profile] mla split: proj=%.3fs scores=%.3fs "
        "softmax=%.3fs accumulate=%.3fs out=%.3fs\n",
        mla_seconds, kda_seconds, routed_seconds, dense_seconds,
        router_projection_seconds, router_selection_seconds,
        expert_gate_seconds, expert_down_seconds, shared_expert_seconds,
        mla_proj_seconds, mla_scores_seconds, mla_softmax_seconds,
        mla_accumulate_seconds, mla_out_seconds);
    slot.cache_on_device = true;
}

int flyweight_v2_bailing_create_slots(const FlyweightV2Model* model, uint32_t capacity,
                                    uint32_t slots,
                                    FlyweightV2BailingRuntime** out) {
    return guarded([&]{
    if(!model||!out||!capacity||!slots)throw std::runtime_error("invalid bailing runtime arguments");
    if(model->architecture!="bailingmoe3")
        throw std::runtime_error("model architecture is not bailingmoe3");
    namespace bailing = flyweight::v2::bailing;
    const auto& c = model->config;
    auto runtime = std::make_unique<FlyweightV2BailingRuntime>();
    runtime->model = model;
    runtime->capacity = capacity;
    runtime->slots.resize(slots);

    auto& g = runtime->geometry;
    g.hidden = c.hidden_size;
    g.heads = c.attention_heads;
    g.qk_rope = c.rotary_dimension;
    g.qk_nope = c.key_length - c.rotary_dimension;
    g.v_head_dim = c.value_length;
    g.q_lora = c.q_lora_rank;
    g.kv_lora = c.kv_lora_rank;
    // The KDA head width is the declared head_dim, NOT hidden/heads. Those
    // differ here -- 128 against 96 -- and using the quotient silently makes
    // every KDA projection the wrong shape, which produces fluent nonsense
    // rather than a crash.
    g.head_dim = c.attention_head_dim
        ? c.attention_head_dim
        : c.hidden_size / (c.attention_heads ? c.attention_heads : 1);
    // The checkpoint's own width, not a constant: every published BailingMoE3
    // uses 4, which is why reading the config was never missed, and is also
    // why a checkpoint that differs would have been silently mis-shaped
    // rather than refused.
    g.conv_width = c.conv_kernel ? c.conv_kernel : 4;
    g.dense_size = c.dense_intermediate_size;
    g.expert_size = c.expert_intermediate_size;
    g.shared_size = c.expert_shared_intermediate_size;
    g.experts = c.expert_count;
    g.experts_used = c.expert_used_count;
    g.groups = c.expert_group_count;
    g.groups_used = c.expert_group_used;
    g.rope_theta = c.rope_freq_base;
    g.epsilon = c.rms_norm_epsilon;
    g.weight_scale = c.expert_weights_scale;
    g.normalize_weights = c.expert_weights_norm;
    // The SwiGLU clamps are deliberately NOT here: they are per layer and
    // differ between the routed and the shared half, so they are bound onto
    // LayerWeights below.

    runtime->embedding_matrix = bailing_matrix(*model, "token_embd.weight");
    runtime->output_norm = bailing_f32(*model, "output_norm.weight");
    runtime->head = bailing_matrix(*model, "output.weight");

    runtime->layers.resize(c.layer_count);
    for(auto& slot : runtime->slots) slot.caches.resize(c.layer_count);
    runtime->decoded_kv_b.resize(c.layer_count);
    for(std::uint32_t index=0;index<c.layer_count;++index){
        auto& w = runtime->layers[index];
        const std::string prefix = "blk." + std::to_string(index) + ".";
        w.full_attention = bailing::layer_is_full_attention(
            index, c.layer_count, c.full_attention_interval);
        w.routed = c.expert_count && index >= c.leading_dense_block_count;
        // Absent arrays leave both clamps off, which is what every checkpoint
        // before Ling 3.0 Flash wants.
        if(index<c.swiglu_clamp_exp.size())
            w.swiglu_limit_exp=c.swiglu_clamp_exp[index];
        if(index<c.swiglu_clamp_shexp.size())
            w.swiglu_limit_shexp=c.swiglu_clamp_shexp[index];
        w.attention_norm = bailing_f32(*model, prefix + "attn_norm.weight");
        w.ffn_norm = bailing_f32(*model, prefix + "ffn_norm.weight");
        if(w.full_attention){
            if(g.q_lora){
                w.mla.q_a = bailing_matrix(*model, prefix + "attn_q_a.weight");
                w.mla.q_a_norm = bailing_f32(*model, prefix + "attn_q_a_norm.weight");
                w.mla.q_b = bailing_matrix(*model, prefix + "attn_q_b.weight");
            } else {
                w.mla.q = bailing_matrix(*model, prefix + "attn_q.weight");
            }
            w.mla.kv_a_mqa = bailing_matrix(*model, prefix + "attn_kv_a_mqa.weight");
            w.mla.kv_a_norm = bailing_f32(*model, prefix + "attn_kv_a_norm.weight");
            {
                // Decoded once here rather than kept packed: the absorbed
                // attention walks along kv_b's rows instead of dotting them.
                const auto packed = bailing_matrix(*model, prefix + "attn_kv_b.weight");
                const std::size_t rows = g.heads * (g.qk_nope + g.v_head_dim);
                runtime->decoded_kv_b[index].resize(rows * g.kv_lora);
                flyweight::v2::bailing::matrix_decode(
                    packed, g.kv_lora, rows, runtime->decoded_kv_b[index].data());
                w.mla.kv_b = runtime->decoded_kv_b[index].data();
            }
            w.mla.gate = bailing_matrix(*model, prefix + "attn_gate.weight");
            w.mla.output = bailing_matrix(*model, prefix + "attn_output.weight");
        } else {
            w.kda.query = bailing_matrix(*model, prefix + "ssm_q.weight");
            w.kda.key = bailing_matrix(*model, prefix + "ssm_k.weight");
            w.kda.value = bailing_matrix(*model, prefix + "ssm_v.weight");
            w.kda.query_conv = bailing_f32(*model, prefix + "ssm_q_conv1d.weight");
            w.kda.key_conv = bailing_f32(*model, prefix + "ssm_k_conv1d.weight");
            w.kda.value_conv = bailing_f32(*model, prefix + "ssm_v_conv1d.weight");
            w.kda.decay = bailing_matrix(*model, prefix + "ssm_f.weight");
            w.kda.beta = bailing_matrix(*model, prefix + "ssm_b.weight");
            w.kda.gate = bailing_matrix(*model, prefix + "ssm_g.weight");
            w.kda.a_log = bailing_f32(*model, prefix + "ssm_a");
            w.kda.dt_bias = bailing_f32(*model, prefix + "ssm_dt.bias");
            w.kda.norm = bailing_f32(*model, prefix + "ssm_norm.weight");
            w.kda.output = bailing_matrix(*model, prefix + "ssm_out.weight");
        }
        if(w.routed){
            w.ffn.router = bailing_matrix(*model, prefix + "ffn_gate_inp.weight");
            w.ffn.router_bias = bailing_f32(*model, prefix + "exp_probs_b.bias", false);
            w.ffn.gate_experts = bailing_matrix(*model, prefix + "ffn_gate_exps.weight");
            w.ffn.up_experts = bailing_matrix(*model, prefix + "ffn_up_exps.weight");
            w.ffn.down_experts = bailing_matrix(*model, prefix + "ffn_down_exps.weight");
            w.ffn.shared_gate = bailing_matrix(*model, prefix + "ffn_gate_shexp.weight", false);
            w.ffn.shared_up = bailing_matrix(*model, prefix + "ffn_up_shexp.weight", false);
            w.ffn.shared_down = bailing_matrix(*model, prefix + "ffn_down_shexp.weight", false);
        } else {
            w.ffn.gate = bailing_matrix(*model, prefix + "ffn_gate.weight");
            w.ffn.up = bailing_matrix(*model, prefix + "ffn_up.weight");
            w.ffn.down = bailing_matrix(*model, prefix + "ffn_down.weight");
        }
        for(auto& slot : runtime->slots)
            bailing::reset_cache(slot.caches[index], w, g, capacity);
    }
    // The nextn draft block, one layer past the executed stack
    // (detect_mtp_layer trimmed layer_count down to exclude it). A full
    // MLA+MoE layer of its own plus the merge and head-norm extras. Loading
    // it costs nothing until a caller drives an MTP round. Gated on eh_proj:
    // some conversions carry only the nextn norms as a stub, and a stub must
    // load exactly as before -- present, inert, and not a reason to fail.
    if(model->mtp_layer != std::numeric_limits<uint32_t>::max() &&
       bailing_matrix(*model,
           "blk." + std::to_string(model->mtp_layer) + ".nextn.eh_proj.weight",
           false).data){
        auto& d = runtime->draft;
        auto& w = d.layer;
        const std::string prefix = "blk." + std::to_string(model->mtp_layer) + ".";
        w.full_attention = true;
        w.routed = c.expert_count != 0;
        if(model->mtp_layer < c.swiglu_clamp_exp.size())
            w.swiglu_limit_exp = c.swiglu_clamp_exp[model->mtp_layer];
        if(model->mtp_layer < c.swiglu_clamp_shexp.size())
            w.swiglu_limit_shexp = c.swiglu_clamp_shexp[model->mtp_layer];
        w.attention_norm = bailing_f32(*model, prefix + "attn_norm.weight");
        w.ffn_norm = bailing_f32(*model, prefix + "ffn_norm.weight");
        if(g.q_lora){
            w.mla.q_a = bailing_matrix(*model, prefix + "attn_q_a.weight");
            w.mla.q_a_norm = bailing_f32(*model, prefix + "attn_q_a_norm.weight");
            w.mla.q_b = bailing_matrix(*model, prefix + "attn_q_b.weight");
        } else {
            w.mla.q = bailing_matrix(*model, prefix + "attn_q.weight");
        }
        w.mla.kv_a_mqa = bailing_matrix(*model, prefix + "attn_kv_a_mqa.weight");
        w.mla.kv_a_norm = bailing_f32(*model, prefix + "attn_kv_a_norm.weight");
        {
            const auto packed = bailing_matrix(*model, prefix + "attn_kv_b.weight");
            const std::size_t rows = g.heads * (g.qk_nope + g.v_head_dim);
            d.decoded_kv_b.resize(rows * g.kv_lora);
            flyweight::v2::bailing::matrix_decode(
                packed, g.kv_lora, rows, d.decoded_kv_b.data());
            w.mla.kv_b = d.decoded_kv_b.data();
        }
        w.mla.gate = bailing_matrix(*model, prefix + "attn_gate.weight");
        w.mla.output = bailing_matrix(*model, prefix + "attn_output.weight");
        if(w.routed){
            w.ffn.router = bailing_matrix(*model, prefix + "ffn_gate_inp.weight");
            w.ffn.router_bias = bailing_f32(*model, prefix + "exp_probs_b.bias", false);
            w.ffn.gate_experts = bailing_matrix(*model, prefix + "ffn_gate_exps.weight");
            w.ffn.up_experts = bailing_matrix(*model, prefix + "ffn_up_exps.weight");
            w.ffn.down_experts = bailing_matrix(*model, prefix + "ffn_down_exps.weight");
            w.ffn.shared_gate = bailing_matrix(*model, prefix + "ffn_gate_shexp.weight", false);
            w.ffn.shared_up = bailing_matrix(*model, prefix + "ffn_up_shexp.weight", false);
            w.ffn.shared_down = bailing_matrix(*model, prefix + "ffn_down_shexp.weight", false);
        } else {
            w.ffn.gate = bailing_matrix(*model, prefix + "ffn_gate.weight");
            w.ffn.up = bailing_matrix(*model, prefix + "ffn_up.weight");
            w.ffn.down = bailing_matrix(*model, prefix + "ffn_down.weight");
        }
        d.enorm = bailing_f32(*model, prefix + "nextn.enorm.weight");
        d.hnorm = bailing_f32(*model, prefix + "nextn.hnorm.weight");
        d.eh_proj = bailing_matrix(*model, prefix + "nextn.eh_proj.weight");
        // Some converters carry a dedicated draft-head norm; Ling shares the
        // per-layer output norm instead, and either may be absent -- fall
        // back to the trunk's final norm, matching the qwen draft's rule.
        d.head_norm = bailing_f32(*model, prefix + "nextn.shared_head_norm.weight", false);
        if(!d.head_norm)
            d.head_norm = bailing_f32(*model, prefix + "layer_output_norm.weight", false);
        if(!d.head_norm) d.head_norm = runtime->output_norm;
        for(auto& slot : runtime->slots)
            bailing::reset_cache(slot.draft_cache, w, g, capacity);
        d.available = true;
    }
    // On by default wherever there is a device to use.
    //
    // It was opt-in while the device had to take prefill as well as decode,
    // which cost about half the prefill rate -- so on a long prompt with a
    // short answer the GPU lost overall, and defaulting to it would have been a
    // regression. Now the host keeps its batched prefill and the device only
    // takes decode (see flyweight_v2_bailing_eval), so the device path is no
    // longer worse at anything: measured 155 vs 158 tok/s prefill, and decode
    // from 44 to 172 tok/s at Q6_K. There is nothing left for the caller to
    // trade off, so there is nothing left to ask them.
    //
    // `FLYWEIGHT_BAILING_GPU=0` still forces the host path, which is what makes
    // the two comparable -- and the host path remains the correctness oracle.
    const char* want_gpu = std::getenv("FLYWEIGHT_BAILING_GPU");
    const bool forced_off = want_gpu && want_gpu[0]=='0';
    const bool forced_on = want_gpu && want_gpu[0]=='1';
    if(!forced_off && (forced_on || flyweight_v2_gpu_available()==1)){
        // Preparation compiles the kernel corpus and uploads every weight, and
        // it can fail for reasons that are not the caller's problem -- no
        // driver, no nvrtc, not enough VRAM for this model. Falling back to the
        // host is always correct and merely slower, so an unasked-for GPU is
        // never allowed to turn into a failed runtime. Asking for it explicitly
        // still reports the failure.
        try {
            runtime->gpu = std::make_unique<BailingGpu>();
            bailing_gpu_prepare(*runtime);
        } catch(const std::exception& failure) {
            // Hand the device back whatever preparation had already taken.
            // Failing part way is now a routine outcome -- asking for more
            // sequence slots than the card has room for is refused rather than
            // discovered -- and a half-uploaded model left behind would make
            // the fallback host runtime share the card with several gigabytes
            // of nothing.
            if(runtime->gpu){
                for(const auto pointer:runtime->gpu->owned)flyweight_gpu_free(pointer);
                runtime->gpu->owned.clear();
            }
            if(forced_on) throw;
            runtime->gpu.reset();
            std::fprintf(stderr,
                "[flyweight] bailingmoe3: staying on the host path (%s)\n",
                failure.what());
        }
    }
    *out = runtime.release();
    return 0; });
}

// The single-sequence spelling, which is what every existing caller wants and
// is exactly one slot.
int flyweight_v2_bailing_create(const FlyweightV2Model* model, uint32_t capacity,
                              FlyweightV2BailingRuntime** out) {
    return flyweight_v2_bailing_create_slots(model, capacity, 1, out);
}

int flyweight_v2_bailing_slot_count(FlyweightV2BailingRuntime* runtime, uint32_t* out){
    return guarded([&]{
    if(!runtime||!out)throw std::runtime_error("invalid bailing slot count arguments");
    *out = static_cast<std::uint32_t>(runtime->slots.size());
    return 0; });
}

void flyweight_v2_bailing_destroy(FlyweightV2BailingRuntime* runtime){
    try{
        if(flyweight::v2::bailing::profiling())
            flyweight::v2::bailing::profile().report(stderr);
        if(runtime&&runtime->gpu){
            for(const auto pointer:runtime->gpu->owned)flyweight_gpu_free(pointer);
            runtime->gpu->owned.clear();
        }
        delete runtime;
    }catch(...){}
}

int flyweight_v2_bailing_reset_slot(FlyweightV2BailingRuntime* runtime, uint32_t slot_index){
    return guarded([&]{
    if(!runtime)throw std::runtime_error("invalid bailing runtime");
    auto& slot = runtime->slot(slot_index);
    for(std::size_t index=0;index<runtime->layers.size();++index)
        flyweight::v2::bailing::reset_cache(slot.caches[index],
            runtime->layers[index], runtime->geometry, runtime->capacity);
    if(runtime->draft.available){
        flyweight::v2::bailing::reset_cache(slot.draft_cache,
            runtime->draft.layer, runtime->geometry, runtime->capacity);
        slot.has_target_hidden = false;
    }
    // The DEVICE caches need clearing too. Missing this leaked a KDA layer's
    // recurrent state from one request into the next -- and because that state
    // is a running summary of everything seen, the contamination compounds
    // rather than washing out. It showed up as identical greedy requests
    // returning different answers, and eventually as degenerate repetition.
    //
    // Only the KDA state and convolution windows actually need it: the MLA
    // latents and rope keys are written before they are read at each position,
    // so stale contents past `position` are never consulted. They are cleared
    // anyway because "reset" that leaves some state behind is the kind of
    // distinction nobody remembers a year later.
    //
    // This slot's device buffers only. Zeroing every slot's would make a reset
    // of one sequence silently destroy the others, which is the same class of
    // bug the paragraph above describes and the opposite direction.
    if(runtime->gpu && runtime->gpu->ready){
        const auto& g = runtime->geometry;
        const std::size_t channels = g.heads * g.head_dim;
        for(auto& layer : runtime->gpu->slots[slot_index].layers){
            if(layer.state)
                flyweight_gpu_memset(layer.state, 0,
                    channels * g.head_dim * 4, 0);
            if(layer.conv_windows)
                flyweight_gpu_memset(layer.conv_windows, 0,
                    3 * channels * (g.conv_width - 1) * 4, 0);
            if(layer.latents)
                flyweight_gpu_memset(layer.latents, 0,
                    static_cast<std::size_t>(runtime->capacity) * g.kv_lora * 4, 0);
            if(layer.rope_keys)
                flyweight_gpu_memset(layer.rope_keys, 0,
                    static_cast<std::size_t>(runtime->capacity) * g.qk_rope * 4, 0);
        }
        flyweight_gpu_sync();
    }
    // Both copies are now zero, so neither is stale and the host one is as good
    // a starting point as the other. Saying so avoids a pointless transfer on
    // the first eval after a reset.
    slot.cache_on_device = false;
    slot.position = 0;
    return 0; });
}

int flyweight_v2_bailing_reset(FlyweightV2BailingRuntime* runtime){
    return flyweight_v2_bailing_reset_slot(runtime, 0);
}

int flyweight_v2_bailing_eval_slot(FlyweightV2BailingRuntime* runtime, uint32_t slot_index,
                                 const uint32_t* tokens, uint32_t count,
                                 float* logits){
    return guarded([&]{
    if(!runtime||!tokens||!count||!logits)
        throw std::runtime_error("invalid bailing eval arguments");
    const auto& g = runtime->geometry;
    const auto& c = runtime->model->config;
    // Named once, up front. Every path below reads and writes THIS slot's
    // position and caches and nothing else, which is the whole property that
    // makes interleaving two sequences give the same answers as running each
    // of them alone.
    auto& slot = runtime->slot(slot_index);
    if(slot.position + count > runtime->capacity)
        throw std::runtime_error("bailing sequence exceeds the runtime capacity");

    // A prompt is watchable; a decoded token is not. Reporting count == 1 would
    // put two indirect calls and a lock acquisition on the server side into
    // every token of every generation, to say "0 of 1" and then "1 of 1".
    const bool watched = runtime->progress && count > 1;
    if(watched){
        runtime->reporting = false;
        bailing_progress(*runtime, 0, count);
    }

    // Each path takes the work it is better at, and the cache follows.
    //
    // The host has weight-stationary batched projections and wins on ordinary
    // prompts. Its causal MLA is quadratic and becomes the bottleneck on long
    // fresh prompts, however; after the tiled device MLA landed, the fully
    // resident token loop wins beyond a conservative 4096-token crossover
    // (measured at 8871: 96.5 vs 56.7 tok/s on Q6_K). Use that automatically.
    // FLYWEIGHT_BAILING_GPU_PREFILL=0/1 forces either side for reproducible A/B.
    //
    // The hazard this replaces: the host and device caches are SEPARATE, and an
    // earlier version routed prefill to the host and decode to the device with
    // no transfer, so decode read a device cache the prompt never touched. The
    // model answered with no context at all -- first token plausible,
    // everything after it invented. It hid through testing because feeding a
    // prompt one token at a time (as every test here does) keeps both on the
    // same path; only a server sending a whole prompt at once splits them.
    // Hence `cache_on_device`: the transfer is not optional and not inferred
    // from the call, it is tracked.
    const char* tiled_prefill_env = std::getenv("FLYWEIGHT_BAILING_TILED_PREFILL");
    const bool tiled_prefill = !tiled_prefill_env ||
        tiled_prefill_env[0] != '0';
    if (count > 1 && tiled_prefill &&
        bailing_gpu_prefill_tiled_supported(*runtime)) {
        if (!slot.cache_on_device) bailing_cache_transfer(*runtime, slot_index, true);
        bailing_gpu_prefill_tiled(*runtime, slot_index, tokens, count, logits);
        if(watched) bailing_progress(*runtime, count, count);
        return 0;
    }
    const char* gpu_prefill_env = std::getenv("FLYWEIGHT_BAILING_GPU_PREFILL");
    const bool gpu_prefill = gpu_prefill_env
        ? gpu_prefill_env[0] == '1'
        : count >= 4096;
    if(runtime->gpu && runtime->gpu->ready && count > 1 && !gpu_prefill){
        if(slot.cache_on_device) bailing_cache_transfer(*runtime,slot_index,false);
        // Falls through to the host batched path below, which leaves the cache
        // on the host; the next single-token eval moves it back.
    } else if(runtime->gpu && runtime->gpu->ready){
        if(!slot.cache_on_device) bailing_cache_transfer(*runtime,slot_index,true);
        for(std::uint32_t index=0;index<count;++index){
            if(tokens[index] >= c.vocabulary_size)
                throw std::runtime_error("token id is outside the vocabulary");
            if(slot.position >= runtime->capacity)
                throw std::runtime_error("bailing sequence exceeds the runtime capacity");
            bailing_gpu_step(*runtime, slot_index, tokens[index], logits);
            // Every 128 tokens, matching the tile the other prefill paths use,
            // so a watcher sees the same rhythm whichever one ran.
            if(watched && (index & 127) == 127)
                bailing_progress(*runtime, index + 1, count);
        }
        if(watched) bailing_progress(*runtime, count, count);
        return 0;
    }
    // Prefill runs the whole batch through each layer at once so the weights
    // are read once rather than once per token; decode (count == 1) takes the
    // same path and degrades to the single-token one inside.
    std::vector<float> current(static_cast<std::size_t>(count) * g.hidden);
    std::vector<float> next(static_cast<std::size_t>(count) * g.hidden);
    if(flyweight::v2::bailing::profiling())
        flyweight::v2::bailing::profile().positions += count;
    {
    flyweight::v2::bailing::ProfileScope gather(
        flyweight::v2::bailing::profile().embedding);
    for(std::uint32_t index=0;index<count;++index){
        const std::uint32_t token = tokens[index];
        if(token >= c.vocabulary_size)
            throw std::runtime_error("token id is outside the vocabulary");
        const auto stride = flyweight::v2::bailing::row_bytes(
            runtime->embedding_matrix.type, g.hidden);
        const auto* base = reinterpret_cast<const std::uint8_t*>(
            runtime->embedding_matrix.data);
        flyweight::v2::bailing::row_decode(base + static_cast<std::size_t>(token)*stride,
            runtime->embedding_matrix.type, g.hidden,
            current.data() + static_cast<std::size_t>(index) * g.hidden);
    }
    }
    const std::size_t depth = runtime->layers.size();
    for(std::size_t layer=0;layer<depth;++layer){
        flyweight::v2::bailing::decoder_layer_batch(
            current.data(), count, runtime->layers[layer], g, slot.position,
            slot.caches[layer], next.data());
        current.swap(next);
        // The whole batch crosses every layer before any one token is finished,
        // so there is no honest per-token count to report part way through:
        // scale the depth reached over the batch instead. A layer of a batch is
        // a prompt answer's worth of work at most, which is prompt enough to
        // drop a request whose client has gone. Cancelling here leaves the
        // layers already run holding rows past `position`, which reset clears.
        if(watched && layer + 1 < depth)
            bailing_progress(*runtime,
                static_cast<std::uint32_t>(
                    static_cast<std::size_t>(count) * (layer + 1) / depth),
                count);
    }
    slot.position += count;
    // The draft block conditions on the target's pre-norm hidden for the last
    // forwarded token, so the host path preserves it. The device paths do not
    // produce a host-side hidden; a caller who prefilled there has no draft
    // conditioning until an MTP round or a host eval provides one.
    if(runtime->draft.available){
        const float* last_row = current.data()
            + static_cast<std::size_t>(count - 1) * g.hidden;
        slot.target_hidden.assign(last_row, last_row + g.hidden);
        slot.has_target_hidden = true;
    }
    // Only the last token's logits are produced; the earlier ones exist purely
    // to advance the caches.
    std::vector<float> normalized(g.hidden);
    {
    flyweight::v2::bailing::ProfileScope stage(
        flyweight::v2::bailing::profile().head);
    const float* last = current.data() + static_cast<std::size_t>(count - 1) * g.hidden;
    flyweight::v2::bailing::rms_norm(last, runtime->output_norm, g.hidden,
                                   g.epsilon, normalized.data());
    flyweight::v2::bailing::matvec(runtime->head, normalized.data(), g.hidden,
                                 c.vocabulary_size, logits);
    }
    if(watched) bailing_progress(*runtime, count, count);
    return 0; });
}

int flyweight_v2_bailing_eval(FlyweightV2BailingRuntime* runtime, const uint32_t* tokens,
                            uint32_t count, float* logits){
    return flyweight_v2_bailing_eval_slot(runtime, 0, tokens, count, logits);
}

// --- bailingmoe3 MTP (host path) -------------------------------------------
//
// Same shape as the qwen runtime's speculative decode, built fold-first: the
// draft block proposes, one batched verify decides, and a rejection rebuilds
// only the KDA state from retained transition inputs instead of re-running
// the forward. Verified tokens are target outputs, so a draft-side defect can
// only lower the acceptance rate, never change the text.

static std::uint32_t bailing_argmax(const float* values, std::size_t count) {
    std::size_t winner = 0;
    for (std::size_t index = 1; index < count; ++index)
        if (values[index] > values[winner]) winner = index;
    return static_cast<std::uint32_t>(winner);
}

// The draft block's forward for one token: embed, merge with the target's
// preceding hidden through eh_proj, run the draft decoder layer (advancing the
// draft cache), and leave the pre-norm hidden in `hidden_out`. The merge order
// -- normalized embedding first, normalized hidden second -- matches the qwen
// draft and the converter both nextn layouts came from.
static void bailing_mtp_draft_layer(
    FlyweightV2BailingRuntime& runtime, BailingSlot& slot, std::uint32_t token,
    const float* previous_hidden, float* hidden_out
) {
    namespace bailing = flyweight::v2::bailing;
    const auto& g = runtime.geometry;
    const auto& c = runtime.model->config;
    if (token >= c.vocabulary_size)
        throw std::runtime_error("token id is outside the vocabulary");
    std::vector<float> embedding(g.hidden), merged(2 * g.hidden), input(g.hidden);
    const auto stride = bailing::row_bytes(runtime.embedding_matrix.type, g.hidden);
    const auto* base = reinterpret_cast<const std::uint8_t*>(
        runtime.embedding_matrix.data);
    bailing::row_decode(base + static_cast<std::size_t>(token) * stride,
                        runtime.embedding_matrix.type, g.hidden, embedding.data());
    bailing::rms_norm(embedding.data(), runtime.draft.enorm, g.hidden, g.epsilon,
                      merged.data());
    bailing::rms_norm(previous_hidden, runtime.draft.hnorm, g.hidden, g.epsilon,
                      merged.data() + g.hidden);
    bailing::matvec(runtime.draft.eh_proj, merged.data(), 2 * g.hidden, g.hidden,
                    input.data());
    if (slot.draft_cache.positions >= runtime.capacity)
        throw std::runtime_error("bailing draft cache exceeds the runtime capacity");
    bailing::decoder_layer(input.data(), runtime.draft.layer, runtime.geometry,
                           slot.draft_cache.positions, slot.draft_cache,
                           hidden_out);
}

static std::uint32_t bailing_mtp_draft(
    FlyweightV2BailingRuntime& runtime, BailingSlot& slot, std::uint32_t token,
    const float* previous_hidden, float* hidden_out
) {
    namespace bailing = flyweight::v2::bailing;
    const auto& g = runtime.geometry;
    const auto& c = runtime.model->config;
    bailing_mtp_draft_layer(runtime, slot, token, previous_hidden, hidden_out);
    std::vector<float> normalized(g.hidden), logits(c.vocabulary_size);
    bailing::rms_norm(hidden_out, runtime.draft.head_norm, g.hidden, g.epsilon,
                      normalized.data());
    bailing::matvec(runtime.head, normalized.data(), g.hidden, c.vocabulary_size,
                    logits.data());
    return bailing_argmax(logits.data(), c.vocabulary_size);
}

// The batched verify: all rows through every layer on the host path, with each
// KDA layer's transition inputs retained in the slot's fold scratch, then a
// head and argmax per row. Advances slot.position and every cache by `count`;
// the caller rolls back on rejection. `hiddens` receives count x hidden
// pre-norm rows.
static void bailing_mtp_verify_rows(
    FlyweightV2BailingRuntime& runtime, BailingSlot& slot,
    const std::uint32_t* tokens, std::uint32_t count,
    std::uint32_t* winners, float* hiddens
) {
    namespace bailing = flyweight::v2::bailing;
    const auto& g = runtime.geometry;
    const auto& c = runtime.model->config;
    if (slot.position + count > runtime.capacity)
        throw std::runtime_error("bailing sequence exceeds the runtime capacity");
    const std::size_t channels = g.heads * g.head_dim;
    const std::size_t depth = runtime.layers.size();
    if (slot.mtp_q.size() != depth) {
        slot.mtp_q.resize(depth); slot.mtp_k.resize(depth);
        slot.mtp_v.resize(depth); slot.mtp_decay.resize(depth);
        slot.mtp_beta.resize(depth);
    }
    std::vector<float> current(static_cast<std::size_t>(count) * g.hidden);
    std::vector<float> next(static_cast<std::size_t>(count) * g.hidden);
    const auto stride = bailing::row_bytes(runtime.embedding_matrix.type, g.hidden);
    const auto* base = reinterpret_cast<const std::uint8_t*>(
        runtime.embedding_matrix.data);
    for (std::uint32_t index = 0; index < count; ++index) {
        if (tokens[index] >= c.vocabulary_size)
            throw std::runtime_error("token id is outside the vocabulary");
        bailing::row_decode(base + static_cast<std::size_t>(tokens[index]) * stride,
                            runtime.embedding_matrix.type, g.hidden,
                            current.data() + static_cast<std::size_t>(index) * g.hidden);
    }
    for (std::size_t layer = 0; layer < depth; ++layer) {
        bailing::KdaCapture capture;
        bailing::KdaCapture* wanted_capture = nullptr;
        if (!runtime.layers[layer].full_attention) {
            slot.mtp_q[layer].resize(static_cast<std::size_t>(count) * channels);
            slot.mtp_k[layer].resize(static_cast<std::size_t>(count) * channels);
            slot.mtp_v[layer].resize(static_cast<std::size_t>(count) * channels);
            slot.mtp_decay[layer].resize(static_cast<std::size_t>(count) * channels);
            slot.mtp_beta[layer].resize(static_cast<std::size_t>(count) * g.heads);
            capture.q = slot.mtp_q[layer].data();
            capture.k = slot.mtp_k[layer].data();
            capture.v = slot.mtp_v[layer].data();
            capture.decay = slot.mtp_decay[layer].data();
            capture.beta = slot.mtp_beta[layer].data();
            wanted_capture = &capture;
        }
        bailing::decoder_layer_batch(
            current.data(), count, runtime.layers[layer], g, slot.position,
            slot.caches[layer], next.data(), wanted_capture);
        current.swap(next);
    }
    slot.position += count;
    std::memcpy(hiddens, current.data(),
                static_cast<std::size_t>(count) * g.hidden * sizeof(float));
    std::vector<float> normalized(g.hidden), logits(c.vocabulary_size);
    for (std::uint32_t row = 0; row < count; ++row) {
        bailing::rms_norm(current.data() + static_cast<std::size_t>(row) * g.hidden,
                          runtime.output_norm, g.hidden, g.epsilon,
                          normalized.data());
        bailing::matvec(runtime.head, normalized.data(), g.hidden,
                        c.vocabulary_size, logits.data());
        winners[row] = bailing_argmax(logits.data(), c.vocabulary_size);
    }
}

// Snapshot every KDA layer's recurrent state and convolution windows into the
// slot's persistent fold scratch (restore=false), or copy them back
// (restore=true). MLA caches need no copy: their rows are position-indexed,
// so rollback is truncating `positions`.
static void bailing_mtp_snapshot(
    FlyweightV2BailingRuntime& runtime, BailingSlot& slot, bool restore
) {
    const std::size_t depth = runtime.layers.size();
    if (slot.mtp_state.size() != depth) {
        slot.mtp_state.resize(depth); slot.mtp_qwin.resize(depth);
        slot.mtp_kwin.resize(depth); slot.mtp_vwin.resize(depth);
    }
    for (std::size_t layer = 0; layer < depth; ++layer) {
        if (runtime.layers[layer].full_attention) continue;
        auto& cache = slot.caches[layer];
        auto copy = [&](std::vector<float>& saved, std::vector<float>& live) {
            if (restore) std::copy(saved.begin(), saved.end(), live.begin());
            else saved = live;
        };
        copy(slot.mtp_state[layer], cache.state);
        copy(slot.mtp_qwin[layer], cache.query_window);
        copy(slot.mtp_kwin[layer], cache.key_window);
        copy(slot.mtp_vwin[layer], cache.value_window);
    }
}

// The fold: from the restored snapshot, replay only the convolution and the
// recurrence over the `rows` accepted tokens, reading the transition inputs
// the verify retained. Mirrors decoder_layer_batch's KDA block exactly --
// same conv steps, one kda_recurrence call -- so the rebuilt state is the
// batch's own state after `rows` tokens, bit for bit. The gate and output
// are not replayed: they never touch state.
static void bailing_mtp_fold(
    FlyweightV2BailingRuntime& runtime, BailingSlot& slot, std::uint32_t rows
) {
    namespace bailing = flyweight::v2::bailing;
    const auto& g = runtime.geometry;
    const std::size_t channels = g.heads * g.head_dim;
    std::vector<float> qc(rows * channels), kc(rows * channels),
        vc(rows * channels), attended(rows * channels);
    for (std::size_t layer = 0; layer < runtime.layers.size(); ++layer) {
        const auto& w = runtime.layers[layer];
        if (w.full_attention) continue;
        auto& cache = slot.caches[layer];
        for (std::uint32_t t = 0; t < rows; ++t) {
            bailing::short_conv_step(
                slot.mtp_q[layer].data() + static_cast<std::size_t>(t) * channels,
                w.kda.query_conv, channels, g.conv_width,
                cache.query_window.data(), qc.data() + static_cast<std::size_t>(t) * channels);
            bailing::short_conv_step(
                slot.mtp_k[layer].data() + static_cast<std::size_t>(t) * channels,
                w.kda.key_conv, channels, g.conv_width,
                cache.key_window.data(), kc.data() + static_cast<std::size_t>(t) * channels);
            bailing::short_conv_step(
                slot.mtp_v[layer].data() + static_cast<std::size_t>(t) * channels,
                w.kda.value_conv, channels, g.conv_width,
                cache.value_window.data(), vc.data() + static_cast<std::size_t>(t) * channels);
        }
        bailing::kda_recurrence(qc.data(), kc.data(), vc.data(),
                                slot.mtp_decay[layer].data(),
                                slot.mtp_beta[layer].data(),
                                w.kda.a_log, w.kda.dt_bias, rows, g.heads,
                                g.head_dim, g.epsilon, cache.state.data(),
                                attended.data());
    }
}

int flyweight_v2_bailing_mtp_available(FlyweightV2BailingRuntime* runtime, int* out){
    return guarded([&]{
    if(!runtime||!out)throw std::runtime_error("invalid bailing MTP arguments");
    *out = runtime->draft.available ? 1 : 0;
    return 0; });
}

int flyweight_v2_bailing_mtp_stats(FlyweightV2BailingRuntime* runtime,
                                 uint64_t* drafted, uint64_t* accepted,
                                 uint64_t* rejected){
    return guarded([&]{
    if(!runtime||!drafted||!accepted||!rejected)
        throw std::runtime_error("invalid bailing MTP arguments");
    *drafted = runtime->mtp_drafted;
    *accepted = runtime->mtp_accepted;
    *rejected = runtime->mtp_rejected;
    return 0; });
}

// One speculative round: draft `wanted` tokens from `next_token` (which has
// not been forwarded yet), verify them in one batched target pass, commit the
// agreed prefix and emit the target's tokens for it. Always emits at least
// one token. The caller feeds the LAST emitted token back as the next round's
// `next_token`. Host path only: the slot's cache is pulled back from the
// device if a GPU decode left it there.
int flyweight_v2_bailing_mtp_round(FlyweightV2BailingRuntime* runtime,
                                 uint32_t slot_index, uint32_t next_token,
                                 uint32_t wanted, uint32_t* out_tokens,
                                 uint32_t* out_count){
    return guarded([&]{
    if(!runtime||!out_tokens||!out_count)
        throw std::runtime_error("invalid bailing MTP round arguments");
    if(!runtime->draft.available)
        throw std::runtime_error("this checkpoint has no nextn draft block");
    auto& slot = runtime->slot(slot_index);
    if(!slot.has_target_hidden)
        throw std::runtime_error(
            "bailing MTP needs a host-path eval first to seed the draft's "
            "target hidden");
    if(slot.cache_on_device) bailing_cache_transfer(*runtime, slot_index, false);
    if(wanted > 8) wanted = 8;
    if(!wanted) wanted = 1;
    const auto& g = runtime->geometry;
    // The draft cache is context-sized and never rewound; recycle it when the
    // round would overflow. The cost is a few poor drafts, not a wrong answer.
    if(slot.draft_cache.positions + wanted + 1 > runtime->capacity)
        flyweight::v2::bailing::reset_cache(slot.draft_cache, runtime->draft.layer,
                                          g, runtime->capacity);
    const auto draft_base = slot.draft_cache.positions;
    std::array<std::uint32_t, 8> drafts{};
    std::vector<float> draft_hidden(g.hidden);
    const float* previous = slot.target_hidden.data();
    std::uint32_t draft_input = next_token;
    for(std::uint32_t index = 0; index < wanted; ++index){
        drafts[index] = bailing_mtp_draft(*runtime, slot, draft_input,
                                          previous, draft_hidden.data());
        draft_input = drafts[index];
        previous = draft_hidden.data();
    }
    runtime->mtp_drafted += wanted;
    std::array<std::uint32_t, 8> inputs{}, winners{};
    inputs[0] = next_token;
    for(std::uint32_t index = 1; index < wanted; ++index)
        inputs[index] = drafts[index - 1];
    bailing_mtp_snapshot(*runtime, slot, false);
    const auto base_position = slot.position;
    std::vector<float> hiddens(static_cast<std::size_t>(wanted) * g.hidden);
    bailing_mtp_verify_rows(*runtime, slot, inputs.data(), wanted,
                            winners.data(), hiddens.data());
    std::uint32_t valid = 0;
    bool rejected = false;
    for(; valid < wanted; ++valid)
        if(winners[valid] != drafts[valid]){ ++valid; rejected = true; break; }
    if(rejected && valid < wanted){
        // Only the KDA state ran ahead: KV rows, hiddens and winners for the
        // accepted prefix came from correct inputs. Restore and fold.
        bailing_mtp_snapshot(*runtime, slot, true);
        bailing_mtp_fold(*runtime, slot, valid);
        for(std::size_t layer = 0; layer < runtime->layers.size(); ++layer)
            if(runtime->layers[layer].full_attention)
                slot.caches[layer].positions = base_position + valid;
        slot.position = base_position + valid;
    }
    const std::uint32_t committed = rejected && valid < wanted ? valid : wanted;
    // Rebuild the draft cache on true hiddens: step zero already consumed the
    // target's real hidden, later steps consumed recursive draft hiddens and
    // are re-appended conditioned on what the verify actually produced.
    slot.draft_cache.positions = draft_base + 1;
    std::vector<float> append_hidden(g.hidden);
    for(std::uint32_t index = 1; index < committed; ++index)
        bailing_mtp_draft_layer(*runtime, slot, inputs[index],
                                hiddens.data() + static_cast<std::size_t>(index - 1) * g.hidden,
                                append_hidden.data());
    const float* last_hidden = hiddens.data()
        + static_cast<std::size_t>(committed - 1) * g.hidden;
    slot.target_hidden.assign(last_hidden, last_hidden + g.hidden);
    slot.has_target_hidden = true;
    runtime->mtp_accepted += rejected ? valid - 1 : wanted;
    if(rejected) ++runtime->mtp_rejected;
    for(std::uint32_t index = 0; index < committed; ++index)
        out_tokens[index] = winners[index];
    *out_count = committed;
    return 0; });
}

int flyweight_v2_bailing_uses_gpu(FlyweightV2BailingRuntime* runtime, int* out){
    return guarded([&]{
    if(!runtime||!out)throw std::runtime_error("invalid bailing uses-gpu arguments");
    // `gpu` is allocated before preparation and kept afterwards even when
    // preparation failed part way; `ready` is the flag eval itself tests, so it
    // is the only one that answers "did this runtime get the device".
    *out = (runtime->gpu && runtime->gpu->ready) ? 1 : 0;
    return 0; });
}

int flyweight_v2_bailing_set_progress(FlyweightV2BailingRuntime* runtime,
                                    int (*callback)(void*, uint32_t, uint32_t),
                                    void* user){
    return guarded([&]{
    if(!runtime)throw std::runtime_error("invalid bailing runtime");
    runtime->progress = callback;
    runtime->progress_user = callback ? user : nullptr;
    runtime->reporting = false;
    runtime->reported = 0;
    return 0; });
}

// One live BailingMoE3 sequence, serialized.
//
// Machine-local like the HF weight cache: native endianness, native float
// layout, no pretence of portability between builds. What it does carry is
// enough geometry to prove a snapshot belongs to this runtime, because the
// failure mode of getting that wrong is not a crash -- it is a model answering
// fluently from somebody else's context, which nothing downstream can detect.
//
// Sized by `position`, not by capacity: a 32-token side-call is a few hundred
// kilobytes and a 32k conversation is the megabytes it actually occupies. That
// is the difference between a prefix cache worth keeping and one that stores
// the same zeros a hundred times.
struct BailingCacheHeader {
    char magic[8];
    std::uint32_t version;
    std::uint32_t layers;
    std::uint32_t kv_lora;
    std::uint32_t qk_rope;
    std::uint32_t heads;
    std::uint32_t head_dim;
    std::uint32_t conv_width;
    std::uint32_t position;
    std::uint64_t payload;  // cache bytes following the header and layer map
};
static_assert(sizeof(BailingCacheHeader) == 48,
              "bailing snapshot header must stay packed as written");
static const char kBailingCacheMagic[8] = "CBLGKV0";
static const std::uint32_t kBailingCacheVersion = 1;

// How many bytes of cache a sequence of `position` tokens occupies. MLA layers
// grow with the sequence; KDA layers are a fixed recurrent state and three
// convolution windows however long the sequence is.
std::uint64_t bailing_cache_payload_bytes(const FlyweightV2BailingRuntime& runtime,
                                          std::uint64_t position) {
    const auto& g = runtime.geometry;
    const std::uint64_t channels = static_cast<std::uint64_t>(g.heads) * g.head_dim;
    const std::uint64_t history = g.conv_width ? g.conv_width - 1 : 0;
    std::uint64_t bytes = 0;
    for (const auto& layer : runtime.layers)
        bytes += layer.full_attention
            ? position * (g.kv_lora + g.qk_rope) * 4
            : (channels * g.head_dim + 3 * channels * history) * 4;
    return bytes;
}

int flyweight_v2_bailing_cache_save_slot(FlyweightV2BailingRuntime* runtime,
                                       uint32_t slot_index, void* buffer,
                                       uint64_t capacity, uint64_t* length){
    return guarded([&]{
    if(!runtime||!length)
        throw std::runtime_error("invalid bailing cache save arguments");
    const auto& g = runtime->geometry;
    auto& slot = runtime->slot(slot_index);
    const std::uint64_t position = slot.position;
    const std::uint64_t payload = bailing_cache_payload_bytes(*runtime, position);
    const std::uint64_t total =
        sizeof(BailingCacheHeader) + runtime->layers.size() + payload;
    *length = total;
    // The sizing call. It deliberately touches nothing else: a caller that only
    // wants to know how big a snapshot would be should not move a cache across
    // the bus to find out.
    if(!buffer || capacity == 0) return 0;
    if(capacity < total)
        throw std::runtime_error("bailing cache snapshot buffer is too small");
    // Whichever copy last ran is the live one, and after a device prefill or a
    // device decode that is not the host's. Snapshotting the host copy then
    // would capture the sequence as it was before that call. The transfer also
    // clears cache_on_device, which is right: the host copy is now current.
    if(slot.cache_on_device) bailing_cache_transfer(*runtime, slot_index, false);

    auto* out = static_cast<std::uint8_t*>(buffer);
    BailingCacheHeader header{};
    std::memcpy(header.magic, kBailingCacheMagic, sizeof(header.magic));
    header.version = kBailingCacheVersion;
    header.layers = static_cast<std::uint32_t>(runtime->layers.size());
    header.kv_lora = static_cast<std::uint32_t>(g.kv_lora);
    header.qk_rope = static_cast<std::uint32_t>(g.qk_rope);
    header.heads = static_cast<std::uint32_t>(g.heads);
    header.head_dim = static_cast<std::uint32_t>(g.head_dim);
    header.conv_width = static_cast<std::uint32_t>(g.conv_width);
    header.position = slot.position;
    header.payload = payload;
    std::memcpy(out, &header, sizeof(header));
    std::uint64_t offset = sizeof(header);
    // Which layers are MLA and which are KDA. Derived from the model rather
    // than stored anywhere, so two checkpoints can agree on every number in the
    // header and still disagree here.
    for(const auto& layer : runtime->layers)
        out[offset++] = layer.full_attention ? 1 : 0;

    auto append = [&](const std::vector<float>& source, std::uint64_t elements){
        if(!elements) return;
        if(source.size() < elements)
            throw std::runtime_error("bailing cache is shorter than its sequence");
        std::memcpy(out + offset, source.data(),
                    static_cast<std::size_t>(elements) * 4);
        offset += elements * 4;
    };
    const std::uint64_t channels = static_cast<std::uint64_t>(g.heads) * g.head_dim;
    const std::uint64_t history = g.conv_width ? g.conv_width - 1 : 0;
    for(std::size_t index=0;index<runtime->layers.size();++index){
        const auto& cache = slot.caches[index];
        if(runtime->layers[index].full_attention){
            append(cache.latents, position * g.kv_lora);
            append(cache.rope_keys, position * g.qk_rope);
        } else {
            append(cache.state, channels * g.head_dim);
            append(cache.query_window, channels * history);
            append(cache.key_window, channels * history);
            append(cache.value_window, channels * history);
        }
    }
    *length = offset;
    return 0; });
}

int flyweight_v2_bailing_cache_save(FlyweightV2BailingRuntime* runtime, void* buffer,
                                  uint64_t capacity, uint64_t* length){
    return flyweight_v2_bailing_cache_save_slot(runtime, 0, buffer, capacity, length);
}

int flyweight_v2_bailing_cache_load_slot(FlyweightV2BailingRuntime* runtime,
                                       uint32_t slot_index, const void* buffer,
                                       uint64_t length){
    return guarded([&]{
    if(!runtime||!buffer)
        throw std::runtime_error("invalid bailing cache load arguments");
    auto& slot = runtime->slot(slot_index);
    if(length < sizeof(BailingCacheHeader))
        throw std::runtime_error("bailing cache snapshot is truncated");
    BailingCacheHeader header{};
    std::memcpy(&header, buffer, sizeof(header));
    if(std::memcmp(header.magic, kBailingCacheMagic, sizeof(header.magic)) != 0)
        throw std::runtime_error("this is not a bailing cache snapshot");
    if(header.version != kBailingCacheVersion)
        throw std::runtime_error(
            "bailing cache snapshot is version " + std::to_string(header.version) +
            ", this build writes " + std::to_string(kBailingCacheVersion));
    const auto& g = runtime->geometry;
    if(header.layers != runtime->layers.size() ||
       header.kv_lora != g.kv_lora || header.qk_rope != g.qk_rope ||
       header.heads != g.heads || header.head_dim != g.head_dim ||
       header.conv_width != g.conv_width)
        throw std::runtime_error(
            "bailing cache snapshot was taken from a differently shaped model");
    if(header.position > runtime->capacity)
        throw std::runtime_error(
            "bailing cache snapshot holds " + std::to_string(header.position) +
            " tokens, more than the runtime capacity of " +
            std::to_string(runtime->capacity));
    const std::uint64_t payload =
        bailing_cache_payload_bytes(*runtime, header.position);
    if(header.payload != payload)
        throw std::runtime_error("bailing cache snapshot payload does not match "
                                 "the geometry it claims");
    if(length < sizeof(header) + header.layers + payload)
        throw std::runtime_error("bailing cache snapshot is truncated");

    const auto* in = static_cast<const std::uint8_t*>(buffer);
    std::uint64_t offset = sizeof(header);
    for(std::size_t index=0;index<runtime->layers.size();++index)
        if((in[offset++] != 0) != runtime->layers[index].full_attention)
            throw std::runtime_error(
                "bailing cache snapshot disagrees about which layers attend");

    auto take = [&](std::vector<float>& target, std::uint64_t elements){
        if(!elements) return;
        if(target.size() < elements)
            throw std::runtime_error("bailing cache is shorter than the snapshot");
        // Bounds-checked before the read, not after: the header is the only
        // thing that says how long the payload is, and a header is exactly what
        // a corrupt snapshot has.
        if(offset + elements * 4 > length)
            throw std::runtime_error("bailing cache snapshot is truncated");
        std::memcpy(target.data(), in + offset,
                    static_cast<std::size_t>(elements) * 4);
        offset += elements * 4;
    };
    const std::uint64_t position = header.position;
    const std::uint64_t channels = static_cast<std::uint64_t>(g.heads) * g.head_dim;
    const std::uint64_t history = g.conv_width ? g.conv_width - 1 : 0;
    for(std::size_t index=0;index<runtime->layers.size();++index){
        auto& cache = slot.caches[index];
        if(runtime->layers[index].full_attention){
            take(cache.latents, position * g.kv_lora);
            take(cache.rope_keys, position * g.qk_rope);
        } else {
            take(cache.state, channels * g.head_dim);
            take(cache.query_window, channels * history);
            take(cache.key_window, channels * history);
            take(cache.value_window, channels * history);
        }
        // Rows past `position` are left as they were. Attention reads exactly
        // `positions` of them and every position is written before it is read,
        // so what is beyond is unreachable rather than stale.
        cache.positions = static_cast<std::size_t>(position);
    }
    slot.position = static_cast<std::uint32_t>(position);
    // The device copy, if there is one, now holds another sequence entirely.
    // Saying the host is authoritative makes the next eval upload this one.
    slot.cache_on_device = false;
    return 0; });
}

int flyweight_v2_bailing_cache_load(FlyweightV2BailingRuntime* runtime,
                                  const void* buffer, uint64_t length){
    return flyweight_v2_bailing_cache_load_slot(runtime, 0, buffer, length);
}

// The names offered, best-compression first, which is also the order a menu
// wants. Q6_K is the loader's default and is marked as such by the caller, not
// here.
static const char* const kHfQuantNames[] = {
    "IQ2_XS","Q2_K","IQ3_XXS","Q3_K","IQ4_XS","Q4_K","Q5_K","Q6_K","Q8_0",
    "F32"};

int flyweight_v2_hf_quant_options(const char* directory,
    FlyweightV2HfQuantOption* out, std::uint32_t capacity, std::uint32_t* count
) { return guarded([&]{
    namespace hf = flyweight::v2::hf;
    if(!directory||!out||!count)
        throw std::runtime_error("directory, output and count are required");
    const std::size_t options=sizeof(kHfQuantNames)/sizeof(kHfQuantNames[0]);
    if(capacity<options)
        throw std::runtime_error("quantization option buffer is too small");
    *count=0;

    const std::string folder=directory;
    const auto config_text=read_text_file(folder+"/config.json");
    if(config_text.empty())throw std::runtime_error("cannot read config.json");
    const auto config=hf::config_from_json(flyweight::v2::json::parse(config_text));
    const auto files=hf_shard_files(folder);
    const auto sources=hf_cache_sources(files);

    // Headers only. parse_header touches the JSON at the front of each shard;
    // the weights behind it are mapped but never faulted in, so this stays a
    // metadata read even on a 52 GiB checkpoint.
    std::vector<std::unique_ptr<FlyweightV2Model>> mappings;
    std::vector<hf::Shard> parsed;
    parsed.reserve(files.size());
    for(const auto& file:files){
        auto shard=std::make_unique<FlyweightV2Model>();
        map_file(file.c_str(),*shard);
        parsed.push_back({flyweight::v2::safetensors::parse_header(shard->data,shard->size),
                          shard->data});
        mappings.push_back(std::move(shard));
    }
    const auto tensors=hf::build_tensors(parsed,config);

    for(std::size_t i=0;i<options;++i){
        hf::Policy policy;
        if(!hf_policy_for(kHfQuantNames[i],policy))
            throw std::runtime_error("unknown quantization name in the option table");
        auto& entry=out[i];
        std::memset(&entry,0,sizeof(entry));
        copy_text(entry.name,sizeof(entry.name),kHfQuantNames[i]);
        // The same imatrix conditions the loader applies, so an offer here is
        // a load that would succeed and a "cached" mark is a cache the open
        // would actually hit.
        hf::cache::SourceFile imatrix_identity;
        const auto imatrix_path=hf_imatrix_path(folder,imatrix_identity);
        const bool imatrix_applies=
            !imatrix_path.empty()&&hf_policy_reads_imatrix(policy);
        auto refusal=hf_policy_unavailable(policy,config);
        if(refusal.empty())
            refusal=hf_policy_needs_missing_imatrix(
                policy,!imatrix_path.empty());
        copy_text(entry.unavailable,sizeof(entry.unavailable),refusal.c_str());
        if(!refusal.empty())continue;
        entry.arena_bytes=hf::arena_bytes(tensors,policy);
        const auto print=hf::cache::fingerprint(
            config_text,sources,policy,
            imatrix_applies?&imatrix_identity:nullptr);
        // The first candidate is where a write would go; an existing one
        // anywhere in the list is what an open would actually use.
        const auto candidates=hf_cache_candidates(folder,print);
        if(!candidates.empty())
            copy_text(entry.cache_path,sizeof(entry.cache_path),candidates.front().c_str());
        for(const auto& candidate:candidates){
            struct stat st{};
            if(stat(candidate.c_str(),&st)!=0||!S_ISREG(st.st_mode))continue;
            entry.cache_bytes=static_cast<std::uint64_t>(st.st_size);
            copy_text(entry.cache_path,sizeof(entry.cache_path),candidate.c_str());
            break;
        }
    }
    *count=static_cast<std::uint32_t>(options);
    return 0; }); }

int flyweight_v2_model_open(const char* path, FlyweightV2Model** out) { return guarded([&]{
    if(!path||!out) throw std::runtime_error("path and output are required");
    auto m=std::make_unique<FlyweightV2Model>();
    if(is_hf_directory(path)){
        load_hf(path,*m);
    }else{
        map_and_parse(path,*m);
        attach_split_shards(*m);
        // After the shards attach, so a split checkpoint's tensors are all
        // visible to the adaptation and to the plan check alike.
        if(m->architecture=="bailingmoe3"){
            bailing_adapt_gguf(*m);
            bailing_validate_plan(*m);
        }
    }
    *out=m.release(); return 0; }); }
int flyweight_v2_model_attach_mtp(FlyweightV2Model* m,const char*path){return guarded([&]{
    if(!m||!path||!path[0])throw std::runtime_error("model and MTP sidecar path are required");
    if(m->mtp_sidecar)throw std::runtime_error("an MTP sidecar is already attached");
    FlyweightV2Model*opened=nullptr;
    if(flyweight_v2_model_open(path,&opened)!=0)
        throw std::runtime_error(error.empty()?"failed to open MTP sidecar":error);
    std::unique_ptr<FlyweightV2Model>sidecar(opened);
    if(sidecar->mtp_layer==std::numeric_limits<std::uint32_t>::max())
        throw std::runtime_error("MTP sidecar has no draft block");
    if(m->mtp_layer!=std::numeric_limits<std::uint32_t>::max()&&
       sidecar->mtp_layer!=m->mtp_layer)
        throw std::runtime_error("MTP sidecar draft layer does not match the base GGUF");
    if(sidecar->config.hidden_size!=m->config.hidden_size||
       sidecar->config.attention_heads!=m->config.attention_heads||
       sidecar->config.attention_kv_heads!=m->config.attention_kv_heads||
       sidecar->config.expert_count!=m->config.expert_count||
       sidecar->config.expert_used_count!=m->config.expert_used_count)
        throw std::runtime_error("MTP sidecar model geometry does not match the base GGUF");
    if(m->mtp_layer==std::numeric_limits<std::uint32_t>::max())
        m->mtp_layer=sidecar->mtp_layer;
    const std::string prefix="blk."+std::to_string(m->mtp_layer)+".";
    std::size_t replaced=0;
    for(auto&target:m->tensors){
        if(target.name.rfind(prefix,0)!=0)continue;
        const auto found=std::find_if(sidecar->tensors.begin(),sidecar->tensors.end(),
            [&](const Tensor&candidate){return candidate.name==target.name;});
        if(found==sidecar->tensors.end())
            throw std::runtime_error("MTP sidecar is missing tensor: "+target.name);
        auto elements=[](const Tensor&tensor){std::uint64_t result=1;for(const auto dimension:tensor.shape)result*=dimension;return result;};
        if(found->shape!=target.shape&&elements(*found)!=elements(target))
            throw std::runtime_error("MTP sidecar tensor shape mismatch: "+target.name);
        target.type=found->type;
        target.offset=found->offset;
        target.size=found->size;
        target.source=sidecar->data;
        ++replaced;
    }
    // Trunk-only GGUFs omit the optional draft block entirely. Append its
    // descriptors while keeping their bytes in the sidecar mapping.
    for(const auto&candidate:sidecar->tensors){
        if(candidate.name.rfind(prefix,0)!=0)continue;
        const auto present=std::any_of(m->tensors.begin(),m->tensors.end(),
            [&](const Tensor&target){return target.name==candidate.name;});
        if(present)continue;
        auto added=candidate;
        added.source=sidecar->data;
        m->tensors.push_back(std::move(added));
        ++replaced;
    }
    if(!replaced)throw std::runtime_error("MTP sidecar did not replace any tensors");
    m->mtp_sidecar=std::move(sidecar);
    return 0;
});}
void flyweight_v2_model_close(FlyweightV2Model* m) { try{delete m;}catch(...){} }
int flyweight_v2_model_info(const FlyweightV2Model* m, FlyweightV2ModelInfo* out) { return guarded([&]{if(!m||!out)throw std::runtime_error("invalid model info handle"); std::memset(out,0,sizeof(*out));out->gguf_version=m->version;out->tensor_count=m->tensor_count();out->metadata_count=m->metadata;out->file_size=m->size;out->alignment=m->alignment;copy_text(out->architecture,sizeof(out->architecture),m->architecture);copy_text(out->name,sizeof(out->name),m->name);copy_text(out->format,sizeof(out->format),m->format());return 0;}); }
int flyweight_v2_model_chat_template(const FlyweightV2Model* m,char*out,uint64_t capacity,uint64_t*length){return guarded([&]{if(!m||!length)throw std::runtime_error("invalid model chat-template arguments");*length=static_cast<uint64_t>(m->chat_template.size());if(!out||capacity==0)return 0;if(capacity<=m->chat_template.size())throw std::runtime_error("chat-template output buffer is too small");std::memcpy(out,m->chat_template.data(),m->chat_template.size());out[m->chat_template.size()]=0;return 0;});}
int flyweight_v2_model_config(const FlyweightV2Model* m, FlyweightV2ModelConfig* out){return guarded([&]{if(!m||!out)throw std::runtime_error("invalid model config handle");std::memset(out,0,sizeof(*out));copy_text(out->architecture,sizeof(out->architecture),m->config.architecture);out->hidden_size=m->config.hidden_size;out->layer_count=m->config.layer_count;out->attention_heads=m->config.attention_heads;out->attention_kv_heads=m->config.attention_kv_heads;out->context_length=m->config.context_length;out->intermediate_size=m->config.intermediate_size;out->expert_count=m->config.expert_count;out->expert_used_count=m->config.expert_used_count;out->vocabulary_size=m->config.vocabulary_size;out->rotary_dimension=m->config.rotary_dimension;out->full_attention_interval=m->config.full_attention_interval;out->sliding_window=m->config.sliding_window;out->sliding_window_pattern_length=static_cast<uint32_t>(m->config.sliding_window_pattern.size());out->rms_norm_epsilon=m->config.rms_norm_epsilon;out->rope_freq_base=m->config.rope_freq_base;out->eos_token_id=m->config.eos_token_id;out->eot_token_id=m->config.eot_token_id;out->bos_token_id=m->config.bos_token_id;
out->q_lora_rank=m->config.q_lora_rank;out->kv_lora_rank=m->config.kv_lora_rank;out->output_lora_rank=m->config.output_lora_rank;out->output_group_count=m->config.output_group_count;
out->indexer_head_count=m->config.indexer_head_count;out->indexer_key_length=m->config.indexer_key_length;out->indexer_top_k=m->config.indexer_top_k;
out->hyper_connection_count=m->config.hyper_connection_count;out->sinkhorn_iterations=m->config.sinkhorn_iterations;
out->expert_group_count=m->config.expert_group_count;out->expert_group_used=m->config.expert_group_used;out->expert_shared_count=m->config.expert_shared_count;out->hash_layer_count=m->config.hash_layer_count;
out->compress_ratios_length=static_cast<std::uint32_t>(m->config.compress_ratios.size());
out->sinkhorn_epsilon=m->config.sinkhorn_epsilon;out->compress_rope_freq_base=m->config.compress_rope_freq_base;
out->rope_scaling_factor=m->config.rope_scaling_factor;out->yarn_beta_fast=m->config.yarn_beta_fast;out->yarn_beta_slow=m->config.yarn_beta_slow;
out->rope_original_context_length=m->config.rope_original_context_length;
out->draft_block_size=m->config.draft_block_size;
out->target_layers_length=static_cast<std::uint32_t>(m->config.target_layers.size());
out->mask_token_id=m->config.mask_token_id;
out->logit_scale=m->config.logit_scale;
out->final_logit_softcap=m->config.final_logit_softcap;return 0;});}

// Multiply a model tensor by a vector, decoding whatever weight type the
// checkpoint stores it as. GGUF reports a matrix as [inputs, outputs], so
// `output_size` rows are each a dot product over `input_size` elements.
int flyweight_v2_matvec(
    const FlyweightV2Model* m, const char* name, const float* input,
    int32_t input_size, float* output, int32_t output_size
){return guarded([&]{
    if(!m||!name||!input||!output)throw std::runtime_error("matvec arguments are required");
    const auto found=std::find_if(m->tensors.begin(),m->tensors.end(),
        [&](const Tensor& tensor){return tensor.name==name;});
    if(found==m->tensors.end())throw std::runtime_error(std::string("tensor not found: ")+name);
    if(found->shape.size()!=2)throw std::runtime_error("matvec needs a 2D tensor");
    if(static_cast<std::int64_t>(found->shape[0])!=input_size||
       static_cast<std::int64_t>(found->shape[1])!=output_size)
        throw std::runtime_error("matvec shape does not match the tensor");
    if(!qwen_cpu_expert_type_supported(found->type))
        throw std::runtime_error("matvec cannot decode this weight type");
    const auto* packed=tensor_data(*m,*found);
    for(std::int32_t row=0;row<output_size;++row)
        output[row]=qwen_quant_dot(packed,found->type,input,input_size,
            static_cast<std::uint64_t>(row));
    return 0;});}

// RMS normalization over `count` consecutive rows of `size`, optionally scaled
// by a per-element gain. DeepSeek-V4 uses both forms: the query and KV latents
// are normalized with a learned gain, while the per-head query norm has none.
int flyweight_v2_deepseek4_rms_norm(
    const float* input, const float* weight, int32_t size, int32_t count,
    float epsilon, float* output
){return guarded([&]{
    if(!input||!output||size<=0||count<=0)
        throw std::runtime_error("rms-norm arguments are invalid");
    for(std::int32_t row=0;row<count;++row){
        const auto offset=static_cast<std::size_t>(row)*static_cast<std::size_t>(size);
        flyweight::v2::deepseek4::rms_norm(
            input+offset,static_cast<std::size_t>(size),epsilon,output+offset);
        if(weight)
            for(std::int32_t i=0;i<size;++i)output[offset+i]*=weight[i];
    }
    return 0;});}

// Grouped matvec: the input is cut into `groups` chunks and chunk g is
// multiplied by the g-th slice of the tensor's output rows. `flyweight_v2_matvec`
// is the groups==1 case; DeepSeek-V4's output projection uses eight.
int flyweight_v2_grouped_matvec(
    const FlyweightV2Model* m, const char* name, const float* input,
    int32_t inputs, float* output, int32_t rank, int32_t groups
){return guarded([&]{
    if(!m||!name||!input||!output||inputs<=0||rank<=0||groups<=0)
        throw std::runtime_error("grouped matvec arguments are invalid");
    const auto found=std::find_if(m->tensors.begin(),m->tensors.end(),
        [&](const Tensor& tensor){return tensor.name==name;});
    if(found==m->tensors.end())throw std::runtime_error(std::string("tensor not found: ")+name);
    if(found->shape.size()!=2||static_cast<std::int64_t>(found->shape[0])!=inputs||
       static_cast<std::int64_t>(found->shape[1])!=static_cast<std::int64_t>(rank)*groups)
        throw std::runtime_error("grouped matvec shape does not match the tensor");
    if(!qwen_cpu_expert_type_supported(found->type))
        throw std::runtime_error("grouped matvec cannot decode this weight type");
    const auto* packed=tensor_data(*m,*found);
    flyweight::v2::deepseek4::grouped_projection(
        input,static_cast<std::size_t>(inputs),static_cast<std::size_t>(rank),
        static_cast<std::size_t>(groups),output,
        [&](const float* source,std::size_t row){
            return qwen_quant_dot(packed,found->type,source,inputs,
                static_cast<std::uint64_t>(row));
        });
    return 0;});}

// Matvec against one expert of a stacked expert tensor. The experts are laid
// out back to back, so expert e's rows start at e*outputs.
int flyweight_v2_expert_matvec(
    const FlyweightV2Model* m, const char* name, int32_t expert,
    const float* input, int32_t inputs, float* output, int32_t outputs
){return guarded([&]{
    if(!m||!name||!input||!output||expert<0||inputs<=0||outputs<=0)
        throw std::runtime_error("expert matvec arguments are invalid");
    const auto found=std::find_if(m->tensors.begin(),m->tensors.end(),
        [&](const Tensor& tensor){return tensor.name==name;});
    if(found==m->tensors.end())throw std::runtime_error(std::string("tensor not found: ")+name);
    if(found->shape.size()!=3||static_cast<std::int64_t>(found->shape[0])!=inputs||
       static_cast<std::int64_t>(found->shape[1])!=outputs)
        throw std::runtime_error("expert matvec shape does not match the tensor");
    if(expert>=static_cast<std::int64_t>(found->shape[2]))
        throw std::runtime_error("expert index is out of range");
    if(!qwen_cpu_expert_type_supported(found->type))
        throw std::runtime_error("expert matvec cannot decode this weight type");
    const auto* packed=tensor_data(*m,*found);
    const auto base=static_cast<std::uint64_t>(expert)*outputs;
    for(std::int32_t row=0;row<outputs;++row)
        output[row]=qwen_quant_dot(packed,found->type,input,inputs,base+row);
    return 0;});}

// Collapse the streams for the output head, which needs only pre-weights.
int flyweight_v2_deepseek4_head(
    const float* streams, const float* fn, const float* scale, const float* base,
    int32_t n_embd, int32_t hc, float rms_epsilon, float hc_epsilon,
    float* pre, float* output
){return guarded([&]{
    if(!streams||!fn||!scale||!base||!pre||!output||n_embd<=0||hc<=0)
        throw std::runtime_error("head arguments are invalid");
    flyweight::v2::deepseek4::hyper_connection_head(
        streams,fn,scale,base,static_cast<std::size_t>(n_embd),
        static_cast<std::size_t>(hc),rms_epsilon,hc_epsilon,pre,output);
    return 0;});}

// One sequence's DeepSeek-V4 state, sized from the architecture rather than
// from the context length wherever the architecture allows it.
//
// Raw latents are bounded by the sliding window on every layer, so they live in
// a ring of `window` entries rather than growing with the sequence. The
// compressor keeps only what a future block can still read: a 4:1 layer pools
// the previous block's rows alongside its own, so two blocks' worth, and a
// 128:1 layer pools only its own. Just the compressed caches scale with
// context, at context/ratio entries.
// Every weight one block reads, resolved to a tensor index once.
//
// The lookups this replaces are linear scans by name over all 1328 descriptors.
// A block reads about twenty weights, so a 43-layer token would spend roughly
// 900 scans of a 1328-entry vector on nothing but finding them again.
struct Deepseek4LayerPlan {
    static constexpr std::uint64_t kAbsent = std::numeric_limits<std::uint64_t>::max();
    std::uint64_t attn_norm = kAbsent, q_a = kAbsent, q_a_norm = kAbsent, q_b = kAbsent;
    std::uint64_t kv = kAbsent, kv_a_norm = kAbsent, sinks = kAbsent;
    std::uint64_t output_a = kAbsent, output_b = kAbsent;
    std::uint64_t hc_attn_fn = kAbsent, hc_attn_scale = kAbsent, hc_attn_base = kAbsent;
    std::uint64_t hc_ffn_fn = kAbsent, hc_ffn_scale = kAbsent, hc_ffn_base = kAbsent;
    std::uint64_t ffn_norm = kAbsent, gate_inp = kAbsent;
    std::uint64_t gate_exps = kAbsent, up_exps = kAbsent, down_exps = kAbsent;
    std::uint64_t gate_shexp = kAbsent, up_shexp = kAbsent, down_shexp = kAbsent;
    // Exactly one of these two: hash layers route by table, the rest by bias.
    std::uint64_t tid2eid = kAbsent, exp_probs_b = kAbsent;
    // Compressed layers only.
    std::uint64_t comp_kv = kAbsent, comp_gate = kAbsent, comp_ape = kAbsent, comp_norm = kAbsent;
    // 4:1 layers only.
    std::uint64_t indexer_proj = kAbsent, indexer_q_b = kAbsent;
    std::uint64_t indexer_comp_kv = kAbsent, indexer_comp_gate = kAbsent;
    std::uint64_t indexer_comp_ape = kAbsent, indexer_comp_norm = kAbsent;

    std::uint32_t resolved() const {
        const std::uint64_t* first = &attn_norm;
        const std::uint64_t* last = &indexer_comp_norm;
        std::uint32_t count = 0;
        for (const std::uint64_t* it = first; it <= last; ++it) count += (*it != kAbsent);
        return count;
    }
};

struct Deepseek4LayerState {
    Deepseek4LayerPlan plan;
    std::uint32_t ratio = 0, window = 0, head_dim = 0;
    std::uint32_t state_width = 0, state_rows = 0, block_capacity = 0;
    // Half precision, as the reference stores them. The composed model rounds
    // its latents the same way, so nothing is lost by matching.
    std::vector<std::uint16_t> latents;    // window x head_dim, ring
    std::vector<std::uint16_t> compressed; // block_capacity x head_dim
    std::vector<float> state_values;  // state_rows x state_width, ring
    std::vector<float> state_scores;
    std::uint32_t positions = 0, blocks = 0;
    // The lightning indexer's own compressor and cache, on 4:1 layers only. The
    // same shape as the pair above at a narrower width -- 128 against 512 --
    // because it only has to rank blocks, not answer with them.
    std::uint32_t indexer_dim = 0, indexer_width = 0;
    std::vector<std::uint16_t> indexer_compressed;
    std::vector<float> indexer_state_values, indexer_state_scores;

    std::uint64_t bytes() const {
        return (latents.capacity() + compressed.capacity() + indexer_compressed.capacity())
                   * sizeof(std::uint16_t) +
               (state_values.capacity() + state_scores.capacity() +
                indexer_state_values.capacity() + indexer_state_scores.capacity()) * sizeof(float);
    }
    // The ring holds the newest `state_rows` rows, so a row's slot is its
    // position modulo the ring size.
    std::uint32_t slot(std::uint32_t position) const { return position % state_rows; }
};

// Scratch a single position's forward needs, allocated once per runtime.
struct Deepseek4Scratch {
    std::vector<float> streams, next_streams, collapsed, hidden;
    std::vector<float> pre, post, comb;
    std::vector<float> low_rank, query, latent, block_out, grouped;
    std::vector<float> keys, attn, derope;
    std::vector<float> state_row, gate, up, activated, expert_out, moe, logits;
    std::vector<float> router, after, embedding, head_pre;
    std::vector<float> routed_gate, routed_up, routed_activated, routed_out;
    std::vector<float> indexer_query, indexer_weights, indexer_scores, indexer_keys;
    std::vector<std::uint8_t> mask, indexer_keep;
    std::vector<std::int32_t> experts;
    std::vector<float> weights;
    std::vector<std::uint8_t> gpu_experts;
};

struct Deepseek4ExpertSlot {
    std::uint64_t key = 0, last_used = 0;
    bool valid = false;
};

struct FlyweightV2Deepseek4Runtime {
    FlyweightV2Model* model = nullptr;
    std::uint32_t context_limit = 0;
    std::vector<Deepseek4LayerState> layers;
    Deepseek4Scratch scratch;
    // Geometry the loop reads on every layer.
    std::uint32_t n_embd = 0, hc = 0, heads = 0, head_dim = 0, rope_dim = 0;
    std::uint32_t q_lora = 0, groups = 0, lora_rank = 0, experts = 0, experts_used = 0;
    std::uint32_t expert_ffn = 0, vocabulary = 0, sinkhorn_iterations = 0, hash_layers = 0;
    std::uint32_t indexer_heads = 0, indexer_dim = 0, indexer_top_k = 0;
    float epsilon = 0.0f, freq_base = 0.0f, weight_scale = 1.5f, clamp = 10.0f;
    float compress_base = 0.0f, freq_scale = 1.0f, attn_factor = 1.0f;
    float beta_fast = 32.0f, beta_slow = 1.0f;
    std::uint32_t original_context = 0;
    std::uint64_t head_fn = 0, head_scale = 0, head_base = 0, output_norm = 0, output = 0;
    std::uint64_t token_embd = 0;

    // Where a token's time goes. Attribution rather than a single number: the
    // routed experts are believed to be nearly all of it, and paging work is
    // only worth doing if that is measured rather than assumed.
    std::uint64_t forward_calls = 0;
    std::uint64_t forward_nanoseconds = 0;
    std::uint64_t routed_expert_nanoseconds = 0, shared_expert_nanoseconds = 0;
    std::uint64_t attention_nanoseconds = 0, head_nanoseconds = 0;
    std::uint64_t attention_core_nanoseconds = 0;
    std::uint64_t routed_expert_bytes = 0;
    // How much the indexer is actually discarding, which is zero until a
    // sequence is long enough for it to run at all.
    std::uint64_t indexer_selections = 0, indexer_candidates = 0;
    // Weight prefetch, on unless FLYWEIGHT_DS4_PREFETCH=off, which exists so the
    // two regimes can be compared rather than assumed.
    bool expert_prefetch = true;
    std::uint64_t expert_prefetch_bytes = 0;
    // The dense half, resident on the device: tensor index -> device pointer.
    // The routed experts are never in here -- 90 GiB against 12 of VRAM -- which
    // is what makes the split this model wants the opposite of the usual one.
    bool gpu = false;
    bool gpu_owner = false;
    std::int32_t gpu_device = 0;
    std::unordered_map<std::uint64_t, std::uint64_t> resident;
    std::uint64_t gpu_input = 0, gpu_output = 0;
    std::uint64_t gpu_input_capacity = 0, gpu_output_capacity = 0;
    std::uint64_t gpu_weight_bytes = 0, gpu_matvec_calls = 0, gpu_batches = 0;
    std::uint64_t gpu_batch = 0, gpu_batch_capacity = 0;
    std::uint64_t expert_cache = 0, expert_cache_bytes = 0, expert_slot_bytes = 0;
    std::uint32_t expert_slots_per_layer = 0;
    std::uint64_t expert_cache_clock = 0, expert_cache_hits = 0;
    std::uint64_t expert_cache_misses = 0, expert_cache_evictions = 0;
    std::vector<Deepseek4ExpertSlot> expert_slots;
    std::unordered_map<std::uint64_t, std::size_t> expert_residency;
    std::unordered_map<std::uint64_t, std::uint32_t> expert_frequency;
    FlyweightV2Deepseek4Runtime* gpu_cache_owner = nullptr;
    std::uint64_t hyper_nanoseconds = 0, matvec_nanoseconds = 0;
    std::uint64_t prefill_calls = 0, prefill_tokens = 0, prefill_nanoseconds = 0;
    std::vector<std::uint32_t> capture_layers;
    std::vector<float> captured;

    std::uint64_t state_bytes() const {
        std::uint64_t total = 0;
        for (const auto& layer : layers) total += layer.bytes();
        return total;
    }
    void reset() {
        for (auto& layer : layers) { layer.positions = 0; layer.blocks = 0; }
    }
};
struct FlyweightV2Deepseek4Snapshot {
    std::vector<Deepseek4LayerState> layers;
    std::vector<float> captured;
};

int flyweight_v2_deepseek4_runtime_create(
    FlyweightV2Model* model, uint32_t context_limit, FlyweightV2Deepseek4Runtime** out
){return guarded([&]{
    if(!model||!out||!context_limit)
        throw std::runtime_error("model, context limit and output are required");
    const bool draft_model=model->config.architecture=="dflash";
    if(model->config.architecture!="deepseek4"&&!draft_model)
        throw std::runtime_error("not a deepseek4/dflash model");
    const auto& config=model->config;
    if(config.context_length&&context_limit>config.context_length)
        throw std::runtime_error("deepseek4 context limit exceeds the checkpoint context length");
    if(config.compress_ratios.size()<config.layer_count)
        throw std::runtime_error("compress-ratio array is shorter than the layer count");
    // One pass over the descriptors builds the name index every layer plan
    // draws from, so resolution is linear in the model rather than quadratic.
    std::unordered_map<std::string,std::uint64_t> by_name;
    by_name.reserve(model->tensors.size()*2);
    for(std::uint64_t index=0;index<model->tensors.size();++index)
        by_name.emplace(model->tensors[index].name,index);
    auto runtime=std::make_unique<FlyweightV2Deepseek4Runtime>();
    runtime->model=model;
    runtime->context_limit=context_limit;
    runtime->layers.resize(config.layer_count);
    const auto head_dim=config.kv_lora_rank?config.kv_lora_rank:config.key_length;
    // A zero sliding window would mean unbounded raw attention; the checkpoint
    // sets 128, and falling back to the context keeps the arithmetic honest
    // rather than silently allocating nothing.
    const auto window=config.sliding_window?config.sliding_window:context_limit;
    for(std::uint32_t index=0;index<config.layer_count;++index){
        auto& layer=runtime->layers[index];
        layer.ratio=config.compress_ratios[index];
        layer.window=std::min(window,context_limit);
        layer.head_dim=head_dim;
        layer.latents.assign(static_cast<std::size_t>(layer.window)*head_dim,0u);

        const std::string prefix="blk."+std::to_string(index)+".";
        auto find=[&](const char* suffix,bool required)->std::uint64_t{
            const auto found=by_name.find(prefix+suffix);
            if(found!=by_name.end())return found->second;
            if(required)
                throw std::runtime_error("deepseek4 layer is missing "+prefix+suffix);
            return Deepseek4LayerPlan::kAbsent;
        };
        auto& plan=layer.plan;
        plan.attn_norm=find("attn_norm.weight",true);
        plan.q_a=find("attn_q_a.weight",true);
        plan.q_a_norm=find("attn_q_a_norm.weight",true);
        plan.q_b=find("attn_q_b.weight",true);
        plan.kv=find("attn_kv.weight",true);
        plan.kv_a_norm=find("attn_kv_a_norm.weight",true);
        plan.sinks=find("attn_sinks.weight",true);
        plan.output_a=find("attn_output_a.weight",true);
        plan.output_b=find("attn_output_b.weight",true);
        plan.hc_attn_fn=find("hc_attn_fn.weight",true);
        plan.hc_attn_scale=find("hc_attn_scale.weight",true);
        plan.hc_attn_base=find("hc_attn_base.weight",true);
        plan.hc_ffn_fn=find("hc_ffn_fn.weight",true);
        plan.hc_ffn_scale=find("hc_ffn_scale.weight",true);
        plan.hc_ffn_base=find("hc_ffn_base.weight",true);
        plan.ffn_norm=find("ffn_norm.weight",true);
        plan.gate_inp=find("ffn_gate_inp.weight",true);
        plan.gate_exps=find("ffn_gate_exps.weight",true);
        plan.up_exps=find("ffn_up_exps.weight",true);
        plan.down_exps=find("ffn_down_exps.weight",true);
        plan.gate_shexp=find("ffn_gate_shexp.weight",true);
        plan.up_shexp=find("ffn_up_shexp.weight",true);
        plan.down_shexp=find("ffn_down_shexp.weight",true);
        // A hash layer routes from a table and carries no bias; every other
        // layer is the reverse. Requiring exactly one catches a checkpoint whose
        // hash_layer_count disagrees with its tensors.
        const bool hashed=index<config.hash_layer_count;
        plan.tid2eid=find("ffn_gate_tid2eid.weight",hashed);
        plan.exp_probs_b=find("exp_probs_b.bias",!hashed);
        if((plan.tid2eid!=Deepseek4LayerPlan::kAbsent)==
           (plan.exp_probs_b!=Deepseek4LayerPlan::kAbsent))
            throw std::runtime_error(
                "deepseek4 layer "+std::to_string(index)+
                " must carry either a routing table or a router bias, not both or neither");

        if(!layer.ratio)continue;
        plan.comp_kv=find("attn_compressor_kv.weight",true);
        plan.comp_gate=find("attn_compressor_gate.weight",true);
        plan.comp_ape=find("attn_compressor_ape.weight",true);
        plan.comp_norm=find("attn_compressor_norm.weight",true);
        if(layer.ratio==4){
            plan.indexer_proj=find("indexer.proj.weight",true);
            plan.indexer_q_b=find("indexer.attn_q_b.weight",true);
            plan.indexer_comp_kv=find("indexer_compressor_kv.weight",true);
            plan.indexer_comp_gate=find("indexer_compressor_gate.weight",true);
            plan.indexer_comp_ape=find("indexer_compressor_ape.weight",true);
            plan.indexer_comp_norm=find("indexer_compressor_norm.weight",true);
        }
        const bool overlapped=layer.ratio==4;
        layer.state_width=(overlapped?2u:1u)*head_dim;
        layer.state_rows=(overlapped?2u:1u)*layer.ratio;
        layer.block_capacity=context_limit/layer.ratio+1;
        // Compressed caches grow with the sequence. Eagerly materializing the
        // advertised one-million-token context consumed several GiB before the
        // first token even arrived.
        layer.compressed.clear();
        const std::size_t rows=static_cast<std::size_t>(layer.state_rows)*layer.state_width;
        layer.state_values.assign(rows,0.0f);
        layer.state_scores.assign(rows,-std::numeric_limits<float>::infinity());
        if(layer.ratio==4&&config.indexer_key_length){
            // The indexer compresses the same blocks at the same ratio, so it
            // shares the ring geometry and differs only in width.
            layer.indexer_dim=config.indexer_key_length;
            layer.indexer_width=2u*layer.indexer_dim;
            layer.indexer_compressed.clear();
            const std::size_t indexer_rows=
                static_cast<std::size_t>(layer.state_rows)*layer.indexer_width;
            layer.indexer_state_values.assign(indexer_rows,0.0f);
            layer.indexer_state_scores.assign(
                indexer_rows,-std::numeric_limits<float>::infinity());
        }
    }
    auto& g=*runtime;
    g.n_embd=config.hidden_size; g.hc=config.hyper_connection_count;
    g.heads=config.attention_heads; g.head_dim=head_dim;
    g.rope_dim=config.rotary_dimension; g.q_lora=config.q_lora_rank;
    g.groups=config.output_group_count; g.lora_rank=config.output_lora_rank;
    g.experts=config.expert_count; g.experts_used=config.expert_used_count;
    g.expert_ffn=config.expert_intermediate_size?config.expert_intermediate_size:config.intermediate_size;
    g.vocabulary=config.vocabulary_size; g.sinkhorn_iterations=config.sinkhorn_iterations;
    g.hash_layers=config.hash_layer_count; g.epsilon=config.rms_norm_epsilon;
    {
        const char* setting=std::getenv("FLYWEIGHT_DS4_PREFETCH");
        g.expert_prefetch=!(setting&&std::string(setting)=="off");
    }
    g.indexer_heads=config.indexer_head_count; g.indexer_dim=config.indexer_key_length;
    g.indexer_top_k=config.indexer_top_k;
    g.freq_base=config.rope_freq_base; g.compress_base=config.compress_rope_freq_base;
    g.weight_scale=config.expert_weights_scale?config.expert_weights_scale:1.0f;
    g.freq_scale=config.rope_scaling_factor>0.0f?1.0f/config.rope_scaling_factor:1.0f;
    g.attn_factor=1.0f/(1.0f+0.1f*std::log(1.0f/g.freq_scale));
    g.beta_fast=config.yarn_beta_fast; g.beta_slow=config.yarn_beta_slow;
    g.original_context=config.rope_original_context_length;
    auto require=[&](const char* name)->std::uint64_t{
        const auto found=by_name.find(name);
        if(found==by_name.end())throw std::runtime_error(std::string("model is missing ")+name);
        return found->second;
    };
    g.head_fn=require("output_hc_fn.weight"); g.head_scale=require("output_hc_scale.weight");
    g.head_base=require("output_hc_base.weight"); g.output_norm=require("output_norm.weight");
    if(draft_model){
        g.output=Deepseek4LayerPlan::kAbsent;
        g.token_embd=Deepseek4LayerPlan::kAbsent;
    }else{
        g.output=require("output.weight"); g.token_embd=require("token_embd.weight");
    }

    auto& sc=g.scratch;
    const std::size_t wide=static_cast<std::size_t>(g.heads)*g.head_dim;
    sc.streams.assign(static_cast<std::size_t>(g.hc)*g.n_embd,0.0f);
    sc.next_streams.assign(sc.streams.size(),0.0f);
    sc.collapsed.assign(g.n_embd,0.0f); sc.hidden.assign(g.n_embd,0.0f);
    sc.pre.assign(g.hc,0.0f); sc.post.assign(g.hc,0.0f); sc.comb.assign(static_cast<std::size_t>(g.hc)*g.hc,0.0f);
    sc.low_rank.assign(g.q_lora,0.0f); sc.query.assign(wide,0.0f);
    sc.latent.assign(g.head_dim,0.0f); sc.block_out.assign(g.n_embd,0.0f);
    sc.grouped.assign(static_cast<std::size_t>(g.groups)*g.lora_rank,0.0f);
    sc.attn.assign(wide,0.0f); sc.derope.assign(wide,0.0f);
    sc.state_row.assign(2ull*g.head_dim,0.0f);
    sc.gate.assign(g.expert_ffn,0.0f); sc.up.assign(g.expert_ffn,0.0f);
    sc.activated.assign(g.expert_ffn,0.0f); sc.expert_out.assign(g.n_embd,0.0f);
    sc.moe.assign(g.n_embd,0.0f); sc.logits.assign(g.vocabulary,0.0f);
    sc.router.assign(g.experts,0.0f);
    sc.after.assign(static_cast<std::size_t>(g.hc)*g.n_embd,0.0f);
    sc.embedding.assign(g.n_embd,0.0f); sc.head_pre.assign(g.hc,0.0f);
    sc.routed_gate.assign(static_cast<std::size_t>(g.experts_used)*g.expert_ffn,0.0f);
    sc.routed_up.assign(sc.routed_gate.size(),0.0f);
    sc.routed_activated.assign(sc.routed_gate.size(),0.0f);
    sc.routed_out.assign(static_cast<std::size_t>(g.experts_used)*g.n_embd,0.0f);
    sc.experts.assign(g.experts_used,0); sc.weights.assign(g.experts_used,0.0f);
    sc.gpu_experts.assign(g.experts_used,0);
    // Keys for one attention: the window plus every compressed block.
    std::uint32_t widest=0;
    for(const auto& layer:g.layers){
        const auto compressed = layer.indexer_dim && g.indexer_top_k
            ? std::min(layer.block_capacity,g.indexer_top_k)
            : layer.block_capacity;
        widest=std::max(widest,layer.window+compressed);
    }
    sc.keys.assign(static_cast<std::size_t>(widest)*g.head_dim,0.0f);
    sc.mask.assign(widest,0);
    // The indexer's scratch: one query per head, one weight per head, and a
    // score and a verdict per compressed block.
    if(g.indexer_dim){
        std::uint32_t blocks=0;
        for(const auto& layer:g.layers)
            if(layer.indexer_dim)blocks=std::max(blocks,layer.block_capacity);
        sc.indexer_query.assign(static_cast<std::size_t>(g.indexer_heads)*g.indexer_dim,0.0f);
        sc.indexer_weights.assign(g.indexer_heads,0.0f);
        sc.indexer_scores.assign(blocks,0.0f);
        sc.indexer_keep.assign(blocks,0);
    }

    *out=runtime.release();
    return 0;});}

void flyweight_v2_deepseek4_runtime_free(FlyweightV2Deepseek4Runtime* runtime){
    try{
        if(runtime&&runtime->gpu_owner){
            for(const auto& entry:runtime->resident)flyweight_gpu_free(entry.second);
            if(runtime->gpu_input)flyweight_gpu_free(runtime->gpu_input);
            if(runtime->gpu_output)flyweight_gpu_free(runtime->gpu_output);
            if(runtime->gpu_batch)flyweight_gpu_free(runtime->gpu_batch);
            if(runtime->expert_cache)flyweight_gpu_free(runtime->expert_cache);
        }
        delete runtime;
    }catch(...){}
}

int flyweight_v2_deepseek4_runtime_reset(FlyweightV2Deepseek4Runtime* runtime){return guarded([&]{
    if(!runtime)throw std::runtime_error("invalid runtime");
    runtime->reset();
    return 0;});}

namespace {

// How wide to fork a matvec: one thread per core, the policy the kernels share.
//
// Measured on this checkpoint: the default team is one thread per *hardware*
// thread, and on a 16-core/32-thread part that is 2.6x slower than one thread
// per core -- 9.3 GiB/s of expert weights against 24.6, and 1.9 tok/s against
// 3.2. Two threads sharing a core contend for the same L1 and the same decode
// units, and the loop forks about eight hundred times per token, so the wider
// team costs barrier time as well. deepseek4's pragmas simply never took a
// `num_threads`.
int ds4_thread_count() { return flyweight::v2::deepseek4::thread_count(); }

void ds4_pin_current_thread() {
    static const bool enabled=[] {
        const char* setting=std::getenv("FLYWEIGHT_DS4_AFFINITY");
        return setting&&std::string(setting)=="on";
    }();
    if(!enabled)return;
#if defined(_WIN32)
    static const std::vector<DWORD_PTR> cpus=[] {
        DWORD_PTR process_affinity, system_affinity;
        GetProcessAffinityMask(GetCurrentProcess(),
                               &process_affinity, &system_affinity);
        std::vector<DWORD_PTR> result;
        for(int cpu=0;cpu<64;++cpu)
            if(process_affinity&(DWORD_PTR(1)<<cpu))
                result.push_back(cpu);
        return result;
    }();
#else
    static const std::vector<int> cpus=[] {
        cpu_set_t allowed;
        CPU_ZERO(&allowed);
        std::vector<int> result;
        if(sched_getaffinity(0,sizeof(allowed),&allowed)==0)
            for(int cpu=0;cpu<CPU_SETSIZE;++cpu)
                if(CPU_ISSET(cpu,&allowed))result.push_back(cpu);
        return result;
    }();
#endif
    thread_local bool pinned=false;
    if(pinned||cpus.empty())return;
#if defined(_OPENMP) || defined(_WIN32)
    const int target=
#ifdef _OPENMP
        omp_get_thread_num();
#else
        0;  // single-threaded fallback
#endif
    // The OpenMP master is also the long-lived inference/scheduler thread.
    // Pinning it leaked affinity into unrelated work after the parallel region;
    // workers are persistent and safe to bind, while the master stays mobile.
    if(target==0)return;
    const auto cpu=cpus[static_cast<std::size_t>(target)%cpus.size()];
#if defined(_WIN32)
    SetThreadAffinityMask(GetCurrentThread(),static_cast<DWORD_PTR>(1)<<cpu);
    pinned=true;
#else
    cpu_set_t one;
    CPU_ZERO(&one); CPU_SET(cpu,&one);
    if(sched_setaffinity(0,sizeof(one),&one)==0)pinned=true;
#endif
#endif
}

// Compile the CUDA kernels once per process.
//
// The set is the Qwen one: the dense half of deepseek4 is Q8_0, Q6_K, BF16 and
// F32, and a matvec for each of those already exists because the Qwen work
// needed them. What deepseek4 adds later -- the Sinkhorn mixer, the compressor,
// the indexer -- goes into the same source, so this stays the one place that
// builds them.
void ds4_gpu_compile(std::int32_t device) {
    static bool built = false;
    static std::mutex guard;
    std::lock_guard<std::mutex> held(guard);
    if (built) return;
    std::vector<std::string> option_storage;
#if !defined(_WIN32)
    for (const char* path : {"/opt/cuda/include", "/usr/local/cuda/include", "/usr/include"})
        if (access((std::string(path) + "/cuda_fp16.h").c_str(), R_OK) == 0) {
            option_storage.push_back(std::string("-I") + path);
            if (access((std::string(path) + "/cccl/cub/config.cuh").c_str(), R_OK) == 0)
                option_storage.push_back(std::string("-I") + path + "/cccl");
        }
#endif
    if (const char* cuda_home = std::getenv("CUDA_HOME")) {
        option_storage.push_back(std::string("-I") + cuda_home + "/include");
        option_storage.push_back(std::string("-I") + cuda_home + "/include/cccl");
    }
    std::vector<const char*> options;
    for (const auto& option : option_storage) options.push_back(option.c_str());
    std::array<char, 16384> log{};
    std::string iq1_grid =
        "extern \"C\" __device__ __constant__ unsigned long long "
        "ds4_iq1s_grid[2048]={";
    for (const auto entry : kIq1sGrid) {
        iq1_grid += std::to_string(entry);
        iq1_grid += "ULL,";
    }
    iq1_grid += "};\n";
    const std::string source =
        std::string(flyweight::v2::qwen_cuda_source) + flyweight::v2::qwen_native_cuda_source +
        iq1_grid + deepseek4_cuda_source;
    if (flyweight_gpu_compile(source.c_str(), options.data(),
                            static_cast<std::int32_t>(options.size()), device,
                            log.data(), static_cast<std::int32_t>(log.size())) != 0)
        throw std::runtime_error(std::string("failed to compile CUDA kernels: ") + log.data());
    built = true;
}

// Dispatch a device matvec on the weight's own type. The dense half of this
// checkpoint uses four of these; anything else is a placement mistake caught
// here rather than a wrong answer later.
int ds4_gpu_matvec(std::uint32_t type, std::uint64_t weights, std::uint64_t input,
                   std::uint64_t output, std::int32_t inputs, std::int32_t outputs,
                   std::uint64_t stream) {
    switch (type) {
        case 19: {  // IQ1_S
            void* arguments[] = {&weights, &input, &output, &inputs, &outputs};
            const auto blocks = static_cast<std::uint32_t>((outputs + 7) / 8);
            return flyweight_gpu_launch_named("ds4_iq1s_matvec", blocks, 1, 256, 0,
                                            stream, arguments);
        }
        case 8: {  // Q8_0
            // Four blocks in flight per warp against the shared kernel's one.
            void* arguments[] = {&weights, &input, &output, &inputs, &outputs};
            const auto blocks = static_cast<std::uint32_t>((outputs + 7) / 8);
            return flyweight_gpu_launch_named("ds4_q8_matvec", blocks, 1, 256, 0,
                                            stream, arguments);
        }
        case 14: {  // Q6_K
            void* arguments[] = {&weights, &input, &output, &inputs, &outputs};
            const auto blocks = static_cast<std::uint32_t>((outputs + 7) / 8);
            return flyweight_gpu_launch_named("ds4_q6k_matvec", blocks, 1, 256, 0,
                                            stream, arguments);
        }
        case 30:  // BF16
            return flyweight_gpu_bf16_matvec_transposed(weights, input, output, inputs, outputs, stream);
        default:
            return qwen_gpu_matvec_by_type(
                type, weights, input, output, inputs, outputs, stream);
    }
}

// Hint that a weight range is about to be read.
//
// On the mapping rather than the file, because a split checkpoint backs
// different tensors with different descriptors and the address is the one thing
// that identifies a range whichever shard it came from.
void ds4_prefetch(const std::uint8_t* address, std::uint64_t bytes) {
    if (!address || !bytes) return;
#if defined(_WIN32)
    // PrefetchVirtualMemory (Win 8.1+) pages in the range without blocking.
    static const DWORD_PTR page_mask = [] {
        SYSTEM_INFO si;
        GetSystemInfo(&si);
        return static_cast<DWORD_PTR>(si.dwPageSize) - 1;
    }();
    const auto base = reinterpret_cast<DWORD_PTR>(address) & ~page_mask;
    const auto end  = reinterpret_cast<DWORD_PTR>(address) + bytes;
    WIN32_MEMORY_RANGE_ENTRY range;
    range.VirtualAddress  = reinterpret_cast<void*>(base);
    range.NumberOfBytes   = static_cast<SIZE_T>(end - base);
    (void)PrefetchVirtualMemory(GetCurrentProcess(), 1, &range, 0);
#else
    static const std::uintptr_t page =
        static_cast<std::uintptr_t>(sysconf(_SC_PAGESIZE));
    const auto start = reinterpret_cast<std::uintptr_t>(address) & ~(page - 1);
    const auto end_  = reinterpret_cast<std::uintptr_t>(address) + bytes;
    (void)madvise(reinterpret_cast<void*>(start),
                  static_cast<std::size_t>(end_ - start), MADV_WILLNEED);
#endif
}

// A monotonic nanosecond stamp, for attributing a token's time.
std::uint64_t ds4_now() {
    return static_cast<std::uint64_t>(
        std::chrono::duration_cast<std::chrono::nanoseconds>(
            std::chrono::steady_clock::now().time_since_epoch()).count());
}

// Multiply a resolved tensor by a vector. The plan holds indices, so this does
// no lookup -- the difference from flyweight_v2_matvec, which searches by name.
// Several projections of the same vector, in one round trip.
//
// A layer reads its normalized hidden state through six or seven different
// weights -- the query's low-rank half, the key/value latent, both compressors
// and the indexer's -- and each of those was crossing separately: upload the
// same four kilobytes, launch, download, wait. The weights they multiply do not
// move at all, so the crossing was most of what a small projection cost.
//
// Uploading once, launching all of them, and waiting once at the end turns
// six stalls into one. The launches are independent and ordered on a single
// stream, so nothing here changes an answer.
struct Ds4Projection {
    std::uint64_t index;
    float* out;
    std::int32_t outputs;
    bool download = true;
};

void ds4_matvec(FlyweightV2Deepseek4Runtime& rt, std::uint64_t index,
                const float* input, std::int32_t inputs, float* out,
                std::int32_t outputs);

bool ds4_matvec_batch(FlyweightV2Deepseek4Runtime& rt, const Ds4Projection* batch,
                      std::size_t count, const float* input, std::int32_t inputs) {
    // Off by an environment switch so the two shapes can be compared in one
    // process: the machine's page cache and this laptop's clocks drift enough
    // between runs to swamp the difference otherwise.
    static const bool batching = [] {
        const char* setting = std::getenv("FLYWEIGHT_DS4_BATCH");
        return !(setting && std::string(setting) == "off");
    }();
    bool device = rt.gpu && batching;
    std::uint64_t total = 0;
    for (std::size_t item = 0; device && item < count; ++item) {
        if (rt.resident.find(batch[item].index) == rt.resident.end()) device = false;
        total += static_cast<std::uint64_t>(batch[item].outputs) * sizeof(float);
    }
    const auto input_bytes = static_cast<std::uint64_t>(inputs) * sizeof(float);
    if (!device || input_bytes > rt.gpu_input_capacity || total > rt.gpu_batch_capacity) {
        for (std::size_t item = 0; item < count; ++item)
            ds4_matvec(rt, batch[item].index, input, inputs, batch[item].out,
                       batch[item].outputs);
        return false;
    }
    const auto batch_started = ds4_now();
    int status = flyweight_gpu_upload(rt.gpu_input, input, input_bytes, 0);
    std::uint64_t offset = 0;
    for (std::size_t item = 0; item < count && status == 0; ++item) {
        const auto& tensor = rt.model->tensors[batch[item].index];
        status = ds4_gpu_matvec(tensor.type, rt.resident[batch[item].index], rt.gpu_input,
                                rt.gpu_batch + offset, inputs, batch[item].outputs, 0);
        offset += static_cast<std::uint64_t>(batch[item].outputs) * sizeof(float);
    }
    offset = 0;
    for (std::size_t item = 0; item < count && status == 0; ++item) {
        const auto bytes = static_cast<std::uint64_t>(batch[item].outputs) * sizeof(float);
        if (batch[item].download)
            status = flyweight_gpu_download(batch[item].out, rt.gpu_batch + offset, bytes, 0);
        offset += bytes;
    }
    if (status == 0) status = flyweight_gpu_sync();
    if (status != 0) throw std::runtime_error("a batched device matvec failed");
    rt.matvec_nanoseconds += ds4_now() - batch_started;
    rt.gpu_matvec_calls += count;
    ++rt.gpu_batches;
    return true;
}

void ds4_cached_experts(
    FlyweightV2Deepseek4Runtime& rt, std::uint32_t layer_index,
    const Deepseek4LayerPlan& plan, Deepseek4Scratch& sc
) {
    std::fill(sc.gpu_experts.begin(),sc.gpu_experts.end(),0);
    auto* cache=rt.gpu_cache_owner?rt.gpu_cache_owner:&rt;
    if(!rt.gpu||!cache->expert_cache||!cache->expert_slots_per_layer)return;
    const auto& model=*rt.model;
    const auto& gate=model.tensors[plan.gate_exps];
    const auto& up=model.tensors[plan.up_exps];
    const auto& down=model.tensors[plan.down_exps];
    // The profitable path groups every hit into two launches. IQ2_XXS has only
    // a scalar matvec kernel, and the launch-heavy version benchmarked slower
    // than the fused host loop, so those layers deliberately remain on CPU.
    if(gate.type!=19||up.type!=19||down.type!=18)return;
    const std::uint64_t gate_bytes=gate.size/rt.experts;
    const std::uint64_t up_bytes=up.size/rt.experts;
    const std::uint64_t down_bytes=down.size/rt.experts;
    const auto begin=static_cast<std::size_t>(layer_index)*cache->expert_slots_per_layer;
    const auto end=begin+cache->expert_slots_per_layer;
    std::array<std::uint64_t,8> gate_ptrs{},up_ptrs{},down_ptrs{};
    std::array<float,8> gpu_weights{};
    std::uint32_t gpu_count=0;
    for(std::uint32_t selected=0;selected<rt.experts_used;++selected){
        const auto expert=static_cast<std::uint32_t>(sc.experts[selected]);
        const auto key=(static_cast<std::uint64_t>(layer_index)<<32)|expert;
        auto found=cache->expert_residency.find(key);
        std::size_t slot_index=0;
        if(found!=cache->expert_residency.end()){
            slot_index=found->second;
            ++cache->expert_cache_hits;
            auto& frequency=cache->expert_frequency[key];
            if(frequency!=std::numeric_limits<std::uint32_t>::max())++frequency;
        }else{
            ++cache->expert_cache_misses;
            auto& frequency=cache->expert_frequency[key];
            if(frequency!=std::numeric_limits<std::uint32_t>::max())++frequency;
            // A one-off route is cheaper on the fused CPU path than a PCIe
            // upload. Admit only after recurrence proves there is reuse.
            if(frequency<2)continue;
            auto free=std::find_if(cache->expert_slots.begin()+begin,
                cache->expert_slots.begin()+end,
                [](const Deepseek4ExpertSlot& slot){return !slot.valid;});
            auto victim=free;
            if(victim==cache->expert_slots.begin()+end){
                victim=std::min_element(cache->expert_slots.begin()+begin,
                    cache->expert_slots.begin()+end,
                    [&](const Deepseek4ExpertSlot& a,const Deepseek4ExpertSlot& b){
                        const auto af=cache->expert_frequency[a.key];
                        const auto bf=cache->expert_frequency[b.key];
                        return af!=bf?af<bf:a.last_used<b.last_used;
                    });
                if(frequency<=cache->expert_frequency[victim->key])continue;
                cache->expert_residency.erase(victim->key);
                ++cache->expert_cache_evictions;
            }
            slot_index=static_cast<std::size_t>(victim-cache->expert_slots.begin());
            const auto base=cache->expert_cache+slot_index*cache->expert_slot_bytes;
            std::uint64_t offset=0;
            for(const auto role:{plan.gate_exps,plan.up_exps,plan.down_exps}){
                const auto& tensor=model.tensors[role];
                const auto bytes=tensor.size/rt.experts;
                if(flyweight_gpu_upload(base+offset,tensor_data(model,tensor)+
                        static_cast<std::uint64_t>(expert)*bytes,bytes,0)!=0)
                    throw std::runtime_error("DeepSeek expert-cache upload failed");
                offset+=bytes;
            }
            victim->key=key; victim->valid=true;
            cache->expert_residency[key]=slot_index;
            // The admission upload is allowed to finish alongside this token's
            // CPU expert calculation; it becomes a GPU hit next time.
            victim->last_used=++cache->expert_cache_clock;
            continue;
        }
        auto& slot=cache->expert_slots[slot_index];
        slot.last_used=++cache->expert_cache_clock;
        const auto base=cache->expert_cache+slot_index*cache->expert_slot_bytes;
        gate_ptrs[gpu_count]=base;
        up_ptrs[gpu_count]=base+gate_bytes;
        down_ptrs[gpu_count]=base+gate_bytes+up_bytes;
        gpu_weights[gpu_count]=sc.weights[selected];
        sc.gpu_experts[selected]=1;
        ++gpu_count;
    }
    if(!gpu_count)return;
    struct PointerBundle {
        std::uint64_t gate[8],up[8],down[8];
        float weights[8];
    } bundle{};
    std::copy_n(gate_ptrs.begin(),gpu_count,bundle.gate);
    std::copy_n(up_ptrs.begin(),gpu_count,bundle.up);
    std::copy_n(down_ptrs.begin(),gpu_count,bundle.down);
    std::copy_n(gpu_weights.begin(),gpu_count,bundle.weights);
    std::uint64_t pointer_buffer=rt.gpu_batch;
    std::uint64_t gate_device=pointer_buffer;
    std::uint64_t up_device=gate_device+sizeof(bundle.gate);
    std::uint64_t down_device=up_device+sizeof(bundle.up);
    std::uint64_t weights_device=down_device+sizeof(bundle.down);
    std::uint64_t aggregate=weights_device+sizeof(bundle.weights);
    int input_size=static_cast<int>(rt.n_embd);
    int intermediate=static_cast<int>(rt.expert_ffn);
    int output_size=static_cast<int>(rt.n_embd);
    int count=static_cast<int>(gpu_count);
    int status=flyweight_gpu_upload(rt.gpu_input,sc.hidden.data(),
        static_cast<std::uint64_t>(rt.n_embd)*sizeof(float),0);
    if(status==0)status=flyweight_gpu_upload(pointer_buffer,&bundle,sizeof(bundle),0);
    if(status==0)status=flyweight_gpu_memset(
        aggregate,0,static_cast<std::uint64_t>(rt.n_embd)*sizeof(float),0);
    void* gate_args[]={&gate_device,&up_device,&rt.gpu_input,&rt.gpu_output,
        &input_size,&intermediate,&count,&rt.clamp};
    if(status==0)status=flyweight_gpu_launch_named("ds4_iq1s_grouped_swiglu",
        rt.expert_ffn,gpu_count,256,0,0,gate_args);
    void* down_args[]={&down_device,&rt.gpu_output,&aggregate,&weights_device,
        &intermediate,&output_size,&count};
    if(status==0)status=flyweight_gpu_launch_named("iq3xxs_grouped_accumulate",
        rt.n_embd,1,256,0,0,down_args);
    if(status==0)status=flyweight_gpu_download(sc.expert_out.data(),aggregate,
        static_cast<std::uint64_t>(rt.n_embd)*sizeof(float),0);
    if(status==0)status=flyweight_gpu_sync();
    if(status!=0)throw std::runtime_error("DeepSeek grouped cached experts failed");
    for(std::uint32_t i=0;i<rt.n_embd;++i)sc.moe[i]+=sc.expert_out[i];
    rt.gpu_matvec_calls+=3*gpu_count;
}

void ds4_quant_dot_many(
    const std::uint8_t* packed, std::uint32_t type,
    const std::vector<const float*>& vectors, int begin, int count,
    int elements, std::uint64_t row, float* output
) {
    if(count==8){
        const float* inputs[8]={vectors[begin],vectors[begin+1],vectors[begin+2],
            vectors[begin+3],vectors[begin+4],vectors[begin+5],vectors[begin+6],
            vectors[begin+7]};
        qwen_quant_dot_oct(packed,type,inputs,elements,row,output);
        return;
    }
    int offset=0;
    for(;offset+4<=count;offset+=4){
        const float* inputs[4]={vectors[begin+offset],vectors[begin+offset+1],
            vectors[begin+offset+2],vectors[begin+offset+3]};
        qwen_quant_dot_quad(packed,type,inputs,elements,row,output+offset);
    }
    if(offset+2<=count){
        qwen_quant_dot_pair(packed,type,vectors[begin+offset],vectors[begin+offset+1],
            elements,row,output[offset],output[offset+1]);
        offset+=2;
    }
    if(offset<count)
        output[offset]=qwen_quant_dot(packed,type,vectors[begin+offset],elements,row);
}

void ds4_cpu_moe_rows(
    FlyweightV2Deepseek4Runtime& rt, std::uint32_t layer_index,
    std::vector<Deepseek4Scratch>& rows
) {
    const auto& model=*rt.model;
    const auto& plan=rt.layers[layer_index].plan;
    const int row_count=static_cast<int>(rows.size());
    const int top_k=static_cast<int>(rt.experts_used);
    const int total=row_count*top_k;
    std::vector<int> counts(rt.experts,0),offsets(rt.experts+1,0);
    for(int row=0;row<row_count;++row)
        for(int slot=0;slot<top_k;++slot)++counts[rows[row].experts[slot]];
    for(std::uint32_t expert=0;expert<rt.experts;++expert)
        offsets[expert+1]=offsets[expert]+counts[expert];
    std::vector<int> occurrences(total),cursor(offsets.begin(),offsets.end()-1);
    std::vector<const float*> hidden(total),activated(total);
    for(int row=0;row<row_count;++row)for(int slot=0;slot<top_k;++slot){
        const int route=row*top_k+slot;
        const int expert=rows[row].experts[slot];
        const int position=cursor[expert]++;
        occurrences[position]=route;
        hidden[position]=rows[row].hidden.data();
        activated[position]=rows[row].routed_activated.data()+
            static_cast<std::size_t>(slot)*rt.expert_ffn;
    }
    std::vector<int> active;
    for(std::uint32_t expert=0;expert<rt.experts;++expert)
        if(counts[expert])active.push_back(static_cast<int>(expert));
    const auto& gate=model.tensors[plan.gate_exps];
    const auto& up=model.tensors[plan.up_exps];
    const auto& down=model.tensors[plan.down_exps];
    const auto gate_span=gate.size/rt.experts,up_span=up.size/rt.experts;
    const auto down_span=down.size/rt.experts;
#pragma omp parallel for schedule(dynamic,16) num_threads(ds4_thread_count())
    for(std::int64_t task=0;task<static_cast<std::int64_t>(active.size())*rt.expert_ffn;++task){
        const int group=static_cast<int>(task/rt.expert_ffn);
        const int output=static_cast<int>(task%rt.expert_ffn);
        const int expert=active[group],begin=offsets[expert],count=counts[expert];
        float gate_values[8]{},up_values[8]{};
        ds4_quant_dot_many(tensor_data(model,gate)+expert*gate_span,gate.type,
            hidden,begin,count,rt.n_embd,output,gate_values);
        ds4_quant_dot_many(tensor_data(model,up)+expert*up_span,up.type,
            hidden,begin,count,rt.n_embd,output,up_values);
        for(int item=0;item<count;++item){
            const int route=occurrences[begin+item];
            const int row=route/top_k,slot=route%top_k;
            const float g=std::clamp(gate_values[item],-rt.clamp,rt.clamp);
            const float u=std::clamp(up_values[item],-rt.clamp,rt.clamp);
            rows[row].routed_activated[static_cast<std::size_t>(slot)*rt.expert_ffn+output]=
                g/(1.0f+std::exp(-g))*u;
        }
    }
#pragma omp parallel for schedule(dynamic,16) num_threads(ds4_thread_count())
    for(std::int64_t task=0;task<static_cast<std::int64_t>(active.size())*rt.n_embd;++task){
        const int group=static_cast<int>(task/rt.n_embd);
        const int output=static_cast<int>(task%rt.n_embd);
        const int expert=active[group],begin=offsets[expert],count=counts[expert];
        float values[8]{};
        ds4_quant_dot_many(tensor_data(model,down)+expert*down_span,down.type,
            activated,begin,count,rt.expert_ffn,output,values);
        for(int item=0;item<count;++item){
            const int route=occurrences[begin+item];
            const int row=route/top_k,slot=route%top_k;
            rows[row].routed_out[static_cast<std::size_t>(slot)*rt.n_embd+output]=values[item];
        }
    }
    for(int row=0;row<row_count;++row){
        auto& sc=rows[row];
        std::fill(sc.moe.begin(),sc.moe.end(),0.0f);
        for(int slot=0;slot<top_k;++slot){
            const float weight=sc.weights[slot];
            const float* output=sc.routed_out.data()+static_cast<std::size_t>(slot)*rt.n_embd;
            for(std::uint32_t item=0;item<rt.n_embd;++item)sc.moe[item]+=output[item]*weight;
        }
    }
}

void ds4_matvec(FlyweightV2Deepseek4Runtime& rt, std::uint64_t index,
                const float* input, std::int32_t inputs, float* out, std::int32_t outputs) {
    const auto& model = *rt.model;
    const auto& tensor = model.tensors[index];
    // On the device when the weights are already there. The activations cross
    // for every call, which is a few kilobytes against the megabytes of weights
    // that do not -- that asymmetry is the whole reason residency pays.
    if (rt.gpu) {
        const auto found = rt.resident.find(index);
        if (found != rt.resident.end()) {
            const auto input_bytes = static_cast<std::uint64_t>(inputs) * sizeof(float);
            const auto output_bytes = static_cast<std::uint64_t>(outputs) * sizeof(float);
            if (input_bytes <= rt.gpu_input_capacity && output_bytes <= rt.gpu_output_capacity) {
                // Asynchronous: the upload, the launch and the download are
                // ordered on one stream, so a single wait at the end covers all
                // three. Waiting on the upload as well doubled the stalls, and
                // at four hundred and seventy calls a token that is the cost
                // that decides whether crossing per call is affordable at all.
                const auto call_started = ds4_now();
                int status = flyweight_gpu_upload(rt.gpu_input, input, input_bytes, 0);
                if (status == 0)
                    status = ds4_gpu_matvec(tensor.type, found->second, rt.gpu_input,
                                            rt.gpu_output, inputs, outputs, 0);
                if (status == 0) status = flyweight_gpu_download(out, rt.gpu_output, output_bytes, 0);
                if (status == 0) status = flyweight_gpu_sync();
                if (status != 0) throw std::runtime_error("a device matvec failed");
                ++rt.gpu_matvec_calls;
                rt.matvec_nanoseconds += ds4_now() - call_started;
                return;
            }
        }
    }
    const auto* packed = tensor_data(model, tensor);
    // Rows are independent, and a token walks about a thousand of these across
    // 43 layers, so the row loop is where the cores go. The test is on the work
    // rather than the row count: the hyper-connection mixer is 24 rows of 16384
    // weights, which a row-count threshold leaves serial despite being a
    // megabyte and a half of reading.
    if (static_cast<std::int64_t>(outputs) * inputs >= (1 << 16)) {
#pragma omp parallel for schedule(static) num_threads(ds4_thread_count())
        for (std::int32_t row = 0; row < outputs; ++row)
            out[row] = qwen_quant_dot(packed, tensor.type, input, inputs,
                                      static_cast<std::uint64_t>(row));
        return;
    }
    for (std::int32_t row = 0; row < outputs; ++row)
        out[row] = qwen_quant_dot(packed, tensor.type, input, inputs,
                                  static_cast<std::uint64_t>(row));
}

void ds4_expert_matvec(const FlyweightV2Model& model, std::uint64_t index, std::int32_t expert,
                       const float* input, std::int32_t inputs, float* out, std::int32_t outputs) {
    const auto& tensor = model.tensors[index];
    const auto* packed = tensor_data(model, tensor);
    const auto base = static_cast<std::uint64_t>(expert) * outputs;
    // The routed experts are most of the arithmetic in a token: seven of them,
    // three matrices each, 43 layers.
#pragma omp parallel for schedule(static) num_threads(ds4_thread_count())
    for (std::int32_t row = 0; row < outputs; ++row)
        out[row] = qwen_quant_dot(packed, tensor.type, input, inputs, base + row);
}

// A resolved f32 tensor's data, for the norms and mixers the loop reads whole.
const float* ds4_f32(const FlyweightV2Model& model, std::uint64_t index) {
    return reinterpret_cast<const float*>(tensor_data(model, model.tensors[index]));
}

// Close one compressed block: pool the rows it covers, normalize, rotate at the
// block's own position, and store it half precision.
//
// The main path and the indexer differ only in width and in which norm they
// read, so they share this. A 4:1 block pools the previous block's rows
// alongside its own, which is why the ring holds twice the ratio and why the
// halves of a row are read separately: the low half belongs to the block a row
// was written for, the high half to the one after it.
void ds4_close_block(const FlyweightV2Deepseek4Runtime& rt, Deepseek4LayerState& layer,
                     std::uint32_t block, std::uint32_t dim, std::uint32_t width,
                     const std::vector<float>& source_values,
                     const std::vector<float>& source_scores,
                     std::uint64_t norm, std::vector<std::uint16_t>& target) {
    namespace ds4 = flyweight::v2::deepseek4;
    const auto& model = *rt.model;
    const bool overlapped = layer.ratio == 4;
    const auto rows = layer.state_rows;
    std::vector<float> pooled_values(static_cast<std::size_t>(rows) * dim, 0.0f);
    std::vector<float> pooled_scores(pooled_values.size(),
                                     -std::numeric_limits<float>::infinity());
    for (std::uint32_t slot_index = 0; slot_index < layer.ratio; ++slot_index) {
        const auto current = block * layer.ratio + slot_index;
        const auto* cv = source_values.data() + static_cast<std::size_t>(current % rows) * width;
        const auto* cs = source_scores.data() + static_cast<std::size_t>(current % rows) * width;
        if (!overlapped) {
            std::copy_n(cv, dim, pooled_values.data() + static_cast<std::size_t>(slot_index) * dim);
            std::copy_n(cs, dim, pooled_scores.data() + static_cast<std::size_t>(slot_index) * dim);
            continue;
        }
        if (block > 0) {
            const auto previous = (block - 1) * layer.ratio + slot_index;
            const auto* pv = source_values.data() + static_cast<std::size_t>(previous % rows) * width;
            const auto* ps = source_scores.data() + static_cast<std::size_t>(previous % rows) * width;
            std::copy_n(pv, dim, pooled_values.data() + static_cast<std::size_t>(slot_index) * dim);
            std::copy_n(ps, dim, pooled_scores.data() + static_cast<std::size_t>(slot_index) * dim);
        }
        std::copy_n(cv + dim, dim,
                    pooled_values.data() + static_cast<std::size_t>(layer.ratio + slot_index) * dim);
        std::copy_n(cs + dim, dim,
                    pooled_scores.data() + static_cast<std::size_t>(layer.ratio + slot_index) * dim);
    }
    std::vector<float> pooled(dim, 0.0f);
    ds4::compress_block(pooled_values.data(), pooled_scores.data(), rows, dim, pooled.data());
    ds4::rms_norm(pooled.data(), dim, rt.epsilon, pooled.data());
    {
        const float* gain = ds4_f32(model, norm);
        for (std::uint32_t i = 0; i < dim; ++i) pooled[i] *= gain[i];
    }
    ds4::rope(pooled.data() + (dim - rt.rope_dim), rt.rope_dim,
              static_cast<std::int32_t>(block), rt.compress_base, rt.freq_scale, false,
              {1.0f, rt.attn_factor, rt.beta_fast, rt.beta_slow, rt.original_context});
    std::uint16_t* slot = target.data() + static_cast<std::size_t>(block) * dim;
    for (std::uint32_t i = 0; i < dim; ++i) slot[i] = ds4::half_bits(pooled[i]);
}

// Run one block over one position, reading and updating its cache.
void ds4_layer_attention_prepare(FlyweightV2Deepseek4Runtime& rt, std::uint32_t index,
                                 std::uint32_t position, std::uint32_t token,
                                 const float* streams, float* out_streams,
                                 Deepseek4Scratch& sc) {
    namespace ds4 = flyweight::v2::deepseek4;
    auto& layer = rt.layers[index];
    const auto& plan = layer.plan;
    const auto& model = *rt.model;
    const auto n_embd = rt.n_embd, hc = rt.hc, head_dim = rt.head_dim;
    const auto wide = static_cast<std::int32_t>(rt.heads) * head_dim;
    const auto attention_started = ds4_now();

    const bool compressed_rope = layer.ratio != 0;
    ds4::YarnParameters yarn{};
    float base = rt.freq_base, scale = 1.0f;
    if (compressed_rope) {
        base = rt.compress_base; scale = rt.freq_scale;
        yarn = {1.0f, rt.attn_factor, rt.beta_fast, rt.beta_slow, rt.original_context};
    }

    // Attention side of the hyper-connection.
    const auto hyper_started = ds4_now();
    ds4::hyper_connection_weights(
        streams, ds4_f32(model, plan.hc_attn_fn), ds4_f32(model, plan.hc_attn_scale),
        ds4_f32(model, plan.hc_attn_base), n_embd, hc, rt.sinkhorn_iterations,
        rt.epsilon, rt.epsilon, sc.pre.data(), sc.post.data(), sc.comb.data());
    ds4::hyper_connection_collapse(streams, sc.pre.data(), n_embd, hc, sc.collapsed.data());
    ds4::rms_norm(sc.collapsed.data(), n_embd, rt.epsilon, sc.hidden.data());
    {
        const float* gain = ds4_f32(model, plan.attn_norm);
        for (std::uint32_t i = 0; i < n_embd; ++i) sc.hidden[i] *= gain[i];
    }
    rt.hyper_nanoseconds += ds4_now() - hyper_started;

    // Everything a layer projects from the normalized hidden state, issued
    // together: the crossing is per round trip, not per weight, so six small
    // projections that shared an input were paying six stalls for one upload's
    // worth of data.
    bool query_chained=false;
    {
        Ds4Projection batch[6];
        std::size_t count = 0;
        static const bool query_chain_enabled=[] {
            const char* setting=std::getenv("FLYWEIGHT_DS4_QUERY_CHAIN");
            return !(setting&&std::string(setting)=="off");
        }();
        const bool chain_query = query_chain_enabled && rt.gpu &&
            rt.resident.find(plan.q_a_norm) != rt.resident.end() &&
            rt.resident.find(plan.q_b) != rt.resident.end();
        batch[count++] = {plan.q_a, sc.low_rank.data(),
                          static_cast<std::int32_t>(rt.q_lora), !chain_query};
        batch[count++] = {plan.kv, sc.latent.data(), static_cast<std::int32_t>(head_dim)};
        if (layer.ratio) {
            const auto slot = position % layer.state_rows;
            batch[count++] = {plan.comp_kv,
                layer.state_values.data() + static_cast<std::size_t>(slot) * layer.state_width,
                static_cast<std::int32_t>(layer.state_width)};
            batch[count++] = {plan.comp_gate,
                layer.state_scores.data() + static_cast<std::size_t>(slot) * layer.state_width,
                static_cast<std::int32_t>(layer.state_width)};
        }
        if (layer.indexer_dim) {
            const auto slot = position % layer.state_rows;
            batch[count++] = {plan.indexer_comp_kv,
                layer.indexer_state_values.data() + static_cast<std::size_t>(slot) * layer.indexer_width,
                static_cast<std::int32_t>(layer.indexer_width)};
            batch[count++] = {plan.indexer_comp_gate,
                layer.indexer_state_scores.data() + static_cast<std::size_t>(slot) * layer.indexer_width,
                static_cast<std::int32_t>(layer.indexer_width)};
        }
        const bool device_batch=ds4_matvec_batch(rt, batch, count, sc.hidden.data(), n_embd);
        if(chain_query&&device_batch){
            // q_a is the first batched output. Keep it resident, normalize it
            // into the input workspace, and feed q_b without a download/upload
            // pair. The final query alone returns to the CPU attention path.
            int status=flyweight_gpu_rms_norm(rt.gpu_batch,rt.resident[plan.q_a_norm],
                rt.gpu_input,static_cast<std::int32_t>(rt.q_lora),rt.epsilon,0);
            const auto& q_b=model.tensors[plan.q_b];
            if(status==0)status=ds4_gpu_matvec(q_b.type,rt.resident[plan.q_b],
                rt.gpu_input,rt.gpu_output,static_cast<std::int32_t>(rt.q_lora),wide,0);
            if(status==0)status=flyweight_gpu_download(sc.query.data(),rt.gpu_output,
                static_cast<std::uint64_t>(wide)*sizeof(float),0);
            if(status==0)status=flyweight_gpu_sync();
            if(status!=0)throw std::runtime_error("the resident query chain failed");
            ++rt.gpu_matvec_calls;
            query_chained=true;
        }
    }

    // Query: low-rank path, per-head norm without gain, then rotate.
    if(!query_chained){
        ds4::rms_norm(sc.low_rank.data(), rt.q_lora, rt.epsilon, sc.low_rank.data());
        const float* gain = ds4_f32(model, plan.q_a_norm);
        for (std::uint32_t i = 0; i < rt.q_lora; ++i) sc.low_rank[i] *= gain[i];
        ds4_matvec(rt, plan.q_b, sc.low_rank.data(), rt.q_lora, sc.query.data(), wide);
    }
    for (std::uint32_t head = 0; head < rt.heads; ++head) {
        float* row = sc.query.data() + static_cast<std::size_t>(head) * head_dim;
        ds4::rms_norm(row, head_dim, rt.epsilon, row);
        ds4::rope(row + (head_dim - rt.rope_dim), rt.rope_dim, static_cast<std::int32_t>(position),
                  base, scale, false, yarn);
    }

    // Key and value are the same latent, stored half precision. Its projection
    // went out with the batch above.
    ds4::rms_norm(sc.latent.data(), head_dim, rt.epsilon, sc.latent.data());
    {
        const float* gain = ds4_f32(model, plan.kv_a_norm);
        for (std::uint32_t i = 0; i < head_dim; ++i) sc.latent[i] *= gain[i];
    }
    ds4::rope(sc.latent.data() + (head_dim - rt.rope_dim), rt.rope_dim,
              static_cast<std::int32_t>(position), base, scale, false, yarn);
    {
        std::uint16_t* slot = layer.latents.data() +
            static_cast<std::size_t>(position % layer.window) * head_dim;
        for (std::uint32_t i = 0; i < head_dim; ++i) slot[i] = ds4::half_bits(sc.latent[i]);
    }

    // The indexer's compressor, kept on every 4:1 layer whatever the length.
    //
    // The cache has to be built from the first token even though nothing reads
    // it until there are more blocks than the indexer may select: a block's
    // rows are projected from a hidden state that is gone by the next step, so
    // there is no way to fill this in later.
    if (layer.indexer_dim) {
        const auto width = layer.indexer_width;
        const auto slot = position % layer.state_rows;
        float* scores = layer.indexer_state_scores.data() + static_cast<std::size_t>(slot) * width;
        // The two projections were issued with the rest of the layer's above.
        const float* ape = ds4_f32(model, plan.indexer_comp_ape) +
            static_cast<std::size_t>(position % layer.ratio) * width;
        for (std::uint32_t i = 0; i < width; ++i) scores[i] += ape[i];
        if ((position + 1) % layer.ratio == 0 && layer.blocks < layer.block_capacity) {
            layer.indexer_compressed.resize(
                (static_cast<std::size_t>(layer.blocks)+1)*layer.indexer_dim);
            ds4_close_block(rt, layer, layer.blocks, layer.indexer_dim, width,
                            layer.indexer_state_values, layer.indexer_state_scores,
                            plan.indexer_comp_norm, layer.indexer_compressed);
        }
    }

    // Compressor state, closing a block when it fills.
    if (layer.ratio) {
        const auto width = layer.state_width;
        const auto slot = position % layer.state_rows;
        float* scores = layer.state_scores.data() + static_cast<std::size_t>(slot) * width;
        const float* ape = ds4_f32(model, plan.comp_ape) +
            static_cast<std::size_t>(position % layer.ratio) * width;
        for (std::uint32_t i = 0; i < width; ++i) scores[i] += ape[i];
        if ((position + 1) % layer.ratio == 0 && layer.blocks < layer.block_capacity) {
            layer.compressed.resize(
                (static_cast<std::size_t>(layer.blocks)+1)*head_dim);
            ds4_close_block(rt, layer, layer.blocks, head_dim, width,
                            layer.state_values, layer.state_scores,
                            plan.comp_norm, layer.compressed);
            ++layer.blocks;
        }
    }

    // The lightning indexer: which of the compressed blocks this position is
    // allowed to see.
    //
    // Only run once there are more visible blocks than it may select. Below
    // that the top-k is everything and the mask it would build is the mask
    // already in force, so skipping is exact rather than an approximation --
    // and it saves reading the query projection, which is the expensive half.
    std::uint32_t visible_blocks = 0;
    for (std::uint32_t block = 0; block < layer.blocks; ++block) {
        if (block * layer.ratio + layer.ratio - 1 > position) break;
        ++visible_blocks;
    }
    const bool selecting = layer.indexer_dim && rt.indexer_top_k &&
                           visible_blocks > rt.indexer_top_k;
    if (selecting) {
        const auto indexer_dim = layer.indexer_dim;
        ds4_matvec(rt, plan.indexer_q_b, sc.low_rank.data(),
                   static_cast<std::int32_t>(rt.q_lora), sc.indexer_query.data(),
                   static_cast<std::int32_t>(rt.indexer_heads * indexer_dim));
        for (std::uint32_t head = 0; head < rt.indexer_heads; ++head) {
            float* row = sc.indexer_query.data() + static_cast<std::size_t>(head) * indexer_dim;
            ds4::rope(row + (indexer_dim - rt.rope_dim), rt.rope_dim,
                      static_cast<std::int32_t>(position), base, scale, false, yarn);
        }
        ds4_matvec(rt, plan.indexer_proj, sc.hidden.data(), n_embd,
                   sc.indexer_weights.data(), static_cast<std::int32_t>(rt.indexer_heads));
        // The reference scales the weights rather than the scores; with the
        // rectifier in between the two are not the same thing.
        const float scale_weights =
            1.0f / std::sqrt(static_cast<float>(indexer_dim * rt.indexer_heads));
        for (std::uint32_t head = 0; head < rt.indexer_heads; ++head)
            sc.indexer_weights[head] *= scale_weights;
        // Score directly from the half cache. Expanding every candidate into a
        // context-sized float scratch buffer added another ~128 MiB at 1M.
#pragma omp parallel for schedule(static) num_threads(ds4_thread_count())
        for (std::int64_t block = 0; block < static_cast<std::int64_t>(visible_blocks); ++block) {
            const std::uint16_t* key = layer.indexer_compressed.data() +
                static_cast<std::size_t>(block) * indexer_dim;
            double total = 0.0;
            for (std::uint32_t head = 0; head < rt.indexer_heads; ++head) {
                const float* query = sc.indexer_query.data() +
                    static_cast<std::size_t>(head) * indexer_dim;
                double dot = 0.0;
                for (std::uint32_t i = 0; i < indexer_dim; ++i)
                    dot += static_cast<double>(query[i]) * ds4::half_value(key[i]);
                if (dot > 0.0) total += dot * sc.indexer_weights[head];
            }
            sc.indexer_scores[static_cast<std::size_t>(block)] = static_cast<float>(total);
        }
        ds4::top_k_select(sc.indexer_scores.data(), visible_blocks, rt.indexer_top_k,
                          sc.indexer_keep.data());
        rt.indexer_selections += rt.indexer_top_k;
        rt.indexer_candidates += visible_blocks;
    }

    // Gather the keys this position may see, raw window then compressed blocks.
    const auto core_started = ds4_now();
    std::uint32_t keys = 0;
    const std::uint32_t first = position + 1 > layer.window ? position + 1 - layer.window : 0;
    for (std::uint32_t p = first; p <= position; ++p) {
        const std::uint16_t* slot = layer.latents.data() +
            static_cast<std::size_t>(p % layer.window) * head_dim;
        float* target = sc.keys.data() + static_cast<std::size_t>(keys) * head_dim;
        for (std::uint32_t i = 0; i < head_dim; ++i) target[i] = ds4::half_value(slot[i]);
        ++keys;
    }
    for (std::uint32_t block = 0; block < visible_blocks; ++block) {
        if (selecting && !sc.indexer_keep[block]) continue;
        const std::uint16_t* slot = layer.compressed.data() +
            static_cast<std::size_t>(block) * head_dim;
        float* target = sc.keys.data() + static_cast<std::size_t>(keys) * head_dim;
        for (std::uint32_t i = 0; i < head_dim; ++i) target[i] = ds4::half_value(slot[i]);
        ++keys;
    }

    ds4::attention_with_sinks(sc.query.data(), sc.keys.data(), ds4_f32(model, plan.sinks),
                              nullptr, rt.heads, head_dim, keys,
                              1.0f / std::sqrt(static_cast<float>(head_dim)), sc.attn.data());
    rt.attention_core_nanoseconds += ds4_now() - core_started;

    // Undo the rotation, then the grouped output projection.
    std::copy(sc.attn.begin(), sc.attn.end(), sc.derope.begin());
    for (std::uint32_t head = 0; head < rt.heads; ++head)
        ds4::rope(sc.derope.data() + static_cast<std::size_t>(head) * head_dim + (head_dim - rt.rope_dim),
                  rt.rope_dim, static_cast<std::int32_t>(position), base, scale, true, yarn);
    {
        const auto inputs = static_cast<std::int32_t>(wide / rt.groups);
        const auto& tensor = model.tensors[plan.output_a];
        const auto resident = rt.gpu ? rt.resident.find(plan.output_a) : rt.resident.end();
        const auto grouped_input_bytes = static_cast<std::uint64_t>(wide) * sizeof(float);
        const auto grouped_output_bytes =
            static_cast<std::uint64_t>(rt.groups * rt.lora_rank) * sizeof(float);
        if (rt.gpu && resident != rt.resident.end() &&
            grouped_input_bytes <= rt.gpu_input_capacity &&
            grouped_output_bytes <= rt.gpu_output_capacity) {
            const auto rows = static_cast<std::int32_t>(rt.groups * rt.lora_rank);
            const auto group_rows = static_cast<std::int32_t>(rt.lora_rank);
            const auto input_bytes = grouped_input_bytes;
            const auto output_bytes = grouped_output_bytes;
            std::uint64_t weights = resident->second;
            std::uint64_t in = rt.gpu_input, out_buffer = rt.gpu_output;
            std::int32_t inputs_arg = inputs, rows_arg = rows, group_arg = group_rows;
            void* arguments[] = {&weights, &in, &out_buffer, &inputs_arg, &rows_arg, &group_arg};
            int status = flyweight_gpu_upload(rt.gpu_input, sc.derope.data(), input_bytes, 0);
            if (status == 0)
                status = flyweight_gpu_launch_named("ds4_q8_grouped_matvec",
                    static_cast<std::uint32_t>((rows + 7) / 8), 1, 256, 0, 0, arguments);
            if (status == 0)
                status = flyweight_gpu_download(sc.grouped.data(), rt.gpu_output, output_bytes, 0);
            if (status == 0) status = flyweight_gpu_sync();
            if (status != 0) throw std::runtime_error("the grouped device matvec failed");
            ++rt.gpu_matvec_calls;
            goto grouped_done;
        }
        {
        const auto* packed = tensor_data(model, tensor);
        // Every group's rows are independent, so this is one flat loop over
        // them rather than two nested ones -- eight groups would otherwise
        // fork eight times, and at 34 MiB a layer this is the largest single
        // read in the attention half.
        const auto rows = static_cast<std::int32_t>(rt.groups * rt.lora_rank);
#pragma omp parallel for schedule(static) num_threads(ds4_thread_count())
        for (std::int32_t row = 0; row < rows; ++row) {
            const auto group = static_cast<std::size_t>(row) / rt.lora_rank;
            sc.grouped[static_cast<std::size_t>(row)] = qwen_quant_dot(
                packed, tensor.type, sc.derope.data() + group * inputs, inputs,
                static_cast<std::uint64_t>(row));
        }
        }
        grouped_done:;
    }
    ds4_matvec(rt, plan.output_b, sc.grouped.data(),
               static_cast<std::int32_t>(rt.groups * rt.lora_rank), sc.block_out.data(), n_embd);

    ds4::hyper_connection_combine(sc.block_out.data(), streams, sc.post.data(), sc.comb.data(),
                                  n_embd, hc, out_streams);
    rt.attention_nanoseconds += ds4_now() - attention_started;

    // Feed-forward side.
    const auto hyper_ffn_started = ds4_now();
    ds4::hyper_connection_weights(
        out_streams, ds4_f32(model, plan.hc_ffn_fn), ds4_f32(model, plan.hc_ffn_scale),
        ds4_f32(model, plan.hc_ffn_base), n_embd, hc, rt.sinkhorn_iterations,
        rt.epsilon, rt.epsilon, sc.pre.data(), sc.post.data(), sc.comb.data());
    ds4::hyper_connection_collapse(out_streams, sc.pre.data(), n_embd, hc, sc.collapsed.data());
    ds4::rms_norm(sc.collapsed.data(), n_embd, rt.epsilon, sc.hidden.data());
    {
        const float* gain = ds4_f32(model, plan.ffn_norm);
        for (std::uint32_t i = 0; i < n_embd; ++i) sc.hidden[i] *= gain[i];
    }

    rt.hyper_nanoseconds += ds4_now() - hyper_ffn_started;
    std::fill(sc.router.begin(),sc.router.end(),0.0f);
    ds4_matvec(rt, plan.gate_inp, sc.hidden.data(), n_embd, sc.router.data(), rt.experts);
    const bool hashed = index < rt.hash_layers;
    if (hashed) {
        const auto* table = reinterpret_cast<const std::int32_t*>(
            tensor_data(model, model.tensors[plan.tid2eid]));
        std::copy_n(table + static_cast<std::size_t>(token) * rt.experts_used,
                    rt.experts_used, sc.experts.begin());
    }
    ds4::moe_router(sc.router.data(), hashed ? nullptr : ds4_f32(model, plan.exp_probs_b),
                    rt.experts, rt.experts_used, rt.weight_scale, 1e-20f, !hashed,
                    sc.experts.data(), sc.weights.data());

}

void ds4_layer_ffn_complete(FlyweightV2Deepseek4Runtime& rt, std::uint32_t index,
                            float* out_streams, Deepseek4Scratch& sc,
                            bool routed_ready=false) {
    namespace ds4 = flyweight::v2::deepseek4;
    const auto& layer = rt.layers[index];
    const auto& plan = layer.plan;
    const auto& model = *rt.model;
    const auto n_embd = rt.n_embd, hc = rt.hc;

    if(!routed_ready){
    std::fill(sc.moe.begin(), sc.moe.end(), 0.0f);
    // Ask for every routed expert's weights before touching any of them.
    //
    // The six are known the moment the router runs, but the loop below reads
    // them one matrix at a time, so a miss stalls on each in turn. Hinting all
    // eighteen ranges up front lets the kernel fetch the later ones while the
    // first is being multiplied. It costs nothing when they are already
    // resident, which measurement says they usually are -- this is for the
    // regime where they are not.
    if (rt.expert_prefetch) {
        for (std::uint32_t slot = 0; slot < rt.experts_used; ++slot) {
            for (const auto matrix : {plan.gate_exps, plan.up_exps, plan.down_exps}) {
                const auto& tensor = model.tensors[matrix];
                const auto span = tensor.size / rt.experts;
                ds4_prefetch(tensor_data(model, tensor) +
                                 static_cast<std::uint64_t>(sc.experts[slot]) * span,
                             span);
                rt.expert_prefetch_bytes += span;
            }
        }
    }
    const auto experts_started = ds4_now();
    ds4_cached_experts(rt,index,plan,sc);
    static const bool expert_batch_enabled=[] {
        const char* setting=std::getenv("FLYWEIGHT_DS4_EXPERT_BATCH");
        return !(setting&&std::string(setting)=="off");
    }();
    // One team spans every selected expert instead of entering OpenMP eighteen
    // times per layer. The row arithmetic and later weighted accumulation keep
    // their original order, so this is bit-identical to the serial expert loop.
    if(expert_batch_enabled){
        const auto& gate_tensor=model.tensors[plan.gate_exps];
        const auto& up_tensor=model.tensors[plan.up_exps];
        const auto& down_tensor=model.tensors[plan.down_exps];
        const auto* gate_packed=tensor_data(model,gate_tensor);
        const auto* up_packed=tensor_data(model,up_tensor);
        const auto* down_packed=tensor_data(model,down_tensor);
#pragma omp parallel for schedule(static) num_threads(ds4_thread_count())
        for(std::int64_t item=0;item<static_cast<std::int64_t>(rt.experts_used)*rt.expert_ffn;++item){
            ds4_pin_current_thread();
            const auto slot=static_cast<std::uint32_t>(item/rt.expert_ffn);
            if(sc.gpu_experts[slot])continue;
            const auto row=static_cast<std::uint32_t>(item%rt.expert_ffn);
            const auto absolute=static_cast<std::uint64_t>(sc.experts[slot])*rt.expert_ffn+row;
            sc.routed_gate[static_cast<std::size_t>(item)]=qwen_quant_dot(
                gate_packed,gate_tensor.type,sc.hidden.data(),n_embd,absolute);
            sc.routed_up[static_cast<std::size_t>(item)]=qwen_quant_dot(
                up_packed,up_tensor.type,sc.hidden.data(),n_embd,absolute);
        }
#pragma omp parallel for schedule(static) num_threads(ds4_thread_count())
        for(std::int64_t item=0;item<static_cast<std::int64_t>(rt.experts_used)*rt.expert_ffn;++item){
            ds4_pin_current_thread();
            const auto slot=static_cast<std::uint32_t>(item/rt.expert_ffn);
            if(sc.gpu_experts[slot])continue;
            const float gate=std::clamp(sc.routed_gate[static_cast<std::size_t>(item)],-rt.clamp,rt.clamp);
            const float up=std::clamp(sc.routed_up[static_cast<std::size_t>(item)],-rt.clamp,rt.clamp);
            sc.routed_activated[static_cast<std::size_t>(item)]=gate/(1.0f+std::exp(-gate))*up;
        }
#pragma omp parallel for schedule(static) num_threads(ds4_thread_count())
        for(std::int64_t item=0;item<static_cast<std::int64_t>(rt.experts_used)*n_embd;++item){
            ds4_pin_current_thread();
            const auto slot=static_cast<std::uint32_t>(item/n_embd);
            if(sc.gpu_experts[slot])continue;
            const auto row=static_cast<std::uint32_t>(item%n_embd);
            const auto absolute=static_cast<std::uint64_t>(sc.experts[slot])*n_embd+row;
            sc.routed_out[static_cast<std::size_t>(item)]=qwen_quant_dot(
                down_packed,down_tensor.type,
                sc.routed_activated.data()+static_cast<std::size_t>(slot)*rt.expert_ffn,
                rt.expert_ffn,absolute);
        }
        for (std::uint32_t slot = 0; slot < rt.experts_used; ++slot) {
            if(sc.gpu_experts[slot])continue;
            const float weight = sc.weights[slot];
            const float* expert_out=sc.routed_out.data()+static_cast<std::size_t>(slot)*n_embd;
            for (std::uint32_t i = 0; i < n_embd; ++i) sc.moe[i] += expert_out[i] * weight;
        }
    }else{
        for (std::uint32_t slot = 0; slot < rt.experts_used; ++slot) {
            if(sc.gpu_experts[slot]){
                continue;
            }
            const auto expert = sc.experts[slot];
            ds4_expert_matvec(model, plan.gate_exps, expert, sc.hidden.data(), n_embd,
                              sc.gate.data(), rt.expert_ffn);
            ds4_expert_matvec(model, plan.up_exps, expert, sc.hidden.data(), n_embd,
                              sc.up.data(), rt.expert_ffn);
            ds4::clamped_swiglu(sc.gate.data(), sc.up.data(), rt.expert_ffn, rt.clamp,
                                sc.activated.data());
            ds4_expert_matvec(model, plan.down_exps, expert, sc.activated.data(), rt.expert_ffn,
                              sc.expert_out.data(), n_embd);
            const float weight = sc.weights[slot];
            for (std::uint32_t i = 0; i < n_embd; ++i) sc.moe[i] += sc.expert_out[i] * weight;
        }
    }
    for (std::uint32_t slot = 0; slot < rt.experts_used; ++slot) {
        // One expert's share of the three matrices, which is what a page-in
        // would have to fetch.
        for (const auto matrix : {plan.gate_exps, plan.up_exps, plan.down_exps})
            rt.routed_expert_bytes += model.tensors[matrix].size / rt.experts;
    }
    rt.routed_expert_nanoseconds += ds4_now() - experts_started;
    }
    const auto shared_started = ds4_now();
    ds4_matvec(rt, plan.gate_shexp, sc.hidden.data(), n_embd, sc.gate.data(), rt.expert_ffn);
    ds4_matvec(rt, plan.up_shexp, sc.hidden.data(), n_embd, sc.up.data(), rt.expert_ffn);
    ds4::clamped_swiglu(sc.gate.data(), sc.up.data(), rt.expert_ffn, rt.clamp, sc.activated.data());
    ds4_matvec(rt, plan.down_shexp, sc.activated.data(), rt.expert_ffn,
               sc.expert_out.data(), n_embd);
    for (std::uint32_t i = 0; i < n_embd; ++i) sc.moe[i] += sc.expert_out[i];
    rt.shared_expert_nanoseconds += ds4_now() - shared_started;

    ds4::hyper_connection_combine(sc.moe.data(), out_streams, sc.post.data(), sc.comb.data(),
                                  n_embd, hc, sc.after.data());
    std::copy(sc.after.begin(), sc.after.end(), out_streams);
}

void ds4_layer_forward(FlyweightV2Deepseek4Runtime& rt, std::uint32_t index,
                       std::uint32_t position, std::uint32_t token,
                       const float* streams, float* out_streams) {
    auto& sc=rt.scratch;
    ds4_layer_attention_prepare(rt,index,position,token,streams,out_streams,sc);
    ds4_layer_ffn_complete(rt,index,out_streams,sc);
}

}  // namespace

int flyweight_v2_deepseek4_runtime_info(
    const FlyweightV2Deepseek4Runtime* runtime, FlyweightV2Deepseek4Info* out
){return guarded([&]{
    if(!runtime||!out)throw std::runtime_error("invalid runtime info request");
    std::memset(out,0,sizeof(*out));
    out->layers=static_cast<std::uint32_t>(runtime->layers.size());
    out->context_limit=runtime->context_limit;
    out->state_bytes=runtime->state_bytes();
    out->positions=runtime->layers.empty()?0:runtime->layers.front().positions;
    for(const auto& layer:runtime->layers)out->resolved_tensors+=layer.plan.resolved();
    for(const auto& layer:runtime->layers){
        if(layer.ratio==4)++out->csa_layers;
        else if(layer.ratio)++out->hca_layers;
        else ++out->window_layers;
    }
    out->forward_calls=runtime->forward_calls;
    out->forward_nanoseconds=runtime->forward_nanoseconds;
    out->routed_expert_nanoseconds=runtime->routed_expert_nanoseconds;
    out->shared_expert_nanoseconds=runtime->shared_expert_nanoseconds;
    out->attention_nanoseconds=runtime->attention_nanoseconds;
    out->attention_core_nanoseconds=runtime->attention_core_nanoseconds;
    out->head_nanoseconds=runtime->head_nanoseconds;
    out->routed_expert_bytes=runtime->routed_expert_bytes;
    out->expert_prefetch_bytes=runtime->expert_prefetch_bytes;
    out->gpu_weight_bytes=runtime->gpu_weight_bytes;
    out->gpu_matvec_calls=runtime->gpu_matvec_calls;
    out->gpu_batches=runtime->gpu_batches;
    out->hyper_nanoseconds=runtime->hyper_nanoseconds;
    out->matvec_nanoseconds=runtime->matvec_nanoseconds;
    out->prefill_calls=runtime->prefill_calls;
    out->prefill_tokens=runtime->prefill_tokens;
    out->prefill_nanoseconds=runtime->prefill_nanoseconds;
    const auto* cache=runtime->gpu_cache_owner?runtime->gpu_cache_owner:runtime;
    out->expert_cache_bytes=cache->expert_cache_bytes;
    out->expert_cache_slots=cache->expert_slots.size();
    out->expert_cache_hits=cache->expert_cache_hits;
    out->expert_cache_misses=cache->expert_cache_misses;
    out->expert_cache_evictions=cache->expert_cache_evictions;
    out->indexer_selections=runtime->indexer_selections;
    out->indexer_candidates=runtime->indexer_candidates;
    return 0;});}

// Run one token through every block and the head.
//
// Positions advance with the call, so feeding a prompt is repeated calls and
// decoding is the same call again -- prefill and decode share one path, which
// is what keeps them from disagreeing. `logits` may be null for prompt tokens
// whose distribution nobody wants.
int flyweight_v2_deepseek4_forward(
    FlyweightV2Deepseek4Runtime* runtime, uint32_t token, float* logits
){return guarded([&]{
    if(!runtime)throw std::runtime_error("invalid runtime");
    namespace ds4=flyweight::v2::deepseek4;
    auto& rt=*runtime;
    const auto& model=*rt.model;
    auto& sc=rt.scratch;
    if(token>=rt.vocabulary)throw std::runtime_error("token id is outside the vocabulary");
    const auto forward_started=ds4_now();
    const auto position=rt.layers.empty()?0u:rt.layers.front().positions;
    if(position>=rt.context_limit)throw std::runtime_error("sequence is past the context limit");

    // Every stream starts as a copy of the embedding.
    {
        const auto& tensor=model.tensors[rt.token_embd];
        if(flyweight_v2_qwen_embedding(&model,token,sc.embedding.data(),
               static_cast<std::int32_t>(rt.n_embd))!=0)
            throw std::runtime_error("cannot read the token embedding");
        for(std::uint32_t stream=0;stream<rt.hc;++stream)
            std::copy(sc.embedding.begin(),sc.embedding.end(),
                      sc.streams.begin()+static_cast<std::size_t>(stream)*rt.n_embd);
    }

    for(std::uint32_t index=0;index<rt.layers.size();++index){
        const auto capture=std::find(rt.capture_layers.begin(),rt.capture_layers.end(),index);
        if(capture!=rt.capture_layers.end()){
            const auto slot=static_cast<std::size_t>(capture-rt.capture_layers.begin());
            auto* destination=rt.captured.data()+slot*rt.n_embd;
            const float inverse=1.0f/static_cast<float>(rt.hc);
            for(std::uint32_t column=0;column<rt.n_embd;++column){
                float sum=0.0f;
                for(std::uint32_t stream=0;stream<rt.hc;++stream)
                    sum+=sc.streams[static_cast<std::size_t>(stream)*rt.n_embd+column];
                destination[column]=sum*inverse;
            }
        }
        ds4_layer_forward(rt,index,position,token,sc.streams.data(),sc.next_streams.data());
        std::swap(sc.streams,sc.next_streams);
        ++rt.layers[index].positions;
    }
    const auto final_capture=std::find(
        rt.capture_layers.begin(),rt.capture_layers.end(),
        static_cast<std::uint32_t>(rt.layers.size()));
    if(final_capture!=rt.capture_layers.end()){
        const auto slot=static_cast<std::size_t>(final_capture-rt.capture_layers.begin());
        auto* destination=rt.captured.data()+slot*rt.n_embd;
        const float inverse=1.0f/static_cast<float>(rt.hc);
        for(std::uint32_t column=0;column<rt.n_embd;++column){
            float sum=0.0f;
            for(std::uint32_t stream=0;stream<rt.hc;++stream)
                sum+=sc.streams[static_cast<std::size_t>(stream)*rt.n_embd+column];
            destination[column]=sum*inverse;
        }
    }

    if(logits){
        const auto head_started=ds4_now();
        ds4::hyper_connection_head(sc.streams.data(),ds4_f32(model,rt.head_fn),
            ds4_f32(model,rt.head_scale),ds4_f32(model,rt.head_base),
            rt.n_embd,rt.hc,rt.epsilon,rt.epsilon,sc.head_pre.data(),sc.collapsed.data());
        ds4::rms_norm(sc.collapsed.data(),rt.n_embd,rt.epsilon,sc.hidden.data());
        const float* gain=ds4_f32(model,rt.output_norm);
        for(std::uint32_t i=0;i<rt.n_embd;++i)sc.hidden[i]*=gain[i];
        ds4_matvec(rt,rt.output,sc.hidden.data(),rt.n_embd,logits,
                   static_cast<std::int32_t>(rt.vocabulary));
        rt.head_nanoseconds+=ds4_now()-head_started;
    }
    ++rt.forward_calls;
    rt.forward_nanoseconds+=ds4_now()-forward_started;
    return 0;});}

int flyweight_v2_deepseek4_capture_layers(
    FlyweightV2Deepseek4Runtime* runtime,const uint32_t* layers,uint32_t count
){return guarded([&]{
    if(!runtime||(!layers&&count))throw std::runtime_error("invalid capture-layer arguments");
    std::vector<std::uint32_t> selected;
    if(count)selected.assign(layers,layers+count);
    std::unordered_set<std::uint32_t> unique;
    for(const auto layer:selected){
        if(!unique.insert(layer).second)throw std::runtime_error("capture layers must be unique");
        if(layer>runtime->layers.size())throw std::runtime_error("capture layer is outside the target model");
    }
    runtime->capture_layers=std::move(selected);
    runtime->captured.assign(static_cast<std::size_t>(count)*runtime->n_embd,0.0f);
    return 0;});}

int flyweight_v2_deepseek4_captured(
    const FlyweightV2Deepseek4Runtime* runtime,float* output,uint64_t elements
){return guarded([&]{
    if(!runtime||(!output&&elements))throw std::runtime_error("invalid captured-feature arguments");
    if(elements<runtime->captured.size())throw std::runtime_error("captured-feature output is too small");
    std::copy(runtime->captured.begin(),runtime->captured.end(),output);
    return static_cast<int>(runtime->capture_layers.size());});}

int flyweight_v2_deepseek4_lm_head(FlyweightV2Deepseek4Runtime*r,const float*hidden,
    uint32_t rows,float*logits,uint64_t elements){return guarded([&]{
    if(!r||!hidden||!logits||!rows||r->output==Deepseek4LayerPlan::kAbsent)
        throw std::runtime_error("invalid DeepSeek LM-head arguments");
    const auto needed=static_cast<std::uint64_t>(rows)*r->vocabulary;
    if(elements<needed)throw std::runtime_error("DeepSeek LM-head output is too small");
    for(std::uint32_t row=0;row<rows;++row)
        ds4_matvec(*r,r->output,hidden+static_cast<std::size_t>(row)*r->n_embd,r->n_embd,
            logits+static_cast<std::size_t>(row)*r->vocabulary,r->vocabulary);
    return 0;});}

static int ds4_prefill_impl(
    FlyweightV2Deepseek4Runtime* runtime,const uint32_t* tokens,uint32_t count,float* all_logits,
    float* all_captures
){return guarded([&]{
    if(!runtime||(!tokens&&count))throw std::runtime_error("invalid prefill arguments");
    if(!count)return 0;
    auto& rt=*runtime;
    const auto& model=*rt.model;
    std::uint32_t row_limit=4;
    if(const char* setting=std::getenv("FLYWEIGHT_DS4_PREFILL_ROWS"))
        row_limit=std::max(1u,static_cast<std::uint32_t>(std::strtoul(setting,nullptr,10)));
    row_limit=std::min({row_limit,count,8u});
    const auto first_position=rt.layers.empty()?0u:rt.layers.front().positions;
    if(static_cast<std::uint64_t>(first_position)+count>rt.context_limit)
        throw std::runtime_error("prefill exceeds the DeepSeek context limit");
    for(std::uint32_t index=0;index<count;++index)
        if(tokens[index]>=rt.vocabulary)
            throw std::runtime_error("prefill token id is outside the vocabulary");
    const auto started=ds4_now();
    for(std::uint32_t chunk=0;chunk<count;chunk+=row_limit){
        const auto rows=std::min(row_limit,count-chunk);
        std::vector<Deepseek4Scratch> scratch(rows,rt.scratch);
        for(std::uint32_t row=0;row<rows;++row){
            auto& sc=scratch[row];
            if(flyweight_v2_qwen_embedding(&model,tokens[chunk+row],sc.embedding.data(),
                   static_cast<std::int32_t>(rt.n_embd))!=0)
                throw std::runtime_error("cannot read a prefill token embedding");
            for(std::uint32_t stream=0;stream<rt.hc;++stream)
                std::copy(sc.embedding.begin(),sc.embedding.end(),
                    sc.streams.begin()+static_cast<std::size_t>(stream)*rt.n_embd);
        }
        for(std::uint32_t layer=0;layer<rt.layers.size();++layer){
            const auto position=rt.layers[layer].positions;
            for(std::uint32_t row=0;row<rows;++row){
                auto& sc=scratch[row];
                if(all_captures){
                    const auto found=std::find(rt.capture_layers.begin(),rt.capture_layers.end(),layer);
                    if(found!=rt.capture_layers.end()){
                        const auto slot=static_cast<std::size_t>(found-rt.capture_layers.begin());
                        auto*dst=all_captures+(static_cast<std::size_t>(chunk+row)*rt.capture_layers.size()+slot)*rt.n_embd;
                        for(std::uint32_t column=0;column<rt.n_embd;++column){
                            float sum=0.0f;for(std::uint32_t stream=0;stream<rt.hc;++stream)
                                sum+=sc.streams[static_cast<std::size_t>(stream)*rt.n_embd+column];
                            dst[column]=sum/static_cast<float>(rt.hc);
                        }
                    }
                }
                ds4_layer_attention_prepare(rt,layer,position+row,tokens[chunk+row],
                    sc.streams.data(),sc.next_streams.data(),sc);
            }
            const auto experts_started=ds4_now();
            ds4_cpu_moe_rows(rt,layer,scratch);
            rt.routed_expert_nanoseconds+=ds4_now()-experts_started;
            for(std::uint32_t row=0;row<rows;++row)
                for(std::uint32_t slot=0;slot<rt.experts_used;++slot)
                    for(const auto matrix:{rt.layers[layer].plan.gate_exps,
                                           rt.layers[layer].plan.up_exps,
                                           rt.layers[layer].plan.down_exps})
                        rt.routed_expert_bytes+=model.tensors[matrix].size/rt.experts;
            for(std::uint32_t row=0;row<rows;++row){
                auto& sc=scratch[row];
                ds4_layer_ffn_complete(rt,layer,sc.next_streams.data(),sc,true);
                std::swap(sc.streams,sc.next_streams);
            }
            rt.layers[layer].positions+=rows;
        }
        if(all_captures){
            const auto final=std::find(rt.capture_layers.begin(),rt.capture_layers.end(),
                static_cast<std::uint32_t>(rt.layers.size()));
            if(final!=rt.capture_layers.end())for(std::uint32_t row=0;row<rows;++row){
                const auto slot=static_cast<std::size_t>(final-rt.capture_layers.begin());
                auto*dst=all_captures+(static_cast<std::size_t>(chunk+row)*rt.capture_layers.size()+slot)*rt.n_embd;
                const auto&sc=scratch[row];
                for(std::uint32_t column=0;column<rt.n_embd;++column){
                    float sum=0.0f;for(std::uint32_t stream=0;stream<rt.hc;++stream)
                        sum+=sc.streams[static_cast<std::size_t>(stream)*rt.n_embd+column];
                    dst[column]=sum/static_cast<float>(rt.hc);
                }
            }
        }
        if(all_logits){
            namespace ds4=flyweight::v2::deepseek4;
            for(std::uint32_t row=0;row<rows;++row){
                auto&sc=scratch[row];
                ds4::hyper_connection_head(sc.streams.data(),ds4_f32(model,rt.head_fn),
                    ds4_f32(model,rt.head_scale),ds4_f32(model,rt.head_base),rt.n_embd,rt.hc,
                    rt.epsilon,rt.epsilon,sc.head_pre.data(),sc.collapsed.data());
                ds4::rms_norm(sc.collapsed.data(),rt.n_embd,rt.epsilon,sc.hidden.data());
                const auto*gain=ds4_f32(model,rt.output_norm);
                for(std::uint32_t i=0;i<rt.n_embd;++i)sc.hidden[i]*=gain[i];
                ds4_matvec(rt,rt.output,sc.hidden.data(),rt.n_embd,
                    all_logits+static_cast<std::size_t>(chunk+row)*rt.vocabulary,rt.vocabulary);
            }
        }
        rt.forward_calls+=rows;
    }
    const auto elapsed=ds4_now()-started;
    ++runtime->prefill_calls;
    runtime->prefill_tokens+=count;
    runtime->prefill_nanoseconds+=elapsed;
    runtime->forward_nanoseconds+=elapsed;
    return 0;});}

int flyweight_v2_deepseek4_prefill(FlyweightV2Deepseek4Runtime*r,const uint32_t*t,uint32_t n){
    return ds4_prefill_impl(r,t,n,nullptr,nullptr);
}
int flyweight_v2_deepseek4_forward_batch(FlyweightV2Deepseek4Runtime*r,const uint32_t*t,
    uint32_t n,float*logits,uint64_t elements){
    if(!r||!logits||elements<static_cast<std::uint64_t>(n)*r->vocabulary){
        error="invalid DeepSeek forward-batch output";return -1;
    }
    return ds4_prefill_impl(r,t,n,logits,nullptr);
}
int flyweight_v2_deepseek4_forward_batch_capture(FlyweightV2Deepseek4Runtime*r,const uint32_t*t,
    uint32_t n,float*logits,uint64_t logits_elements,float*captures,uint64_t capture_elements){
    if(!r||!logits||!captures||logits_elements<static_cast<std::uint64_t>(n)*r->vocabulary||
       capture_elements<static_cast<std::uint64_t>(n)*r->capture_layers.size()*r->n_embd){
        error="invalid DeepSeek batch-capture output";return -1;
    }
    return ds4_prefill_impl(r,t,n,logits,captures);
}
int flyweight_v2_deepseek4_snapshot(const FlyweightV2Deepseek4Runtime*r,FlyweightV2Deepseek4Snapshot**out){return guarded([&]{
    if(!r||!out)throw std::runtime_error("invalid DeepSeek snapshot arguments");
    auto snapshot=std::make_unique<FlyweightV2Deepseek4Snapshot>();snapshot->layers=r->layers;
    snapshot->captured=r->captured;*out=snapshot.release();return 0;});}
int flyweight_v2_deepseek4_restore(FlyweightV2Deepseek4Runtime*r,const FlyweightV2Deepseek4Snapshot*s){return guarded([&]{
    if(!r||!s||s->layers.size()!=r->layers.size())throw std::runtime_error("invalid DeepSeek snapshot");
    r->layers=s->layers;r->captured=s->captured;return 0;});}
void flyweight_v2_deepseek4_snapshot_free(FlyweightV2Deepseek4Snapshot*s){try{delete s;}catch(...){}}

// Put one checkpoint tensor through the GPU and report how far the answer
// drifts from the CPU's.
//
// The dense half of this model is 6.9 GiB against 90 GiB of routed experts, and
// it is read in full every token at CPU memory bandwidth -- which is why it is
// the half worth moving. Before any of that is built, this establishes the one
// thing everything else assumes: that the existing quantized matvec kernels,
// written for Qwen, decode a deepseek4 tensor to the same numbers this
// runtime's own kernels do. Both paths see the same input vector, so a
// disagreement here is a decode difference and nothing else.
// Put the dense half of the model on the device and keep it there.
//
// Which half is the whole point: the routed experts are 90 GiB and can never be
// resident, while everything else is under 7 and is read in full on every
// token. The activations that cross for each call are a few kilobytes against
// megabytes of weights that do not move at all, and that asymmetry is what
// makes this pay even before the fiddlier pieces have device kernels.
//
// The grouped output projection is left off for now: it does not go through
// ds4_matvec, so uploading it would spend 1.4 GiB of VRAM on nothing.
int flyweight_v2_deepseek4_runtime_gpu(
    FlyweightV2Deepseek4Runtime* runtime, int32_t device
){return guarded([&]{
    if(!runtime)throw std::runtime_error("invalid runtime");
    auto& rt=*runtime;
    if(rt.gpu)return 0;
    if(flyweight_gpu_init(device)!=0)throw std::runtime_error("failed to initialize CUDA");
    ds4_gpu_compile(device);
    rt.gpu_device=device;

    std::vector<std::uint64_t> wanted;
    auto want=[&](std::uint64_t index){
        if(index!=Deepseek4LayerPlan::kAbsent)wanted.push_back(index);
    };
    for(const auto& layer:rt.layers){
        const auto& plan=layer.plan;
        // Whether the shared expert belongs on the device is a measurement
        // rather than a deduction, and the first attempt at it was taken on a
        // 100 W supply shared with a 16-core CPU. FLYWEIGHT_DS4_SHEXP=cpu keeps
        // it off so the two can be compared with everything else held still.
        static const bool shexp_on_device = [] {
            const char* setting = std::getenv("FLYWEIGHT_DS4_SHEXP");
            return !(setting && std::string(setting) == "cpu");
        }();
        for(const auto index:{plan.q_a,plan.q_a_norm,plan.q_b,plan.kv,plan.output_b,plan.comp_kv,
                              plan.comp_gate,plan.gate_inp,plan.indexer_proj,plan.indexer_q_b,
                              plan.indexer_comp_kv,plan.indexer_comp_gate,plan.output_a})
            want(index);
        if(shexp_on_device)
            for(const auto index:{plan.gate_shexp,plan.up_shexp,plan.down_shexp})
                want(index);
    }
    want(rt.output);
    std::sort(wanted.begin(),wanted.end());
    wanted.erase(std::unique(wanted.begin(),wanted.end()),wanted.end());

    std::uint32_t widest_input=0,widest_output=0;
    std::unordered_map<std::uint64_t,std::uint64_t> resident;
    std::uint64_t weight_bytes=0,input_buffer=0,output_buffer=0,batch_buffer=0;
    std::uint64_t expert_cache=0,expert_cache_bytes=0,expert_slot_bytes=0;
    std::uint32_t expert_slots_per_layer=0;
    // Device allocation has several failure points. Keep ownership local until
    // the complete set is usable so a failed enable neither leaks VRAM nor
    // leaves a half-populated runtime that cannot safely be retried.
    auto release_pending=[&]{
        for(const auto& entry:resident)flyweight_gpu_free(entry.second);
        if(input_buffer)flyweight_gpu_free(input_buffer);
        if(output_buffer)flyweight_gpu_free(output_buffer);
        if(batch_buffer)flyweight_gpu_free(batch_buffer);
        if(expert_cache)flyweight_gpu_free(expert_cache);
        resident.clear(); input_buffer=output_buffer=batch_buffer=0;
        expert_cache=0;
    };
    for(const auto index:wanted){
        const auto& tensor=rt.model->tensors[index];
        // Only what there is a device matvec for; anything else stays on the
        // CPU rather than being uploaded and never used.
        if(tensor.type!=0&&tensor.type!=8&&tensor.type!=14&&tensor.type!=30)continue;
        std::uint64_t pointer=0;
        if(flyweight_gpu_alloc(tensor.size,&pointer)!=0){
            release_pending();
            throw std::runtime_error("out of device memory for the dense weights");
        }
        if(flyweight_gpu_upload_sync(pointer,tensor_data(*rt.model,tensor),tensor.size)!=0){
            flyweight_gpu_free(pointer);
            release_pending();
            throw std::runtime_error("failed to upload a dense weight");
        }
        resident.emplace(index,pointer);
        weight_bytes+=tensor.size;
        const auto rows=tensor.shape.size()>0?tensor.shape[0]:0;
        std::uint64_t columns=1;
        for(std::size_t axis=1;axis<tensor.shape.size();++axis)columns*=tensor.shape[axis];
        widest_input=std::max<std::uint32_t>(widest_input,static_cast<std::uint32_t>(rows));
        widest_output=std::max<std::uint32_t>(widest_output,static_cast<std::uint32_t>(columns));
    }
    // The grouped output projection hands over every head's output at once,
    // which is wider than any single tensor's row count, so the activation
    // buffer is sized for the largest thing that actually crosses rather than
    // for the largest weight.
    widest_input=std::max(widest_input,rt.heads*rt.head_dim);
    const auto input_capacity=static_cast<std::uint64_t>(widest_input)*sizeof(float);
    const auto output_capacity=static_cast<std::uint64_t>(widest_output)*sizeof(float);
    // One batch holds every projection of the hidden state a layer makes.
    const auto batch_capacity=8ull*output_capacity;
    if(flyweight_gpu_alloc(input_capacity,&input_buffer)!=0||
       flyweight_gpu_alloc(output_capacity,&output_buffer)!=0||
       flyweight_gpu_alloc(batch_capacity,&batch_buffer)!=0){
        release_pending();
        throw std::runtime_error("out of device memory for the activation buffers");
    }
    // Keep a layer-partitioned cache. A global LRU is pathological here: the
    // next token visits all 43 layers in the same order and evicts every early
    // layer before it can be reused. Equal per-layer partitions preserve route
    // recurrence and guarantee that one layer cannot churn another's experts.
    for(const auto& layer:rt.layers){
        std::uint64_t bytes=0;
        for(const auto index:{layer.plan.gate_exps,layer.plan.up_exps,layer.plan.down_exps})
            bytes+=rt.model->tensors[index].size/rt.experts;
        expert_slot_bytes=std::max(expert_slot_bytes,bytes);
    }
    expert_slot_bytes=device_align(expert_slot_bytes);
    // Opt-in on this hardware: even grouped execution is slightly slower than
    // the 16-core IQ path at its measured route recurrence. Larger-memory GPUs
    // can enable it explicitly and inspect the exported hit-rate counters.
    std::uint64_t requested_mib=0;
    if(const char* setting=std::getenv("FLYWEIGHT_DS4_EXPERT_CACHE_MIB")){
        if(std::string(setting)=="off")requested_mib=0;
        else requested_mib=std::strtoull(setting,nullptr,10);
    }
    FlyweightV2GpuInfo gpu_info{};
    gpu_probe(gpu_info,device);
    constexpr std::uint64_t reserve=768ull*1024*1024;
    const auto available=gpu_info.free_memory>reserve?gpu_info.free_memory-reserve:0;
    const auto budget=std::min<std::uint64_t>(requested_mib*1024ull*1024ull,available);
    if(expert_slot_bytes&&!rt.layers.empty()){
        expert_slots_per_layer=static_cast<std::uint32_t>(
            budget/expert_slot_bytes/rt.layers.size());
        expert_cache_bytes=static_cast<std::uint64_t>(expert_slots_per_layer)*
            rt.layers.size()*expert_slot_bytes;
        if(expert_slots_per_layer<rt.experts_used)expert_cache_bytes=0;
    }
    if(expert_cache_bytes&&flyweight_gpu_alloc(expert_cache_bytes,&expert_cache)!=0){
        // Caching is an optimization. Dense placement remains useful when a
        // fragmented or concurrently used device cannot satisfy this request.
        expert_cache=expert_cache_bytes=0;
        expert_slots_per_layer=0;
    }
    rt.resident=std::move(resident);
    rt.gpu_weight_bytes=weight_bytes;
    rt.gpu_input=input_buffer; rt.gpu_output=output_buffer; rt.gpu_batch=batch_buffer;
    rt.gpu_input_capacity=input_capacity; rt.gpu_output_capacity=output_capacity;
    rt.gpu_batch_capacity=batch_capacity;
    rt.expert_cache=expert_cache; rt.expert_cache_bytes=expert_cache_bytes;
    rt.expert_slot_bytes=expert_slot_bytes;
    rt.expert_slots_per_layer=expert_slots_per_layer;
    rt.expert_slots.resize(static_cast<std::size_t>(expert_slots_per_layer)*rt.layers.size());
    rt.gpu_cache_owner=&rt;
    input_buffer=output_buffer=batch_buffer=expert_cache=0;
    rt.gpu=true;
    rt.gpu_owner=true;
    return 0;});}

int flyweight_v2_deepseek4_runtime_gpu_share(
    FlyweightV2Deepseek4Runtime* runtime,const FlyweightV2Deepseek4Runtime* owner
){return guarded([&]{
    if(!runtime||!owner||!owner->gpu)
        throw std::runtime_error("a GPU-enabled owner runtime is required");
    if(runtime->gpu)throw std::runtime_error("runtime already has a GPU workspace");
    if(runtime->model!=owner->model||runtime->n_embd!=owner->n_embd||
       runtime->heads!=owner->heads||runtime->vocabulary!=owner->vocabulary)
        throw std::runtime_error("GPU workspace owner is incompatible");
    runtime->gpu=true; runtime->gpu_owner=false; runtime->gpu_device=owner->gpu_device;
    runtime->resident=owner->resident;
    runtime->gpu_input=owner->gpu_input; runtime->gpu_output=owner->gpu_output;
    runtime->gpu_batch=owner->gpu_batch;
    runtime->gpu_input_capacity=owner->gpu_input_capacity;
    runtime->gpu_output_capacity=owner->gpu_output_capacity;
    runtime->gpu_batch_capacity=owner->gpu_batch_capacity;
    runtime->gpu_weight_bytes=owner->gpu_weight_bytes;
    runtime->expert_cache=owner->expert_cache;
    runtime->expert_cache_bytes=owner->expert_cache_bytes;
    runtime->expert_slot_bytes=owner->expert_slot_bytes;
    runtime->expert_slots_per_layer=owner->expert_slots_per_layer;
    runtime->gpu_cache_owner=const_cast<FlyweightV2Deepseek4Runtime*>(owner);
    return 0;});}

// Make this thread's CUDA context current.
//
// The driver retains the device's primary context and makes it current on the
// calling thread, and "current" is per thread. The weights are uploaded from
// whichever thread built the runtime, but the scheduler forwards from its own,
// where nothing was current and every launch failed. Cheap and idempotent, so
// a worker can simply call it once when it starts.
int flyweight_v2_deepseek4_runtime_gpu_attach(FlyweightV2Deepseek4Runtime* runtime
){return guarded([&]{
    if(!runtime)throw std::runtime_error("invalid runtime");
    if(!runtime->gpu)return 0;
    if(flyweight_gpu_init(runtime->gpu_device)!=0)
        throw std::runtime_error("cannot attach this thread to the CUDA context");
    return 0;});}

int flyweight_v2_deepseek4_gpu_matvec_check(
    FlyweightV2Model* model, const char* name, const float* input, int32_t inputs,
    int32_t outputs, float* out_gpu, float* out_cpu, int32_t device,
    int32_t iterations, double* seconds
){return guarded([&]{
    if(!model||!name||!input||!out_gpu||!out_cpu||inputs<=0||outputs<=0)
        throw std::runtime_error("gpu matvec check arguments are invalid");
    const auto found=std::find_if(model->tensors.begin(),model->tensors.end(),
        [&](const Tensor& tensor){return tensor.name==name;});
    if(found==model->tensors.end())
        throw std::runtime_error(std::string("tensor not found: ")+name);
    if(flyweight_gpu_init(device)!=0)
        throw std::runtime_error("failed to initialize CUDA");
    ds4_gpu_compile(device);

    // The CPU answer first, from the same path the runtime uses.
    for(std::int32_t row=0;row<outputs;++row)
        out_cpu[row]=qwen_quant_dot(tensor_data(*model,*found),found->type,input,inputs,
                                    static_cast<std::uint64_t>(row));

    // A three-dimensional routed tensor can be checked one expert at a time;
    // allocating its complete 18 GiB backing just to exercise an 8 MiB kernel
    // slice would make the checker unusable on the device this path targets.
    const std::uint64_t weight_bytes = found->shape.size() == 3
        ? found->size / found->shape[2] : found->size;
    std::uint64_t weights=0,vector=0,result=0;
    if(flyweight_gpu_alloc(weight_bytes,&weights)!=0)
        throw std::runtime_error("cannot allocate device weights");
    if(flyweight_gpu_alloc(static_cast<std::uint64_t>(inputs)*sizeof(float),&vector)!=0)
        throw std::runtime_error("cannot allocate the device input");
    if(flyweight_gpu_alloc(static_cast<std::uint64_t>(outputs)*sizeof(float),&result)!=0)
        throw std::runtime_error("cannot allocate the device output");
    int status=flyweight_gpu_upload_sync(weights,tensor_data(*model,*found),weight_bytes);
    if(status==0)status=flyweight_gpu_upload_sync(vector,input,
        static_cast<std::uint64_t>(inputs)*sizeof(float));
    if(status==0)status=ds4_gpu_matvec(found->type,weights,vector,result,inputs,outputs,0);
    if(status==0)status=flyweight_gpu_sync();
    // Repeat the resident matvec for timing. The weights are already on the
    // device, so this measures the kernel against VRAM rather than the upload,
    // which is the number that decides whether moving the dense half is worth
    // it. This laptop's clocks ramp under load, so the caller must ask for
    // enough iterations to get past that.
    if(status==0&&iterations>0){
        const auto started=ds4_now();
        for(std::int32_t i=0;i<iterations&&status==0;++i)
            status=ds4_gpu_matvec(found->type,weights,vector,result,inputs,outputs,0);
        if(status==0)status=flyweight_gpu_sync();
        if(seconds)*seconds=static_cast<double>(ds4_now()-started)/1e9;
    }
    if(status==0)status=flyweight_gpu_download(out_gpu,result,
        static_cast<std::uint64_t>(outputs)*sizeof(float),0);
    if(status==0)status=flyweight_gpu_sync();
    flyweight_gpu_free(weights);flyweight_gpu_free(vector);flyweight_gpu_free(result);
    if(status!=0)throw std::runtime_error(
        "the device matvec failed with status "+std::to_string(status));
    return 0;});}

int flyweight_v2_deepseek4_indexer_key(
    const FlyweightV2Deepseek4Runtime* runtime, uint32_t layer, uint32_t block,
    float* out, int32_t outputs
){return guarded([&]{
    if(!runtime||!out)throw std::runtime_error("invalid indexer key request");
    if(layer>=runtime->layers.size())throw std::runtime_error("layer is out of range");
    const auto& state=runtime->layers[layer];
    if(!state.indexer_dim)throw std::runtime_error("this layer carries no indexer");
    if(block>=state.blocks)throw std::runtime_error("that block has not been compressed yet");
    if(outputs!=static_cast<std::int32_t>(state.indexer_dim))
        throw std::runtime_error("output size does not match the indexer key length");
    const std::uint16_t* stored=state.indexer_compressed.data()+
        static_cast<std::size_t>(block)*state.indexer_dim;
    for(std::uint32_t i=0;i<state.indexer_dim;++i)
        out[i]=flyweight::v2::deepseek4::half_value(stored[i]);
    return 0;});}

int flyweight_v2_deepseek4_indexer_scores(
    const float* queries, const float* keys, const float* weights,
    int32_t heads, int32_t dim, int32_t entries, float* out
){return guarded([&]{
    if(!queries||!keys||!weights||!out||heads<=0||dim<=0||entries<0)
        throw std::runtime_error("indexer score arguments are invalid");
    flyweight::v2::deepseek4::indexer_scores(queries,keys,weights,
        static_cast<std::size_t>(heads),static_cast<std::size_t>(dim),
        static_cast<std::size_t>(entries),out);
    return 0;});}

int flyweight_v2_deepseek4_top_k(
    const float* scores, int32_t entries, int32_t keep, uint8_t* out
){return guarded([&]{
    if(!scores||!out||entries<0||keep<0)
        throw std::runtime_error("top-k arguments are invalid");
    flyweight::v2::deepseek4::top_k_select(scores,static_cast<std::size_t>(entries),
        static_cast<std::size_t>(keep),out);
    return 0;});}

// Round-trip a float through half precision, so the storage format the caches
// use can be checked from outside.
float flyweight_v2_deepseek4_half_round_trip(float value){
    return flyweight::v2::deepseek4::half_value(flyweight::v2::deepseek4::half_bits(value));
}

// Gather the state rows one compressed block pools.
int flyweight_v2_deepseek4_gather_block(
    const float* values, const float* scores, int32_t width, int32_t head_dim,
    int32_t ratio, int32_t block, int32_t overlapped,
    float* out_values, float* out_scores, int32_t* rows
){return guarded([&]{
    if(!values||!scores||!out_values||!out_scores||width<=0||head_dim<=0||ratio<=0||block<0)
        throw std::runtime_error("gather-block arguments are invalid");
    const std::int32_t expected=overlapped?2*head_dim:head_dim;
    if(width!=expected)
        throw std::runtime_error("state width does not match the block kind");
    const auto count=flyweight::v2::deepseek4::gather_block(
        values,scores,static_cast<std::size_t>(width),static_cast<std::size_t>(head_dim),
        static_cast<std::size_t>(ratio),static_cast<std::size_t>(block),overlapped!=0,
        out_values,out_scores);
    if(rows)*rows=static_cast<std::int32_t>(count);
    return 0;});}

// Build the attention mask for one query position of a DeepSeek-V4 layer.
int flyweight_v2_deepseek4_visible_keys(
    int32_t position, int32_t raw_positions, int32_t blocks, int32_t ratio,
    int32_t window, uint8_t* mask, int32_t* visible
){return guarded([&]{
    if(!mask||position<0||raw_positions<0||blocks<0||ratio<0||window<0)
        throw std::runtime_error("visible-key arguments are invalid");
    if(blocks&&!ratio)throw std::runtime_error("compressed blocks need a non-zero ratio");
    const auto count=flyweight::v2::deepseek4::visible_keys(
        static_cast<std::size_t>(position),static_cast<std::size_t>(raw_positions),
        static_cast<std::size_t>(blocks),static_cast<std::size_t>(ratio),
        static_cast<std::size_t>(window),mask);
    if(visible)*visible=static_cast<std::int32_t>(count);
    return 0;});}

// Pool one block of positions into a single compressed latent.
int flyweight_v2_deepseek4_compress(
    const float* values, const float* scores, int32_t positions, int32_t width,
    float* output
){return guarded([&]{
    if(!values||!scores||!output||positions<=0||width<=0)
        throw std::runtime_error("compress arguments are invalid");
    flyweight::v2::deepseek4::compress_block(
        values,scores,static_cast<std::size_t>(positions),
        static_cast<std::size_t>(width),output);
    return 0;});}

// Expert routing for one token: pick the experts and weight them.
int flyweight_v2_deepseek4_router(
    const float* logits, const float* bias, int32_t experts, int32_t used,
    float weight_scale, float sum_floor, int32_t select, int32_t* chosen, float* weights
){return guarded([&]{
    if(!logits||!chosen||!weights||experts<=0||used<=0||used>experts)
        throw std::runtime_error("router arguments are invalid");
    if(!select)
        for(std::int32_t slot=0;slot<used;++slot)
            if(chosen[slot]<0||chosen[slot]>=experts)
                throw std::runtime_error("routed expert id is out of range");
    flyweight::v2::deepseek4::moe_router(
        logits,bias,static_cast<std::size_t>(experts),static_cast<std::size_t>(used),
        weight_scale,sum_floor,select!=0,chosen,weights);
    return 0;});}

// SwiGLU with both halves clamped, as the per-layer swiglu limits require.
int flyweight_v2_deepseek4_swiglu(
    const float* gate, const float* up, int32_t size, float limit, float* output
){return guarded([&]{
    if(!gate||!up||!output||size<=0)throw std::runtime_error("swiglu arguments are invalid");
    flyweight::v2::deepseek4::clamped_swiglu(
        gate,up,static_cast<std::size_t>(size),limit,output);
    return 0;});}

// Rotary embedding over `count` rows, each `stride` wide, rotating the trailing
// `rope_dim` elements at `position`.
int flyweight_v2_deepseek4_rope(
    float* values, int32_t stride, int32_t rope_dim, int32_t count,
    int32_t position, float freq_base, float freq_scale, int32_t inverse,
    float ext_factor, float attn_factor, float beta_fast, float beta_slow,
    int32_t original_context
){return guarded([&]{
    if(!values||stride<=0||rope_dim<=0||rope_dim>stride||count<=0)
        throw std::runtime_error("rope arguments are invalid");
    flyweight::v2::deepseek4::YarnParameters yarn{
        ext_factor,attn_factor,beta_fast,beta_slow,
        static_cast<std::uint32_t>(original_context<0?0:original_context)};
    for(std::int32_t row=0;row<count;++row)
        flyweight::v2::deepseek4::rope(
            values+static_cast<std::size_t>(row)*stride+(stride-rope_dim),
            static_cast<std::size_t>(rope_dim),position,freq_base,freq_scale,inverse!=0,yarn);
    return 0;});}

// Attention over the shared KV latent with per-head sink logits.
int flyweight_v2_deepseek4_attention(
    const float* queries, const float* latents, const float* sinks,
    const uint8_t* mask, int32_t heads, int32_t head_dim, int32_t positions,
    float scale, float* output
){return guarded([&]{
    if(!queries||!latents||!output||heads<=0||head_dim<=0||positions<=0)
        throw std::runtime_error("attention arguments are invalid");
    flyweight::v2::deepseek4::attention_with_sinks(
        queries,latents,sinks,mask,static_cast<std::size_t>(heads),
        static_cast<std::size_t>(head_dim),static_cast<std::size_t>(positions),
        scale,output);
    return 0;});}

// One DeepSeek-V4 hyper-connection step: derive the mixing weights from the
// stream state, collapse the streams into the vector a block reads, and -- when
// `block` is supplied -- write that block's output back into every stream.
// Exposed so the pieces can be diffed against the reference implementation one
// at a time, before there is a whole forward pass to compare.
int flyweight_v2_deepseek4_hyper_connection(
    const float* streams, const float* fn, const float* scale, const float* base,
    int32_t n_embd, int32_t hc, int32_t sinkhorn_iterations,
    float rms_epsilon, float hc_epsilon,
    const float* block,
    float* mixes, float* pre, float* post, float* comb,
    float* collapsed, float* combined
){return guarded([&]{
    if(!streams||!fn||!scale||!base||!pre||!post||!comb)
        throw std::runtime_error("hyper-connection inputs are required");
    if(n_embd<=0||hc<=0||sinkhorn_iterations<=0)
        throw std::runtime_error("hyper-connection geometry is invalid");
    namespace ds4=flyweight::v2::deepseek4;
    ds4::hyper_connection_weights(
        streams,fn,scale,base,static_cast<std::size_t>(n_embd),static_cast<std::size_t>(hc),
        static_cast<std::uint32_t>(sinkhorn_iterations),rms_epsilon,hc_epsilon,
        pre,post,comb,mixes);
    if(collapsed)
        ds4::hyper_connection_collapse(streams,pre,static_cast<std::size_t>(n_embd),
            static_cast<std::size_t>(hc),collapsed);
    if(block&&combined)
        ds4::hyper_connection_combine(block,streams,post,comb,
            static_cast<std::size_t>(n_embd),static_cast<std::size_t>(hc),combined);
    return 0;});}

// Whether the runtime can decode a GGML weight type at all. This is the CPU
// path's set, which is the widest one: a type missing here cannot be executed
// by any backend, so a checkpoint carrying it is unusable rather than slow.
int flyweight_v2_quant_supported(uint32_t type){return qwen_cpu_expert_type_supported(type)?1:0;}

// Copy the per-layer attention-kind array. Returns the number of entries written.
int flyweight_v2_model_compress_ratios(const FlyweightV2Model* m,uint32_t* out,int32_t capacity){return guarded([&]{
    if(!m)throw std::runtime_error("invalid model handle");
    const auto& ratios=m->config.compress_ratios;
    if(!out||capacity<=0)return static_cast<int>(ratios.size());
    const auto written=std::min(static_cast<std::size_t>(capacity),ratios.size());
    std::copy_n(ratios.begin(),written,out);
    return static_cast<int>(written);});}
int flyweight_v2_model_target_layers(const FlyweightV2Model* m,uint32_t* out,int32_t capacity){return guarded([&]{
    if(!m)throw std::runtime_error("invalid model handle");
    const auto& layers=m->config.target_layers;
    if(!out||capacity<=0)return static_cast<int>(layers.size());
    const auto written=std::min(static_cast<std::size_t>(capacity),layers.size());
    std::copy_n(layers.begin(),written,out);
    return static_cast<int>(written);});}
int flyweight_v2_model_attention_window(const FlyweightV2Model* m,uint32_t layer,uint32_t*out){return guarded([&]{if(!m||!out)throw std::runtime_error("invalid model attention-window handle");*out=attention_window(*m,layer);return 0;});}
int flyweight_v2_tensor_info(const FlyweightV2Model* m,uint64_t i,FlyweightV2TensorInfo* out){return guarded([&]{if(!m||!out||i>=m->tensors.size())throw std::runtime_error("tensor index out of range");return fill(m->tensors[i],*out);});}
int flyweight_v2_tensor_find(const FlyweightV2Model* m,const char* name,FlyweightV2TensorInfo* out){return guarded([&]{if(!m||!name||!out)throw std::runtime_error("invalid tensor lookup");for(auto const&t:m->tensors)if(t.name==name)return fill(t,*out);throw std::runtime_error("tensor not found");});}
int flyweight_v2_qwen_validate(const FlyweightV2Model*m){return guarded([&]{if(!m)throw std::runtime_error("invalid model handle");if(m->config.architecture.find("qwen")!=0)throw std::runtime_error("model architecture is not Qwen");if(!m->config.hidden_size||!m->config.layer_count||!m->config.attention_heads)throw std::runtime_error("Qwen config is incomplete");return 0;});}
int flyweight_v2_qwen_tensor_role(const FlyweightV2Model*m,const char*role,FlyweightV2TensorInfo*out){return guarded([&]{if(!m||!role||!out)throw std::runtime_error("invalid Qwen tensor role lookup");std::vector<std::string> candidates;if(std::strcmp(role,"token_embeddings")==0)candidates={"token_embd.weight","model.embed_tokens.weight","embed_tokens.weight"};else if(std::strcmp(role,"final_norm")==0)candidates={"output_norm.weight","model.norm.weight","norm.weight"};else if(std::strcmp(role,"lm_head")==0)candidates={"output.weight","lm_head.weight","token_embd.weight","model.embed_tokens.weight","embed_tokens.weight"};else throw std::runtime_error("unknown Qwen tensor role");for(auto const&candidate:candidates)for(auto const&t:m->tensors)if(t.name==candidate)return fill(t,*out);throw std::runtime_error("Qwen tensor role is missing");});}
int flyweight_v2_qwen_layer_tensor(const FlyweightV2Model*m,uint32_t layer,const char*role,FlyweightV2TensorInfo*out){return guarded([&]{if(!m||!role||!out)throw std::runtime_error("invalid Qwen layer tensor lookup");std::string prefix="blk."+std::to_string(layer)+".";std::vector<std::string> suffixes;if(std::strcmp(role,"input_norm")==0)suffixes={"attn_norm.weight"};else if(std::strcmp(role,"qkv")==0)suffixes={"attn_qkv.weight"};else if(std::strcmp(role,"attention_q")==0)suffixes={"attn_q.weight"};else if(std::strcmp(role,"attention_k")==0)suffixes={"attn_k.weight"};else if(std::strcmp(role,"attention_v")==0)suffixes={"attn_v.weight"};else if(std::strcmp(role,"attention_output")==0)suffixes={"attn_output.weight","attn_out.weight"};else if(std::strcmp(role,"attention_gate")==0)suffixes={"attn_gate.weight"};else if(std::strcmp(role,"ssm_output")==0)suffixes={"ssm_out.weight"};else if(std::strcmp(role,"ssm_alpha")==0)suffixes={"ssm_alpha.weight"};else if(std::strcmp(role,"ssm_beta")==0)suffixes={"ssm_beta.weight"};else if(std::strcmp(role,"ssm_conv")==0)suffixes={"ssm_conv1d.weight"};else if(std::strcmp(role,"ssm_dt_bias")==0)suffixes={"ssm_dt.bias"};else if(std::strcmp(role,"ssm_a")==0)suffixes={"ssm_a"};else if(std::strcmp(role,"ssm_norm")==0)suffixes={"ssm_norm.weight"};else if(std::strcmp(role,"post_attention_norm")==0)suffixes={"post_attention_norm.weight"};else if(std::strcmp(role,"router")==0)suffixes={"ffn_gate_inp.weight"};else if(std::strcmp(role,"shared_gate")==0)suffixes={"ffn_gate_shexp.weight"};else throw std::runtime_error("unknown Qwen layer tensor role");for(auto const&suffix:suffixes)for(auto const&t:m->tensors)if(t.name==prefix+suffix)return fill(t,*out);throw std::runtime_error("Qwen layer tensor role is missing");});}
float half_to_float(uint16_t bits){uint32_t sign=(bits&0x8000u)<<16, exponent=(bits>>10)&0x1fu, fraction=bits&0x3ffu;uint32_t result;if(exponent==0){if(!fraction)result=sign;else{exponent=1;while((fraction&0x400u)==0){fraction<<=1;--exponent;}result=sign|((exponent+112)<<23)|((fraction&0x3ffu)<<13);}}else if(exponent==31)result=sign|0x7f800000u|(fraction<<13);else result=sign|((exponent+112)<<23)|(fraction<<13);float value;std::memcpy(&value,&result,sizeof(value));return value;}
// Q5_0: 2-byte scale, a 32-bit plane of fifth bits, then 32 nibbles. The two
// halves of a block are the low and high nibbles, and each takes its fifth bit
// from bit `within` of the plane -- so the halves read bits 0-15 and 16-31.
// Only the host side needs this: it exists so a Q5_0 tensor can be repacked to
// Q8_0 on the way to the device, which is what the MTP draft block does when a
// companion GGUF is quantized differently from the trunk.
inline float qwen_q5_0_value(const uint8_t* data, uint64_t index) {
    constexpr uint64_t kBlock = 22;  // 2 + 4 + 16
    const uint64_t block = index / 32, within = index % 32;
    const uint8_t* base = data + block * kBlock;
    uint16_t scale_bits = 0;
    std::memcpy(&scale_bits, base, 2);
    uint32_t plane = 0;
    std::memcpy(&plane, base + 2, 4);
    const uint64_t nibble = within % 16;
    const uint8_t byte = base[6 + nibble];
    const uint8_t low = (within < 16) ? (byte & 0x0F) : (byte >> 4);
    const uint8_t fifth = static_cast<uint8_t>((plane >> within) & 1u) << 4;
    return half_to_float(scale_bits) *
           (static_cast<float>(low | fifth) - 16.0f);
}

float tensor_value(const uint8_t*data,uint32_t type,uint64_t index){if(type==6)return qwen_q5_0_value(data,index);if(type==0){float value;std::memcpy(&value,data+index*4,4);return value;}if(type==1){uint16_t value;std::memcpy(&value,data+index*2,2);return half_to_float(value);}if(type==30){uint16_t value;std::memcpy(&value,data+index*2,2);uint32_t bits=static_cast<uint32_t>(value)<<16;float result;std::memcpy(&result,&bits,4);return result;}if(type==8){uint64_t block=index/32,within=index%32;uint16_t scale;std::memcpy(&scale,data+block*kQ8BlockSize,2);int8_t quant;std::memcpy(&quant,data+block*kQ8BlockSize+2+within,1);return half_to_float(scale)*static_cast<float>(quant);}if(type==40)return qwen_nvfp4_value(data,index);if(type==10)return qwen_q2k_value(data,index);if(type==11)return qwen_q3k_value(data,index);if(type==16)return qwen_iq2xxs_value(data,index);if(type==18)return qwen_iq3xxs_value(data,index);if(type==22)return qwen_iq2s_value(data,index);if(type==21)return qwen_iq3s_value(data,index);if(type==17)return qwen_iq2xs_value(data,index);if(type==23)return qwen_iq4xs_value(data,index);if(type==29)return qwen_iq1m_value(data,index);if(type==12)return qwen_q4k_value(data,index);if(type==13)return qwen_q5_value(data,index);if(type==14)return qwen_q6_value(data,index);throw std::runtime_error("unsupported Qwen CPU tensor type");}
int flyweight_v2_dspark_encode(const FlyweightV2Model*m,const float*features,uint64_t elements,float*output,uint64_t output_elements){return guarded([&]{
    if(!m||!features||!output)throw std::runtime_error("DSpark encoder arguments are required");
    if(m->config.architecture!="dflash")throw std::runtime_error("not a DFlash/DSpark sidecar");
    const auto width=m->config.hidden_size;
    const auto expected=static_cast<std::uint64_t>(m->config.target_layers.size())*width;
    if(!width||elements!=expected||output_elements<width)throw std::runtime_error("DSpark encoder feature shape is invalid");
    if(flyweight_v2_matvec(m,"fc.weight",features,static_cast<int32_t>(elements),output,static_cast<int32_t>(width))!=0)
        throw std::runtime_error(error.empty()?"DSpark fusion projection failed":error);
    const auto norm=std::find_if(m->tensors.begin(),m->tensors.end(),[](const Tensor&t){return t.name=="enc.output_norm.weight";});
    if(norm==m->tensors.end())throw std::runtime_error("DSpark encoder norm is missing");
    double square=0.0;for(std::uint32_t i=0;i<width;++i)square+=static_cast<double>(output[i])*output[i];
    const float scale=1.0f/std::sqrt(static_cast<float>(square/width)+m->config.rms_norm_epsilon);
    const auto*data=tensor_data(*m,*norm);
    for(std::uint32_t i=0;i<width;++i)output[i]*=scale*tensor_value(data,norm->type,i);
    return 0;});}

struct FlyweightV2DsparkRuntime {
    FlyweightV2Model* model=nullptr;
    std::uint32_t context_limit=0,position=0,width=0,rope_dim=0,window=0;
    float epsilon=0.0f,freq_base=10000.0f;
    std::vector<std::uint16_t> cache;
    std::vector<float> projected;
    FlyweightV2Deepseek4Runtime* decoder=nullptr;
};

int flyweight_v2_dspark_runtime_create(FlyweightV2Model*m,uint32_t context_limit,FlyweightV2DsparkRuntime**out){return guarded([&]{
    if(!m||!out||!context_limit)throw std::runtime_error("DSpark model, context and output are required");
    if(m->config.architecture!="dflash")throw std::runtime_error("not a DFlash/DSpark sidecar");
    if(context_limit>m->config.context_length)throw std::runtime_error("DSpark context exceeds checkpoint limit");
    auto runtime=std::make_unique<FlyweightV2DsparkRuntime>();
    runtime->model=m;runtime->context_limit=context_limit;runtime->width=m->config.kv_lora_rank;
    runtime->rope_dim=m->config.rotary_dimension;runtime->window=std::min(context_limit,m->config.sliding_window);
    runtime->epsilon=m->config.rms_norm_epsilon;runtime->freq_base=m->config.rope_freq_base;
    if(!runtime->width||runtime->rope_dim>runtime->width||!runtime->window)
        throw std::runtime_error("DSpark KV geometry is incomplete");
    runtime->cache.assign(static_cast<std::size_t>(m->config.layer_count)*runtime->window*runtime->width,0);
    runtime->projected.resize(runtime->width);
    if(flyweight_v2_deepseek4_runtime_create(m,context_limit,&runtime->decoder)!=0)
        throw std::runtime_error(error.empty()?"cannot create DSpark decoder plan":error);
    *out=runtime.release();return 0;});}
void flyweight_v2_dspark_runtime_free(FlyweightV2DsparkRuntime*r){try{if(r)flyweight_v2_deepseek4_runtime_free(r->decoder);delete r;}catch(...){}}
int flyweight_v2_dspark_runtime_reset(FlyweightV2DsparkRuntime*r){return guarded([&]{
    if(!r)throw std::runtime_error("invalid DSpark runtime");
    r->position=0;std::fill(r->cache.begin(),r->cache.end(),0);
    if(flyweight_v2_deepseek4_runtime_reset(r->decoder)!=0)
        throw std::runtime_error(error.empty()?"cannot reset DSpark decoder":error);
    return 0;});}

int flyweight_v2_dspark_inject(FlyweightV2DsparkRuntime*r,const float*fused,uint64_t elements){return guarded([&]{
    if(!r||!fused||elements!=r->model->config.hidden_size)throw std::runtime_error("invalid DSpark injection");
    if(r->position>=r->context_limit)throw std::runtime_error("DSpark cache is past its context limit");
    auto& m=*r->model;
    for(std::uint32_t layer=0;layer<m.config.layer_count;++layer){
        const auto prefix="blk."+std::to_string(layer)+".";
        if(flyweight_v2_matvec(&m,(prefix+"attn_kv.weight").c_str(),fused,static_cast<int32_t>(elements),r->projected.data(),r->width)!=0)
            throw std::runtime_error(error.empty()?"DSpark KV projection failed":error);
        const auto norm_name=prefix+"attn_kv_a_norm.weight";
        const auto norm=std::find_if(m.tensors.begin(),m.tensors.end(),[&](const Tensor&t){return t.name==norm_name;});
        if(norm==m.tensors.end())throw std::runtime_error("DSpark KV norm is missing");
        double square=0.0;for(const auto value:r->projected)square+=static_cast<double>(value)*value;
        const float scale=1.0f/std::sqrt(static_cast<float>(square/r->width)+r->epsilon);
        const auto*gain=tensor_data(m,*norm);
        for(std::uint32_t i=0;i<r->width;++i)r->projected[i]*=scale*tensor_value(gain,norm->type,i);
        flyweight::v2::deepseek4::rope(r->projected.data()+r->width-r->rope_dim,r->rope_dim,
            static_cast<std::int32_t>(r->position),r->freq_base,1.0f,false);
        auto*slot=r->cache.data()+(static_cast<std::size_t>(layer)*r->window+r->position%r->window)*r->width;
        auto*decoder_slot=r->decoder->layers[layer].latents.data()+
            static_cast<std::size_t>(r->position%r->window)*r->width;
        for(std::uint32_t i=0;i<r->width;++i)slot[i]=decoder_slot[i]=flyweight::v2::deepseek4::half_bits(r->projected[i]);
        r->decoder->layers[layer].positions=r->position+1;
    }
    ++r->position;return 0;});}

int flyweight_v2_dspark_cached(const FlyweightV2DsparkRuntime*r,uint32_t layer,uint32_t position,float*output,uint64_t elements){return guarded([&]{
    if(!r||!output||layer>=r->model->config.layer_count||position>=r->position||
       r->position-position>r->window||elements<r->width)throw std::runtime_error("invalid DSpark cache read");
    const auto*slot=r->cache.data()+(static_cast<std::size_t>(layer)*r->window+position%r->window)*r->width;
    for(std::uint32_t i=0;i<r->width;++i)output[i]=flyweight::v2::deepseek4::half_value(slot[i]);
    return 0;});}

int flyweight_v2_dspark_heads(const FlyweightV2Model*m,const float*base_logits,const float*hidden,
    uint32_t rows,uint32_t anchor_token,float*logits,float*confidence,uint32_t*tokens){return guarded([&]{
    if(!m||!base_logits||!hidden||!logits||!confidence||!tokens)
        throw std::runtime_error("DSpark head arguments are required");
    if(m->config.architecture!="dflash"||!rows||rows>m->config.draft_block_size)
        throw std::runtime_error("invalid DSpark head block");
    const auto vocab=m->config.vocabulary_size,width=m->config.hidden_size;
    if(anchor_token>=vocab)throw std::runtime_error("DSpark anchor is outside the vocabulary");
    auto find=[&](const char*name)->const Tensor&{
        const auto found=std::find_if(m->tensors.begin(),m->tensors.end(),[&](const Tensor&t){return t.name==name;});
        if(found==m->tensors.end())throw std::runtime_error(std::string("DSpark tensor is missing: ")+name);
        return *found;
    };
    const auto&w1=find("markov_w1.weight");const auto&w2=find("markov_w2.weight");
    const auto&conf=find("conf_proj.weight");
    if(w1.shape.size()!=2||w1.shape[0]!=256||w1.shape[1]!=vocab||
       w2.shape.size()!=2||w2.shape[0]!=256||w2.shape[1]!=vocab||
       conf.shape.size()!=2||conf.shape[0]!=width+256||conf.shape[1]!=1)
        throw std::runtime_error("DSpark head tensor geometry is invalid");
    const auto*w1_data=tensor_data(*m,w1);const auto*conf_data=tensor_data(*m,conf);
    std::vector<float> feature(256),bias(vocab);
    auto previous=anchor_token;
    for(std::uint32_t row=0;row<rows;++row){
        for(std::uint32_t i=0;i<256;++i)
            feature[i]=tensor_value(w1_data,w1.type,static_cast<std::uint64_t>(previous)*256+i);
        if(flyweight_v2_matvec(m,"markov_w2.weight",feature.data(),256,bias.data(),vocab)!=0)
            throw std::runtime_error(error.empty()?"DSpark Markov projection failed":error);
        auto*out=logits+static_cast<std::size_t>(row)*vocab;
        const auto*base=base_logits+static_cast<std::size_t>(row)*vocab;
        std::uint32_t best=0;
        for(std::uint32_t token=0;token<vocab;++token){
            out[token]=base[token]+bias[token];if(out[token]>out[best])best=token;
        }
        double score=0.0;const auto*state=hidden+static_cast<std::size_t>(row)*width;
        for(std::uint32_t i=0;i<width;++i)score+=static_cast<double>(state[i])*tensor_value(conf_data,conf.type,i);
        for(std::uint32_t i=0;i<256;++i)score+=static_cast<double>(feature[i])*tensor_value(conf_data,conf.type,width+i);
        confidence[row]=flyweight::v2::deepseek4::sigmoid(static_cast<float>(score));
        tokens[row]=best;previous=best;
    }
    return 0;});}

int flyweight_v2_dspark_attention(const FlyweightV2DsparkRuntime*r,uint32_t layer,
    const float*queries,const float*noise_kv,uint32_t rows,float*output,uint64_t elements){return guarded([&]{
    if(!r||!queries||!noise_kv||!output||layer>=r->model->config.layer_count||
       !rows||rows>r->model->config.draft_block_size)
        throw std::runtime_error("invalid DSpark attention block");
    const auto heads=r->model->config.attention_heads,width=r->width;
    const auto needed=static_cast<std::uint64_t>(rows)*heads*width;
    if(elements<needed)throw std::runtime_error("DSpark attention output is too small");
    const auto sink_name="blk."+std::to_string(layer)+".attn_sinks.weight";
    const auto sink=std::find_if(r->model->tensors.begin(),r->model->tensors.end(),[&](const Tensor&t){return t.name==sink_name;});
    if(sink==r->model->tensors.end())throw std::runtime_error("DSpark attention sinks are missing");
    const auto*sink_data=tensor_data(*r->model,*sink);
    std::vector<float>sinks(heads),keys(static_cast<std::size_t>(r->window+rows)*width);
    for(std::uint32_t h=0;h<heads;++h)sinks[h]=tensor_value(sink_data,sink->type,h);
    const float scale=1.0f/std::sqrt(static_cast<float>(width));
    for(std::uint32_t row=0;row<rows;++row){
        const auto query_position=r->position+row;
        const auto first=query_position+1>r->window?query_position+1-r->window:0u;
        std::uint32_t count=0;
        for(std::uint32_t position=first;position<r->position;++position){
            const auto*slot=r->cache.data()+(static_cast<std::size_t>(layer)*r->window+position%r->window)*width;
            auto*target=keys.data()+static_cast<std::size_t>(count++)*width;
            for(std::uint32_t i=0;i<width;++i)target[i]=flyweight::v2::deepseek4::half_value(slot[i]);
        }
        for(std::uint32_t noise=0;noise<rows;++noise){
            const auto key_position=r->position+noise;
            if(key_position<=query_position&&query_position-key_position>=r->window)continue;
            std::copy_n(noise_kv+static_cast<std::size_t>(noise)*width,width,
                keys.data()+static_cast<std::size_t>(count++)*width);
        }
        flyweight::v2::deepseek4::attention_with_sinks(
            queries+static_cast<std::size_t>(row)*heads*width,keys.data(),sinks.data(),nullptr,
            heads,width,count,scale,output+static_cast<std::size_t>(row)*heads*width);
    }
    return 0;});}

int flyweight_v2_dspark_attention_stage(FlyweightV2DsparkRuntime*r,uint32_t layer,
    const float*streams,uint32_t rows,float*output,uint64_t elements){return guarded([&]{
    if(!r||!streams||!output||layer>=r->decoder->layers.size()||!rows||rows>r->model->config.draft_block_size)
        throw std::runtime_error("invalid DSpark attention stage");
    auto&rt=*r->decoder;const auto&plan=rt.layers[layer].plan;const auto&model=*rt.model;
    namespace ds4=flyweight::v2::deepseek4;
    const auto stream_width=static_cast<std::size_t>(rt.hc)*rt.n_embd;
    if(elements<static_cast<std::uint64_t>(rows)*stream_width)throw std::runtime_error("DSpark stage output is too small");
    const auto wide=static_cast<std::size_t>(rt.heads)*rt.head_dim;
    std::vector<Deepseek4Scratch>scratch(rows,rt.scratch);
    std::vector<float>queries(static_cast<std::size_t>(rows)*wide);
    std::vector<float>latents(static_cast<std::size_t>(rows)*rt.head_dim);
    std::vector<float>attended(static_cast<std::size_t>(rows)*wide);
    for(std::uint32_t row=0;row<rows;++row){
        auto&sc=scratch[row];const auto*input=streams+static_cast<std::size_t>(row)*stream_width;
        ds4::hyper_connection_weights(input,ds4_f32(model,plan.hc_attn_fn),ds4_f32(model,plan.hc_attn_scale),
            ds4_f32(model,plan.hc_attn_base),rt.n_embd,rt.hc,rt.sinkhorn_iterations,rt.epsilon,rt.epsilon,
            sc.pre.data(),sc.post.data(),sc.comb.data());
        ds4::hyper_connection_collapse(input,sc.pre.data(),rt.n_embd,rt.hc,sc.collapsed.data());
        ds4::rms_norm(sc.collapsed.data(),rt.n_embd,rt.epsilon,sc.hidden.data());
        const auto*attn_gain=ds4_f32(model,plan.attn_norm);
        for(std::uint32_t i=0;i<rt.n_embd;++i)sc.hidden[i]*=attn_gain[i];
        ds4_matvec(rt,plan.q_a,sc.hidden.data(),rt.n_embd,sc.low_rank.data(),rt.q_lora);
        ds4::rms_norm(sc.low_rank.data(),rt.q_lora,rt.epsilon,sc.low_rank.data());
        const auto*q_gain=ds4_f32(model,plan.q_a_norm);
        for(std::uint32_t i=0;i<rt.q_lora;++i)sc.low_rank[i]*=q_gain[i];
        ds4_matvec(rt,plan.q_b,sc.low_rank.data(),rt.q_lora,sc.query.data(),static_cast<int32_t>(wide));
        ds4_matvec(rt,plan.kv,sc.hidden.data(),rt.n_embd,sc.latent.data(),rt.head_dim);
        const auto position=r->position+row;
        for(std::uint32_t head=0;head<rt.heads;++head){
            auto*q=sc.query.data()+static_cast<std::size_t>(head)*rt.head_dim;
            ds4::rms_norm(q,rt.head_dim,rt.epsilon,q);
            ds4::rope(q+rt.head_dim-rt.rope_dim,rt.rope_dim,position,rt.freq_base,1.0f,false);
        }
        ds4::rms_norm(sc.latent.data(),rt.head_dim,rt.epsilon,sc.latent.data());
        const auto*kv_gain=ds4_f32(model,plan.kv_a_norm);
        for(std::uint32_t i=0;i<rt.head_dim;++i)sc.latent[i]*=kv_gain[i];
        ds4::rope(sc.latent.data()+rt.head_dim-rt.rope_dim,rt.rope_dim,position,rt.freq_base,1.0f,false);
        std::copy_n(sc.query.data(),wide,queries.data()+static_cast<std::size_t>(row)*wide);
        std::copy_n(sc.latent.data(),rt.head_dim,latents.data()+static_cast<std::size_t>(row)*rt.head_dim);
    }
    if(flyweight_v2_dspark_attention(r,layer,queries.data(),latents.data(),rows,attended.data(),attended.size())!=0)
        throw std::runtime_error(error.empty()?"DSpark block attention failed":error);
    for(std::uint32_t row=0;row<rows;++row){
        auto&sc=scratch[row];const auto position=r->position+row;
        std::copy_n(attended.data()+static_cast<std::size_t>(row)*wide,wide,sc.derope.data());
        for(std::uint32_t head=0;head<rt.heads;++head)
            ds4::rope(sc.derope.data()+static_cast<std::size_t>(head)*rt.head_dim+rt.head_dim-rt.rope_dim,
                rt.rope_dim,position,rt.freq_base,1.0f,true);
        const auto&wa=model.tensors[plan.output_a];const auto*packed=tensor_data(model,wa);
        const auto inputs=static_cast<std::int32_t>(wide/rt.groups);
        for(std::uint32_t out_row=0;out_row<rt.groups*rt.lora_rank;++out_row){
            const auto group=out_row/rt.lora_rank;
            sc.grouped[out_row]=qwen_quant_dot(packed,wa.type,sc.derope.data()+static_cast<std::size_t>(group)*inputs,inputs,out_row);
        }
        ds4_matvec(rt,plan.output_b,sc.grouped.data(),rt.groups*rt.lora_rank,sc.block_out.data(),rt.n_embd);
        const auto*input=streams+static_cast<std::size_t>(row)*stream_width;
        auto*out=output+static_cast<std::size_t>(row)*stream_width;
        ds4::hyper_connection_combine(sc.block_out.data(),input,sc.post.data(),sc.comb.data(),rt.n_embd,rt.hc,out);
    }
    return 0;});}

int flyweight_v2_dspark_ffn_stage(FlyweightV2DsparkRuntime*r,uint32_t layer,
    const float*streams,uint32_t rows,float*output,uint64_t elements){return guarded([&]{
    if(!r||!streams||!output||layer>=r->decoder->layers.size()||!rows||rows>r->model->config.draft_block_size)
        throw std::runtime_error("invalid DSpark FFN stage");
    auto&rt=*r->decoder;const auto&plan=rt.layers[layer].plan;const auto&model=*rt.model;
    namespace ds4=flyweight::v2::deepseek4;
    const auto stream_width=static_cast<std::size_t>(rt.hc)*rt.n_embd;
    if(elements<static_cast<std::uint64_t>(rows)*stream_width)throw std::runtime_error("DSpark FFN output is too small");
    for(std::uint32_t row=0;row<rows;++row){
        Deepseek4Scratch sc=rt.scratch;
        const auto*input=streams+static_cast<std::size_t>(row)*stream_width;
        auto*out=output+static_cast<std::size_t>(row)*stream_width;
        std::copy_n(input,stream_width,out);
        ds4::hyper_connection_weights(out,ds4_f32(model,plan.hc_ffn_fn),ds4_f32(model,plan.hc_ffn_scale),
            ds4_f32(model,plan.hc_ffn_base),rt.n_embd,rt.hc,rt.sinkhorn_iterations,rt.epsilon,rt.epsilon,
            sc.pre.data(),sc.post.data(),sc.comb.data());
        ds4::hyper_connection_collapse(out,sc.pre.data(),rt.n_embd,rt.hc,sc.collapsed.data());
        ds4::rms_norm(sc.collapsed.data(),rt.n_embd,rt.epsilon,sc.hidden.data());
        const auto*gain=ds4_f32(model,plan.ffn_norm);
        for(std::uint32_t i=0;i<rt.n_embd;++i)sc.hidden[i]*=gain[i];
        std::fill(sc.router.begin(),sc.router.end(),0.0f);
        ds4_matvec(rt,plan.gate_inp,sc.hidden.data(),rt.n_embd,sc.router.data(),rt.experts);
        ds4::moe_router(sc.router.data(),ds4_f32(model,plan.exp_probs_b),rt.experts,rt.experts_used,
            rt.weight_scale,1e-20f,true,sc.experts.data(),sc.weights.data());
        ds4_layer_ffn_complete(rt,layer,out,sc);
    }
    return 0;});}

int flyweight_v2_dspark_decode_hidden(FlyweightV2DsparkRuntime*r,const float*embeddings,
    uint32_t rows,float*hidden,float*normalized,uint64_t elements){return guarded([&]{
    if(!r||!embeddings||!hidden||!normalized||!rows||rows>r->model->config.draft_block_size)
        throw std::runtime_error("invalid DSpark decode block");
    auto&rt=*r->decoder;const auto width=rt.n_embd;
    if(elements<static_cast<std::uint64_t>(rows)*width)throw std::runtime_error("DSpark hidden output is too small");
    const auto stream_width=static_cast<std::size_t>(rt.hc)*width;
    std::vector<float>streams(static_cast<std::size_t>(rows)*stream_width),next(streams.size());
    for(std::uint32_t row=0;row<rows;++row)for(std::uint32_t stream=0;stream<rt.hc;++stream)
        std::copy_n(embeddings+static_cast<std::size_t>(row)*width,width,
            streams.data()+static_cast<std::size_t>(row)*stream_width+static_cast<std::size_t>(stream)*width);
    for(std::uint32_t layer=0;layer<rt.layers.size();++layer){
        if(flyweight_v2_dspark_attention_stage(r,layer,streams.data(),rows,next.data(),next.size())!=0)
            throw std::runtime_error(error.empty()?"DSpark attention stage failed":error);
        if(flyweight_v2_dspark_ffn_stage(r,layer,next.data(),rows,streams.data(),streams.size())!=0)
            throw std::runtime_error(error.empty()?"DSpark FFN stage failed":error);
    }
    namespace ds4=flyweight::v2::deepseek4;const auto&model=*rt.model;
    for(std::uint32_t row=0;row<rows;++row){
        std::vector<float>pre(rt.hc);
        auto*out=hidden+static_cast<std::size_t>(row)*width;
        ds4::hyper_connection_head(streams.data()+static_cast<std::size_t>(row)*stream_width,
            ds4_f32(model,rt.head_fn),ds4_f32(model,rt.head_scale),ds4_f32(model,rt.head_base),
            width,rt.hc,rt.epsilon,rt.epsilon,pre.data(),out);
        auto*norm=normalized+static_cast<std::size_t>(row)*width;
        ds4::rms_norm(out,width,rt.epsilon,norm);const auto*gain=ds4_f32(model,rt.output_norm);
        for(std::uint32_t i=0;i<width;++i)norm[i]*=gain[i];
    }
    return 0;});}
const Tensor& qwen_role_tensor(const FlyweightV2Model&m,const char*role){std::vector<std::string> candidates;if(std::strcmp(role,"token_embeddings")==0)candidates={"token_embd.weight","model.embed_tokens.weight","embed_tokens.weight"};else if(std::strcmp(role,"lm_head")==0)candidates={"output.weight","lm_head.weight"};else throw std::runtime_error("unknown Qwen tensor role");for(auto const&candidate:candidates)for(auto const&t:m.tensors)if(t.name==candidate)return t;throw std::runtime_error("Qwen tensor role is missing");}
int flyweight_v2_qwen_embedding(const FlyweightV2Model*m,uint32_t token,float*out,uint64_t elements){return guarded([&]{if(!m||!out)throw std::runtime_error("invalid embedding arguments");const Tensor&t=qwen_role_tensor(*m,"token_embeddings");if(t.shape.size()!=2)throw std::runtime_error("embedding shape is invalid");uint64_t width=t.shape[0]==m->config.hidden_size?t.shape[0]:t.shape[1],vocab=t.shape[0]==m->config.hidden_size?t.shape[1]:t.shape[0];if(token>=vocab||elements<width)throw std::runtime_error("embedding token or buffer is invalid");const auto*data=tensor_data(*m,t);for(uint64_t i=0;i<width;i++)out[i]=tensor_value(data,t.type,static_cast<uint64_t>(token)*width+i);return 0;});}
int flyweight_v2_qwen_lm_head(const FlyweightV2Model*m,const float*hidden,float*logits,uint64_t vocabulary,uint64_t elements){return guarded([&]{if(!m||!hidden||!logits)throw std::runtime_error("invalid LM-head arguments");const Tensor&t=qwen_role_tensor(*m,"lm_head");if(t.shape.size()!=2)throw std::runtime_error("LM-head shape is invalid");uint64_t width=t.shape[0]==m->config.hidden_size?t.shape[0]:t.shape[1],vocab=t.shape[0]==m->config.hidden_size?t.shape[1]:t.shape[0];if(vocabulary<vocab||elements<width)throw std::runtime_error("LM-head shape or buffer is invalid");const auto*data=tensor_data(*m,t);for(uint64_t row=0;row<vocab;row++){float sum=0;for(uint64_t column=0;column<width;column++)sum+=tensor_value(data,t.type,row*width+column)*hidden[column];logits[row]=sum;}return 0;});}
int flyweight_v2_qwen_token_text(const FlyweightV2Model*m,uint32_t token,char*out,uint64_t capacity){return guarded([&]{if(!m||!out||capacity==0)throw std::runtime_error("invalid token text arguments");if(token>=m->vocabulary.size())throw std::runtime_error("token is outside the GGUF vocabulary");const std::string&value=m->vocabulary[token];if(capacity<=value.size())throw std::runtime_error("token text buffer is too small");std::memcpy(out,value.data(),value.size());out[value.size()]=0;return 0;});}
int flyweight_v2_token_id(const FlyweightV2Model*m,const char*text,uint32_t*token){return guarded([&]{if(!m||!text||!token)throw std::runtime_error("invalid token lookup arguments");const auto it=m->vocabulary_ids.find(text);if(it==m->vocabulary_ids.end())throw std::runtime_error("token text is not in the GGUF vocabulary");*token=it->second;return 0;});}
std::string gguf_byte_encode(const char*text){return flyweight::v2::alphabet::encode(text);}
// One vocabulary piece back to the bytes it stands for; the tool grammar
// matches literal bytes against candidate tokens and the vocabulary spells
// them in the byte-level alphabet. See flyweight_v2_byte_alphabet.hpp.
std::string gguf_byte_decode(const std::string&piece){
    return flyweight::v2::alphabet::decode(piece);
}
std::vector<std::string> gguf_utf8_symbols(const std::string&text){std::vector<std::string> symbols;for(size_t i=0;i<text.size();){unsigned char c=text[i];size_t width=(c<0x80)?1:(c<0xE0?2:(c<0xF0?3:4));if(i+width>text.size())width=1;symbols.emplace_back(text.data()+i,width);i+=width;}return symbols;}
// One UTF-8 codepoint at `offset`, with its encoded width.
std::uint32_t gguf_utf8_codepoint(const std::string& text, size_t offset, size_t& width) {
    const auto lead=static_cast<unsigned char>(text[offset]);
    width=lead<0x80?1:(lead<0xE0?2:(lead<0xF0?3:4));
    if(offset+width>text.size()){width=1;return lead;}
    if(width==1)return lead;
    std::uint32_t value=lead&(0xFFu>>(width+1));
    for(size_t i=1;i<width;++i)
        value=(value<<6)|(static_cast<unsigned char>(text[offset+i])&0x3Fu);
    return value;
}

// Letter/number classification for the pre-tokenizer's \p{L} and \p{N}.
// Exact over ASCII, which is what a code model overwhelmingly sees; above ASCII
// it treats general punctuation, symbol and private-use blocks as non-letters
// and everything else as a letter, rather than carrying a full Unicode category
// table. Non-Latin prose can therefore split differently from the reference.
bool gguf_codepoint_is_number(std::uint32_t codepoint) {
    return codepoint>='0'&&codepoint<='9';
}
bool gguf_codepoint_is_letter(std::uint32_t codepoint) {
    if(codepoint<0x80)
        return (codepoint>='A'&&codepoint<='Z')||(codepoint>='a'&&codepoint<='z');
    if(codepoint>=0x2000&&codepoint<=0x206F)return false;  // general punctuation
    if(codepoint>=0x2070&&codepoint<=0x2BFF)return false;  // sub/superscripts, symbols, arrows
    if(codepoint>=0x3000&&codepoint<=0x303F)return false;  // CJK punctuation
    if(codepoint>=0xE000&&codepoint<=0xF8FF)return false;  // private use
    if(codepoint>=0xFE00&&codepoint<=0xFE0F)return false;  // variation selectors
    if(codepoint>=0xFF00&&codepoint<=0xFF0F)return false;  // fullwidth punctuation
    if(codepoint>=0x1F000)return false;                    // emoji and pictographs
    return true;
}
bool gguf_codepoint_is_space(std::uint32_t codepoint) {
    return codepoint==' '||codepoint=='\t'||codepoint=='\n'||codepoint=='\r'||
           codepoint=='\f'||codepoint=='\v'||codepoint==0x85||codepoint==0xA0||
           (codepoint>=0x2000&&codepoint<=0x200A)||codepoint==0x2028||
           codepoint==0x2029||codepoint==0x202F||codepoint==0x205F||codepoint==0x3000;
}

// Laguna's BPE pre-tokenizer, as a direct transcription of the two regexes the
// reference implementation applies in sequence. Splitting here keeps merges from
// running across word, digit and whitespace boundaries the model never saw:
// notably each digit stands alone, so numbers do not collapse into one token.
std::vector<std::string> laguna_pretokenize(const std::string& text) {
    std::vector<std::string> pieces;
    // First regex: "[^\n]+|[\n]+" -- newline runs separate from everything else.
    for(size_t start=0;start<text.size();){
        const bool newline=text[start]=='\n';
        size_t end=start;
        while(end<text.size()&&(text[end]=='\n')==newline)++end;
        const std::string segment=text.substr(start,end-start);
        start=end;
        if(newline){pieces.push_back(segment);continue;}
        // Second regex, greedy alternation at each position.
        for(size_t at=0;at<segment.size();){
            size_t width=0;
            const auto first=gguf_utf8_codepoint(segment,at,width);
            const size_t begin=at;
            // "'s" and friends, either case.
            if(first=='\''&&at+1<segment.size()){
                const char follow=static_cast<char>(std::tolower(
                    static_cast<unsigned char>(segment[at+1])));
                const char third=at+2<segment.size()
                    ?static_cast<char>(std::tolower(static_cast<unsigned char>(segment[at+2]))):0;
                size_t length=0;
                if(follow=='s'||follow=='t'||follow=='m'||follow=='d')length=2;
                else if((follow=='r'&&third=='e')||(follow=='v'&&third=='e')||
                        (follow=='l'&&third=='l'))length=3;
                if(length){pieces.push_back(segment.substr(at,length));at+=length;continue;}
            }
            // "[^\r\n\p{L}\p{N}]?\p{L}+": an optional single leading non-letter.
            {
                size_t probe=at,probe_width=width;
                auto codepoint=first;
                if(!gguf_codepoint_is_letter(codepoint)&&!gguf_codepoint_is_number(codepoint)&&
                   codepoint!='\r'&&codepoint!='\n'){
                    const size_t after=probe+probe_width;
                    if(after<segment.size()){
                        size_t next_width=0;
                        const auto next=gguf_utf8_codepoint(segment,after,next_width);
                        if(gguf_codepoint_is_letter(next)){probe=after;probe_width=next_width;codepoint=next;}
                    }
                }
                if(gguf_codepoint_is_letter(codepoint)){
                    size_t end_at=probe;
                    while(end_at<segment.size()){
                        size_t letter_width=0;
                        if(!gguf_codepoint_is_letter(
                                gguf_utf8_codepoint(segment,end_at,letter_width)))break;
                        end_at+=letter_width;
                    }
                    pieces.push_back(segment.substr(begin,end_at-begin));at=end_at;continue;
                }
            }
            // "\p{N}": exactly one digit.
            if(gguf_codepoint_is_number(first)){
                pieces.push_back(segment.substr(at,width));at+=width;continue;
            }
            // " ?[^\s\p{L}\p{N}]+[\r\n]*"
            {
                size_t probe=at;
                if(first==' '){
                    const size_t after=at+width;
                    if(after<segment.size()){
                        size_t next_width=0;
                        const auto next=gguf_utf8_codepoint(segment,after,next_width);
                        if(!gguf_codepoint_is_space(next)&&!gguf_codepoint_is_letter(next)&&
                           !gguf_codepoint_is_number(next))probe=after;
                    }
                }
                if(probe<segment.size()){
                    size_t end_at=probe,run_width=0;
                    while(end_at<segment.size()){
                        const auto codepoint=gguf_utf8_codepoint(segment,end_at,run_width);
                        if(gguf_codepoint_is_space(codepoint)||gguf_codepoint_is_letter(codepoint)||
                           gguf_codepoint_is_number(codepoint))break;
                        end_at+=run_width;
                    }
                    if(end_at>probe){
                        while(end_at<segment.size()&&
                              (segment[end_at]=='\r'||segment[end_at]=='\n'))++end_at;
                        pieces.push_back(segment.substr(begin,end_at-begin));at=end_at;continue;
                    }
                }
            }
            // "\s+(?!\S)" and "\s+": a whitespace run, with the last space split
            // off when more non-space follows, which is what the lookahead does.
            {
                size_t end_at=at,space_width=0;
                while(end_at<segment.size()&&
                      gguf_codepoint_is_space(gguf_utf8_codepoint(segment,end_at,space_width)))
                    end_at+=space_width;
                if(end_at>at){
                    size_t stop=end_at;
                    if(end_at<segment.size()&&end_at-at>1)--stop;
                    pieces.push_back(segment.substr(at,stop-at));at=stop;continue;
                }
            }
            pieces.push_back(segment.substr(at,width));at+=width;
        }
    }
    return pieces;
}

// UTF-8 decoded into codepoints alongside the byte offset each one starts at,
// with a trailing entry for the end so a piece can be sliced by codepoint index.
struct Utf8Text {
    std::vector<std::uint32_t> code;
    std::vector<std::size_t> offset;
};

Utf8Text gguf_utf8_decode(const std::string& text) {
    Utf8Text out;
    for(std::size_t at=0;at<text.size();){
        std::size_t width=0;
        const auto codepoint=gguf_utf8_codepoint(text,at,width);
        if(!width)width=1;
        out.code.push_back(codepoint);
        out.offset.push_back(at);
        at+=width;
    }
    out.offset.push_back(text.size());
    return out;
}

// The reference pre-tokenizer applies its regexes in sequence, each one
// splitting the pieces the previous one produced. Within a piece every match
// becomes a piece of its own, and so does every gap between matches.
// Takes a function pointer rather than a template parameter: this lives inside
// the extern "C" block, which templates may not.
using GgufMatcher = std::size_t (*)(const std::vector<std::uint32_t>&, std::size_t);

std::vector<std::string> gguf_regex_split(
    const std::vector<std::string>& pieces, GgufMatcher match_at
) {
    std::vector<std::string> out;
    for(const auto& piece:pieces){
        const auto text=gguf_utf8_decode(piece);
        std::size_t gap=0;
        for(std::size_t at=0;at<text.code.size();){
            const auto length=match_at(text.code,at);
            if(!length){++at;continue;}
            if(at>gap)
                out.push_back(piece.substr(text.offset[gap],text.offset[at]-text.offset[gap]));
            out.push_back(piece.substr(text.offset[at],text.offset[at+length]-text.offset[at]));
            at+=length;
            gap=at;
        }
        if(gap<text.code.size())out.push_back(piece.substr(text.offset[gap]));
    }
    return out;
}

// The reference does not evaluate \p{...} against real Unicode categories for
// ASCII. It "collapses" the text first: codepoints below 128 are left as
// themselves and every category in the pattern is rewritten as an explicit
// ASCII character class, while each non-ASCII codepoint becomes a single marker
// byte standing for its category. Those ASCII classes are not the true
// categories. '~' is the one ASCII character listed in neither the punctuation
// class nor the symbol class, so it matches no category at all and splits
// differently from every other ASCII symbol -- " +++" stays one piece while
// " ~~~" becomes " " and "~~~". Reproduced here deliberately: agreeing with the
// reference tokenizer matters more than agreeing with Unicode.
bool joyai_is_letter(std::uint32_t c) {
    if(c<128)return (c>='A'&&c<='Z')||(c>='a'&&c<='z');
    return flyweight::unicode::is_letter(c);
}
bool joyai_is_number(std::uint32_t c) {
    if(c<128)return c>='0'&&c<='9';
    return flyweight::unicode::is_number(c);
}
bool joyai_is_accent_mark(std::uint32_t c) {
    // The collapse lists no sub-128 codepoints for \p{M}.
    return c<128?false:flyweight::unicode::is_accent_mark(c);
}
bool joyai_is_punctuation(std::uint32_t c) {
    if(c<128)
        return (c>=0x21&&c<=0x23)||(c>=0x25&&c<=0x2A)||(c>=0x2C&&c<=0x2F)||
               (c>=0x3A&&c<=0x3B)||(c>=0x3F&&c<=0x40)||(c>=0x5B&&c<=0x5D)||
               c==0x5F||c==0x7B||c==0x7D;
    return flyweight::unicode::is_punctuation(c);
}
bool joyai_is_symbol(std::uint32_t c) {
    if(c<128)
        return c==0x24||c==0x2B||(c>=0x3C&&c<=0x3E)||c==0x5E||c==0x60||c==0x7C;
    return flyweight::unicode::is_symbol(c);
}

// "\p{N}{1,3}": digits break into groups of at most three.
std::size_t joyai_match_number(const std::vector<std::uint32_t>& code,std::size_t at) {
    std::size_t length=0;
    while(length<3&&at+length<code.size()&&joyai_is_number(code[at+length]))++length;
    return length;
}

// "[一-龥぀-ゟ゠-ヿ]+": CJK ideographs, hiragana and katakana stand apart from
// the Latin-oriented word rule below.
bool joyai_is_cjk(std::uint32_t codepoint) {
    return (codepoint>=0x4E00&&codepoint<=0x9FA5)||
           (codepoint>=0x3040&&codepoint<=0x309F)||
           (codepoint>=0x30A0&&codepoint<=0x30FF);
}

std::size_t joyai_match_cjk(const std::vector<std::uint32_t>& code,std::size_t at) {
    std::size_t length=0;
    while(at+length<code.size()&&joyai_is_cjk(code[at+length]))++length;
    return length;
}

// The third regex, an ordered alternation. Each branch is tried in the order it
// appears in the pattern and the first that matches wins:
//
//   [ASCII punctuation][A-Za-z]+
// | [^\r\n\p{L}\p{P}\p{S}]?[\p{L}\p{M}]+
// | ?[\p{P}\p{S}]+[\r\n]*
// | \s*[\r\n]+
// | \s+(?!\S)
// | \s+
std::size_t joyai_match_word(const std::vector<std::uint32_t>& code,std::size_t at) {
    namespace unicode=flyweight::unicode;
    const auto size=code.size();
    const auto first=code[at];
    auto letter_or_mark=[](std::uint32_t c){return joyai_is_letter(c)||joyai_is_accent_mark(c);};
    auto punct_or_symbol=[](std::uint32_t c){return joyai_is_punctuation(c)||joyai_is_symbol(c);};
    // Every ASCII punctuation and symbol character, spelled out in the pattern
    // as a literal class rather than as \p{P}\p{S}.
    auto ascii_mark=[](std::uint32_t c){
        return (c>=0x21&&c<=0x2F)||(c>=0x3A&&c<=0x40)||(c>=0x5B&&c<=0x60)||(c>=0x7B&&c<=0x7E);
    };
    auto ascii_letter=[](std::uint32_t c){return (c>='A'&&c<='Z')||(c>='a'&&c<='z');};

    if(ascii_mark(first)){
        std::size_t length=1;
        while(at+length<size&&ascii_letter(code[at+length]))++length;
        if(length>1)return length;
    }
    // The optional leading character is greedy, so the branch that consumes it
    // is tried before the branch that does not.
    for(int leading=1;leading>=0;--leading){
        if(leading&&(first=='\r'||first=='\n'||unicode::is_letter(first)||punct_or_symbol(first)))
            continue;
        const std::size_t start=at+static_cast<std::size_t>(leading);
        std::size_t length=0;
        while(start+length<size&&letter_or_mark(code[start+length]))++length;
        if(length)return static_cast<std::size_t>(leading)+length;
    }
    for(int leading=1;leading>=0;--leading){
        if(leading&&first!=' ')continue;
        const std::size_t start=at+static_cast<std::size_t>(leading);
        std::size_t length=0;
        while(start+length<size&&punct_or_symbol(code[start+length]))++length;
        if(!length)continue;
        std::size_t tail=0;
        while(start+length+tail<size&&
              (code[start+length+tail]=='\r'||code[start+length+tail]=='\n'))++tail;
        return static_cast<std::size_t>(leading)+length+tail;
    }
    std::size_t run=0;
    while(at+run<size&&unicode::is_whitespace(code[at+run]))++run;
    if(run){
        // "\s*[\r\n]+" backtracks so the match ends on the last line break in
        // the whitespace run.
        std::size_t through_break=0;
        for(std::size_t index=0;index<run;++index)
            if(code[at+index]=='\r'||code[at+index]=='\n')through_break=index+1;
        if(through_break)return through_break;
        // "\s+(?!\S)" forbids a non-space immediately after the match, so a run
        // that is followed by text hands its last character back to that text.
        const std::size_t trimmed=(at+run<size)?run-1:run;
        if(trimmed)return trimmed;
        return run;  // "\s+"
    }
    return 0;
}

// DeepSeek-V4's `joyai-llm` pre-tokenizer, transcribed from the three regexes
// the reference applies in sequence. Unicode categories come from a generated
// table (unicode_categories.h) rather than the codepoint-range approximation
// the other pre-tokenizers use: this model is CJK-heavy and the pattern draws
// explicit \p{L}/\p{M}/\p{P}/\p{S} distinctions that the approximation blurs.
// The GPT-4o pre-tokenizer, which is what llama.cpp maps the `llama4` pre onto
// and therefore what Muse Glimmer uses. Transcribed from the single regex the
// reference applies, alternative by alternative and in order, because regex
// alternation takes the first branch that matches at each position.
//
// Two details are easy to lose. The case split is ASCII only -- the reference
// spells it `[^a-z]` and `[^A-Z]` behind a `\p{L}` lookahead -- so every
// non-ASCII letter counts as both upper- and lower-ish and letter runs in
// non-Latin scripts stay whole. And digits come in groups of at most three,
// unlike the Laguna pre-tokenizer above where every digit stands alone.
// The llama3 / BailingMoE3 pre-tokenizer, transcribed from the single regex
// the reference applies, alternative by alternative and in order:
//
//   '(?i:[sdmt]|ll|ve|re)
// | [^\r\n\p{L}\p{N}]?+\p{L}+
// | \p{N}
// |  ?[^\s\p{L}\p{N}]++[\r\n]*
// | \s*[\r\n]
// | \s+(?!\S)
// | \s+
//
// It looks close enough to the GPT-4o pattern above to tempt sharing one
// implementation. It is not. Two branches differ, and only one of them is
// obvious:
//
//   * digits stand alone here (`\p{N}`), not in groups of up to three;
//   * and the letter run is plain `\p{L}+` with NO case machinery, where
//     GPT-4o splits an upper-to-lower transition. That second difference is
//     the one that bites: sharing GPT-4o's rule splits "mV" into "m" + "V",
//     which lands them in separate pre-tokens, which means BPE can never apply
//     the (m, V) merge no matter how correct the merge table is. The symptom
//     shows up as a ~2% token mismatch on mixed-case input and nowhere else.
//
// The quantifiers are possessive (`?+`, `++`), so the optional leading
// character in the letter branch is never given back: if it is consumed and no
// letter follows, the branch fails outright rather than retrying without it.
std::vector<std::string> llama_bpe_pretokenize(const std::string& text) {
    const auto decoded=gguf_utf8_decode(text);
    const std::size_t count=decoded.code.size();
    std::vector<std::string> pieces;
    auto at_code=[&](std::size_t index){return decoded.code[index];};
    auto slice=[&](std::size_t from,std::size_t to){
        return text.substr(decoded.offset[from],decoded.offset[to]-decoded.offset[from]);
    };
    auto is_space=[&](std::uint32_t c){return gguf_codepoint_is_space(c);};
    auto is_letter=[&](std::uint32_t c){return joyai_is_letter(c);};
    auto is_number=[&](std::uint32_t c){return joyai_is_number(c);};

    for(std::size_t at=0;at<count;){
        // Alternative 1: an apostrophe plus a contraction tail, either case.
        if(at_code(at)=='\''&&at+1<count){
            auto lower=[&](std::size_t k){
                const auto c=at_code(k);
                return c<128?static_cast<std::uint32_t>(
                    std::tolower(static_cast<int>(c))):c;
            };
            const auto first=lower(at+1);
            std::size_t length=0;
            if(first=='s'||first=='d'||first=='m'||first=='t')length=2;
            else if(at+2<count){
                const auto second=lower(at+2);
                if((first=='l'&&second=='l')||(first=='v'&&second=='e')||
                   (first=='r'&&second=='e'))length=3;
            }
            if(length){pieces.push_back(slice(at,at+length));at+=length;continue;}
        }
        // Alternative 2: an optional leading character that is neither a line
        // break, a letter nor a digit, then a run of letters.
        {
            std::size_t probe=at;
            const auto first=at_code(probe);
            if(first!='\r'&&first!='\n'&&!is_letter(first)&&!is_number(first))++probe;
            if(probe<count&&is_letter(at_code(probe))){
                std::size_t end=probe;
                while(end<count&&is_letter(at_code(end)))++end;
                pieces.push_back(slice(at,end));at=end;continue;
            }
        }
        // Alternative 3: exactly one digit.
        if(is_number(at_code(at))){
            pieces.push_back(slice(at,at+1));at+=1;continue;
        }
        // Alternative 4: an optional single leading space, then a run of
        // characters that are neither whitespace, letters nor digits, then any
        // trailing line breaks.
        {
            auto plain=[&](std::size_t index){
                const auto c=at_code(index);
                return !is_space(c)&&!is_letter(c)&&!is_number(c);
            };
            std::size_t probe=at;
            if(at_code(probe)==' '&&probe+1<count&&plain(probe+1))++probe;
            if(probe<count&&plain(probe)){
                std::size_t end=probe;
                while(end<count&&plain(end))++end;
                while(end<count&&(at_code(end)=='\r'||at_code(end)=='\n'))++end;
                pieces.push_back(slice(at,end));at=end;continue;
            }
        }
        // Alternative 5: `\s*[\r\n]`. The `\s*` is greedy and backtracks, so
        // this reaches the LAST line break in the whitespace run, not the
        // first -- `\s*` happily consumes line breaks on the way there. Taking
        // the first instead splits "\r\r" into two pieces, which stops BPE from
        // ever applying the (\r, \r) merge.
        {
            std::size_t end=at,last_break=count;
            while(end<count&&is_space(at_code(end))){
                if(at_code(end)=='\r'||at_code(end)=='\n')last_break=end;
                ++end;
            }
            if(last_break!=count){
                pieces.push_back(slice(at,last_break+1));at=last_break+1;continue;
            }
        }
        // Alternatives 6 and 7: a whitespace run. The lookahead in `\s+(?!\S)`
        // makes the run stop one short when a non-space follows, leaving that
        // last space to open the next piece.
        if(is_space(at_code(at))){
            std::size_t end=at;
            while(end<count&&is_space(at_code(end)))++end;
            if(end<count&&end>at+1)--end;
            pieces.push_back(slice(at,end));at=end;continue;
        }
        pieces.push_back(slice(at,at+1));at+=1;
    }
    return pieces;
}

std::vector<std::string> gpt4o_pretokenize(const std::string& text,
                                          std::size_t max_digit_run) {
    const auto decoded=gguf_utf8_decode(text);
    const std::size_t count=decoded.code.size();
    std::vector<std::string> pieces;
    auto at_code=[&](std::size_t index){return decoded.code[index];};
    auto slice=[&](std::size_t from,std::size_t to){
        return text.substr(decoded.offset[from],decoded.offset[to]-decoded.offset[from]);
    };
    auto is_space=[&](std::uint32_t c){return gguf_codepoint_is_space(c);};
    auto upperish=[&](std::uint32_t c){return joyai_is_letter(c)&&!(c>='a'&&c<='z');};
    auto lowerish=[&](std::uint32_t c){return joyai_is_letter(c)&&!(c>='A'&&c<='Z');};
    // 's 't 're 've 'm 'll 'd, either case.
    auto contraction=[&](std::size_t index)->std::size_t{
        if(index>=count||at_code(index)!='\'')return 0;
        auto lower=[&](std::size_t k){
            const auto c=at_code(k);
            return c<128?static_cast<std::uint32_t>(
                std::tolower(static_cast<int>(c))):c;
        };
        if(index+1>=count)return 0;
        const auto first=lower(index+1);
        if(first=='s'||first=='t'||first=='m'||first=='d')return 2;
        if(index+2<count){
            const auto second=lower(index+2);
            if((first=='r'&&second=='e')||(first=='v'&&second=='e')||
               (first=='l'&&second=='l'))return 3;
        }
        return 0;
    };
    for(std::size_t at=0;at<count;){
        // Alternatives 1 and 2: an optional single leading character that is
        // neither a line break, a letter nor a digit, then a letter run. The
        // first alternative wants upper-ish* lower-ish+, the second upper-ish+
        // lower-ish*; taken greedily in that order they accept any letter run,
        // which is why one pass over the two loops covers both.
        {
            std::size_t probe=at;
            if(!joyai_is_letter(at_code(probe))&&!joyai_is_number(at_code(probe))&&
               at_code(probe)!='\r'&&at_code(probe)!='\n'&&
               probe+1<count&&joyai_is_letter(at_code(probe+1)))
                ++probe;
            if(probe<count&&joyai_is_letter(at_code(probe))){
                std::size_t end=probe;
                while(end<count&&upperish(at_code(end)))++end;
                while(end<count&&lowerish(at_code(end)))++end;
                if(end>probe){
                    end+=contraction(end);
                    pieces.push_back(slice(at,end));at=end;continue;
                }
            }
        }
        // Alternative 3: a bounded digit run. GPT-4o spells this \p{N}{1,3};
        // the llama3/BailingMoE3 variant of the same pattern spells it \p{N},
        // i.e. every digit alone. That single character is the only difference
        // between the two regexes, so the run length is a parameter rather
        // than a second transcription.
        if(joyai_is_number(at_code(at))){
            std::size_t end=at;
            while(end<count&&end-at<max_digit_run&&joyai_is_number(at_code(end)))++end;
            pieces.push_back(slice(at,end));at=end;continue;
        }
        // Alternative 4: an optional single leading space, then a run of
        // characters that are neither whitespace, letters nor digits.
        {
            auto plain=[&](std::size_t index){
                const auto c=at_code(index);
                return !is_space(c)&&!joyai_is_letter(c)&&!joyai_is_number(c);
            };
            std::size_t probe=at;
            if(at_code(probe)==' '&&probe+1<count&&plain(probe+1))++probe;
            if(probe<count&&plain(probe)){
                std::size_t end=probe;
                while(end<count&&plain(end))++end;
                while(end<count&&(at_code(end)=='\r'||at_code(end)=='\n'||
                                  at_code(end)=='/'))++end;
                pieces.push_back(slice(at,end));at=end;continue;
            }
        }
        // Alternatives 5 to 7 are all whitespace runs, and the order between
        // them is what gives a following word its single leading space.
        if(is_space(at_code(at))){
            std::size_t run=at;
            while(run<count&&is_space(at_code(run)))++run;
            // 5: "\s*[\r\n]+", greedy, so the piece ends at the last line break
            // in the run and any trailing spaces start a new piece.
            std::size_t last_break=run;
            while(last_break>at&&at_code(last_break-1)!='\r'&&
                  at_code(last_break-1)!='\n')--last_break;
            if(last_break>at){pieces.push_back(slice(at,last_break));at=last_break;continue;}
            // 6: "\s+(?!\S)". Mid-text the run gives up its last character to
            // the word that follows; at the end of the text it keeps all of it.
            const std::size_t end=run<count?run-1:run;
            if(end>at){pieces.push_back(slice(at,end));at=end;continue;}
            // 7: "\s+" on whatever is left, which is a lone separator.
            pieces.push_back(slice(at,run));at=run;continue;
        }
        // Unreachable for well-formed input, but the scan must still advance.
        pieces.push_back(slice(at,at+1));at=at+1;
    }
    return pieces;
}

std::vector<std::string> deepseek4_pretokenize(const std::string& text) {
    std::vector<std::string> pieces{text};
    pieces=gguf_regex_split(pieces,joyai_match_number);
    pieces=gguf_regex_split(pieces,joyai_match_cjk);
    pieces=gguf_regex_split(pieces,joyai_match_word);
    return pieces;
}

// BPE over one pre-tokenized piece, appending its ids to `result`.
void gguf_bpe_piece(const FlyweightV2Model& m, const std::string& piece,
                    std::vector<uint32_t>& result) {
    const std::string encoded=gguf_byte_encode(piece.c_str());
    auto symbols=gguf_utf8_symbols(encoded);
    if(symbols.empty())return;
    const auto&ranks=m.merge_ranks;
    struct Candidate{int rank;int left;int right;};
    struct Later{bool operator()(const Candidate&a,const Candidate&b)const{return a.rank!=b.rank?a.rank>b.rank:a.left>b.left;}};
    std::vector<int> next(symbols.size()),previous(symbols.size());
    std::vector<bool> alive(symbols.size(),true);
    for(size_t i=0;i<symbols.size();i++){next[i]=static_cast<int>(i)+1;previous[i]=static_cast<int>(i)-1;}
    next[symbols.size()-1]=-1;
    std::priority_queue<Candidate,std::vector<Candidate>,Later> queue;
    auto propose=[&](int left){
        if(left<0)return;
        const int right=next[left];
        if(right<0)return;
        const auto it=ranks.find(symbols[left]+" "+symbols[right]);
        if(it!=ranks.end())queue.push({it->second,left,right});
    };
    for(size_t i=0;i+1<symbols.size();i++)propose(static_cast<int>(i));
    while(!queue.empty()){
        const auto candidate=queue.top();queue.pop();
        const int left=candidate.left,right=candidate.right;
        if(!alive[left]||!alive[right]||next[left]!=right)continue;
        const auto it=ranks.find(symbols[left]+" "+symbols[right]);
        if(it==ranks.end()||it->second!=candidate.rank)continue;
        symbols[left]+=symbols[right];
        alive[right]=false;
        next[left]=next[right];
        if(next[left]>=0)previous[next[left]]=left;
        propose(previous[left]);
        propose(left);
    }
    for(int index=0;index>=0;index=next[index]){
        const auto it=m.vocabulary_ids.find(symbols[index]);
        result.push_back(it==m.vocabulary_ids.end()?0:it->second);
    }
}

// The pre-tokenizer a checkpoint asks for by name. Anything unlisted keeps the
// historical behaviour of no pre-tokenization at all, which is what the Qwen
// checkpoints have always run.
std::vector<std::string> gguf_pretokenize(const FlyweightV2Model& m,
                                          const std::string& text) {
    if(m.tokenizer_pre=="joyai-llm")return deepseek4_pretokenize(text);
    if(m.tokenizer_pre=="llama4")return gpt4o_pretokenize(text,3);
    if(m.tokenizer_pre=="llama-bpe")return llama_bpe_pretokenize(text);
    return laguna_pretokenize(text);
}

// Pre-tokenizer boundaries, so the split can be checked against the reference
// patterns without a vocabulary in the way. Writes the byte offset each piece
// starts at plus a trailing end offset, so `count` is one more than the number
// of pieces. A null `offsets` asks for the count alone.
int flyweight_v2_pretokenize(const FlyweightV2Model*m,const char*text,uint64_t*offsets,uint64_t capacity,uint64_t*count){return guarded([&]{
    if(!m||!text||!count)throw std::runtime_error("invalid pretokenize arguments");
    const std::string input(text);
    const auto pieces=gguf_pretokenize(*m,input);
    *count=pieces.size()+1;
    if(!offsets)return 0;
    if(capacity<*count)throw std::runtime_error("pretokenize output buffer is too small");
    std::uint64_t at=0,index=0;
    for(const auto& piece:pieces){offsets[index++]=at;at+=piece.size();}
    offsets[index]=at;
    return 0;});}

int flyweight_v2_tokenize(const FlyweightV2Model*m,const char*text,uint32_t*tokens,uint64_t capacity,uint64_t*count){return guarded([&]{
    if(!m||!text||!count)throw std::runtime_error("invalid tokenize arguments");
    // Muse Glimmer's chat format is built entirely out of control tokens
    // (<|start|>, <|message|>, <|eom|>, <|eot|>), and BPE would happily shred
    // them into ordinary text, so it takes the exact-match split too.
    if(m->tokenizer_pre=="laguna"||m->tokenizer_pre=="joyai-llm"||
       m->tokenizer_pre=="llama4"||m->tokenizer_pre=="llama-bpe"){
        // Control tokens are split out by exact match first: they are ordinary
        // text to BPE, and Laguna spells them with characters whose merges would
        // never reassemble the single reserved id.
        std::vector<uint32_t> result;
        const std::string input(text);
        size_t at=0,plain=0;
        auto flush=[&](size_t end){
            if(end<=plain)return;
            const auto segment=input.substr(plain,end-plain);
            for(const auto& piece:gguf_pretokenize(*m,segment))
                gguf_bpe_piece(*m,piece,result);
        };
        while(at<input.size()){
            const auto* match=static_cast<const std::pair<std::string,std::uint32_t>*>(nullptr);
            for(const auto& control:m->control_tokens)
                if(input.compare(at,control.first.size(),control.first)==0){match=&control;break;}
            if(!match){++at;continue;}
            flush(at);
            result.push_back(match->second);
            at+=match->first.size();
            plain=at;
        }
        flush(input.size());
        *count=result.size();
        if(capacity<result.size()||!tokens)throw std::runtime_error("token output buffer is too small");
        std::copy(result.begin(),result.end(),tokens);
        return 0;
    }
    const std::string encoded=gguf_byte_encode(text);
    auto symbols=gguf_utf8_symbols(encoded);
    // Greedy BPE with the same semantics as a full rescan per merge (lowest
    // merge rank first, leftmost pair on ties) but heap-driven over a linked
    // list: O(n log n). The naive rescan was O(n^2) and took minutes on the
    // ~100KB system prompts agentic clients send.
    const auto&ranks=m->merge_ranks;
    struct Candidate{int rank;int left;int right;};
    struct Later{bool operator()(const Candidate&a,const Candidate&b)const{return a.rank!=b.rank?a.rank>b.rank:a.left>b.left;}};
    std::vector<int> next(symbols.size()),previous(symbols.size());
    std::vector<bool> alive(symbols.size(),true);
    for(size_t i=0;i<symbols.size();i++){next[i]=static_cast<int>(i)+1;previous[i]=static_cast<int>(i)-1;}
    if(!symbols.empty())next[symbols.size()-1]=-1;
    std::priority_queue<Candidate,std::vector<Candidate>,Later> queue;
    auto propose=[&](int left){
        if(left<0)return;
        const int right=next[left];
        if(right<0)return;
        const auto it=ranks.find(symbols[left]+" "+symbols[right]);
        if(it!=ranks.end())queue.push({it->second,left,right});
    };
    for(size_t i=0;i+1<symbols.size();i++)propose(static_cast<int>(i));
    while(!queue.empty()){
        const auto candidate=queue.top();queue.pop();
        const int left=candidate.left,right=candidate.right;
        if(!alive[left]||!alive[right]||next[left]!=right)continue;
        // Stale heap entries survive after a neighbor merged; the pair text
        // may have changed, so re-validate its rank before applying.
        const auto it=ranks.find(symbols[left]+" "+symbols[right]);
        if(it==ranks.end()||it->second!=candidate.rank)continue;
        symbols[left]+=symbols[right];
        alive[right]=false;
        next[left]=next[right];
        if(next[left]>=0)previous[next[left]]=left;
        propose(previous[left]);
        propose(left);
    }
    std::vector<uint32_t> result;
    for(int index=symbols.empty()?-1:0;index>=0;index=next[index]){
        const auto it=m->vocabulary_ids.find(symbols[index]);
        result.push_back(it==m->vocabulary_ids.end()?0:it->second);
    }
    *count=result.size();
    if(capacity<result.size()||!tokens)throw std::runtime_error("token output buffer is too small");
    std::copy(result.begin(),result.end(),tokens);
    return 0;
});}
int flyweight_v2_tensor_read(const FlyweightV2Model* m,uint64_t i,void* dst,uint64_t bytes){return guarded([&]{if(!m||m->read_tensor(i,dst,bytes)!=0)throw std::runtime_error("invalid tensor read");return 0;});}
int flyweight_v2_tensor_read_slice(const FlyweightV2Model*m,uint64_t i,uint64_t offset,void*dst,uint64_t bytes){return guarded([&]{if(!m||!dst||i>=m->tensors.size())throw std::runtime_error("invalid tensor slice read");const auto&t=m->tensors[i];if(offset>t.size||bytes>t.size-offset)throw std::runtime_error("tensor slice is out of bounds");std::memcpy(dst,tensor_data(*m,t)+offset,static_cast<size_t>(bytes));return 0;});}
int flyweight_v2_tensor_view(const FlyweightV2Model*m,uint64_t i,uint64_t offset,uint64_t bytes,const void**out){return guarded([&]{if(!m||!out||i>=m->tensors.size())throw std::runtime_error("invalid tensor view");const auto&t=m->tensors[i];if(offset>t.size||bytes>t.size-offset)throw std::runtime_error("tensor view is out of bounds");*out=tensor_data(*m,t)+offset;return 0;});}
int flyweight_v2_qwen_runtime_create(FlyweightV2Model*m,const FlyweightV2QwenRuntimeOptions*options,FlyweightV2QwenRuntime**out){return guarded([&]{
    if(!m||!out)throw std::runtime_error("model and runtime output are required");
    const bool gemma4=m->config.architecture=="gemma4";
    const bool laguna=m->config.architecture=="laguna";
    const bool muse=m->config.architecture=="muse-glimmer";
    const bool qwen4exp=m->config.architecture=="qwen4exp";
    // DeepSeek-V4 loads and describes itself but has no execution path yet, so
    // it gets its own message rather than looking like an unknown format.
    if(m->config.architecture=="deepseek4")throw std::runtime_error(
        "deepseek4 checkpoints load and report their configuration, but the native runtime "
        "cannot execute them yet (no hyper-connection, compressed-attention or indexer path)");
    if(m->config.architecture.find("qwen")!=0&&!gemma4&&!laguna&&!muse)throw std::runtime_error("native runtime supports Qwen, Gemma 4, Laguna and Muse Glimmer models");
    // Dense checkpoints report no experts at all, so only require a routing
    // width from the ones that actually route.
    const bool dense_ffn=has_tensor(*m,"blk.0.ffn_gate.weight");
    if(!m->config.hidden_size||!m->config.layer_count)throw std::runtime_error("Qwen runtime config is incomplete");
    if(!dense_ffn&&(!m->config.expert_count||!m->config.expert_used_count))throw std::runtime_error("Qwen runtime config is incomplete");
    auto runtime=std::make_unique<FlyweightV2QwenRuntime>();
    runtime->model=m;
    runtime->options=options?*options:FlyweightV2QwenRuntimeOptions{};
    if(runtime->options.device<0)throw std::runtime_error("Qwen runtime device must be non-negative");
    if(!flyweight::v2::valid_expert_execution_mode(runtime->options.moe_device))
        throw std::runtime_error("Qwen runtime MoE device is invalid");
    runtime->expert_mode=
        flyweight::v2::expert_execution_mode(runtime->options.moe_device);
    // Every expert placement other than `cpu` exists to divide work between a
    // host and a device. With no device there is nothing to divide, and the
    // resident/hybrid/auto paths degrade into copying expert weights from the
    // GGUF mapping into another host buffer before computing on them -- 13x
    // slower than running them in place. `auto` in particular is the serving
    // default, so leaving this to the caller means the default configuration is
    // the pathological one.
    if(flyweight_backend_is_cpu()&&
       runtime->expert_mode!=flyweight::v2::ExpertExecutionMode::cpu){
        std::fprintf(stderr,
            "[flyweight] expert placement forced to CPU: the CPU backend has "
            "no device to page experts to\n");
        runtime->expert_mode=flyweight::v2::ExpertExecutionMode::cpu;
        runtime->options.moe_device=
            flyweight::v2::expert_execution_mode_value(runtime->expert_mode);
    }
    if(runtime->options.mtp_drafts>8)throw std::runtime_error("native Qwen MTP supports at most 8 drafts");
    // qwen4exp MTP runs in stream space end to end (draft, cache replay,
    // verify hand-over); see plans/qwen4exp-semantics.md. A checkpoint with no
    // draft block -- UD-IQ1_S is one -- trips the generic check below.
    if(gemma4&&runtime->options.mtp_drafts)throw std::runtime_error("native Gemma 4 MTP is not implemented");
    if(gemma4&&qwen_expert_policy(
            *runtime,flyweight::v2::ExpertExecutionPhase::prepare
        ).is_streamed_gpu())
        throw std::runtime_error("native Gemma 4 supports --moe-device cpu or hybrid");
    if(laguna&&runtime->options.mtp_drafts)throw std::runtime_error("native Laguna MTP is not implemented");
    // Muse Glimmer's speculative drafter is a separate DFlash checkpoint rather
    // than in-model MTP heads, so there is nothing here to draft with.
    if(muse&&runtime->options.mtp_drafts)throw std::runtime_error("native Muse Glimmer MTP is not implemented");
    if(gemma4&&m->config.per_layer_embedding_size)throw std::runtime_error("native Gemma 4 per-layer embeddings are not implemented");
    if(gemma4&&m->config.shared_kv_layers)throw std::runtime_error("native Gemma 4 shared-KV tail layers are not implemented");
    if(runtime->options.expert_top_k>m->config.expert_used_count)throw std::runtime_error("native Qwen expert_top_k cannot exceed the model's trained expert_used_count");
    if(runtime->options.expert_top_p<0.0f||runtime->options.expert_top_p>1.0f)throw std::runtime_error("native Qwen expert_top_p must be within [0, 1]");
    if(runtime->options.prefill_cache_seed>256)throw std::runtime_error("native Qwen prefill_cache_seed supports at most 256 experts per layer");
    if(runtime->options.cpu_prefetch_auto>1)throw std::runtime_error("native Qwen cpu_prefetch_auto must be boolean");
    if(runtime->options.cpu_prefetch_mib&&runtime->options.cpu_prefetch_auto)throw std::runtime_error("native Qwen CPU prefetch modes are mutually exclusive");
    if(runtime->options.next_layer_prefetch>64)throw std::runtime_error("native Qwen next-layer prefetch supports at most 64 experts");
    if(runtime->options.hybrid_prefill_cpu>1)
        throw std::runtime_error("native Qwen hybrid prefill policy is invalid");
    if(runtime->options.immutable_residency>1)
        throw std::runtime_error("native Qwen expert residency policy is invalid");
    if(runtime->options.prefill_cache_seed_auto>1)
        throw std::runtime_error("native Qwen prefill cache seed auto policy is invalid");
    if(runtime->options.strict_resident>1)
        throw std::runtime_error("native Qwen strict resident policy is invalid");
    if(runtime->options.dense_requant>2)
        throw std::runtime_error("native Qwen dense requant policy is invalid");
    if(runtime->options.strict_resident&&
       runtime->expert_mode!=flyweight::v2::ExpertExecutionMode::streamed_gpu)
        throw std::runtime_error(
            "native Qwen strict resident policy requires streamed GPU execution");
     if(gemma4&&runtime->options.next_layer_prefetch)throw std::runtime_error("native Gemma 4 next-layer prefetch is not implemented");
     if(runtime->options.next_layer_prefetch&&runtime->options.mtp_drafts)throw std::runtime_error("native Qwen next-layer prefetch does not support MTP yet");
    if(runtime->options.cache_type_k<0||runtime->options.cache_type_k>6)throw std::runtime_error("native Qwen cache_type_k must be 0 (f32), 1 (f16), 2 (bf16), 3 (q8_0), 4 (turbo3), 5 (turbo4), or 6 (auto)");
    if(runtime->options.cache_type_v<0||runtime->options.cache_type_v>6)throw std::runtime_error("native Qwen cache_type_v must be 0 (f32), 1 (f16), 2 (bf16), 3 (q8_0), 4 (turbo3), 5 (turbo4), or 6 (auto)");
    if(!runtime->options.context_limit)runtime->options.context_limit=m->config.context_length?m->config.context_length:4096;
    if(gemma4)build_gemma4_plan(*runtime);
    else if(laguna)build_laguna_plan(*runtime);
    else if(muse)build_muse_glimmer_plan(*runtime);
    else if(qwen4exp)build_qwen4exp_plan(*runtime);
    else build_qwen_plan(*runtime);
    // qwen4exp: the flag gates the gated-residual bookends in the forward
    // paths, and the graph-capture sites refuse to capture on it at bring-up
    // (the delta capture would bake in the plain residual add the bookends
    // replace; a replay would corrupt the streams silently).
    runtime->qwen4exp=qwen4exp;
    // Resolve cache type `auto`. Must run after the layer plan, which is what
    // supplies head_dim; the rule and its measurements live in
    // flyweight::v2::attention so they can be pinned by a contract test.
    if(runtime->options.cache_type_k==flyweight::v2::attention::kCacheTypeAuto||
       runtime->options.cache_type_v==flyweight::v2::attention::kCacheTypeAuto){
        bool head_dim_ok=true;
        for(const auto&layer:runtime->layers){
            if(!layer.attention)continue;
            if(!flyweight::v2::attention::turbo_head_dim_ok(layer.head_dim)){
                head_dim_ok=false;break;
            }
        }
        const auto resolved=flyweight::v2::attention::auto_cache_type(
            runtime->options.context_limit,
            runtime->model->config.expert_count>0,
            head_dim_ok);
        if(runtime->options.cache_type_k==flyweight::v2::attention::kCacheTypeAuto)
            runtime->options.cache_type_k=resolved;
        if(runtime->options.cache_type_v==flyweight::v2::attention::kCacheTypeAuto)
            runtime->options.cache_type_v=resolved;
    }
    if(runtime->options.next_layer_prefetch&&runtime->layers.size()>1){
        const auto experts=static_cast<std::size_t>(m->config.expert_count);
        runtime->expert_transitions.resize(
            (runtime->layers.size()-1)*experts*experts
        );
    }
    if(runtime->options.mtp_drafts&&!runtime->mtp_available)throw std::runtime_error("native Qwen MTP was requested but the model has no draft block");
    // Prompt tokens are processed through the batched rows forward in chunks
    // of this size. Bigger chunks amortize expert weight reads further (the
    // CPU MoE reads each routed expert once per chunk); the cost is ~200MB
    // of workspace + pinned staging at 1024. 0 or 1 falls back to
    // one-token-at-a-time decode.
    // Dense low-bit checkpoints need a much wider gate/up scratch than MoE,
    // and their quantized row kernels amortize weights in small token tiles.
    // A 1024-row allocation consumes ~633 MiB for Qwen3.6-27B and can evict
    // several whole FFN blocks to the CPU, which decode pays for on every
    // token. That is what held this at 64, but the residency penalty does not
    // actually bite until well past it: the workspace grows ~0.7 MiB per row,
    // and on a 12 GiB card measured decode 32.20 / 32.10 / 32.06 / 30.83 tok/s
    // at 64 / 128 / 256 / 384 rows. Prefill over the same sweep went 337 / 353
    // / 366 / 372. 256 is where the curves cross: +8.6% prefill for -0.4%
    // decode, against 384's +10.4% for -4.3%.
    // Gemma 4 chunks through its own layer-synchronous rows driver; 256 rows
    // spread each layer's routed-expert reads over enough tokens that the CPU
    // MoE stops being the prefill wall, without sizing the row scratch past
    // what its 8192-wide global-attention projections already need.
    // Laguna has a rows forward too, but its leading dense block is
    // 12x the expert width, so the row scratch is far larger per token than a
    // pure-MoE Qwen checkpoint's; 256 keeps the batch worth batching without
    // sizing every scratch region for a 1024-row dense SwiGLU.
    // Muse Glimmer is dense, so it batches like the other dense checkpoints.
    runtime->prefill_rows=gemma4?256:(laguna?256:(dense_ffn?256:1024));
    if(const char*env=std::getenv("FLYWEIGHT_PREFILL_ROWS")){
        const long value=std::strtol(env,nullptr,10);
        runtime->prefill_rows=static_cast<std::uint32_t>(std::clamp<long>(value,0,4096));
    }
    // Mid-prefill recurrent-state checkpoints let a follow-up request whose
    // prompt diverges mid-stream (agentic clients mutate the prefix: injected
    // reminders, re-rendered tool calls) resume from the nearest checkpoint <=
    // the divergence point instead of reprefilling the whole prompt. `interval`
    // is the position of the first checkpoint; the rest are geometric
    // (interval<<k) so early coverage (the stable system+tools prefix) is dense.
    // `slots` is the total snapshot pool (one reserved for the exact
    // end-of-prompt snapshot). interval=0 disables mid checkpoints (end
    // snapshots only, legacy behavior); slots=0 falls back to the default.
    runtime->prefill_checkpoint_interval=runtime->options.prefill_checkpoint_interval;
    const std::size_t checkpoint_slots=runtime->options.prefill_checkpoint_slots
        ? std::clamp<std::size_t>(runtime->options.prefill_checkpoint_slots,1,256):4;
    runtime->prefill_snapshots.assign(checkpoint_slots,QwenPrefillSnapshot{});
    // Independent decode slots (llama.cpp --parallel): 0/1 = single-sequence.
    runtime->parallel_sequences=std::clamp<std::uint32_t>(
        runtime->options.parallel_sequences?runtime->options.parallel_sequences:1,1,16);
    // A scratch size for the slots past the first. Refusing the nonsensical
    // settings here beats laying out a slot nothing can be routed to: a
    // scratch slot bigger than the context is the full slot under another
    // name, and one asked for without a second slot to apply it to is a
    // request the operator will read as having taken effect.
    if(runtime->options.scratch_context){
        if(runtime->options.scratch_context>runtime->options.context_limit)
            throw std::runtime_error(
                "native Qwen scratch context ("+std::to_string(runtime->options.scratch_context)
                +") exceeds the context window ("+std::to_string(runtime->options.context_limit)+")");
        if(runtime->parallel_sequences<2)
            throw std::runtime_error(
                "native Qwen scratch context needs --parallel 2 or more; with a single slot "
                "there are no side slots to shrink");
        if(runtime->options.mtp_drafts)
            throw std::runtime_error(
                "native Qwen scratch context is unavailable under MTP drafting, which runs "
                "one slot");
    }
    // Host RAM budget for the spilled-slot prompt cache. UINT32_MAX selects a
    // conservative automatic budget: one eighth of currently available RAM,
    // capped at llama-server's practical 8 GiB default. This leaves ample
    // headroom for the model mapping, filesystem cache, and request buffers.
    if(runtime->options.prompt_cache_mib==std::numeric_limits<std::uint32_t>::max()){
        const auto available=available_host_memory();
        runtime->host_cache_limit_bytes=std::min<std::uint64_t>(
            8ull*1024*1024*1024,available/8
        );
        runtime->host_cache_limit_bytes=
            (runtime->host_cache_limit_bytes/(1024*1024))*(1024*1024);
    }else{
        runtime->host_cache_limit_bytes=
            static_cast<std::uint64_t>(runtime->options.prompt_cache_mib)*1024ull*1024;
    }
    if(runtime->host_cache_limit_bytes)
        std::fprintf(stderr,"[flyweight] prompt cache budget %.1f GiB%s\n",
            runtime->host_cache_limit_bytes/static_cast<double>(1024ull*1024*1024),
            runtime->options.prompt_cache_mib==std::numeric_limits<std::uint32_t>::max()
                ? " (auto)":"");
    runtime->cuda_ready=false;
    runtime->decode_ready=false;
    *out=runtime.release();
    return 0;
});}
void flyweight_v2_qwen_runtime_destroy(FlyweightV2QwenRuntime*runtime){try{if(runtime)release_qwen_device(*runtime);delete runtime;}catch(...){}}
int flyweight_v2_qwen_runtime_info(const FlyweightV2QwenRuntime*runtime,FlyweightV2QwenRuntimeInfo*out){return guarded([&]{
    if(!runtime||!out)throw std::runtime_error("invalid Qwen runtime info handle");
    std::memset(out,0,sizeof(*out));
    out->layers=static_cast<uint32_t>(runtime->layers.size());
    out->attention_layers=static_cast<uint32_t>(std::count_if(runtime->layers.begin(),runtime->layers.end(),[](const QwenLayerPlan&layer){return layer.attention;}));
    out->swa_layers=static_cast<uint32_t>(std::count_if(runtime->layers.begin(),runtime->layers.end(),[](const QwenLayerPlan&layer){return layer.attention_window!=0;}));
    out->sliding_window=runtime->model->config.sliding_window;
    out->swa_full=runtime->options.swa_full?1:0;
    out->deltanet_layers=out->layers-out->attention_layers;
    out->hidden_size=runtime->model->config.hidden_size;
    out->expert_count=runtime->model->config.expert_count;
    out->expert_used_count=runtime->model->config.expert_used_count;
    out->mtp_available=runtime->mtp_available?1:0;
    out->mtp_enabled=runtime->options.mtp_drafts?1:0;
    out->mtp_drafts=runtime->options.mtp_drafts;
    out->mtp_layer=runtime->mtp_available?runtime->model->mtp_layer:std::numeric_limits<std::uint32_t>::max();
    out->context_limit=runtime->options.context_limit;
    out->resolved_cache_type_k=runtime->options.cache_type_k;
    out->resolved_cache_type_v=runtime->options.cache_type_v;
    out->static_tensor_bytes=runtime->static_tensor_bytes;
    out->expert_tensor_bytes=runtime->expert_tensor_bytes;
    const std::uint64_t sequence_slots=std::max<std::size_t>(1,runtime->sequences.size());
    out->gpu_allocated_bytes=runtime->static_arena_bytes+runtime->workspace_bytes+
        runtime->slots_state_bytes+runtime->expert_staging_bytes+
        runtime->expert_cache_bytes+runtime->expert_native_cache_bytes+
        sequence_slots*runtime->prefill_snapshots.size()*
            runtime->prefill_snapshot_bytes;
    out->workspace_bytes=runtime->workspace_bytes;
    out->state_bytes=runtime->state_bytes;
    out->expert_staging_bytes=runtime->expert_staging_bytes;
    out->expert_cache_bytes=runtime->expert_cache_bytes;
    out->expert_cache_slots=runtime->expert_slots.size();
    out->expert_cache_hits=runtime->expert_cache_hits;
    out->expert_cache_misses=runtime->expert_cache_misses;
    out->expert_cache_evictions=runtime->expert_cache_evictions;
    out->expert_cache_admissions=runtime->expert_cache_admissions;
    out->expert_cache_rejections=runtime->expert_cache_rejections;
    out->expert_cache_prompt_bypasses=runtime->expert_cache_prompt_bypasses;
    out->prefix_cache_hits=runtime->prefix_cache_hits;
    out->prefix_cache_misses=runtime->prefix_cache_misses;
    out->prefix_cache_reused_tokens=runtime->prefix_cache_reused_tokens;
    out->prefix_cache_reprefilled_tokens=runtime->prefix_cache_reprefilled_tokens;
    out->prefix_cache_last_prompt_tokens=runtime->prefix_cache_last_prompt_tokens;
    out->prefix_cache_last_reused_tokens=runtime->prefix_cache_last_reused_tokens;
    out->prefix_cache_last_lcp_live=runtime->prefix_cache_last_lcp_live;
    out->prefix_cache_last_lcp_snapshot=runtime->prefix_cache_last_lcp_snapshot;
    out->prompt_cache_entries=runtime->host_prompts.size();
    out->prompt_cache_used_bytes=runtime->host_cache_used_bytes;
    out->mtp_tensor_bytes=runtime->mtp_tensor_bytes;
    out->mtp_draft_tokens=runtime->mtp_draft_tokens;
    out->mtp_accepted_tokens=runtime->mtp_accepted_tokens;
    out->mtp_rejected_tokens=runtime->mtp_rejected_tokens;
    out->mtp_draft_nanoseconds=runtime->mtp_draft_nanoseconds;
    out->mtp_verify_nanoseconds=runtime->mtp_verify_nanoseconds;
    out->mtp_rollback_nanoseconds=runtime->mtp_rollback_nanoseconds;
    out->decode_calls=runtime->decode_calls;
    out->decode_nanoseconds=runtime->decode_nanoseconds;
    out->route_wait_nanoseconds=runtime->route_wait_nanoseconds;
    out->expert_page_nanoseconds=runtime->expert_page_nanoseconds;
    out->tail_wait_nanoseconds=runtime->tail_wait_nanoseconds;
    out->position=runtime->position;
    out->device=runtime->options.device;
    out->moe_device=
        flyweight::v2::expert_execution_mode_value(runtime->expert_mode);
    out->cuda_ready=runtime->cuda_ready?1:0;
    out->decode_ready=runtime->decode_ready?1:0;
    out->route_expert_sum=runtime->route_expert_sum;
    out->expert_compute_nanoseconds=runtime->expert_compute_nanoseconds;
    out->prefill_cache_seeded_experts=runtime->prefill_cache_seeded_experts;
    out->prefill_cache_seed_nanoseconds=runtime->prefill_cache_seed_nanoseconds;
    out->direct_paging=runtime->dma_paging?1:0;
    out->paging_registration_nanoseconds=runtime->paging_registration_nanoseconds;
    out->host_available_bytes=runtime->host_available_bytes;
    out->cpu_prefetch_experts=runtime->cpu_prefetch_experts;
    out->cpu_prefetch_bytes=runtime->cpu_prefetch_bytes;
    out->cpu_prefetch_nanoseconds=runtime->cpu_prefetch_nanoseconds;
    out->cpu_prefetch_pages=runtime->cpu_prefetch_pages;
    out->cpu_prefetch_cold_pages=runtime->cpu_prefetch_cold_pages;
    out->cpu_prefetch_loaded_pages=runtime->cpu_prefetch_loaded_pages;
    out->cpu_prefetch_auto_skips=runtime->cpu_prefetch_auto_skips;
    out->cpu_prefetch_last_budget_bytes=runtime->cpu_prefetch_last_budget_bytes;
    out->prefill_calls=runtime->prefill_calls;
    out->prefill_tokens=runtime->prefill_tokens;
    out->prefill_nanoseconds=runtime->prefill_nanoseconds;
    out->prefill_route_wait_nanoseconds=runtime->prefill_route_wait_nanoseconds;
    out->prefill_expert_nanoseconds=runtime->prefill_expert_nanoseconds;
    out->prefill_direct_quant=qwen_prefill_direct_quant_enabled(*runtime);
    out->prefill_direct_quant_width=out->prefill_direct_quant?
        ((flyweight_cpu_features()&2u)!=0?8:4):0;
    out->prefill_profile=runtime->prefill_profile?1:0;
    out->prefill_gpu_core_nanoseconds=runtime->prefill_gpu_core_nanoseconds;
    out->prefill_gpu_router_nanoseconds=runtime->prefill_gpu_router_nanoseconds;
    out->prefill_gpu_transfer_nanoseconds=runtime->prefill_gpu_transfer_nanoseconds;
    out->prefill_gpu_split_layers=runtime->prefill_gpu_split_layers;
    out->prefill_ple_nanoseconds=runtime->prefill_ple_nanoseconds;
    out->expert_history_loaded_entries=runtime->expert_history_loaded_entries;
    out->expert_history_saves=runtime->expert_history_saves;
    out->next_layer_prefetch_predictions=runtime->next_layer_prefetch_predictions;
    out->next_layer_prefetch_hits=runtime->next_layer_prefetch_hits;
    out->next_layer_prefetch_bytes=runtime->next_layer_prefetch_bytes;
    out->next_layer_prefetch_trained_pairs=runtime->next_layer_prefetch_trained_pairs;
    out->route_recurrence_observations=runtime->route_recurrence_observations;
    out->route_recurrence_prev_hits=runtime->route_recurrence_prev_hits;
    out->route_recurrence_window_hits=runtime->route_recurrence_window_hits;
    out->route_recurrence_layer_samples=runtime->route_recurrence_layer_samples;
    out->route_recurrence_window_experts=runtime->route_recurrence_window_experts;
    out->route_recurrence_resident=runtime->route_recurrence_resident;
    out->route_recurrence_miss_in_window=runtime->route_recurrence_miss_in_window;
    out->route_recurrence_miss_cold=runtime->route_recurrence_miss_cold;
    out->nvfp4_tensor_core_moe_calls=runtime->nvfp4_tensor_core_moe_calls;
    out->nvfp4_tensor_core_moe_fallbacks=runtime->nvfp4_tensor_core_moe_fallbacks;
    out->nvfp4_tensor_core_moe_last_status=runtime->nvfp4_tensor_core_moe_last_status;
    out->host_ffn_layers=runtime->host_ffn_layers;
    out->host_ffn_bytes=runtime->host_ffn_bytes;
    out->dense_host_nanoseconds=runtime->dense_host_nanoseconds;
    out->expert_cache_deferred_admissions=
        runtime->expert_cache_deferred_admissions;
    out->expert_residency_epochs=runtime->expert_residency_epochs;
    out->expert_residency_frozen=runtime->expert_residency_frozen?1:0;
    out->prefill_cache_seed_bytes=runtime->prefill_cache_seed_bytes;
    out->prefill_cache_seed_selected_experts=
        runtime->prefill_cache_seed_selected_experts;
    out->prefill_cache_seed_hits=runtime->prefill_cache_seed_hits;
    out->prefill_cache_seed_avoided_misses=
        runtime->prefill_cache_seed_avoided_misses;
    out->prefill_cache_seed_auto_skips=runtime->prefill_cache_seed_auto_skips;
    out->prefill_cache_seed_budget_stops=
        runtime->prefill_cache_seed_budget_stops;
    out->sampling_gpu_topk_calls=runtime->sampling_gpu_topk_calls;
    out->sampling_gpu_topk_bytes=runtime->sampling_gpu_topk_bytes;
    out->sampling_full_download_bytes=runtime->sampling_full_download_bytes;
    out->sampling_nanoseconds=runtime->sampling_nanoseconds;
    out->grammar_constrained_steps=runtime->grammar_constrained_steps;
    out->grammar_rejected_candidates=runtime->grammar_rejected_candidates;
    out->grammar_empty_candidate_sets=runtime->grammar_empty_candidate_sets;
    out->multi_decode_batches=runtime->multi_decode_batches;
    out->multi_decode_tokens=runtime->multi_decode_tokens;
    out->prefill_streamed_bytes=runtime->prefill_streamed_bytes;
    out->kv_slots=sequence_slots;
    out->kv_reserved_bytes=runtime->slots_state_bytes;
    out->kv_peak_live_bytes=runtime->kv_peak_live_bytes;
    out->kv_peak_tokens=runtime->kv_peak_tokens;
    out->kv_peak_tokens_max=runtime->kv_peak_tokens_max;
    out->kv_occupancy_samples=runtime->kv_occupancy_samples;
    out->prefix_donations=runtime->prefix_donations;
    out->prefix_donated_tokens=runtime->prefix_donated_tokens;
    out->expert_cache_unused_admissions=runtime->expert_cache_unused_admissions;
    return 0;
});}
int flyweight_v2_qwen_runtime_reset(FlyweightV2QwenRuntime*runtime){return guarded([&]{if(!runtime)throw std::runtime_error("invalid Qwen runtime handle");if(runtime->state&&flyweight_gpu_memset(runtime->state,0,runtime->state_bytes,runtime->stream)!=0)throw std::runtime_error("failed to reset native Qwen state");runtime->position=0;runtime->last_output_token=0;runtime->last_output_greedy=true;runtime->processed_tokens.clear();runtime->mtp_cache_tokens=0;runtime->mtp_has_target_hidden=false;runtime->cancelled=false;runtime->cache_admission_enabled=true;qwen_unfreeze_expert_residency(*runtime);return 0;});}
int flyweight_v2_qwen_runtime_cancel(FlyweightV2QwenRuntime*runtime){return guarded([&]{if(!runtime)throw std::runtime_error("invalid Qwen runtime handle");runtime->cancelled=true;return 0;});}
// The wide MMQ shape: the same 128x128 tile spread over 16 warps at 2x4
// fragments instead of 8 at 4x4, which halves the per-thread accumulator and
// brings the kernel from 255 registers / 8 resident warps to 128 / 16. The
// compile below injects the fragment-grid defines ahead of the corpus, and
// the two launch sites in v2_mtp_verifier.inc read this same predicate for
// the 512-thread block size -- the defines and the block size must agree or
// warps 8..15 index rows past the tile. GPU only: the CPU backend's kernel
// corpus is generated at build time with the default shape, and occupancy is
// not its problem anyway.
//
// On by default: greedy output is bit-identical (same per-element K order,
// only the warp->fragment assignment moves), and the 27B IQ2_XXS dense
// checkpoint measured +6% at pp1024 / +9% at pp2048, 6 of 6 interleaved
// pairs. FLYWEIGHT_MMQ_WIDE=0 restores the 256-thread shape.
//
// Only the env read is cached: flyweight_backend_select can flip the backend
// between runtimes in one process (the test suite does), and a static that
// froze the backend answer sent 512-thread launches at the CPU shim's
// 256-shape kernels -- out-of-tile shared writes in emulation, i.e. heap
// corruption that surfaced as nondeterministic parity failures two tests
// later. The backend must be asked every time.
static bool qwen_mmq_wide(){
    static const bool enabled=[]{
        const char*setting=std::getenv("FLYWEIGHT_MMQ_WIDE");
        return !setting||setting[0]!='0';
    }();
    return enabled&&!flyweight_backend_is_cpu();
}
int flyweight_v2_qwen_runtime_prepare(FlyweightV2QwenRuntime*runtime){return guarded([&]{
    // Before anything reads weights in bulk: the hashed n-gram table is not
    // read the way the rest of the mapping is, and the kernel cannot know that.
    qwen_advise_access_pattern(*runtime);
    if(!runtime)throw std::runtime_error("invalid Qwen runtime handle");
    if(runtime->static_arena)return 0;
    if(flyweight_gpu_init(runtime->options.device)!=0)throw std::runtime_error("failed to initialize native CUDA runtime");
    std::vector<std::string> option_storage;
#if !defined(_WIN32)
    for(const char*path:{"/opt/cuda/include","/usr/local/cuda/include","/usr/include"})if(access((std::string(path)+"/cuda_fp16.h").c_str(),R_OK)==0){option_storage.push_back(std::string("-I")+path);if(access((std::string(path)+"/cccl/cub/config.cuh").c_str(),R_OK)==0)option_storage.push_back(std::string("-I")+path+"/cccl");}
#endif
    if(const char*cuda_home=std::getenv("CUDA_HOME")){option_storage.push_back(std::string("-I")+cuda_home+"/include");option_storage.push_back(std::string("-I")+cuda_home+"/include/cccl");}
    std::vector<const char*>compile_options;for(const auto&option:option_storage)compile_options.push_back(option.c_str());
    std::array<char,16384>compile_log{};
    const std::string cuda_source=
        (qwen_mmq_wide()
             ?std::string("#define FLYWEIGHT_MMQ_ROW_WARPS 4\n"
                          "#define FLYWEIGHT_MMQ_ROW_FRAGS 2\n")
             :std::string())+
        std::string(flyweight::v2::qwen_cuda_source)+flyweight::v2::qwen_native_cuda_source;
    if(flyweight_gpu_compile(cuda_source.c_str(),compile_options.data(),static_cast<int32_t>(compile_options.size()),runtime->options.device,compile_log.data(),compile_log.size())!=0)throw std::runtime_error(std::string("failed to compile native Qwen CUDA kernels: ")+compile_log.data());
    runtime->cuda_ready=true;
    if(flyweight_gpu_stream_create(&runtime->stream)!=0)throw std::runtime_error("failed to create native CUDA stream");
    if(flyweight_gpu_stream_create(&runtime->prefetch_stream)!=0){flyweight_gpu_stream_destroy(runtime->stream);throw std::runtime_error("failed to create native Qwen prefetch stream");}
    // On by default. This started off-by-default because the delta capture
    // measured neutral on a MoE checkpoint, where decode waits on ~40 blocking
    // round trips per token for expert routing -- the host already runs ahead,
    // so freeing host time only lengthens the next wait. A dense model has no
    // routing round trips, which puts host submission back on the critical path:
    // 44.6 -> 41.6 ms/token on Qwen3.8-27B IQ2_XXS, generation byte-identical.
    // Neutral on MoE and a clear win on dense, so the default follows dense.
    const char*graph_setting=std::getenv("FLYWEIGHT_CUDA_GRAPHS");
    runtime->cuda_graphs=!graph_setting||graph_setting[0]!='0';
    if(runtime->cuda_graphs&&flyweight_gpu_stream_create(&runtime->graph_stream)!=0)
        runtime->cuda_graphs=false;
    runtime->prefill_profile=std::getenv("FLYWEIGHT_PREFILL_PROFILE")&&
        std::getenv("FLYWEIGHT_PREFILL_PROFILE")[0]=='1';
    if(const char*fused=std::getenv("FLYWEIGHT_FUSED_ATTENTION"))
        runtime->fused_attention=fused[0]!='0';
    else if(const char*fused=std::getenv("FLYWEIGHT_FUSED_Q8_ATTENTION"))
        runtime->fused_attention=fused[0]!='0'; // legacy name
    if(const char*fused=std::getenv("FLYWEIGHT_FUSED_MOE_GATE_UP"))
        runtime->fused_moe_gate_up=fused[0]!='0';
    // Opt-in; see the plan-builder gate for why. Both sites must agree, or the
    // arena is reserved for selection that never runs (or vice versa).
    const char*qsa_setting=std::getenv("FLYWEIGHT_QSA");
    runtime->qsa_enabled=qsa_setting&&qsa_setting[0]=='1';
    if(const char*strict=std::getenv("FLYWEIGHT_EXPERT_CACHE_STRICT_ADMISSION"))
        runtime->strict_cache_admission=strict[0]!='0';
    // FLYWEIGHT_MTP_PROFILE reuses the prefill profiling events to split MTP
    // verification into GPU core / router / transfer, so it needs the same
    // timed events -- which only exist when prefill profiling is on.
    if(std::getenv("FLYWEIGHT_MTP_PROFILE"))runtime->prefill_profile=1;
    const int route_event_status=runtime->prefill_profile
        ?flyweight_gpu_timed_event_create(&runtime->route_event)
        :flyweight_gpu_event_create(&runtime->route_event);
    if(route_event_status!=0){release_qwen_device(*runtime);throw std::runtime_error("failed to create native Qwen route event");}
    // The pipeline's second event is never measured, so it is always untimed.
    if(flyweight_gpu_event_create(&runtime->route_event_pipeline)!=0||
       flyweight_gpu_event_create(&runtime->stream_upload_event)!=0||
       flyweight_gpu_event_create(&runtime->stream_upload_event_pipeline)!=0||
       flyweight_gpu_event_create(&runtime->stream_consumed_event)!=0||
       flyweight_gpu_event_create(&runtime->stream_consumed_event_pipeline)!=0){
        release_qwen_device(*runtime);
        throw std::runtime_error("failed to create native Qwen pipeline route event");
    }
    if(runtime->prefill_profile&&(
       flyweight_gpu_timed_event_create(&runtime->prefill_layer_start_event)!=0||
       flyweight_gpu_timed_event_create(&runtime->prefill_core_end_event)!=0||
       flyweight_gpu_timed_event_create(&runtime->prefill_router_end_event)!=0)){
        release_qwen_device(*runtime);
        throw std::runtime_error("failed to create native Qwen prefill profiling events");
    }
    if(const char*profile=std::getenv("FLYWEIGHT_CUDA_PROFILE");profile&&profile[0]&&profile[0]!='0'){
        runtime->cuda_profile=true;
        runtime->cuda_layer_profiles.resize(runtime->layers.size());
        auto create_profile_event=[&](std::uint64_t&event){
            if(flyweight_gpu_timed_event_create(&event)!=0){
                release_qwen_device(*runtime);
                throw std::runtime_error("failed to create native Qwen CUDA profiling event");
            }
        };
        for(auto&events:runtime->cuda_layer_profiles){
            create_profile_event(events.pre_start);create_profile_event(events.pre_end);
            create_profile_event(events.recurrent_start);create_profile_event(events.recurrent_end);
            create_profile_event(events.shared_start);create_profile_event(events.shared_end);
            create_profile_event(events.expert_start);create_profile_event(events.expert_end);
        }
        create_profile_event(runtime->cuda_tail_start);
        create_profile_event(runtime->cuda_lm_start);
        create_profile_event(runtime->cuda_lm_end);
        create_profile_event(runtime->cuda_tail_end);
    }
    try {

        // Byte cursor into the state arena. Attention KV regions size per the
        // configured cache precision (f32=4B, f16=2B/elem); DeltaNet conv/recurrent
        // state stays f32. Each region is 16-byte aligned so mixed f16/f32 regions
        // never leave a following f32 access misaligned.
        // KV region bytes per element count and cache type (q8_0 = 34B/32-elem block).
        auto kv_bytes=[](std::uint64_t elems,int type){return kv_type_bytes(elems,type);};
        const int ck_type=runtime->options.cache_type_k, cv_type=runtime->options.cache_type_v;
        std::uint64_t state_cursor=0;
        auto reserve=[&](std::uint64_t bytes)->std::uint64_t{const auto at=state_cursor;state_cursor=(state_cursor+bytes+15)/16*16;return at;};
        std::uint64_t max_vector=runtime->model->config.vocabulary_size;
        // The layout below runs once per distinct slot size. It writes into
        // runtime->layers as it always has -- the geometry is read back out
        // afterwards -- so the sizing rules stay in one place and a scratch
        // slot cannot be laid out by a second, drifting copy of them.
        std::uint64_t geometry_context=runtime->options.context_limit;
        auto build_geometry=[&]{
        state_cursor=0;
        for(std::uint32_t layer_number=0;layer_number<runtime->layers.size();++layer_number){
            auto&layer=runtime->layers[layer_number];
            for(auto index:layer.static_tensors){const auto&t=runtime->model->tensors[index];if(t.shape.size()==2)max_vector=std::max(max_vector,t.shape[1]);}
            if(layer.attention){
                const auto&key=runtime->model->tensors[tensor_index(*runtime->model,"blk."+std::to_string(layer_number)+".attn_k.weight")];
                const auto head_dim=layer.head_dim;
                const auto batch_room=std::max<std::uint64_t>(runtime->prefill_rows,9);
                layer.cache_capacity=layer.attention_window&&!runtime->options.swa_full
                    ?std::min<std::uint64_t>(geometry_context,static_cast<std::uint64_t>(layer.attention_window)+batch_room)
                    :geometry_context;
                // The TurboQuant rotation is a Walsh-Hadamard butterfly over the
                // whole head, so head_dim has to be a power of two, and the
                // rotated row is staged in a fixed shared-memory scratch that
                // caps it at 512. Rejecting here beats silently corrupting the
                // cache on a 96- or 80-wide head.
                if((kv_type_is_turbo(ck_type)||kv_type_is_turbo(cv_type))
                   &&(head_dim<32||head_dim>512||(head_dim&(head_dim-1))!=0))
                    throw std::runtime_error(
                        "native Qwen turbo3/turbo4 KV cache needs a head_dim that is a power of two "
                        "between 32 and 512, but layer "+std::to_string(layer_number)+" has "
                        +std::to_string(head_dim));
                const auto cache_floats=layer.kv_heads*layer.cache_capacity*head_dim;
                layer.state_first=reserve(kv_bytes(cache_floats,ck_type));
                layer.state_second=reserve(kv_bytes(cache_floats,cv_type));
            }else{
                const auto&conv=runtime->model->tensors[tensor_index(*runtime->model,"blk."+std::to_string(layer_number)+".ssm_conv1d.weight")];
                const auto&a=runtime->model->tensors[tensor_index(*runtime->model,"blk."+std::to_string(layer_number)+".ssm_a")];
                const auto&norm=runtime->model->tensors[tensor_index(*runtime->model,"blk."+std::to_string(layer_number)+".ssm_norm.weight")];
                // The whole tensor, not its first two extents: a checkpoint may
                // describe the same bytes with a singleton dimension in the
                // middle, and reserving less than the convolution writes puts
                // the conv state on top of the recurrent state.
                layer.state_first=reserve(tensor_elements(conv)*sizeof(float));
                layer.state_second=reserve(a.shape[0]*norm.shape[0]*norm.shape[0]*sizeof(float));
            }
            if(layer.ple_conv!=std::numeric_limits<std::uint64_t>::max()){
                // Dilated causal conv: (kernel-1)*dilation past columns, where
                // the dilation is the n-gram order (transformers sets
                // conv_dilation = ngram_size).
                const auto&ple_conv=runtime->model->tensors[layer.ple_conv];
                const auto kernel=ple_conv.shape[0];
                const auto wide=ple_conv.shape[1];
                const auto dilation=std::max<std::uint64_t>(
                    runtime->model->config.ple_ngram_size,1);
                layer.state_third=reserve((kernel-1)*dilation*wide*sizeof(float));
            }
            if(layer.qsa_ratio&&layer.indexer_q!=std::numeric_limits<std::uint64_t>::max()){
                // QSA block-key store + raw-key ring, both f32 (the scoring is
                // fp32 end to end and the store is 1/16th of the f16 KV slab).
                // +1 block of slack so a chunk that lands exactly on capacity
                // never writes past the store.
                const auto key_len=runtime->model->config.indexer_key_length;
                layer.qsa_blocks=reserve(
                    (layer.cache_capacity/layer.qsa_ratio+1)*key_len*sizeof(float));
                layer.qsa_ring=reserve(
                    static_cast<std::uint64_t>(layer.qsa_ratio)*key_len*sizeof(float));
            }
        }
        if(runtime->options.mtp_drafts){
            auto&layer=runtime->mtp_layer_plan;
            layer.cache_capacity=geometry_context;
            const auto&key=runtime->model->tensors[layer.static_tensors[2]];
            const auto head_dim=key.shape[1]/runtime->model->config.attention_kv_heads;
            const auto cache_floats=runtime->model->config.attention_kv_heads*geometry_context*head_dim;
            layer.state_first=reserve(kv_bytes(cache_floats,ck_type));
            layer.state_second=reserve(kv_bytes(cache_floats,cv_type));
            // What the trunk hands the draft. On qwen4exp that is the
            // hyper-connection STREAMS (hc_count*hidden), not the collapsed
            // hidden the LM head sees: the draft's hnorm spans the whole
            // stream row and its eh_proj runs per stream, so reserving one
            // hidden here would hand over a quarter of the state.
            // See plans/qwen4exp-semantics.md, MTP section.
            const std::uint64_t handover_floats=
                runtime->model->config.architecture=="qwen4exp"
                    ? static_cast<std::uint64_t>(
                          runtime->model->config.hyper_connection_count)
                      *runtime->model->config.hidden_size
                    : runtime->model->config.hidden_size;
            runtime->mtp_target_hidden_offset=reserve(handover_floats*sizeof(float));
            runtime->mtp_draft_hidden_offset=reserve(handover_floats*sizeof(float));
            runtime->mtp_verified_hidden_offset=reserve(
                8ULL*handover_floats*sizeof(float));
            runtime->mtp_snapshot_offset=state_cursor;
            const auto snapshot_start=state_cursor;
            for(std::uint32_t layer_number=0;layer_number<runtime->layers.size();++layer_number){
                auto&target=runtime->layers[layer_number];
                // The PLE ring rides the snapshot independently of the layer
                // kind: its host layer happens to be a DeltaNet one today, but
                // ple_layers is metadata and nothing pins the two together.
                if(target.ple_conv!=std::numeric_limits<std::uint64_t>::max()){
                    const auto&ple_conv=runtime->model->tensors[target.ple_conv];
                    target.snapshot_third=reserve((ple_conv.shape[0]-1)
                        *std::max<std::uint64_t>(runtime->model->config.ple_ngram_size,1)
                        *ple_conv.shape[1]*sizeof(float));
                }
                if(target.attention)continue;
                const auto&conv=runtime->model->tensors[tensor_index(*runtime->model,"blk."+std::to_string(layer_number)+".ssm_conv1d.weight")];
                const auto&a=runtime->model->tensors[tensor_index(*runtime->model,"blk."+std::to_string(layer_number)+".ssm_a")];
                const auto&norm=runtime->model->tensors[tensor_index(*runtime->model,"blk."+std::to_string(layer_number)+".ssm_norm.weight")];
                target.snapshot_first=reserve(conv.shape[0]*conv.shape[1]*sizeof(float));
                target.snapshot_second=reserve(a.shape[0]*norm.shape[0]*norm.shape[0]*sizeof(float));
            }
            runtime->mtp_snapshot_bytes=state_cursor-snapshot_start;
            // Fold retention: per DeltaNet layer, room for the verifier's
            // 8-row maximum of qkv projections (conv.shape[1] channels) and
            // decay+beta logits (2 * value_heads each row). See QwenLayerPlan.
            for(std::uint32_t layer_number=0;layer_number<runtime->layers.size();++layer_number){
                auto&target=runtime->layers[layer_number];
                if(target.attention)continue;
                const auto&conv=runtime->model->tensors[tensor_index(*runtime->model,"blk."+std::to_string(layer_number)+".ssm_conv1d.weight")];
                const auto&a=runtime->model->tensors[tensor_index(*runtime->model,"blk."+std::to_string(layer_number)+".ssm_a")];
                target.mtp_retain_qkv=reserve(8ULL*conv.shape[1]*sizeof(float));
                target.mtp_retain_logits=reserve(16ULL*a.shape[0]*sizeof(float));
                runtime->mtp_delta_layers=true;
            }
        }
        runtime->state_bytes=device_align(state_cursor);
        };  // build_geometry

        // Read the offsets the layout just wrote back out of the layer plans,
        // so a geometry is by construction whatever the sizing rules produced.
        auto capture_geometry=[&]{
            QwenSlotGeometry g;
            g.context_limit=geometry_context;
            g.state_bytes=runtime->state_bytes;
            auto capture_layer=[](const QwenLayerPlan& layer){
                QwenLayerOffsets o;
                o.cache_capacity=layer.cache_capacity;
                o.state_first=layer.state_first;
                o.state_second=layer.state_second;
                o.state_third=layer.state_third;
                o.snapshot_first=layer.snapshot_first;
                o.snapshot_second=layer.snapshot_second;
                o.snapshot_third=layer.snapshot_third;
                o.qsa_blocks=layer.qsa_blocks;
                o.qsa_ring=layer.qsa_ring;
                o.mtp_retain_qkv=layer.mtp_retain_qkv;
                o.mtp_retain_logits=layer.mtp_retain_logits;
                return o;
            };
            g.layers.reserve(runtime->layers.size());
            for(const auto&layer:runtime->layers)g.layers.push_back(capture_layer(layer));
            g.mtp_layer=capture_layer(runtime->mtp_layer_plan);
            g.mtp_target_hidden_offset=runtime->mtp_target_hidden_offset;
            g.mtp_draft_hidden_offset=runtime->mtp_draft_hidden_offset;
            g.mtp_verified_hidden_offset=runtime->mtp_verified_hidden_offset;
            g.mtp_snapshot_offset=runtime->mtp_snapshot_offset;
            g.mtp_snapshot_bytes=runtime->mtp_snapshot_bytes;
            return g;
        };

        // Every slot layout for one context, plus what the slots will reserve
        // -- decided here so the budget estimate and the allocation later
        // cannot disagree. Callable more than once so the budget check can
        // search for a context that fits instead of refusing the request.
        auto plan_slots=[&](std::uint64_t context){
            runtime->options.context_limit=context;
            geometry_context=context;
            build_geometry();
            runtime->geometries.clear();
            runtime->geometries.push_back(capture_geometry());
            // A scratch size, when asked for and when it is genuinely smaller.
            // Equal or larger would allocate a second identical layout and buy
            // nothing, so it collapses back to one.
            if(runtime->options.scratch_context&&
               runtime->options.scratch_context<context){
                geometry_context=runtime->options.scratch_context;
                build_geometry();
                runtime->geometries.push_back(capture_geometry());
                // Leave the layer plans describing the full-context slot: slot
                // 0 holds it, and qwen_apply_geometry re-points them on a switch.
                geometry_context=context;
                build_geometry();
            }
            runtime->state_bytes=runtime->geometries[0].state_bytes;
            const std::size_t planned_slots=runtime->options.mtp_drafts?1:
                std::max<std::uint32_t>(1u,runtime->parallel_sequences);
            runtime->slots_state_bytes=0;
            for(std::size_t i=0;i<planned_slots;++i)
                runtime->slots_state_bytes+=
                    runtime->geometries[qwen_slot_geometry_index(*runtime,i)].state_bytes;
        };
        plan_slots(runtime->options.context_limit);
        // QSA scratch geometry: one shape for every selecting layer. Mixed
        // nonzero ratios would need per-layer score buffers; no checkpoint
        // ships them, so refuse instead of sizing for one and overrunning on
        // the other.
        std::uint64_t qsa_heads=0,qsa_key_len=0,qsa_budget=0,qsa_ratio=0;
        if(runtime->qwen4exp)for(const auto&layer:runtime->layers){
            if(!layer.qsa_ratio||layer.indexer_q==std::numeric_limits<std::uint64_t>::max())continue;
            if(qsa_ratio&&qsa_ratio!=layer.qsa_ratio)
                throw std::runtime_error("qwen4exp QSA layers disagree on the compress ratio");
            qsa_heads=runtime->model->config.indexer_head_count;
            qsa_key_len=runtime->model->config.indexer_key_length;
            qsa_budget=runtime->model->config.indexer_top_k;
            qsa_ratio=layer.qsa_ratio;
        }
        runtime->decode_workspace_layout=flyweight::v2::workspace::qwen_decode(
            runtime->model->config.hidden_size,runtime->scratch_elements,
            runtime->model->config.expert_used_count,runtime->moe_intermediate,
            runtime->model->config.expert_count,
            runtime->model->config.vocabulary_size,
            runtime->model->config.attention_heads,
            runtime->options.context_limit,
            runtime->qwen4exp?runtime->model->config.hyper_connection_count:0,
            runtime->qwen4exp?runtime->model->config.hyper_connection_low_rank:0,
            runtime->qwen4exp&&!runtime->model->config.ple_layers.empty(),
            qsa_heads,qsa_key_len,qsa_budget,qsa_ratio);
        // The batched rows forward (MTP verification and chunked prefill)
        // needs workspace and host staging proportional to its row capacity.
        runtime->forward_rows_capacity=std::max<std::uint32_t>(runtime->prefill_rows,9);
        const std::uint64_t rows=runtime->forward_rows_capacity;
        const std::uint64_t hidden=runtime->model->config.hidden_size;
        const std::uint64_t top_k=runtime->model->config.expert_used_count;
        // The chunked DeltaNet prefill kernels need per-chunk intermediates, but
        // only for models that have DeltaNet layers at the head_dim they support.
        // Anything else leaves these regions empty and keeps the sequential path.
        std::uint64_t delta_value_heads=0;
        for(const auto&layer:runtime->layers){
            if(layer.attention)continue;
            const auto heads=runtime->model->tensors[layer.static_tensors[8]].shape[0];
            const auto dim=runtime->model->tensors[layer.static_tensors[9]].shape[0];
            if(dim==flyweight::v2::workspace::kDeltaDim)delta_value_heads=heads;
            break;
        }
        runtime->delta_value_heads=static_cast<std::uint32_t>(delta_value_heads);
        runtime->rows_workspace_layout=flyweight::v2::workspace::qwen_rows(
            rows,hidden,runtime->scratch_elements,top_k,
            runtime->moe_intermediate,runtime->model->config.expert_count,
            runtime->model->config.attention_heads,
            runtime->options.context_limit,delta_value_heads,
            runtime->options.mtp_drafts!=0,
            runtime->qwen4exp?runtime->model->config.hyper_connection_count:0,
            runtime->qwen4exp?runtime->model->config.hyper_connection_low_rank:0,
            runtime->qwen4exp&&!runtime->model->config.ple_layers.empty(),
            qsa_heads,qsa_key_len,qsa_budget,qsa_ratio);
        runtime->rows_host_layout=
            flyweight::v2::workspace::qwen_rows_host(
                rows,hidden,top_k,runtime->moe_intermediate);
        runtime->decode_host_layout=
            flyweight::v2::workspace::qwen_decode_host(
                hidden,top_k,runtime->moe_intermediate);
        // Multi-sequence decode slices one decode workspace per sequence out of
        // the (rows-forward-sized) workspace; capacity is bounded by what fits.
        runtime->decode_slice_bytes=runtime->decode_workspace_layout.bytes;
        runtime->decode_host_block_bytes=runtime->decode_host_layout.bytes;
        runtime->workspace_bytes=device_align(std::max<std::uint64_t>(
            16ULL*1024*1024,
            std::max({max_vector*sizeof(float)*8,
                      runtime->decode_workspace_layout.bytes,
                      runtime->rows_workspace_layout.bytes})
        ));
        // The embedding table is a per-token gather, so it need not be resident:
        // staging just the row each token reads returns the whole
        // vocab x hidden matrix to the expert cache. Requires an untied lm_head
        // (a tied head multiplies by the full matrix every token) and rows that
        // divide evenly, which every 2-D vocab x hidden layout does. Decided
        // here because the dense spill accounting below and the arena sizing
        // both depend on it.
        //
        // What the freed VRAM buys decides the default, and it differs by model
        // kind. It always costs the same thing: a per-token read of a random
        // row of the mapping, and host registration does not populate those
        // pages, so the read is a cold major fault on the critical path.
        //
        // On MoE the gain is indirect -- more expert slots, so a lower miss
        // rate. Measured on the reference SM120 laptop with
        // Qwen3.6-35B-Fast-NVFP4 that is 575 more slots and 30% fewer misses
        // but ~1 ms/token more route_wait, a ~2% net loss. So it stays off.
        //
        // On a dense model the gain is direct: there are no experts to page,
        // so every freed byte is feed-forward that stops being re-read from
        // RAM on *every* token. Measured on Muse-Glimmer-30B IQ2_XXS (a 10 GiB
        // checkpoint on a 12 GiB card) returning the 1.03 GiB embedding table
        // takes the spill from nine blocks to one and decode from 11.9 to 17.8
        // tok/s -- a 1.5x that one 5.4 KB row read per token cannot dent. So it
        // defaults on there.
        //
        // FLYWEIGHT_EMBED_HOST forces it either way.
        {
            const auto& table=runtime->model->tensors[runtime->token_embeddings];
            const auto vocabulary=runtime->model->config.vocabulary_size;
            const char* embed_env=std::getenv("FLYWEIGHT_EMBED_HOST");
            runtime->embedding_row_bytes=
                (vocabulary&&table.size%vocabulary==0)?table.size/vocabulary:0;
            const bool dense_model=runtime->model->config.expert_count==0;
            const bool wanted=embed_env?embed_env[0]=='1':dense_model;
            runtime->embeddings_host_resident=
                runtime->embedding_row_bytes!=0&&
                runtime->lm_head!=runtime->token_embeddings&&
                !runtime->gemma4&&
                wanted;
            if(runtime->embeddings_host_resident)
                runtime->static_tensor_bytes-=table.size;
        }
        // A dense model has no experts to page, so every weight would otherwise
        // have to be resident and the card becomes a hard ceiling. Spill whole
        // blocks' feed-forward to the host until the static set fits, working
        // from the last block back; attention and the DeltaNet recurrence stay
        // on the GPU because they are far smaller and latency-critical.
        if(!runtime->layers.empty()&&runtime->layers.front().dense_ffn){
            std::uint64_t resident=0;
            for(const auto&layer:runtime->layers)for(auto tensor:layer.static_tensors)resident+=device_align(runtime->model->tensors[tensor].size);
            for(auto tensor:{runtime->token_embeddings,runtime->final_norm,runtime->lm_head}){
                if(tensor==runtime->token_embeddings&&runtime->embeddings_host_resident)continue;
                resident+=device_align(runtime->model->tensors[tensor].size);
            }
            if(runtime->mtp_available){
                for(auto tensor:runtime->mtp_layer_plan.static_tensors)resident+=device_align(runtime->model->tensors[tensor].size);
                for(auto tensor:runtime->mtp_special_tensors)
                    if(tensor!=runtime->final_norm)
                        resident+=device_align(runtime->model->tensors[tensor].size);
            }
            std::uint64_t budget=runtime->options.gpu_cache_bytes;
            if(!budget){
                FlyweightV2GpuInfo gi{};
                if(gpu_probe(gi,runtime->options.device)==0&&gi.free_memory>0){
                    // Deliberately much tighter than the expert-cache auto-fit
                    // margin: that one reserves room for a cache this model does
                    // not have, whereas here the workspace, KV state, staging and
                    // an alignment headroom are all subtracted explicitly below.
                    // Every 96 MiB left unused here is another block whose
                    // feed-forward has to be re-read from RAM on every token.
                    // Deliberately tighter than the auto-fit check's
                    // max(2 GiB, VRAM/8), and it stays that way: raising it to
                    // match was measured twice and reverted both times. The two
                    // margins encode different policies -- keep weights on the
                    // GPU for speed here, leave the desktop its headroom there
                    // -- and forcing this one up spills so much that host and
                    // device arithmetic diverge: 11 contract tests, MTP fold
                    // bitwise-identity and transformers parity among them,
                    // changed their tokens. The residual disagreement is what
                    // the context clamp below absorbs.
                    const std::uint64_t margin=std::max<std::uint64_t>(384ull*1024*1024,gi.total_memory/32);
                    budget=gi.free_memory>margin?gi.free_memory-margin:0;
                }
            }
            // The workspace, KV state and staging still have to fit alongside
            // the weights, so reserve them before deciding what stays.
            // Headroom on top of the named arenas: per-tensor alignment padding
            // and the snapshot pool land on the same budget, and stopping the
            // spill exactly at the limit leaves the allocation to fail by a
            // rounding error.
            // The snapshot pool is one allocation per checkpoint slot per
            // decode slot and reached 1212 MiB on a 2-slot 27B -- an order more
            // than the alignment headroom that was standing in for it, and
            // exactly the shortfall that killed prepare after the spill had
            // already decided it was done.
            // Auto-fit only. Under an explicit --gpu-cache-mib the later check
            // has always run and refuses with the same arithmetic spelled out,
            // so nothing there was ever silently wrong; widening this changes
            // how many blocks such a run spills, and a feed-forward moved to
            // the host is different arithmetic, so it moves that run's tokens.
            // Measured: it re-spills the dense contract fixture entirely and
            // the host and resident paths stop agreeing.
            const std::size_t snapshot_slots=runtime->options.mtp_drafts?1:
                std::max<std::uint32_t>(1u,runtime->parallel_sequences);
            // Estimated here rather than read from prefill_snapshot_bytes,
            // which is not sized until after this point. Moving that sizing
            // earlier was tried and reverted: it changes what prefix donation
            // copies between slots and the donated turn decodes garbage, so
            // the authoritative value stays where the rest of prepare expects
            // it and this is a same-formula estimate for the budget only.
            std::uint64_t snapshot_estimate=0;
            for(const auto&layer:runtime->layers){
                if(layer.ple_conv!=std::numeric_limits<std::uint64_t>::max()){
                    const auto&ple=runtime->model->tensors[layer.ple_conv];
                    snapshot_estimate+=(ple.shape[0]-1)*std::max<std::uint64_t>(
                        runtime->model->config.ple_ngram_size,1)*ple.shape[1];
                }
                if(layer.attention){
                    if(layer.qsa_ratio&&layer.indexer_q!=
                            std::numeric_limits<std::uint64_t>::max())
                        snapshot_estimate+=static_cast<std::uint64_t>(layer.qsa_ratio)
                            *runtime->model->config.indexer_key_length;
                    continue;
                }
                const auto&conv=runtime->model->tensors[layer.static_tensors[6]];
                const auto&a=runtime->model->tensors[layer.static_tensors[8]];
                const auto&norm=runtime->model->tensors[layer.static_tensors[9]];
                snapshot_estimate+=conv.shape[0]*conv.shape[1]
                    +a.shape[0]*norm.shape[0]*norm.shape[0];
            }
            // Auto-fit only: under an explicit --gpu-cache-mib the later check
            // has always refused with this arithmetic spelled out, and widening
            // the reservation there re-spills the dense contract fixture, whose
            // host and resident paths then stop agreeing.
            const std::uint64_t snapshot_pool=runtime->options.gpu_cache_bytes?0:
                snapshot_slots*runtime->prefill_snapshots.size()*
                    device_align(snapshot_estimate*sizeof(float));
            const std::uint64_t reserved_base=runtime->workspace_bytes
                +runtime->slots_state_bytes
                +runtime->expert_staging_bytes
                +snapshot_pool
                +std::max<std::uint64_t>(1024ull*1024,budget/64);
            // Room for the batch staging buffer, but only when something is
            // going to spill -- a model that fits entirely never allocates it.
            // Sized for the widest feed-forward triple, since one block is
            // staged at a time.
            std::uint64_t ffn_stage=0;
            for(const auto&layer:runtime->layers){
                if(!layer.dense_ffn)continue;
                const std::size_t ffn_base=qwen_ffn_base(*runtime,layer);
                std::uint64_t triple=0;
                for(std::size_t role=1;role<=3;++role)
                    triple+=device_align(runtime->model->tensors[
                        layer.static_tensors[ffn_base+role]].size);
                ffn_stage=std::max(ffn_stage,triple);
            }
            const bool will_spill=
                resident>(budget>reserved_base?budget-reserved_base:0);
            const std::uint64_t reserved=reserved_base+(will_spill?ffn_stage:0);
            const std::uint64_t weight_budget=budget>reserved?budget-reserved:0;
            // Mixed-quant checkpoints deliberately assign slower IQ formats
            // to important layers. Their CPU fallback is scalar today, while
            // Q2_K/Q3_K/Q5_K/Q6_K have AVX2/AVX-512 row kernels. Spill the
            // SIMD-capable blocks first instead of blindly taking the tail and
            // turning several IQ3_S/IQ4_XS layers into the decode bottleneck.
            // Two overrides, both for measurement and for pinning residency on a
            // card whose free VRAM moves under you (the auto budget probes it
            // once at startup). FLYWEIGHT_HOST_FFN_BLOCKS=N spills exactly N
            // blocks regardless of budget -- the equivalent of llama.cpp's
            // --n-cpu-ffn. FLYWEIGHT_HOST_FFN_ORDER=naive takes them sequentially
            // from block 0 with no quantization awareness, which is what that
            // flag does, and exists so the ordering can be A/B'd against the
            // default on the same runtime.
            const char* forced_env=std::getenv("FLYWEIGHT_HOST_FFN_BLOCKS");
            const std::uint32_t forced_blocks=forced_env
                ?static_cast<std::uint32_t>(std::strtoul(forced_env,nullptr,10)):0;
            const char* order_env=std::getenv("FLYWEIGHT_HOST_FFN_ORDER");
            const bool naive_order=order_env&&std::strcmp(order_env,"naive")==0;
            auto want_more=[&]{
                return forced_env?runtime->host_ffn_layers<forced_blocks
                                 :resident>weight_budget;
            };
            auto spill_pass=[&](bool simd_only){
                for(auto layer=runtime->layers.rbegin();
                    layer!=runtime->layers.rend()&&want_more();++layer){
                    if(!layer->dense_ffn||layer->ffn_on_host)continue;
                    const std::size_t ffn_base=qwen_ffn_base(*runtime,*layer);
                    bool all_simd=true;
                    for(std::size_t role=1;role<=3;++role)
                        all_simd=all_simd&&qwen_simd_quant_type(
                            runtime->model->tensors[layer->static_tensors[ffn_base+role]].type);
                    if(simd_only&&!all_simd)continue;
                    std::uint64_t freed=0;
                    for(std::size_t role=1;role<=3;++role)
                        freed+=device_align(runtime->model->tensors[
                            layer->static_tensors[ffn_base+role]].size);
                    layer->ffn_on_host=true;
                    resident-=freed;
                    runtime->host_ffn_bytes+=freed;
                    ++runtime->host_ffn_layers;
                }
            };
            auto spill_pass_naive=[&]{
                for(auto layer=runtime->layers.begin();
                    layer!=runtime->layers.end()&&want_more();++layer){
                    if(!layer->dense_ffn||layer->ffn_on_host)continue;
                    const std::size_t ffn_base=qwen_ffn_base(*runtime,*layer);
                    std::uint64_t freed=0;
                    for(std::size_t role=1;role<=3;++role)
                        freed+=device_align(runtime->model->tensors[
                            layer->static_tensors[ffn_base+role]].size);
                    layer->ffn_on_host=true;
                    resident-=freed;
                    runtime->host_ffn_bytes+=freed;
                    ++runtime->host_ffn_layers;
                }
            };
            if(naive_order){
                spill_pass_naive();
            }else{
                spill_pass(true);
                spill_pass(false);
            }
            if(runtime->host_ffn_layers)runtime->host_ffn_stage_bytes=ffn_stage;
            // Re-encode the spilled feed-forward as Q8_0 for the host dot; see
            // host_ffn_q8. Bounded because the expansion is ~3.3x and a fully
            // spilled 27B is ~17 GiB: FLYWEIGHT_HOST_FFN_Q8_MIB caps it (0 turns
            // the re-encoding off), and blocks past the cap keep the original
            // format, which still works, just slower.
            // Which format to re-encode into is a bandwidth question, not a
            // speed-of-decode one. The host dot on IQ2_S runs at 2.2x its
            // bandwidth roof, so a format only helps if it is SIMD-capable
            // *and* no more than ~2.2x the size. Q8_0 is 3.3x and measured
            // 17% slower than doing nothing -- it is bandwidth-bound at
            // 89 ms/token where the codebook path was compute-bound at 60.
            // Q2_K is 1.02x, Q3_K 1.34x, and both have AVX-512 row kernels.
            std::uint32_t q8_format=11;  // Q3_K
            if(const char*env=std::getenv("FLYWEIGHT_HOST_FFN_FORMAT")){
                if(std::strcmp(env,"q2_k")==0)q8_format=10;
                else if(std::strcmp(env,"q3_k")==0)q8_format=11;
                else if(std::strcmp(env,"q8_0")==0)q8_format=8;
                else if(std::strcmp(env,"off")==0)q8_format=0;
            }
            std::uint64_t q8_budget=8192ull*1024*1024;
            if(const char*env=std::getenv("FLYWEIGHT_HOST_FFN_Q8_MIB"))
                q8_budget=std::strtoull(env,nullptr,10)*1024ull*1024;
            if(!q8_format)q8_budget=0;
            if(runtime->host_ffn_layers&&q8_budget){
                const auto started=std::chrono::steady_clock::now();
                for(auto&layer:runtime->layers){
                    if(!layer.ffn_on_host||!layer.dense_ffn)continue;
                    const std::size_t ffn_base=qwen_ffn_base(*runtime,layer);
                    for(std::size_t role=1;role<=3;++role){
                        const auto index=layer.static_tensors[ffn_base+role];
                        const auto&tensor=runtime->model->tensors[index];
                        if(qwen_simd_quant_type(tensor.type))continue;
                        std::uint64_t elements=1;
                        for(auto dimension:tensor.shape)elements*=dimension;
                        const std::uint64_t columns=tensor.shape[0];
                        const std::uint64_t rows_out=elements/std::max<std::uint64_t>(columns,1);
                        const std::uint64_t grain=q8_format==8?32:256;
                        if(!columns||columns%grain||!rows_out)continue;
                        const std::uint64_t row_bytes=q8_format==8
                            ?(columns/32)*kQ8BlockSize
                            :(columns/256)*(q8_format==10?kQ2KBlockSize:kQ3KBlockSize);
                        const std::uint64_t total=row_bytes*rows_out;
                        if(runtime->host_ffn_q8_bytes+total>q8_budget)continue;
                        std::vector<std::uint8_t> packed(total);
                        std::atomic<bool> failed{false};
                        #pragma omp parallel for schedule(static) \
                            num_threads(qwen_cpu_thread_count(*runtime))
                        for(std::int64_t row=0;row<static_cast<std::int64_t>(rows_out);++row){
                            if(failed.load(std::memory_order_relaxed))continue;
                            std::vector<float> scratch(columns);
                            try{
                                qwen_dequant_row(tensor_data(*runtime->model,tensor),
                                                 tensor.type,static_cast<int>(columns),
                                                 static_cast<std::uint64_t>(row),scratch.data());
                                auto*dst=packed.data()+static_cast<std::uint64_t>(row)*row_bytes;
                                if(q8_format==8)pack_q8_0(scratch.data(),columns,dst);
                                else if(q8_format==10)pack_q2_k(scratch.data(),columns,dst);
                                else pack_q3_k(scratch.data(),columns,dst);
                            }catch(...){failed.store(true,std::memory_order_relaxed);}
                        }
                        if(failed.load())continue;
                        runtime->host_ffn_q8_bytes+=total;
                        runtime->host_ffn_q8_type=q8_format;
                        runtime->host_ffn_q8.emplace(index,std::move(packed));
                    }
                }
                if(runtime->host_ffn_q8_bytes){
                    const auto elapsed=std::chrono::duration_cast<std::chrono::milliseconds>(
                        std::chrono::steady_clock::now()-started).count();
                    std::fprintf(stderr,
                        "[flyweight] spilled feed-forward re-encoded to %s for the host: "
                        "%zu tensors, %llu MiB of RAM, %lld ms\n",
                        q8_format==8?"Q8_0":(q8_format==10?"Q2_K":"Q3_K"),
                        runtime->host_ffn_q8.size(),
                        static_cast<unsigned long long>(runtime->host_ffn_q8_bytes/(1024ull*1024)),
                        static_cast<long long>(elapsed));
                }
            }
            if(runtime->host_ffn_layers)
                std::fprintf(stderr,
                    "[flyweight] dense feed-forward: %u of %zu blocks on CPU (%llu MiB spilled)\n",
                    runtime->host_ffn_layers,runtime->layers.size(),
                    static_cast<unsigned long long>(runtime->host_ffn_bytes/(1024ull*1024)));
        }
        std::vector<bool> persistent(runtime->model->tensors.size(),false);
        std::vector<bool> preserve_bf16(runtime->model->tensors.size(),false);
        if(!runtime->embeddings_host_resident)
            persistent[runtime->token_embeddings]=true;
        persistent[runtime->final_norm]=true;
        persistent[runtime->lm_head]=true;
        if(runtime->rope_factors!=std::numeric_limits<std::uint64_t>::max())
            persistent[runtime->rope_factors]=true;
        // qwen4exp head collapse: the norm rides final_norm above; the low-rank
        // mixer pair must reach the device too. The n-gram table deliberately
        // does NOT: it is read 16 rows per token on the host.
        if(runtime->qwen4exp){
            persistent[runtime->output_hc_down]=true;
            persistent[runtime->output_hc_up]=true;
        }
        for(const auto&layer:runtime->layers){
            const std::size_t ffn_base=qwen_ffn_base(*runtime,layer);
            for(std::size_t slot=0;slot<layer.static_tensors.size();++slot){
                // A spilled block reads its gate/up/down from the mapping, so
                // those three must not consume arena space.
                if(layer.ffn_on_host&&slot>ffn_base&&slot<=ffn_base+3)continue;
                persistent[layer.static_tensors[slot]]=true;
            }
        }
        if(runtime->options.mtp_drafts){
            for(auto tensor:runtime->mtp_layer_plan.static_tensors){persistent[tensor]=true;preserve_bf16[tensor]=true;}
            for(auto tensor:runtime->mtp_special_tensors){persistent[tensor]=true;preserve_bf16[tensor]=true;}
        }
        // NVFP4 checkpoints quantize only the routed experts and ship every
        // dense weight as bf16, which costs ~1.9x Q8_0's bytes for the same
        // values. Those weights are re-read in full on every token, so the
        // difference lands directly in route_wait -- on the reference SM120
        // laptop the NVFP4 build reads 2.72 GB before the router against the
        // Q6_K build's 1.53 GB, and that gap is the entire decode gap between
        // them. Requantize them once here instead. Q8_0 is what the K-quant
        // checkpoints already use for the same tensors, and every consumer
        // dispatches on the effective type, so nothing downstream changes.
        //
        // The routed experts are deliberately excluded: they are paged through
        // the expert cache from the mapping, not the static arena, and they are
        // the tensors NVFP4 exists to compress. The public dense_requant policy
        // controls this; FLYWEIGHT_REQUANT_BF16 remains a legacy override only
        // while the policy is auto.
        {
            FlyweightV2GpuInfo probe{};
            if(gpu_probe(probe,runtime->options.device)==0)
                runtime->int8_tensor_cores=
                    probe.compute_major*10+probe.compute_minor>=75;
        }
        runtime->device_tensor_types.resize(runtime->model->tensors.size());
        for(std::uint64_t index=0;index<runtime->model->tensors.size();++index)
            runtime->device_tensor_types[index]=runtime->model->tensors[index].type;
        // A draft block can be quantized differently from the trunk it is
        // attached to, because it can come from a different file. The released
        // pair is exactly that: UD-IQ1_S -- the only Qwen3.8-Flash-Next quant
        // that fits this box -- stores hc_*_up as Q8_0, while the standalone
        // MTP companion is Q4_K_M and stores it as Q5_0, a type the matvec
        // driver has no kernel for. Repack those to Q8_0 on the way to the
        // device, the same flip the IQ1_M head takes below.
        //
        // Scoped to the draft block on purpose: it cannot perturb a
        // configuration that already works, the block is a single layer so the
        // growth is bounded, and requantizing a DRAFT weight cannot change
        // output at all -- verify re-scores every drafted token with the
        // target, so this can only move the acceptance rate.
        //
        // Must run AFTER the loop above: that one assigns every entry from the
        // file type, so a flip written earlier is silently overwritten.
        if(runtime->mtp_available){
            auto requant_draft=[&](std::uint64_t index){
                if(index>=runtime->device_tensor_types.size())return;
                const auto&draft_tensor=runtime->model->tensors[index];
                if(draft_tensor.type==0||qwen_matvec_supported(draft_tensor.type))return;
                std::uint64_t elements=1;
                for(auto dimension:draft_tensor.shape)elements*=dimension;
                if(!elements||elements%32)
                    throw std::runtime_error(
                        "MTP draft tensor \""+draft_tensor.name+"\" has type "+
                        std::to_string(draft_tensor.type)+", for which there is no "
                        "matvec kernel, and is not a whole number of 32-value "
                        "blocks so it cannot be converted to Q8_0 either");
                runtime->device_tensor_types[index]=8;
            };
            for(auto tensor:runtime->mtp_layer_plan.static_tensors)requant_draft(tensor);
            for(auto tensor:runtime->mtp_special_tensors)requant_draft(tensor);
        }
        // IQ1_M reaches the device as itself wherever a kernel can read it, and
        // as Q8_0 where none can.
        //
        // iq1m_matvec_transposed covers the dense projections, which is where
        // these tensors actually appear: on the checkpoints this was written for
        // the only IQ1_M weights are the two per-head DeltaNet gate projections
        // (hidden x 48). Those now run from their packed bytes.
        //
        // The head and the embedding table are the gap. Their kernels decode a
        // row or a token directly rather than going through the matvec dispatch,
        // and neither qwen_lm_head_argmax_kernel nor qwen_embedding_kernel has
        // an IQ1_M entry, so those two are converted to Q8_0 here.
        //
        // Converting is expensive and has to stay bounded. IQ1_M is 56 bytes per
        // 256 values against Q8_0's 272, so a converted tensor costs 4.86x its
        // file bytes -- and the head is one of the largest tensors in a
        // checkpoint. On a model quantized broadly to IQ1_M (which is what a
        // 1.75 bpw format exists for) that inflation would surface as an OOM at
        // the arena allocation with nothing pointing back here, so past a
        // threshold this stops with the numbers instead of paying it silently.
        {
            std::uint64_t converted=0,growth=0,persistent_source_bytes=0;
            for(std::uint64_t index=0;index<persistent.size();++index){
                if(!persistent[index])continue;
                const auto& tensor=runtime->model->tensors[index];
                persistent_source_bytes+=tensor.size;
                if(tensor.type!=29)continue;
                // Everything else is read through a matvec dispatch that now
                // has a case 29, so it stays packed.
                if(index!=runtime->lm_head&&index!=runtime->token_embeddings)
                    continue;
                std::uint64_t elements=1;
                for(auto dimension:tensor.shape)elements*=dimension;
                // A partial trailing block would need its own padding path that
                // the packer does not implement. Skipping would leave the tensor
                // at type 29, which the head and embedding kernels cannot read,
                // so the failure would surface later as an unsupported-type
                // error naming neither the tensor nor the reason. Stop here.
                if(elements==0||elements%256)
                    throw std::runtime_error(
                        "IQ1_M tensor \""+tensor.name+"\" is not a whole number "
                        "of 256-value blocks and cannot be converted to Q8_0, "
                        "which is the only form its kernel can read it in");
                runtime->device_tensor_types[index]=8;
                ++converted;
                growth+=(elements/32)*kQ8BlockSize-tensor.size;
            }
            // Warn rather than refuse. This conversion is mandatory -- the
            // head and embedding kernels cannot read type 29 at all -- so
            // failing here does not save the user anything, it just turns a
            // model that would have run into one that will not load. What the
            // bound is really for is traceability: at 4.86x, an IQ1_M head can
            // add enough to make the arena allocation fail much later with
            // nothing pointing back here, so say so up front and let it run.
            if(converted&&growth>persistent_source_bytes/16)
                std::fprintf(stderr,
                    "[flyweight] warning: this checkpoint stores its head or "
                    "embedding table as IQ1_M, whose kernels cannot read that "
                    "type. Converting to Q8_0 adds %llu MiB to %llu MiB of "
                    "static weights (4.86x on those tensors); if the GPU arena "
                    "allocation fails below, this is why.\n",
                    static_cast<unsigned long long>(growth/(1024ull*1024)),
                    static_cast<unsigned long long>(
                        persistent_source_bytes/(1024ull*1024)));
            if(converted){
                runtime->static_tensor_bytes+=growth;
                std::fprintf(stderr,
                    "[flyweight] IQ1_M requant: %llu tensors to Q8_0 "
                    "(%llu MiB added)\n",
                    static_cast<unsigned long long>(converted),
                    static_cast<unsigned long long>(growth/(1024ull*1024)));
            }
        }
        {
            std::uint32_t requant_mode=runtime->options.dense_requant;
            if(requant_mode==0){
                const char* legacy=std::getenv("FLYWEIGHT_REQUANT_BF16");
                if(legacy&&legacy[0]=='0')requant_mode=2;
                else if(legacy&&legacy[0]=='1')requant_mode=1;
            }
            std::uint64_t candidate_count=0;
            std::uint64_t persistent_bytes=0;
            for(std::uint64_t index=0;index<persistent.size();++index){
                if(!persistent[index])continue;
                const auto&tensor=runtime->model->tensors[index];
                // The effective size, not the file size: the IQ1_M loop above
                // has already flipped those tensors to Q8_0, and counting their
                // file bytes here would undercount the arena by 4.86x each --
                // biasing the auto decision towards keeping bf16 exactly when
                // memory is tighter than this estimate believes.
                persistent_bytes+=device_align(
                    qwen_device_tensor_size(*runtime,index));
                if(preserve_bf16[index]||tensor.type!=30)continue;
                std::uint64_t elements=1;
                for(auto dimension:tensor.shape)elements*=dimension;
                if(elements&&elements%32==0)++candidate_count;
            }
            std::uint64_t auto_budget=runtime->options.gpu_cache_bytes;
            if(requant_mode==0&&auto_budget==0){
                FlyweightV2GpuInfo gi{};
                if(gpu_probe(gi,runtime->options.device)==0&&gi.free_memory>0){
                    const std::uint64_t margin=std::max<std::uint64_t>(
                        2048ull*1024*1024,gi.total_memory/8);
                    if(gi.free_memory>margin)auto_budget=gi.free_memory-margin;
                }
            }
            const std::uint64_t requant_slots=runtime->options.mtp_drafts?1:
                std::max<std::uint32_t>(1u,runtime->parallel_sequences);
            const std::uint64_t estimated_base=persistent_bytes+
                runtime->workspace_bytes+runtime->slots_state_bytes;
            const auto requant_policy=qwen_expert_policy(
                *runtime,flyweight::v2::ExpertExecutionPhase::prepare);
            const std::uint64_t useful_expert_cache=
                requant_policy.routed_gpu_execution_allowed()
                    ?runtime->expert_tensor_bytes:0;
            const std::uint64_t estimated_need=estimated_base+useful_expert_cache;
            const bool auto_pressure=candidate_count&&
                (auto_budget==0||estimated_need>auto_budget);
            const bool requantize=requant_mode==1||
                (requant_mode==0&&auto_pressure);
            if(requantize){
                for(std::uint64_t index=0;index<persistent.size();++index){
                    if(!persistent[index])continue;
                    if(preserve_bf16[index])continue;
                    const auto& tensor=runtime->model->tensors[index];
                    if(tensor.type!=30)continue;
                    std::uint64_t elements=1;
                    for(auto dimension:tensor.shape)elements*=dimension;
                    // A partial trailing block would need its own padding path;
                    // every Qwen dense weight divides evenly.
                    if(elements==0||elements%32)continue;
                    runtime->device_tensor_types[index]=8;
                    ++runtime->requantized_tensors;
                    runtime->requantized_saved_bytes+=
                        tensor.size-(elements/32)*kQ8BlockSize;
                }
            }else if(requant_mode==0&&candidate_count){
                std::fprintf(stderr,
                    "[flyweight] dense requant auto: kept %llu bf16 tensors "
                    "(estimated %llu MiB need within %llu MiB GPU budget)\n",
                    static_cast<unsigned long long>(candidate_count),
                    static_cast<unsigned long long>(estimated_need/(1024ull*1024)),
                    static_cast<unsigned long long>(auto_budget/(1024ull*1024)));
            }
        }
        // lm_head_type is captured from the checkpoint at plan time, so it has
        // to follow the head through requantization.
        runtime->lm_head_type=qwen_device_type(*runtime,runtime->lm_head);
        if(runtime->requantized_tensors){
            runtime->static_tensor_bytes-=runtime->requantized_saved_bytes;
            std::fprintf(stderr,
                "[flyweight] dense requant %s: %llu bf16 tensors to Q8_0 "
                "(%llu MiB freed)\n",
                runtime->options.dense_requant==1?"q8":"auto",
                static_cast<unsigned long long>(runtime->requantized_tensors),
                static_cast<unsigned long long>(
                    runtime->requantized_saved_bytes/(1024ull*1024)));
        }
        // Aliased tensors are read straight out of the GGUF mapping and need no
        // arena space; see qwen_alias_static_tensor.
        for(std::uint64_t index=0;index<persistent.size();++index)if(persistent[index]&&!qwen_alias_static_tensor(*runtime,index))runtime->static_arena_bytes+=device_align(qwen_device_tensor_size(*runtime,index));
        // Dense checkpoints have no routed experts, so there is nothing to size
        // a paging slot from -- and expert_count is zero, which would trap.
        std::uint64_t one_expert=0;
        if(runtime->model->config.expert_count)
            for(const auto&layer:runtime->layers){if(layer.dense_ffn)continue;std::uint64_t bytes=0;for(auto tensor:layer.expert_tensors)bytes+=runtime->model->tensors[tensor].size/runtime->model->config.expert_count;one_expert=std::max(one_expert,bytes);}
        runtime->expert_staging_bytes=device_align(one_expert*runtime->model->config.expert_used_count*2);
        runtime->host_staging_bytes=std::max(
            runtime->expert_staging_bytes,
            device_align(runtime->rows_host_layout.bytes));
        auto prepare_policy=qwen_expert_policy(
            *runtime,flyweight::v2::ExpertExecutionPhase::prepare);
        runtime->multi_decode_capacity=1;
        const std::uint64_t decode_slots=runtime->options.mtp_drafts?1:std::max<std::uint32_t>(1u,runtime->parallel_sequences);
        // Gemma 4 supports independent sequence slots, but its hybrid expert
        // path currently schedules those slots sequentially. The Qwen
        // layer-overlapped multi-decode driver assumes separate gate/up/down
        // expert tensors and must not consume Gemma's fused Q4_0 bundles.
        if(decode_slots>1&&runtime->model->config.expert_count&&
           prepare_policy.routed_cpu_execution_allowed()&&
           !runtime->gemma4&&!runtime->laguna){
            const std::uint64_t by_workspace=runtime->workspace_bytes/runtime->decode_slice_bytes;
            runtime->multi_decode_capacity=static_cast<std::uint32_t>(
                std::min(std::min(decode_slots,by_workspace),std::uint64_t{4}));
            // Sequence-private host blocks sit BEFORE the shared paging area, so
            // the paging area keeps its full expert_staging_bytes capacity.
            if(runtime->multi_decode_capacity>1)
                runtime->host_staging_bytes=std::max(runtime->host_staging_bytes,
                    runtime->multi_decode_capacity*runtime->decode_host_block_bytes+runtime->expert_staging_bytes);
        }
        runtime->expert_slot_bytes=device_align(one_expert);
        // Prefill snapshot slots hold every DeltaNet layer's conv+recurrent
        // state. They coexist with MTP: the MTP round's own snapshot lives in
        // the state arena and is per-round transient, while these are separate
        // device buffers written during prefill and read at prompt admission,
        // when no round is in flight. Gating them off under --mtp-draft (as
        // this originally did) silently disabled turn-to-turn prefix reuse for
        // every client that re-renders the previous assistant reply -- each
        // 70k-token turn reprefilled cold, minutes per turn, for the sake of a
        // conflict that does not exist.
        std::uint64_t snapshot_floats=0;
        for(const auto&layer:runtime->layers){
            if(layer.ple_conv!=std::numeric_limits<std::uint64_t>::max()){
                const auto&ple_conv=runtime->model->tensors[layer.ple_conv];
                snapshot_floats+=(ple_conv.shape[0]-1)*
                    std::max<std::uint64_t>(runtime->model->config.ple_ngram_size,1)*
                    ple_conv.shape[1];
            }
            if(layer.attention){
                // The QSA raw-key ring is the one piece of indexer state a
                // snapshot must carry: the block-key store is position-bounded
                // (append-only, like the KV slabs), but the ring holds the
                // partial block behind the snapshot position, whose raw keys
                // later tokens overwrite.
                if(layer.qsa_ratio&&layer.indexer_q!=std::numeric_limits<std::uint64_t>::max())
                    snapshot_floats+=static_cast<std::uint64_t>(layer.qsa_ratio)
                        *runtime->model->config.indexer_key_length;
                continue;
            }
            const auto&conv=runtime->model->tensors[layer.static_tensors[6]];
            const auto&a=runtime->model->tensors[layer.static_tensors[8]];
            const auto&norm=runtime->model->tensors[layer.static_tensors[9]];
            snapshot_floats+=conv.shape[0]*conv.shape[1]+a.shape[0]*norm.shape[0]*norm.shape[0];
        }
        runtime->prefill_snapshot_bytes=snapshot_floats?device_align(snapshot_floats*sizeof(float)):0;
        // One KV+DeltaNet state arena per parallel decode slot. MTP manages its
        // own state inside the arena, so it stays single-slot.
        const std::size_t slot_count=runtime->options.mtp_drafts?1:std::max<std::uint32_t>(1u,runtime->parallel_sequences);
        runtime->sequences.assign(slot_count,QwenSequence{});
        // Which slot is big matters only to the router, which will not send a
        // prompt to a slot that cannot hold it.
        for(std::size_t i=0;i<runtime->sequences.size();++i)
            runtime->sequences[i].geometry=qwen_slot_geometry_index(*runtime,i);
        runtime->slot_owner.assign(slot_count,-1);
        runtime->slot_events.assign(slot_count,0);
        for(auto&event:runtime->slot_events)
            if(flyweight_gpu_event_create(&event)!=0)throw std::runtime_error("failed to create native Qwen slot events");
        if(flyweight_gpu_event_create(&runtime->staging_event)!=0)throw std::runtime_error("failed to create native Qwen staging event");
        if(flyweight_gpu_event_create(&runtime->prefetch_event)!=0){flyweight_gpu_event_destroy(runtime->staging_event);throw std::runtime_error("failed to create native Qwen prefetch event");}
        // Prefill expert-streaming arena, env-budgeted while the plan is in
        // its measured stages (plans/prefill-expert-stream.md). Charged into
        // the base total below like every other arena, so the expert cache
        // shrinks by exactly what the stream takes. Meaningless without a
        // routed-expert model, and the CPU backend would stream to itself.
        runtime->prefill_stream_bytes=0;
        runtime->prefill_stream_scratch_bytes=0;
        runtime->prefill_stream_scratch_span=0;
        // The option decides; the env var overrides it for A/B. Auto (-1)
        // takes the measured 48 MiB optimum -- small on purpose, because the
        // restaging cost scales with the budget while the densest-first
        // benefit saturates -- and, unlike an explicit budget, degrades to
        // zero below rather than failing prepare when the GPU budget cannot
        // carry the arena.
        std::int64_t stream_request=runtime->options.prefill_expert_stream_mib;
        if(const char*stream_setting=
               std::getenv("FLYWEIGHT_PREFILL_EXPERT_STREAM_MIB"))
            stream_request=std::strtoll(stream_setting,nullptr,10);
        bool stream_auto=stream_request<0;
        if(stream_auto)stream_request=48;
        if(stream_request>0&&runtime->model->config.expert_count&&
           !flyweight_backend_is_cpu()&&
           qwen_expert_policy(*runtime,
               flyweight::v2::ExpertExecutionPhase::prepare)
                   .routed_gpu_execution_allowed())
            runtime->prefill_stream_bytes=
                static_cast<std::uint64_t>(stream_request)*1024ull*1024;
        else stream_auto=false;
        if(runtime->prefill_stream_bytes){
            // Expert-GEMM scratch, sized for the widest span. The offset
            // derivation in qwen_forward_rows walks these regions in exactly
            // this order; the two must not drift.
            const std::uint64_t stream_rows=runtime->forward_rows_capacity;
            const std::uint64_t stream_hidden=
                runtime->model->config.hidden_size;
            const std::uint64_t stream_inter=runtime->moe_intermediate;
            const std::uint64_t stream_top_k=
                runtime->model->config.expert_used_count;
            std::uint64_t span=0;
            span+=device_align(stream_rows*stream_top_k*sizeof(std::int32_t));
            span+=device_align(stream_rows*stream_top_k*sizeof(float));
            span+=device_align(stream_rows*stream_hidden*sizeof(float));
            span+=device_align(stream_rows*stream_hidden);
            span+=device_align(stream_rows*(stream_hidden/32+1)*sizeof(float));
            span+=device_align(stream_rows*2*stream_inter*sizeof(float));
            span+=device_align(stream_rows*stream_inter*sizeof(float));
            span+=device_align(stream_rows*stream_inter);
            span+=device_align(stream_rows*(stream_inter/32+1)*sizeof(float));
            span+=device_align(stream_rows*stream_hidden*sizeof(float));
            // The routed MMQ's index and pointer tables. Small -- ~200 KiB at
            // this model's shape -- but they ride the same walk, so the worst
            // case has to be sized here and taken in the same order there.
            // Worst-case padding is one short block per expert.
            const std::uint64_t stream_routes=stream_rows*stream_top_k;
            const std::uint64_t stream_experts=
                runtime->model->config.expert_count;
            const std::uint64_t stream_padded=stream_routes+
                stream_experts*(flyweight::v2::moe::kMmqBlockSize-1);
            const std::uint64_t stream_blocks=
                stream_padded/flyweight::v2::moe::kMmqBlockSize+1;
            for(int role=0;role<3;++role)
                span+=device_align(stream_experts*sizeof(std::uint64_t));
            span+=device_align(stream_blocks*sizeof(std::int32_t));
            span+=device_align(stream_padded*sizeof(std::int32_t));
            span+=device_align(stream_routes*sizeof(std::int32_t));
            span+=device_align(stream_routes*sizeof(float));
            runtime->prefill_stream_scratch_span=span;
            runtime->prefill_stream_scratch_bytes=2*span;
        }
        const auto base_total=runtime->static_arena_bytes+runtime->workspace_bytes+runtime->slots_state_bytes+runtime->expert_staging_bytes+slot_count*runtime->prefill_snapshots.size()*runtime->prefill_snapshot_bytes+runtime->host_ffn_stage_bytes+runtime->prefill_stream_bytes+runtime->prefill_stream_scratch_bytes;
        const char* nvfp4_persistent_env =
            std::getenv("FLYWEIGHT_NVFP4_PERSISTENT");
        const bool persistent_nvfp4_eligible=
            runtime->model->config.expert_count&&
            std::all_of(runtime->layers.begin(),runtime->layers.end(),
                [&](const QwenLayerPlan&layer){
                    return layer.expert_tensors[0]<
                               runtime->model->tensors.size()&&
                           layer.expert_tensors[1]<
                               runtime->model->tensors.size()&&
                           layer.expert_tensors[2]<
                               runtime->model->tensors.size()&&
                           runtime->model->tensors[
                               layer.expert_tensors[0]].type==40&&
                           runtime->model->tensors[
                               layer.expert_tensors[1]].type==40&&
                           runtime->model->tensors[
                               layer.expert_tensors[2]].type==40;
                });
        const bool persistent_nvfp4_requested=
            persistent_nvfp4_eligible&&nvfp4_persistent_env&&
            nvfp4_persistent_env[0]=='1';
        // gpu_cache_bytes is the TOTAL GPU budget (base allocations + expert
        // cache). 0 = auto-fit: probe free VRAM and use most of it, leaving a
        // headroom margin. Any positive value is an exact manual budget.
        std::uint64_t gpu_budget=runtime->options.gpu_cache_bytes;
        const bool auto_fit=(gpu_budget==0);
        // Not gated on routed_gpu_execution_allowed(): the budget bounds the
        // BASE allocations as well as the expert cache, and every feasibility
        // check below reads `gpu_budget &&`. Deriving it only for models with
        // routed experts left a dense one with no budget and therefore no
        // check at all -- `--parallel 2` on a model that fits exactly once ran
        // the whole prepare, including a 31 s host re-encode, and then died on
        // whichever allocation happened to be next.
        if(auto_fit){
            FlyweightV2GpuInfo gi{};
            if(gpu_probe(gi,runtime->options.device)==0&&gi.free_memory>0){
                // Leave headroom for the CUDA context, activations and other
                // apps. A flat margin starves tight cards (leaving <2 GiB free
                // measurably slows decode via memory pressure), so scale it
                // with total VRAM: max(2 GiB, 1/8 of the card).
                const std::uint64_t margin=std::max<std::uint64_t>(2048ull*1024*1024, gi.total_memory/8);
                if(gi.free_memory>margin)gpu_budget=gi.free_memory-margin;
            }
        }
        // The automatic streaming budget yields before it starves anything:
        // if the arena plus scratch push the base allocations past the GPU
        // budget, auto drops to zero where an explicit request would fail
        // prepare below.
        std::uint64_t base_total_resolved=base_total;
        if(stream_auto&&gpu_budget&&base_total_resolved>gpu_budget&&
           base_total_resolved-runtime->prefill_stream_bytes-
               runtime->prefill_stream_scratch_bytes<=gpu_budget){
            base_total_resolved-=runtime->prefill_stream_bytes+
                runtime->prefill_stream_scratch_bytes;
            runtime->prefill_stream_bytes=0;
            runtime->prefill_stream_scratch_bytes=0;
            runtime->prefill_stream_scratch_span=0;
            std::fprintf(stderr,
                "[flyweight] prefill expert streaming disabled: the GPU "
                "budget cannot carry the arena\n");
        }
        // Auto-fit was exempt from this, on the theory that it yields rather
        // than fails. It does yield -- but only for the expert cache and the
        // streaming arena, never for the base allocations, so a request that
        // does not fit fell straight through to whichever device allocation
        // happened to be next and died naming its victim ("failed to allocate
        // native Qwen prefill snapshots") instead of the cause. The same
        // arithmetic is known here, one message earlier, with the shortfall in
        // it.
        // FLYWEIGHT_GPU_PLAN=1 prints what prepare thinks it needs against what
        // it thinks it has. Every "failed to allocate ..." is one of these two
        // numbers being wrong, and without them the only evidence is which
        // allocation happened to be next.
        if(const char*plan=std::getenv("FLYWEIGHT_GPU_PLAN");plan&&plan[0]=='1'){
            FlyweightV2GpuInfo now{};
            gpu_probe(now,runtime->options.device);
            auto m=[](std::uint64_t b){return static_cast<double>(b)/(1024.0*1024.0);};
            std::fprintf(stderr,
                "[flyweight] gpu plan: static %.0f + workspace %.0f + slots %.0f"
                " + staging %.0f + snapshots %.0f + host-ffn stage %.0f"
                " + stream %.0f = %.0f MiB; budget %.0f MiB (%s); free now %.0f MiB\n",
                m(runtime->static_arena_bytes),m(runtime->workspace_bytes),
                m(runtime->slots_state_bytes),m(runtime->expert_staging_bytes),
                m(slot_count*runtime->prefill_snapshots.size()*
                  runtime->prefill_snapshot_bytes),
                m(runtime->host_ffn_stage_bytes),
                m(runtime->prefill_stream_bytes+runtime->prefill_stream_scratch_bytes),
                m(base_total_resolved),m(gpu_budget),auto_fit?"auto":"explicit",
                m(now.free_memory));
        }
        // Auto-fit shrinks the context to what the card can hold rather than
        // refusing the request. The context is the one term here nobody
        // usually chose -- it defaults to the checkpoint's maximum, which on a
        // 262144-token model reserves 16 GiB for a single slot -- whereas
        // --parallel is always typed on purpose. So the context yields first
        // and the slot count is left alone; a run that still does not fit
        // falls through to the refusal below, which names both.
        //
        // Only under auto-fit: an explicit --gpu-cache-mib is a statement about
        // the budget, and silently serving a shorter context than asked for
        // would answer a question the caller had already answered.
        if(auto_fit&&!runtime->options.strict_resident&&gpu_budget&&
           base_total_resolved>gpu_budget&&runtime->slots_state_bytes){
            const auto fixed=base_total_resolved-runtime->slots_state_bytes;
            const std::uint64_t requested=runtime->options.context_limit;
            // Below this a server is not worth starting, and the refusal says
            // more than a context nothing fits in.
            constexpr std::uint64_t kContextFloor=2048;
            if(gpu_budget>fixed&&requested>kContextFloor){
                const auto room=gpu_budget-fixed;
                std::uint64_t low=kContextFloor,high=requested,best=0;
                while(low<=high){
                    const auto middle=low+(high-low)/2;
                    plan_slots(middle);
                    if(runtime->slots_state_bytes<=room){
                        best=middle;
                        low=middle+1;
                    }else{
                        if(middle==kContextFloor)break;
                        high=middle-1;
                    }
                }
                plan_slots(best?best:requested);
                if(best){
                    base_total_resolved=fixed+runtime->slots_state_bytes;
                    std::fprintf(stderr,
                        "[flyweight] context %llu -> %llu tokens: %llu slot%s "
                        "of the requested context need %llu MiB more than this "
                        "device has free. Pass --context-window to choose the "
                        "trade yourself.\n",
                        static_cast<unsigned long long>(requested),
                        static_cast<unsigned long long>(best),
                        static_cast<unsigned long long>(slot_count),
                        slot_count==1?"":"s",
                        static_cast<unsigned long long>(
                            (base_total-gpu_budget)/(1024ull*1024)));
                }
            }
        }
        if(!runtime->options.strict_resident&&
           gpu_budget&&base_total_resolved>gpu_budget){
            auto mib=[](std::uint64_t b){return std::to_string(b/(1024ull*1024));};
            throw std::runtime_error(
                "native Qwen base CUDA allocations ("+mib(base_total_resolved)+" MiB = static weights "
                +mib(runtime->static_arena_bytes)+" + workspace "+mib(runtime->workspace_bytes)+" + "
                +(runtime->geometries.size()>1
                    ?"1x KV slot "+mib(runtime->geometries[0].state_bytes)+" + "
                     +std::to_string(slot_count-1)+"x scratch slot "
                     +mib(runtime->geometries[1].state_bytes)+" ("
                    :std::to_string(slot_count)+"x KV slot "+mib(runtime->state_bytes)+" (")
                +mib(runtime->slots_state_bytes)+") + staging "+mib(runtime->expert_staging_bytes)
                +") exceed the "
                +(auto_fit?"VRAM this device has free (":"--gpu-cache-mib budget (")
                +mib(gpu_budget)+" MiB), before any expert cache. Short by "
                +mib(base_total_resolved-gpu_budget)+" MiB. Lower --parallel or "
                 "--context-window, shrink the side slots with --scratch-context"
                +(auto_fit?", or free VRAM on this device."
                          :", or raise --gpu-cache-mib."));
        }
        // expert_slot_bytes is zero for a dense model, which has no expert set
        // to cache and would trap on the slot arithmetic below.
        if(prepare_policy.routed_gpu_execution_allowed()&&
           gpu_budget>base_total_resolved&&runtime->expert_slot_bytes){
            auto available=gpu_budget-base_total_resolved;
            const std::uint64_t cache_copies=
                persistent_nvfp4_requested?2:1;
            const auto slot_budget=runtime->expert_slot_bytes*cache_copies;
            auto cache=(available/slot_budget)*runtime->expert_slot_bytes;
            // Never allocate more cache than the whole expert set (every
            // (layer,expert) resident => zero misses); saves VRAM on big GPUs.
            const auto max_cache=static_cast<std::uint64_t>(
                qwen_cache_layer_count(*runtime))*
                runtime->model->config.expert_count*runtime->expert_slot_bytes;
            if(cache>max_cache)cache=max_cache;
            const char*whole_layer_setting=
                std::getenv("FLYWEIGHT_LAGUNA_WHOLE_LAYERS");
            // Gemma 4 takes the same placement: its expert selection is close
            // to uniform, so per-expert paging replaces a miss with a 3.3 MB
            // PCIe upload every few tokens and eats what the cache saves --
            // measured 85% hits and still no faster than pure-CPU experts.
            // Whole pinned layers page each bundle once and never again,
            // which is the static GPU/CPU layer split llama.cpp runs.
            const bool whole_layer_enabled=(runtime->gemma4||
                (runtime->laguna&&qwen_model_has_iq_experts(*runtime)))&&
                (!whole_layer_setting||whole_layer_setting[0]!='0');
            if(whole_layer_enabled&&!runtime->options.strict_resident){
                const auto experts=runtime->model->config.expert_count;
                std::vector<std::uint32_t> candidates;
                for(std::uint32_t layer=0;layer<runtime->layers.size();++layer)
                    if(!runtime->layers[layer].dense_ffn)
                        candidates.push_back(layer);
                std::size_t layer_count=experts
                    ?std::min<std::size_t>(
                        candidates.size(),cache/runtime->expert_slot_bytes/experts)
                    :0;
                char*end=nullptr;
                const auto requested=whole_layer_setting
                    ?std::strtoul(whole_layer_setting,&end,10):0;
                if(whole_layer_setting&&end!=whole_layer_setting&&requested)
                    layer_count=std::min<std::size_t>(layer_count,requested);
                runtime->whole_expert_layer_slots.assign(
                    runtime->layers.size(),-1);
                std::size_t slot=0;
                for(std::size_t index=0;index<layer_count;++index){
                    const auto layer=candidates[candidates.size()-layer_count+index];
                    runtime->whole_expert_layer_slots[layer]=
                        static_cast<std::int32_t>(slot);
                    slot+=experts;
                }
                cache=slot*runtime->expert_slot_bytes;
                if(!layer_count)
                    runtime->whole_expert_layer_slots.clear();
                std::fprintf(stderr,
                    "[flyweight] Laguna whole-layer placement selected %zu "
                    "layers (%zu expert bundles)\n",layer_count,slot);
            }
            runtime->expert_cache_bytes=(cache/runtime->expert_slot_bytes<runtime->model->config.expert_used_count)?0:cache;
        }
        const auto resident_expert_bytes=static_cast<std::uint64_t>(
            qwen_cache_layer_count(*runtime))*
            runtime->model->config.expert_count*runtime->expert_slot_bytes;
        if(runtime->options.strict_resident&&resident_expert_bytes&&
           runtime->expert_cache_bytes<resident_expert_bytes){
            auto mib=[](std::uint64_t bytes){
                return std::to_string((bytes+1024ull*1024-1)/(1024ull*1024));
            };
            const auto kv_bytes=runtime->slots_state_bytes;
            const auto snapshot_bytes=slot_count*
                runtime->prefill_snapshots.size()*runtime->prefill_snapshot_bytes;
            const auto required=runtime->static_arena_bytes+kv_bytes+
                runtime->workspace_bytes+runtime->expert_staging_bytes+
                snapshot_bytes+resident_expert_bytes*
                    (persistent_nvfp4_requested?2:1);
            throw std::runtime_error(
                "resident expert mode requires "+mib(required)+
                " MiB CUDA budget = static "+mib(runtime->static_arena_bytes)+
                " MiB + KV/state "+mib(kv_bytes)+
                " MiB + workspace "+mib(runtime->workspace_bytes)+
                " MiB + staging "+mib(runtime->expert_staging_bytes)+
                " MiB + snapshots "+mib(snapshot_bytes)+
                " MiB + experts "+mib(resident_expert_bytes)+
                (persistent_nvfp4_requested
                    ? " MiB + persistent NVFP4 mirror "+
                        mib(resident_expert_bytes)
                    : "")+
                " MiB; available budget is "+mib(gpu_budget)+
                " MiB. Resident mode never falls back to paging; use "
                "expert_mode='auto' or raise --gpu-cache-mib.");
        }
        // A dense model routes nothing, so the expert staging arena is empty --
        // and a zero-byte CUDA allocation is an error, not a no-op.
        if(flyweight_gpu_alloc(runtime->static_arena_bytes,&runtime->static_arena)!=0||
           flyweight_gpu_alloc(runtime->workspace_bytes,&runtime->workspace)!=0||
           (runtime->expert_staging_bytes&&flyweight_gpu_alloc(runtime->expert_staging_bytes,&runtime->expert_staging)!=0)||
           (runtime->prefill_stream_bytes&&flyweight_gpu_alloc(runtime->prefill_stream_bytes,&runtime->prefill_stream_arena)!=0)||
           (runtime->prefill_stream_scratch_bytes&&flyweight_gpu_alloc(runtime->prefill_stream_scratch_bytes,&runtime->prefill_stream_scratch)!=0)||
           (runtime->host_staging_bytes&&flyweight_gpu_host_alloc(runtime->host_staging_bytes,&runtime->host_staging)!=0))throw std::runtime_error("failed to allocate native Qwen CUDA arenas");
        // A turbo cache is expanded to f16 one layer at a time so the cuBLAS
        // attention path can run on it, so this only has to hold the widest
        // attention layer's live window (K and V), not the whole cache.
        // q8_0 shares the buffer: staged for the same reason, and the same
        // f16 window once expanded.
        auto stages_kv=[](int t){return kv_type_is_turbo(t)||t==3;};
        if(stages_kv(runtime->options.cache_type_k)
           ||stages_kv(runtime->options.cache_type_v)){
            std::uint64_t widest=0;
            for(const auto& layer:runtime->layers){
                if(!layer.attention)continue;
                widest=std::max<std::uint64_t>(widest,
                    static_cast<std::uint64_t>(layer.kv_heads)*layer.cache_capacity*layer.head_dim);
            }
            if(widest){
                runtime->turbo_kv_stage_stride=device_align(widest*sizeof(std::uint16_t));
                runtime->turbo_kv_stage_bytes=runtime->turbo_kv_stage_stride*2;
                if(flyweight_gpu_alloc(runtime->turbo_kv_stage_bytes,&runtime->turbo_kv_stage)!=0)
                    throw std::runtime_error("failed to allocate native Qwen turbo KV staging");
            }
        }
        // Importance-matrix capture. A slot per input channel of every 2-D
        // weight and per (expert, channel) of every stacked 3-D one; tensors
        // the forward never projects through simply stay at zero rows and are
        // skipped on read-back. Allocated only under the switch: the arena is
        // tens of MB on a large checkpoint and calibration is a dedicated run,
        // not a serving mode.
        if(const char* capture=std::getenv("FLYWEIGHT_IMATRIX");
           capture&&capture[0]&&std::strcmp(capture,"0")!=0){
            const auto& tensors=runtime->model->tensors;
            runtime->imatrix_offsets.assign(tensors.size(),~0ull);
            std::uint64_t total=0;
            for(std::size_t i=0;i<tensors.size();++i){
                const auto& shape=tensors[i].shape;
                if(shape.size()<2||shape.size()>3)continue;
                runtime->imatrix_offsets[i]=total;
                total+=shape[0]*(shape.size()==3?shape[2]:1);
            }
            runtime->imatrix_elements=total;
            runtime->imatrix_host.assign(total,0.0f);
            runtime->imatrix_rows.assign(tensors.size(),0);
            if(flyweight_gpu_alloc(total*sizeof(float),&runtime->imatrix_sums)!=0||
               flyweight_gpu_memset(runtime->imatrix_sums,0,total*sizeof(float),
                                  runtime->stream)!=0||
               flyweight_gpu_stream_sync(runtime->stream)!=0)
                throw std::runtime_error(
                    "failed to allocate the native Qwen imatrix arena");
        }
        if(runtime->embeddings_host_resident){
            // Sized for the widest gather: the rows forward embeds a whole
            // prefill chunk in one launch, single-token decode uses row 0 only.
            const std::uint64_t capacity=
                std::max<std::uint64_t>(1,runtime->forward_rows_capacity);
            runtime->embedding_stage_bytes=
                device_align(capacity*runtime->embedding_row_bytes);
            if(flyweight_gpu_alloc(runtime->embedding_stage_bytes,&runtime->embedding_stage)!=0||
               flyweight_gpu_host_alloc(runtime->embedding_stage_bytes,&runtime->embedding_host)!=0||
               flyweight_gpu_alloc(device_align(capacity*sizeof(std::uint32_t)),&runtime->embedding_row_index)!=0||
               flyweight_gpu_event_create(&runtime->embedding_event)!=0)
                throw std::runtime_error(
                    "failed to allocate native Qwen embedding staging");
            // The rows kernel gathers by token id; the staged copy is already
            // in row order, so it is indexed by a fixed identity permutation.
            std::vector<std::uint32_t> identity(capacity);
            for(std::uint64_t row=0;row<capacity;++row)
                identity[row]=static_cast<std::uint32_t>(row);
            if(flyweight_gpu_upload_sync(runtime->embedding_row_index,identity.data(),
                                       capacity*sizeof(std::uint32_t))!=0)
                throw std::runtime_error(
                    "failed to seed native Qwen embedding row index");
        }
        if(runtime->host_ffn_layers){
            // Pinned so the round trip to the host SwiGLU is a plain DMA. Only
            // the DMA endpoints need pinning; the activation scratch lives on
            // the heap. Sized for a whole prefill batch rather than one row,
            // because that path now copies the batch in one transfer -- at 128
            // rows of a 5120-wide hidden state this is 5 MiB, against the 40
            // KiB a single row needed.
            // forward_rows_capacity, not prefill_rows: it is the bound
            // qwen_forward_rows enforces on `rows`, and MTP verification hands
            // it a batch of its own.
            const std::uint64_t staged_rows=
                std::max<std::uint64_t>(runtime->forward_rows_capacity,1);
            runtime->dense_host_bytes=device_align(
                2ull*staged_rows*runtime->model->config.hidden_size*sizeof(float));
            if(flyweight_gpu_host_alloc(runtime->dense_host_bytes,&runtime->dense_host)!=0)
                throw std::runtime_error("failed to allocate native Qwen dense host scratch");
            // Device side of the batch path: one block's feed-forward triple,
            // uploaded per block per chunk and consumed by the ordinary device
            // kernels. Reused across blocks, which is safe because the upload
            // and the kernels that read it are ordered on the one stream.
            if(runtime->host_ffn_stage_bytes&&
               flyweight_gpu_alloc(runtime->host_ffn_stage_bytes,
                                 &runtime->host_ffn_stage)!=0)
                throw std::runtime_error(
                    "failed to allocate native Qwen host feed-forward staging");
        }
        for(auto&seq:runtime->sequences){
            const auto bytes=seq.geometry<runtime->geometries.size()
                ?runtime->geometries[seq.geometry].state_bytes:runtime->state_bytes;
            if(flyweight_gpu_alloc(bytes,&seq.state)!=0)throw std::runtime_error("failed to allocate native Qwen sequence state");
        }
        runtime->state=runtime->sequences[0].state;
        runtime->active_sequence=0;
        qwen_apply_geometry(*runtime,runtime->sequences[0].geometry);
        if(runtime->expert_cache_bytes&&flyweight_gpu_alloc(runtime->expert_cache_bytes,&runtime->expert_cache)!=0)throw std::runtime_error("failed to allocate native Qwen expert cache");
        if(runtime->expert_cache_bytes&&persistent_nvfp4_requested){
            runtime->expert_native_cache_bytes=runtime->expert_cache_bytes;
            if(flyweight_gpu_alloc(runtime->expert_native_cache_bytes,
                                 &runtime->expert_native_cache)!=0)
                throw std::runtime_error(
                    "failed to allocate persistent NVFP4 expert cache");
        }
        if(runtime->prefill_snapshot_bytes){
            // Slot 0's checkpoint pool lives in runtime->prefill_snapshots (the
            // active mirror); slots 1..N-1 own theirs in sequences[i].
            for(auto&snapshot:runtime->prefill_snapshots)if(flyweight_gpu_alloc(runtime->prefill_snapshot_bytes,&snapshot.device)!=0)throw std::runtime_error("failed to allocate native Qwen prefill snapshots");
            for(std::size_t i=1;i<runtime->sequences.size();++i){
                runtime->sequences[i].prefill_snapshots.assign(runtime->prefill_snapshots.size(),QwenPrefillSnapshot{});
                for(auto&snapshot:runtime->sequences[i].prefill_snapshots)if(flyweight_gpu_alloc(runtime->prefill_snapshot_bytes,&snapshot.device)!=0)throw std::runtime_error("failed to allocate native Qwen prefill snapshots");
            }
        }
        if(runtime->expert_slot_bytes)runtime->expert_slots.resize(runtime->expert_cache_bytes/runtime->expert_slot_bytes);
        // Say how many expert slots this budget bought, and where the budget
        // came from. Slot count decides expert placement, placement decides
        // greedy output, and under auto-fit the count follows free VRAM at
        // startup -- so two boots of the same server can answer differently.
        // Naming the exact --gpu-cache-mib to pin turns "output changed after
        // a reboot" from a debugging session into a log line.
        if(!runtime->expert_slots.empty()){
            if(auto_fit)
                std::fprintf(stderr,
                    "[flyweight] expert cache: %zu slots (%llu MiB) from an "
                    "auto-fit %llu MiB GPU budget; free VRAM varies between "
                    "runs, pass --gpu-cache-mib %llu to make expert placement "
                    "reproducible\n",
                    runtime->expert_slots.size(),
                    static_cast<unsigned long long>(
                        runtime->expert_cache_bytes/(1024ull*1024)),
                    static_cast<unsigned long long>(gpu_budget/(1024ull*1024)),
                    static_cast<unsigned long long>(gpu_budget/(1024ull*1024)));
            else
                std::fprintf(stderr,
                    "[flyweight] expert cache: %zu slots (%llu MiB) within "
                    "the %llu MiB --gpu-cache-mib budget\n",
                    runtime->expert_slots.size(),
                    static_cast<unsigned long long>(
                        runtime->expert_cache_bytes/(1024ull*1024)),
                    static_cast<unsigned long long>(gpu_budget/(1024ull*1024)));
        }
        // Hybrid is an optimization over the CPU expert path, not a hard
        // requirement. Auto-fit can legitimately leave no room for even one
        // routed-expert working set after static weights, KV state, workspace,
        // and staging are allocated (notably for large Q6 models on 12 GiB
        // cards). In that case, keep the CUDA-resident shared model and execute
        // routed experts on CPU instead of failing on the first decode.
        // Routed experts in an IQ codebook format have no grouped GPU kernel.
        // Without this the dispatch below falls through to the k-quant kernel
        // and decodes codebook bytes as super-block scales, which produces
        // fluent-looking output for as long as the expert cache stays cold and
        // then degenerates once experts become resident.
        // IQ experts do have grouped GPU kernels now, but splitting a layer
        // between the router on the GPU and the experts on the host costs a
        // round trip per layer. Laguna therefore concentrates its cache into
        // complete pinned layers; other IQ models still require an explicitly
        // seeded set before the GPU path is allowed.
        const bool seeded_placement=runtime->options.prefill_cache_seed!=0||
                                    runtime->options.prefill_cache_seed_auto!=0;
        // Gated on the decode policy, not the prepare one: `auto` prepares as
        // CPU and only promotes a hot set at decode, so a prepare-phase test
        // would let IQ experts reach the GPU anyway.
        const bool gpu_at_decode=!qwen_expert_policy(
            *runtime,flyweight::v2::ExpertExecutionPhase::decode).is_cpu();
        if(gpu_at_decode&&runtime->model->config.expert_count&&
           (!qwen_gpu_experts_executable(*runtime)||
            (qwen_model_has_iq_experts(*runtime)&&!seeded_placement&&
             runtime->whole_expert_layer_slots.empty()))){
            runtime->expert_mode=flyweight::v2::ExpertExecutionMode::cpu;
            runtime->options.moe_device=flyweight::v2::expert_execution_mode_value(
                runtime->expert_mode);
            prepare_policy=qwen_expert_policy(
                *runtime,flyweight::v2::ExpertExecutionPhase::prepare);
            std::fprintf(stderr,
                "[flyweight] routed experts stay on the CPU MoE; set "
                "--prefill-cache-seed or FLYWEIGHT_LAGUNA_WHOLE_LAYERS to "
                "place IQ experts on the GPU\n");
        }
        if(prepare_policy.is_hybrid()&&runtime->model->config.expert_count&&
           runtime->expert_slots.empty()){
            runtime->expert_mode=flyweight::v2::ExpertExecutionMode::cpu;
            runtime->options.moe_device=flyweight::v2::expert_execution_mode_value(
                runtime->expert_mode);
            prepare_policy=qwen_expert_policy(
                *runtime,flyweight::v2::ExpertExecutionPhase::prepare);
            std::fprintf(stderr,
                "[flyweight] hybrid expert cache does not fit; falling back to CPU MoE\n");
        }
        runtime->expert_history.resize(
            qwen_cache_layer_count(*runtime) *
            runtime->model->config.expert_count
        );
        qwen_load_expert_history(*runtime);
        runtime->device_tensors.assign(runtime->model->tensors.size(),0);
        std::uint64_t cursor=0;
        std::vector<float> widened;
        std::vector<std::uint8_t> packed;
        for(std::uint64_t index=0;index<persistent.size();++index){
            if(!persistent[index])continue;
            const auto&t=runtime->model->tensors[index];
            const auto device_bytes=qwen_device_tensor_size(*runtime,index);
            if(const auto* aliased=qwen_alias_static_tensor(*runtime,index)){
                // Point at the mapping instead of copying into the arena, and
                // leave the cursor alone -- no arena space was reserved.
                runtime->device_tensors[index]=
                    reinterpret_cast<std::uint64_t>(aliased);
                runtime->aliased_tensors++;
                runtime->aliased_tensor_bytes+=device_bytes;
                continue;
            }
            runtime->device_tensors[index]=runtime->static_arena+cursor;
            if(qwen_device_type(*runtime,index)==8&&t.type!=8){
                const std::uint64_t elements=device_bytes/kQ8BlockSize*32;
                widened.resize(elements);
                packed.resize(device_bytes);
                const auto* source=tensor_data(*runtime->model,t);
                if(t.type==30){
                    const auto* halves=
                        reinterpret_cast<const std::uint16_t*>(source);
                    for(std::uint64_t i=0;i<elements;++i)
                        widened[i]=qwen_bf16_value(halves[i]);
                }else{
                    for(std::uint64_t i=0;i<elements;++i)
                        widened[i]=tensor_value(source,t.type,i);
                }
                pack_q8_0(widened.data(),elements,packed.data());
                if(flyweight_gpu_upload_sync(runtime->device_tensors[index],packed.data(),device_bytes)!=0)throw std::runtime_error("failed to upload requantized native Qwen static tensor");
            }else if(flyweight_gpu_upload_sync(runtime->device_tensors[index],tensor_data(*runtime->model,t),t.size)!=0)throw std::runtime_error("failed to upload native Qwen static tensor");
            cursor+=device_align(device_bytes);
        }
        if(runtime->aliased_tensors)
            std::fprintf(stderr,
                "[flyweight] %llu static tensors (%llu MiB) served from the GGUF "
                "mapping; device arena is %llu MiB\n",
                static_cast<unsigned long long>(runtime->aliased_tensors),
                static_cast<unsigned long long>(runtime->aliased_tensor_bytes/(1024ull*1024)),
                static_cast<unsigned long long>(runtime->static_arena_bytes/(1024ull*1024)));
        if(runtime->options.strict_resident&&resident_expert_bytes){
            const auto experts=runtime->model->config.expert_count;
            const auto cache_layers=qwen_cache_layer_count(*runtime);
            for(std::uint32_t layer=0;layer<cache_layers;++layer){
                const auto&plan=layer<runtime->layers.size()
                    ? runtime->layers[layer]:runtime->mtp_layer_plan;
                for(std::uint32_t expert=0;expert<experts;++expert){
                    const auto slot_index=
                        static_cast<std::size_t>(layer)*experts+expert;
                    auto&slot=runtime->expert_slots[slot_index];
                    const auto slot_base=runtime->expert_cache+
                        slot_index*runtime->expert_slot_bytes;
                    std::uint64_t role_offset=0;
                    for(int role=0;role<3;++role){
                        const auto&t=runtime->model->tensors[
                            plan.expert_tensors[role]];
                        const auto bytes=t.size/experts;
                        const auto source_offset=
                            static_cast<std::uint64_t>(expert)*bytes;
                        if(flyweight_gpu_upload(
                                slot_base+role_offset,
                                tensor_data(*runtime->model,t)+source_offset,
                                bytes,runtime->stream)!=0)
                            throw std::runtime_error(
                                "failed to prepare resident expert tensor");
                        role_offset+=bytes;
                    }
                    if(role_offset>runtime->expert_slot_bytes)
                        throw std::runtime_error(
                            "resident expert bundle exceeds its cache slot");
                    slot.key=(static_cast<std::uint64_t>(layer)<<32)|expert;
                    slot.valid=true;
                    slot.last_used=++runtime->expert_clock;
                    runtime->expert_residency[slot.key]=slot_index;
                }
            }
            std::fprintf(stderr,
                "[flyweight] resident expert mode prepared %zu experts (%llu MiB)\n",
                runtime->expert_slots.size(),
                static_cast<unsigned long long>(
                    resident_expert_bytes/(1024ull*1024)));
        }else if(!runtime->whole_expert_layer_slots.empty()){
            const auto experts=runtime->model->config.expert_count;
            std::size_t prepared=0;
            for(std::uint32_t layer=0;layer<runtime->layers.size();++layer){
                const auto first_slot=runtime->whole_expert_layer_slots[layer];
                if(first_slot<0)continue;
                const auto&plan=runtime->layers[layer];
                for(std::uint32_t expert=0;expert<experts;++expert){
                    const auto slot_index=static_cast<std::size_t>(first_slot)+expert;
                    auto&slot=runtime->expert_slots.at(slot_index);
                    const auto slot_base=runtime->expert_cache+
                        slot_index*runtime->expert_slot_bytes;
                    std::uint64_t role_offset=0;
                    for(int role=0;role<3;++role){
                        const auto&t=runtime->model->tensors[
                            plan.expert_tensors[role]];
                        const auto bytes=t.size/experts;
                        const auto source_offset=
                            static_cast<std::uint64_t>(expert)*bytes;
                        if(flyweight_gpu_upload(
                                slot_base+role_offset,
                                tensor_data(*runtime->model,t)+source_offset,
                                bytes,runtime->stream)!=0)
                            throw std::runtime_error(
                                "failed to prepare Laguna whole-layer expert");
                        role_offset+=bytes;
                    }
                    if(role_offset>runtime->expert_slot_bytes)
                        throw std::runtime_error(
                            "Laguna whole-layer expert exceeds its cache slot");
                    slot.key=(static_cast<std::uint64_t>(layer)<<32)|expert;
                    slot.valid=true;
                    slot.pinned=true;
                    slot.last_used=++runtime->expert_clock;
                    runtime->expert_residency[slot.key]=slot_index;
                    ++prepared;
                }
            }
            std::fprintf(stderr,
                "[flyweight] prepared %zu pinned Laguna whole-layer experts "
                "(%llu MiB)\n",prepared,
                static_cast<unsigned long long>(
                    runtime->expert_cache_bytes/(1024ull*1024)));
        }
        if(flyweight_gpu_memset(runtime->workspace,0,runtime->workspace_bytes,runtime->stream)!=0||
           flyweight_gpu_memset(runtime->state,0,runtime->state_bytes,runtime->stream)!=0||
           flyweight_gpu_stream_sync(runtime->stream)!=0)throw std::runtime_error("failed to initialize native Qwen CUDA arenas");
    }catch(...){release_qwen_device(*runtime);throw;}
        const auto paging_policy=qwen_expert_policy(
            *runtime,flyweight::v2::ExpertExecutionPhase::prepare);
        if(runtime->options.expert_paging>2)
            throw std::runtime_error("native Qwen expert paging mode is invalid");
        runtime->host_available_bytes=available_host_memory();
        const bool forced_direct=runtime->options.expert_paging==2||
            std::getenv("FLYWEIGHT_V2_DMA_PAGING");
        const auto model_bytes=runtime->model->mapped_bytes();
        const auto registration_headroom=std::max<std::uint64_t>(
            4ull*1024*1024*1024,model_bytes/4);
        // The old all-or-nothing test: the whole mapping plus headroom in RAM.
        // Kept because when it passes there is nothing to budget.
        const bool auto_direct=runtime->options.expert_paging==0&&
            runtime->host_available_bytes>=model_bytes+registration_headroom;
        // Partial coverage: register what fits when the whole mapping does not.
        // OPT-IN, not automatic, and the measurement is why. On the reference box
        // (68 GiB checkpoint, 60 GiB RAM) this pins 19.1 GiB over 25 of 48 layers
        // and a warm interleaved A/B measured 25.4 tok/s staged against 24.4
        // direct -- no win, inside the noise. Half the admissions still stage,
        // and the working set (39.8 experts + 25.7 n-gram + dense) exceeds RAM
        // whatever the pinning, so page pressure dominates either way. It stays
        // available for boxes where the numbers come out differently, but it does
        // not turn itself on to buy nothing and lock pages doing it.
        const bool budgeted_direct=forced_direct&&!auto_direct&&
            runtime->host_available_bytes>kRegisterReserveBytes+kRegisterMinimumBytes;
        // Both GPU-side MoE paths page experts in from the mmap, so both benefit
        // from registering it; only the pure-CPU path never touches the device
        // cache. This used to read `moe_device==2`, which left the streamed-GPU
        // path staging every miss through a host memcpy and made the GPU branch
        // of the next-layer prefetcher (which tests moe_device==0) dead code.
        // A dense model pages no experts, so pinning the whole mapping buys
        // nothing and costs seconds of registration plus locked host pages.
        const bool pages_experts=!runtime->options.strict_resident&&
            !runtime->layers.empty()&&!runtime->layers.front().dense_ffn;
        if(pages_experts&&paging_policy.routed_gpu_execution_allowed()&&
           (forced_direct||auto_direct||budgeted_direct)){
            const auto registration_started=std::chrono::steady_clock::now();
            // Register the EXPERT TENSORS, not the whole mapping. Registering the
            // file wholesale needs the entire checkpoint plus headroom to fit in
            // RAM, which a 68 GiB model on a 60 GiB box never does -- so direct
            // paging silently stayed off and every cache miss staged a ~1.2 MiB
            // expert bundle through a host memcpy (measured 95-97% of the
            // decode MoE phase's host time; plans/decode-device-dispatch.md
            // phase 0b). What is paged is the experts, so that is what has to be
            // pinned, and the budget then scales with them rather than with the
            // file. Coverage is per layer and may be partial: a layer whose
            // three tensors did not fit keeps the staged path.
            //
            // Page-align each span outward and merge, so adjacent tensors are
            // registered once and no partial page is left out.
            constexpr std::uint64_t kPage=4096;
            struct Span{const std::uint8_t* start;const std::uint8_t* end;};
            std::vector<Span> spans;
            std::vector<std::uint32_t> span_layer;
            // Aligned OUTWARD, then merged below. A registered range has to cover
            // every byte an upload reads: a copy whose source straddles the edge
            // of a registration fails (`DMA cache upload failed`), so trimming
            // inward to avoid shared boundary pages is not an option -- the
            // ranges must be merged into maximal spans instead.
            auto tensor_span=[&](std::uint64_t index){
                const auto& t=runtime->model->tensors[index];
                const auto* base=tensor_data(*runtime->model,t);
                const auto start=reinterpret_cast<std::uintptr_t>(base)/kPage*kPage;
                const auto end=(reinterpret_cast<std::uintptr_t>(base+t.size)
                    +kPage-1)/kPage*kPage;
                return Span{reinterpret_cast<const std::uint8_t*>(start),
                    reinterpret_cast<const std::uint8_t*>(end)};
            };
            std::uint64_t expert_bytes=0;
            for(const auto& layer:runtime->layers)
                for(int role=0;role<3;++role)
                    expert_bytes+=runtime->model->tensors[layer.expert_tensors[role]].size;
            // Registration LOCKS pages, so anything else the decode reads out of
            // the mapping has to keep its page cache or the memcpy this removes
            // simply reappears as major faults. Measured on qwen4exp: pinning all
            // 37 GiB of experts on a 60 GiB box cut the MoE phase from ~118 ms to
            // ~21 ms per token and pushed ~85 ms into the PLE n-gram gathers,
            // for no net win. So the budget subtracts the host-resident tables
            // (the 25.7 GiB n-gram table, host embeddings) as well as a reserve
            // for the OS -- partial expert coverage beats starving them.
            // Only tables we will NOT register are subtracted: the n-gram table is
            // gathered on the host (16 rows a token), so pinning it would buy
            // nothing and cost 25.7 GiB. The embedding IS registerable, so it is
            // charged to the budget below rather than reserved from it.
            std::uint64_t host_resident_bytes=0;
            if(runtime->ple_table!=std::numeric_limits<std::uint64_t>::max())
                host_resident_bytes+=
                    runtime->model->tensors[runtime->ple_table].size;
            const auto floor_bytes=kRegisterReserveBytes+host_resident_bytes;
            std::uint64_t budget=runtime->host_available_bytes>floor_bytes
                ?runtime->host_available_bytes-floor_bytes:0;
            if(const char* cap=std::getenv("FLYWEIGHT_EXPERT_REGISTER_MIB"))
                budget=std::min<std::uint64_t>(budget,
                    std::strtoull(cap,nullptr,10)*1024ull*1024ull);
            std::uint64_t planned=0;std::uint32_t covered=0;
            for(std::uint32_t index=0;index<runtime->layers.size();++index){
                const auto& layer=runtime->layers[index];
                std::uint64_t layer_bytes=0;
                for(int role=0;role<3;++role)
                    layer_bytes+=runtime->model->tensors[layer.expert_tensors[role]].size;
                if(planned+layer_bytes>budget)break;
                planned+=layer_bytes;++covered;
                for(int role=0;role<3;++role){
                    spans.push_back(tensor_span(layer.expert_tensors[role]));
                    span_layer.push_back(index);
                }
            }
            // The embedding stager DMAs a single row straight from the mapping
            // when it is registered, so include it if it still fits.
            const bool want_embedding=
                runtime->token_embeddings!=std::numeric_limits<std::uint64_t>::max()&&
                runtime->embeddings_host_resident;
            std::uint64_t embedding_bytes=want_embedding
                ?runtime->model->tensors[runtime->token_embeddings].size:0;
            const bool embedding_fits=want_embedding&&planned+embedding_bytes<=budget;
            if(embedding_fits){
                spans.push_back(tensor_span(runtime->token_embeddings));
                span_layer.push_back(std::numeric_limits<std::uint32_t>::max());
                planned+=embedding_bytes;
            }
            // Merge into maximal spans. Tensors are laid out in file order, so
            // this collapses to a handful of ranges (usually one per shard) and
            // no upload can straddle an edge.
            auto ordered=spans;
            std::sort(ordered.begin(),ordered.end(),
                [](const Span& a,const Span& b){return a.start<b.start;});
            std::vector<Span> merged;
            for(const auto& span:ordered){
                if(!merged.empty()&&span.start<=merged.back().end)
                    merged.back().end=std::max(merged.back().end,span.end);
                else merged.push_back(span);
            }
            // Promotion is therefore all-or-nothing per merged range rather than
            // per layer; with one range covering every expert it lands as a
            // single step once the worker finishes.
            const auto promoted=span_layer;
            if(!covered&&!embedding_fits){
                if(forced_direct)
                    throw std::runtime_error(
                        "direct expert paging requested, but no expert layer fits the "
                        "registration budget");
                std::fprintf(stderr,
                    "[flyweight] direct expert paging unavailable; using staged copies\n");
            }else{
                // Registration is ~2 GiB/s, so pinning 37 GiB costs ~20s -- which
                // is why this runs on a worker instead of at the front of the
                // first token. Until it finishes every layer stays on the staged
                // path, which is correct and merely slower, so a short one-shot
                // run no longer pays for a pin it will not use.
                runtime->model_registered=true;
                runtime->dma_paging=true;
                std::fprintf(stderr,
                    "[flyweight] direct expert paging: registering %.1f GiB "
                    "(%u/%zu expert layers%s) in the background\n",
                    planned/1073741824.0,covered,runtime->layers.size(),
                    embedding_fits?" + embeddings":"");
                if(covered<runtime->layers.size())
                    std::fprintf(stderr,
                        "[flyweight] %zu expert layers exceed the %.1f GiB "
                        "registration budget and keep staged copies; raise host RAM "
                        "or set FLYWEIGHT_EXPERT_REGISTER_MIB to tune\n",
                        runtime->layers.size()-covered,budget/1073741824.0);
                auto* target=runtime;
                target->registration_thread=std::thread([target,merged,promoted,
                                                         registration_started]{
                    std::vector<std::pair<const std::uint8_t*,std::uint64_t>> ranges;
                    bool ok=true;
                    for(const auto& span:merged){
                        if(target->registration_cancel.load(std::memory_order_acquire)){
                            ok=false;break;
                        }
                        const auto bytes=static_cast<std::uint64_t>(span.end-span.start);
                        if(flyweight_gpu_host_register(span.start,bytes)!=0){ok=false;break;}
                        ranges.emplace_back(span.start,bytes);
                    }
                    std::lock_guard<std::mutex> guard(target->registration_mutex);
                    if(!ok){
                        // Partial coverage cannot be used: an upload whose source
                        // straddles the edge of a registration fails outright, and
                        // the merged spans are exactly where those edges are. Roll
                        // back and stay staged.
                        for(const auto& [base,bytes]:ranges){
                            (void)bytes;flyweight_gpu_host_unregister(base);
                        }
                    }else{
                        target->registered_ranges=std::move(ranges);
                        target->registration_done=promoted;
                    }
                    target->paging_registration_nanoseconds=
                        std::chrono::duration_cast<std::chrono::nanoseconds>(
                            std::chrono::steady_clock::now()
                            -registration_started).count();
                    target->registration_ready.fetch_add(1,std::memory_order_release);
                });
            }
        }
        runtime->decode_ready=true;
        // What is left for everything else on this card. On a machine whose GPU
        // also drives the display, the desktop compositor and the browser
        // allocate from the same pool, and when they cannot they do not fail
        // loudly -- they paint stale or torn tiles, which reads as the chat UI
        // corrupting itself rather than as anything to do with the model. Auto
        // fit already reserves max(2 GiB, VRAM/8); an explicit --gpu-cache-mib
        // reserves nothing, so this is the only warning such a run ever gets.
        {
            FlyweightV2GpuInfo after{};
            constexpr std::uint64_t kDesktopHeadroom=768ull*1024*1024;
            const auto policy=qwen_expert_policy(
                *runtime,flyweight::v2::ExpertExecutionPhase::prepare);
            if(policy.routed_gpu_execution_allowed()&&
               gpu_probe(after,runtime->options.device)==0&&after.available&&
               after.free_memory&&after.free_memory<kDesktopHeadroom){
                std::fprintf(stderr,
                    "[flyweight] only %llu MiB of VRAM is free after loading. "
                    "If this GPU also drives your display, the desktop and "
                    "browser draw from the same memory and will render "
                    "corrupted rather than fail; lower --gpu-cache-mib, "
                    "--context-window or --parallel if you see that.\n",
                    static_cast<unsigned long long>(
                        after.free_memory/(1024ull*1024)));
            }
        }
    return 0;
});}

// KV cache kernel selection by configured precision (0=f32, 1=f16, 2=bf16).
// K and V are independent: append is one kv_store_<t> launch per cache; scores
// read K, values read V; the fused prefill only runs when K==V (else per-token).
// KV byte size for `elems` elements at a given type (q8_0 = 34 bytes per 32-elem block).
inline std::uint64_t kv_region_bytes(std::uint64_t elems,int type){return type==3?(elems/32)*kQ8BlockSize:elems*(type==0?4:2);}
// Cache precision codes: 0=f32, 1=f16, 2=bf16, 3=q8_0, 4=turbo3, 5=turbo4.
// The turbo store kernels come in K and V flavours because the two rotate under
// different sign streams, which is what lets values be accumulated rotated.
inline const char* kv_store_kernel(int t,bool key){return t==5?(key?"kv_store_turbo4_k":"kv_store_turbo4_v"):t==4?(key?"kv_store_turbo3_k":"kv_store_turbo3_v"):t==3?"kv_store_q8":t==2?"kv_store_bf16":t==1?"kv_store_f16":"kv_store_f32";}
inline const char* kv_scores_kernel(const FlyweightV2QwenRuntime& r){int t=r.options.cache_type_k;return t==5?"kv_attention_scores_turbo4":t==4?"kv_attention_scores_turbo3":t==3?"kv_attention_scores_q8":t==2?"kv_attention_scores_bf16":t==1?"kv_attention_scores_f16":"kv_attention_scores";}
inline const char* kv_values_kernel(const FlyweightV2QwenRuntime& r){int t=r.options.cache_type_v;return t==5?"kv_attention_values_turbo4":t==4?"kv_attention_values_turbo3":t==3?"kv_attention_values_q8":t==2?"kv_attention_values_bf16":t==1?"kv_attention_values_f16":"kv_attention_values";}
inline const char* kv_scores_ring_kernel(const FlyweightV2QwenRuntime& r){int t=r.options.cache_type_k;return t==5?"kv_attention_scores_turbo4_ring":t==4?"kv_attention_scores_turbo3_ring":t==3?"kv_attention_scores_q8_ring":t==2?"kv_attention_scores_bf16_ring":t==1?"kv_attention_scores_f16_ring":"kv_attention_scores_ring";}
inline const char* kv_values_ring_kernel(const FlyweightV2QwenRuntime& r){int t=r.options.cache_type_v;return t==5?"kv_attention_values_turbo4_ring":t==4?"kv_attention_values_turbo3_ring":t==3?"kv_attention_values_q8_ring":t==2?"kv_attention_values_bf16_ring":t==1?"kv_attention_values_f16_ring":"kv_attention_values_ring";}
// The turbo tiles rank below the cuBLAS staging path (see the dispatch) and
// above the serial per-head kernels; this switch drops them back out of the
// fallback tier entirely for A/B.
inline bool qwen_turbo_fused_disabled(){
    static const bool disabled=[]{
        const char*s=std::getenv("FLYWEIGHT_TURBO_FUSED_ATTENTION");
        return s&&s[0]=='0';}();
    return disabled;
}
inline const char* kv_fused_tiles_kernel(const FlyweightV2QwenRuntime& r){
    if(r.options.cache_type_k!=r.options.cache_type_v)return nullptr;
    const int t=r.options.cache_type_k;
    if((t==4||t==5)&&qwen_turbo_fused_disabled())return nullptr;
    return t==3?"kv_attention_fused_q8_tiles":
           t==2?"kv_attention_fused_bf16_tiles":
           t==1?"kv_attention_fused_f16_tiles":
           t==4?"kv_attention_fused_turbo3_tiles":
           t==5?"kv_attention_fused_turbo4_tiles":nullptr;
}
// The 256-dim twin of the above. Kept a separate instantiation rather than
// widening the 128 one so models with 128-dim heads keep their shared-memory
// footprint and register count exactly as they were.
inline const char* kv_fused_tiles_kernel256(const FlyweightV2QwenRuntime& r){
    if(r.options.cache_type_k!=r.options.cache_type_v)return nullptr;
    const int t=r.options.cache_type_k;
    if((t==4||t==5)&&qwen_turbo_fused_disabled())return nullptr;
    return t==3?"kv_attention_fused_q8_tiles256":
           t==2?"kv_attention_fused_bf16_tiles256":
           t==1?"kv_attention_fused_f16_tiles256":
           t==4?"kv_attention_fused_turbo3_tiles256":
           t==5?"kv_attention_fused_turbo4_tiles256":nullptr;
}
// The 512-dim triplet, for Gemma 4's global layers.
inline const char* kv_fused_tiles_kernel512(const FlyweightV2QwenRuntime& r){
    if(r.options.cache_type_k!=r.options.cache_type_v)return nullptr;
    const int t=r.options.cache_type_k;
    if((t==4||t==5)&&qwen_turbo_fused_disabled())return nullptr;
    return t==3?"kv_attention_fused_q8_tiles512":
           t==2?"kv_attention_fused_bf16_tiles512":
           t==1?"kv_attention_fused_f16_tiles512":
           t==4?"kv_attention_fused_turbo3_tiles512":
           t==5?"kv_attention_fused_turbo4_tiles512":nullptr;
}
// The merge must pair with the tiles kernel above it: same record stride, and
// for the turbo pair the combined vector still carries the cache's rotation,
// which only the turbo merge undoes. Mixing them returns rotated garbage.
inline const char* kv_fused_merge_kernel_name(
    const FlyweightV2QwenRuntime& r,int width){
    const bool turbo=kv_type_is_turbo(r.options.cache_type_k);
    return width==128
        ?(turbo?"kv_attention_fused_turbo_merge":"kv_attention_fused_merge")
        :width==256
        ?(turbo?"kv_attention_fused_turbo_merge256":"kv_attention_fused_merge256")
        :(turbo?"kv_attention_fused_turbo_merge512":"kv_attention_fused_merge512");
}
// Width the fused path would run `head_dim` at, or 0 when it cannot.
inline int kv_fused_width(int head_dim){
    return head_dim==128?128:head_dim==256?256:head_dim==512?512:0;
}
// Grouped-query kernel for `share` query heads per KV head, or nullptr when the
// shape is not one of the instantiated ones. Opt-out via FLYWEIGHT_GQA_ATTENTION=0.
inline const char* kv_gqa_tiles_kernel(
    const FlyweightV2QwenRuntime& r,int head_dim,int heads,int kv_heads,int tile
){
    static const bool enabled=[]{
        const char*env=std::getenv("FLYWEIGHT_GQA_ATTENTION");
        return !env||env[0]!='0';
    }();
    if(!enabled||kv_heads<=0||head_dim!=256)return nullptr;
    if(r.options.cache_type_k!=r.options.cache_type_v)return nullptr;
    const int share=heads/kv_heads;
    if(heads!=kv_heads*share||(share!=8&&share!=4))return nullptr;
    if(tile!=256&&tile!=512)return nullptr;
    const int t=r.options.cache_type_k;
    const bool narrow=tile==256;
    if(share==8)
        return t==3?(narrow?"kv_attention_gqa_rows_q8_256_s8_t256"
                           :"kv_attention_gqa_rows_q8_256_s8_t512"):
               t==2?(narrow?"kv_attention_gqa_rows_bf16_256_s8_t256"
                           :"kv_attention_gqa_rows_bf16_256_s8_t512"):
               t==1?(narrow?"kv_attention_gqa_rows_f16_256_s8_t256"
                           :"kv_attention_gqa_rows_f16_256_s8_t512"):nullptr;
    return t==3?(narrow?"kv_attention_gqa_rows_q8_256_s4_t256"
                       :"kv_attention_gqa_rows_q8_256_s4_t512"):
           t==2?(narrow?"kv_attention_gqa_rows_bf16_256_s4_t256"
                       :"kv_attention_gqa_rows_bf16_256_s4_t512"):
           t==1?(narrow?"kv_attention_gqa_rows_f16_256_s4_t256"
                       :"kv_attention_gqa_rows_f16_256_s4_t512"):nullptr;
}
// Floats per (head, tile) partial record: the head dimension plus the running
// maximum and denominator. Must match the kernel width that wrote it.
inline int kv_fused_record_stride(int width){return width+2;}
// The narrowest instantiated tile whose records both fit the merge's ceiling and
// fit the partial buffer, or 0 when neither does and the caller must use another
// path. The buffer holds `heads * context_limit` floats and a record is
// head_dim + 2 of them, so a 256-token tile overruns a *full* context by a
// fraction of a percent -- which is exactly when the 512 twin is picked.
inline int kv_gqa_rows_tile_tokens(
    const FlyweightV2QwenRuntime& r,int tokens
){
    // Narrowest first: the narrow tile is faster at every measured context
    // because it fills the device, where a wide tile on two KV heads is a
    // handful of blocks. The wide twin exists for the case the narrow one
    // cannot serve rather than because it is ever preferred.
    for(const int tile:{256,512}){
        const std::uint64_t records=
            (static_cast<std::uint64_t>(tokens)+tile-1)/tile;
        if(records==0||records>512)continue;
        if(records*kv_fused_record_stride(256)>
           static_cast<std::uint64_t>(r.options.context_limit))continue;
        return tile;
    }
    return 0;
}
// Prefill counterpart of qwen_turbo_cublas_attention: stage the whole causal
// window as f16 once for the chunk, then run the fused f16 prefill kernel over
// it instead of walking the cache once per row.
//
// The cuBLAS prefill entry point is deliberately not used here even though it is
// faster for f16: it fuses the output gate, and the gate is a sigmoid, so
// applying it before the inverse rotation would be wrong. The fused kernel keeps
// the gate as a separate launch, which composes with the rotation correctly.
//
// Stages the causal window as f16 and rotates the query rows in place, leaving
// the caller to run either cuBLAS or the fused kernel over the staged copy and
// then inverse-rotate and gate. Returns false, having touched nothing, when the
// configuration is not eligible.
// Widen one layer's live KV window into f16 so the tensor-core path can read
// it. Turbo has always come through here; q8_0 now does too, and only when the
// caller says the cuBLAS path will actually be taken -- for a short prefix the
// warp kernel wins and staging the window would be work for nothing. `rotated`
// Returns 0 when it declines, 1 when it staged, and 2 when it staged AND
// rotated the queries -- only turbo's are, and the caller has to undo exactly
// what was done. A code rather than an out-parameter because the call sits in
// an if-init, which holds one declaration statement.
inline int qwen_kv_prefill_stage(
    FlyweightV2QwenRuntime& runtime, const QwenLayerPlan& layer,
    std::uint64_t queries, std::uint64_t cache_keys, std::uint64_t cache_values,
    int heads, int kv_heads, int head_dim, int rows, int base,
    bool cublas_available,
    std::uint64_t& stage_keys, std::uint64_t& stage_values
){
    const int ck=runtime.options.cache_type_k, cv=runtime.options.cache_type_v;
    if(ck!=cv)return 0;
    const bool turbo=kv_type_is_turbo(ck);
    if(!turbo&&!(ck==3&&cublas_available))return 0;
    if(!runtime.turbo_kv_stage||rows<=0||heads<=0||kv_heads<=0)return 0;
    if(heads%kv_heads!=0||head_dim<32||(head_dim&31)!=0||head_dim>256)return 0;
    if(layer.attention_window)return 0;  // ring layers keep the per-token path
    const char*env=std::getenv("FLYWEIGHT_TURBO_CUBLAS");
    if(env&&env[0]=='0')return 0;
    const int window=base+rows;
    if(window<=0||static_cast<std::uint64_t>(window)>layer.cache_capacity)return 0;
    const std::uint64_t needed=
        static_cast<std::uint64_t>(kv_heads)*window*head_dim*sizeof(std::uint16_t);
    if(needed>runtime.turbo_kv_stage_stride)return 0;

    auto launch=[&](const char*name,std::uint32_t gx,std::uint32_t gy,void**args){
        if(flyweight_gpu_launch_named(name,gx,gy,256,0,runtime.stream,args)!=0)
            throw std::runtime_error(std::string("native Qwen turbo prefill kernel failed: ")+name);
    };
    stage_keys=runtime.turbo_kv_stage;
    stage_values=runtime.turbo_kv_stage+runtime.turbo_kv_stage_stride;
    const char*dequant=ck==5?"kv_dequant_turbo4_f16":
                       ck==4?"kv_dequant_turbo3_f16":"kv_dequant_q8_f16";
    const std::uint32_t token_blocks=(static_cast<std::uint32_t>(window)+7)/8;
    int tokens=window,slot_capacity=static_cast<int>(layer.cache_capacity),origin=0;
    void*key_args[]={&cache_keys,&stage_keys,&kv_heads,&head_dim,&tokens,&slot_capacity,&origin};
    launch(dequant,static_cast<std::uint32_t>(kv_heads),token_blocks,key_args);
    void*value_args[]={&cache_values,&stage_values,&kv_heads,&head_dim,&tokens,&slot_capacity,&origin};
    launch(dequant,static_cast<std::uint32_t>(kv_heads),token_blocks,value_args);

    // Only turbo stores rotated: q8_0 is a plain codec, so its staged window
    // is already in the query's basis and the gate can be left to the
    // attention kernel rather than undone afterwards.
    if(turbo){
        // Queries and attended rows are both [row][head][head_dim] contiguous,
        // so the rotation runs over rows*heads vectors in one launch.
        int vectors=rows*heads,key_stream=0;
        void*rotate_args[]={&queries,&vectors,&head_dim,&key_stream};
        launch("turbo_rotate_rows",static_cast<std::uint32_t>(vectors),1,rotate_args);
        return 2;
    }
    return 1;
}

inline int kv_fused_tile_tokens(const FlyweightV2QwenRuntime&){return 1024;}
inline int kv_fused_grid_heads(const FlyweightV2QwenRuntime&,int heads,int){return heads;}
inline int qwen_cublas_attention_min_tokens(){
    static const int threshold=[]{
        const char*env=std::getenv("FLYWEIGHT_CUBLAS_ATTENTION_MIN_TOKENS");
        if(!env||!env[0])
            return flyweight::v2::attention::kDefaultCublasMinTokens;
        char*end=nullptr;
        const long value=std::strtol(env,&end,10);
        if(end==env||*end!='\0'||value<1||
           value>std::numeric_limits<std::int32_t>::max())
            return flyweight::v2::attention::kDefaultCublasMinTokens;
        return static_cast<int>(value);
    }();
    return threshold;
}
inline bool qwen_cublas_attention_eligible(
    const FlyweightV2QwenRuntime&runtime,int tokens,int first_slot,int capacity
){
    const char*env=std::getenv("FLYWEIGHT_CUBLAS_ATTENTION");
    const bool enabled=!env||env[0]!='0';
    return flyweight::v2::attention::cublas_eligible(
        runtime.options.cache_type_k,runtime.options.cache_type_v,
        tokens,first_slot,capacity,enabled,
        qwen_cublas_attention_min_tokens());
}

// Attention for a turbo KV cache: expand the live window to f16 in the shared
// staging buffer and hand it to cuBLAS, which is roughly 2x faster than the
// warp-per-token fused kernel and much faster than the scores+values pair.
//
// Keys and values stay rotated in the staging copy, so the query is rotated on
// the way in and the output inverse-rotated on the way out. Both are only
// heads*head_dim elements, negligible beside the window itself, and it avoids a
// Walsh-Hadamard per cached token.
//
// Eligibility is decided before anything is mutated, so a false return leaves
// the caller's normal dispatch free to run on an untouched query. Failures
// after that point throw rather than fall back, because the query has already
// been rotated in place by then.
inline bool qwen_turbo_cublas_attention(
    FlyweightV2QwenRuntime& runtime, std::uint64_t queries, std::uint64_t query_f16,
    std::uint64_t cache_keys, std::uint64_t cache_values, std::uint64_t scores_f16,
    std::uint64_t attended, int heads, int kv_heads, int head_dim,
    int tokens, int capacity, int first_slot, float scale
){
    const int ck=runtime.options.cache_type_k, cv=runtime.options.cache_type_v;
    const bool diagnose=std::getenv("FLYWEIGHT_TURBO_DIAG")!=nullptr;
    auto decline=[&](const char*why)->bool{
        if(diagnose){
            static const char*last=nullptr;
            if(last!=why){last=why;std::fprintf(stderr,"[turbo] declined: %s\n",why);}
        }
        return false;
    };
    if(ck!=cv||!kv_type_is_turbo(ck))return decline("cache type not a matched turbo pair");
    if(!runtime.turbo_kv_stage)return decline("no staging buffer");
    if(tokens<=0||heads<=0||kv_heads<=0)return decline("degenerate shape");
    if(heads%kv_heads!=0||head_dim<32||(head_dim&31)!=0)return decline("head geometry");
    if(tokens<qwen_cublas_attention_min_tokens())return decline("below cuBLAS token threshold");
    if(first_slot<0||first_slot>=capacity)return decline("ring slot out of range");
    const char*env=std::getenv("FLYWEIGHT_TURBO_CUBLAS");
    if(env&&env[0]=='0')return decline("disabled by FLYWEIGHT_TURBO_CUBLAS=0");
    const std::uint64_t needed=
        static_cast<std::uint64_t>(kv_heads)*tokens*head_dim*sizeof(std::uint16_t);
    if(needed>runtime.turbo_kv_stage_stride)return decline("staging buffer too small");
    if(diagnose){
        static bool announced=false;
        if(!announced){announced=true;std::fprintf(stderr,"[turbo] cuBLAS attention path engaged\n");}
    }

    auto launch=[&](const char*name,std::uint32_t gx,std::uint32_t gy,void**args){
        if(flyweight_gpu_launch_named(name,gx,gy,256,0,runtime.stream,args)!=0)
            throw std::runtime_error(std::string("native Qwen turbo attention kernel failed: ")+name);
    };
    std::uint64_t stage_keys=runtime.turbo_kv_stage;
    std::uint64_t stage_values=runtime.turbo_kv_stage+runtime.turbo_kv_stage_stride;
    const char*dequant=ck==5?"kv_dequant_turbo4_f16":"kv_dequant_turbo3_f16";
    const std::uint32_t token_blocks=(static_cast<std::uint32_t>(tokens)+7)/8;
    void*key_args[]={&cache_keys,&stage_keys,&kv_heads,&head_dim,&tokens,&capacity,&first_slot};
    launch(dequant,static_cast<std::uint32_t>(kv_heads),token_blocks,key_args);
    void*value_args[]={&cache_values,&stage_values,&kv_heads,&head_dim,&tokens,&capacity,&first_slot};
    launch(dequant,static_cast<std::uint32_t>(kv_heads),token_blocks,value_args);

    int key_stream=0,value_stream=1,unwrapped=0;
    void*rotate_args[]={&queries,&heads,&head_dim,&key_stream};
    launch("turbo_rotate_rows",static_cast<std::uint32_t>(heads),1,rotate_args);
    // The staging copy is already unwrapped, so cuBLAS sees a linear window.
    if(flyweight_gpu_attention_f16_cublas(queries,query_f16,stage_keys,stage_values,
            scores_f16,attended,runtime.stream,heads,kv_heads,head_dim,tokens,
            tokens,unwrapped,scale)!=0)
        throw std::runtime_error("native Qwen turbo cuBLAS attention failed");
    void*unrotate_args[]={&attended,&heads,&head_dim,&value_stream};
    launch("turbo_unrotate_rows",static_cast<std::uint32_t>(heads),1,unrotate_args);
    return true;
}

void qwen_mtp_dense_projection(
    FlyweightV2QwenRuntime& runtime, std::size_t tensor_index_value,
    std::uint64_t input, std::uint64_t output,
    int input_size, int output_size
) {
    std::uint64_t matrix = runtime.device_tensors[tensor_index_value];
    const auto type = qwen_device_type(runtime, tensor_index_value);
    auto launch = [&](const char* name, std::uint32_t grid_x, void** args) {
        if (flyweight_gpu_launch_named(
                name, grid_x, 1, 256, 0, runtime.stream, args) != 0) {
            throw std::runtime_error(
                std::string("native MTP dense projection failed: ") + name);
        }
    };
    switch (type) {
        case 0: {
            void* args[] = {
                &matrix, &input, &output, &input_size, &output_size};
            launch("qwen_f32_matvec_warp", (output_size + 7) / 8, args);
            return;
        }
        case 30: {
            // bf16_matvec takes (rows, columns), the reverse of the
            // quantized matvec helpers.
            void* args[] = {
                &matrix, &input, &output, &output_size, &input_size};
            launch("bf16_matvec_warp", (output_size + 7) / 8, args);
            return;
        }
        default: {
            const int status = qwen_matvec_driver(
                type, matrix, input, output, input_size, output_size,
                runtime.stream);
            if (status == 0) return;
            if (status == kQwenMatvecUnsupported)
                throw std::runtime_error(
                    "native MTP dense projection type is unsupported: " +
                    std::to_string(type));
        }
    }
    throw std::runtime_error("native MTP dense projection failed");
}

// qwen4exp twin of qwen_mtp_append_pair: rebuild ONE draft-cache K/V entry from
// a token and the target's streams. Only the key/value path is needed, so this
// stops at the attention-boundary block input -- no inject gates, no MoE, no
// head. Fusion and bookend shapes are the draft forward's; see the comment on
// qwen4exp_mtp_draft.
void qwen4exp_mtp_append_pair(
    FlyweightV2QwenRuntime& runtime, std::uint32_t token,
    std::uint64_t input_hidden
) {
    const auto& config = runtime.model->config;
    const int hidden = static_cast<int>(config.hidden_size);
    const int hc_count = static_cast<int>(config.hyper_connection_count);
    const int hc_low_rank = static_cast<int>(config.hyper_connection_low_rank);
    const int hc_wide_size = hc_count * hidden;
    const int kv_heads = static_cast<int>(config.attention_kv_heads);
    const auto& layer = runtime.mtp_layer_plan;
    const int head_dim = static_cast<int>(
        runtime.model->tensors[layer.static_tensors[2]].shape[1] / kv_heads);
    const int kv_size = kv_heads * head_dim;
    const float epsilon = config.rms_norm_epsilon ? config.rms_norm_epsilon : 1.0e-6f;
    std::uint64_t cursor = runtime.workspace;
    const auto workspace_end = runtime.workspace + runtime.workspace_bytes;
    auto take = [&](std::uint64_t bytes) {
        const auto result = cursor;
        cursor += device_align(bytes);
        if (cursor > workspace_end)
            throw std::runtime_error("native qwen4exp MTP workspace is too small");
        return result;
    };
    const auto embedding = take(hidden * sizeof(float));
    const auto normalized_embedding = take(hidden * sizeof(float));
    const auto normalized_hidden = take(hc_wide_size * sizeof(float));
    const auto concatenated = take(2ULL * hidden * sizeof(float));
    const auto hc_streams = take(hc_wide_size * sizeof(float));
    const auto hc_normed = take(hc_wide_size * sizeof(float));
    const auto hc_wide = take(hc_wide_size * sizeof(float));
    const auto hc_low = take(hc_low_rank * sizeof(float));
    const auto normalized = take(hidden * sizeof(float));
    const auto keys = take(kv_size * sizeof(float));
    const auto values = take(kv_size * sizeof(float));
    const auto rotated_keys = take(kv_size * sizeof(float));
    auto launch = [&](const char* name, std::uint32_t gx, std::uint32_t gy, void** args) {
        if (flyweight_gpu_launch_named(name, gx, gy, 256, 0, runtime.stream, args) != 0)
            throw std::runtime_error(
                std::string("native qwen4exp MTP CUDA kernel failed: ") + name);
    };
    const std::uint32_t embedding_token = token;
    const auto embedding_matrix = qwen_stage_embedding_rows(runtime, &embedding_token, 1);
    int token_value = runtime.embeddings_host_resident ? 0 : static_cast<int>(token);
    void* embedding_args[] = {const_cast<std::uint64_t*>(&embedding_matrix),
                              const_cast<std::uint64_t*>(&embedding), &token_value,
                              const_cast<int*>(&hidden)};
    launch(qwen_embedding_kernel(qwen_device_type(runtime, runtime.token_embeddings), false),
           (hidden + 255) / 256, 1, embedding_args);
    auto rms_wide = [&](std::uint64_t input, std::uint64_t weight,
                        std::uint64_t output, int count) {
        int one_centered = 0;
        void* args[] = {&input, &weight, &output, &count,
                        const_cast<float*>(&epsilon), &one_centered};
        launch("rms_norm", 1, 1, args);
    };
    rms_wide(embedding, runtime.device_tensors[runtime.mtp_special_tensors[1]],
             normalized_embedding, hidden);
    rms_wide(input_hidden, runtime.device_tensors[runtime.mtp_special_tensors[2]],
             normalized_hidden, hc_wide_size);
    for (int stream = 0; stream < hc_count; ++stream) {
        const auto stream_hidden =
            normalized_hidden + static_cast<std::uint64_t>(stream) * hidden * sizeof(float);
        const auto stream_out =
            hc_streams + static_cast<std::uint64_t>(stream) * hidden * sizeof(float);
        void* concat_args[] = {const_cast<std::uint64_t*>(&normalized_embedding),
                               const_cast<std::uint64_t*>(&stream_hidden),
                               const_cast<std::uint64_t*>(&concatenated),
                               const_cast<int*>(&hidden)};
        launch("qwen_concat_pair", (hidden + 255) / 256, 1, concat_args);
        qwen_mtp_dense_projection(runtime, runtime.mtp_special_tensors[0],
                                  concatenated, stream_out, hidden * 2, hidden);
    }
    // Attention-boundary hc_pre, inject-less: only the block input is wanted.
    {
        auto norm_weights = runtime.device_tensors[layer.static_tensors[0]];
        void* norm_args[] = {const_cast<std::uint64_t*>(&hc_streams), &norm_weights,
                             const_cast<std::uint64_t*>(&hc_normed),
                             const_cast<int*>(&hidden), const_cast<float*>(&epsilon)};
        launch("qwen4_group_rms", hc_count, 1, norm_args);
        qwen_mtp_dense_projection(runtime, layer.hc_attn_down, hc_normed, hc_low,
                                  hc_wide_size, hc_low_rank);
        float divisor = static_cast<float>(hc_count);
        void* silu_args[] = {const_cast<std::uint64_t*>(&hc_low),
                             const_cast<int*>(&hc_low_rank), &divisor};
        launch("qwen4_silu_scale",
               (static_cast<std::uint32_t>(hc_low_rank) + 255) / 256, 1, silu_args);
        qwen_mtp_dense_projection(runtime, layer.hc_attn_up, hc_low, hc_wide,
                                  hc_low_rank, hc_wide_size);
        void* mix_args[] = {const_cast<std::uint64_t*>(&hc_normed),
                            const_cast<std::uint64_t*>(&hc_wide),
                            const_cast<std::uint64_t*>(&normalized),
                            const_cast<int*>(&hc_count), const_cast<int*>(&hidden)};
        launch("qwen4_hc_mix", (static_cast<std::uint32_t>(hidden) + 255) / 256, 1, mix_args);
    }
    qwen_mtp_dense_projection(runtime, layer.static_tensors[2], normalized,
                              keys, hidden, kv_size);
    qwen_mtp_dense_projection(runtime, layer.static_tensors[3], normalized,
                              values, hidden, kv_size);
    const int rotary = std::min(
        static_cast<int>(config.rotary_dimension ? config.rotary_dimension : head_dim),
        static_cast<int>(head_dim));
    const int position = static_cast<int>(runtime.mtp_cache_tokens);
    const float theta = config.rope_freq_base ? config.rope_freq_base : 1000000.0f;
    const auto key_norm = runtime.device_tensors[layer.static_tensors[6]];
    void* key_args[] = {const_cast<std::uint64_t*>(&keys),
                        const_cast<std::uint64_t*>(&key_norm),
                        const_cast<std::uint64_t*>(&rotated_keys),
                        const_cast<int*>(&kv_heads), const_cast<int*>(&head_dim),
                        const_cast<int*>(&rotary), const_cast<int*>(&position),
                        const_cast<float*>(&theta), const_cast<float*>(&epsilon)};
    launch("qwen_attention_key", kv_heads, 1, key_args);
    auto cache_keys = runtime.state + layer.state_first;
    auto cache_values = runtime.state + layer.state_second;
    int capacity = static_cast<int>(runtime.options.context_limit);
    void* key_store_args[] = {const_cast<std::uint64_t*>(&rotated_keys), &cache_keys,
                              const_cast<int*>(&kv_heads), const_cast<int*>(&head_dim),
                              const_cast<int*>(&position), &capacity};
    launch(kv_store_kernel(runtime.options.cache_type_k, true), kv_heads, 1, key_store_args);
    void* value_store_args[] = {const_cast<std::uint64_t*>(&values), &cache_values,
                                const_cast<int*>(&kv_heads), const_cast<int*>(&head_dim),
                                const_cast<int*>(&position), &capacity};
    launch(kv_store_kernel(runtime.options.cache_type_v, false), kv_heads, 1, value_store_args);
    ++runtime.mtp_cache_tokens;
}

void qwen_mtp_append_pair(
    FlyweightV2QwenRuntime& runtime, std::uint32_t token,
    std::uint64_t input_hidden
) {
    // Branch here rather than at the call sites: prompt-pair, the true-cache
    // commit and the rows prefill all reach this one entry point.
    if (runtime.qwen4exp) {
        qwen4exp_mtp_append_pair(runtime, token, input_hidden);
        return;
    }
    const int hidden = static_cast<int>(runtime.model->config.hidden_size);
    const int kv_heads = static_cast<int>(runtime.model->config.attention_kv_heads);
    const auto& layer = runtime.mtp_layer_plan;
    const int head_dim = static_cast<int>(
        runtime.model->tensors[layer.static_tensors[2]].shape[1] / kv_heads
    );
    const int kv_size = kv_heads * head_dim;
    const float epsilon = runtime.model->config.rms_norm_epsilon
        ? runtime.model->config.rms_norm_epsilon : 1.0e-6f;
    std::uint64_t cursor = runtime.workspace;
    const auto workspace_end = runtime.workspace + runtime.workspace_bytes;
    auto take = [&](std::uint64_t bytes) {
        const auto result = cursor;
        cursor += device_align(bytes);
        if (cursor > workspace_end) throw std::runtime_error("native MTP workspace is too small");
        return result;
    };
    const auto embedding = take(hidden * sizeof(float));
    const auto normalized_embedding = take(hidden * sizeof(float));
    const auto normalized_hidden = take(hidden * sizeof(float));
    const auto concatenated = take(2ULL * hidden * sizeof(float));
    const auto fused = take(hidden * sizeof(float));
    const auto normalized = take(hidden * sizeof(float));
    const auto keys = take(kv_size * sizeof(float));
    const auto values = take(kv_size * sizeof(float));
    auto launch = [&](const char* name, std::uint32_t grid_x,
                      std::uint32_t grid_y, void** arguments) {
        if (flyweight_gpu_launch_named(
                name, grid_x, grid_y, 256, 0, runtime.stream, arguments
            ) != 0) {
            throw std::runtime_error(std::string("native MTP CUDA kernel failed: ") + name);
        }
    };
    const std::uint32_t embedding_token = token;
    const auto embedding_matrix =
        qwen_stage_embedding_rows(runtime, &embedding_token, 1);
    int token_value = runtime.embeddings_host_resident
        ? 0 : static_cast<int>(token);
    void* embedding_args[] = {
        const_cast<std::uint64_t*>(&embedding_matrix),
        const_cast<std::uint64_t*>(&embedding), &token_value,
        const_cast<int*>(&hidden),
    };
    launch(qwen_embedding_kernel(qwen_device_type(runtime, runtime.token_embeddings), false), (hidden + 255) / 256, 1, embedding_args);
    auto rms = [&](std::uint64_t input, std::uint64_t weight,
                   std::uint64_t output) {
        int one_centered = 0;
        void* args[] = {&input, &weight, &output, const_cast<int*>(&hidden),
                        const_cast<float*>(&epsilon), &one_centered};
        launch("rms_norm", 1, 1, args);
    };
    rms(embedding, runtime.device_tensors[runtime.mtp_special_tensors[1]], normalized_embedding);
    rms(input_hidden, runtime.device_tensors[runtime.mtp_special_tensors[2]], normalized_hidden);
    void* concat_args[] = {
        const_cast<std::uint64_t*>(&normalized_embedding),
        const_cast<std::uint64_t*>(&normalized_hidden),
        const_cast<std::uint64_t*>(&concatenated), const_cast<int*>(&hidden),
    };
    launch("qwen_concat_pair", (hidden + 255) / 256, 1, concat_args);
    qwen_mtp_dense_projection(
        runtime, runtime.mtp_special_tensors[0], concatenated,
        fused, hidden * 2, hidden);
    rms(fused, runtime.device_tensors[layer.static_tensors[0]], normalized);
    qwen_mtp_dense_projection(
        runtime, layer.static_tensors[2], normalized,
        keys, hidden, kv_size);
    qwen_mtp_dense_projection(
        runtime, layer.static_tensors[3], normalized,
        values, hidden, kv_size);
    const int rotary = std::min(static_cast<int>(runtime.model->config.rotary_dimension
        ? runtime.model->config.rotary_dimension : head_dim), static_cast<int>(head_dim));
    const int position = static_cast<int>(runtime.mtp_cache_tokens);
    const float theta = runtime.model->config.rope_freq_base
        ? runtime.model->config.rope_freq_base : 1000000.0f;
    const auto key_norm = runtime.device_tensors[layer.static_tensors[6]];
    const auto rotated_keys = fused;
    void* key_args[] = {
        const_cast<std::uint64_t*>(&keys), const_cast<std::uint64_t*>(&key_norm),
        const_cast<std::uint64_t*>(&rotated_keys), const_cast<int*>(&kv_heads),
        const_cast<int*>(&head_dim), const_cast<int*>(&rotary),
        const_cast<int*>(&position), const_cast<float*>(&theta),
        const_cast<float*>(&epsilon),
    };
    launch("qwen_attention_key", kv_heads, 1, key_args);
    auto cache_keys = runtime.state + layer.state_first;
    auto cache_values = runtime.state + layer.state_second;
    int capacity = static_cast<int>(runtime.options.context_limit);
    // One store per cache so K and V follow their configured precisions
    // independently, exactly as the target decode path does.
    void* key_store_args[] = {
        const_cast<std::uint64_t*>(&rotated_keys), &cache_keys,
        const_cast<int*>(&kv_heads), const_cast<int*>(&head_dim),
        const_cast<int*>(&position), &capacity,
    };
    launch(kv_store_kernel(runtime.options.cache_type_k,true), kv_heads, 1, key_store_args);
    void* value_store_args[] = {
        const_cast<std::uint64_t*>(&values), &cache_values,
        const_cast<int*>(&kv_heads), const_cast<int*>(&head_dim),
        const_cast<int*>(&position), &capacity,
    };
    launch(kv_store_kernel(runtime.options.cache_type_v,false), kv_heads, 1, value_store_args);
    ++runtime.mtp_cache_tokens;
}

void qwen_mtp_append_pair_batch2(
    FlyweightV2QwenRuntime& runtime, const std::uint32_t* tokens,
    std::uint64_t first_hidden, std::uint64_t second_hidden
) {
    constexpr int rows=2;
    const int hidden=static_cast<int>(runtime.model->config.hidden_size);
    const int kv_heads=static_cast<int>(runtime.model->config.attention_kv_heads);
    const auto&layer=runtime.mtp_layer_plan;
    const int head_dim=static_cast<int>(
        runtime.model->tensors[layer.static_tensors[2]].shape[1]/kv_heads);
    const int kv_size=kv_heads*head_dim;
    const float epsilon=runtime.model->config.rms_norm_epsilon
        ?runtime.model->config.rms_norm_epsilon:1.0e-6f;
    std::uint64_t cursor=runtime.workspace;
    const auto workspace_end=runtime.workspace+runtime.workspace_bytes;
    auto take=[&](std::uint64_t bytes){const auto result=cursor;
        cursor+=device_align(bytes);if(cursor>workspace_end)
            throw std::runtime_error("native MTP batch-commit workspace is too small");
        return result;};
    const auto embeddings=take(2ULL*hidden*sizeof(float));
    const auto norm_embeddings=take(2ULL*hidden*sizeof(float));
    const auto hidden_rows=take(2ULL*hidden*sizeof(float));
    const auto norm_hiddens=take(2ULL*hidden*sizeof(float));
    const auto concatenated=take(4ULL*hidden*sizeof(float));
    const auto fused=take(2ULL*hidden*sizeof(float));
    const auto normalized=take(2ULL*hidden*sizeof(float));
    const auto keys=take(2ULL*kv_size*sizeof(float));
    const auto values=take(2ULL*kv_size*sizeof(float));
    const auto token_rows=take(2ULL*sizeof(std::uint32_t));
    auto launch=[&](const char*name,std::uint32_t gx,std::uint32_t gy,
                    std::uint32_t bx,void**args,std::uint32_t shared=0){
        if(flyweight_gpu_launch_named(name,gx,gy,bx,shared,runtime.stream,args)!=0)
            throw std::runtime_error(std::string("native MTP batch-commit kernel failed: ")+name);
    };
    auto copy_hidden=[&](std::uint64_t source,int row){
        auto destination=hidden_rows+static_cast<std::uint64_t>(row)*hidden*sizeof(float);
        void*args[]={&source,&destination,const_cast<int*>(&hidden)};
        launch("qwen_copy_vector",(hidden+255)/256,1,256,args);
    };
    copy_hidden(first_hidden,0);copy_hidden(second_hidden,1);
    const auto embedding_matrix=qwen_stage_embedding_rows(runtime,tokens,rows);
    if(!runtime.embeddings_host_resident&&
       flyweight_gpu_upload(token_rows,tokens,2*sizeof(std::uint32_t),runtime.stream)!=0)
        throw std::runtime_error("native MTP batch-commit token upload failed");
    const auto embedding_index=runtime.embeddings_host_resident
        ?runtime.embedding_row_index:token_rows;
    void*embedding_args[]={const_cast<std::uint64_t*>(&embedding_matrix),
        const_cast<std::uint64_t*>(&embedding_index),
        const_cast<std::uint64_t*>(&embeddings),
        const_cast<int*>(&rows),const_cast<int*>(&hidden)};
    launch(qwen_embedding_kernel(qwen_device_type(runtime,runtime.token_embeddings),true),
        (hidden+255)/256,rows,256,embedding_args);
    auto rms_rows=[&](std::uint64_t input,std::uint64_t weight,std::uint64_t output){
        int one_centered=0;void*args[]={&input,&weight,&output,
            const_cast<int*>(&rows),const_cast<int*>(&hidden),
            const_cast<float*>(&epsilon),&one_centered};
        launch("rms_norm_rows",rows,1,256,args);
    };
    rms_rows(embeddings,runtime.device_tensors[runtime.mtp_special_tensors[1]],norm_embeddings);
    rms_rows(hidden_rows,runtime.device_tensors[runtime.mtp_special_tensors[2]],norm_hiddens);
    for(int row=0;row<rows;++row){
        auto embedding=norm_embeddings+static_cast<std::uint64_t>(row)*hidden*sizeof(float);
        auto input_hidden=norm_hiddens+static_cast<std::uint64_t>(row)*hidden*sizeof(float);
        auto output=concatenated+static_cast<std::uint64_t>(row)*2*hidden*sizeof(float);
        void*args[]={&embedding,&input_hidden,&output,const_cast<int*>(&hidden)};
        launch("qwen_concat_pair",(hidden+255)/256,1,256,args);
    }
    auto dense_pair=[&](std::size_t index,std::uint64_t input,std::uint64_t output,
                        int input_size,int output_size){
        auto matrix=runtime.device_tensors[index];
        if(qwen_device_type(runtime,index)==8){
            void*args[]={&matrix,&input,&output,&input_size,&output_size};
            launch("q8_matvec_transposed_pair",(output_size+7)/8,1,256,args);
            return;
        }
        for(int row=0;row<rows;++row)
            qwen_mtp_dense_projection(runtime,index,
                input+static_cast<std::uint64_t>(row)*input_size*sizeof(float),
                output+static_cast<std::uint64_t>(row)*output_size*sizeof(float),
                input_size,output_size);
    };
    dense_pair(runtime.mtp_special_tensors[0],concatenated,fused,2*hidden,hidden);
    rms_rows(fused,runtime.device_tensors[layer.static_tensors[0]],normalized);
    dense_pair(layer.static_tensors[2],normalized,keys,hidden,kv_size);
    dense_pair(layer.static_tensors[3],normalized,values,hidden,kv_size);
    const int rotary=std::min(static_cast<int>(runtime.model->config.rotary_dimension
        ?runtime.model->config.rotary_dimension:head_dim),static_cast<int>(head_dim));
    const float theta=runtime.model->config.rope_freq_base
        ?runtime.model->config.rope_freq_base:1000000.0f;
    const auto key_norm=runtime.device_tensors[layer.static_tensors[6]];
    const auto cache_keys=runtime.state+layer.state_first;
    const auto cache_values=runtime.state+layer.state_second;
    int capacity=static_cast<int>(runtime.options.context_limit);
    for(int row=0;row<rows;++row){
        auto key=keys+static_cast<std::uint64_t>(row)*kv_size*sizeof(float);
        auto value=values+static_cast<std::uint64_t>(row)*kv_size*sizeof(float);
        auto rotated=fused+static_cast<std::uint64_t>(row)*hidden*sizeof(float);
        int position=static_cast<int>(runtime.mtp_cache_tokens)+row;
        void*key_args[]={&key,const_cast<std::uint64_t*>(&key_norm),&rotated,
            const_cast<int*>(&kv_heads),const_cast<int*>(&head_dim),
            const_cast<int*>(&rotary),&position,const_cast<float*>(&theta),
            const_cast<float*>(&epsilon)};
        launch("qwen_attention_key",kv_heads,1,256,key_args);
        void*key_store_args[]={&rotated,const_cast<std::uint64_t*>(&cache_keys),
            const_cast<int*>(&kv_heads),const_cast<int*>(&head_dim),&position,&capacity};
        launch(kv_store_kernel(runtime.options.cache_type_k,true),kv_heads,1,256,key_store_args);
        void*value_store_args[]={&value,const_cast<std::uint64_t*>(&cache_values),
            const_cast<int*>(&kv_heads),const_cast<int*>(&head_dim),&position,&capacity};
        launch(kv_store_kernel(runtime.options.cache_type_v,false),kv_heads,1,256,value_store_args);
    }
    runtime.mtp_cache_tokens+=rows;
}

void qwen_mtp_append_prompt_pair(
    FlyweightV2QwenRuntime& runtime, std::uint32_t token
) {
    if (!runtime.options.mtp_drafts || !runtime.mtp_has_target_hidden) return;
    qwen_mtp_append_pair(
        runtime, token,
        runtime.state + runtime.mtp_target_hidden_offset);
}

// qwen4exp's draft block. Same job as qwen_mtp_draft below -- one token in,
// one drafted token out, the block's own hidden state left where the next
// round can read it -- but it runs in STREAM space throughout, because that is
// what the nextn block consumes and produces on this architecture.
//
// The shape differences from the qwen twin, all from the MTP section of
// plans/qwen4exp-semantics.md (llama.cpp `graph_mtp` is the reference; there
// is no transformers module to check against):
//
//   * `input_hidden` is hc_count*hidden wide -- the target's streams, not the
//     collapsed hidden the LM head sees.
//   * hnorm is ONE rms over that whole row, then split into streams. It is not
//     a per-stream (grouped) norm; deepseek4's MTP is, and the two forms differ
//     only in the denominator, so getting it wrong is invisible.
//   * eh_proj is [2*hidden -> hidden] applied PER STREAM, with the one normed
//     embedding broadcast across all of them and concatenated FIRST.
//   * the bookends are the gated-residual boundaries (hc_pre/hc_post), not
//     attn_norm/post_attention_norm, and the output is collapsed by the
//     ordinary inject-less output_hc_* boundary before the LM head.
//   * attention runs DENSE. The block carries indexer tensors and the
//     reference loads and ignores them, so `qsa_ratio` stays 0 and the draft
//     needs no block-key store of its own.
//
// A wrong fusion here costs acceptance rate and NEVER text: the verify pass
// re-scores every drafted token with the target model.
std::uint32_t qwen4exp_mtp_draft(
    FlyweightV2QwenRuntime& runtime, std::uint32_t token,
    std::uint64_t input_hidden
) {
    const auto& config = runtime.model->config;
    const int hidden_size = static_cast<int>(config.hidden_size);
    const int hc_count = static_cast<int>(config.hyper_connection_count);
    const int hc_low_rank = static_cast<int>(config.hyper_connection_low_rank);
    const int hc_wide_size = hc_count * hidden_size;
    const int experts = static_cast<int>(config.expert_count);
    const int top_k = static_cast<int>(config.expert_used_count);
    const int intermediate = static_cast<int>(runtime.moe_intermediate);
    const int heads = static_cast<int>(config.attention_heads);
    const int kv_heads = static_cast<int>(config.attention_kv_heads);
    const auto& layer = runtime.mtp_layer_plan;
    const int head_dim = static_cast<int>(
        runtime.model->tensors[layer.static_tensors[2]].shape[1] / kv_heads);
    const int q_size = heads * 2 * head_dim, kv_size = kv_heads * head_dim;
    const float epsilon = config.rms_norm_epsilon ? config.rms_norm_epsilon : 1.0e-6f;
    std::uint64_t cursor = runtime.workspace;
    const auto workspace_end = runtime.workspace + runtime.workspace_bytes;
    auto take = [&](std::uint64_t bytes) {
        const auto result = cursor;
        cursor += device_align(bytes);
        if (cursor > workspace_end)
            throw std::runtime_error("native qwen4exp MTP workspace is too small");
        return result;
    };
    const auto embedding = take(hidden_size * sizeof(float));
    const auto norm_embedding = take(hidden_size * sizeof(float));
    const auto norm_hidden = take(hc_wide_size * sizeof(float));
    const auto concatenated = take(2ULL * hidden_size * sizeof(float));
    const auto hc_streams = take(hc_wide_size * sizeof(float));
    const auto hc_normed = take(hc_wide_size * sizeof(float));
    const auto hc_wide = take(hc_wide_size * sizeof(float));
    const auto hc_low = take(hc_low_rank * sizeof(float));
    const auto hc_gates = take(hc_count * sizeof(float));
    const auto normalized = take(hidden_size * sizeof(float));
    const auto block_output = take(hidden_size * sizeof(float));
    const auto first = take(runtime.scratch_elements * sizeof(float));
    const auto second = take(runtime.scratch_elements * sizeof(float));
    const auto third = take(runtime.scratch_elements * sizeof(float));
    const auto fourth = take(runtime.scratch_elements * sizeof(float));
    const auto activated = take(top_k * runtime.moe_intermediate * sizeof(float));
    (void)activated;
    const auto router_logits = take(experts * sizeof(float));
    const auto selected_device = take(top_k * sizeof(std::int32_t));
    const auto route_weights = take(top_k * sizeof(float));
    const auto argmax_device = take(sizeof(std::uint64_t));
    const auto attention_scores = take(
        static_cast<std::uint64_t>(heads) * runtime.options.context_limit * sizeof(float));
    auto launch = [&](const char* name, std::uint32_t gx, std::uint32_t gy, void** args) {
        if (flyweight_gpu_launch_named(name, gx, gy, 256, 0, runtime.stream, args) != 0)
            throw std::runtime_error(
                std::string("native qwen4exp MTP CUDA kernel failed: ") + name);
    };
    auto dense_index = [&](std::size_t index, std::uint64_t input, std::uint64_t output,
                           int input_size, int output_size) {
        qwen_mtp_dense_projection(runtime, index, input, output, input_size, output_size);
    };
    auto dense = [&](std::size_t role, std::uint64_t input, std::uint64_t output,
                     int input_size, int output_size) {
        dense_index(layer.static_tensors.at(role), input, output, input_size, output_size);
    };
    auto tensor = [&](std::size_t role) {
        return runtime.device_tensors[layer.static_tensors.at(role)];
    };
    // Whole-row rms over `count` elements, weight applied directly (baked +1).
    auto rms_wide = [&](std::uint64_t input, std::uint64_t weight,
                        std::uint64_t output, int count) {
        int one_centered = 0;
        void* args[] = {&input, &weight, &output, &count,
                        const_cast<float*>(&epsilon), &one_centered};
        launch("rms_norm", 1, 1, args);
    };
    // One gated-residual boundary: streams -> block input, leaving the inject
    // gates in hc_gates. `inject_index` == max is the inject-less collapse form.
    auto hc_pre = [&](std::uint64_t norm_weights, std::uint64_t down_index,
                      std::uint64_t up_index, std::uint64_t inject_index) {
        void* norm_args[] = {const_cast<std::uint64_t*>(&hc_streams), &norm_weights,
                             const_cast<std::uint64_t*>(&hc_normed),
                             const_cast<int*>(&hidden_size), const_cast<float*>(&epsilon)};
        launch("qwen4_group_rms", hc_count, 1, norm_args);
        dense_index(down_index, hc_normed, hc_low, hc_wide_size, hc_low_rank);
        float divisor = static_cast<float>(hc_count);
        void* silu_args[] = {const_cast<std::uint64_t*>(&hc_low),
                             const_cast<int*>(&hc_low_rank), &divisor};
        launch("qwen4_silu_scale",
               (static_cast<std::uint32_t>(hc_low_rank) + 255) / 256, 1, silu_args);
        dense_index(up_index, hc_low, hc_wide, hc_low_rank, hc_wide_size);
        if (inject_index != std::numeric_limits<std::uint64_t>::max())
            dense_index(inject_index, hc_normed, hc_gates, hc_wide_size, hc_count);
        void* mix_args[] = {const_cast<std::uint64_t*>(&hc_normed),
                            const_cast<std::uint64_t*>(&hc_wide),
                            const_cast<std::uint64_t*>(&normalized),
                            const_cast<int*>(&hc_count), const_cast<int*>(&hidden_size)};
        launch("qwen4_hc_mix",
               (static_cast<std::uint32_t>(hidden_size) + 255) / 256, 1, mix_args);
    };
    auto hc_post = [&](std::uint64_t output) {
        void* inject_args[] = {const_cast<std::uint64_t*>(&hc_streams), &output,
                               const_cast<std::uint64_t*>(&hc_gates),
                               const_cast<int*>(&hc_count), const_cast<int*>(&hidden_size)};
        launch("qwen4_hc_inject",
               (static_cast<std::uint32_t>(hc_wide_size) + 255) / 256, 1, inject_args);
    };

    // --- input fusion ------------------------------------------------------
    const std::uint32_t embedding_token = token;
    const auto embedding_matrix = qwen_stage_embedding_rows(runtime, &embedding_token, 1);
    int token_value = runtime.embeddings_host_resident ? 0 : static_cast<int>(token);
    void* embedding_args[] = {const_cast<std::uint64_t*>(&embedding_matrix),
                              const_cast<std::uint64_t*>(&embedding), &token_value,
                              const_cast<int*>(&hidden_size)};
    launch(qwen_embedding_kernel(qwen_device_type(runtime, runtime.token_embeddings), false),
           (hidden_size + 255) / 256, 1, embedding_args);
    rms_wide(embedding, runtime.device_tensors[runtime.mtp_special_tensors[1]],
             norm_embedding, hidden_size);
    rms_wide(input_hidden, runtime.device_tensors[runtime.mtp_special_tensors[2]],
             norm_hidden, hc_wide_size);
    // eh_proj per stream, embedding half first and shared by every stream.
    for (int stream = 0; stream < hc_count; ++stream) {
        const auto stream_hidden =
            norm_hidden + static_cast<std::uint64_t>(stream) * hidden_size * sizeof(float);
        const auto stream_out =
            hc_streams + static_cast<std::uint64_t>(stream) * hidden_size * sizeof(float);
        void* concat_args[] = {const_cast<std::uint64_t*>(&norm_embedding),
                               const_cast<std::uint64_t*>(&stream_hidden),
                               const_cast<std::uint64_t*>(&concatenated),
                               const_cast<int*>(&hidden_size)};
        launch("qwen_concat_pair", (hidden_size + 255) / 256, 1, concat_args);
        dense_index(runtime.mtp_special_tensors[0], concatenated, stream_out,
                    hidden_size * 2, hidden_size);
    }

    // --- attention (dense) -------------------------------------------------
    hc_pre(tensor(0), layer.hc_attn_down, layer.hc_attn_up, layer.hc_attn_inject);
    dense(1, normalized, first, hidden_size, q_size);
    dense(2, normalized, second, hidden_size, kv_size);
    dense(3, normalized, third, hidden_size, kv_size);
    const int rotary = static_cast<int>(
        config.rotary_dimension ? config.rotary_dimension : head_dim);
    const int position = static_cast<int>(runtime.mtp_cache_tokens);
    const float theta = config.rope_freq_base ? config.rope_freq_base : 1000000.0f;
    auto qnorm = tensor(5), knorm = tensor(6);
    std::uint64_t queries = fourth, gates = fourth + q_size / 2 * sizeof(float);
    void* q_args[] = {const_cast<std::uint64_t*>(&first), &qnorm, &queries, &gates,
                      const_cast<int*>(&heads), const_cast<int*>(&head_dim),
                      const_cast<int*>(&rotary), const_cast<int*>(&position),
                      const_cast<float*>(&theta), const_cast<float*>(&epsilon)};
    launch("qwen_attention_query", heads, 1, q_args);
    std::uint64_t keys = first;
    void* k_args[] = {const_cast<std::uint64_t*>(&second), &knorm, &keys,
                      const_cast<int*>(&kv_heads), const_cast<int*>(&head_dim),
                      const_cast<int*>(&rotary), const_cast<int*>(&position),
                      const_cast<float*>(&theta), const_cast<float*>(&epsilon)};
    launch("qwen_attention_key", kv_heads, 1, k_args);
    auto cache_keys = runtime.state + layer.state_first;
    auto cache_values = runtime.state + layer.state_second;
    int capacity = static_cast<int>(runtime.options.context_limit);
    void* key_store_args[] = {&keys, &cache_keys, const_cast<int*>(&kv_heads),
                              const_cast<int*>(&head_dim), const_cast<int*>(&position),
                              &capacity};
    launch(kv_store_kernel(runtime.options.cache_type_k, true), kv_heads, 1, key_store_args);
    void* value_store_args[] = {const_cast<std::uint64_t*>(&third), &cache_values,
                                const_cast<int*>(&kv_heads), const_cast<int*>(&head_dim),
                                const_cast<int*>(&position), &capacity};
    launch(kv_store_kernel(runtime.options.cache_type_v, false), kv_heads, 1, value_store_args);
    std::uint64_t attended = second;
    int tokens = position + 1;
    float scale = 1.0f / std::sqrt(static_cast<float>(head_dim));
    void* score_args[] = {&queries, &cache_keys, const_cast<std::uint64_t*>(&attention_scores),
                          const_cast<int*>(&heads), const_cast<int*>(&kv_heads),
                          const_cast<int*>(&head_dim), &tokens, &capacity, &scale};
    launch(kv_scores_kernel(runtime), heads, (tokens + 255) / 256, score_args);
    void* value_args[] = {const_cast<std::uint64_t*>(&attention_scores), &cache_values,
                          &attended, const_cast<int*>(&heads), const_cast<int*>(&kv_heads),
                          const_cast<int*>(&head_dim), &tokens, &capacity};
    launch(kv_values_kernel(runtime), heads, 1, value_args);
    std::uint64_t gated = third;
    int elements = heads * head_dim;
    void* gate_args[] = {&attended, &gates, &gated, &elements};
    launch("qwen_attention_gate", (elements + 255) / 256, 1, gate_args);
    dense(4, gated, block_output, elements, hidden_size);
    hc_post(block_output);

    // --- MoE ---------------------------------------------------------------
    hc_pre(tensor(7), layer.hc_ffn_down, layer.hc_ffn_up, layer.hc_ffn_inject);
    auto* staging = static_cast<std::uint8_t*>(runtime.host_staging);
    dense(8, normalized, router_logits, hidden_size, experts);
    if (flyweight_gpu_route_topk(router_logits, selected_device, route_weights,
                               experts, top_k, runtime.stream) != 0)
        throw std::runtime_error("native qwen4exp MTP routing failed");
    auto* selected_host = reinterpret_cast<std::int32_t*>(staging);
    const auto weights_offset = device_align(top_k * sizeof(std::int32_t));
    const auto input_offset = weights_offset + device_align(top_k * sizeof(float));
    const auto cpu_activated_offset = input_offset + device_align(hidden_size * sizeof(float));
    const auto output_offset = cpu_activated_offset
        + device_align(top_k * runtime.moe_intermediate * sizeof(float));
    auto* cpu_weights = reinterpret_cast<float*>(staging + weights_offset);
    auto* cpu_input = reinterpret_cast<float*>(staging + input_offset);
    auto* cpu_activated = reinterpret_cast<float*>(staging + cpu_activated_offset);
    auto* cpu_output = reinterpret_cast<float*>(staging + output_offset);
    if (flyweight_gpu_download(selected_host, selected_device, top_k * sizeof(std::int32_t),
                             runtime.stream) != 0
        || flyweight_gpu_download(cpu_weights, route_weights, top_k * sizeof(float),
                                runtime.stream) != 0
        || flyweight_gpu_download(cpu_input, normalized, hidden_size * sizeof(float),
                                runtime.stream) != 0)
        throw std::runtime_error("native qwen4exp MTP route transfer failed");
    {
        // Shared expert, then the routed experts on the host -- the same split
        // the ordinary qwen4exp decode uses.
        auto up_half = first + static_cast<std::uint64_t>(intermediate) * sizeof(float);
        dense(9, normalized, first, hidden_size, intermediate);
        dense(10, normalized, up_half, hidden_size, intermediate);
        int count = intermediate;
        void* silu_args[] = {const_cast<std::uint64_t*>(&first),
                             const_cast<std::uint64_t*>(&second), &count};
        launch("silu_mul", (static_cast<std::uint32_t>(count) + 255) / 256, 1, silu_args);
        dense(11, second, third, intermediate, hidden_size);
    }
    {
        auto shared_gate = tensor(12);
        const auto sg_type = runtime.model->tensors[layer.static_tensors.at(12)].type;
        void* shared_args[] = {const_cast<std::uint64_t*>(&normalized), &shared_gate,
                               const_cast<std::uint64_t*>(&third),
                               const_cast<int*>(&hidden_size)};
        launch(sg_type == 30 ? "qwen_shared_scale_bf16" : "qwen_shared_scale", 1, 1, shared_args);
    }
    if (flyweight_gpu_stream_sync(runtime.stream) != 0)
        throw std::runtime_error("native qwen4exp MTP route synchronization failed");
    for (int rank = 0; rank < top_k; ++rank) {
        if (selected_host[rank] < 0 || selected_host[rank] >= experts)
            throw std::runtime_error("native qwen4exp MTP routing selected an invalid expert");
        cpu_weights[rank] *=
            qwen_expert_role_scale(runtime, layer.expert_down_scale, selected_host[rank]);
    }
    qwen_cpu_moe(runtime, layer, selected_host, cpu_weights, top_k,
                 cpu_input, cpu_activated, cpu_output);
    if (flyweight_gpu_upload(fourth, cpu_output, hidden_size * sizeof(float),
                           runtime.stream) != 0)
        throw std::runtime_error("native qwen4exp MTP expert upload failed");
    {
        float one = 1.0f;
        int count = hidden_size;
        void* add_args[] = {const_cast<std::uint64_t*>(&third),
                            const_cast<std::uint64_t*>(&fourth), &one, &count};
        launch("scaled_add", (hidden_size + 255) / 256, 1, add_args);
    }
    hc_post(third);

    // --- hand-over and head ------------------------------------------------
    // The streams are what the next round reads back, so the whole row goes to
    // the draft-hidden slot (which is reserved hc_count*hidden wide on this
    // arch for exactly this reason).
    {
        auto draft_hidden = runtime.state + runtime.mtp_draft_hidden_offset;
        int count = hc_wide_size;
        void* copy_args[] = {const_cast<std::uint64_t*>(&hc_streams), &draft_hidden, &count};
        launch("qwen_copy_vector", (hc_wide_size + 255) / 256, 1, copy_args);
    }
    // No final norm on this arch: the inject-less collapse IS it.
    hc_pre(runtime.device_tensors[runtime.output_hc_norm],
           runtime.output_hc_down, runtime.output_hc_up,
           std::numeric_limits<std::uint64_t>::max());
    if (flyweight_gpu_memset(argmax_device, 0, sizeof(std::uint64_t), runtime.stream) != 0)
        throw std::runtime_error("native qwen4exp MTP argmax reset failed");
    int vocabulary = static_cast<int>(config.vocabulary_size);
    auto lm_head = runtime.device_tensors[runtime.lm_head];
    void* argmax_args[] = {&lm_head, const_cast<std::uint64_t*>(&normalized),
                           const_cast<std::uint64_t*>(&argmax_device),
                           const_cast<int*>(&hidden_size), &vocabulary};
    launch(qwen_lm_head_argmax_kernel(runtime.lm_head_type), (vocabulary + 7) / 8, 1, argmax_args);
    auto* packed_winner = reinterpret_cast<std::uint64_t*>(staging);
    if (flyweight_gpu_download(packed_winner, argmax_device, sizeof(*packed_winner),
                             runtime.stream) != 0
        || flyweight_gpu_stream_sync(runtime.stream) != 0)
        throw std::runtime_error("native qwen4exp MTP output synchronization failed");
    ++runtime.mtp_cache_tokens;
    ++runtime.mtp_draft_tokens;
    return 0xffffffffu - static_cast<std::uint32_t>(*packed_winner);
}

std::uint32_t qwen_mtp_draft(
    FlyweightV2QwenRuntime& runtime, std::uint32_t token,
    std::uint64_t input_hidden
) {
    const int hidden_size=static_cast<int>(runtime.model->config.hidden_size);
    const int experts=static_cast<int>(runtime.model->config.expert_count);
    const int top_k=static_cast<int>(runtime.model->config.expert_used_count);
    const int intermediate=static_cast<int>(runtime.moe_intermediate);
    const int heads=static_cast<int>(runtime.model->config.attention_heads);
    const int kv_heads=static_cast<int>(runtime.model->config.attention_kv_heads);
    const auto&layer=runtime.mtp_layer_plan;
    const int head_dim=static_cast<int>(runtime.model->tensors[layer.static_tensors[2]].shape[1]/kv_heads);
    const int q_size=heads*2*head_dim,kv_size=kv_heads*head_dim;
    const float epsilon=runtime.model->config.rms_norm_epsilon?runtime.model->config.rms_norm_epsilon:1.0e-6f;
    std::uint64_t cursor=runtime.workspace;
    const auto workspace_end=runtime.workspace+runtime.workspace_bytes;
    auto take=[&](std::uint64_t bytes){const auto result=cursor;cursor+=device_align(bytes);if(cursor>workspace_end)throw std::runtime_error("native MTP workspace is too small");return result;};
    const auto embedding=take(hidden_size*sizeof(float));
    const auto norm_embedding=take(hidden_size*sizeof(float));
    const auto norm_hidden=take(hidden_size*sizeof(float));
    const auto concatenated=take(2ULL*hidden_size*sizeof(float));
    std::uint64_t hidden=take(hidden_size*sizeof(float));
    std::uint64_t residual=take(hidden_size*sizeof(float));
    const auto normalized=take(hidden_size*sizeof(float));
    const auto first=take(runtime.scratch_elements*sizeof(float));
    const auto second=take(runtime.scratch_elements*sizeof(float));
    const auto third=take(runtime.scratch_elements*sizeof(float));
    const auto fourth=take(runtime.scratch_elements*sizeof(float));
    const auto activated=take(top_k*runtime.moe_intermediate*sizeof(float));
    const auto router_logits=take(experts*sizeof(float));
    const auto selected_device=take(top_k*sizeof(std::int32_t));
    const auto route_weights=take(top_k*sizeof(float));
    const auto argmax_device=take(sizeof(std::uint64_t));
    const auto attention_scores=take(static_cast<std::uint64_t>(heads)*runtime.options.context_limit*sizeof(float));
    auto launch=[&](const char*name,std::uint32_t gx,std::uint32_t gy,void**args){if(flyweight_gpu_launch_named(name,gx,gy,256,0,runtime.stream,args)!=0)throw std::runtime_error(std::string("native MTP CUDA kernel failed: ")+name);};
    auto dense_index=[&](std::size_t index,std::uint64_t input,std::uint64_t output,int input_size,int output_size){
        qwen_mtp_dense_projection(
            runtime,index,input,output,input_size,output_size);
    };
    auto rms=[&](std::uint64_t input,std::uint64_t weight,std::uint64_t output){int one_centered=0;void*args[]={&input,&weight,&output,const_cast<int*>(&hidden_size),const_cast<float*>(&epsilon),&one_centered};launch("rms_norm",1,1,args);};
    auto add=[&](std::uint64_t target,std::uint64_t source){float scale=1.0f;int count=hidden_size;void*args[]={&target,&source,&scale,&count};launch("scaled_add",(hidden_size+255)/256,1,args);};
    auto tensor=[&](std::size_t role){return runtime.device_tensors[layer.static_tensors.at(role)];};
    auto dense=[&](std::size_t role,std::uint64_t input,std::uint64_t output,int input_size,int output_size){dense_index(layer.static_tensors.at(role),input,output,input_size,output_size);};
    const std::uint32_t embedding_token=token;
    const auto embedding_matrix=qwen_stage_embedding_rows(runtime,&embedding_token,1);
    int token_value=runtime.embeddings_host_resident?0:static_cast<int>(token);
    void*embedding_args[]={const_cast<std::uint64_t*>(&embedding_matrix),const_cast<std::uint64_t*>(&embedding),&token_value,const_cast<int*>(&hidden_size)};
    launch(qwen_embedding_kernel(qwen_device_type(runtime,runtime.token_embeddings),false),(hidden_size+255)/256,1,embedding_args);

    rms(embedding,runtime.device_tensors[runtime.mtp_special_tensors[1]],norm_embedding);
    rms(input_hidden,runtime.device_tensors[runtime.mtp_special_tensors[2]],norm_hidden);
    void*concat_args[]={const_cast<std::uint64_t*>(&norm_embedding),const_cast<std::uint64_t*>(&norm_hidden),const_cast<std::uint64_t*>(&concatenated),const_cast<int*>(&hidden_size)};
    launch("qwen_concat_pair",(hidden_size+255)/256,1,concat_args);
    dense_index(runtime.mtp_special_tensors[0],concatenated,hidden,hidden_size*2,hidden_size);

    rms(hidden,tensor(0),normalized);
    dense(1,normalized,first,hidden_size,q_size);
    dense(2,normalized,second,hidden_size,kv_size);
    dense(3,normalized,third,hidden_size,kv_size);
    const int rotary=static_cast<int>(runtime.model->config.rotary_dimension?runtime.model->config.rotary_dimension:head_dim);
    const int position=static_cast<int>(runtime.mtp_cache_tokens);
    const float theta=runtime.model->config.rope_freq_base?runtime.model->config.rope_freq_base:1000000.0f;
    auto qnorm=tensor(5),knorm=tensor(6);std::uint64_t queries=fourth,gates=fourth+q_size/2*sizeof(float);
    void*q_args[]={const_cast<std::uint64_t*>(&first),&qnorm,&queries,&gates,const_cast<int*>(&heads),const_cast<int*>(&head_dim),const_cast<int*>(&rotary),const_cast<int*>(&position),const_cast<float*>(&theta),const_cast<float*>(&epsilon)};
    launch("qwen_attention_query",heads,1,q_args);
    std::uint64_t keys=first;
    void*k_args[]={const_cast<std::uint64_t*>(&second),&knorm,&keys,const_cast<int*>(&kv_heads),const_cast<int*>(&head_dim),const_cast<int*>(&rotary),const_cast<int*>(&position),const_cast<float*>(&theta),const_cast<float*>(&epsilon)};
    launch("qwen_attention_key",kv_heads,1,k_args);
    auto cache_keys=runtime.state+layer.state_first,cache_values=runtime.state+layer.state_second;int capacity=static_cast<int>(runtime.options.context_limit);
    void*key_store_args[]={&keys,&cache_keys,const_cast<int*>(&kv_heads),const_cast<int*>(&head_dim),const_cast<int*>(&position),&capacity};
    launch(kv_store_kernel(runtime.options.cache_type_k,true),kv_heads,1,key_store_args);
    void*value_store_args[]={const_cast<std::uint64_t*>(&third),&cache_values,const_cast<int*>(&kv_heads),const_cast<int*>(&head_dim),const_cast<int*>(&position),&capacity};
    launch(kv_store_kernel(runtime.options.cache_type_v,false),kv_heads,1,value_store_args);
    std::uint64_t attended=second;int tokens=position+1;float scale=1.0f/std::sqrt(static_cast<float>(head_dim));
    void*score_args[]={&queries,&cache_keys,const_cast<std::uint64_t*>(&attention_scores),const_cast<int*>(&heads),const_cast<int*>(&kv_heads),const_cast<int*>(&head_dim),&tokens,&capacity,&scale};
    launch(kv_scores_kernel(runtime),heads,(tokens+255)/256,score_args);
    void*value_args[]={const_cast<std::uint64_t*>(&attention_scores),&cache_values,&attended,const_cast<int*>(&heads),const_cast<int*>(&kv_heads),const_cast<int*>(&head_dim),&tokens,&capacity};
    launch(kv_values_kernel(runtime),heads,1,value_args);
    std::uint64_t gated=third;int elements=heads*head_dim;void*gate_args[]={&attended,&gates,&gated,&elements};launch("qwen_attention_gate",(elements+255)/256,1,gate_args);
    dense(4,gated,residual,elements,hidden_size);add(residual,hidden);

    rms(residual,tensor(7),normalized);
    auto*staging=static_cast<std::uint8_t*>(runtime.host_staging);
    if(layer.dense_ffn){
        // Dense draft block: the nextn layer of a dense checkpoint carries its
        // own gate/up/down instead of a router and experts.
        const int dense_intermediate=static_cast<int>(runtime.moe_intermediate);
        const auto up_half=first+static_cast<std::uint64_t>(dense_intermediate)*sizeof(float);
        dense(8,normalized,first,hidden_size,dense_intermediate);
        dense(9,normalized,up_half,hidden_size,dense_intermediate);
        int dense_count=dense_intermediate;
        void*dense_silu_args[]={const_cast<std::uint64_t*>(&first),const_cast<std::uint64_t*>(&second),&dense_count};
        launch("silu_mul",(static_cast<std::uint32_t>(dense_count)+255)/256,1,dense_silu_args);
        dense(10,second,third,dense_intermediate,hidden_size);
        add(residual,third);std::swap(hidden,residual);
    }else{
    dense(8,normalized,router_logits,hidden_size,experts);

    if(flyweight_gpu_route_topk(router_logits,selected_device,route_weights,experts,top_k,runtime.stream)!=0)throw std::runtime_error("native MTP routing failed");
    auto*selected_host=reinterpret_cast<std::int32_t*>(staging);
    const auto weights_offset=device_align(top_k*sizeof(std::int32_t));const auto input_offset=weights_offset+device_align(top_k*sizeof(float));const auto cpu_activated_offset=input_offset+device_align(hidden_size*sizeof(float));const auto output_offset=cpu_activated_offset+device_align(top_k*runtime.moe_intermediate*sizeof(float));
    auto*cpu_weights=reinterpret_cast<float*>(staging+weights_offset);auto*cpu_input=reinterpret_cast<float*>(staging+input_offset);auto*cpu_activated=reinterpret_cast<float*>(staging+cpu_activated_offset);auto*cpu_output=reinterpret_cast<float*>(staging+output_offset);
    if(flyweight_gpu_download(selected_host,selected_device,top_k*sizeof(std::int32_t),runtime.stream)!=0||flyweight_gpu_download(cpu_weights,route_weights,top_k*sizeof(float),runtime.stream)!=0||flyweight_gpu_download(cpu_input,normalized,hidden_size*sizeof(float),runtime.stream)!=0)throw std::runtime_error("native MTP route transfer failed");
    auto shared_gate_matrix=tensor(9),shared_up_matrix=tensor(10);
    const auto shexp_type=runtime.model->tensors[layer.static_tensors.at(9)].type;
    if(shexp_type==40){
        // NVFP4 shared expert: gate/up scales before the non-linear SiLU, the
        // linear down scale on the down projection itself.
        auto shared_gate_scale=layer.shared_gate_scale,shared_up_scale=layer.shared_up_scale;
        void*silu_args[]={&shared_gate_matrix,&shared_up_matrix,const_cast<std::uint64_t*>(&normalized),const_cast<std::uint64_t*>(&second),const_cast<int*>(&hidden_size),const_cast<int*>(&intermediate),&shared_gate_scale,&shared_up_scale};
        launch("nvfp4_swiglu_transposed",intermediate,1,silu_args);
        auto shared_down_matrix=tensor(11);auto shared_down_scale=layer.shared_down_scale;
        void*down_args[]={&shared_down_matrix,const_cast<std::uint64_t*>(&second),const_cast<std::uint64_t*>(&third),const_cast<int*>(&intermediate),const_cast<int*>(&hidden_size),&shared_down_scale};
        launch("nvfp4_matvec_transposed",hidden_size,1,down_args);
    }else if(shexp_type==8){
        void*silu_args[]={&shared_gate_matrix,&shared_up_matrix,const_cast<std::uint64_t*>(&normalized),const_cast<std::uint64_t*>(&second),const_cast<int*>(&hidden_size),const_cast<int*>(&intermediate)};
        launch("q8_swiglu_transposed_warp",(intermediate+7)/8,1,silu_args);
        dense(11,second,third,intermediate,hidden_size);
    }else{
        // No fused SwiGLU kernel for this weight type: project gate and up into one
        // contiguous scratch pair and let silu_mul combine them.
        auto up_half=first+static_cast<std::uint64_t>(intermediate)*sizeof(float);
        dense(9,normalized,first,hidden_size,intermediate);
        dense(10,normalized,up_half,hidden_size,intermediate);
        int count=intermediate;
        void*silu_args[]={const_cast<std::uint64_t*>(&first),const_cast<std::uint64_t*>(&second),&count};
        launch("silu_mul",(static_cast<std::uint32_t>(count)+255)/256,1,silu_args);
        dense(11,second,third,intermediate,hidden_size);
    }
    auto shared_gate=tensor(12);
    {
        const auto sg_type=runtime.model->tensors[layer.static_tensors.at(12)].type;
        void*shared_args[]={const_cast<std::uint64_t*>(&normalized),&shared_gate,const_cast<std::uint64_t*>(&third),const_cast<int*>(&hidden_size)};
        launch(sg_type==30?"qwen_shared_scale_bf16":"qwen_shared_scale",1,1,shared_args);
    }
    if(flyweight_gpu_stream_sync(runtime.stream)!=0)throw std::runtime_error("native MTP route synchronization failed");
    // qwen_cpu_moe expects the linear down scale already folded into the routing
    // weights; it applies gate/up itself.
    for(int rank=0;rank<top_k;++rank){
        if(selected_host[rank]<0||selected_host[rank]>=experts)
            throw std::runtime_error("native MTP routing selected an invalid expert");
        cpu_weights[rank]*=qwen_expert_role_scale(runtime,layer.expert_down_scale,selected_host[rank]);
    }
    qwen_cpu_moe(runtime,layer,selected_host,cpu_weights,top_k,cpu_input,cpu_activated,cpu_output);
    if(flyweight_gpu_upload(fourth,cpu_output,hidden_size*sizeof(float),runtime.stream)!=0)throw std::runtime_error("native MTP expert upload failed");
    add(third,fourth);add(residual,third);std::swap(hidden,residual);
    }
    auto draft_hidden=runtime.state+runtime.mtp_draft_hidden_offset;void*copy_args[]={&hidden,&draft_hidden,const_cast<int*>(&hidden_size)};launch("qwen_copy_vector",(hidden_size+255)/256,1,copy_args);
    rms(hidden,runtime.device_tensors[runtime.mtp_special_tensors[3]],normalized);
    if(flyweight_gpu_memset(argmax_device,0,sizeof(std::uint64_t),runtime.stream)!=0)throw std::runtime_error("native MTP argmax reset failed");
    int vocabulary=static_cast<int>(runtime.model->config.vocabulary_size);auto lm_head=runtime.device_tensors[runtime.lm_head];void*argmax_args[]={&lm_head,const_cast<std::uint64_t*>(&normalized),const_cast<std::uint64_t*>(&argmax_device),const_cast<int*>(&hidden_size),&vocabulary};launch(qwen_lm_head_argmax_kernel(runtime.lm_head_type),(vocabulary+7)/8,1,argmax_args);
    auto*packed_winner=reinterpret_cast<std::uint64_t*>(staging);if(flyweight_gpu_download(packed_winner,argmax_device,sizeof(*packed_winner),runtime.stream)!=0||flyweight_gpu_stream_sync(runtime.stream)!=0)throw std::runtime_error("native MTP output synchronization failed");
    ++runtime.mtp_cache_tokens;++runtime.mtp_draft_tokens;
    return 0xffffffffu-static_cast<std::uint32_t>(*packed_winner);
}

void qwen_snapshot_delta_state(FlyweightV2QwenRuntime& runtime, bool restore) {
    auto launch_copy=[&](std::uint64_t source,std::uint64_t destination,std::uint64_t bytes){
        const int elements=static_cast<int>(bytes/sizeof(float));
        void*args[]={&source,&destination,const_cast<int*>(&elements)};
        if(flyweight_gpu_launch_named("qwen_copy_vector",(elements+255)/256,1,256,0,runtime.stream,args)!=0)
            throw std::runtime_error("native MTP state snapshot failed");
    };
    for(std::uint32_t layer_number=0;layer_number<runtime.layers.size();++layer_number){
        auto&layer=runtime.layers[layer_number];
        // qwen4exp's PLE conv ring, same treatment as the DeltaNet conv below.
        // The verify pass advances it by `wanted` tokens; a rejection has to
        // put it back or the target keeps generating from a state a few tokens
        // into a future it did not take. Mirrors the prefill checkpoint, which
        // has always carried this ring.
        if(layer.ple_conv!=std::numeric_limits<std::uint64_t>::max()){
            const auto&ple_conv=runtime.model->tensors[layer.ple_conv];
            const auto ple_bytes=(ple_conv.shape[0]-1)
                *std::max<std::uint64_t>(runtime.model->config.ple_ngram_size,1)
                *ple_conv.shape[1]*sizeof(float);
            const auto live=runtime.state+layer.state_third;
            const auto saved=runtime.state+layer.snapshot_third;
            launch_copy(restore?saved:live,restore?live:saved,ple_bytes);
        }
        if(layer.attention)continue;
        const auto&conv=runtime.model->tensors[tensor_index(*runtime.model,"blk."+std::to_string(layer_number)+".ssm_conv1d.weight")];
        const auto&a=runtime.model->tensors[tensor_index(*runtime.model,"blk."+std::to_string(layer_number)+".ssm_a")];
        const auto&norm=runtime.model->tensors[tensor_index(*runtime.model,"blk."+std::to_string(layer_number)+".ssm_norm.weight")];
        const auto conv_bytes=conv.shape[0]*conv.shape[1]*sizeof(float);
        const auto recurrent_bytes=a.shape[0]*norm.shape[0]*norm.shape[0]*sizeof(float);
        const auto live_conv=runtime.state+layer.state_first;
        const auto live_recurrent=runtime.state+layer.state_second;
        const auto saved_conv=runtime.state+layer.snapshot_first;
        const auto saved_recurrent=runtime.state+layer.snapshot_second;
        launch_copy(restore?saved_conv:live_conv,restore?live_conv:saved_conv,conv_bytes);
        launch_copy(restore?saved_recurrent:live_recurrent,restore?live_recurrent:saved_recurrent,recurrent_bytes);
    }
}

// ReplaySSM-style fold: rebuild the DeltaNet state after an MTP rejection by
// replaying only the causal-conv and recurrence kernels over the `rows`
// accepted tokens, reading the transition inputs the verify pass wrote into
// each layer's retention arena. Bitwise identical to the full re-forward it
// replaces -- same kernels, same launch geometry, same inputs, same restored
// snapshot -- while skipping the attention/FFN/projection work whose results
// (KV rows, hidden states, verified tokens) the batch verify already produced
// correctly for the accepted prefix. The caller must restore the state
// snapshot first. The verifier caps at 8 rows, far below the chunked-WY
// threshold, so mirroring the sequential kernel selection is exhaustive.
void qwen_mtp_fold_delta_state(FlyweightV2QwenRuntime& runtime, int rows) {
    const auto&layout=runtime.rows_workspace_layout;
    // Scratch: conv output, recurrence output and the gate operand land in the
    // idle rows workspace. Gates only shape the (discarded) output, never the
    // state, so the stale contents of `second` are acceptable.
    const auto convolved=layout.fourth.address(runtime.workspace);
    const auto gates=layout.second.address(runtime.workspace);
    const auto output=layout.first.address(runtime.workspace);
    const float epsilon=runtime.model->config.rms_norm_epsilon
        ?runtime.model->config.rms_norm_epsilon:1.0e-6f;
    auto launch=[&](const char*name,std::uint32_t gx,std::uint32_t bx,void**args){
        if(flyweight_gpu_launch_named(name,gx,1,bx,0,runtime.stream,args)!=0)
            throw std::runtime_error(std::string("native MTP fold kernel failed: ")+name);
    };
    for(std::uint32_t layer_number=0;layer_number<runtime.layers.size();++layer_number){
        auto&layer=runtime.layers[layer_number];
        if(layer.attention)continue;
        auto tensor=[&](std::size_t role){return runtime.device_tensors[layer.static_tensors.at(role)];};
        int channels=static_cast<int>(runtime.model->tensors[layer.static_tensors[1]].shape[1]);
        int value_heads=static_cast<int>(runtime.model->tensors[layer.static_tensors[8]].shape[0]);
        int head_dim=static_cast<int>(runtime.model->tensors[layer.static_tensors[9]].shape[0]);
        int value_dim=value_heads*head_dim;
        int key_heads=(channels-value_dim)/(2*head_dim);
        int kernel_size=static_cast<int>(runtime.model->tensors[layer.static_tensors[6]].shape[0]);
        const auto qkv=runtime.state+layer.mtp_retain_qkv;
        const auto decay_logits=runtime.state+layer.mtp_retain_logits;
        // The retention layout was written with the full batch's row count,
        // which places the beta logits behind mtp_fold_rows decay rows even
        // when the fold replays fewer.
        const auto beta_logits=decay_logits+
            static_cast<std::uint64_t>(runtime.mtp_fold_rows)*value_heads*sizeof(float);
        auto conv_state=runtime.state+layer.state_first;auto conv_weights=tensor(6);
        void*conv_args[]={const_cast<std::uint64_t*>(&qkv),&conv_weights,&conv_state,const_cast<std::uint64_t*>(&convolved),&rows,&channels,&kernel_size};
        if(kernel_size<=8)launch("delta_conv_chunk",(static_cast<std::uint32_t>(channels)+255)/256,256,conv_args);
        else launch("delta_conv_sequence",static_cast<std::uint32_t>(channels),1,conv_args);
        auto recurrent_state=runtime.state+layer.state_second;auto decay=tensor(8),dt=tensor(7),norm=tensor(9);
        // MTP fold replay is unreachable for qwen4exp (no draft block), so the
        // gate stays the qwen3.5 silu form.
        int gate_sigmoid=0;
        void*recurrent_args[]={const_cast<std::uint64_t*>(&convolved),const_cast<std::uint64_t*>(&gates),const_cast<std::uint64_t*>(&beta_logits),const_cast<std::uint64_t*>(&decay_logits),&decay,&dt,&norm,&recurrent_state,const_cast<std::uint64_t*>(&output),&rows,&key_heads,&value_heads,&head_dim,const_cast<float*>(&epsilon),&gate_sigmoid};
        if(head_dim==128)launch("qwen_delta_recurrent_chunk",static_cast<std::uint32_t>(value_heads),128,recurrent_args);
        else launch("qwen_delta_recurrent_rows",static_cast<std::uint32_t>(value_heads),256,recurrent_args);
    }
}

// Model-agnostic expert-router pruning. Given the `count` experts selected
// by route_topk with their (softmax) weights in the parallel `weights` array,
// keep the smallest nucleus whose cumulative weight fraction reaches top_p,
// after a hard cap of cap_k (0 = no extra cap), always keeping at least one.
// Reorders selected/weights into descending-weight order over the kept set and
// renormalizes the kept weights to sum to 1 (so the MoE combination preserves
// scale). Returns the number of experts kept. No model-specific constants:
// this is a function of the router distribution alone, so it generalizes to
// any MoE (Qwen, Gemma, DeepSeek, ...) and any expert quantization.
static int apply_expert_router_policy(std::int32_t* selected, float* weights,
        int count, int cap_k, float top_p) {
    if(count<=0) return count;
    if(cap_k>0 && cap_k<count) count=cap_k;
    std::array<int,256> order{};
    for(int i=0;i<count;++i) order[i]=i;
    std::sort(order.begin(), order.begin()+count,
        [&](int a,int b){return weights[a]>weights[b];});
    std::array<float,256> w{}; std::array<std::int32_t,256> e{};
    float total=0.0f;
    for(int i=0;i<count;++i){ w[i]=weights[order[i]]; e[i]=selected[order[i]]; total+=w[i]; }
    if(total<=0.0f) total=1.0f;
    int keep=count;
    if(top_p>0.0f && top_p<1.0f){
        float cum=0.0f; keep=0;
        for(int i=0;i<count;++i){ cum+=w[i]; ++keep; if(cum/total>=top_p) break; }
        if(keep<1) keep=1;
    }
    float sum=0.0f;
    for(int i=0;i<keep;++i) sum+=w[i];
    if(sum<=0.0f) sum=1.0f;
    for(int i=0;i<keep;++i){ selected[i]=e[i]; weights[i]=w[i]/sum; }
    return keep;
}

// Copy every DeltaNet layer's conv+recurrent state between the live state
// arena and a prefill snapshot buffer (packed sequentially in layer order).
void qwen_prefill_snapshot_copy(
    FlyweightV2QwenRuntime& runtime, std::uint64_t snapshot, bool restore
) {
    auto launch_copy=[&](std::uint64_t source,std::uint64_t destination,std::uint64_t bytes){
        const int elements=static_cast<int>(bytes/sizeof(float));
        void*args[]={&source,&destination,const_cast<int*>(&elements)};
        if(flyweight_gpu_launch_named("qwen_copy_vector",(static_cast<std::uint32_t>(elements)+255)/256,1,256,0,runtime.stream,args)!=0)
            throw std::runtime_error("native Qwen prefill snapshot copy failed");
    };
    std::uint64_t cursor=snapshot;
    for(const auto&layer:runtime.layers){
        // qwen4exp: the PLE conv ring is recurrent state exactly like the
        // DeltaNet conv and must ride every checkpoint/restore with it.
        if(layer.ple_conv!=std::numeric_limits<std::uint64_t>::max()){
            const auto&ple_conv=runtime.model->tensors[layer.ple_conv];
            const auto ple_bytes=(ple_conv.shape[0]-1)*
                std::max<std::uint64_t>(runtime.model->config.ple_ngram_size,1)*
                ple_conv.shape[1]*sizeof(float);
            const auto live=runtime.state+layer.state_third;
            launch_copy(restore?cursor:live,restore?live:cursor,ple_bytes);
            cursor+=ple_bytes;
        }
        if(layer.attention){
            // QSA raw-key ring: the partial block behind the snapshot position
            // (the block-key store needs no copy -- position bounds it).
            // Order and sizes mirror the snapshot_floats accumulator exactly.
            if(layer.qsa_ratio&&layer.indexer_q!=std::numeric_limits<std::uint64_t>::max()){
                const auto ring_bytes=static_cast<std::uint64_t>(layer.qsa_ratio)
                    *runtime.model->config.indexer_key_length*sizeof(float);
                const auto live=runtime.state+layer.qsa_ring;
                launch_copy(restore?cursor:live,restore?live:cursor,ring_bytes);
                cursor+=ring_bytes;
            }
            continue;
        }
        const auto&conv=runtime.model->tensors[layer.static_tensors[6]];
        const auto&a=runtime.model->tensors[layer.static_tensors[8]];
        const auto&norm=runtime.model->tensors[layer.static_tensors[9]];
        const auto conv_bytes=conv.shape[0]*conv.shape[1]*sizeof(float);
        const auto recurrent_bytes=a.shape[0]*norm.shape[0]*norm.shape[0]*sizeof(float);
        const auto live_conv=runtime.state+layer.state_first;
        const auto live_recurrent=runtime.state+layer.state_second;
        launch_copy(restore?cursor:live_conv,restore?live_conv:cursor,conv_bytes);
        cursor+=conv_bytes;
        launch_copy(restore?cursor:live_recurrent,restore?live_recurrent:cursor,recurrent_bytes);
        cursor+=recurrent_bytes;
    }
}

struct AttentionCacheView { int slot, first, tokens, capacity; };
inline AttentionCacheView attention_cache_view(const QwenLayerPlan& layer,std::uint64_t position){
    const auto capacity=static_cast<int>(layer.cache_capacity);
    const auto visible=layer.attention_window
        ?std::min<std::uint64_t>(position+1,layer.attention_window):position+1;
    const auto first_absolute=position+1-visible;
    return {static_cast<int>(position%layer.cache_capacity),static_cast<int>(first_absolute%layer.cache_capacity),static_cast<int>(visible),capacity};
}
inline bool swa_snapshot_is_resident(const FlyweightV2QwenRuntime& runtime,std::uint64_t snapshot_position,std::uint64_t live_position){
    if(snapshot_position>live_position)return false;
    for(const auto& layer:runtime.layers)if(layer.attention_window&&layer.cache_capacity<runtime.options.context_limit){
        const auto rollback_room=layer.cache_capacity-layer.attention_window;
        if(live_position-snapshot_position>rollback_room)return false;
    }
    return true;
}

// ---- QSA selection (qwen4exp phase 3, plans/qwen4exp-semantics.md) --------
// The indexer maintenance (raw-key ring + block-key store) runs on every
// token from position 0 so history exists when the context first crosses the
// budget; the selection itself is a no-op until (position+1)/ratio exceeds
// top_k/ratio complete blocks, which keeps the dense path -- and its
// phase-2 bit-exactness -- untouched below 2051 visible tokens.
// Sparse attention reads the cache through kv_ld like the ring kernels; the
// turbo codecs rotate rows and keep their own staged path, so a turbo cache
// stays on the dense fallback (announced once).
inline const char* kv_scores_indexed_kernel(const FlyweightV2QwenRuntime& r){int t=r.options.cache_type_k;return t==3?"kv_attention_scores_q8_indexed":t==2?"kv_attention_scores_bf16_indexed":t==1?"kv_attention_scores_f16_indexed":t==0?"kv_attention_scores_indexed":nullptr;}
inline const char* kv_values_indexed_kernel(const FlyweightV2QwenRuntime& r){int t=r.options.cache_type_v;return t==3?"kv_attention_values_q8_indexed":t==2?"kv_attention_values_bf16_indexed":t==1?"kv_attention_values_f16_indexed":t==0?"kv_attention_values_indexed":nullptr;}
// A layer selects when it carries indexer tensors AND a compress ratio. The
// window exclusion is structural, not incidental: QSA selects in POSITION
// space and hands the attention kernels those positions as cache slots, which
// only coincides on a global layer where capacity == context_limit. No arch
// ships both today; the guard is what keeps the next one from silently
// addressing the wrong rows.
inline bool qwen_qsa_layer(const FlyweightV2QwenRuntime& r,const QwenLayerPlan& layer){
    return r.qsa_enabled&&r.qwen4exp&&layer.attention&&layer.qsa_ratio
        &&!layer.attention_window
        &&layer.indexer_q!=std::numeric_limits<std::uint64_t>::max()
        &&r.model->config.indexer_head_count&&r.model->config.indexer_key_length
        &&r.model->config.indexer_top_k;
}
// Whether the block store can still take this position's block. Past the
// context limit the KV slabs wrap and the conversation is already lost, but
// the block store is indexed linearly (a block key is not a ring row), so it
// must stop rather than write past its reservation into the next layer's KV.
// Only the rows path can get here -- both decode entry points bound the
// position first -- and only on a prompt longer than the configured context.
inline bool qwen_qsa_in_range(const QwenLayerPlan& layer,std::uint64_t position){
    return position<layer.cache_capacity;
}
inline bool qwen_qsa_selecting(const FlyweightV2QwenRuntime& r,const QwenLayerPlan& layer,std::uint64_t position){
    const auto ratio=layer.qsa_ratio;
    if(!ratio||!qwen_qsa_in_range(layer,position))return false;
    if((position+1)/ratio<=r.model->config.indexer_top_k/ratio)return false;
    if(kv_scores_indexed_kernel(r)&&kv_values_indexed_kernel(r))return true;
    static bool announced=false;
    if(!announced){
        announced=true;
        std::fprintf(stderr,"[flyweight] QSA selection needs a f32/f16/bf16/q8 KV cache; "
            "turbo caches keep the dense fallback (approximate past the indexer budget)\n");
    }
    return false;
}
// Download the block scores, pick the top budget/ratio blocks -- score
// descending, block index ascending on ties, deterministic -- and upload the
// slot list: the winning blocks' positions ascending, then the incomplete
// tail. Positions ARE cache slots here (QSA layers carry no window). Returns
// the list length. Costs one stream sync per selecting layer; CUDA graphs
// are already off for the arch.
inline int qwen_qsa_select(
    const FlyweightV2QwenRuntime& runtime,const QwenLayerPlan& layer,
    std::uint64_t block_scores_device,std::uint64_t slots_device,
    std::uint64_t position
){
    const int ratio=static_cast<int>(layer.qsa_ratio);
    const int visible=static_cast<int>(position)+1;
    const int complete=visible/ratio;
    const int budget_blocks=static_cast<int>(runtime.model->config.indexer_top_k)/ratio;
    static thread_local std::vector<float> scores;
    static thread_local std::vector<std::int32_t> order,slots;
    scores.resize(complete);
    if(flyweight_gpu_download(scores.data(),block_scores_device,
            static_cast<std::uint64_t>(complete)*sizeof(float),runtime.stream)!=0
       ||flyweight_gpu_stream_sync(runtime.stream)!=0)
        throw std::runtime_error("native Qwen QSA score download failed");
    order.resize(complete);
    for(int i=0;i<complete;++i)order[i]=i;
    // A non-finite score would make `better` non-transitive (NaN compares
    // unequal to everything yet greater than nothing), and nth_element on a
    // comparator that is not a strict weak ordering is undefined -- it can
    // walk off the range. Sink them instead: a block whose score overflowed is
    // exactly the block to drop.
    for(int i=0;i<complete;++i)
        if(!std::isfinite(scores[i]))scores[i]=-std::numeric_limits<float>::max();
    const auto better=[&](std::int32_t a,std::int32_t b){
        return scores[a]!=scores[b]?scores[a]>scores[b]:a<b;};
    std::nth_element(order.begin(),order.begin()+budget_blocks,order.end(),better);
    std::sort(order.begin(),order.begin()+budget_blocks);
    slots.clear();
    for(int i=0;i<budget_blocks;++i)
        for(int j=0;j<ratio;++j)slots.push_back(order[i]*ratio+j);
    for(int t=complete*ratio;t<visible;++t)slots.push_back(t);
    if(flyweight_gpu_upload(slots_device,slots.data(),
            slots.size()*sizeof(std::int32_t),runtime.stream)!=0)
        throw std::runtime_error("native Qwen QSA slot upload failed");
    // FLYWEIGHT_QSA_TRACE=1: proof the selection is live and how hard it prunes.
    static const bool trace=std::getenv("FLYWEIGHT_QSA_TRACE")!=nullptr;
    if(trace){
        static std::uint64_t calls=0,kept=0,candidates=0;
        ++calls;kept+=slots.size();candidates+=visible;
        if(calls==1||calls%4096==0)
            std::fprintf(stderr,
                "[qsa] selections=%llu kept=%llu/%llu tokens (%.1f%%)\n",
                (unsigned long long)calls,(unsigned long long)kept,
                (unsigned long long)candidates,
                candidates?100.0*kept/candidates:0.0);
    }
    return static_cast<int>(slots.size());
}
// No fused turbo prefill kernel yet, so a turbo cache falls back to the
// separate scores+values path, which handles every precision.
inline bool kv_fused_prefill_ok(const FlyweightV2QwenRuntime& r){return r.options.cache_type_k==r.options.cache_type_v&&!kv_type_is_turbo(r.options.cache_type_k);}
inline const char* kv_prefill_kernel(const FlyweightV2QwenRuntime& r){int t=r.options.cache_type_k;return t==3?"kv_attention_prefill_q8":t==2?"kv_attention_prefill_bf16":t==1?"kv_attention_prefill_f16":"kv_attention_prefill";}

// Dump one attention layer's live KV window for the TurboQuant quality harness
// (native/tools/bench_turboquant.cpp). The file is little-endian and
// headerless apart from two int32s: count, head_dim, then count*head_dim f32
// keys followed by the same again for values, one row per (kv_head, position).
//
// Whatever precision the cache runs at is decoded to f32 on the way out, so a
// dump taken under cache_type f16 or q8_0 still describes the distribution the
// codec would actually see rather than an idealized f32 one. Only the live
// window is written: for a sliding-window layer the ring has stale slots
// outside it, and feeding those to the harness would skew the norm statistics
// that drive the K/V bit allocation.
// The tensors the capture actually saw, in model order. Deterministic, so a
// caller can enumerate entries by slot across two calls.
static std::vector<std::size_t> qwen_imatrix_active(
    const FlyweightV2QwenRuntime& runtime
) {
    std::vector<std::size_t> active;
    for(std::size_t i=0;i<runtime.imatrix_rows.size();++i)
        if(runtime.imatrix_rows[i])active.push_back(i);
    return active;
}

int flyweight_v2_qwen_imatrix_count(
    FlyweightV2QwenRuntime* runtime, uint64_t* count
){return guarded([&]{
    if(!runtime||!count)throw std::runtime_error("invalid Qwen imatrix arguments");
    *count=qwen_imatrix_active(*runtime).size();
    return 0;
});}

int flyweight_v2_qwen_imatrix_entry(
    FlyweightV2QwenRuntime* runtime, uint64_t slot, char* name,
    uint64_t name_capacity, float* sums, uint64_t sums_capacity,
    uint64_t* width, uint64_t* rows_seen
){return guarded([&]{
    if(!runtime)throw std::runtime_error("invalid Qwen imatrix arguments");
    const auto active=qwen_imatrix_active(*runtime);
    if(slot>=active.size())
        throw std::runtime_error("Qwen imatrix entry slot out of range");
    const auto index=active[slot];
    const auto& tensor=runtime->model->tensors[index];
    const auto& shape=tensor.shape;
    const std::uint64_t channels=shape[0]*(shape.size()==3?shape[2]:1);
    if(name&&name_capacity)copy_text(name,name_capacity,tensor.name);
    if(width)*width=channels;
    if(rows_seen)*rows_seen=runtime->imatrix_rows[index];
    if(!sums)return 0;
    if(sums_capacity<channels)
        throw std::runtime_error("Qwen imatrix entry buffer is too small");
    const auto offset=runtime->imatrix_offsets[index];
    if(flyweight_gpu_download(sums,runtime->imatrix_sums+offset*sizeof(float),
                            channels*sizeof(float),runtime->stream)!=0||
       flyweight_gpu_stream_sync(runtime->stream)!=0)
        throw std::runtime_error("failed to download the Qwen imatrix arena");
    // The CPU expert path accumulated beside the device, not into it.
    const float* host=runtime->imatrix_host.data()+offset;
    for(std::uint64_t i=0;i<channels;++i)sums[i]+=host[i];
    return 0;
});}

int flyweight_v2_qwen_runtime_dump_kv(
    FlyweightV2QwenRuntime* runtime, uint32_t layer_index, const char* path
){return guarded([&]{
    if(!runtime||!path)throw std::runtime_error("invalid Qwen KV dump arguments");
    if(!runtime->state)throw std::runtime_error("native Qwen runtime is not prepared");
    if(runtime->position==0)throw std::runtime_error("native Qwen KV dump needs at least one decoded token");
    if(layer_index>=runtime->layers.size())throw std::runtime_error("native Qwen KV dump layer index out of range");
    const auto& layer=runtime->layers[layer_index];
    if(!layer.attention)throw std::runtime_error("native Qwen KV dump requires an attention layer");

    const auto view=attention_cache_view(layer,runtime->position-1);
    const int head_dim=static_cast<int>(layer.head_dim);
    const int kv_heads=static_cast<int>(layer.kv_heads);
    const int capacity=view.capacity,tokens=view.tokens;
    const int ck=runtime->options.cache_type_k,cv=runtime->options.cache_type_v;
    if((ck>=3||cv>=3)&&head_dim%32!=0)
        throw std::runtime_error("native Qwen KV dump cannot decode a blocked cache type with a head_dim that is not a multiple of 32");

    const std::uint64_t elements=static_cast<std::uint64_t>(kv_heads)*capacity*head_dim;
    auto region_bytes=[&](int type){return kv_type_bytes(elements,type);};

    std::vector<std::uint8_t> raw_keys(region_bytes(ck)),raw_values(region_bytes(cv));
    if(flyweight_gpu_download(raw_keys.data(),runtime->state+layer.state_first,raw_keys.size(),runtime->stream)!=0
       ||flyweight_gpu_download(raw_values.data(),runtime->state+layer.state_second,raw_values.size(),runtime->stream)!=0)
        throw std::runtime_error("failed to download native Qwen KV cache");
    if(flyweight_gpu_stream_sync(runtime->stream)!=0)
        throw std::runtime_error("failed to synchronize native Qwen KV dump");

    auto element=[&](const std::uint8_t* raw,int type,std::uint64_t row,int d)->float{
        if(type==3){
            const auto* base=raw+row*static_cast<std::uint64_t>(head_dim/32)*kQ8BlockSize;
            return qwen_q8_value(base,static_cast<std::uint64_t>(d));
        }
        const std::uint64_t index=row*head_dim+d;
        if(type==0){float value=0.0f;std::memcpy(&value,raw+index*4,4);return value;}
        std::uint16_t bits=0;std::memcpy(&bits,raw+index*2,2);
        if(type==2){const std::uint32_t wide=static_cast<std::uint32_t>(bits)<<16;float value=0.0f;std::memcpy(&value,&wide,4);return value;}
        return qwen_half_value(bits);
    };

    const std::int32_t count=kv_heads*tokens;
    std::vector<float> keys(static_cast<std::size_t>(count)*head_dim);
    std::vector<float> values(static_cast<std::size_t>(count)*head_dim);
    // A turbo cache stores rotated rows, so decoding one means undoing the
    // rotation with the same sign stream the store kernel used (0 for keys,
    // 1 for values). Routing that through the CPU reference in turboquant.h
    // also means this dump reads back what the kernels wrote using the
    // definition the contract test pins.
    std::vector<float> rotated(static_cast<std::size_t>(head_dim));
    auto fill=[&](const std::uint8_t* raw,int type,std::uint64_t row,float* out,std::uint32_t stream){
        if(kv_type_is_turbo(type)){
            const auto turbo=type==4?TurboType::Turbo3:TurboType::Turbo4;
            const auto stride=static_cast<std::uint64_t>(head_dim/kTurboBlock)*turbo_block_bytes(turbo);
            turbo_decode_vector(raw+row*stride,head_dim,turbo,rotated.data());
            turbo_inverse_rotate(rotated.data(),out,head_dim,stream);
            return;
        }
        for(int d=0;d<head_dim;++d)out[d]=element(raw,type,row,d);
    };
    for(int head=0;head<kv_heads;++head)for(int t=0;t<tokens;++t){
        const std::uint64_t row=static_cast<std::uint64_t>(head)*capacity+(view.first+t)%capacity;
        const auto offset=(static_cast<std::size_t>(head)*tokens+t)*head_dim;
        fill(raw_keys.data(),ck,row,keys.data()+offset,0u);
        fill(raw_values.data(),cv,row,values.data()+offset,1u);
    }

    std::ofstream out(path,std::ios::binary);
    if(!out)throw std::runtime_error("failed to open native Qwen KV dump for writing");
    const std::int32_t header[2]={count,static_cast<std::int32_t>(head_dim)};
    out.write(reinterpret_cast<const char*>(header),sizeof(header));
    out.write(reinterpret_cast<const char*>(keys.data()),
              static_cast<std::streamsize>(keys.size()*sizeof(float)));
    out.write(reinterpret_cast<const char*>(values.data()),
              static_cast<std::streamsize>(values.size()*sizeof(float)));
    if(!out)throw std::runtime_error("failed to write native Qwen KV dump");
    return 0;
});}

// Defined below gemma4_decode; the rows driver in v2_mtp_verifier.inc
// dispatches here for Gemma 4 chunks.
void gemma4_prefill_rows(
    FlyweightV2QwenRuntime& runtime, const std::uint32_t* tokens, int rows);

#include "v2_mtp_verifier.inc"

// Layer-synchronous chunked prefill. Attention stays a per-row pass with the
// decode kernels -- a row's attention at layer L needs only earlier rows' KV
// at L, which the in-order pass has already stored -- while the expert FFN
// batches across the whole chunk on the CPU, where gathering rows by expert
// is what turns 793 MB of expert reads per token into one read per chunk.
// No logits and no argmax: the caller keeps the prompt tail on the one-token
// path, exactly like the Qwen rows forward.
void gemma4_prefill_rows(
    FlyweightV2QwenRuntime& runtime, const std::uint32_t* tokens, int rows) {
    const auto started=std::chrono::steady_clock::now();
    if(rows<1||rows>static_cast<int>(runtime.forward_rows_capacity))
        throw std::runtime_error("native Gemma 4 prefill chunk exceeds the rows capacity");
    const int hidden_size=static_cast<int>(runtime.model->config.hidden_size);
    const int experts=static_cast<int>(runtime.model->config.expert_count);
    const int top_k=static_cast<int>(runtime.model->config.expert_used_count);
    const int dense_intermediate=static_cast<int>(runtime.model->config.dense_intermediate_size);
    const float epsilon=runtime.model->config.rms_norm_epsilon
        ?runtime.model->config.rms_norm_epsilon:1.0e-6f;
    const auto&rows_layout=runtime.rows_workspace_layout;
    if(rows_layout.bytes>runtime.workspace_bytes)
        throw std::runtime_error("native Gemma 4 rows workspace is too small");
    const std::uint64_t hidden_stride=static_cast<std::uint64_t>(hidden_size)*sizeof(float);
    const std::uint64_t scratch_stride=static_cast<std::uint64_t>(runtime.scratch_elements)*sizeof(float);
    const std::uint64_t hidden_rows=rows_layout.hidden.address(runtime.workspace);
    const std::uint64_t residual_rows=rows_layout.residual.address(runtime.workspace);
    const std::uint64_t normalized_rows=rows_layout.normalized.address(runtime.workspace);
    const std::uint64_t first_rows=rows_layout.first.address(runtime.workspace);
    const std::uint64_t second_rows=rows_layout.second.address(runtime.workspace);
    const std::uint64_t third_rows=rows_layout.third.address(runtime.workspace);
    const std::uint64_t router_rows=rows_layout.router_logits.address(runtime.workspace);
    const std::uint64_t selected_rows=rows_layout.selected_device.address(runtime.workspace);
    const std::uint64_t weight_rows=rows_layout.route_weights.address(runtime.workspace);
    const std::uint64_t attention_scores=rows_layout.attention_scores.address(runtime.workspace);
    auto*staging=static_cast<std::uint8_t*>(runtime.host_staging);
    const auto&host_layout=runtime.rows_host_layout;
    auto*host_selected=reinterpret_cast<std::int32_t*>(staging+host_layout.selected.offset);
    auto*host_weights=reinterpret_cast<float*>(staging+host_layout.weights.offset);
    auto*host_input=reinterpret_cast<float*>(staging+host_layout.input.offset);
    auto*host_output=reinterpret_cast<float*>(staging+host_layout.output.offset);
    // FLYWEIGHT_KERNEL_PROFILE=1 brackets every rows launch with CUDA events,
    // same mechanism as the Qwen decode profiler: relative split only, since
    // the events serialize the stream. Reported once per chunk.
    static const bool kernel_profile=
        std::getenv("FLYWEIGHT_KERNEL_PROFILE")&&
        std::getenv("FLYWEIGHT_KERNEL_PROFILE")[0]=='1';
    struct RowsProfileEntry{double milliseconds=0.0;std::uint64_t calls=0;};
    static std::map<std::string,RowsProfileEntry> rows_profile_totals;
    static constexpr std::size_t kRowsProfileCapacity=49152;
    static std::vector<std::uint64_t> rows_profile_events;
    static std::vector<const char*> rows_profile_labels;
    static std::size_t rows_profile_used=0;
    if(kernel_profile&&rows_profile_events.empty()){
        rows_profile_events.resize(2*kRowsProfileCapacity);
        rows_profile_labels.resize(kRowsProfileCapacity);
        for(auto&event:rows_profile_events)
            flyweight_gpu_timed_event_create(&event);
    }
    auto launch=[&](const char* name,std::uint32_t grid_x,std::uint32_t grid_y,
                    std::uint32_t block_x,void** arguments,std::uint32_t shared=0){
        const bool traced=kernel_profile&&rows_profile_used<kRowsProfileCapacity;
        const std::size_t slot=rows_profile_used;
        if(traced)flyweight_gpu_event_record(rows_profile_events[2*slot],runtime.stream);
        if(flyweight_gpu_launch_named(name,grid_x,grid_y,block_x,shared,runtime.stream,arguments)!=0)
            throw std::runtime_error(std::string("native Gemma 4 CUDA kernel failed: ")+name);
        if(traced){
            flyweight_gpu_event_record(rows_profile_events[2*slot+1],runtime.stream);
            rows_profile_labels[slot]=name;
            ++rows_profile_used;
        }
    };
    auto rms=[&](std::uint64_t input,std::uint64_t weights,std::uint64_t output){
        int one_centered=0;
        void* args[]={&input,&weights,&output,const_cast<int*>(&hidden_size),
                      const_cast<float*>(&epsilon),&one_centered};
        launch("rms_norm",1,1,256,args);
    };
    auto q4=[&](std::uint64_t matrix,std::uint64_t input,std::uint64_t output,
                int input_size,int output_size){
        void* args[]={&matrix,&input,&output,&input_size,&output_size};
        launch("gemma_q4_0_matvec",(output_size+7)/8,1,256,args);
    };
    auto f32=[&](std::uint64_t matrix,std::uint64_t input,std::uint64_t output,
                 int input_size,int output_size){
        void* args[]={&matrix,&input,&output,&input_size,&output_size};
        launch("qwen_f32_matvec_warp",(output_size+7)/8,1,256,args);
    };
    auto add=[&](std::uint64_t target,std::uint64_t source){
        float scale=1.0f;int count=hidden_size;
        void* args[]={&target,&source,&scale,&count};
        launch("scaled_add",(hidden_size+255)/256,1,256,args);
    };
    {
        const auto embedding=runtime.device_tensors[runtime.token_embeddings];
        const float scale=std::sqrt(static_cast<float>(hidden_size));
        for(int row=0;row<rows;++row){
            if(tokens[row]>=runtime.model->config.vocabulary_size)
                throw std::runtime_error("native Gemma 4 prompt token is out of range");
            const std::uint64_t hidden_r=hidden_rows+row*hidden_stride;
            const int token=static_cast<int>(tokens[row]);
            void* args[]={const_cast<std::uint64_t*>(&embedding),
                          const_cast<std::uint64_t*>(&hidden_r),
                          const_cast<int*>(&token),const_cast<int*>(&hidden_size),
                          const_cast<float*>(&scale)};
            launch("gemma_q4_0_embedding",(hidden_size+255)/256,1,256,args);
        }
    }
    const std::uint64_t base_position=runtime.position;
    // Rows-batched kernels collapse the ~27 per-row launches per layer into
    // ~16 per-layer ones (the attention core stays per-row: a row's scores
    // must see exactly the stores of earlier rows). Off-switch for A/B.
    static const bool env_rows_kernels=[]{
        const char*s=std::getenv("FLYWEIGHT_GEMMA_ROWS_KERNELS");return !s||s[0]!='0';}();
    const long long h_elems=hidden_size;
    const long long s_elems=static_cast<long long>(runtime.scratch_elements);
    const std::uint64_t fourth_rows=rows_layout.fourth.address(runtime.workspace);
    auto rms_rows=[&](std::uint64_t inputs,std::uint64_t weights,std::uint64_t outputs,
                      long long in_stride,long long out_stride){
        void* args[]={&inputs,&weights,&outputs,const_cast<int*>(&hidden_size),
                      const_cast<int*>(&rows),&in_stride,&out_stride,
                      const_cast<float*>(&epsilon)};
        launch("gemma_rms_rows",rows,1,256,args);
    };
    auto add_rows=[&](std::uint64_t targets,std::uint64_t sources,
                      long long t_stride,long long s_stride){
        void* args[]={&targets,&sources,const_cast<int*>(&hidden_size),
                      const_cast<int*>(&rows),&t_stride,&s_stride};
        launch("gemma_scaled_add_rows",(hidden_size+255)/256,rows,256,args);
    };
    auto q4_rows=[&](std::uint64_t matrix,std::uint64_t inputs,std::uint64_t outputs,
                     int input_size,int output_size,long long in_stride,long long out_stride){
        void* args[]={&matrix,&inputs,&outputs,&input_size,&output_size,
                      const_cast<int*>(&rows),&in_stride,&out_stride};
        launch("gemma_q4_0_matvec_rows",(output_size+7)/8,rows,256,args);
    };
    // DP4A GEMMs over Q8 activations for the dense projections: one weight
    // pass serves 8 tokens instead of one. Changes the numerics (activations
    // quantize to int8, like llama.cpp's default MMQ prefill), hence its own
    // off-switch. Requires the scratch regions to carry the widest GEMM input.
    static const bool env_mmq=[]{
        const char*s=std::getenv("FLYWEIGHT_GEMMA_MMQ");return !s||s[0]!='0';}();
    const std::uint64_t q8_rows_base=rows_layout.rows_q8.address(runtime.workspace);
    const std::uint64_t q8_scales_base=rows_layout.rows_q8_scales.address(runtime.workspace);
    const bool use_mmq=env_rows_kernels&&env_mmq&&hidden_size%32==0&&
        static_cast<std::uint64_t>(hidden_size)<=runtime.scratch_elements;
    auto quantize_rows=[&](std::uint64_t input,long long in_stride,int elements){
        void* args[]={&input,&in_stride,
                      const_cast<std::uint64_t*>(&q8_rows_base),
                      const_cast<std::uint64_t*>(&q8_scales_base),
                      &elements,const_cast<int*>(&rows)};
        launch("gemma_quantize_q8_rows",(elements+31)/32,rows,32,args);
    };
    auto mmq_rows=[&](std::uint64_t matrix,std::uint64_t outputs,
                      int input_size,int output_size,long long out_stride){
        void* args[]={&matrix,
                      const_cast<std::uint64_t*>(&q8_rows_base),
                      const_cast<std::uint64_t*>(&q8_scales_base),
                      &outputs,&input_size,&output_size,
                      const_cast<int*>(&rows),&out_stride};
        launch("gemma_q4_0_q8_mmq_rows",(output_size+31)/32,
               (static_cast<std::uint32_t>(rows)+7)/8,256,args);
    };
    for(std::uint32_t layer_number=0;layer_number<runtime.layers.size();++layer_number){
        auto& layer=runtime.layers[layer_number];
        auto tensor=[&](std::size_t role){return runtime.device_tensors[layer.static_tensors.at(role)];};
        const int heads=static_cast<int>(layer.attention_heads);
        const int kv_heads=static_cast<int>(layer.kv_heads);
        const int head_dim=static_cast<int>(layer.head_dim);
        const int q_elements=heads*head_dim,kv_elements=kv_heads*head_dim;
        const int rotary=static_cast<int>(layer.rotary_dim);
        const float theta=layer.rope_theta;
        const std::uint64_t rope_factors=layer.attention_window||
            runtime.rope_factors==std::numeric_limits<std::uint64_t>::max()
            ?0:runtime.device_tensors[runtime.rope_factors];
        if(env_rows_kernels){
            const int base_position_int=static_cast<int>(base_position);
            // K dims must divide 32 for the DP4A tile loop; output dims are
            // bounds-guarded and free.
            const bool layer_mmq=use_mmq&&q_elements%32==0&&
                static_cast<std::uint64_t>(q_elements)<=runtime.scratch_elements&&
                dense_intermediate%32==0;
            rms_rows(hidden_rows,tensor(0),normalized_rows,h_elems,h_elems);
            if(layer_mmq){
                quantize_rows(normalized_rows,h_elems,hidden_size);
                mmq_rows(tensor(1),first_rows,hidden_size,q_elements,s_elems);
                mmq_rows(tensor(2),second_rows,hidden_size,kv_elements,s_elems);
                mmq_rows(tensor(3),third_rows,hidden_size,kv_elements,s_elems);
            }else{
                q4_rows(tensor(1),normalized_rows,first_rows,hidden_size,q_elements,h_elems,s_elems);
                q4_rows(tensor(2),normalized_rows,second_rows,hidden_size,kv_elements,h_elems,s_elems);
                q4_rows(tensor(3),normalized_rows,third_rows,hidden_size,kv_elements,h_elems,s_elems);
            }
            // Fused chunk attention for global layers: queries and attended
            // rows sit on a compact heads*head_dim stride (that is the layout
            // the kv prefill kernel assumes), and one launch replaces the
            // per-row scores/values pairs. Window layers keep the per-row ring
            // path, whose cost the window bounds.
            static const bool env_prefill_attention=[]{
                const char*s=std::getenv("FLYWEIGHT_GEMMA_PREFILL_ATTENTION");
                return !s||s[0]!='0';}();
            // Gemma 4 global layers run head_dim 512, past the shared fused
            // kernel's register budget; the wide twin covers them when the
            // cache is f16 (the default).
            const bool layer_wide_attention=head_dim>256&&head_dim<=512&&
                runtime.options.cache_type_k==1&&runtime.options.cache_type_v==1;
            const bool layer_fused_attention=env_prefill_attention&&
                !layer.attention_window&&head_dim%32==0&&
                (head_dim<=256||layer_wide_attention)&&
                kv_fused_prefill_ok(runtime);
            const long long a_elems=q_elements;
            auto qnorm=tensor(5),knorm=tensor(6);
            void* q_args[]={const_cast<std::uint64_t*>(&first_rows),&qnorm,
                            const_cast<std::uint64_t*>(&fourth_rows),const_cast<int*>(&heads),
                            const_cast<int*>(&head_dim),const_cast<int*>(&rotary),
                            const_cast<int*>(&base_position_int),const_cast<float*>(&theta),
                            const_cast<float*>(&epsilon),const_cast<std::uint64_t*>(&rope_factors),
                            const_cast<int*>(&rows),const_cast<long long*>(&s_elems),
                            const_cast<long long*>(&a_elems)};
            launch("gemma_head_norm_rope_rows",heads,rows,256,q_args);
            void* k_args[]={const_cast<std::uint64_t*>(&second_rows),&knorm,
                            const_cast<std::uint64_t*>(&first_rows),const_cast<int*>(&kv_heads),
                            const_cast<int*>(&head_dim),const_cast<int*>(&rotary),
                            const_cast<int*>(&base_position_int),const_cast<float*>(&theta),
                            const_cast<float*>(&epsilon),const_cast<std::uint64_t*>(&rope_factors),
                            const_cast<int*>(&rows),const_cast<long long*>(&s_elems),
                            const_cast<long long*>(&s_elems)};
            launch("gemma_head_norm_rope_rows",kv_heads,rows,256,k_args);
            void* v_args[]={const_cast<std::uint64_t*>(&third_rows),
                            const_cast<std::uint64_t*>(&second_rows),const_cast<int*>(&kv_heads),
                            const_cast<int*>(&head_dim),const_cast<float*>(&epsilon),
                            const_cast<int*>(&rows),const_cast<long long*>(&s_elems),
                            const_cast<long long*>(&s_elems)};
            launch("gemma_head_rms_rows",kv_heads,rows,256,v_args);
            std::uint64_t cache_keys=runtime.state+layer.state_first;
            std::uint64_t cache_values=runtime.state+layer.state_second;
            for(int row=0;row<rows;++row){
                const std::uint64_t first_r=first_rows+row*scratch_stride;
                const std::uint64_t second_r=second_rows+row*scratch_stride;
                const std::uint64_t third_r=third_rows+row*a_elems*sizeof(float);
                const std::uint64_t fourth_r=fourth_rows+row*a_elems*sizeof(float);
                const int position=static_cast<int>(base_position)+row;
                const auto view=attention_cache_view(layer,position);
                int slot=view.slot,capacity=view.capacity;
                void* k_store_args[]={const_cast<std::uint64_t*>(&first_r),&cache_keys,
                                      const_cast<int*>(&kv_heads),const_cast<int*>(&head_dim),
                                      &slot,&capacity};
                launch(kv_store_kernel(runtime.options.cache_type_k,true),kv_heads,1,256,k_store_args);
                void* v_store_args[]={const_cast<std::uint64_t*>(&second_r),&cache_values,
                                      const_cast<int*>(&kv_heads),const_cast<int*>(&head_dim),
                                      &slot,&capacity};
                launch(kv_store_kernel(runtime.options.cache_type_v,false),kv_heads,1,256,v_store_args);
                if(layer_fused_attention)continue;
                int visible=view.tokens,first_slot=view.first;float attention_scale=1.0f;
                void* score_args[]={const_cast<std::uint64_t*>(&fourth_r),&cache_keys,
                                    const_cast<std::uint64_t*>(&attention_scores),
                                    const_cast<int*>(&heads),const_cast<int*>(&kv_heads),
                                    const_cast<int*>(&head_dim),&visible,&capacity,&first_slot,
                                    &attention_scale};
                launch(kv_scores_ring_kernel(runtime),heads,(visible+255)/256,256,score_args);
                void* value_args[]={const_cast<std::uint64_t*>(&attention_scores),&cache_values,
                                    const_cast<std::uint64_t*>(&third_r),const_cast<int*>(&heads),
                                    const_cast<int*>(&kv_heads),const_cast<int*>(&head_dim),
                                    &visible,&capacity,&first_slot};
                launch(kv_values_ring_kernel(runtime),heads,1,256,value_args);
            }
            if(layer_fused_attention){
                // Every row's KV is stored above; the kernel masks causally,
                // so row r still attends to exactly positions 0..base+r.
                const auto view=attention_cache_view(layer,base_position);
                int capacity=view.capacity;float attention_scale=1.0f;
                void* attention_args[]={const_cast<std::uint64_t*>(&fourth_rows),
                                        &cache_keys,&cache_values,
                                        const_cast<std::uint64_t*>(&third_rows),
                                        const_cast<int*>(&heads),
                                        const_cast<int*>(&kv_heads),
                                        const_cast<int*>(&head_dim),
                                        const_cast<int*>(&base_position_int),
                                        const_cast<int*>(&rows),&capacity,
                                        &attention_scale};
                if(layer_wide_attention)
                    launch("gemma_kv_prefill_wide_f16",heads,
                           (static_cast<std::uint32_t>(rows)+15)/16,256,attention_args);
                else
                    launch(kv_prefill_kernel(runtime),heads,
                           (static_cast<std::uint32_t>(rows)+31)/32,256,attention_args);
            }
            if(layer_mmq){
                quantize_rows(third_rows,a_elems,q_elements);
                mmq_rows(tensor(4),residual_rows,q_elements,hidden_size,h_elems);
            }else{
                q4_rows(tensor(4),third_rows,residual_rows,q_elements,hidden_size,a_elems,h_elems);
            }
            rms_rows(residual_rows,tensor(7),normalized_rows,h_elems,h_elems);
            add_rows(hidden_rows,normalized_rows,h_elems,h_elems);
            rms_rows(hidden_rows,tensor(8),normalized_rows,h_elems,h_elems);
            auto dense_gate=tensor(9),dense_up=tensor(10);
            if(layer_mmq){
                // fourth (Q) and second (V projection) are both consumed by
                // the attention loop above, so they carry the gate and up
                // halves until the combine writes first.
                quantize_rows(normalized_rows,h_elems,hidden_size);
                mmq_rows(dense_gate,fourth_rows,hidden_size,dense_intermediate,s_elems);
                mmq_rows(dense_up,second_rows,hidden_size,dense_intermediate,s_elems);
                void* combine_args[]={const_cast<std::uint64_t*>(&fourth_rows),
                                      const_cast<std::uint64_t*>(&second_rows),
                                      const_cast<std::uint64_t*>(&first_rows),
                                      const_cast<int*>(&dense_intermediate),
                                      const_cast<int*>(&rows),
                                      const_cast<long long*>(&s_elems),
                                      const_cast<long long*>(&s_elems),
                                      const_cast<long long*>(&s_elems)};
                launch("gemma_geglu_combine_rows",(dense_intermediate+255)/256,rows,256,combine_args);
                quantize_rows(first_rows,s_elems,dense_intermediate);
                mmq_rows(tensor(11),second_rows,dense_intermediate,hidden_size,s_elems);
            }else{
                void* dense_args[]={&dense_gate,&dense_up,
                                    const_cast<std::uint64_t*>(&normalized_rows),
                                    const_cast<std::uint64_t*>(&first_rows),
                                    const_cast<int*>(&hidden_size),
                                    const_cast<int*>(&dense_intermediate),
                                    const_cast<int*>(&rows),
                                    const_cast<long long*>(&h_elems),
                                    const_cast<long long*>(&s_elems)};
                launch("gemma_q4_0_geglu_rows",(dense_intermediate+7)/8,rows,256,dense_args);
                q4_rows(tensor(11),first_rows,second_rows,dense_intermediate,hidden_size,s_elems,s_elems);
            }
            rms_rows(second_rows,tensor(12),third_rows,s_elems,s_elems);
            auto router_scale=tensor(13);
            void* router_args[]={const_cast<std::uint64_t*>(&hidden_rows),&router_scale,
                                 const_cast<std::uint64_t*>(&normalized_rows),
                                 const_cast<int*>(&hidden_size),
                                 const_cast<float*>(&epsilon),
                                 const_cast<int*>(&rows),
                                 const_cast<long long*>(&h_elems),
                                 const_cast<long long*>(&h_elems)};
            launch("gemma_router_input_rows",rows,1,256,router_args);
            const long long experts_stride=experts;
            auto router_matrix=tensor(14);
            void* logits_args[]={&router_matrix,
                                 const_cast<std::uint64_t*>(&normalized_rows),
                                 const_cast<std::uint64_t*>(&router_rows),
                                 const_cast<int*>(&hidden_size),
                                 const_cast<int*>(&experts),
                                 const_cast<int*>(&rows),
                                 const_cast<long long*>(&h_elems),
                                 const_cast<long long*>(&experts_stride)};
            launch("gemma_f32_matvec_rows",(experts+7)/8,rows,256,logits_args);
            void* topk_args[]={const_cast<std::uint64_t*>(&router_rows),
                               const_cast<std::uint64_t*>(&selected_rows),
                               const_cast<std::uint64_t*>(&weight_rows),
                               const_cast<int*>(&rows),
                               const_cast<int*>(&experts),
                               const_cast<int*>(&top_k)};
            launch("route_topk_rows",rows,1,256,topk_args,
                   static_cast<std::uint32_t>(experts*sizeof(float)));
            rms_rows(hidden_rows,tensor(15),normalized_rows,h_elems,h_elems);
            if(flyweight_gpu_download(host_selected,selected_rows,
                                    static_cast<std::uint64_t>(rows)*top_k*sizeof(std::int32_t),
                                    runtime.stream)!=0||
               flyweight_gpu_download(host_weights,weight_rows,
                                    static_cast<std::uint64_t>(rows)*top_k*sizeof(float),
                                    runtime.stream)!=0||
               flyweight_gpu_download(host_input,normalized_rows,
                                    static_cast<std::uint64_t>(rows)*hidden_size*sizeof(float),
                                    runtime.stream)!=0)
                throw std::runtime_error("native Gemma 4 rows MoE input transfer failed");
        }else{
        for(int row=0;row<rows;++row){
            const std::uint64_t hidden_r=hidden_rows+row*hidden_stride;
            const std::uint64_t residual_r=residual_rows+row*hidden_stride;
            const std::uint64_t normalized_r=normalized_rows+row*hidden_stride;
            const std::uint64_t first_r=first_rows+row*scratch_stride;
            const std::uint64_t second_r=second_rows+row*scratch_stride;
            const std::uint64_t third_r=third_rows+row*scratch_stride;
            const std::uint64_t fourth_r=rows_layout.fourth.address(runtime.workspace)+row*scratch_stride;
            const int position=static_cast<int>(base_position)+row;
            rms(hidden_r,tensor(0),normalized_r);
            q4(tensor(1),normalized_r,first_r,hidden_size,q_elements);
            q4(tensor(2),normalized_r,second_r,hidden_size,kv_elements);
            q4(tensor(3),normalized_r,third_r,hidden_size,kv_elements);
            auto qnorm=tensor(5),knorm=tensor(6);
            void* q_args[]={const_cast<std::uint64_t*>(&first_r),&qnorm,
                            const_cast<std::uint64_t*>(&fourth_r),const_cast<int*>(&heads),
                            const_cast<int*>(&head_dim),const_cast<int*>(&rotary),
                            const_cast<int*>(&position),const_cast<float*>(&theta),
                            const_cast<float*>(&epsilon),const_cast<std::uint64_t*>(&rope_factors)};
            launch("gemma_head_norm_rope",heads,1,256,q_args);
            void* k_args[]={const_cast<std::uint64_t*>(&second_r),&knorm,
                            const_cast<std::uint64_t*>(&first_r),const_cast<int*>(&kv_heads),
                            const_cast<int*>(&head_dim),const_cast<int*>(&rotary),
                            const_cast<int*>(&position),const_cast<float*>(&theta),
                            const_cast<float*>(&epsilon),const_cast<std::uint64_t*>(&rope_factors)};
            launch("gemma_head_norm_rope",kv_heads,1,256,k_args);
            void* v_norm_args[]={const_cast<std::uint64_t*>(&third_r),
                                 const_cast<std::uint64_t*>(&second_r),const_cast<int*>(&kv_heads),
                                 const_cast<int*>(&head_dim),const_cast<float*>(&epsilon)};
            launch("gemma_head_rms",kv_heads,1,256,v_norm_args);
            std::uint64_t cache_keys=runtime.state+layer.state_first;
            std::uint64_t cache_values=runtime.state+layer.state_second;
            const auto view=attention_cache_view(layer,position);
            int slot=view.slot,capacity=view.capacity;
            void* k_store_args[]={const_cast<std::uint64_t*>(&first_r),&cache_keys,
                                  const_cast<int*>(&kv_heads),const_cast<int*>(&head_dim),
                                  &slot,&capacity};
            launch(kv_store_kernel(runtime.options.cache_type_k,true),kv_heads,1,256,k_store_args);
            void* v_store_args[]={const_cast<std::uint64_t*>(&second_r),&cache_values,
                                  const_cast<int*>(&kv_heads),const_cast<int*>(&head_dim),
                                  &slot,&capacity};
            launch(kv_store_kernel(runtime.options.cache_type_v,false),kv_heads,1,256,v_store_args);
            int visible=view.tokens,first_slot=view.first;float attention_scale=1.0f;
            void* score_args[]={const_cast<std::uint64_t*>(&fourth_r),&cache_keys,
                                const_cast<std::uint64_t*>(&attention_scores),
                                const_cast<int*>(&heads),const_cast<int*>(&kv_heads),
                                const_cast<int*>(&head_dim),&visible,&capacity,&first_slot,
                                &attention_scale};
            launch(kv_scores_ring_kernel(runtime),heads,(visible+255)/256,256,score_args);
            void* value_args[]={const_cast<std::uint64_t*>(&attention_scores),&cache_values,
                                const_cast<std::uint64_t*>(&third_r),const_cast<int*>(&heads),
                                const_cast<int*>(&kv_heads),const_cast<int*>(&head_dim),
                                &visible,&capacity,&first_slot};
            launch(kv_values_ring_kernel(runtime),heads,1,256,value_args);
            q4(tensor(4),third_r,residual_r,q_elements,hidden_size);
            rms(residual_r,tensor(7),normalized_r);
            add(hidden_r,normalized_r);
            rms(hidden_r,tensor(8),normalized_r);
            auto dense_gate=tensor(9),dense_up=tensor(10);
            void* dense_args[]={&dense_gate,&dense_up,const_cast<std::uint64_t*>(&normalized_r),
                                const_cast<std::uint64_t*>(&first_r),const_cast<int*>(&hidden_size),
                                const_cast<int*>(&dense_intermediate)};
            launch("gemma_q4_0_geglu",(dense_intermediate+7)/8,1,256,dense_args);
            q4(tensor(11),first_r,second_r,dense_intermediate,hidden_size);
            rms(second_r,tensor(12),third_r);
            auto router_scale=tensor(13);
            void* router_input_args[]={const_cast<std::uint64_t*>(&hidden_r),&router_scale,
                                       const_cast<std::uint64_t*>(&normalized_r),
                                       const_cast<int*>(&hidden_size),
                                       const_cast<float*>(&epsilon)};
            launch("gemma_router_input",1,1,256,router_input_args);
            const std::uint64_t router_r=router_rows+static_cast<std::uint64_t>(row)*experts*sizeof(float);
            const std::uint64_t selected_r=selected_rows+static_cast<std::uint64_t>(row)*top_k*sizeof(std::int32_t);
            const std::uint64_t weights_r=weight_rows+static_cast<std::uint64_t>(row)*top_k*sizeof(float);
            f32(tensor(14),normalized_r,router_r,hidden_size,experts);
            if(flyweight_gpu_route_topk(router_r,selected_r,weights_r,
                                     experts,top_k,runtime.stream)!=0)
                throw std::runtime_error("native Gemma 4 routing failed");
            rms(hidden_r,tensor(15),normalized_r);
            if(flyweight_gpu_download(host_selected+static_cast<std::size_t>(row)*top_k,
                                    selected_r,top_k*sizeof(std::int32_t),runtime.stream)!=0||
               flyweight_gpu_download(host_weights+static_cast<std::size_t>(row)*top_k,
                                    weights_r,top_k*sizeof(float),runtime.stream)!=0||
               flyweight_gpu_download(host_input+static_cast<std::size_t>(row)*hidden_size,
                                    normalized_r,hidden_size*sizeof(float),runtime.stream)!=0)
                throw std::runtime_error("native Gemma 4 rows MoE input transfer failed");
        }
        }
        const auto wait_started=std::chrono::steady_clock::now();
        if(flyweight_gpu_event_record(runtime.route_event,runtime.stream)!=0||
           flyweight_gpu_event_sync(runtime.route_event)!=0)
            throw std::runtime_error("native Gemma 4 rows route synchronization failed");
        runtime.route_wait_nanoseconds+=std::chrono::duration_cast<
            std::chrono::nanoseconds>(std::chrono::steady_clock::now()-wait_started).count();
        runtime.route_expert_sum+=static_cast<std::uint64_t>(rows)*top_k;
        const auto expert_started=std::chrono::steady_clock::now();
        gemma_cpu_moe_rows(runtime,layer,rows,host_selected,host_weights,
                           host_input,host_output);
        runtime.expert_compute_nanoseconds+=std::chrono::duration_cast<
            std::chrono::nanoseconds>(std::chrono::steady_clock::now()-expert_started).count();
        // One contiguous upload for the whole chunk; residual is free until
        // the combine below rewrites it row by row.
        if(flyweight_gpu_upload(residual_rows,host_output,
                              static_cast<std::uint64_t>(rows)*hidden_size*sizeof(float),
                              runtime.stream)!=0)
            throw std::runtime_error("native Gemma 4 rows MoE output upload failed");
        if(env_rows_kernels){
            rms_rows(residual_rows,tensor(16),first_rows,h_elems,s_elems);
            add_rows(third_rows,first_rows,s_elems,s_elems);
            rms_rows(third_rows,tensor(17),normalized_rows,s_elems,h_elems);
            add_rows(hidden_rows,normalized_rows,h_elems,h_elems);
            auto layer_scale=tensor(18);
            void* scale_args[]={const_cast<std::uint64_t*>(&hidden_rows),&layer_scale,
                                const_cast<int*>(&hidden_size),const_cast<int*>(&rows),
                                const_cast<long long*>(&h_elems)};
            launch("gemma_scale_vector_rows",(hidden_size+255)/256,rows,256,scale_args);
        }else for(int row=0;row<rows;++row){
            const std::uint64_t hidden_r=hidden_rows+row*hidden_stride;
            const std::uint64_t residual_r=residual_rows+row*hidden_stride;
            const std::uint64_t normalized_r=normalized_rows+row*hidden_stride;
            const std::uint64_t first_r=first_rows+row*scratch_stride;
            const std::uint64_t third_r=third_rows+row*scratch_stride;
            rms(residual_r,tensor(16),first_r);
            add(third_r,first_r);
            rms(third_r,tensor(17),normalized_r);
            add(hidden_r,normalized_r);
            auto layer_scale=tensor(18);
            void* scale_args[]={const_cast<std::uint64_t*>(&hidden_r),&layer_scale,
                                const_cast<int*>(&hidden_size)};
            launch("gemma_scale_vector",(hidden_size+255)/256,1,256,scale_args);
        }
    }
    if(flyweight_gpu_stream_sync(runtime.stream)!=0)
        throw std::runtime_error("native Gemma 4 rows synchronization failed");
    if(kernel_profile){
        for(std::size_t slot=0;slot<rows_profile_used;++slot){
            float milliseconds=0.0f;
            if(flyweight_gpu_event_elapsed(rows_profile_events[2*slot],
                                         rows_profile_events[2*slot+1],
                                         &milliseconds)!=0)continue;
            auto&entry=rows_profile_totals[rows_profile_labels[slot]];
            entry.milliseconds+=milliseconds;++entry.calls;
        }
        const std::size_t untraced=rows_profile_used>=kRowsProfileCapacity
            ?rows_profile_used-kRowsProfileCapacity:0;
        rows_profile_used=0;
        double total=0.0;
        for(const auto&entry:rows_profile_totals)total+=entry.second.milliseconds;
        std::vector<std::pair<std::string,RowsProfileEntry>> sorted(
            rows_profile_totals.begin(),rows_profile_totals.end());
        std::sort(sorted.begin(),sorted.end(),[](const auto&a,const auto&b){
            return a.second.milliseconds>b.second.milliseconds;});
        std::fprintf(stderr,
            "[rows-profile] chunk of %d rows at position %llu: %.2f ms/token "
            "kernel time (untraced=%zu)\n",rows,
            static_cast<unsigned long long>(base_position),
            total/rows,untraced);
        for(const auto&entry:sorted)
            std::fprintf(stderr,
                "[rows-profile]   %-40s %7.3f ms/tok %5.1f%% %7.1f calls/chunk\n",
                entry.first.c_str(),entry.second.milliseconds/rows,
                100.0*entry.second.milliseconds/total,
                static_cast<double>(entry.second.calls));
        rows_profile_totals.clear();
    }
    runtime.processed_tokens.insert(runtime.processed_tokens.end(),tokens,tokens+rows);
    runtime.position+=static_cast<std::uint64_t>(rows);
    runtime.decode_calls+=static_cast<std::uint64_t>(rows);
    runtime.decode_nanoseconds+=std::chrono::duration_cast<std::chrono::nanoseconds>(std::chrono::steady_clock::now()-started).count();
    ++runtime.prefill_calls;
    runtime.prefill_tokens+=static_cast<std::uint64_t>(rows);
    runtime.prefill_nanoseconds+=std::chrono::duration_cast<std::chrono::nanoseconds>(
        std::chrono::steady_clock::now()-started).count();
}

static int gemma4_decode(FlyweightV2QwenRuntime& runtime, std::uint32_t input_token,
                         std::uint32_t& output_token) {
    const auto decode_started=std::chrono::steady_clock::now();
    const auto expert_policy=qwen_expert_policy(
        runtime,flyweight::v2::ExpertExecutionPhase::decode);
    const int hidden_size=static_cast<int>(runtime.model->config.hidden_size);
    const int experts=static_cast<int>(runtime.model->config.expert_count);
    const int top_k=static_cast<int>(runtime.model->config.expert_used_count);
    const int dense_intermediate=static_cast<int>(runtime.model->config.dense_intermediate_size);
    const float epsilon=runtime.model->config.rms_norm_epsilon
        ?runtime.model->config.rms_norm_epsilon:1.0e-6f;
    // The canonical decode layout carves the same regions this used to take()
    // ad hoc, from the same configuration values -- and it is what the sampler
    // reads: its logits buffer and top-k scratch must not overlap the buffers
    // holding the final normalized hidden state this decode leaves behind.
    const auto&workspace_layout=runtime.decode_workspace_layout;
    if(workspace_layout.bytes>runtime.workspace_bytes)
        throw std::runtime_error("native Gemma 4 workspace is too small");
    std::uint64_t hidden=workspace_layout.hidden.address(runtime.workspace);
    std::uint64_t residual=workspace_layout.residual.address(runtime.workspace);
    const std::uint64_t normalized=workspace_layout.normalized.address(runtime.workspace);
    const std::uint64_t first=workspace_layout.first.address(runtime.workspace);
    const std::uint64_t second=workspace_layout.second.address(runtime.workspace);
    const std::uint64_t third=workspace_layout.third.address(runtime.workspace);
    const std::uint64_t fourth=workspace_layout.fourth.address(runtime.workspace);
    const std::uint64_t activated=workspace_layout.activated.address(runtime.workspace);
    const std::uint64_t router_logits=workspace_layout.router_logits.address(runtime.workspace);
    const std::uint64_t selected_device=workspace_layout.selected_device.address(runtime.workspace);
    const std::uint64_t route_weights=workspace_layout.route_weights.address(runtime.workspace);
    const std::uint64_t argmax_device=workspace_layout.argmax_device.address(runtime.workspace);
    const std::uint64_t attention_scores=workspace_layout.attention_scores.address(runtime.workspace);
    auto* staging=static_cast<std::uint8_t*>(runtime.host_staging);
    auto* selected_host=reinterpret_cast<std::int32_t*>(staging);
    const auto weights_offset=device_align(top_k*sizeof(std::int32_t));
    const auto input_offset=weights_offset+device_align(top_k*sizeof(float));
    const auto activated_offset=input_offset+device_align(hidden_size*sizeof(float));
    const auto output_offset=activated_offset+
        device_align(static_cast<std::uint64_t>(top_k)*runtime.moe_intermediate*sizeof(float));
    auto* cpu_weights=reinterpret_cast<float*>(staging+weights_offset);
    auto* cpu_input=reinterpret_cast<float*>(staging+input_offset);
    auto* cpu_activated=reinterpret_cast<float*>(staging+activated_offset);
    auto* cpu_output=reinterpret_cast<float*>(staging+output_offset);
    if(output_offset+hidden_size*sizeof(float)>runtime.host_staging_bytes)
        throw std::runtime_error("native Gemma 4 CPU MoE workspace overflow");
    // Kernels enqueue on launch_stream so a CUDA-graph capture can redirect
    // the same lambdas onto the capture stream; outside capture it is always
    // runtime.stream.
    std::uint64_t launch_stream=runtime.stream;
    // Opt-in: capturing the pinned FFN chain measured 2% SLOWER than eager
    // launches on Linux (56.3 vs 57.6 tok/s, 3x interleaved A/B) -- decode
    // submission overhead is already hidden behind the serial CPU-expert
    // layers. Kept for WDDM, where per-launch submission costs several times
    // more and the delta-block graphs measurably pay.
    static const bool env_graph_gemma=[]{
        const char*s=std::getenv("FLYWEIGHT_CUDA_GRAPH_GEMMA");return s&&s[0]=='1';}();
    const bool gemma_graph_eligible=runtime.cuda_graphs&&env_graph_gemma;
    auto launch=[&](const char* name,std::uint32_t grid_x,std::uint32_t grid_y,
                    std::uint32_t block_x,void** arguments,std::uint32_t shared=0){
        if(flyweight_gpu_launch_named(name,grid_x,grid_y,block_x,shared,launch_stream,arguments)!=0)
            throw std::runtime_error(std::string("native Gemma 4 CUDA kernel failed: ")+name);
    };
    auto rms=[&](std::uint64_t input,std::uint64_t weights,std::uint64_t output){
        int one_centered=0;
        void* args[]={&input,&weights,&output,const_cast<int*>(&hidden_size),
                      const_cast<float*>(&epsilon),&one_centered};
        launch("rms_norm",1,1,256,args);
    };
    auto q4=[&](std::uint64_t matrix,std::uint64_t input,std::uint64_t output,
                int input_size,int output_size){
        void* args[]={&matrix,&input,&output,&input_size,&output_size};
        launch("gemma_q4_0_matvec",(output_size+7)/8,1,256,args);
    };
    auto f32=[&](std::uint64_t matrix,std::uint64_t input,std::uint64_t output,
                 int input_size,int output_size){
        void* args[]={&matrix,&input,&output,&input_size,&output_size};
        launch("qwen_f32_matvec_warp",(output_size+7)/8,1,256,args);
    };
    auto add=[&](std::uint64_t target,std::uint64_t source){
        float scale=1.0f;int count=hidden_size;
        void* args[]={&target,&source,&scale,&count};
        launch("scaled_add",(hidden_size+255)/256,1,256,args);
    };
    {
        const auto embedding=runtime.device_tensors[runtime.token_embeddings];
        const int token=static_cast<int>(input_token);
        const float scale=std::sqrt(static_cast<float>(hidden_size));
        void* args[]={const_cast<std::uint64_t*>(&embedding),&hidden,
                      const_cast<int*>(&token),const_cast<int*>(&hidden_size),
                      const_cast<float*>(&scale)};
        launch("gemma_q4_0_embedding",(hidden_size+255)/256,1,256,args);
    }
    for(std::uint32_t layer_number=0;layer_number<runtime.layers.size();++layer_number){
        auto& layer=runtime.layers[layer_number];
        auto tensor=[&](std::size_t role){return runtime.device_tensors[layer.static_tensors.at(role)];};

        // Attention: learned pre/post RMS norms, per-head Q/K normalization,
        // unscaled dot products, and a layer-specific local/global KV shape.
        rms(hidden,tensor(0),normalized);
        const int heads=static_cast<int>(layer.attention_heads);
        const int kv_heads=static_cast<int>(layer.kv_heads);
        const int head_dim=static_cast<int>(layer.head_dim);
        const int q_elements=heads*head_dim,kv_elements=kv_heads*head_dim;
        q4(tensor(1),normalized,first,hidden_size,q_elements);
        q4(tensor(2),normalized,second,hidden_size,kv_elements);
        q4(tensor(3),normalized,third,hidden_size,kv_elements);
        const int rotary=static_cast<int>(layer.rotary_dim);
        const int position=static_cast<int>(runtime.position);
        const float theta=layer.rope_theta;
        const std::uint64_t rope_factors=layer.attention_window||
            runtime.rope_factors==std::numeric_limits<std::uint64_t>::max()
            ?0:runtime.device_tensors[runtime.rope_factors];
        auto qnorm=tensor(5),knorm=tensor(6);
        void* q_args[]={const_cast<std::uint64_t*>(&first),&qnorm,
                        const_cast<std::uint64_t*>(&fourth),const_cast<int*>(&heads),
                        const_cast<int*>(&head_dim),const_cast<int*>(&rotary),
                        const_cast<int*>(&position),const_cast<float*>(&theta),
                        const_cast<float*>(&epsilon),const_cast<std::uint64_t*>(&rope_factors)};
        launch("gemma_head_norm_rope",heads,1,256,q_args);
        void* k_args[]={const_cast<std::uint64_t*>(&second),&knorm,
                        const_cast<std::uint64_t*>(&first),const_cast<int*>(&kv_heads),
                        const_cast<int*>(&head_dim),const_cast<int*>(&rotary),
                        const_cast<int*>(&position),const_cast<float*>(&theta),
                        const_cast<float*>(&epsilon),const_cast<std::uint64_t*>(&rope_factors)};
        launch("gemma_head_norm_rope",kv_heads,1,256,k_args);
        void* v_norm_args[]={const_cast<std::uint64_t*>(&third),
                             const_cast<std::uint64_t*>(&second),const_cast<int*>(&kv_heads),
                             const_cast<int*>(&head_dim),const_cast<float*>(&epsilon)};
        launch("gemma_head_rms",kv_heads,1,256,v_norm_args);
        std::uint64_t cache_keys=runtime.state+layer.state_first;
        std::uint64_t cache_values=runtime.state+layer.state_second;
        const auto view=attention_cache_view(layer,runtime.position);
        int slot=view.slot,capacity=view.capacity;
        void* k_store_args[]={const_cast<std::uint64_t*>(&first),&cache_keys,
                              const_cast<int*>(&kv_heads),const_cast<int*>(&head_dim),
                              &slot,&capacity};
        launch(kv_store_kernel(runtime.options.cache_type_k,true),kv_heads,1,256,k_store_args);
        void* v_store_args[]={const_cast<std::uint64_t*>(&second),&cache_values,
                              const_cast<int*>(&kv_heads),const_cast<int*>(&head_dim),
                              &slot,&capacity};
        launch(kv_store_kernel(runtime.options.cache_type_v,false),kv_heads,1,256,v_store_args);
        int tokens=view.tokens,first_slot=view.first;float attention_scale=1.0f;
        // Tiled online-softmax attention keeps long-context occupancy: the
        // values-ring kernel's heads-only grid was 70% of prefill GPU time at
        // 13k for the same reason, and decode pays it once per token. The 512
        // instantiation covers the five global layers.
        static const bool env_fused_decode=[]{
            const char*s=std::getenv("FLYWEIGHT_GEMMA_FUSED_ATTENTION");
            return !s||s[0]!='0';}();
        const int fused_width=kv_fused_width(head_dim);
        const char* fused_tiles_name=!env_fused_decode||!fused_width?nullptr
            :fused_width==128?kv_fused_tiles_kernel(runtime)
            :fused_width==256?kv_fused_tiles_kernel256(runtime)
            :kv_fused_tiles_kernel512(runtime);
        const int fused_tile_count=(tokens+1023)/1024;
        if(fused_tiles_name&&heads/kv_heads<=8&&fused_tile_count<=512&&
           static_cast<std::uint64_t>(fused_tile_count)*
               kv_fused_record_stride(fused_width)<=runtime.options.context_limit){
            void* fused_args[]={const_cast<std::uint64_t*>(&fourth),&cache_keys,
                                &cache_values,
                                const_cast<std::uint64_t*>(&attention_scores),
                                const_cast<int*>(&heads),const_cast<int*>(&kv_heads),
                                const_cast<int*>(&head_dim),&tokens,&capacity,
                                &first_slot,&attention_scale};
            launch(fused_tiles_name,heads,fused_tile_count,256,fused_args);
            void* merge_args[]={const_cast<std::uint64_t*>(&attention_scores),
                                const_cast<std::uint64_t*>(&third),
                                const_cast<int*>(&heads),
                                const_cast<int*>(&head_dim),
                                const_cast<int*>(&fused_tile_count)};
            launch(kv_fused_merge_kernel_name(runtime,fused_width),
                   heads,1,256,merge_args);
        }else{
            void* score_args[]={const_cast<std::uint64_t*>(&fourth),&cache_keys,
                                const_cast<std::uint64_t*>(&attention_scores),
                                const_cast<int*>(&heads),const_cast<int*>(&kv_heads),
                                const_cast<int*>(&head_dim),&tokens,&capacity,&first_slot,
                                &attention_scale};
            launch(kv_scores_ring_kernel(runtime),heads,(tokens+255)/256,256,score_args);
            void* value_args[]={const_cast<std::uint64_t*>(&attention_scores),&cache_values,
                                const_cast<std::uint64_t*>(&third),const_cast<int*>(&heads),
                                const_cast<int*>(&kv_heads),const_cast<int*>(&head_dim),
                                &tokens,&capacity,&first_slot};
            launch(kv_values_ring_kernel(runtime),heads,1,256,value_args);
        }
        q4(tensor(4),third,residual,q_elements,hidden_size);
        rms(residual,tensor(7),normalized);
        add(hidden,normalized);

        // Dense GEGLU path and the router. Token-invariant launches: fixed
        // weights, fixed workspace slots, no position and no KV cache, so a
        // pinned layer's capture below can record this chain into its graph.
        auto enqueue_ffn_router=[&]{
            rms(hidden,tensor(8),normalized);
            auto dense_gate=tensor(9),dense_up=tensor(10);
            void* dense_args[]={&dense_gate,&dense_up,const_cast<std::uint64_t*>(&normalized),
                                const_cast<std::uint64_t*>(&first),const_cast<int*>(&hidden_size),
                                const_cast<int*>(&dense_intermediate)};
            launch("gemma_q4_0_geglu",(dense_intermediate+7)/8,1,256,dense_args);
            q4(tensor(11),first,second,dense_intermediate,hidden_size);
            rms(second,tensor(12),third);
            // Router consumes the pre-FFN residual. Experts consume their own
            // learned normalization of that same residual.
            auto router_scale=tensor(13);
            void* router_input_args[]={&hidden,&router_scale,
                                       const_cast<std::uint64_t*>(&normalized),
                                       const_cast<int*>(&hidden_size),
                                       const_cast<float*>(&epsilon)};
            launch("gemma_router_input",1,1,256,router_input_args);
            f32(tensor(14),normalized,router_logits,hidden_size,experts);
            if(flyweight_gpu_route_topk(router_logits,selected_device,route_weights,
                                     experts,top_k,launch_stream)!=0)
                throw std::runtime_error("native Gemma 4 routing failed");
            rms(hidden,tensor(15),normalized);
        };
        // A whole-layer-pinned layer holds all of its experts on device by
        // construction, so the routed indices never need to visit the host:
        // route_topk's outputs feed the pinned grouped kernels directly and
        // this layer costs no download and no event sync. That also makes the
        // whole dense+router+MoE chain graph-capturable -- one submission per
        // token instead of ~16.
        const bool layer_pinned=!expert_policy.is_cpu()&&
            layer_number<runtime.whole_expert_layer_slots.size()&&
            runtime.whole_expert_layer_slots[layer_number]>=0;
        if(layer_pinned){
            runtime.route_expert_sum+=top_k;
            runtime.expert_cache_hits+=top_k;
            const auto layer_base=runtime.expert_cache+
                static_cast<std::uint64_t>(
                    runtime.whole_expert_layer_slots[layer_number])*
                    runtime.expert_slot_bytes;
            const auto& gate_up_tensor=runtime.model->tensors[layer.expert_tensors[0]];
            const auto& down_tensor=runtime.model->tensors[layer.expert_tensors[1]];
            const std::uint64_t down_offset=gate_up_tensor.size/experts;
            const std::uint64_t scale_offset=down_offset+down_tensor.size/experts;
            const int intermediate=static_cast<int>(runtime.moe_intermediate);
            auto enqueue_pinned_moe=[&]{
                if(flyweight_gpu_memset(fourth,0,hidden_size*sizeof(float),launch_stream)!=0)
                    throw std::runtime_error("native Gemma 4 pinned output reset failed");
                void* gate_args[]={const_cast<std::uint64_t*>(&layer_base),
                                   const_cast<std::uint64_t*>(&runtime.expert_slot_bytes),
                                   const_cast<std::uint64_t*>(&selected_device),
                                   const_cast<std::uint64_t*>(&normalized),
                                   const_cast<std::uint64_t*>(&activated),
                                   const_cast<int*>(&hidden_size),
                                   const_cast<int*>(&intermediate),
                                   const_cast<int*>(&top_k)};
                launch("gemma_q4_0_pinned_geglu",(intermediate+7)/8,top_k,256,gate_args);
                void* down_args[]={const_cast<std::uint64_t*>(&layer_base),
                                   const_cast<std::uint64_t*>(&runtime.expert_slot_bytes),
                                   const_cast<std::uint64_t*>(&down_offset),
                                   const_cast<std::uint64_t*>(&scale_offset),
                                   const_cast<std::uint64_t*>(&selected_device),
                                   const_cast<std::uint64_t*>(&activated),
                                   const_cast<std::uint64_t*>(&fourth),
                                   const_cast<std::uint64_t*>(&route_weights),
                                   const_cast<int*>(&intermediate),
                                   const_cast<int*>(&hidden_size),
                                   const_cast<int*>(&top_k)};
                launch("gemma_q4_0_pinned_accumulate",(hidden_size+7)/8,1,256,down_args);
                rms(fourth,tensor(16),first);
                add(third,first);
                rms(third,tensor(17),residual);
                add(hidden,residual);
                auto layer_scale=tensor(18);
                void* scale_args[]={&hidden,&layer_scale,const_cast<int*>(&hidden_size)};
                launch("gemma_scale_vector",(hidden_size+255)/256,1,256,scale_args);
            };
            bool ffn_launched=false;
            if(gemma_graph_eligible&&layer.gemma_ffn_graph){
                if(flyweight_gpu_graph_launch(layer.gemma_ffn_graph,runtime.stream)==0){
                    ++runtime.cuda_graph_replays;ffn_launched=true;
                }else{
                    flyweight_gpu_graph_destroy(layer.gemma_ffn_graph);
                    layer.gemma_ffn_graph=0;++runtime.cuda_graph_fallbacks;
                }
            }
            if(gemma_graph_eligible&&!ffn_launched&&!layer.gemma_ffn_graph_attempted){
                // Capture records without executing; the replay below is the
                // only execution this token, keeping it ordered on
                // runtime.stream behind the eager attention above.
                layer.gemma_ffn_graph_attempted=true;
                if(flyweight_gpu_graph_begin(runtime.graph_stream)==0){
                    bool capture_enqueued=true;
                    launch_stream=runtime.graph_stream;
                    try{enqueue_ffn_router();enqueue_pinned_moe();}
                    catch(...){capture_enqueued=false;}
                    launch_stream=runtime.stream;
                    std::uint64_t captured_graph=0;
                    const int capture_status=flyweight_gpu_graph_end(
                        runtime.graph_stream,&captured_graph);
                    if(capture_enqueued&&capture_status==0){
                        layer.gemma_ffn_graph=captured_graph;++runtime.cuda_graph_builds;
                        if(std::getenv("FLYWEIGHT_CUDA_GRAPH_TRACE"))
                            std::fprintf(stderr,
                                "[cuda-graphs] gemma layer %u FFN captured\n",
                                layer_number);
                        if(flyweight_gpu_graph_launch(layer.gemma_ffn_graph,runtime.stream)==0){
                            ++runtime.cuda_graph_replays;ffn_launched=true;
                        }else{
                            flyweight_gpu_graph_destroy(layer.gemma_ffn_graph);
                            layer.gemma_ffn_graph=0;++runtime.cuda_graph_fallbacks;
                        }
                    }else{
                        if(capture_status==0&&captured_graph)
                            flyweight_gpu_graph_destroy(captured_graph);
                        ++runtime.cuda_graph_fallbacks;
                        if(std::getenv("FLYWEIGHT_CUDA_GRAPH_TRACE"))
                            std::fprintf(stderr,
                                "[cuda-graphs] gemma layer %u FFN capture failed "
                                "(enqueued=%d status=%d)\n",
                                layer_number,capture_enqueued?1:0,capture_status);
                    }
                }
            }
            if(!ffn_launched){enqueue_ffn_router();enqueue_pinned_moe();}
            continue;
        }
        enqueue_ffn_router();
        if(flyweight_gpu_download(selected_host,selected_device,top_k*sizeof(std::int32_t),runtime.stream)!=0||
           flyweight_gpu_download(cpu_weights,route_weights,top_k*sizeof(float),runtime.stream)!=0||
           flyweight_gpu_download(cpu_input,normalized,hidden_size*sizeof(float),runtime.stream)!=0)
            throw std::runtime_error("native Gemma 4 CPU MoE input transfer failed");
        if(flyweight_gpu_event_record(runtime.route_event,runtime.stream)!=0||
           flyweight_gpu_event_sync(runtime.route_event)!=0)
            throw std::runtime_error("native Gemma 4 route synchronization failed");
        runtime.route_expert_sum+=top_k;
        if(expert_policy.is_cpu()){
            const auto compute_started=std::chrono::steady_clock::now();
            gemma_cpu_moe(runtime,layer,selected_host,cpu_weights,top_k,cpu_input,
                          cpu_activated,cpu_output);
            runtime.expert_compute_nanoseconds+=std::chrono::duration_cast<std::chrono::nanoseconds>(
                std::chrono::steady_clock::now()-compute_started).count();
            if(flyweight_gpu_upload(fourth,cpu_output,hidden_size*sizeof(float),runtime.stream)!=0)
                throw std::runtime_error("native Gemma 4 CPU MoE output upload failed");
        }else{
            const auto pager_started=std::chrono::steady_clock::now();
            std::array<std::int32_t,256> cpu_selected{};
            std::array<float,256> cpu_compact_weights{},gpu_compact_weights{};
            std::array<std::uint64_t,256> gate_up_pointers{},down_pointers{};
            int cpu_count=0,gpu_count=0;
            const auto& gate_up_tensor=runtime.model->tensors[layer.expert_tensors[0]];
            const auto& down_tensor=runtime.model->tensors[layer.expert_tensors[1]];
            const auto& scale_tensor=runtime.model->tensors[layer.expert_tensors[2]];
            const auto gate_up_bytes=gate_up_tensor.size/experts;
            const auto down_bytes=down_tensor.size/experts;
            const auto scale_bytes=scale_tensor.size/experts;
            const auto* expert_scales=reinterpret_cast<const float*>(tensor_data(*runtime.model,scale_tensor));
            std::uint64_t paging_cursor=device_align(output_offset+hidden_size*sizeof(float));
            for(int rank=0;rank<top_k;++rank){
                const int expert=selected_host[rank];
                if(expert<0||expert>=experts)throw std::runtime_error("native Gemma 4 selected an invalid expert");
                const auto cache_key=(static_cast<std::uint64_t>(layer_number)<<32)|static_cast<std::uint32_t>(expert);
                auto resident=runtime.expert_residency.find(cache_key);
                if(resident!=runtime.expert_residency.end()){
                    const auto slot_index=resident->second;
                    auto& cache_slot=runtime.expert_slots[slot_index];
                    const auto&history=record_expert_access(
                        runtime,layer_number,expert);
                    if(expert_policy.residency_may_change())
                        cache_slot.last_used=history.last_used;
                    const auto base=runtime.expert_cache+slot_index*runtime.expert_slot_bytes;
                    gate_up_pointers[gpu_count]=base;
                    down_pointers[gpu_count]=base+gate_up_bytes;
                    gpu_compact_weights[gpu_count]=cpu_weights[rank]*expert_scales[expert];
                    ++gpu_count;record_expert_cache_hit(runtime,cache_slot);
                    continue;
                }
                ++runtime.expert_cache_misses;
                cpu_selected[cpu_count]=expert;
                cpu_compact_weights[cpu_count]=cpu_weights[rank];
                ++cpu_count;
                if(runtime.expert_slots.empty())continue;
                const auto slot_index=select_expert_cache_slot(runtime,layer_number,expert,true);
                if(slot_index==kNoExpertSlot)continue;
                auto& cache_slot=runtime.expert_slots[slot_index];
                cache_slot.key=cache_key;cache_slot.valid=true;cache_slot.last_used=runtime.expert_clock;
                runtime.expert_residency[cache_key]=slot_index;
                const auto bundle_bytes=gate_up_bytes+down_bytes+scale_bytes;
                if(paging_cursor+bundle_bytes>runtime.host_staging_bytes)
                    throw std::runtime_error("native Gemma 4 hybrid paging workspace overflow");
                std::memcpy(staging+paging_cursor,tensor_data(*runtime.model,gate_up_tensor)+static_cast<std::uint64_t>(expert)*gate_up_bytes,gate_up_bytes);
                std::memcpy(staging+paging_cursor+gate_up_bytes,tensor_data(*runtime.model,down_tensor)+static_cast<std::uint64_t>(expert)*down_bytes,down_bytes);
                std::memcpy(staging+paging_cursor+gate_up_bytes+down_bytes,tensor_data(*runtime.model,scale_tensor)+static_cast<std::uint64_t>(expert)*scale_bytes,scale_bytes);
                const auto base=runtime.expert_cache+slot_index*runtime.expert_slot_bytes;
                if(flyweight_gpu_upload(base,staging+paging_cursor,bundle_bytes,runtime.stream)!=0)
                    throw std::runtime_error("native Gemma 4 hybrid expert upload failed");
                paging_cursor+=device_align(bundle_bytes);
            }
            if(flyweight_gpu_memset(fourth,0,hidden_size*sizeof(float),runtime.stream)!=0)
                throw std::runtime_error("native Gemma 4 hybrid output reset failed");
            if(gpu_count){
                const auto table_bytes=device_align(static_cast<std::uint64_t>(gpu_count)*(
                    2*sizeof(std::uint64_t)+sizeof(float)));
                const auto table_host=device_align(paging_cursor);
                if(table_host+table_bytes>runtime.host_staging_bytes||table_bytes>runtime.expert_staging_bytes)
                    throw std::runtime_error("native Gemma 4 hybrid pointer workspace overflow");
                std::memcpy(staging+table_host,gate_up_pointers.data(),gpu_count*sizeof(std::uint64_t));
                std::memcpy(staging+table_host+gpu_count*sizeof(std::uint64_t),down_pointers.data(),gpu_count*sizeof(std::uint64_t));
                std::memcpy(staging+table_host+2*gpu_count*sizeof(std::uint64_t),gpu_compact_weights.data(),gpu_count*sizeof(float));
                const auto table_device=runtime.expert_staging+runtime.expert_staging_bytes-table_bytes;
                if(flyweight_gpu_upload(table_device,staging+table_host,table_bytes,runtime.stream)!=0)
                    throw std::runtime_error("native Gemma 4 hybrid pointer upload failed");
                const auto gate_table=table_device;
                const auto down_table=gate_table+gpu_count*sizeof(std::uint64_t);
                const auto weight_table=down_table+gpu_count*sizeof(std::uint64_t);
                const int intermediate=static_cast<int>(runtime.moe_intermediate);
                void* gate_args[]={const_cast<std::uint64_t*>(&gate_table),
                                   const_cast<std::uint64_t*>(&normalized),
                                   const_cast<std::uint64_t*>(&activated),
                                   const_cast<int*>(&hidden_size),const_cast<int*>(&intermediate),&gpu_count};
                launch("gemma_q4_0_grouped_geglu",(intermediate+7)/8,gpu_count,256,gate_args);
                void* down_args[]={const_cast<std::uint64_t*>(&down_table),
                                   const_cast<std::uint64_t*>(&activated),
                                   const_cast<std::uint64_t*>(&fourth),
                                   const_cast<std::uint64_t*>(&weight_table),
                                   const_cast<int*>(&intermediate),const_cast<int*>(&hidden_size),&gpu_count};
                launch("gemma_q4_0_grouped_accumulate",(hidden_size+7)/8,1,256,down_args);
            }
            if(cpu_count){
                const auto compute_started=std::chrono::steady_clock::now();
                gemma_cpu_moe(runtime,layer,cpu_selected.data(),cpu_compact_weights.data(),
                              cpu_count,cpu_input,cpu_activated,cpu_output);
                runtime.expert_compute_nanoseconds+=std::chrono::duration_cast<std::chrono::nanoseconds>(
                    std::chrono::steady_clock::now()-compute_started).count();
                if(flyweight_gpu_upload(first,cpu_output,hidden_size*sizeof(float),runtime.stream)!=0)
                    throw std::runtime_error("native Gemma 4 hybrid CPU output upload failed");
                add(fourth,first);
            }
            runtime.expert_page_nanoseconds+=std::chrono::duration_cast<std::chrono::nanoseconds>(
                std::chrono::steady_clock::now()-pager_started).count();
        }
        rms(fourth,tensor(16),first);
        add(third,first);
        rms(third,tensor(17),residual);
        add(hidden,residual);
        auto layer_scale=tensor(18);
        void* scale_args[]={&hidden,&layer_scale,const_cast<int*>(&hidden_size)};
        launch("gemma_scale_vector",(hidden_size+255)/256,1,256,scale_args);
    }
    rms(hidden,runtime.device_tensors[runtime.final_norm],normalized);
    // A sampled or penalized step re-projects the head from this normalized
    // state into the layout's logits region; the fused argmax below never
    // writes logits, exactly like the Qwen greedy path.
    runtime.last_sampling_normalized=normalized;
    runtime.last_sampling_logits=workspace_layout.logits.address(runtime.workspace);
    const int vocabulary=static_cast<int>(runtime.model->config.vocabulary_size);
    if(flyweight_gpu_memset(argmax_device,0,sizeof(std::uint64_t),runtime.stream)!=0)
        throw std::runtime_error("native Gemma 4 argmax reset failed");
    auto lm_head=runtime.device_tensors[runtime.lm_head];
    void* argmax_args[]={&lm_head,const_cast<std::uint64_t*>(&normalized),
                         const_cast<std::uint64_t*>(&argmax_device),
                         const_cast<int*>(&hidden_size),const_cast<int*>(&vocabulary)};
    launch("gemma_q4_0_lm_argmax",(vocabulary+7)/8,1,256,argmax_args);
    auto* packed_winner=reinterpret_cast<std::uint64_t*>(staging);
    if(flyweight_gpu_download(packed_winner,argmax_device,sizeof(*packed_winner),runtime.stream)!=0||
       flyweight_gpu_stream_sync(runtime.stream)!=0)
        throw std::runtime_error("native Gemma 4 output synchronization failed");
    output_token=0xffffffffu-static_cast<std::uint32_t>(*packed_winner);
    runtime.last_output_token=output_token;
    runtime.last_output_greedy=true;
    runtime.processed_tokens.push_back(input_token);
    ++runtime.position;
    ++runtime.decode_calls;
    runtime.decode_nanoseconds+=std::chrono::duration_cast<std::chrono::nanoseconds>(
        std::chrono::steady_clock::now()-decode_started).count();
    return 0;
}

int flyweight_v2_qwen_runtime_synchronize(FlyweightV2QwenRuntime*runtime){return guarded([&]{if(!runtime||!runtime->stream)throw std::runtime_error("native Qwen runtime is not prepared");if(flyweight_gpu_stream_sync(runtime->stream)!=0)throw std::runtime_error("native Qwen CUDA synchronization failed");return 0;});}
int flyweight_v2_qwen_runtime_decode(FlyweightV2QwenRuntime*runtime,uint32_t input_token,uint32_t*output_token){return guarded([&]{
    if(!runtime||!output_token)throw std::runtime_error("invalid native Qwen decode arguments");
    if(!runtime->decode_ready)throw std::runtime_error("native Qwen runtime is not prepared for decode");
    if(runtime->cancelled)throw std::runtime_error("native Qwen runtime is cancelled");
    if(runtime->position>=runtime->options.context_limit)throw std::runtime_error("native Qwen context limit exceeded");
    if(input_token>=runtime->model->config.vocabulary_size)throw std::runtime_error("native Qwen input token is out of range");
    if(runtime->cache_admission_enabled)
        qwen_freeze_expert_residency(*runtime);
    qwen_absorb_registration(*runtime);
    if(runtime->gemma4)return gemma4_decode(*runtime,input_token,*output_token);
    const auto expert_phase=runtime->cache_admission_enabled
        ?flyweight::v2::ExpertExecutionPhase::decode
        :flyweight::v2::ExpertExecutionPhase::prefill;
    const auto expert_policy=qwen_expert_policy(*runtime,expert_phase);
    qwen_mtp_append_prompt_pair(*runtime,input_token);
    const auto decode_started=std::chrono::steady_clock::now();
    const int hidden_size=static_cast<int>(runtime->model->config.hidden_size);
    const int experts=static_cast<int>(runtime->model->config.expert_count);
    // Optionally route fewer experts than the model trained with (top-k pruning):
    // route_topk renormalizes the softmax over the kept experts, and every
    // downstream buffer/loop is sized from this count, so the whole MoE phase
    // (routing, DtoH, GPU grouped kernels, CPU experts) shrinks proportionally.
    const int configured_top_k=static_cast<int>(runtime->model->config.expert_used_count);
    const int top_k=(runtime->options.expert_top_k>0&&static_cast<int>(runtime->options.expert_top_k)<configured_top_k)?static_cast<int>(runtime->options.expert_top_k):configured_top_k;
    const float epsilon=runtime->model->config.rms_norm_epsilon?runtime->model->config.rms_norm_epsilon:1.0e-6f;
    const auto&workspace_layout=runtime->decode_workspace_layout;
    if(workspace_layout.bytes>runtime->workspace_bytes)
        throw std::runtime_error("native Qwen workspace is too small");
    std::uint64_t hidden=workspace_layout.hidden.address(runtime->workspace);
    std::uint64_t residual=workspace_layout.residual.address(runtime->workspace);
    const std::uint64_t normalized=workspace_layout.normalized.address(runtime->workspace);
    const std::uint64_t first=workspace_layout.first.address(runtime->workspace);
    const std::uint64_t second=workspace_layout.second.address(runtime->workspace);
    const std::uint64_t third=workspace_layout.third.address(runtime->workspace);
    const std::uint64_t fourth=workspace_layout.fourth.address(runtime->workspace);
    const std::uint64_t dense_q8=workspace_layout.dense_q8.address(runtime->workspace);
    const std::uint64_t dense_q8_scales=workspace_layout.dense_q8_scales.address(runtime->workspace);
    const std::uint64_t activated=workspace_layout.activated.address(runtime->workspace);
    const std::uint64_t router_logits=workspace_layout.router_logits.address(runtime->workspace);
    const std::uint64_t selected_device=workspace_layout.selected_device.address(runtime->workspace);
    const std::uint64_t route_weights=workspace_layout.route_weights.address(runtime->workspace);
    const std::uint64_t logits=workspace_layout.logits.address(runtime->workspace);
    const std::uint64_t argmax_device=workspace_layout.argmax_device.address(runtime->workspace);
    const std::uint64_t attention_scores=workspace_layout.attention_scores.address(runtime->workspace);
    // qwen4exp gated-residual buffers; the regions are zero-sized elsewhere.
    const std::uint64_t hc_streams=workspace_layout.streams.address(runtime->workspace);
    const std::uint64_t hc_normed=workspace_layout.hc_normed.address(runtime->workspace);
    const std::uint64_t hc_wide=workspace_layout.hc_wide.address(runtime->workspace);
    const std::uint64_t hc_low=workspace_layout.hc_low.address(runtime->workspace);
    const std::uint64_t hc_gates=workspace_layout.hc_gates.address(runtime->workspace);
    const std::uint64_t ple_embed=workspace_layout.ple_embed.address(runtime->workspace);
    const int hc_count=static_cast<int>(runtime->model->config.hyper_connection_count);
    const int hc_low_rank=static_cast<int>(runtime->model->config.hyper_connection_low_rank);
    auto*staging=static_cast<std::uint8_t*>(runtime->host_staging);
    auto*selected_host=reinterpret_cast<std::int32_t*>(staging);
    std::uint64_t launch_stream=runtime->stream;
    // FLYWEIGHT_KERNEL_PROFILE=1 brackets every decode launch with CUDA events and
    // accumulates GPU time per kernel name. The events serialize the stream, so
    // the absolute total runs high; the point is the relative split, which is
    // what says whether a token is spent reading weights or somewhere else.
    static const bool lm_diagnostics=std::getenv("FLYWEIGHT_LM_DIAG")!=nullptr;
    static const bool kernel_profile=
        std::getenv("FLYWEIGHT_KERNEL_PROFILE")&&
        std::getenv("FLYWEIGHT_KERNEL_PROFILE")[0]=='1';
    struct KernelProfileEntry{double milliseconds=0.0;std::uint64_t calls=0;};
    static std::map<std::string,KernelProfileEntry> kernel_profile_totals;
    // Events are recorded on the stream and only read back once the token is
    // done. Syncing per launch would stall the CPU, and an idle GPU drops from
    // ~2.2 GHz to a few hundred MHz -- which silently rescales every number the
    // profile reports. Nothing here blocks until the decode has been issued.
    static constexpr std::size_t kKernelProfileCapacity=4096;
    static std::vector<std::uint64_t> kernel_profile_events;
    static std::vector<std::string> kernel_profile_labels;
    static std::size_t kernel_profile_used=0;
    static std::uint64_t kernel_profile_overflow=0;
    static std::uint64_t kernel_profile_decodes=0;
    if(kernel_profile&&kernel_profile_events.empty()){
        kernel_profile_events.resize(2*kKernelProfileCapacity);
        kernel_profile_labels.resize(kKernelProfileCapacity);
        for(auto&event:kernel_profile_events)
            flyweight_gpu_timed_event_create(&event);
    }
    auto launch_named=[&](const char*name,std::uint32_t grid_x,std::uint32_t grid_y,std::uint32_t block_x,void**arguments,std::uint32_t shared=0){
        const bool traced=kernel_profile&&kernel_profile_used<kKernelProfileCapacity;
        const std::size_t slot=kernel_profile_used;
        if(traced)flyweight_gpu_event_record(kernel_profile_events[2*slot],launch_stream);
        if(flyweight_gpu_launch_named(name,grid_x,grid_y,block_x,shared,launch_stream,arguments)!=0)throw std::runtime_error(std::string("native Qwen CUDA kernel failed: ")+name);
        if(traced){
            flyweight_gpu_event_record(kernel_profile_events[2*slot+1],launch_stream);
            char key[192];
            std::snprintf(key,sizeof(key),"%s grid=%u",name,grid_x);
            kernel_profile_labels[slot]=key;
            ++kernel_profile_used;
        }else if(kernel_profile)++kernel_profile_overflow;
    };
    auto kernel_profile_collect=[&]{
        if(!kernel_profile)return;
        for(std::size_t slot=0;slot<kernel_profile_used;++slot){
            float milliseconds=0.0f;
            if(flyweight_gpu_event_elapsed(kernel_profile_events[2*slot],
                                         kernel_profile_events[2*slot+1],
                                         &milliseconds)!=0)continue;
            auto&entry=kernel_profile_totals[kernel_profile_labels[slot]];
            entry.milliseconds+=milliseconds;++entry.calls;
        }
        kernel_profile_used=0;
    };
    auto kernel_profile_report=[&]{
        if(!kernel_profile)return;
        double total=0.0;

        for(const auto&entry:kernel_profile_totals)total+=entry.second.milliseconds;
        std::vector<std::pair<std::string,KernelProfileEntry>> sorted(
            kernel_profile_totals.begin(),kernel_profile_totals.end());
        std::sort(sorted.begin(),sorted.end(),[](const auto&a,const auto&b){
            return a.second.milliseconds>b.second.milliseconds;});
        std::uint64_t calls=0;
        for(const auto&entry:kernel_profile_totals)calls+=entry.second.calls;
        const double decodes=static_cast<double>(kernel_profile_decodes?kernel_profile_decodes:1);
        std::fprintf(stderr,
            "[kernel-profile] %.2f ms/token of kernel time over %.0f decodes, "
            "%.0f launches/token across %zu distinct kernels (untraced=%llu)\n",
            total/decodes,decodes,calls/decodes,kernel_profile_totals.size(),
            static_cast<unsigned long long>(kernel_profile_overflow));
        for(const auto&entry:sorted)
            std::fprintf(stderr,"[kernel-profile]   %-52s %7.3f ms/tok %6.1f%%  %5.1f calls/tok %7.1f us\n",
                entry.first.c_str(),entry.second.milliseconds/decodes,
                100.0*entry.second.milliseconds/total,
                entry.second.calls/decodes,
                1000.0*entry.second.milliseconds/entry.second.calls);
        kernel_profile_totals.clear();
        kernel_profile_overflow=0;kernel_profile_decodes=0;
    };
    // The Q8 copy of `normalized` is reused across the projections that share
    // it, so rms -- its only writer -- has to drop that memo.
    std::uint64_t q8_cached_input=0;
    auto rms=[&](std::uint64_t input,std::uint64_t weights,std::uint64_t output){int one_centered=0;q8_cached_input=0;void*args[]={&input,&weights,&output,const_cast<int*>(&hidden_size),const_cast<float*>(&epsilon),&one_centered};launch_named("rms_norm",1,1,1024,args);};
    // Muse Glimmer's two post-norms run at a tighter epsilon than the model's
    // own rms_norm_epsilon (1e-8 against 1e-5) and are safe in place: rms_norm
    // block-reduces before any write, then each thread only rewrites the index
    // it read.
    const float post_norm_epsilon=1.0e-8f;
    auto post_rms=[&](std::uint64_t values,std::uint64_t weights){
        int one_centered=0;q8_cached_input=0;
        void*args[]={&values,&weights,&values,const_cast<int*>(&hidden_size),
                     const_cast<float*>(&post_norm_epsilon),&one_centered};
        launch_named("rms_norm",1,1,1024,args);
    };
    auto q8=[&](std::uint64_t matrix,std::uint64_t input,std::uint64_t output,int input_size,int output_size){if(flyweight_gpu_q8_matvec_transposed(matrix,input,output,input_size,output_size,launch_stream)!=0)throw std::runtime_error("native Qwen Q8 projection failed");};
    auto f32=[&](std::uint64_t matrix,std::uint64_t input,std::uint64_t output,int input_size,int output_size){void*args[]={&matrix,&input,&output,&input_size,&output_size};launch_named("qwen_f32_matvec_warp",(output_size+7)/8,1,256,args);};
    const char*iq2_q8_setting=std::getenv("FLYWEIGHT_IQ2_Q8_DECODE");
    const bool iq2_q8_enabled=
        !iq2_q8_setting||iq2_q8_setting[0]!='0';
    // Quantize the activation to 32-value Q8 blocks once, then run a group-wise
    // DP4A matvec instead of reconstructing every weight in f32. Requires a
    // superblock-aligned row so the Q8 blocks line up with the weight groups.
    auto q8_decode=[&](const char*kernel,std::uint64_t matrix,std::uint64_t input,
                       std::uint64_t output,int input_size,int output_size){
        if(!iq2_q8_enabled||(input_size&255))return false;
        // q/k/v and gate/up all project the same normalized vector, so the
        // quantization is hoisted out of the repeats. Only `normalized` is
        // memoized: every other source buffer is rewritten in place by kernels
        // this lambda cannot see.
        if(input!=normalized||input!=q8_cached_input){
            void*quant_args[]={&input,const_cast<std::uint64_t*>(&dense_q8),
                const_cast<std::uint64_t*>(&dense_q8_scales),&input_size};
            launch_named("quantize_q8_blocks",(input_size+31)/32,1,32,quant_args);
            q8_cached_input=(input==normalized)?input:0;
        }
        void*matvec_args[]={&matrix,const_cast<std::uint64_t*>(&dense_q8),
            const_cast<std::uint64_t*>(&dense_q8_scales),&output,
            &input_size,&output_size};
        launch_named(kernel,output_size,1,128,matvec_args);
        return true;
    };
    // Dense projections keep whatever type the checkpoint stored. The NVFP4 Qwen3.6
    // builds ship attention/SSM/router weights as bf16, older checkpoints as Q8_0;
    // reading one as the other reinterprets the bytes and drives the residual stream
    // to ~1e7, so the tensor type has to pick the kernel.
    auto dense_matvec=[&](std::size_t index,std::uint64_t input,std::uint64_t output,int input_size,int output_size){
        std::uint64_t matrix=runtime->device_tensors[index];
        const auto type=qwen_device_type(*runtime,index);
        if(type==0){void*args[]={&matrix,&input,&output,&input_size,&output_size};launch_named("qwen_f32_matvec_warp",(output_size+7)/8,1,256,args);return;}
        // bf16_matvec takes (rows, columns), the reverse of the quantized matvecs.
        if(type==30){void*args[]={&matrix,&input,&output,&output_size,&input_size};launch_named("bf16_matvec_warp",(output_size+7)/8,1,256,args);return;}
        const auto*format=flyweight::v2::qwen_format(type);
        if(format&&format->matvec_q8_warp&&
           q8_decode(format->matvec_q8_warp,matrix,input,output,input_size,output_size))return;
        const int status=qwen_matvec_driver(type,matrix,input,output,input_size,output_size,launch_stream);
        if(status==0)return;
        if(status==kQwenMatvecUnsupported)
            throw std::runtime_error("native Qwen dense projection type is unsupported: "+std::to_string(type));
        throw std::runtime_error("native Qwen dense projection failed");
    };
    auto add=[&](std::uint64_t target,std::uint64_t source){float scale=1.0f;int count=hidden_size;void*args[]={&target,&source,&scale,&count};launch_named("scaled_add",(hidden_size+255)/256,1,256,args);};
    // qwen4exp gated-residual bookends (plans/qwen4exp-semantics.md, pinned by
    // qwen4exp_reference_check.py). hc_pre replaces the pre-block rms: it norms
    // the streams (grouped), mixes them to one block input in `normalized`,
    // and leaves the raw inject logits in hc_gates. hc_post replaces the
    // residual add: streams += 2*sigmoid(logits/hc) x block_output, on the
    // PRE-norm streams. The head collapse is hc_pre without an inject.
    auto hc_pre=[&](std::uint64_t norm_weights,std::uint64_t down_index,
                    std::uint64_t up_index,std::uint64_t inject_index){
        const int hc_wide_size=hc_count*hidden_size;
        void*norm_args[]={const_cast<std::uint64_t*>(&hc_streams),&norm_weights,
            const_cast<std::uint64_t*>(&hc_normed),
            const_cast<int*>(&hidden_size),const_cast<float*>(&epsilon)};
        launch_named("qwen4_group_rms",hc_count,1,256,norm_args);
        dense_matvec(down_index,hc_normed,hc_low,hc_wide_size,hc_low_rank);
        float divisor=static_cast<float>(hc_count);
        void*silu_args[]={const_cast<std::uint64_t*>(&hc_low),
            const_cast<int*>(&hc_low_rank),&divisor};
        launch_named("qwen4_silu_scale",
            (static_cast<std::uint32_t>(hc_low_rank)+255)/256,1,256,silu_args);
        dense_matvec(up_index,hc_low,hc_wide,hc_low_rank,hc_wide_size);
        if(inject_index!=std::numeric_limits<std::uint64_t>::max())
            dense_matvec(inject_index,hc_normed,hc_gates,hc_wide_size,hc_count);
        void*mix_args[]={const_cast<std::uint64_t*>(&hc_normed),
            const_cast<std::uint64_t*>(&hc_wide),
            const_cast<std::uint64_t*>(&normalized),
            const_cast<int*>(&hc_count),const_cast<int*>(&hidden_size)};
        launch_named("qwen4_hc_mix",
            (static_cast<std::uint32_t>(hidden_size)+255)/256,1,256,mix_args);
        // `normalized` was rewritten behind the rms memo's back.
        q8_cached_input=0;
    };
    auto hc_post=[&](std::uint64_t block_output){
        const int hc_wide_size=hc_count*hidden_size;
        void*inject_args[]={const_cast<std::uint64_t*>(&hc_streams),
            &block_output,const_cast<std::uint64_t*>(&hc_gates),
            const_cast<int*>(&hc_count),const_cast<int*>(&hidden_size)};
        launch_named("qwen4_hc_inject",
            (static_cast<std::uint32_t>(hc_wide_size)+255)/256,1,256,inject_args);
    };
    // The PLE block at the top of its layer: gather already uploaded to
    // ple_embed by qwen4_stage_ple_embed; this runs the key/query gating, the
    // dilated conv (stateful, state_third), and adds the delta to the streams.
    auto ple_block=[&](const QwenLayerPlan&layer){
        const int hc_wide_size=hc_count*hidden_size;
        dense_matvec(layer.ple_key,ple_embed,hc_wide,hidden_size,hc_wide_size);
        auto norm_key=runtime->device_tensors[layer.ple_norm_key];
        void*key_args[]={const_cast<std::uint64_t*>(&hc_wide),&norm_key,
            const_cast<std::uint64_t*>(&hc_wide),
            const_cast<int*>(&hidden_size),const_cast<float*>(&epsilon)};
        launch_named("qwen4_group_rms",hc_count,1,256,key_args);
        auto norm_query=runtime->device_tensors[layer.ple_norm_query];
        void*query_args[]={const_cast<std::uint64_t*>(&hc_streams),&norm_query,
            const_cast<std::uint64_t*>(&hc_normed),
            const_cast<int*>(&hidden_size),const_cast<float*>(&epsilon)};
        launch_named("qwen4_group_rms",hc_count,1,256,query_args);
        void*gate_args[]={const_cast<std::uint64_t*>(&hc_wide),
            const_cast<std::uint64_t*>(&hc_normed),
            const_cast<std::uint64_t*>(&hc_gates),
            const_cast<int*>(&hidden_size)};
        launch_named("qwen4_ple_gate",hc_count,1,256,gate_args);
        // value borrows `normalized`; the boundary that follows rewrites it.
        dense_matvec(layer.ple_value,ple_embed,normalized,hidden_size,hidden_size);
        void*gv_args[]={const_cast<std::uint64_t*>(&normalized),
            const_cast<std::uint64_t*>(&hc_gates),
            const_cast<std::uint64_t*>(&hc_wide),
            const_cast<int*>(&hc_count),const_cast<int*>(&hidden_size)};
        launch_named("qwen4_ple_gv",
            (static_cast<std::uint32_t>(hc_wide_size)+255)/256,1,256,gv_args);
        auto norm_conv=runtime->device_tensors[layer.ple_norm_conv];
        void*gvn_args[]={const_cast<std::uint64_t*>(&hc_wide),&norm_conv,
            const_cast<std::uint64_t*>(&hc_normed),
            const_cast<int*>(&hidden_size),const_cast<float*>(&epsilon)};
        launch_named("qwen4_group_rms",hc_count,1,256,gvn_args);
        const auto&conv=runtime->model->tensors[layer.ple_conv];
        int kernel_size=static_cast<int>(conv.shape[0]);
        int dilation=static_cast<int>(std::max<std::uint32_t>(
            runtime->model->config.ple_ngram_size,1));
        auto conv_weights=runtime->device_tensors[layer.ple_conv];
        std::uint64_t conv_state=runtime->state+layer.state_third;
        void*conv_args[]={const_cast<std::uint64_t*>(&hc_normed),&conv_weights,
            &conv_state,const_cast<std::uint64_t*>(&hc_normed),
            const_cast<int*>(&hc_wide_size),&kernel_size,&dilation};
        launch_named("qwen4_ple_conv_step",
            (static_cast<std::uint32_t>(hc_wide_size)+255)/256,1,256,conv_args);
        void*ple_add_args[]={const_cast<std::uint64_t*>(&hc_streams),
            const_cast<std::uint64_t*>(&hc_wide),
            const_cast<std::uint64_t*>(&hc_normed),
            const_cast<int*>(&hc_wide_size)};
        launch_named("qwen4_ple_add",
            (static_cast<std::uint32_t>(hc_wide_size)+255)/256,1,256,ple_add_args);
        q8_cached_input=0;
    };
    auto profile_record=[&](std::uint64_t event){if(runtime->cuda_profile&&flyweight_gpu_event_record(event,runtime->stream)!=0)throw std::runtime_error("native Qwen CUDA profiling event failed");};
    {
        const std::uint32_t embedding_token=input_token;
        const auto embedding=qwen_stage_embedding_rows(*runtime,&embedding_token,1);
        const int token=runtime->embeddings_host_resident?0:static_cast<int>(input_token);int width=hidden_size;
        void*args[]={const_cast<std::uint64_t*>(&embedding),const_cast<std::uint64_t*>(&hidden),const_cast<int*>(&token),&width};
        launch_named(qwen_embedding_kernel(qwen_device_type(*runtime,runtime->token_embeddings),false),(hidden_size+255)/256,1,256,args);
        // Muse Glimmer RMS-normalizes the raw embedding before the first block,
        // with no learned weight of its own. Skipping it leaves the residual
        // stream off by the embedding norm and the model drifts within a few
        // tokens. gemma_head_rms is the weightless form; one "head" is the
        // whole row.
        if(runtime->muse){
            int heads=1,width=hidden_size;
            void*norm_args[]={const_cast<std::uint64_t*>(&hidden),
                              const_cast<std::uint64_t*>(&hidden),&heads,&width,
                              const_cast<float*>(&epsilon)};
            launch_named("gemma_head_rms",1,1,256,norm_args);
        }
        // qwen4exp: the residual state is hc_count copies of the embedding,
        // and the PLE gather for this token stages up front so blk.1's ple
        // block finds it uploaded.
        if(runtime->qwen4exp){
            const int hc_wide_size=hc_count*hidden_size;
            void*init_args[]={const_cast<std::uint64_t*>(&hidden),
                const_cast<std::uint64_t*>(&hc_streams),
                const_cast<int*>(&hc_count),const_cast<int*>(&hidden_size)};
            launch_named("qwen4_hc_init",
                (static_cast<std::uint32_t>(hc_wide_size)+255)/256,1,256,init_args);
            if(runtime->ple_table!=std::numeric_limits<std::uint64_t>::max()&&
               !runtime->model->config.ple_layers.empty())
                qwen4_stage_ple_embed(*runtime,input_token,
                                      runtime->processed_tokens,ple_embed);
        }
    }
    if(qwen_lm_diag_enabled()){
        static int ec=0;
        if(ec<2){++ec;float v[8]={};if(flyweight_gpu_download(v,hidden,sizeof(v),runtime->stream)==0&&flyweight_gpu_stream_sync(runtime->stream)==0)
            std::fprintf(stderr,"[diag] after_embed hidden[0..7]=% .6e % .6e % .6e % .6e % .6e % .6e % .6e % .6e\n",v[0],v[1],v[2],v[3],v[4],v[5],v[6],v[7]);}
    }
    // Cache per-layer env vars once; std::getenv on Windows crosses the kernel
    // boundary (~1-3 us) vs Linux vDSO (~20 ns).  With ~40 calls across 30
    // layers this was ~120 us/token of pure overhead.
    static const bool env_delta_serial=[]{const char*s=std::getenv("FLYWEIGHT_DELTA_SERIAL");return s&&s[0]=='1';}();
    static const bool env_nvfp4_persistent=[]{const char*s=std::getenv("FLYWEIGHT_NVFP4_PERSISTENT");return s&&s[0]=='1';}();
    static const bool env_nvfp4_tc=[]{const char*s=std::getenv("FLYWEIGHT_NVFP4_DECODE_TENSOR_CORES");return s&&s[0]=='1';}();
    static const bool env_nvfp4_tc_trace=std::getenv("FLYWEIGHT_NVFP4_TENSOR_CORE_TRACE")!=nullptr;
    static const bool env_nvfp4_tiled=[]{const char*s=std::getenv("FLYWEIGHT_NVFP4_TILED");return s&&s[0]=='1';}();
    static const bool env_lm_diag=std::getenv("FLYWEIGHT_LM_DIAG")!=nullptr;
    static const bool env_graph_trace=std::getenv("FLYWEIGHT_CUDA_GRAPH_TRACE")!=nullptr;
    // Lets the delta-block capture be A/B'd on its own, with the shared-expert
    // graphs left enabled either way.
    static const bool env_graph_delta=[]{const char*s=std::getenv("FLYWEIGHT_CUDA_GRAPH_DELTA");return !s||s[0]!='0';}();
    // Same, for the dense SwiGLU capture.
    static const bool env_graph_ffn=[]{const char*s=std::getenv("FLYWEIGHT_CUDA_GRAPH_FFN");return !s||s[0]!='0';}();
    for(std::uint32_t layer_number=0;layer_number<runtime->layers.size();++layer_number){
        auto&layer=runtime->layers[layer_number];
        auto*profile=runtime->cuda_profile?&runtime->cuda_layer_profiles[layer_number]:nullptr;
        // Profiling and diagnostics both inject event records and stream syncs
        // into the block below, neither of which survives capture, so they keep
        // the eager path.
        // qwen4exp: the delta capture would bake in the skipped residual add
        // and the streams would never see the inject on replay; graphs stay
        // off for the arch until the bookends are capture-clean.
        const bool graph_capturable=runtime->cuda_graphs&&!profile&&!env_lm_diag
            &&!runtime->qwen4exp;
        const bool graph_eligible=graph_capturable&&env_graph_delta;
        const bool ffn_graph_eligible=graph_capturable&&env_graph_ffn;
        if(profile)profile_record(profile->pre_start);
        auto tensor=[&](std::size_t role){return runtime->device_tensors[layer.static_tensors.at(role)];};
        auto dense=[&](std::size_t role,std::uint64_t input,std::uint64_t output,int input_size,int output_size){dense_matvec(layer.static_tensors.at(role),input,output,input_size,output_size);};
        if(runtime->qwen4exp){
            if(layer.ple_conv!=std::numeric_limits<std::uint64_t>::max())
                ple_block(layer);
            hc_pre(tensor(0),layer.hc_attn_down,layer.hc_attn_up,
                   layer.hc_attn_inject);
        }else rms(hidden,tensor(0),normalized);
        std::size_t moe_base=0;
        if(!layer.attention){
            int channels=static_cast<int>(runtime->model->tensors[layer.static_tensors[1]].shape[1]);
            int gate_elements=static_cast<int>(runtime->model->tensors[layer.static_tensors[2]].shape[1]);
            int value_heads=static_cast<int>(runtime->model->tensors[layer.static_tensors[8]].shape[0]);
            int head_dim=static_cast<int>(runtime->model->tensors[layer.static_tensors[9]].shape[0]);
            int value_dim=value_heads*head_dim;
            int key_heads=(channels-value_dim)/(2*head_dim);
            // Everything from here to the residual add is token-invariant: no
            // position, no KV cache, and the recurrent/conv state lives at a
            // fixed device address that the kernels update in place. That makes
            // the whole block capturable once and replayable every token, which
            // is worth ~9 launches x 30 layers of WDDM submission per token.
            auto enqueue_delta=[&]{
            dense(1,normalized,first,hidden_size,channels);
            dense(2,normalized,second,hidden_size,gate_elements);
            dense(4,normalized,third,hidden_size,value_heads);
            dense(5,normalized,third+value_heads*sizeof(float),hidden_size,value_heads);
            if(qwen_lm_diag_enabled()&&layer_number==0){
                float v[4]={};flyweight_gpu_stream_sync(runtime->stream);
                if(flyweight_gpu_download(v,first,sizeof(v),runtime->stream)==0&&flyweight_gpu_stream_sync(runtime->stream)==0)
                    std::fprintf(stderr,"[diag] L0 after_qkv first[0..3]=% .6e % .6e % .6e % .6e\n",v[0],v[1],v[2],v[3]);
                if(flyweight_gpu_download(v,second,sizeof(v),runtime->stream)==0&&flyweight_gpu_stream_sync(runtime->stream)==0)
                    std::fprintf(stderr,"[diag] L0 after_gate second[0..3]=% .6e % .6e % .6e % .6e\n",v[0],v[1],v[2],v[3]);
            }
            int kernel_size=static_cast<int>(runtime->model->tensors[layer.static_tensors[6]].shape[0]);
            std::uint64_t conv_state=runtime->state+layer.state_first;
            auto conv_weights=tensor(6);
            void*conv_args[]={const_cast<std::uint64_t*>(&first),&conv_weights,&conv_state,const_cast<std::uint64_t*>(&fourth),&channels,&kernel_size};
            launch_named("delta_conv_step",(channels+255)/256,1,256,conv_args);
            std::uint64_t recurrent_state=runtime->state+layer.state_second;
            auto decay=tensor(8),dt=tensor(7),norm=tensor(9);
            std::uint64_t beta=third+value_heads*sizeof(float);
            int gate_sigmoid=runtime->qwen4exp?1:0;
            void*recurrent_args[]={const_cast<std::uint64_t*>(&fourth),const_cast<std::uint64_t*>(&second),&beta,const_cast<std::uint64_t*>(&third),&decay,&dt,&norm,&recurrent_state,const_cast<std::uint64_t*>(&first),&key_heads,&value_heads,&head_dim,const_cast<float*>(&epsilon),&gate_sigmoid};
            // Split the key loop across `slices` groups of head_dim threads. Four
            // groups is the most a 1024-thread block allows at head_dim 128, which
            // is what every Qwen3.6 checkpoint uses; wider heads get fewer, and
            // anything past a 1024-thread block falls back to the serial kernel.
            // FLYWEIGHT_DELTA_SERIAL forces that fallback for A/B measurement --
            // the split form is 2.9x here (1.92 -> 0.67 ms/token over 30 layers).
            if(profile)profile_record(profile->recurrent_start);
            int recurrent_slices=head_dim>0?1024/head_dim:0;
            if(recurrent_slices>4)recurrent_slices=4;
            if(recurrent_slices>=1&&!env_delta_serial){
                const int recurrent_block=head_dim*recurrent_slices;
                const std::uint32_t recurrent_shared=
                    static_cast<std::uint32_t>((4*head_dim+recurrent_block)*sizeof(float));
                launch_named("qwen_delta_recurrent_split",value_heads,1,recurrent_block,recurrent_args,recurrent_shared);
            }else{
                launch_named("qwen_delta_recurrent",value_heads,1,256,recurrent_args);
            }
            if(profile)profile_record(profile->recurrent_end);
            if(qwen_lm_diag_enabled()&&layer_number==0){
                float v[4]={};flyweight_gpu_stream_sync(runtime->stream);
                if(flyweight_gpu_download(v,fourth,sizeof(v),runtime->stream)==0&&flyweight_gpu_stream_sync(runtime->stream)==0)
                    std::fprintf(stderr,"[diag] L0 after_recur fourth[0..3]=% .6e % .6e % .6e % .6e\n",v[0],v[1],v[2],v[3]);
                if(flyweight_gpu_download(v,first,sizeof(v),runtime->stream)==0&&flyweight_gpu_stream_sync(runtime->stream)==0)
                    std::fprintf(stderr,"[diag] L0 after_recur first[0..3]=% .6e % .6e % .6e % .6e\n",v[0],v[1],v[2],v[3]);
            }
            dense(3,first,residual,value_dim,hidden_size);
            if(qwen_lm_diag_enabled()&&layer_number==0){
                float v[4]={};flyweight_gpu_stream_sync(runtime->stream);
                if(flyweight_gpu_download(v,residual,sizeof(v),runtime->stream)==0&&flyweight_gpu_stream_sync(runtime->stream)==0)
                    std::fprintf(stderr,"[diag] L0 after_ssm_out residual[0..3]=% .6e % .6e % .6e % .6e\n",v[0],v[1],v[2],v[3]);
                fprintf(stderr,"[diag] L0 value_dim=%d hidden_size=%d\n",value_dim,hidden_size);
                // Also download first bytes of ssm_out tensor to verify data
                auto ssm_out_gpu=tensor(3);
                unsigned char buf[34]={};
                if(flyweight_gpu_download(buf,ssm_out_gpu,sizeof(buf),runtime->stream)==0&&flyweight_gpu_stream_sync(runtime->stream)==0){
                    fprintf(stderr,"[diag] L0 ssm_out bytes[0..33]: %02x %02x %02x %02x %02x %02x\n",buf[0],buf[1],buf[2],buf[3],buf[4],buf[5]);
                }
            }
            // qwen4exp replaces the residual add with the gated-residual
            // inject AFTER the (never-captured) block; see below.
            if(!runtime->qwen4exp)add(residual,hidden);
            };
            // Capture records without executing, so the in-place state update
            // happens exactly once per token -- via the replay below, never via
            // the capture pass. The graph is launched on runtime->stream so it
            // stays ordered behind the eager rms() above.
            bool delta_launched=false;
            const auto delta_started=env_graph_trace?std::chrono::steady_clock::now():std::chrono::steady_clock::time_point{};
            // One graph per sequence slot: the capture below records this
            // slot's conv/recurrent state addresses.
            const std::size_t graph_slot=runtime->active_sequence;
            if(layer.delta_graphs.size()<runtime->sequences.size()){
                layer.delta_graphs.resize(runtime->sequences.size(),0);
                layer.delta_graph_attempted.resize(runtime->sequences.size(),0);
            }
            if(graph_eligible&&layer.delta_graphs[graph_slot]){
                if(flyweight_gpu_graph_launch(layer.delta_graphs[graph_slot],runtime->stream)==0){
                    ++runtime->cuda_graph_replays;delta_launched=true;
                }else{
                    flyweight_gpu_graph_destroy(layer.delta_graphs[graph_slot]);
                    layer.delta_graphs[graph_slot]=0;++runtime->cuda_graph_fallbacks;
                }
            }
            if(graph_eligible&&!delta_launched&&!layer.delta_graph_attempted[graph_slot]){
                layer.delta_graph_attempted[graph_slot]=1;
                if(flyweight_gpu_graph_begin(runtime->graph_stream)==0){
                    bool capture_enqueued=true;
                    launch_stream=runtime->graph_stream;
                    try{enqueue_delta();}catch(...){capture_enqueued=false;}
                    launch_stream=runtime->stream;
                    std::uint64_t captured_graph=0;
                    const int capture_status=flyweight_gpu_graph_end(runtime->graph_stream,&captured_graph);
                    if(capture_enqueued&&capture_status==0){
                        layer.delta_graphs[graph_slot]=captured_graph;++runtime->cuda_graph_builds;
                        if(flyweight_gpu_graph_launch(layer.delta_graphs[graph_slot],runtime->stream)==0){
                            ++runtime->cuda_graph_replays;delta_launched=true;
                        }else{
                            flyweight_gpu_graph_destroy(layer.delta_graphs[graph_slot]);
                            layer.delta_graphs[graph_slot]=0;++runtime->cuda_graph_fallbacks;
                        }
                    }else{
                        if(capture_status==0&&captured_graph)flyweight_gpu_graph_destroy(captured_graph);
                        ++runtime->cuda_graph_fallbacks;
                    }
                }
            }
            if(!delta_launched)enqueue_delta();
            if(env_graph_trace){
                runtime->delta_host_ns+=std::chrono::duration_cast<std::chrono::nanoseconds>(
                    std::chrono::steady_clock::now()-delta_started).count();
                ++runtime->delta_host_calls;
            }
            if(runtime->qwen4exp)hc_post(residual);
            moe_base=10;
        }else if(runtime->muse){
            // Muse Glimmer attention: plain Q/K/V projections, per-head QK RMS
            // norm with interleaved RoPE on the sliding-window layers only, and
            // a per-channel sigmoid output gate taken from the same
            // pre-attention hidden state.
            const int heads=static_cast<int>(layer.attention_heads);
            const int kv_heads=static_cast<int>(layer.kv_heads);
            const int head_dim=static_cast<int>(layer.head_dim);
            const int q_size=heads*head_dim,kv_size=kv_heads*head_dim;
            if(static_cast<std::uint64_t>(2*q_size)>runtime->scratch_elements)
                throw std::runtime_error("native Muse Glimmer attention scratch is too small");
            dense(1,normalized,first,hidden_size,q_size);
            dense(2,normalized,second,hidden_size,kv_size);
            dense(3,normalized,third,hidden_size,kv_size);
            const std::uint64_t queries=fourth;
            const std::uint64_t gates=fourth+static_cast<std::uint64_t>(q_size)*sizeof(float);
            dense(7,normalized,gates,hidden_size,q_size);
            // Zero on the full-attention layers, which run NoPE.
            const int rotary=static_cast<int>(layer.rotary_dim);
            const int position=static_cast<int>(runtime->position);
            const float theta=layer.rope_theta;
            auto qnorm=tensor(5),knorm=tensor(6);
            void*q_args[]={const_cast<std::uint64_t*>(&first),&qnorm,
                           const_cast<std::uint64_t*>(&queries),const_cast<int*>(&heads),
                           const_cast<int*>(&head_dim),const_cast<int*>(&rotary),
                           const_cast<int*>(&position),const_cast<float*>(&theta),
                           const_cast<float*>(&epsilon)};
            launch_named("muse_head_norm_rope",heads,1,256,q_args);
            const std::uint64_t keys=first;
            void*k_args[]={const_cast<std::uint64_t*>(&second),&knorm,
                           const_cast<std::uint64_t*>(&keys),const_cast<int*>(&kv_heads),
                           const_cast<int*>(&head_dim),const_cast<int*>(&rotary),
                           const_cast<int*>(&position),const_cast<float*>(&theta),
                           const_cast<float*>(&epsilon)};
            launch_named("muse_head_norm_rope",kv_heads,1,256,k_args);
            std::uint64_t cache_keys=runtime->state+layer.state_first,cache_values=runtime->state+layer.state_second;
            const auto view=attention_cache_view(layer,runtime->position);
            int slot=view.slot,capacity=view.capacity;
            void*k_store_args[]={const_cast<std::uint64_t*>(&keys),&cache_keys,const_cast<int*>(&kv_heads),const_cast<int*>(&head_dim),&slot,&capacity};
            launch_named(kv_store_kernel(runtime->options.cache_type_k,true),kv_heads,1,256,k_store_args);
            void*v_store_args[]={const_cast<std::uint64_t*>(&third),&cache_values,const_cast<int*>(&kv_heads),const_cast<int*>(&head_dim),&slot,&capacity};
            launch_named(kv_store_kernel(runtime->options.cache_type_v,false),kv_heads,1,256,v_store_args);
            std::uint64_t attended=second;int tokens=view.tokens,first_slot=view.first;
            // The q_norm weights already carry qk_scale_factor, so the score
            // scale is the plain 1/sqrt(head_dim).
            float scale=1.0f/std::sqrt(static_cast<float>(head_dim));
            void*score_args[]={const_cast<std::uint64_t*>(&queries),&cache_keys,const_cast<std::uint64_t*>(&attention_scores),const_cast<int*>(&heads),const_cast<int*>(&kv_heads),const_cast<int*>(&head_dim),&tokens,&capacity,&first_slot,&scale};
            launch_named(kv_scores_ring_kernel(*runtime),heads,(tokens+255)/256,256,score_args);
            void*value_args[]={const_cast<std::uint64_t*>(&attention_scores),&cache_values,&attended,const_cast<int*>(&heads),const_cast<int*>(&kv_heads),const_cast<int*>(&head_dim),&tokens,&capacity,&first_slot};
            launch_named(kv_values_ring_kernel(*runtime),heads,1,256,value_args);
            std::uint64_t gated=third;
            int gate_elements=q_size;
            void*gate_args[]={&attended,const_cast<std::uint64_t*>(&gates),&gated,
                              &gate_elements};
            launch_named("qwen_attention_gate",(q_size+255)/256,1,256,gate_args);
            dense(4,gated,residual,q_size,hidden_size);
            // Post-attention norm before the residual join, not after.
            post_rms(residual,tensor(8));
            add(residual,hidden);
            moe_base=9;
        }else if(runtime->laguna){
            // Laguna attention: plain Q/K/V projections (no fused gate), per-head
            // QK RMS norm, per-layer-type RoPE, then a softplus per-head output
            // gate computed from the same pre-attention hidden state.
            const int heads=static_cast<int>(layer.attention_heads);
            const int kv_heads=static_cast<int>(layer.kv_heads);
            const int head_dim=static_cast<int>(layer.head_dim);
            const int q_size=heads*head_dim,kv_size=kv_heads*head_dim;
            if(static_cast<std::uint64_t>(q_size)+heads>runtime->scratch_elements)
                throw std::runtime_error("native Laguna attention scratch is too small");
            dense(1,normalized,first,hidden_size,q_size);
            dense(2,normalized,second,hidden_size,kv_size);
            dense(3,normalized,third,hidden_size,kv_size);
            const std::uint64_t queries=fourth;
            const std::uint64_t gates=fourth+static_cast<std::uint64_t>(q_size)*sizeof(float);
            dense(7,normalized,gates,hidden_size,heads);
            const int rotary=static_cast<int>(layer.rotary_dim);
            const int position=static_cast<int>(runtime->position);
            const float theta=layer.rope_theta;
            const float freq_scale=layer.rope_freq_scale;
            const float ext_factor=layer.rope_ext_factor;
            const float mscale=layer.rope_attn_factor;
            float corr_low=0.0f,corr_high=0.0f;
            if(ext_factor!=0.0f)
                qwen_yarn_correction_dims(
                    rotary,layer.rope_orig_context,theta,
                    layer.rope_beta_fast,layer.rope_beta_slow,corr_low,corr_high);
            auto qnorm=tensor(5),knorm=tensor(6);
            void*q_args[]={const_cast<std::uint64_t*>(&first),&qnorm,
                           const_cast<std::uint64_t*>(&queries),const_cast<int*>(&heads),
                           const_cast<int*>(&head_dim),const_cast<int*>(&rotary),
                           const_cast<int*>(&position),const_cast<float*>(&theta),
                           const_cast<float*>(&epsilon),const_cast<float*>(&freq_scale),
                           const_cast<float*>(&ext_factor),const_cast<float*>(&mscale),
                           &corr_low,&corr_high};
            launch_named("laguna_head_norm_rope",heads,1,256,q_args);
            const std::uint64_t keys=first;
            void*k_args[]={const_cast<std::uint64_t*>(&second),&knorm,
                           const_cast<std::uint64_t*>(&keys),const_cast<int*>(&kv_heads),
                           const_cast<int*>(&head_dim),const_cast<int*>(&rotary),
                           const_cast<int*>(&position),const_cast<float*>(&theta),
                           const_cast<float*>(&epsilon),const_cast<float*>(&freq_scale),
                           const_cast<float*>(&ext_factor),const_cast<float*>(&mscale),
                           &corr_low,&corr_high};
            launch_named("laguna_head_norm_rope",kv_heads,1,256,k_args);
            std::uint64_t cache_keys=runtime->state+layer.state_first,cache_values=runtime->state+layer.state_second;
            const auto view=attention_cache_view(layer,runtime->position);
            int slot=view.slot,capacity=view.capacity;
            void*k_store_args[]={const_cast<std::uint64_t*>(&keys),&cache_keys,const_cast<int*>(&kv_heads),const_cast<int*>(&head_dim),&slot,&capacity};
            launch_named(kv_store_kernel(runtime->options.cache_type_k,true),kv_heads,1,256,k_store_args);
            void*v_store_args[]={const_cast<std::uint64_t*>(&third),&cache_values,const_cast<int*>(&kv_heads),const_cast<int*>(&head_dim),&slot,&capacity};
            launch_named(kv_store_kernel(runtime->options.cache_type_v,false),kv_heads,1,256,v_store_args);
            std::uint64_t attended=second;int tokens=view.tokens,first_slot=view.first;
            float scale=1.0f/std::sqrt(static_cast<float>(head_dim));
            void*score_args[]={const_cast<std::uint64_t*>(&queries),&cache_keys,const_cast<std::uint64_t*>(&attention_scores),const_cast<int*>(&heads),const_cast<int*>(&kv_heads),const_cast<int*>(&head_dim),&tokens,&capacity,&first_slot,&scale};
            launch_named(kv_scores_ring_kernel(*runtime),heads,(tokens+255)/256,256,score_args);
            void*value_args[]={const_cast<std::uint64_t*>(&attention_scores),&cache_values,&attended,const_cast<int*>(&heads),const_cast<int*>(&kv_heads),const_cast<int*>(&head_dim),&tokens,&capacity,&first_slot};
            launch_named(kv_values_ring_kernel(*runtime),heads,1,256,value_args);
            std::uint64_t gated=third;
            void*gate_args[]={&attended,const_cast<std::uint64_t*>(&gates),&gated,
                              const_cast<int*>(&heads),const_cast<int*>(&head_dim)};
            launch_named("laguna_attention_gate",(q_size+255)/256,1,256,gate_args);
            dense(4,gated,residual,q_size,hidden_size);
            add(residual,hidden);
            moe_base=8;
        }else{
            const int heads=static_cast<int>(runtime->model->config.attention_heads);
            const int kv_heads=static_cast<int>(runtime->model->config.attention_kv_heads);
            const int head_dim=static_cast<int>(runtime->model->tensors[layer.static_tensors[2]].shape[1]/kv_heads);
            const int q_size=heads*2*head_dim,kv_size=kv_heads*head_dim;
            dense(1,normalized,first,hidden_size,q_size);
            dense(2,normalized,second,hidden_size,kv_size);
            dense(3,normalized,third,hidden_size,kv_size);
            const int rotary=static_cast<int>(runtime->model->config.rotary_dimension?runtime->model->config.rotary_dimension:head_dim);
            const int position=static_cast<int>(runtime->position);
            const float theta=runtime->model->config.rope_freq_base?runtime->model->config.rope_freq_base:1000000.0f;
            auto qnorm=tensor(5),knorm=tensor(6);std::uint64_t queries=fourth,gates=fourth+q_size/2*sizeof(float);
            void*q_args[]={const_cast<std::uint64_t*>(&first),&qnorm,&queries,&gates,const_cast<int*>(&heads),const_cast<int*>(&head_dim),const_cast<int*>(&rotary),const_cast<int*>(&position),const_cast<float*>(&theta),const_cast<float*>(&epsilon)};
            launch_named("qwen_attention_query",heads,1,256,q_args);
            std::uint64_t keys=first;
            void*k_args[]={const_cast<std::uint64_t*>(&second),&knorm,&keys,const_cast<int*>(&kv_heads),const_cast<int*>(&head_dim),const_cast<int*>(&rotary),const_cast<int*>(&position),const_cast<float*>(&theta),const_cast<float*>(&epsilon)};
            launch_named("qwen_attention_key",kv_heads,1,256,k_args);
            std::uint64_t cache_keys=runtime->state+layer.state_first,cache_values=runtime->state+layer.state_second;
            const auto view=attention_cache_view(layer,runtime->position);
            int slot=view.slot,capacity=view.capacity;
            void*k_store_args[]={&keys,&cache_keys,const_cast<int*>(&kv_heads),const_cast<int*>(&head_dim),&slot,&capacity};
            launch_named(kv_store_kernel(runtime->options.cache_type_k,true),kv_heads,1,256,k_store_args);
            void*v_store_args[]={const_cast<std::uint64_t*>(&third),&cache_values,const_cast<int*>(&kv_heads),const_cast<int*>(&head_dim),&slot,&capacity};
            launch_named(kv_store_kernel(runtime->options.cache_type_v,false),kv_heads,1,256,v_store_args);
            std::uint64_t attended=second;int tokens=view.tokens,first_slot=view.first;float scale=1.0f/std::sqrt(static_cast<float>(head_dim));
            // QSA indexer: maintain the raw-key ring + block-key store every
            // token; select once the context outgrows the budget. `first` is
            // free (the roped attention key was consumed by kv_store) and
            // becomes the normed+roped indexer queries.
            int qsa_selected=0;
            if(qwen_qsa_layer(*runtime,layer)){
                const int qsa_heads=static_cast<int>(runtime->model->config.indexer_head_count);
                const int key_len=static_cast<int>(runtime->model->config.indexer_key_length);
                const int ratio=static_cast<int>(layer.qsa_ratio);
                const int qsa_rotary=std::min(rotary,key_len);
                const std::uint64_t qsa_query=workspace_layout.qsa_query.address(runtime->workspace);
                const std::uint64_t qsa_raw=qsa_query+static_cast<std::uint64_t>(qsa_heads)*key_len*sizeof(float);
                const std::uint64_t qsa_scores=workspace_layout.qsa_block_scores.address(runtime->workspace);
                const std::uint64_t qsa_slots=workspace_layout.qsa_slots.address(runtime->workspace);
                std::uint64_t ring=runtime->state+layer.qsa_ring;
                std::uint64_t blocks=runtime->state+layer.qsa_blocks;
                auto knorm_w=runtime->device_tensors[layer.indexer_k_norm];
                dense_matvec(layer.indexer_k,normalized,qsa_raw,hidden_size,key_len);
                void*store_args[]={const_cast<std::uint64_t*>(&qsa_raw),&ring,&blocks,&knorm_w,
                    const_cast<int*>(&position),const_cast<int*>(&ratio),
                    const_cast<int*>(&key_len),const_cast<int*>(&qsa_rotary),
                    const_cast<float*>(&theta),const_cast<float*>(&epsilon)};
                launch_named("qsa_key_store",1,1,256,store_args);
                if(qwen_qsa_selecting(*runtime,layer,runtime->position)){
                    const int complete=(position+1)/ratio;
                    dense_matvec(layer.indexer_q,normalized,qsa_query,hidden_size,qsa_heads*key_len);
                    auto qnorm_w=runtime->device_tensors[layer.indexer_q_norm];
                    std::uint64_t prepped=first;
                    void*prep_args[]={const_cast<std::uint64_t*>(&qsa_query),&qnorm_w,&prepped,
                        const_cast<int*>(&qsa_heads),const_cast<int*>(&key_len),
                        const_cast<int*>(&qsa_rotary),const_cast<int*>(&position),
                        const_cast<float*>(&theta),const_cast<float*>(&epsilon)};
                    launch_named("qwen_attention_key",qsa_heads,1,256,prep_args);
                    void*score_args[]={&prepped,&blocks,const_cast<std::uint64_t*>(&qsa_scores),
                        const_cast<int*>(&complete),const_cast<int*>(&qsa_heads),
                        const_cast<int*>(&key_len)};
                    launch_named("qsa_block_scores",
                        (static_cast<std::uint32_t>(complete)+255)/256,1,256,score_args);
                    qsa_selected=qwen_qsa_select(*runtime,layer,qsa_scores,qsa_slots,runtime->position);
                }
            }
            if(profile)profile_record(profile->recurrent_start);
            if(qsa_selected){
                const std::uint64_t qsa_slots=workspace_layout.qsa_slots.address(runtime->workspace);
                void*score_args[]={&queries,&cache_keys,const_cast<std::uint64_t*>(&qsa_slots),
                    const_cast<std::uint64_t*>(&attention_scores),
                    const_cast<int*>(&heads),const_cast<int*>(&kv_heads),
                    const_cast<int*>(&head_dim),&qsa_selected,&capacity,&scale};
                launch_named(kv_scores_indexed_kernel(*runtime),heads,
                    (static_cast<std::uint32_t>(qsa_selected)+255)/256,256,score_args);
                void*value_args[]={const_cast<std::uint64_t*>(&attention_scores),&cache_values,
                    const_cast<std::uint64_t*>(&qsa_slots),&attended,
                    const_cast<int*>(&heads),const_cast<int*>(&kv_heads),
                    const_cast<int*>(&head_dim),&qsa_selected,&capacity};
                launch_named(kv_values_indexed_kernel(*runtime),heads,1,256,value_args);
            }else{
            const char* fused_tiles=kv_fused_tiles_kernel(*runtime);
            const int fused_tile_tokens=kv_fused_tile_tokens(*runtime);
            // The staging path keeps its priority by measurement, not theory:
            // rewriting the window to f16 looks like O(context) waste, but the
            // fused turbo tiles decode every KV row once per *query* head --
            // an 8x in-loop ALU repeat under GQA -- and measured 25.4 against
            // cuBLAS' 29.9 tok/s at 12k on the 27B (2/2 interleaved pairs).
            // The tiles take over where cuBLAS declines (window past the
            // staging buffer, short windows), which used to fall all the way
            // to the serial per-head kernels.
            const bool cublas_done=
                qwen_turbo_cublas_attention(
                    *runtime,queries,first,cache_keys,cache_values,
                    attention_scores,attended,heads,kv_heads,head_dim,tokens,
                    capacity,first_slot,scale)||
                (qwen_cublas_attention_eligible(
                    *runtime,tokens,first_slot,capacity)&&
                flyweight_gpu_attention_f16_cublas(
                    queries,first,cache_keys,cache_values,attention_scores,
                    attended,runtime->stream,heads,kv_heads,head_dim,tokens,
                    capacity,first_slot,scale)==0);
            const int gqa_tile_tokens=(!cublas_done&&runtime->fused_attention)
                ? kv_gqa_rows_tile_tokens(*runtime,tokens) : 0;
            const char* gqa_tiles=gqa_tile_tokens
                ? kv_gqa_tiles_kernel(
                      *runtime,head_dim,heads,kv_heads,gqa_tile_tokens)
                : nullptr;
            const int gqa_tile_count=gqa_tiles
                ? (tokens+gqa_tile_tokens-1)/gqa_tile_tokens : 0;
            // Ranked below cuBLAS by measurement, not by principle: at 49k on
            // an f16 cache cuBLAS is 320 us against this kernel's 563. Where
            // cuBLAS cannot go -- any cache but f16, a wrapped ring, a window it
            // declines -- this is the best of what is left, by a wide margin on
            // a quantized cache: q8 at 49k is 528 us against 909 for the
            // per-head kernel, which decodes every row once per query head.
            //
            // End to end that is smaller than it sounds, and worth knowing
            // before reading a disappointing A/B as a broken kernel: Ornith runs
            // full attention on 10 of 40 layers and the rest of the token is
            // MoE, so 17k q8 decode moves 35.4 -> 37.3 tok/s (+5.5%), token
            // identical, one process per setting.
            const bool gqa_done=gqa_tiles!=nullptr;
            // Which of the four decode paths ran, announced once. Every one of
            // them is correct and they differ only in speed, so a silent
            // fallback looks exactly like a fast path that is not engaged --
            // which is what an A/B measures instead of what it meant to.
            if(std::getenv("FLYWEIGHT_ATTENTION_DIAG")){
                static const char*announced=nullptr;
                const char*chosen=cublas_done?"cublas"
                    :gqa_done?gqa_tiles
                    :runtime->fused_attention?"fused per-head tiles":"serial ring";
                if(announced!=chosen){
                    announced=chosen;
                    std::fprintf(stderr,"[attention] decode path: %s\n",chosen);
                }
            }
            if(cublas_done){
                // Turbo or tensor-core attention already wrote `attended`.
            }else if(gqa_done){
                void*gqa_args[]={&queries,&cache_keys,&cache_values,
                    const_cast<std::uint64_t*>(&attention_scores),
                    const_cast<int*>(&heads),const_cast<int*>(&kv_heads),
                    const_cast<int*>(&head_dim),&tokens,&capacity,&first_slot,&scale};
                // Always eight warps: the block's warps split the tile's tokens,
                // and each of them carries the whole group's queries.
                launch_named(gqa_tiles,kv_heads,gqa_tile_count,256,gqa_args);
                void*merge_args[]={const_cast<std::uint64_t*>(&attention_scores),
                    &attended,const_cast<int*>(&heads),
                    const_cast<int*>(&head_dim),
                    const_cast<int*>(&gqa_tile_count)};
                launch_named("kv_attention_fused_merge256",heads,1,256,merge_args);
            }else if(runtime->fused_attention&&kv_fused_width(head_dim)&&
               (kv_fused_width(head_dim)==128
                    ? fused_tiles
                    : kv_fused_width(head_dim)==256
                        ? kv_fused_tiles_kernel256(*runtime)
                        : kv_fused_tiles_kernel512(*runtime))&&
               heads/kv_heads<=8&&
               (tokens+fused_tile_tokens-1)/fused_tile_tokens<=512&&
               static_cast<std::uint64_t>((tokens+fused_tile_tokens-1)/fused_tile_tokens)*
                   kv_fused_record_stride(kv_fused_width(head_dim))<=
                   runtime->options.context_limit){
                const int width=kv_fused_width(head_dim);
                const char* tiles=width==128
                    ? fused_tiles : width==256
                        ? kv_fused_tiles_kernel256(*runtime)
                        : kv_fused_tiles_kernel512(*runtime);
                const int tile_count=(tokens+fused_tile_tokens-1)/fused_tile_tokens;
                void*fused_args[]={&queries,&cache_keys,&cache_values,
                    const_cast<std::uint64_t*>(&attention_scores),
                    const_cast<int*>(&heads),const_cast<int*>(&kv_heads),
                    const_cast<int*>(&head_dim),&tokens,&capacity,&first_slot,&scale};
                launch_named(tiles,kv_fused_grid_heads(*runtime,heads,kv_heads),tile_count,256,fused_args);
                void*merge_args[]={const_cast<std::uint64_t*>(&attention_scores),
                    &attended,const_cast<int*>(&heads),
                    const_cast<int*>(&head_dim),const_cast<int*>(&tile_count)};
                launch_named(kv_fused_merge_kernel_name(*runtime,width),
                    heads,1,256,merge_args);
            }else{
                void*score_args[]={&queries,&cache_keys,const_cast<std::uint64_t*>(&attention_scores),const_cast<int*>(&heads),const_cast<int*>(&kv_heads),const_cast<int*>(&head_dim),&tokens,&capacity,&first_slot,&scale};
                launch_named(kv_scores_ring_kernel(*runtime),heads,(tokens+255)/256,256,score_args);
                void*value_args[]={const_cast<std::uint64_t*>(&attention_scores),&cache_values,&attended,const_cast<int*>(&heads),const_cast<int*>(&kv_heads),const_cast<int*>(&head_dim),&tokens,&capacity,&first_slot};
                launch_named(kv_values_ring_kernel(*runtime),heads,1,256,value_args);
            }
            }
            if(profile)profile_record(profile->recurrent_end);
            std::uint64_t gated=third;int elements=heads*head_dim;
            void*gate_args[]={&attended,&gates,&gated,&elements};
            launch_named("qwen_attention_gate",(elements+255)/256,1,256,gate_args);
            dense(4,gated,residual,elements,hidden_size);
            if(runtime->qwen4exp)hc_post(residual);
            else add(residual,hidden);
            moe_base=7;
        }
        if(runtime->qwen4exp)
            hc_pre(tensor(moe_base),layer.hc_ffn_down,layer.hc_ffn_up,
                   layer.hc_ffn_inject);
        else rms(residual,tensor(moe_base),normalized);
        if(qwen_lm_diag_enabled()&&layer_number==0){
            float v[4]={};flyweight_gpu_stream_sync(runtime->stream);
            if(flyweight_gpu_download(v,normalized,sizeof(v),runtime->stream)==0&&flyweight_gpu_stream_sync(runtime->stream)==0)
                std::fprintf(stderr,"[diag] L0 after_rms normalized[0..3]=% .6e % .6e % .6e % .6e\n",v[0],v[1],v[2],v[3]);
            if(flyweight_gpu_download(v,residual,sizeof(v),runtime->stream)==0&&flyweight_gpu_stream_sync(runtime->stream)==0)
                std::fprintf(stderr,"[diag] L0 after_rms residual[0..3]=% .6e % .6e % .6e % .6e\n",v[0],v[1],v[2],v[3]);
        }
        if(layer.dense_ffn){
            // Dense block: one SwiGLU over the layer's own gate/up/down, with no
            // router, shared expert or expert paging to run. Laguna mixes dense
            // leading blocks with MoE blocks in one model, and the two widths
            // differ, so take this block's width from its own gate projection.
            const int dense_intermediate=runtime->laguna
                ?static_cast<int>(runtime->model->tensors[layer.static_tensors.at(moe_base+1)].shape[1])
                :static_cast<int>(runtime->moe_intermediate);
            if(layer.ffn_on_host){
                auto*scratch=static_cast<float*>(runtime->dense_host);
                float*host_input=scratch;
                float*host_output=host_input+hidden_size;
                if(flyweight_gpu_download(host_input,normalized,hidden_size*sizeof(float),runtime->stream)!=0||
                   flyweight_gpu_stream_sync(runtime->stream)!=0)throw std::runtime_error("native dense host FFN input transfer failed");
                const auto host_started=timing_enabled()?std::chrono::steady_clock::now():std::chrono::steady_clock::time_point{};
                qwen_cpu_dense_ffn(*runtime,layer,host_input,host_output);
                if(timing_enabled())runtime->dense_host_nanoseconds+=std::chrono::duration_cast<std::chrono::nanoseconds>(std::chrono::steady_clock::now()-host_started).count();
                if(flyweight_gpu_upload(third,host_output,hidden_size*sizeof(float),runtime->stream)!=0)throw std::runtime_error("native dense host FFN output transfer failed");
            }else{
            auto enqueue_dense_ffn=[&]{
                const auto up_half=first+static_cast<std::uint64_t>(dense_intermediate)*sizeof(float);
                dense(moe_base+1,normalized,first,hidden_size,dense_intermediate);
                dense(moe_base+2,normalized,up_half,hidden_size,dense_intermediate);
                int dense_count=dense_intermediate;
                void*dense_silu_args[]={const_cast<std::uint64_t*>(&first),const_cast<std::uint64_t*>(&second),&dense_count};
                launch_named("silu_mul",(static_cast<std::uint32_t>(dense_count)+255)/256,1,256,dense_silu_args);
                dense(moe_base+3,second,third,dense_intermediate,hidden_size);
            };
            // Seven launches a layer -- three quantize, three matvec, one
            // silu_mul -- none of which reads the position or the token, so a
            // single capture replays for the whole sequence. On a dense model
            // this is the largest capturable run of launches in the step; the
            // delta and shared-expert graphs above never cover it.
            bool dense_ffn_launched=false;
            if(ffn_graph_eligible&&layer.dense_ffn_graph){
                if(flyweight_gpu_graph_launch(layer.dense_ffn_graph,runtime->stream)==0){
                    ++runtime->cuda_graph_replays;dense_ffn_launched=true;
                }else{
                    flyweight_gpu_graph_destroy(layer.dense_ffn_graph);
                    layer.dense_ffn_graph=0;++runtime->cuda_graph_fallbacks;
                }
            }
            if(ffn_graph_eligible&&!dense_ffn_launched&&!layer.dense_ffn_graph_attempted){
                layer.dense_ffn_graph_attempted=true;
                if(flyweight_gpu_graph_begin(runtime->graph_stream)==0){
                    bool capture_enqueued=true;
                    launch_stream=runtime->graph_stream;
                    try{enqueue_dense_ffn();}catch(...){capture_enqueued=false;}
                    launch_stream=runtime->stream;
                    std::uint64_t captured_graph=0;
                    const int capture_status=flyweight_gpu_graph_end(runtime->graph_stream,&captured_graph);
                    if(capture_enqueued&&capture_status==0){
                        layer.dense_ffn_graph=captured_graph;++runtime->cuda_graph_builds;
                        if(flyweight_gpu_graph_launch(layer.dense_ffn_graph,runtime->stream)==0){
                            ++runtime->cuda_graph_replays;dense_ffn_launched=true;
                        }else{
                            flyweight_gpu_graph_destroy(layer.dense_ffn_graph);
                            layer.dense_ffn_graph=0;++runtime->cuda_graph_fallbacks;
                        }
                    }else{
                        if(capture_status==0&&captured_graph)flyweight_gpu_graph_destroy(captured_graph);
                        ++runtime->cuda_graph_fallbacks;
                    }
                }else{
                    ++runtime->cuda_graph_fallbacks;
                }
            }
            if(!dense_ffn_launched)enqueue_dense_ffn();
            }
            if(profile){profile_record(profile->pre_end);profile_record(profile->shared_start);profile_record(profile->shared_end);profile_record(profile->expert_start);}
        }else{
        dense(moe_base+1,normalized,router_logits,hidden_size,experts);
        if(runtime->laguna){
            // Sigmoid routing with the score-correction bias, sum-normalized
            // over the selection and scaled by the trained routing factor.
            auto bias=runtime->device_tensors[layer.router_bias];
            int normalize=runtime->model->config.expert_weights_norm?1:0;
            float weight_scale=runtime->model->config.expert_weights_scale;
            void*route_args[]={const_cast<std::uint64_t*>(&router_logits),&bias,
                               const_cast<std::uint64_t*>(&selected_device),
                               const_cast<std::uint64_t*>(&route_weights),
                               const_cast<int*>(&experts),const_cast<int*>(&top_k),
                               &normalize,&weight_scale};
            launch_named("route_topk_sigmoid_bias",1,1,256,route_args,
                         static_cast<std::uint32_t>(2*experts*sizeof(float)));
        }else if(flyweight_gpu_route_topk(router_logits,selected_device,route_weights,experts,top_k,runtime->stream)!=0)throw std::runtime_error("native Qwen routing failed");
        const auto cpu_weights_offset=device_align(top_k*sizeof(std::int32_t));
        const auto cpu_input_offset=cpu_weights_offset+device_align(top_k*sizeof(float));
        const auto cpu_activated_offset=cpu_input_offset+device_align(hidden_size*sizeof(float));
        const auto cpu_output_offset=cpu_activated_offset+device_align(top_k*runtime->moe_intermediate*sizeof(float));
        auto*cpu_weights=reinterpret_cast<float*>(staging+cpu_weights_offset);auto*cpu_input=reinterpret_cast<float*>(staging+cpu_input_offset);auto*cpu_activated=reinterpret_cast<float*>(staging+cpu_activated_offset);auto*cpu_output=reinterpret_cast<float*>(staging+cpu_output_offset);
        if(flyweight_gpu_download(selected_host,selected_device,top_k*sizeof(std::int32_t),runtime->stream)!=0)throw std::runtime_error("native Qwen route transfer failed");
        if(expert_policy.routed_cpu_execution_allowed()&&
           (flyweight_gpu_download(cpu_weights,route_weights,top_k*sizeof(float),runtime->stream)!=0||
            flyweight_gpu_download(cpu_input,normalized,hidden_size*sizeof(float),runtime->stream)!=0))
            throw std::runtime_error("native Qwen CPU MoE input transfer failed");
        if(flyweight_gpu_event_record(runtime->route_event,runtime->stream)!=0)throw std::runtime_error("native Qwen route event failed");
        if(profile){profile_record(profile->pre_end);profile_record(profile->shared_start);}
        const int intermediate=static_cast<int>(runtime->moe_intermediate);
        auto enqueue_shared=[&]{
            auto shared_gate_matrix=tensor(moe_base+2),shared_up_matrix=tensor(moe_base+3);
            // The device type, not the stored one: bf16 shared experts are
            // requantized to Q8_0 on upload and must still take the fused path.
            const auto shexp_type=qwen_device_type(*runtime,layer.static_tensors.at(moe_base+2));
            if(shexp_type==40){
                // NVFP4 shared expert. One block per output row (the kernel block-reduces),
                // and weight_scale_2 passed in f32 -- it is far too small for E4M3.
                auto shared_gate_scale=layer.shared_gate_scale,shared_up_scale=layer.shared_up_scale;
                void*silu_args[]={&shared_gate_matrix,&shared_up_matrix,const_cast<std::uint64_t*>(&normalized),const_cast<std::uint64_t*>(&second),const_cast<int*>(&hidden_size),const_cast<int*>(&intermediate),&shared_gate_scale,&shared_up_scale};
                launch_named("nvfp4_swiglu_transposed",intermediate,1,256,silu_args);
                auto shared_down_matrix=tensor(moe_base+4);auto shared_down_scale=layer.shared_down_scale;
                void*down_args[]={&shared_down_matrix,const_cast<std::uint64_t*>(&second),const_cast<std::uint64_t*>(&third),const_cast<int*>(&intermediate),const_cast<int*>(&hidden_size),&shared_down_scale};
                launch_named("nvfp4_matvec_transposed",hidden_size,1,256,down_args);
            }else if(shexp_type==8){
                void*silu_args[]={&shared_gate_matrix,&shared_up_matrix,const_cast<std::uint64_t*>(&normalized),const_cast<std::uint64_t*>(&second),const_cast<int*>(&hidden_size),const_cast<int*>(&intermediate)};
                launch_named("q8_swiglu_transposed_warp",(intermediate+7)/8,1,256,silu_args);
                dense(moe_base+4,second,third,intermediate,hidden_size);
            }else{
                // No fused SwiGLU kernel for this weight type (Laguna ships a
                // k-quant shared expert): project gate and up into one
                // contiguous pair and let silu_mul combine them.
                const auto up_half=first+static_cast<std::uint64_t>(intermediate)*sizeof(float);
                dense(moe_base+2,normalized,first,hidden_size,intermediate);
                dense(moe_base+3,normalized,up_half,hidden_size,intermediate);
                int count=intermediate;
                void*silu_args[]={const_cast<std::uint64_t*>(&first),const_cast<std::uint64_t*>(&second),&count};
                launch_named("silu_mul",(static_cast<std::uint32_t>(count)+255)/256,1,256,silu_args);
                dense(moe_base+4,second,third,intermediate,hidden_size);
            }
            // Qwen gates the shared expert on a learned projection; Laguna's is
            // always on, so its block ends at the down projection.
            if(!runtime->laguna){
                auto shared_gate=tensor(moe_base+5);
                const auto sg_type=runtime->model->tensors[layer.static_tensors.at(moe_base+5)].type;
                void*shared_args[]={const_cast<std::uint64_t*>(&normalized),&shared_gate,const_cast<std::uint64_t*>(&third),const_cast<int*>(&hidden_size)};
                launch_named(sg_type==30?"qwen_shared_scale_bf16":"qwen_shared_scale",1,1,256,shared_args);
            }
        };
        bool shared_launched=false;
        if(runtime->cuda_graphs&&layer.shared_graph){
            if(flyweight_gpu_graph_launch(layer.shared_graph,runtime->stream)==0){
                ++runtime->cuda_graph_replays;
                shared_launched=true;
            }else{
                flyweight_gpu_graph_destroy(layer.shared_graph);
                layer.shared_graph=0;
                ++runtime->cuda_graph_fallbacks;
            }
        }
        if(runtime->cuda_graphs&&!shared_launched&&!layer.shared_graph_attempted){
            layer.shared_graph_attempted=true;
            if(flyweight_gpu_graph_begin(runtime->graph_stream)==0){
                bool capture_enqueued=true;
                launch_stream=runtime->graph_stream;
                try{enqueue_shared();}catch(...){capture_enqueued=false;}
                launch_stream=runtime->stream;
                std::uint64_t captured_graph=0;
                const int capture_status=flyweight_gpu_graph_end(runtime->graph_stream,&captured_graph);
                if(capture_enqueued&&capture_status==0){
                    layer.shared_graph=captured_graph;
                    ++runtime->cuda_graph_builds;
                    if(flyweight_gpu_graph_launch(layer.shared_graph,runtime->stream)==0){
                        ++runtime->cuda_graph_replays;
                        shared_launched=true;
                    }else{
                        flyweight_gpu_graph_destroy(layer.shared_graph);
                        layer.shared_graph=0;
                        ++runtime->cuda_graph_fallbacks;
                    }
                }else{
                    flyweight_gpu_graph_destroy(captured_graph);
                    ++runtime->cuda_graph_fallbacks;
                }
            }else{
                ++runtime->cuda_graph_fallbacks;
            }
        }
        if(!shared_launched)enqueue_shared();
        if(profile)profile_record(profile->shared_end);
        const auto route_wait_started=timing_enabled()?std::chrono::steady_clock::now():std::chrono::steady_clock::time_point{};
        if(flyweight_gpu_event_sync(runtime->route_event)!=0)throw std::runtime_error("native Qwen route event failed");
        if(timing_enabled())runtime->route_wait_nanoseconds+=std::chrono::duration_cast<std::chrono::nanoseconds>(std::chrono::steady_clock::now()-route_wait_started).count();
        if(profile)profile_record(profile->expert_start);
        // Optional adaptive expert pruning (top-p / hard top-k). Runs only for
        // the CPU/hybrid paths, whose expert weights are the host `cpu_weights`
        // this reorders/renormalizes; the streamed-GPU path keeps its device
        // weights and is left at full top_k for now.
        int route_count=top_k;
        if(expert_policy.route_pruning_allowed()&&
           (runtime->options.expert_top_k>0||
            (runtime->options.expert_top_p>0.0f&&runtime->options.expert_top_p<1.0f)))
            route_count=apply_expert_router_policy(selected_host,cpu_weights,top_k,static_cast<int>(runtime->options.expert_top_k),runtime->options.expert_top_p);
        runtime->route_expert_sum+=static_cast<std::uint64_t>(route_count);
        // Consume the previous layer's prefetch before issuing this one. The
        // uploads pending here were predicted for *this* layer, so this is where
        // they have to land; waiting after the call below would instead have made
        // the main stream block on the layer-ahead prefetch it had just queued,
        // serializing the copy engine against the very kernels it runs ahead of.
        const auto pager_started=timing_enabled()?std::chrono::steady_clock::now():std::chrono::steady_clock::time_point{};
        qwen_observe_route_recurrence(
            *runtime,
            runtime->sequences[runtime->active_sequence].expert_prefetch,
            layer_number,selected_host,route_count
        );
        qwen_wait_for_prefetch_layer(*runtime,layer_number);
        qwen_observe_and_prefetch_next_layer(
            *runtime,
            runtime->sequences[runtime->active_sequence].expert_prefetch,
            layer_number,selected_host,route_count
        );
        if(expert_policy.is_cpu()&&expert_policy.records_prefill_frequency())
            for(int rank=0;rank<route_count;++rank)
                record_expert_access(*runtime,layer_number,
                    static_cast<std::uint32_t>(selected_host[rank]));
        if(expert_policy.is_cpu()){
            if(cpu_output_offset+hidden_size*sizeof(float)>runtime->expert_staging_bytes)throw std::runtime_error("native CPU MoE workspace overflow");
            const auto compute_started=timing_enabled()?std::chrono::steady_clock::now():std::chrono::steady_clock::time_point{};
            qwen_cpu_moe(*runtime,layer,selected_host,cpu_weights,route_count,cpu_input,cpu_activated,cpu_output);
            if(timing_enabled())runtime->expert_compute_nanoseconds+=std::chrono::duration_cast<std::chrono::nanoseconds>(std::chrono::steady_clock::now()-compute_started).count();
            if(flyweight_gpu_upload(fourth,cpu_output,hidden_size*sizeof(float),runtime->stream)!=0)throw std::runtime_error("native CPU MoE output upload failed");
            add(third,fourth);
        }else if(expert_policy.is_hybrid()){
            if(runtime->expert_slots.empty())throw std::runtime_error("native hybrid MoE requires an expert cache budget");
            std::array<std::int32_t,256>cpu_selected{};
            std::array<float,256>cpu_compact_weights{},gpu_compact_weights{};
            std::array<float,256>gpu_gate_scales{},gpu_up_scales{},gpu_down_scales{};
            std::array<std::uint64_t,256>gate_pointers{},up_pointers{},
                down_pointers{},native_pointers{};
            int cpu_count=0,gpu_count=0;
            std::uint64_t staging_cursor=device_align(cpu_output_offset+hidden_size*sizeof(float));
            struct PendingUpload{
                std::uint64_t device,host_offset,bytes;
                std::size_t slot_index;
            };
            std::array<PendingUpload,256>pending{};int pending_count=0;
            auto&moe_prof=qwen_moe_profile();
            for(int rank=0;rank<route_count;++rank){
                const int expert=selected_host[rank];if(expert<0||expert>=experts)throw std::runtime_error("native hybrid MoE selected an invalid expert");
                const auto cache_key=(static_cast<std::uint64_t>(layer_number)<<32)|static_cast<std::uint32_t>(expert);
                const double t_lookup=moe_now();
                auto resident=runtime->expert_residency.find(cache_key);
                if(moe_profile_enabled()){moe_prof.lookup+=moe_now()-t_lookup;++moe_prof.lookups;}
                if(resident!=runtime->expert_residency.end()){
                    const auto slot_index=resident->second;auto&slot=runtime->expert_slots[slot_index];
                    const double t_access=moe_now();
                    const auto&history=record_expert_access(
                        *runtime,layer_number,expert);
                    if(expert_policy.residency_may_change())
                        slot.last_used=history.last_used;
                    record_expert_cache_hit(*runtime,slot);
                    if(moe_profile_enabled())moe_prof.access+=moe_now()-t_access;
                    const auto device_base=runtime->expert_cache+slot_index*runtime->expert_slot_bytes;std::uint64_t role_offset=0;
                    for(int role=0;role<3;++role){const auto bytes=runtime->model->tensors[layer.expert_tensors[role]].size/experts;const auto pointer=device_base+role_offset;if(role==0)gate_pointers[gpu_count]=pointer;else if(role==1)up_pointers[gpu_count]=pointer;else down_pointers[gpu_count]=pointer;role_offset+=bytes;}
                    if(slot.native_valid&&runtime->expert_native_cache)
                        native_pointers[gpu_count]=
                            runtime->expert_native_cache+
                            slot_index*runtime->expert_slot_bytes;
                    gpu_compact_weights[gpu_count]=cpu_weights[rank];
                    // Scales ride alongside the pointer table. SiLU makes the gate path
                    // non-linear, so gate/up must be applied before the activation; the
                    // down projection is linear, so its scale rides on `up` and comes
                    // out of the accumulate unchanged.
                    const double t_scales=moe_now();
                    gpu_gate_scales[gpu_count]=qwen_expert_role_scale(*runtime,layer.expert_gate_scale,expert);
                    gpu_down_scales[gpu_count]=qwen_expert_role_scale(*runtime,layer.expert_down_scale,expert);
                    gpu_up_scales[gpu_count]=qwen_expert_role_scale(*runtime,layer.expert_up_scale,expert)
                        *gpu_down_scales[gpu_count];
                    if(moe_profile_enabled())moe_prof.scales+=moe_now()-t_scales;
                    ++gpu_count;continue;
                }
                ++runtime->expert_cache_misses;cpu_selected[cpu_count]=expert;cpu_compact_weights[cpu_count]=cpu_weights[rank];
                if(layer.expert_down_scale!=std::numeric_limits<std::uint64_t>::max()){
                    const auto&st=runtime->model->tensors[layer.expert_down_scale];
                    const auto experts_count=runtime->model->config.expert_count;
                    const auto scale_bytes=st.size/experts_count;
                    float ds=1.0f;std::memcpy(&ds,tensor_data(*runtime->model,st)+static_cast<std::uint64_t>(expert)*scale_bytes,sizeof(float));
                    cpu_compact_weights[cpu_count]*=ds;
                }
                ++cpu_count;
                const double t_admit=moe_now();
                const auto slot_index=select_expert_cache_slot(*runtime,layer_number,expert,true);
                if(moe_profile_enabled()){moe_prof.admission+=moe_now()-t_admit;++moe_prof.admissions;}
                if(slot_index==kNoExpertSlot)continue;
                auto&slot=runtime->expert_slots[slot_index];slot.key=cache_key;slot.valid=true;slot.native_valid=false;slot.last_used=++runtime->expert_clock;runtime->expert_residency[cache_key]=slot_index;
                const auto slot_base=runtime->expert_cache+slot_index*runtime->expert_slot_bytes;
                if(layer.expert_dma){
                    // DMA each role straight from the registered mmap into the cache slot;
                    // no CPU staging memcpy (the 4.4 ms/token page-in cost we are attacking).
                    std::uint64_t role_offset=0;
                    for(int role=0;role<3;++role){const auto&t=runtime->model->tensors[layer.expert_tensors[role]];const auto bytes=t.size/experts;const auto offset=static_cast<std::uint64_t>(expert)*bytes;if(flyweight_gpu_upload(slot_base+role_offset,tensor_data(*runtime->model,t)+offset,bytes,runtime->stream)!=0)throw std::runtime_error("native hybrid MoE DMA cache upload failed");role_offset+=bytes;}
                    if(runtime->expert_native_cache){
                        const auto gate_bytes=runtime->model->tensors[
                            layer.expert_tensors[0]].size/experts;
                        const auto up_bytes=runtime->model->tensors[
                            layer.expert_tensors[1]].size/experts;
                        const int status=flyweight_gpu_nvfp4_prepare_expert(
                            slot_base,slot_base+gate_bytes,
                            slot_base+gate_bytes+up_bytes,
                            runtime->expert_native_cache+
                                slot_index*runtime->expert_slot_bytes,
                            runtime->stream,hidden_size,intermediate);
                        if(status!=0)throw std::runtime_error(
                            "persistent NVFP4 expert preparation failed");
                        slot.native_valid=true;
                    }
                }else{
                    const auto bundle_start=staging_cursor;
                    const double t_stage=moe_now();
                    for(int role=0;role<3;++role){const auto&t=runtime->model->tensors[layer.expert_tensors[role]];const auto bytes=t.size/experts;const auto offset=static_cast<std::uint64_t>(expert)*bytes;if(staging_cursor+bytes>runtime->expert_staging_bytes)throw std::runtime_error("native hybrid MoE staging overflow");std::memcpy(staging+staging_cursor,tensor_data(*runtime->model,t)+offset,bytes);staging_cursor+=bytes;}
                    if(moe_profile_enabled()){moe_prof.staging+=moe_now()-t_stage;moe_prof.staged_bytes+=staging_cursor-bundle_start;}
                    pending[pending_count++]={slot_base,bundle_start,
                        staging_cursor-bundle_start,slot_index};
                }
            }
            const double t_table=moe_now();
            if(gpu_count){
                const auto table_bytes=static_cast<std::uint64_t>(gpu_count)*(3*sizeof(std::uint64_t)+4*sizeof(float));
                const auto table_host=device_align(staging_cursor);const auto table_device=runtime->expert_staging+runtime->expert_staging_bytes-device_align(table_bytes);
                if(table_host+table_bytes>runtime->expert_staging_bytes)throw std::runtime_error("native hybrid MoE pointer staging overflow");
                std::memcpy(staging+table_host,gate_pointers.data(),gpu_count*sizeof(std::uint64_t));
                std::memcpy(staging+table_host+gpu_count*sizeof(std::uint64_t),up_pointers.data(),gpu_count*sizeof(std::uint64_t));
                std::memcpy(staging+table_host+2*gpu_count*sizeof(std::uint64_t),down_pointers.data(),gpu_count*sizeof(std::uint64_t));
                std::memcpy(staging+table_host+3*gpu_count*sizeof(std::uint64_t),gpu_compact_weights.data(),gpu_count*sizeof(float));
                std::memcpy(staging+table_host+3*gpu_count*sizeof(std::uint64_t)+gpu_count*sizeof(float),gpu_gate_scales.data(),gpu_count*sizeof(float));
                std::memcpy(staging+table_host+3*gpu_count*sizeof(std::uint64_t)+2*gpu_count*sizeof(float),gpu_up_scales.data(),gpu_count*sizeof(float));
                std::memcpy(staging+table_host+3*gpu_count*sizeof(std::uint64_t)+3*gpu_count*sizeof(float),gpu_down_scales.data(),gpu_count*sizeof(float));
                if(flyweight_gpu_upload(table_device,staging+table_host,table_bytes,runtime->stream)!=0)throw std::runtime_error("native hybrid MoE table upload failed");
                const auto gate_table=table_device,up_table=gate_table+gpu_count*sizeof(std::uint64_t),down_table=up_table+gpu_count*sizeof(std::uint64_t),weight_table=down_table+gpu_count*sizeof(std::uint64_t);
                const auto gate_scale_table=weight_table+gpu_count*sizeof(float),up_scale_table=gate_scale_table+gpu_count*sizeof(float),down_scale_table=up_scale_table+gpu_count*sizeof(float);
                if(moe_profile_enabled())moe_prof.table+=moe_now()-t_table;
                const auto gate_type=runtime->model->tensors[layer.expert_tensors[0]].type;
                const auto down_type=runtime->model->tensors[layer.expert_tensors[2]].type;
        // The grouped dispatch below ends in a k-quant fallback, so an
        // unhandled type would be decoded as Q5_K rather than rejected. Prepare
        // already routes such models to the CPU; this makes the silent path
        // unreachable if that ever stops holding.
        if(!qwen_gpu_expert_type_supported(gate_type)||
           !qwen_gpu_expert_type_supported(down_type))
            throw std::runtime_error(
                "native GPU expert quantization is unsupported: "+
                std::to_string(gate_type)+"/"+std::to_string(down_type));
                const bool persistent_enabled=env_nvfp4_persistent&&runtime->expert_native_cache&&
                    std::all_of(native_pointers.begin(),
                        native_pointers.begin()+gpu_count,
                        [](std::uint64_t pointer){return pointer!=0;});
                const bool tc_enabled=env_nvfp4_tc;
                bool tc_done=false;
                if(persistent_enabled&&gate_type==40&&down_type==40){
                    const int tc_status=flyweight_gpu_nvfp4_moe_persistent(
                        native_pointers.data(),weight_table,
                        gate_scale_table,up_scale_table,down_scale_table,
                        normalized,activated,third,runtime->stream,
                        hidden_size,intermediate,gpu_count);
                    if(tc_status==0){++runtime->nvfp4_tensor_core_moe_calls;runtime->nvfp4_tensor_core_moe_last_status=0;tc_done=true;}
                    else{
                        ++runtime->nvfp4_tensor_core_moe_fallbacks;
                        runtime->nvfp4_tensor_core_moe_last_status=tc_status;
                        if(env_nvfp4_tc_trace)
                            std::fprintf(stderr,"[nvfp4-persistent] hybrid fallback status=%d experts=%d\n",tc_status,gpu_count);
                    }
                }
                if(!tc_done&&tc_enabled&&gate_type==40&&down_type==40){
                    const int tc_status=flyweight_gpu_nvfp4_moe_cublas(
                        gate_table,up_table,down_table,normalized,activated,third,
                        weight_table,gate_scale_table,up_scale_table,down_scale_table,runtime->stream,
                        hidden_size,intermediate,gpu_count);
                    if(tc_status==0){++runtime->nvfp4_tensor_core_moe_calls;runtime->nvfp4_tensor_core_moe_last_status=0;tc_done=true;}
                    else{
                        ++runtime->nvfp4_tensor_core_moe_fallbacks;
                        runtime->nvfp4_tensor_core_moe_last_status=tc_status;
                        if(env_nvfp4_tc_trace)
                            std::fprintf(stderr,"[nvfp4-tc] hybrid fallback status=%d experts=%d\n",tc_status,gpu_count);
                    }
                }
                if(!tc_done){
                    const double t_launch=moe_now();
                    const bool nvfp4_tiled=env_nvfp4_tiled;
                    void*gate_up_args[]={const_cast<std::uint64_t*>(&gate_table),const_cast<std::uint64_t*>(&up_table),const_cast<std::uint64_t*>(&normalized),const_cast<std::uint64_t*>(&activated),const_cast<int*>(&hidden_size),const_cast<int*>(&intermediate),&gpu_count,const_cast<std::uint64_t*>(&gate_scale_table),const_cast<std::uint64_t*>(&up_scale_table)};
                    launch_named(qwen_grouped_swiglu_name(gate_type,nvfp4_tiled,false).c_str(),gate_type==40&&nvfp4_tiled?(intermediate+7)/8:intermediate,gpu_count,256,gate_up_args);
                    const int status=qwen_launch_grouped_accumulate(runtime->stream,down_type,down_table,activated,third,weight_table,intermediate,hidden_size,gpu_count);
                    if(status!=0)throw std::runtime_error("native hybrid MoE down projection failed");
                    if(moe_profile_enabled())moe_prof.queue+=moe_now()-t_launch;
                }
            }
            const double t_queue=moe_now();
            for(int index=0;index<pending_count;++index){
                const auto&upload=pending[index];
                if(flyweight_gpu_upload(upload.device,
                        staging+upload.host_offset,upload.bytes,
                        runtime->stream)!=0)
                    throw std::runtime_error(
                        "native hybrid MoE cache upload failed");
                if(runtime->expert_native_cache){
                    const auto gate_bytes=runtime->model->tensors[
                        layer.expert_tensors[0]].size/experts;
                    const auto up_bytes=runtime->model->tensors[
                        layer.expert_tensors[1]].size/experts;
                    const int status=flyweight_gpu_nvfp4_prepare_expert(
                        upload.device,upload.device+gate_bytes,
                        upload.device+gate_bytes+up_bytes,
                        runtime->expert_native_cache+
                            upload.slot_index*runtime->expert_slot_bytes,
                        runtime->stream,hidden_size,intermediate);
                    if(status!=0)throw std::runtime_error(
                        "persistent NVFP4 expert preparation failed");
                    runtime->expert_slots[
                        upload.slot_index].native_valid=true;
                }
            }
            if(moe_profile_enabled())moe_prof.queue+=moe_now()-t_queue;
            if(cpu_count){
                const auto compute_started=timing_enabled()?std::chrono::steady_clock::now():std::chrono::steady_clock::time_point{};
                qwen_cpu_moe(*runtime,layer,cpu_selected.data(),cpu_compact_weights.data(),cpu_count,cpu_input,cpu_activated,cpu_output);
                if(timing_enabled())runtime->expert_compute_nanoseconds+=std::chrono::duration_cast<std::chrono::nanoseconds>(std::chrono::steady_clock::now()-compute_started).count();
                if(flyweight_gpu_upload(fourth,cpu_output,hidden_size*sizeof(float),runtime->stream)!=0)throw std::runtime_error("native hybrid MoE output upload failed");
                add(third,fourth);
            }
        }else{
        std::uint64_t staging_cursor=device_align(top_k*sizeof(std::int32_t));
        bool has_uncached_expert=false;
        std::array<std::uint64_t,256> gate_pointers{},up_pointers{},down_pointers{};
        std::array<float,256> route_gate_scales{},route_up_scales{},route_down_scales{};
        for(int rank=0;rank<top_k;++rank){
            const int expert=selected_host[rank];if(expert<0||expert>=experts)throw std::runtime_error("native Qwen router selected an invalid expert");
            // Same convention as the cached path: gate/up before SiLU, down folded
            // into `up` because the accumulate that follows is linear.
            route_gate_scales[rank]=qwen_expert_role_scale(*runtime,layer.expert_gate_scale,expert);
            route_down_scales[rank]=qwen_expert_role_scale(*runtime,layer.expert_down_scale,expert);
            route_up_scales[rank]=qwen_expert_role_scale(*runtime,layer.expert_up_scale,expert)
                *route_down_scales[rank];
            std::uint64_t device_base=0;
            const auto cache_key=(static_cast<std::uint64_t>(layer_number)<<32)|static_cast<std::uint32_t>(expert);
            if(!runtime->expert_slots.empty()){
                auto resident=runtime->expert_residency.find(cache_key);
                if(resident!=runtime->expert_residency.end()){
                    const auto slot_index=resident->second;
                    auto&slot=runtime->expert_slots[slot_index];
                    record_expert_cache_hit(*runtime,slot);
                    const auto&history=record_expert_access(
                        *runtime,layer_number,expert);
                    if(expert_policy.residency_may_change())
                        slot.last_used=history.last_used;
                    device_base=runtime->expert_cache+slot_index*runtime->expert_slot_bytes;
                    std::uint64_t role_offset=0;
                    for(int role=0;role<3;++role){const auto bytes=runtime->model->tensors[layer.expert_tensors[role]].size/experts;const auto pointer=device_base+role_offset;if(role==0)gate_pointers[rank]=pointer;else if(role==1)up_pointers[rank]=pointer;else down_pointers[rank]=pointer;role_offset+=bytes;}
                }else{
                    ++runtime->expert_cache_misses;
                    const auto slot_index=select_expert_cache_slot(*runtime,layer_number,expert,true);
                    if(slot_index!=kNoExpertSlot){
                        auto&slot=runtime->expert_slots[slot_index];slot.key=cache_key;slot.valid=true;slot.last_used=++runtime->expert_clock;runtime->expert_residency[cache_key]=slot_index;
                        device_base=runtime->expert_cache+slot_index*runtime->expert_slot_bytes;
                        if(layer.expert_dma){
                            // The mmap is CUDA-registered, so each role goes straight from
                            // the file mapping into the cache slot. That removes the host
                            // memcpy from the critical path -- the CPU has already synced on
                            // route_event by here, so every byte it copies is GPU idle time.
                            std::uint64_t role_offset=0;
                            for(int role=0;role<3;++role){const auto&t=runtime->model->tensors[layer.expert_tensors[role]];const auto bytes=t.size/experts;const auto offset=static_cast<std::uint64_t>(expert)*bytes;if(flyweight_gpu_upload(device_base+role_offset,tensor_data(*runtime->model,t)+offset,bytes,runtime->stream)!=0)throw std::runtime_error("native Qwen cached expert DMA upload failed");role_offset+=bytes;}
                        }else{
                            // Size and bound-check the bundle BEFORE copying:
                            // the check used to run after the memcpys, so the
                            // overflow it guards against had already happened
                            // by the time it could fire.
                            std::uint64_t bundle_bytes=0;
                            for(int role=0;role<3;++role)bundle_bytes+=runtime->model->tensors[layer.expert_tensors[role]].size/experts;
                            if(staging_cursor+bundle_bytes>runtime->expert_staging_bytes)throw std::runtime_error("native Qwen expert staging overflow");
                            std::uint64_t copied=0;
                            for(int role=0;role<3;++role){const auto&t=runtime->model->tensors[layer.expert_tensors[role]];const auto bytes=t.size/experts;const auto offset=static_cast<std::uint64_t>(expert)*bytes;std::memcpy(staging+staging_cursor+copied,tensor_data(*runtime->model,t)+offset,bytes);copied+=bytes;}
                            if(flyweight_gpu_upload(device_base,staging+staging_cursor,bundle_bytes,runtime->stream)!=0)throw std::runtime_error("native Qwen cached expert upload failed");
                            staging_cursor+=bundle_bytes;
                        }
                        std::uint64_t role_offset=0;
                        for(int role=0;role<3;++role){const auto bytes=runtime->model->tensors[layer.expert_tensors[role]].size/experts;const auto pointer=device_base+role_offset;if(role==0)gate_pointers[rank]=pointer;else if(role==1)up_pointers[rank]=pointer;else down_pointers[rank]=pointer;role_offset+=bytes;}
                    }else{
                        has_uncached_expert=true;
                        for(int role=0;role<3;++role){const auto&t=runtime->model->tensors[layer.expert_tensors[role]];const auto bytes=t.size/experts;const auto offset=static_cast<std::uint64_t>(expert)*bytes;if(staging_cursor+bytes>runtime->expert_staging_bytes)throw std::runtime_error("native Qwen expert staging overflow");std::memcpy(staging+staging_cursor,tensor_data(*runtime->model,t)+offset,bytes);const auto pointer=runtime->expert_staging+staging_cursor;if(role==0)gate_pointers[rank]=pointer;else if(role==1)up_pointers[rank]=pointer;else down_pointers[rank]=pointer;staging_cursor+=bytes;}
                    }
                }
            }else{
                ++runtime->expert_cache_misses;
                has_uncached_expert=true;
                for(int role=0;role<3;++role){const auto&t=runtime->model->tensors[layer.expert_tensors[role]];const auto bytes=t.size/experts;const auto offset=static_cast<std::uint64_t>(expert)*bytes;if(staging_cursor+bytes>runtime->expert_staging_bytes)throw std::runtime_error("native Qwen expert staging overflow");std::memcpy(staging+staging_cursor,tensor_data(*runtime->model,t)+offset,bytes);const auto pointer=runtime->expert_staging+staging_cursor;if(role==0)gate_pointers[rank]=pointer;else if(role==1)up_pointers[rank]=pointer;else down_pointers[rank]=pointer;staging_cursor+=bytes;}
            }
        }
        const auto table_bytes=static_cast<std::uint64_t>(top_k)*(sizeof(std::uint64_t)*3+sizeof(float)*3);const auto table_host=device_align(staging_cursor);const auto table_device=runtime->expert_staging+runtime->expert_staging_bytes-device_align(table_bytes);const auto gate_table=table_device;const auto up_table=gate_table+top_k*sizeof(std::uint64_t);const auto down_table=up_table+top_k*sizeof(std::uint64_t);std::memcpy(staging+table_host,gate_pointers.data(),top_k*sizeof(std::uint64_t));std::memcpy(staging+table_host+top_k*sizeof(std::uint64_t),up_pointers.data(),top_k*sizeof(std::uint64_t));std::memcpy(staging+table_host+2*top_k*sizeof(std::uint64_t),down_pointers.data(),top_k*sizeof(std::uint64_t));
        const auto gate_scale_table=down_table+top_k*sizeof(std::uint64_t),up_scale_table=gate_scale_table+top_k*sizeof(float),down_scale_table=up_scale_table+top_k*sizeof(float);
        std::memcpy(staging+table_host+3*top_k*sizeof(std::uint64_t),route_gate_scales.data(),top_k*sizeof(float));
        std::memcpy(staging+table_host+3*top_k*sizeof(std::uint64_t)+top_k*sizeof(float),route_up_scales.data(),top_k*sizeof(float));
        std::memcpy(staging+table_host+3*top_k*sizeof(std::uint64_t)+2*top_k*sizeof(float),route_down_scales.data(),top_k*sizeof(float));
        if(table_host+table_bytes>runtime->expert_staging_bytes)throw std::runtime_error("native Qwen expert staging overflow");
        if(has_uncached_expert&&staging_cursor&&flyweight_gpu_upload(runtime->expert_staging,staging,staging_cursor,runtime->stream)!=0)throw std::runtime_error("native Qwen expert upload failed");
        if(flyweight_gpu_upload(table_device,staging+table_host,table_bytes,runtime->stream)!=0)throw std::runtime_error("native Qwen expert pointer upload failed");
        const auto gate_type=runtime->model->tensors[layer.expert_tensors[0]].type;
        const auto down_type=runtime->model->tensors[layer.expert_tensors[2]].type;
        // The grouped dispatch below ends in a k-quant fallback, so an
        // unhandled type would be decoded as Q5_K rather than rejected. Prepare
        // already routes such models to the CPU; this makes the silent path
        // unreachable if that ever stops holding.
        if(!qwen_gpu_expert_type_supported(gate_type)||
           !qwen_gpu_expert_type_supported(down_type))
            throw std::runtime_error(
                "native GPU expert quantization is unsupported: "+
                std::to_string(gate_type)+"/"+std::to_string(down_type));
        const bool tc_enabled=env_nvfp4_tc;
        bool tc_done=false;
        if(tc_enabled&&gate_type==40&&down_type==40){
            const int tc_status=flyweight_gpu_nvfp4_moe_cublas(
                gate_table,up_table,down_table,normalized,activated,third,
                route_weights,gate_scale_table,up_scale_table,down_scale_table,runtime->stream,
                hidden_size,intermediate,top_k);
            if(tc_status==0){++runtime->nvfp4_tensor_core_moe_calls;runtime->nvfp4_tensor_core_moe_last_status=0;tc_done=true;}
            else{
                ++runtime->nvfp4_tensor_core_moe_fallbacks;
                runtime->nvfp4_tensor_core_moe_last_status=tc_status;
                if(env_nvfp4_tc_trace)
                    std::fprintf(stderr,"[nvfp4-tc] decode fallback status=%d experts=%d\n",tc_status,top_k);
            }
        }
        if(!tc_done){
            const bool nvfp4_tiled=env_nvfp4_tiled;
            void*gate_up_args[]={const_cast<std::uint64_t*>(&gate_table),const_cast<std::uint64_t*>(&up_table),const_cast<std::uint64_t*>(&normalized),const_cast<std::uint64_t*>(&activated),const_cast<int*>(&hidden_size),const_cast<int*>(&intermediate),const_cast<int*>(&top_k),const_cast<std::uint64_t*>(&gate_scale_table),const_cast<std::uint64_t*>(&up_scale_table)};
            launch_named(qwen_grouped_swiglu_name(gate_type,nvfp4_tiled,false).c_str(),gate_type==40&&nvfp4_tiled?(intermediate+7)/8:intermediate,top_k,256,gate_up_args);
            const int down_status=qwen_launch_grouped_accumulate(runtime->stream,down_type,down_table,activated,third,route_weights,intermediate,hidden_size,top_k);
            if(down_status!=0)throw std::runtime_error("native Qwen expert down projection failed");
        }
        }
        if(qwen_lm_diag_enabled()&&layer_number==0){
            float v[4]={};flyweight_gpu_stream_sync(runtime->stream);
            if(flyweight_gpu_download(v,third,sizeof(v),runtime->stream)==0&&flyweight_gpu_stream_sync(runtime->stream)==0)
                std::fprintf(stderr,"[diag] L0 before_final_add third[0..3]=% .6e % .6e % .6e % .6e\n",v[0],v[1],v[2],v[3]);
            if(flyweight_gpu_download(v,residual,sizeof(v),runtime->stream)==0&&flyweight_gpu_stream_sync(runtime->stream)==0)
                std::fprintf(stderr,"[diag] L0 before_final_add residual[0..3]=% .6e % .6e % .6e % .6e\n",v[0],v[1],v[2],v[3]);
        }
        if(timing_enabled())runtime->expert_page_nanoseconds+=std::chrono::duration_cast<std::chrono::nanoseconds>(std::chrono::steady_clock::now()-pager_started).count();
        }
        // The feed-forward output takes its own norm before rejoining the
        // residual stream, mirroring the post-attention norm above.
        if(runtime->muse)post_rms(third,tensor(13));
        if(runtime->qwen4exp)hc_post(third);
        else add(residual,third);
        if(profile)profile_record(profile->expert_end);
        std::swap(hidden,residual);
        if(qwen_lm_diag_enabled()&&(layer_number==0||layer_number==3||layer_number==33||layer_number==39)){
            static int lc=0;
            if(lc<8){++lc;float v[4]={};if(flyweight_gpu_download(v,hidden,sizeof(v),runtime->stream)==0&&flyweight_gpu_stream_sync(runtime->stream)==0)
                std::fprintf(stderr,"[diag] after_layer_%u hidden[0..3]=% .6e % .6e % .6e % .6e\n",layer_number,v[0],v[1],v[2],v[3]);}
        }
    }
    if(runtime->cuda_profile)profile_record(runtime->cuda_tail_start);
    if(runtime->options.mtp_drafts){
        auto target_hidden=runtime->state+runtime->mtp_target_hidden_offset;
        // qwen4exp's draft block consumes the hyper-connection STREAMS, so the
        // hand-over is captured here -- before the inject-less collapse below
        // reduces them to the LM head's input. Handing over the collapsed
        // vector would be both the wrong width and the wrong quantity.
        const std::uint64_t source=runtime->qwen4exp?hc_streams:hidden;
        int count=runtime->qwen4exp?hc_count*hidden_size:hidden_size;
        void*copy_args[]={const_cast<std::uint64_t*>(&source),&target_hidden,&count};
        launch_named("qwen_copy_vector",(count+255)/256,1,256,copy_args);
        runtime->mtp_has_target_hidden=true;
    }
    // qwen4exp carries no final norm: the head collapse (the inject-less
    // gated-residual boundary over output_hc_*) is the last operation before
    // the LM head, exactly as the reference model ends.
    if(runtime->qwen4exp)
        hc_pre(runtime->device_tensors[runtime->output_hc_norm],
               runtime->output_hc_down,runtime->output_hc_up,
               std::numeric_limits<std::uint64_t>::max());
    else rms(hidden,runtime->device_tensors[runtime->final_norm],normalized);
    if(runtime->cuda_profile)profile_record(runtime->cuda_lm_start);
    runtime->last_sampling_normalized=normalized;
    runtime->last_sampling_logits=logits;
    if(qwen_lm_diag_enabled()){
        static int diag_count=0;
        if(diag_count<2){
            ++diag_count;
            float nvec[8]={};
            if(flyweight_gpu_download(nvec,normalized,sizeof(nvec),runtime->stream)==0&&flyweight_gpu_stream_sync(runtime->stream)==0){
                std::fprintf(stderr,"[lm-diag] normalized[0..7]=% .6e % .6e % .6e % .6e % .6e % .6e % .6e % .6e\n",nvec[0],nvec[1],nvec[2],nvec[3],nvec[4],nvec[5],nvec[6],nvec[7]);
            }
            std::fprintf(stderr,"[lm-diag] lm_head_type=%u lm_head_tensor=%zu hidden_size=%d\n",runtime->lm_head_type,runtime->lm_head,hidden_size);
        }
    }
    int vocabulary=static_cast<int>(runtime->model->config.vocabulary_size);
    if(flyweight_gpu_memset(argmax_device,0,sizeof(std::uint64_t),runtime->stream)!=0)throw std::runtime_error("native Qwen argmax reset failed");
    auto lm_head=runtime->device_tensors[runtime->lm_head];
    // The LM head is the largest single tensor read per token. Where the type
    // has a group-decode kernel, quantize the activation once and use it; the
    // fused per-element head remains the fallback.
    const char*lm_q8_kernel=qwen_q8_lm_head_kernel(runtime->lm_head_type);
    if(lm_q8_kernel&&iq2_q8_enabled&&(hidden_size&255)==0){
        if(normalized!=q8_cached_input){
            void*quant_args[]={const_cast<std::uint64_t*>(&normalized),
                const_cast<std::uint64_t*>(&dense_q8),
                const_cast<std::uint64_t*>(&dense_q8_scales),
                const_cast<int*>(&hidden_size)};
            launch_named("quantize_q8_blocks",(hidden_size+31)/32,1,32,quant_args);
            q8_cached_input=normalized;
        }
        void*lm_args[]={&lm_head,const_cast<std::uint64_t*>(&dense_q8),
            const_cast<std::uint64_t*>(&dense_q8_scales),
            const_cast<std::uint64_t*>(&argmax_device),
            const_cast<int*>(&hidden_size),&vocabulary};
        launch_named(lm_q8_kernel,(vocabulary+7)/8,1,256,lm_args);
    }else{
        void*argmax_args[]={&lm_head,const_cast<std::uint64_t*>(&normalized),const_cast<std::uint64_t*>(&argmax_device),const_cast<int*>(&hidden_size),&vocabulary};
        launch_named(qwen_lm_head_argmax_kernel(runtime->lm_head_type),(vocabulary+7)/8,1,256,argmax_args);
    }
    if(runtime->cuda_profile)profile_record(runtime->cuda_lm_end);
    auto*packed_winner=reinterpret_cast<std::uint64_t*>(staging);
    if(flyweight_gpu_download(packed_winner,argmax_device,sizeof(*packed_winner),runtime->stream)!=0)throw std::runtime_error("native Qwen output transfer failed");
    if(runtime->cuda_profile)profile_record(runtime->cuda_tail_end);
    const auto tail_wait_started=timing_enabled()?std::chrono::steady_clock::now():std::chrono::steady_clock::time_point{};
    if(flyweight_gpu_stream_sync(runtime->stream)!=0)throw std::runtime_error("native Qwen output synchronization failed");
    if(timing_enabled())runtime->tail_wait_nanoseconds+=std::chrono::duration_cast<std::chrono::nanoseconds>(std::chrono::steady_clock::now()-tail_wait_started).count();
    kernel_profile_collect();
    if(kernel_profile)++kernel_profile_decodes;
    if(runtime->cuda_profile){
        float delta_ms=0.0f,attention_ms=0.0f,shared_ms=0.0f,expert_ms=0.0f,tail_ms=0.0f,lm_ms=0.0f,recurrent_ms=0.0f,attention_core_ms=0.0f;
        std::uint32_t delta_layers=0,attention_layers=0;
        auto elapsed=[](std::uint64_t start,std::uint64_t end){float value=0.0f;if(flyweight_gpu_event_elapsed(start,end,&value)!=0)throw std::runtime_error("native Qwen CUDA profiling measurement failed");return value;};
        for(std::size_t index=0;index<runtime->layers.size();++index){
            const auto&events=runtime->cuda_layer_profiles[index];
            const float pre=elapsed(events.pre_start,events.pre_end);
            if(runtime->layers[index].attention){attention_ms+=pre;attention_core_ms+=elapsed(events.recurrent_start,events.recurrent_end);++attention_layers;}else{delta_ms+=pre;++delta_layers;recurrent_ms+=elapsed(events.recurrent_start,events.recurrent_end);}
            shared_ms+=elapsed(events.shared_start,events.shared_end);
            expert_ms+=elapsed(events.expert_start,events.expert_end);
        }
        tail_ms=elapsed(runtime->cuda_tail_start,runtime->cuda_tail_end);
        lm_ms=elapsed(runtime->cuda_lm_start,runtime->cuda_lm_end);
        std::fprintf(stderr,"[cuda-profile] position=%llu delta=%.3fms/%u (recurrent=%.3fms) attention=%.3fms/%u (core=%.3fms) shared=%.3fms expert=%.3fms tail=%.3fms (lm=%.3fms) total=%.3fms\n",
            static_cast<unsigned long long>(runtime->position),delta_ms,delta_layers,recurrent_ms,attention_ms,attention_layers,attention_core_ms,
            shared_ms,expert_ms,tail_ms,lm_ms,delta_ms+attention_ms+shared_ms+expert_ms+tail_ms);
    }
    *output_token=0xffffffffu-static_cast<std::uint32_t>(*packed_winner);
    if(std::getenv("FLYWEIGHT_TOK_DEBUG")){
        static int tc=0; if(tc++<60)
            std::fprintf(stderr,"[tok-dbg] step=%d in=%u -> out=%u\n",tc,input_token,*output_token);
    }
    runtime->last_output_token=*output_token;
    runtime->last_output_greedy=true;
    runtime->processed_tokens.push_back(input_token);
    ++runtime->position;
    // Reported periodically rather than once: the first token pays for capture,
    // so only the steady state says whether replay is actually cheaper.
    if(std::getenv("FLYWEIGHT_CUDA_GRAPH_TRACE")&&runtime->decode_calls%500==0)
    {
        const double per_token=runtime->delta_host_calls
            ?static_cast<double>(runtime->delta_host_ns)/1e6
             /static_cast<double>(runtime->decode_calls?runtime->decode_calls:1)
            :0.0;
        std::fprintf(stderr,
            "[cuda-graphs] tok=%llu builds=%llu replays=%llu fallbacks=%llu "
            "delta_host=%.3f ms/token over %llu blocks\n",
            static_cast<unsigned long long>(runtime->decode_calls),
            static_cast<unsigned long long>(runtime->cuda_graph_builds),
            static_cast<unsigned long long>(runtime->cuda_graph_replays),
            static_cast<unsigned long long>(runtime->cuda_graph_fallbacks),
            per_token,
            static_cast<unsigned long long>(runtime->delta_host_calls));
    }
    ++runtime->decode_calls;
    runtime->decode_nanoseconds+=std::chrono::duration_cast<std::chrono::nanoseconds>(std::chrono::steady_clock::now()-decode_started).count();
    if(runtime->decode_calls%50==0)kernel_profile_report();
    if(moe_profile_enabled()){
        ++qwen_moe_profile().tokens;
        if(runtime->decode_calls%50==0)qwen_moe_profile_report();
    }
    return 0;
});}

static double qwen_sampling_uniform(QwenSamplingState& sampling) {
    std::uint64_t value=sampling.rng;
    if(!value)value=0x9e3779b97f4a7c15ULL;
    value^=value>>12;value^=value<<25;value^=value>>27;
    sampling.rng=value;
    const std::uint64_t random=value*2685821657736338717ULL;
    return static_cast<double>(random>>11)*(1.0/9007199254740992.0);
}

// The bytes a token stands for, which is what the tool grammar matches. The
// vocabulary holds the GGUF byte-level spelling, so this is the same decode the
// Python side does when it turns generated tokens back into text.
static std::string qwen_token_bytes(
        const FlyweightV2QwenRuntime& runtime,std::uint32_t token) {
    if(!runtime.model||token>=runtime.model->vocabulary.size())return std::string();
    return gguf_byte_decode(runtime.model->vocabulary[token]);
}

static std::uint32_t qwen_sample_last_logits(
        FlyweightV2QwenRuntime& runtime,QwenSamplingState& sampling,
        std::uint32_t greedy_token) {
    // Every exit runs through this: the penalty window and the tool grammar
    // both track what was actually emitted, and a path that returns a token
    // without recording it desynchronizes them from the transcript.
    const auto commit=[&](std::uint32_t token)->std::uint32_t{
        sampling.remember(token);
        if(!sampling.grammar.empty()||!sampling.response_grammar.empty()||
           sampling.markup_ban.enabled()){
            const auto bytes=qwen_token_bytes(runtime,token);
            if(!sampling.grammar.empty())sampling.grammar.observe(bytes);
            if(!sampling.response_grammar.empty())
                sampling.response_grammar.observe(bytes);
            sampling.markup_ban.observe(bytes);
        }
        return token;
    };
    // Greedy decode needs the candidate machinery below when a call is open
    // (to constrain the choice) or when penalties are configured (to apply them
    // before the choice). Otherwise the fused argmax already has the answer --
    // unless that answer is the tool markup a banned request must not emit,
    // which is the one step a ban re-routes through the candidates.
    const bool ban_trips=sampling.markup_ban.enabled()&&
        !sampling.markup_ban.accepts(qwen_token_bytes(runtime,greedy_token));
    if(!sampling.enabled()&&!sampling.constrains()&&!sampling.penalizes()&&
       !ban_trips)
        return commit(greedy_token);
    struct SamplingTimer {
        std::uint64_t& nanoseconds;
        std::chrono::steady_clock::time_point started;
        ~SamplingTimer(){nanoseconds+=std::chrono::duration_cast<
            std::chrono::nanoseconds>(std::chrono::steady_clock::now()-started).count();}
    } sampling_timer{runtime.sampling_nanoseconds,
                     std::chrono::steady_clock::now()};
    if(!runtime.last_sampling_normalized||!runtime.last_sampling_logits)
        throw std::runtime_error("native Qwen sampling state is unavailable");
    const auto vocabulary=runtime.model->config.vocabulary_size;
    const auto bytes=static_cast<std::uint64_t>(vocabulary)*sizeof(float);
    const std::size_t requested=sampling.top_k?
        std::min<std::size_t>(sampling.top_k,vocabulary):vocabulary;
    // A constrained step needs something to choose between: the token the model
    // wants is precisely the one the grammar may forbid, and a top_k of 1 would
    // leave no alternative. Widen to the reduction's capacity while a call is
    // open; outside one this is the caller's top_k untouched.
    // The same widening serves the penalties: demoting the top candidate only
    // changes the answer if something else is in the set to take its place, so
    // a top_k of 1 would make a configured penalty a silent no-op.
    const std::size_t count=(sampling.constrains()||sampling.penalizes()||
                             ban_trips)
        ?std::min<std::size_t>(
            std::max<std::size_t>(
                requested,flyweight::v2::workspace::kSamplingConstrainedMinimum),
            vocabulary)
        :requested;
    // Sampling from a single candidate is identical to greedy decode. Avoid
    // both the second LM-head projection and all candidate transfers.
    if(count==1){runtime.last_output_token=greedy_token;
        runtime.last_output_greedy=true;
        return commit(greedy_token);}
    const auto lm_head=runtime.device_tensors[runtime.lm_head];
    // The head type picks the kernel, exactly as it does for the greedy argmax
    // above. This used to name Q4_K, Q6_K and bf16 and read every other type as
    // Q8_0, which reinterprets the bytes of the head outright: a Q3_K head
    // decodes to noise and pins the distribution to a handful of tokens. It
    // stayed hidden because greedy decode never calls this -- it runs a fused
    // argmax kernel that does dispatch on the type -- so the corruption only
    // appeared once temperature was nonzero.
    //
    // Prefer the group-decode kernel, for the reason greedy decode does: the
    // head is the largest tensor read per token, and the per-element decoder
    // leaves most of the bandwidth unused -- Q3_K measures 282 GB/s against 72
    // GB/s for the reference, worth ~6 ms/token on this checkpoint's 0.567 GB
    // head. Greedy cannot share the projection because its kernel is a fused
    // argmax that never writes the logits this path needs.
    const int hidden=static_cast<int>(runtime.model->config.hidden_size);
    static const bool q8_head_enabled=[]{
        const char*setting=std::getenv("FLYWEIGHT_IQ2_Q8_DECODE");
        return !setting||setting[0]!='0';
    }();
    const char*q8_kernel=q8_head_enabled?
        qwen_q8_matvec_kernel(runtime.lm_head_type):nullptr;
    const std::uint64_t q8_input=
        runtime.decode_workspace_layout.dense_q8.address(runtime.workspace);
    const std::uint64_t q8_scales=
        runtime.decode_workspace_layout.dense_q8_scales.address(runtime.workspace);
    int projected=-1;
    if(runtime.gemma4){
        // Gemma 4's head is the tied Q4_0 embedding table, which the generic
        // dispatch below carries no kernel for; project with the same matvec
        // the Gemma decode path uses for its dense Q4_0 projections.
        int vocabulary_rows=static_cast<int>(vocabulary);
        void*matvec_args[]={const_cast<std::uint64_t*>(&lm_head),
            &runtime.last_sampling_normalized,
            &runtime.last_sampling_logits,
            const_cast<int*>(&hidden),&vocabulary_rows};
        projected=flyweight_gpu_launch_named("gemma_q4_0_matvec",
                (vocabulary_rows+7)/8,1,256,0,runtime.stream,matvec_args);
        if(projected!=0)
            throw std::runtime_error(
                "native Gemma 4 sampling LM-head projection failed");
    }else if(q8_kernel&&(hidden&255)==0&&q8_input&&q8_scales){
        void*quantize_args[]={
            const_cast<std::uint64_t*>(&runtime.last_sampling_normalized),
            const_cast<std::uint64_t*>(&q8_input),
            const_cast<std::uint64_t*>(&q8_scales),const_cast<int*>(&hidden)};
        int vocabulary_rows=static_cast<int>(vocabulary);
        void*matvec_args[]={const_cast<std::uint64_t*>(&lm_head),
            const_cast<std::uint64_t*>(&q8_input),
            const_cast<std::uint64_t*>(&q8_scales),
            const_cast<std::uint64_t*>(&runtime.last_sampling_logits),
            const_cast<int*>(&hidden),&vocabulary_rows};
        projected=flyweight_gpu_launch_named("quantize_q8_blocks",
                (hidden+31)/32,1,32,0,runtime.stream,quantize_args);
        if(projected==0)
            projected=flyweight_gpu_launch_named(q8_kernel,
                    vocabulary_rows,1,128,0,runtime.stream,matvec_args);
    }
    if(projected!=0)
        projected=runtime.lm_head_type==30?
            flyweight_gpu_bf16_matvec_transposed(
                lm_head,runtime.last_sampling_normalized,runtime.last_sampling_logits,
                hidden,static_cast<std::int32_t>(vocabulary),runtime.stream):
            qwen_gpu_matvec_by_type(
                runtime.lm_head_type,lm_head,runtime.last_sampling_normalized,
                runtime.last_sampling_logits,hidden,
                static_cast<std::int32_t>(vocabulary),runtime.stream);
    if(projected!=0)
        throw std::runtime_error(
            "native Qwen sampling LM-head projection failed for tensor type "+
            std::to_string(runtime.lm_head_type));
    // Muse Glimmer scales the head output and then tanh-softcaps it, and
    // Gemma 4 softcaps without the scale. Both are monotonic, so the greedy
    // argmax path above is unaffected and skips this entirely -- but
    // temperature and top-p read the actual distribution, and the softcap
    // compresses it, so it has to run before candidate selection.
    if((runtime.muse||runtime.gemma4)&&
       (runtime.model->config.logit_scale!=0.0f||
        runtime.model->config.final_logit_softcap!=0.0f)){
        const float scale=runtime.model->config.logit_scale!=0.0f
            ?runtime.model->config.logit_scale:1.0f;
        const float softcap=runtime.model->config.final_logit_softcap;
        int vocabulary_count=static_cast<int>(vocabulary);
        void*args[]={const_cast<std::uint64_t*>(&runtime.last_sampling_logits),
                     &vocabulary_count,const_cast<float*>(&scale),
                     const_cast<float*>(&softcap)};
        if(flyweight_gpu_launch_named("muse_logit_softcap",
                (vocabulary_count+255)/256,1,256,0,runtime.stream,args)!=0)
            throw std::runtime_error("native Muse Glimmer logit softcap failed");
    }
    // Scratch reused across calls: the sampler runs once per generated token,
    // and fresh vectors here were pure allocator churn -- worst in the
    // full-download fallback below, which sized one to the vocabulary every
    // token. thread_local keeps the capacities warm and stays safe if two
    // runtimes ever sample from different threads.
    static thread_local std::vector<std::uint32_t> candidates;
    static thread_local std::vector<float> candidate_logits;
    candidates.clear();
    candidate_logits.clear();
    static const bool gpu_topk_enabled=[]{
        const char*setting=std::getenv("FLYWEIGHT_SAMPLING_GPU_TOPK");
        return !setting||setting[0]!='0';
    }();
    if(gpu_topk_enabled&&sampling.top_k&&
       count<=flyweight::v2::workspace::kSamplingTopKCapacity&&
       vocabulary<=flyweight::v2::workspace::kSamplingSortItemsPerBlock*
                   flyweight::v2::workspace::kSamplingSortBlockCapacity){
        const auto selected_device=runtime.decode_workspace_layout.
            sampling_selected.address(runtime.workspace);
        const auto values_device=runtime.decode_workspace_layout.
            sampling_logits.address(runtime.workspace);
        const auto sort_indices_a=runtime.decode_workspace_layout.
            sampling_sort_indices_a.address(runtime.workspace);
        const auto sort_values_a=runtime.decode_workspace_layout.
            sampling_sort_values_a.address(runtime.workspace);
        const auto sort_indices_b=runtime.decode_workspace_layout.
            sampling_sort_indices_b.address(runtime.workspace);
        const auto sort_values_b=runtime.decode_workspace_layout.
            sampling_sort_values_b.address(runtime.workspace);
        if(flyweight_gpu_sampling_topk(
                runtime.last_sampling_logits,selected_device,values_device,
                sort_indices_a,sort_values_a,sort_indices_b,sort_values_b,
                static_cast<std::int32_t>(vocabulary),
                static_cast<std::int32_t>(count),runtime.stream)!=0)
            throw std::runtime_error("native Qwen sampling top-k reduction failed");
        const auto candidate_bytes=count*sizeof(std::uint32_t);
        const auto value_offset=device_align(candidate_bytes);
        const auto value_bytes=count*sizeof(float);
        if(value_offset+value_bytes>runtime.host_staging_bytes)
            throw std::runtime_error("native Qwen sampling candidate workspace is too small");
        auto*selected_host=static_cast<std::uint32_t*>(runtime.host_staging);
        auto*values_host=reinterpret_cast<float*>(
            static_cast<std::uint8_t*>(runtime.host_staging)+value_offset);
        if(flyweight_gpu_download(
                selected_host,selected_device,candidate_bytes,runtime.stream)!=0||
           flyweight_gpu_download(
                values_host,values_device,value_bytes,runtime.stream)!=0||
           flyweight_gpu_stream_sync(runtime.stream)!=0)
            throw std::runtime_error("native Qwen sampling candidate transfer failed");
        candidates.assign(selected_host,selected_host+count);
        candidate_logits.assign(values_host,values_host+count);
        if(candidates.empty()||candidates.front()>=vocabulary){
            return commit(greedy_token);}
        ++runtime.sampling_gpu_topk_calls;
        runtime.sampling_gpu_topk_bytes+=candidate_bytes+value_bytes;
    }else{
        if(bytes>runtime.host_staging_bytes)
            throw std::runtime_error("native Qwen sampling workspace is too small");
        auto*logits=static_cast<float*>(runtime.host_staging);
        if(flyweight_gpu_download(
                logits,runtime.last_sampling_logits,bytes,runtime.stream)!=0||
           flyweight_gpu_stream_sync(runtime.stream)!=0)
            throw std::runtime_error("native Qwen sampling logits transfer failed");
        runtime.sampling_full_download_bytes+=bytes;
        candidates.resize(vocabulary);
        for(std::uint32_t token=0;token<vocabulary;++token)candidates[token]=token;
        auto greater=[&](std::uint32_t left,std::uint32_t right){
            const float a=logits[left],b=logits[right];
            if(std::isnan(a))return false;
            if(std::isnan(b))return true;
            return a!=b?a>b:left<right;
        };
        if(count<candidates.size()){
            std::partial_sort(candidates.begin(),candidates.begin()+count,
                              candidates.end(),greater);
            candidates.resize(count);
        }else std::sort(candidates.begin(),candidates.end(),greater);
        candidate_logits.reserve(candidates.size());
        for(const auto token:candidates)candidate_logits.push_back(logits[token]);
    }
    // Inside a tool call, keep only the candidates that leave it well-formed.
    //
    // This runs before the penalties and the nucleus cut so both operate on the
    // set the sampler may actually draw from -- in particular, a repetition
    // penalty must not spend its effect on a token that was never selectable.
    // The candidate list arrives sorted by logit and this preserves that order.
    if(sampling.constrains()||ban_trips){
        static thread_local std::vector<std::uint32_t> allowed;
        static thread_local std::vector<float> allowed_logits;
        allowed.clear();
        allowed_logits.clear();
        allowed.reserve(candidates.size());
        allowed_logits.reserve(candidates.size());
        for(std::size_t index=0;index<candidates.size();++index){
            const auto bytes=qwen_token_bytes(runtime,candidates[index]);
            if(!sampling.grammar.accepts(bytes))continue;
            if(!sampling.response_grammar.accepts(bytes))continue;
            // The ban filter: on the step where the argmax reached for tool
            // markup, keep every candidate that is not it. If nothing at all
            // survives (the whole set completes the tag -- vanishingly rare),
            // the empty-set fallback below still emits the greedy token: the
            // ban prefers a marker slipping through over a token the model
            // assigned no probability to.
            if(!sampling.markup_ban.accepts(bytes))continue;
            allowed.push_back(candidates[index]);
            allowed_logits.push_back(candidate_logits[index]);
        }
        ++runtime.grammar_constrained_steps;
        runtime.grammar_rejected_candidates+=candidates.size()-allowed.size();
        if(allowed.empty()){
            // Nothing in the candidate set continues the call. Taking the
            // unconstrained token is deliberate: the grammar disarms itself
            // once the text leaves the language it describes, which hands the
            // tail back to the tolerant parser -- whereas forcing a token from
            // outside the top-k would answer with something the model assigned
            // almost no probability to. Counted, because a model that lands
            // here often is one the grammar is failing to help.
            ++runtime.grammar_empty_candidate_sets;
            return commit(greedy_token);
        }
        candidates.swap(allowed);
        candidate_logits.swap(allowed_logits);
    }
    // Discourage what was just said. This runs over the top-k candidates
    // rather than the whole vocabulary: a token the model is looping on is by
    // definition near the top of the distribution, so it is always in this
    // set, and penalizing 20 entries costs nothing next to a full download.
    if(sampling.penalizes()&&!sampling.recent.empty()){
        for(std::size_t index=0;index<candidates.size();++index){
            std::uint32_t occurrences=0;
            for(const auto token:sampling.recent)
                if(token==candidates[index])++occurrences;
            if(!occurrences)continue;
            float logit=candidate_logits[index];
            if(sampling.repetition_penalty!=1.0f)
                logit=logit>0.0f?logit/sampling.repetition_penalty
                                :logit*sampling.repetition_penalty;
            logit-=sampling.presence_penalty;
            logit-=sampling.frequency_penalty*static_cast<float>(occurrences);
            candidate_logits[index]=logit;
        }
        // Both the nucleus cut and the cumulative draw below read this list as
        // sorted, and a penalty can demote a candidate past its neighbours.
        static thread_local std::vector<std::size_t> order;
        order.resize(candidates.size());
        for(std::size_t index=0;index<order.size();++index)order[index]=index;
        std::stable_sort(order.begin(),order.end(),
            [&](std::size_t left,std::size_t right){
                const float a=candidate_logits[left],b=candidate_logits[right];
                if(std::isnan(a))return false;
                if(std::isnan(b))return true;
                return a!=b?a>b:candidates[left]<candidates[right];
            });
        static thread_local std::vector<std::uint32_t> sorted_candidates;
        static thread_local std::vector<float> sorted_logits;
        sorted_candidates.resize(candidates.size());
        sorted_logits.resize(candidates.size());
        for(std::size_t index=0;index<order.size();++index){
            sorted_candidates[index]=candidates[order[index]];
            sorted_logits[index]=candidate_logits[order[index]];
        }
        candidates.swap(sorted_candidates);
        candidate_logits.swap(sorted_logits);
    }
    // Greedy decode, now that the candidates carry whatever the grammar and the
    // penalties had to say about them: the best surviving candidate is the
    // answer, and it is where the fused argmax's token gets overridden. Below
    // this point everything reads `temperature`, which is zero here.
    if(!sampling.enabled()){
        // Equality with the fused argmax, not provenance, is what a later
        // full-prompt reuse needs: a penalty or grammar that happened to keep
        // the argmax leaves the remembered token valid for a greedy hit.
        runtime.last_output_token=candidates.front();
        runtime.last_output_greedy=candidates.front()==greedy_token;
        return commit(candidates.front());
    }
    const double maximum=static_cast<double>(candidate_logits.front())/
        sampling.temperature;
    static thread_local std::vector<double> probabilities;
    probabilities.resize(candidates.size());
    double total=0.0;
    for(std::size_t index=0;index<candidates.size();++index){
        const double scaled=static_cast<double>(candidate_logits[index])/
            sampling.temperature;
        const double probability=std::isfinite(scaled)?
            std::exp(scaled-maximum):0.0;
        probabilities[index]=probability;total+=probability;
    }
    if(!(total>0.0)||!std::isfinite(total)){
        return commit(greedy_token);}
    std::size_t keep=probabilities.size();
    if(sampling.top_p<1.0f){
        double cumulative=0.0;
        for(std::size_t index=0;index<probabilities.size();++index){
            cumulative+=probabilities[index]/total;
            if(cumulative>=sampling.top_p){keep=index+1;break;}
        }
    }
    double kept_total=0.0;
    for(std::size_t index=0;index<keep;++index)kept_total+=probabilities[index];
    const double threshold=qwen_sampling_uniform(sampling)*kept_total;
    double cumulative=0.0;
    for(std::size_t index=0;index<keep;++index){
        cumulative+=probabilities[index];
        if(threshold<=cumulative){
            runtime.last_output_token=candidates[index];
            runtime.last_output_greedy=candidates[index]==greedy_token;
            return commit(candidates[index]);
        }
    }
    runtime.last_output_token=candidates[keep-1];
    runtime.last_output_greedy=candidates[keep-1]==greedy_token;
    return commit(candidates[keep-1]);
}

// Leading tokens shared by a slot's committed tokens and the new prompt.
static std::uint64_t qwen_sequence_match(
        const std::vector<std::uint32_t>& tokens,
        const std::uint32_t* prompt, std::uint64_t prompt_count) {
    const std::uint64_t limit = std::min<std::uint64_t>(tokens.size(), prompt_count);
    std::uint64_t i = 0;
    while (i < limit && tokens[i] == prompt[i]) ++i;
    return i;
}

// How many tokens make a shared prefix substantial in absolute terms. The
// synthetic fixtures run 256-token contexts, so the real bar would make every
// prefix-routing path unreachable in a test; the override exists for them.
// Read per call, never latched in a static -- a cached first read is what once
// made a kill-switch test vacuous.
static std::uint64_t qwen_prefix_bar() {
    if (const char* setting = std::getenv("FLYWEIGHT_PREFIX_BAR")) {
        const auto value = std::strtoull(setting, nullptr, 10);
        if (value) return value;
    }
    return 2048;
}

// A large absolute shared prefix is worth restoring even when it is less than
// half of a very long previous conversation. The fractional rule alone caused
// 15-30k reusable agent prefixes to be discarded after a long side branch.
static bool qwen_cache_match_useful(
        std::uint64_t match, std::size_t cached_tokens) {
    return match != 0 &&
        (match >= qwen_prefix_bar() || match >= (cached_tokens + 1) / 2);
}

// Whether handing this slot to a prompt that shares `match` of its tokens is
// worth avoiding. Taking a slot overwrites everything it holds PAST the shared
// prefix, so that residual -- not the fraction matched -- is the work at risk,
// and it is what a donation preserves.
//
// The fraction is the wrong question, and a real 35B measurement is what said
// so: a side-request under a 2352-token system prompt covered 97% of a
// 2417-token conversation, so a "matched more than half, must be a
// continuation" rule declined to donate. Read the other way round, only 65
// tokens were at risk and declining was right -- but the same rule declines
// just as firmly when the conversation is 30k and 28k of it would be lost.
//
// Residual zero means the prompt extends the conversation and there is nothing
// to preserve; a few hundred means a client re-rendered its last reply, not
// worth moving a conversation for; thousands means something branched off it.
static bool qwen_donation_is_worthwhile(
        std::uint64_t match, std::size_t cached_tokens) {
    return cached_tokens > match &&
        cached_tokens - match >= qwen_prefix_bar();
}

// Save the live (active) working set into its slot and load `target`'s. The KV
// arena is per-slot, so only the pointer + host bookkeeping move -- no device
// copy. Snapshots stay global; the reuse KV-safety guard keeps them correct
// across slots by tying reuse to the (per-slot) processed_tokens.
static void qwen_switch_sequence(FlyweightV2QwenRuntime& runtime, std::size_t target) {
    if (target == runtime.active_sequence) return;
    QwenSequence& cur = runtime.sequences[runtime.active_sequence];
    cur.position = runtime.position;
    cur.last_output_token = runtime.last_output_token;
    cur.last_output_greedy = runtime.last_output_greedy;
    cur.processed_tokens.swap(runtime.processed_tokens);
    cur.prefill_snapshots.swap(runtime.prefill_snapshots);
    cur.prefill_snapshot_clock = runtime.prefill_snapshot_clock;
    QwenSequence& next = runtime.sequences[target];
    runtime.state = next.state;
    qwen_apply_geometry(runtime, next.geometry);
    runtime.position = next.position;
    runtime.last_output_token = next.last_output_token;
    runtime.last_output_greedy = next.last_output_greedy;
    runtime.processed_tokens.swap(next.processed_tokens);
    runtime.prefill_snapshots.swap(next.prefill_snapshots);
    runtime.prefill_snapshot_clock = next.prefill_snapshot_clock;
    runtime.active_sequence = target;
}

// KV region bytes for `elems` elements at a cache precision. Shares the one
// definition with the prepare-time sizing so a new precision cannot size the
// arena one way and the live-state ranges another.
static std::uint64_t qwen_kv_region_bytes(std::uint64_t elems, int type) {
    return kv_type_bytes(elems, type);
}

// The device ranges of a slot arena that hold live conversation state at
// `position`: per attention layer, each kv-head's used [0, position) prefix of
// the K and V slabs (layout is head-major: head*capacity + pos, so the used
// prefix is contiguous per head); per DeltaNet layer, the full conv+recurrent
// state. Bytes beyond `position` in the KV slabs are never read (attention is
// position-bounded), so spill/restore can skip them: a spill copies
// position/capacity of the KV instead of the whole arena.
//
// `position_indexed_only` drops the ranges that are NOT a function of position
// -- the DeltaNet conv/recurrent state, the PLE ring, the QSA raw-key ring --
// leaving exactly the regions whose contents at `position` depend only on
// tokens below it. Those are the ranges one slot may donate to another; the
// recurrent remainder has to come from a checkpoint taken at that same
// position, which is what qwen_prefill_snapshot_copy carries. The split is a
// parameter rather than a second function so the layout stays defined once.
extern "C++" {
static std::vector<std::pair<std::uint64_t, std::uint64_t>> qwen_used_state_ranges(
        const FlyweightV2QwenRuntime& runtime, std::uint64_t position,
        bool position_indexed_only = false) {
    std::vector<std::pair<std::uint64_t, std::uint64_t>> ranges;
    const int ck = runtime.options.cache_type_k, cv = runtime.options.cache_type_v;
    for (std::uint32_t layer_number = 0; layer_number < runtime.layers.size(); ++layer_number) {
        const auto& layer = runtime.layers[layer_number];
        if (layer.attention) {
            const auto& key = runtime.model->tensors[tensor_index(
                *runtime.model, "blk." + std::to_string(layer_number) + ".attn_k.weight")];
            const auto head_dim = layer.head_dim;
            const auto kv_heads = layer.kv_heads;
            for (int cache = 0; cache < 2; ++cache) {
                const auto base = cache ? layer.state_second : layer.state_first;
                const int type = cache ? cv : ck;
                const auto slab = qwen_kv_region_bytes(layer.cache_capacity * head_dim, type);
                const auto used = qwen_kv_region_bytes(std::min<std::uint64_t>(position,layer.cache_capacity) * head_dim, type);
                if (!used) continue;
                for (std::uint64_t head = 0; head < kv_heads; ++head)
                    ranges.emplace_back(base + head * slab, used);
            }
            if (layer.qsa_ratio && layer.indexer_q != std::numeric_limits<std::uint64_t>::max()) {
                // QSA state: the used [0, position/ratio) prefix of the block-key
                // store (position-bounded like KV), plus the whole raw-key ring.
                const auto key_len = runtime.model->config.indexer_key_length;
                const auto used_blocks =
                    std::min<std::uint64_t>(position, layer.cache_capacity) / layer.qsa_ratio;
                if (used_blocks)
                    ranges.emplace_back(layer.qsa_blocks, used_blocks * key_len * sizeof(float));
                if (!position_indexed_only)
                    ranges.emplace_back(
                        layer.qsa_ring,
                        static_cast<std::uint64_t>(layer.qsa_ratio) * key_len * sizeof(float));
            }
        } else if (!position_indexed_only) {
            const auto& conv = runtime.model->tensors[layer.static_tensors[6]];
            const auto& a = runtime.model->tensors[layer.static_tensors[8]];
            const auto& norm = runtime.model->tensors[layer.static_tensors[9]];
            ranges.emplace_back(layer.state_first, tensor_elements(conv) * sizeof(float));
            ranges.emplace_back(layer.state_second, a.shape[0] * norm.shape[0] * norm.shape[0] * sizeof(float));
        }
        if (!position_indexed_only &&
            layer.ple_conv != std::numeric_limits<std::uint64_t>::max()) {
            const auto& ple_conv = runtime.model->tensors[layer.ple_conv];
            ranges.emplace_back(
                layer.state_third,
                (ple_conv.shape[0] - 1) *
                    std::max<std::uint64_t>(
                        runtime.model->config.ple_ngram_size, 1) *
                    ple_conv.shape[1] * sizeof(float));
        }
    }
    return ranges;
}

// How much of the reservation a session has actually touched. Each slot
// reserves state_bytes for the full context up front; the live set at a given
// position is what a paged pool would have had to hold instead, so the gap
// between them is the VRAM phase 2 of plans/paged-kv-cache.md would reclaim
// (and hand to the expert cache, which is where decode spends it).
//
// Idle slots are counted, not skipped: at position 0 the used ranges are
// exactly the non-pageable floor (DeltaNet conv+recurrent, the PLE ring, the
// QSA raw-key ring), which a pool still has to allocate per slot.
static void qwen_note_kv_occupancy(FlyweightV2QwenRuntime& runtime) {
    if (runtime.sequences.empty() || !runtime.state_bytes) return;
    std::uint64_t tokens = 0, live = 0;
    for (std::size_t slot = 0; slot < runtime.sequences.size(); ++slot) {
        // The active slot's position lives in the runtime mirror, the rest in
        // sequences[] -- the same convention qwen_spill_slot_to_host reads by.
        // Both engine call sites are outside a qwen_park_active window, so the
        // mirror is authoritative here rather than stale.
        const auto position = slot == runtime.active_sequence
            ? runtime.position : runtime.sequences[slot].position;
        tokens += position;
        runtime.kv_peak_tokens_max =
            std::max(runtime.kv_peak_tokens_max, position);
        for (const auto& range : qwen_used_state_ranges(runtime, position))
            live += range.second;
    }
    ++runtime.kv_occupancy_samples;
    // One sample, both figures: reporting the largest token count and the
    // largest byte count from different moments would describe a state the
    // runtime was never in.
    if (live > runtime.kv_peak_live_bytes) {
        runtime.kv_peak_live_bytes = live;
        runtime.kv_peak_tokens = tokens;
    }
}

// Sample on the way out of a request however it ends. A generate that the
// callback aborts, or that cancellation cuts short, still grew the KV it grew;
// sampling only on the clean return would under-report exactly the interleaved
// traffic this is meant to characterize.
// The live ranges of a slot under ITS OWN geometry rather than the active
// one. Slots may be laid out differently, so anything that reads or writes an
// inactive slot's arena -- spill, restore, donation -- has to ask with that
// slot's offsets. Borrowing the geometry and putting it back is what keeps one
// definition of the layout instead of a second, drifting copy.
static std::vector<std::pair<std::uint64_t, std::uint64_t>> qwen_slot_state_ranges(
        FlyweightV2QwenRuntime& runtime, std::size_t geometry,
        std::uint64_t position, bool position_indexed_only = false) {
    const std::size_t active = runtime.sequences.empty()
        ? 0 : runtime.sequences[runtime.active_sequence].geometry;
    if (geometry != active) qwen_apply_geometry(runtime, geometry);
    auto ranges = qwen_used_state_ranges(runtime, position, position_indexed_only);
    if (geometry != active) qwen_apply_geometry(runtime, active);
    return ranges;
}

// Two range lists describe the same state in different layouts only if they
// agree region for region. Capacities that differ enough to change a region's
// length (a sliding window that fits one slot's context and not the other's)
// make the copy meaningless, so refuse rather than transfer a truncated one.
static bool qwen_ranges_are_compatible(
        const std::vector<std::pair<std::uint64_t, std::uint64_t>>& a,
        const std::vector<std::pair<std::uint64_t, std::uint64_t>>& b) {
    if (a.size() != b.size()) return false;
    for (std::size_t i = 0; i < a.size(); ++i)
        if (a[i].second != b[i].second) return false;
    return true;
}

struct QwenKvOccupancyGuard {
    FlyweightV2QwenRuntime& runtime;
    ~QwenKvOccupancyGuard() {
        try { qwen_note_kv_occupancy(runtime); } catch (...) {}
    }
};
}  // extern "C++"

static void qwen_free_host_prompt(FlyweightV2QwenRuntime& runtime, std::size_t idx) {
    QwenHostPrompt& e = runtime.host_prompts[idx];
    std::free(e.state);
    for (auto& s : e.snapshots) std::free(s.state);
    runtime.host_cache_used_bytes -= e.bytes;
    runtime.host_prompts.erase(runtime.host_prompts.begin() + idx);
}

static void qwen_evict_host_lru(FlyweightV2QwenRuntime& runtime) {
    if (runtime.host_prompts.empty()) return;
    std::size_t lru = 0;
    for (std::size_t i = 1; i < runtime.host_prompts.size(); ++i)
        if (runtime.host_prompts[i].clock < runtime.host_prompts[lru].clock) lru = i;
    qwen_free_host_prompt(runtime, lru);
}

// Spill an inactive slot's full arena (KV + DeltaNet state) and its reuse
// checkpoints to host RAM, so a later request that continues this conversation
// restores from RAM instead of reprefilling ~30k tokens cold. Only substantial
// conversations are worth caching; short side-requests are skipped.
static void qwen_spill_slot_to_host(FlyweightV2QwenRuntime& runtime, std::size_t slot) {
    if (!runtime.host_cache_limit_bytes || !runtime.state_bytes) return;
    const QwenSequence& seq = runtime.sequences[slot];
    const bool active = slot == runtime.active_sequence;
    const auto& tokens = active ? runtime.processed_tokens : seq.processed_tokens;
    const auto position = active ? runtime.position : seq.position;
    const auto last_output = active ? runtime.last_output_token : seq.last_output_token;
    const auto last_output_greedy =
        active ? runtime.last_output_greedy : seq.last_output_greedy;
    const auto& snapshots = active ? runtime.prefill_snapshots : seq.prefill_snapshots;
    // Ignore tiny utility prompts, but retain ordinary conversations before a
    // title/quota/subagent request displaces the sole GPU slot.
    if (tokens.size() < 256) return;
    for (auto& e : runtime.host_prompts)
        if (e.tokens == tokens) { e.clock = ++runtime.host_cache_clock; return; }
    // Pack only the live ranges (used KV prefixes + DeltaNet state) instead of
    // the whole arena: at small positions the spill is a fraction of state_bytes.
    // Under this slot's own geometry -- it need not be the active slot's.
    const auto ranges = qwen_slot_state_ranges(runtime, seq.geometry, position);
    std::uint64_t packed_bytes = 0;
    for (const auto& r : ranges) packed_bytes += r.second;
    if (packed_bytes > runtime.host_cache_limit_bytes) return;
    std::uint64_t valid_snaps = 0;
    for (const auto& s : snapshots) if (s.valid) ++valid_snaps;
    const std::uint64_t snapshot_capacity = runtime.prefill_snapshot_bytes
        ? (runtime.host_cache_limit_bytes-packed_bytes)/runtime.prefill_snapshot_bytes
        : 0;
    const std::uint64_t snapshots_to_copy=std::min(valid_snaps,snapshot_capacity);
    const std::uint64_t need =
        packed_bytes + snapshots_to_copy * runtime.prefill_snapshot_bytes;
    while (runtime.host_cache_used_bytes > runtime.host_cache_limit_bytes - need
           && !runtime.host_prompts.empty())
        qwen_evict_host_lru(runtime);
    if (runtime.host_cache_used_bytes > runtime.host_cache_limit_bytes - need) return;
    void* buf = std::malloc(packed_bytes);
    if (!buf) return;
    std::uint64_t cursor = 0;
    bool copy_failed = false;
    for (const auto& r : ranges) {
        if (flyweight_gpu_download(static_cast<char*>(buf) + cursor,
                seq.state + r.first, r.second, runtime.stream) != 0) { copy_failed = true; break; }
        cursor += r.second;
    }
    if (copy_failed || flyweight_gpu_stream_sync(runtime.stream) != 0) { std::free(buf); return; }
    QwenHostPrompt e;
    e.tokens = tokens;
    e.state = buf;
    e.position = position;
    e.last_output_token = last_output;
    e.last_output_greedy = last_output_greedy;
    e.bytes = packed_bytes;
    e.geometry = seq.geometry;
    std::uint64_t copied_snapshots=0;
    for (const auto& s : snapshots) {
        if (!s.valid || s.tokens.empty() || copied_snapshots>=snapshots_to_copy) continue;
        void* sbuf = std::malloc(runtime.prefill_snapshot_bytes);
        if (!sbuf) break;  // partial checkpoint set is fine; arena reuse still works
        if (flyweight_gpu_download(sbuf, s.device, runtime.prefill_snapshot_bytes, runtime.stream) != 0
            || flyweight_gpu_stream_sync(runtime.stream) != 0) { std::free(sbuf); break; }
        e.snapshots.push_back({s.tokens, sbuf, s.last_output, s.last_output_greedy});
        e.bytes += runtime.prefill_snapshot_bytes;
        ++copied_snapshots;
    }
    e.clock = ++runtime.host_cache_clock;
    runtime.host_cache_used_bytes += e.bytes;
    runtime.host_prompts.push_back(std::move(e));
}

// Restore a host-cached conversation into an (inactive) victim slot: HtoD the
// saved arena, its checkpoints, and bookkeeping. Any leftover victim checkpoints
// beyond the restored set are invalidated.
static bool qwen_restore_host_to_slot(FlyweightV2QwenRuntime& runtime,
        std::size_t entry_idx, std::size_t victim) {
    QwenHostPrompt& e = runtime.host_prompts[entry_idx];
    QwenSequence& seq = runtime.sequences[victim];
    const bool active = victim == runtime.active_sequence;
    auto& tokens = active ? runtime.processed_tokens : seq.processed_tokens;
    auto& snapshots = active ? runtime.prefill_snapshots : seq.prefill_snapshots;
    auto& snapshot_clock = active
        ? runtime.prefill_snapshot_clock : seq.prefill_snapshot_clock;
    // A conversation longer than this slot can hold has nowhere to go: the
    // caller falls back to prefilling it on a slot that fits.
    if (e.position > qwen_slot_context(runtime, victim)) return false;
    // The packed layout is a pure function of (geometry, position), so the
    // ranges recomputed here match the spill order exactly -- but the spill's
    // geometry and the victim's need not be the same one, and the buffer was
    // written under the former while it lands under the latter.
    const auto source = qwen_slot_state_ranges(runtime, e.geometry, e.position);
    const auto ranges = qwen_slot_state_ranges(runtime, seq.geometry, e.position);
    if (!qwen_ranges_are_compatible(source, ranges)) return false;
    std::uint64_t cursor = 0;
    for (const auto& r : ranges) {
        if (flyweight_gpu_upload(seq.state + r.first,
                static_cast<const char*>(e.state) + cursor, r.second, runtime.stream) != 0)
            return false;
        cursor += r.second;
    }
    if (flyweight_gpu_stream_sync(runtime.stream) != 0) return false;
    tokens = std::move(e.tokens);
    if (active) {
        runtime.position = e.position;
        runtime.last_output_token = e.last_output_token;
        runtime.last_output_greedy = e.last_output_greedy;
    } else {
        seq.position = e.position;
        seq.last_output_token = e.last_output_token;
        seq.last_output_greedy = e.last_output_greedy;
    }
    std::size_t slot_i = 0;
    for (auto& hs : e.snapshots) {
        if (slot_i >= snapshots.size()) break;
        auto& dst = snapshots[slot_i++];
        if (flyweight_gpu_upload(dst.device, hs.state, runtime.prefill_snapshot_bytes, runtime.stream) != 0
            || flyweight_gpu_stream_sync(runtime.stream) != 0)
            break;
        dst.tokens = std::move(hs.tokens);
        dst.last_output = hs.last_output;
        dst.last_output_greedy = hs.last_output_greedy;
        dst.valid = true;
        dst.clock = ++snapshot_clock;
    }
    for (std::size_t i = slot_i; i < snapshots.size(); ++i)
        snapshots[i].valid = false;
    qwen_free_host_prompt(runtime, entry_idx);
    return true;
}

// Copy a cached prefix from one slot into another, so a prompt that merely
// SHARES a conversation's opening does not have to take over the slot holding
// it (plans/paged-kv-cache.md, phase 1).
//
// A subagent or a side-request under the main agent's system prompt matches
// that slot on 15-30k tokens while continuing nothing. Routing it there reuses
// the prefix and then overwrites everything past it, so the main conversation's
// next turn reprefills -- or, with a host cache, pays a full spill and restore.
// Donation gives the newcomer its own slot seeded with the shared prefix and
// leaves the donor untouched.
//
// What may be copied is exactly the position-indexed state, whose contents at
// [0, p) depend only on tokens below p. The recurrent remainder cannot be
// sliced, so p must be a position where the donor holds a checkpoint -- except
// on attention-only architectures, which have no recurrent state and can be cut
// anywhere, the same property that lets gemma4 rewind in qwen_prompt_begin.
//
// Returns true with `target` active, seeded, and ready for qwen_prompt_begin to
// reuse through its ordinary live-prefix path.
static bool qwen_donate_prefix(FlyweightV2QwenRuntime& runtime,
        std::size_t donor, std::size_t target,
        const std::uint32_t* prompt, std::uint64_t prompt_count) {
    if (donor == target || donor >= runtime.sequences.size() ||
        target >= runtime.sequences.size() || prompt_count < 2)
        return false;
    // A kill switch for a routing change that moves conversations between
    // slots: =0 restores the take-the-slot behaviour without a rebuild. Read
    // per call, never latched (see qwen_prefix_bar).
    if (const char* setting = std::getenv("FLYWEIGHT_PREFIX_DONATE");
        setting && setting[0] == '0')
        return false;
    const bool donor_active = donor == runtime.active_sequence;
    const auto& donor_tokens = donor_active
        ? runtime.processed_tokens : runtime.sequences[donor].processed_tokens;
    const auto donor_position = donor_active
        ? runtime.position : runtime.sequences[donor].position;
    if (!donor_position || donor_tokens.empty()) return false;

    // Everything the donation needs is copied out before the switch below moves
    // the donor's bookkeeping into sequences[donor].
    std::uint64_t donated = 0, snapshot_device = 0;
    std::uint32_t last_output = 0;
    bool last_output_greedy = false;
    if (runtime.prefill_snapshot_bytes) {
        const auto& snapshots = donor_active
            ? runtime.prefill_snapshots
            : runtime.sequences[donor].prefill_snapshots;
        for (const auto& candidate : snapshots) {
            if (!candidate.valid || candidate.tokens.empty()) continue;
            const std::uint64_t count = candidate.tokens.size();
            if (count <= donated) continue;
            // A strict prefix, deliberately: donating the whole prompt would
            // raise the same last-logits question qwen_prompt_begin answers for
            // a full hit, and one prefilled token regenerates real logits.
            if (count >= prompt_count || count > donor_tokens.size()) continue;
            if (!std::equal(candidate.tokens.begin(), candidate.tokens.end(), prompt))
                continue;
            // The KV-safety guard, donor side: a checkpoint the donor has since
            // diverged from describes attention KV the donor no longer holds.
            if (!std::equal(candidate.tokens.begin(), candidate.tokens.end(),
                            donor_tokens.begin()))
                continue;
            if (!swa_snapshot_is_resident(runtime, count, donor_position)) continue;
            donated = count;
            snapshot_device = candidate.device;
            last_output = candidate.last_output;
            last_output_greedy = candidate.last_output_greedy;
        }
    } else {
        // No recurrent state anywhere in the stack, so no checkpoint is needed
        // and the cut can land on the exact divergence point.
        const auto limit = std::min<std::uint64_t>(
            donor_tokens.size(), prompt_count - 1);
        std::uint64_t match = 0;
        while (match < limit && donor_tokens[match] == prompt[match]) ++match;
        if (match && swa_snapshot_is_resident(runtime, match, donor_position))
            donated = match;
    }
    // Any checkpoint at all is enough, and the bar that guards the caller does
    // not belong here: taking the donor's slot would have reused from these
    // same checkpoints, so a donation of `donated` tokens buys the newcomer
    // exactly what stealing the slot would have, and leaves the donor standing.
    // Measured on the 35B: the best checkpoint below a 2352-token shared
    // opening sat at 1572, and a 2048 bar here declined a donation that was
    // free. Zero is the real floor -- with nothing to seed, relocating would
    // trade the newcomer's reuse for the donor's, which is a different bet.
    if (!donated) return false;
    // The target may be a smaller slot than the donor; it cannot take more
    // prefix than it has room for.
    if (donated > qwen_slot_context(runtime, target)) return false;

    // Source and destination offsets coincide only when the two slots share a
    // geometry, so both lists are derived and checked rather than assumed.
    const auto source = qwen_slot_state_ranges(
        runtime, runtime.sequences[donor].geometry, donated, true);
    const auto destination = qwen_slot_state_ranges(
        runtime, runtime.sequences[target].geometry, donated, true);
    if (!qwen_ranges_are_compatible(source, destination)) return false;

    // Committed now, so the target's own conversation is about to be lost --
    // preserve it exactly as the recycle path would. Done here rather than in
    // the caller so an ineligible donation costs no spill.
    if (runtime.host_cache_limit_bytes) qwen_spill_slot_to_host(runtime, target);
    const auto donor_state = runtime.sequences[donor].state;
    qwen_switch_sequence(runtime, target);
    bool ok = true;
    for (std::size_t i = 0; i < destination.size(); ++i)
        if (flyweight_gpu_copy_device(runtime.state + destination[i].first,
                donor_state + source[i].first, destination[i].second,
                runtime.stream) != 0) {
            ok = false;
            break;
        }
    if (ok && snapshot_device)
        try { qwen_prefill_snapshot_copy(runtime, snapshot_device, true); }
        catch (const std::exception&) { ok = false; }
    if (ok && flyweight_gpu_stream_sync(runtime.stream) != 0) ok = false;
    if (!ok) {
        // A partial copy leaves the target holding two conversations spliced
        // together, which is worse than a cold start. Reset covers the arena
        // and the bookkeeping; the caller proceeds as if nothing was donated.
        flyweight_v2_qwen_runtime_reset(&runtime);
        return false;
    }

    // Seed the target so qwen_prompt_begin's live-prefix path reuses it. Tokens
    // come from `prompt`, not the donor's checkpoint: they are equal by the
    // check above, and the switch has since swapped the snapshot vectors.
    runtime.processed_tokens.assign(prompt, prompt + donated);
    runtime.position = donated;
    runtime.last_output_token = last_output;
    // Only a checkpoint knows whether its token was the unadjusted argmax, and
    // a donation always leaves at least one token to prefill, so the value is
    // never read as a full-hit seed. False is the safe reading either way.
    runtime.last_output_greedy = snapshot_device ? last_output_greedy : false;
    // Draft-block state describes the conversation this slot held a moment ago.
    // It is acceptance-only -- the verifier guards the text -- but stale entries
    // draft from a history the target no longer has.
    runtime.mtp_cache_tokens = 0;
    runtime.mtp_has_target_hidden = false;

    // The target's own checkpoints described the conversation just overwritten.
    // Install the donated one in their place so a later divergence on this slot
    // still has something to fall back to.
    for (auto& s : runtime.prefill_snapshots) s.valid = false;
    if (snapshot_device && !runtime.prefill_snapshots.empty()) {
        auto& dst = runtime.prefill_snapshots.front();
        if (flyweight_gpu_copy_device(dst.device, snapshot_device,
                runtime.prefill_snapshot_bytes, runtime.stream) == 0 &&
            flyweight_gpu_stream_sync(runtime.stream) == 0) {
            dst.tokens.assign(prompt, prompt + donated);
            dst.last_output = last_output;
            dst.last_output_greedy = last_output_greedy;
            dst.valid = true;
            dst.clock = ++runtime.prefill_snapshot_clock;
        }
    }

    ++runtime.prefix_donations;
    runtime.prefix_donated_tokens += donated;
    if (std::getenv("FLYWEIGHT_ROUTE_TRACE"))
        std::fprintf(stderr, "[route] donated %llu tokens from slot %zu to %zu\n",
            static_cast<unsigned long long>(donated), donor, target);
    return true;
}

// Route a prompt to the slot/host entry it continues; recycle the LRU slot
// otherwise. A slot/entry owns the prompt when it covers most of its committed
// tokens or provides a substantial absolute prefix. Before a divergent request
// overwrites that slot, the host cache preserves it even in one-slot mode.
static void qwen_route_sequence(FlyweightV2QwenRuntime& runtime,
        const std::uint32_t* prompt, std::uint64_t prompt_count,
        std::uint64_t required = 0) {
    const bool host_cache = runtime.host_cache_limit_bytes != 0;
    if (runtime.sequences.size() <= 1 && !host_cache) return;
    constexpr std::size_t kNone = std::numeric_limits<std::size_t>::max();
    // Slots need not be the same size, so "can this slot hold the request"
    // comes before every other consideration -- a scratch slot that matched a
    // long prompt's opening is still the wrong home for it. `required` counts
    // the tokens the request will occupy (prompt plus what it may generate);
    // zero means the caller could not say, and only the prompt is checked.
    const std::uint64_t need = std::max<std::uint64_t>(required, prompt_count);
    auto fits = [&](std::size_t slot) {
        return qwen_slot_context(runtime, slot) >= need;
    };
    std::size_t best = kNone;
    std::uint64_t best_match = 0;
    auto consider = [&](std::size_t i, const std::vector<std::uint32_t>& tokens) {
        if (!fits(i)) return;
        const std::uint64_t m = qwen_sequence_match(tokens, prompt, prompt_count);
        if (m > best_match && qwen_cache_match_useful(m,tokens.size())) {
            best_match = m; best = i;
        }
    };
    consider(runtime.active_sequence, runtime.processed_tokens);
    for (std::size_t i = 0; i < runtime.sequences.size(); ++i)
        if (i != runtime.active_sequence) consider(i, runtime.sequences[i].processed_tokens);
    std::size_t best_host = kNone;
    std::uint64_t best_host_match = 0;
    if (host_cache)
        for (std::size_t i = 0; i < runtime.host_prompts.size(); ++i) {
            const auto& t = runtime.host_prompts[i].tokens;
            const std::uint64_t m = qwen_sequence_match(t, prompt, prompt_count);
            if (m > best_host_match && qwen_cache_match_useful(m,t.size())) {
                best_host_match = m; best_host = i;
            }
        }
    // The slot to recycle: the smallest one that can hold the request, so a
    // short side-request lands on scratch and leaves the full-context slot for
    // the conversation that needs it; LRU breaks ties. Under symmetric slots
    // every capacity is equal and this is the plain LRU it always was, which
    // never picks the active slot (that one is the most-recently-stamped).
    auto lru_slot = [&]() {
        std::size_t v = 0;
        bool found = false;
        for (std::size_t i = 0; i < runtime.sequences.size(); ++i) {
            if (!fits(i)) continue;
            if (!found) { v = i; found = true; continue; }
            const auto here = qwen_slot_context(runtime, i);
            const auto there = qwen_slot_context(runtime, v);
            if (here != there ? here < there
                              : runtime.sequences[i].clock < runtime.sequences[v].clock)
                v = i;
        }
        return v;  // slot 0 spans the whole context, so something always fits
    };
    if (std::getenv("FLYWEIGHT_ROUTE_TRACE"))
        std::fprintf(stderr, "[route] active=%zu best=%lld match=%llu host_best=%lld host_match=%llu entries=%zu\n",
            runtime.active_sequence, best==kNone?-1LL:(long long)best, (unsigned long long)best_match,
            best_host==kNone?-1LL:(long long)best_host, (unsigned long long)best_host_match,
            runtime.host_prompts.size());
    if (best != kNone && best_match >= best_host_match) {
        const auto& best_tokens = best == runtime.active_sequence
            ? runtime.processed_tokens : runtime.sequences[best].processed_tokens;
        // A prompt that extends this conversation belongs on its slot. One that
        // only shares its opening does not: taking the slot would reuse the
        // prefix and overwrite the conversation behind it. Give it a slot of
        // its own with the prefix copied in, when there is one to spare and
        // enough of the donor would otherwise be lost to justify the copy.
        if (qwen_donation_is_worthwhile(best_match, best_tokens.size())) {
            const std::size_t victim = lru_slot();
            if (victim != best &&
                qwen_donate_prefix(runtime, best, victim, prompt, prompt_count)) {
                runtime.sequences[runtime.active_sequence].clock =
                    ++runtime.sequence_clock;
                return;
            }
        }
        if (host_cache && best_match < best_tokens.size())
            qwen_spill_slot_to_host(runtime, best);
        if (best != runtime.active_sequence) qwen_switch_sequence(runtime, best);
    } else if (best_host != kNone) {
        const std::size_t victim = lru_slot();
        qwen_spill_slot_to_host(runtime, victim);
        // Spilling may evict or reorder host entries to stay within budget;
        // resolve the index again rather than restoring a stale vector index.
        best_host=kNone;
        best_host_match=0;
        for(std::size_t i=0;i<runtime.host_prompts.size();++i){
            const auto&t=runtime.host_prompts[i].tokens;
            const auto m=qwen_sequence_match(t,prompt,prompt_count);
            if(m>best_host_match&&qwen_cache_match_useful(m,t.size())){
                best_host_match=m;best_host=i;
            }
        }
        if(best_host!=kNone)qwen_restore_host_to_slot(runtime,best_host,victim);
        qwen_switch_sequence(runtime, victim);
    } else {
        const std::size_t victim = lru_slot();
        if (host_cache) qwen_spill_slot_to_host(runtime, victim);
        if (victim != runtime.active_sequence) qwen_switch_sequence(runtime, victim);
        if (std::getenv("FLYWEIGHT_ROUTE_TRACE"))
            std::fprintf(stderr, "[route] victim=%zu spilled_entries=%zu\n", victim, runtime.host_prompts.size());
    }
    runtime.sequences[runtime.active_sequence].clock = ++runtime.sequence_clock;
}

// Prompt admission: diagnostics, exact-prefix reuse (live or snapshot, with the
// KV-safety guard), reset on miss, reuse stats, and the spread checkpoint
// targets. Shared verbatim by the blocking generate path and the engine so the
// two cannot drift. Disables expert-cache admission (prompt tokens must not
// pollute it); callers re-enable it when prefill completes.
static int qwen_prompt_begin(FlyweightV2QwenRuntime* runtime,
        const uint32_t* prompt, uint64_t prompt_count, QwenPromptPlan& plan,
        bool require_last_logits=false) {
    // Prefix-reuse diagnostics (computed before any state mutation below). The
    // longest common prefix against the live processed_tokens and against the
    // best snapshot shows where this prompt diverges from what is cached: a
    // small LCP means early prefix churn (system/tool schema), a large LCP with
    // a miss means divergence only near the tail. Reuse itself still requires an
    // *exact* prefix (DeltaNet recurrent state cannot be rewound mid-sequence).
    auto lcp_with=[&](const std::uint32_t*tokens,std::uint64_t count)->std::uint64_t{
        const std::uint64_t limit=std::min<std::uint64_t>(count,prompt_count);
        std::uint64_t i=0;
        while(i<limit&&tokens[i]==prompt[i])++i;
        return i;
    };
    runtime->prefix_cache_last_prompt_tokens=prompt_count;
    runtime->prefix_cache_last_lcp_live=runtime->processed_tokens.empty()?0:
        lcp_with(runtime->processed_tokens.data(),runtime->processed_tokens.size());
    std::uint64_t best_snapshot_lcp=0;
    for(const auto&candidate:runtime->prefill_snapshots){
        if(!candidate.valid||candidate.tokens.empty())continue;
        best_snapshot_lcp=std::max(best_snapshot_lcp,lcp_with(candidate.tokens.data(),candidate.tokens.size()));
    }
    runtime->prefix_cache_last_lcp_snapshot=best_snapshot_lcp;
    std::uint64_t prompt_start=0;
    bool reusable=!runtime->processed_tokens.empty()&&
        runtime->processed_tokens.size()<=prompt_count;
    if(reusable){
        reusable=std::equal(
            runtime->processed_tokens.begin(),runtime->processed_tokens.end(),
            prompt
        );
        // An exact full-prompt hit remembers the previously selected token but
        // not the full LM-head logits. Sampling must replay at least the final
        // uncached prompt section so it can draw a fresh token from real logits.
        // The remembered token cuts the other way too: if the previous request
        // sampled it (or a grammar overrode it), a greedy hit would emit a
        // temperature-drawn first token, so only a greedily-selected token may
        // be reused without replaying.
        if(runtime->processed_tokens.size()==prompt_count&&
           (require_last_logits||!runtime->last_output_greedy))
            reusable=false;
    }
    uint32_t next_token=0;
    if(reusable){
        prompt_start=runtime->processed_tokens.size();
        next_token=runtime->last_output_token;
        ++runtime->prefix_cache_hits;
        runtime->prefix_cache_reused_tokens+=prompt_start;
        runtime->cancelled=false;
    }else if(runtime->gemma4&&runtime->prefix_cache_last_lcp_live>0){
        // Attention-only state can REWIND to a shared prefix: a KV row below
        // the cut depends only on tokens below it, so truncating position and
        // processed_tokens splices nothing. Recurrent archs cannot (that is
        // what the exact-prefix rule and the snapshots exist for). The cut
        // must stay inside every sliding-window ring's rollback room -- the
        // batch_room slack past the window -- exactly like a snapshot
        // restore; past it the ring has overwritten the window's oldest
        // slots. This is what lets a re-sent or tail-diverged prompt reuse
        // the conversation the host cache just restored instead of
        // reprefilling it cold.
        const std::uint64_t cut=std::min<std::uint64_t>(
            runtime->prefix_cache_last_lcp_live,prompt_count-1);
        if(cut&&swa_snapshot_is_resident(*runtime,cut,runtime->position)){
            runtime->processed_tokens.resize(cut);
            runtime->position=cut;
            prompt_start=cut;
            ++runtime->prefix_cache_hits;
            runtime->prefix_cache_reused_tokens+=cut;
            runtime->cancelled=false;
        }else{
            ++runtime->prefix_cache_misses;
            const int status=flyweight_v2_qwen_runtime_reset(runtime);if(status)return status;
        }
    }else{
        // The live state diverged (typically: the client re-encoded the
        // previous assistant reply differently than it was generated), but a
        // prefill snapshot may still match the conversation prefix exactly.
        // Only attention KV that already holds this exact prefix is valid: the
        // KV cache reflects the live processed_tokens (recurrent DeltaNet state
        // is restored from the snapshot, but attention KV is not copied). So a
        // snapshot is reusable only when its tokens are a prefix of BOTH the new
        // prompt AND the sequence currently in the KV cache. Skipping the latter
        // check would splice stale KV from a diverged conversation.
        QwenPrefillSnapshot*snapshot=nullptr;
        for(auto&candidate:runtime->prefill_snapshots){
            if(!candidate.valid||candidate.tokens.empty()||candidate.tokens.size()>prompt_count)continue;
            if(candidate.tokens.size()==prompt_count&&
               (require_last_logits||!candidate.last_output_greedy))continue;
            if(!swa_snapshot_is_resident(*runtime,candidate.tokens.size(),runtime->position))continue;
            if(!std::equal(candidate.tokens.begin(),candidate.tokens.end(),prompt))continue;
            if(candidate.tokens.size()>runtime->processed_tokens.size())continue;
            if(!std::equal(candidate.tokens.begin(),candidate.tokens.end(),runtime->processed_tokens.begin()))continue;
            if(!snapshot||candidate.tokens.size()>snapshot->tokens.size())snapshot=&candidate;
        }
        if(snapshot){
            qwen_prefill_snapshot_copy(*runtime,snapshot->device,true);
            runtime->processed_tokens.assign(snapshot->tokens.begin(),snapshot->tokens.end());
            runtime->position=snapshot->tokens.size();
            runtime->last_output_token=snapshot->last_output;
            runtime->last_output_greedy=snapshot->last_output_greedy;
            prompt_start=snapshot->tokens.size();
            next_token=snapshot->last_output;
            snapshot->clock=++runtime->prefill_snapshot_clock;
            ++runtime->prefix_cache_hits;
            runtime->prefix_cache_reused_tokens+=prompt_start;
            runtime->cancelled=false;
            // The draft block's cache and conditioning hidden describe the
            // conversation the checkpoint rewound away from. Both are
            // acceptance-only state -- the verifier guards the text -- so
            // start them over rather than let stale entries draft from a
            // history the target no longer has. The suffix prefill reseeds
            // the target hidden as it runs.
            runtime->mtp_cache_tokens=0;
            runtime->mtp_has_target_hidden=false;
        }else{
            ++runtime->prefix_cache_misses;
            const int status=flyweight_v2_qwen_runtime_reset(runtime);if(status)return status;
        }
    }
    runtime->prefix_cache_last_reused_tokens=prompt_start;
    runtime->prefix_cache_reprefilled_tokens+=prompt_count-prompt_start;
    if(std::getenv("FLYWEIGHT_PREFIX_TRACE"))
        std::fprintf(stderr,
            "[prefix] prompt=%llu reused=%llu reprefill=%llu lcp_live=%llu lcp_snapshot=%llu\n",
            static_cast<unsigned long long>(prompt_count),
            static_cast<unsigned long long>(prompt_start),
            static_cast<unsigned long long>(prompt_count-prompt_start),
            static_cast<unsigned long long>(runtime->prefix_cache_last_lcp_live),
            static_cast<unsigned long long>(runtime->prefix_cache_last_lcp_snapshot));
    auto& prompt_sequence = runtime->sequences[runtime->active_sequence];
    prompt_sequence.prompt_expert_frequency.assign(
        static_cast<std::size_t>(runtime->layers.size()) *
            runtime->model->config.expert_count,
        0
    );
    prompt_sequence.prompt_expert_history.resize(runtime->expert_history.size());
    std::transform(
        runtime->expert_history.begin(), runtime->expert_history.end(),
        prompt_sequence.prompt_expert_history.begin(),
        [](const QwenExpertHistory& history) { return history.frequency; }
    );
    prompt_sequence.prompt_expert_observations = 0;
    runtime->cache_admission_enabled=false;
    // Mid-prefill checkpoint targets, spread EVENLY across this prompt (like
    // llama.cpp's spread context checkpoints) so a mid-conversation divergence
    // always finds a checkpoint within one spacing of the divergence point.
    // Spacing adapts to the prompt: max(interval, prompt/(mids+1)), so short
    // prompts keep dense early coverage while a 30k prompt spreads its slots
    // over the whole range instead of clustering at 256/512/1024 (measured live:
    // geometric placement reused only 1024 of a 13313-token shared prefix). The
    // last slot is reserved for the exact end-of-prompt snapshot saved below.
    // Targets already covered by the reused prefix are skipped.
    plan.targets.clear();
    if(runtime->prefill_snapshot_bytes&&
       runtime->prefill_checkpoint_interval&&runtime->prefill_snapshots.size()>1){
        const std::size_t mid_slots=runtime->prefill_snapshots.size()-1;
        const std::uint64_t spacing=std::max<std::uint64_t>(
            runtime->prefill_checkpoint_interval,prompt_count/(mid_slots+1));
        for(std::uint64_t pos=spacing;
            pos<prompt_count&&plan.targets.size()<mid_slots;pos+=spacing)
            plan.targets.push_back(pos);
    }
    plan.next_target=0;
    while(plan.next_target<plan.targets.size()&&plan.targets[plan.next_target]<=prompt_start)++plan.next_target;
    plan.prompt_start=prompt_start;
    plan.next_token=next_token;
    return 0;
}

// Save every checkpoint target the prefill has reached.
static void qwen_prompt_checkpoints(FlyweightV2QwenRuntime* runtime,
        const uint32_t* prompt, QwenPromptPlan& plan) {
    while(plan.next_target<plan.targets.size()&&runtime->position>=plan.targets[plan.next_target]){
        auto&slot=runtime->prefill_snapshots[plan.next_target];
        qwen_prefill_snapshot_copy(*runtime,slot.device,false);
        slot.tokens.assign(prompt,prompt+runtime->position);
        slot.last_output=0; // mid-prefill resume keeps prefilling; last_output unused
        slot.last_output_greedy=false;
        slot.valid=true;
        slot.clock=++runtime->prefill_snapshot_clock;
        ++plan.next_target;
    }
}

// One bounded prefill unit: a single rows-chunk (clamped to the next checkpoint
// target) while enough prompt remains, then the single-token tail (bounded per
// unit so a unit's latency stays small even when chunking is disabled). Sets
// `done` once the whole prompt has been processed and next_token is known.
// Chunked prefill batches prompt tokens through the rows forward so weight
// reads amortize across the chunk; the final prompt token still runs through
// single-token decode to produce next_token. MTP needs per-token prompt pairs
// from decode, so it keeps the one-token path.
static int qwen_prefill_unit(FlyweightV2QwenRuntime* runtime, const uint32_t* prompt,
        uint64_t prompt_count, QwenPromptPlan& plan, uint64_t& index,
        uint32_t& next_token, bool& done,QwenSamplingState* sampling=nullptr) {
    done=false;
    // index+3<=prompt_count is prompt_count-1-index>=2 without the unsigned
    // underflow that fired when a cache/snapshot reuse left index==prompt_count
    // (it read 1024 tokens past the prompt -> "input token out of range").
    if(runtime->prefill_rows>1&&prompt_count>1&&
       index+3<=prompt_count&&!runtime->cancelled){
        uint64_t rows=std::min<uint64_t>(runtime->prefill_rows,prompt_count-1-index);
        // Stop the chunk exactly at the next checkpoint target so checkpoints
        // land at precise positions instead of only at chunk boundaries (a
        // 1024-row chunk would otherwise overshoot an early divergence point).
        if(plan.next_target<plan.targets.size()){
            const uint64_t target=plan.targets[plan.next_target];
            if(target>index&&target-index<rows)rows=target-index;
        }
        qwen_prefill_rows(*runtime,prompt+index,static_cast<int>(rows));
        if(std::getenv("FLYWEIGHT_PREFILL_LAUNCHES"))qwen_prefill_launch_report();
        index+=rows;
        qwen_prompt_checkpoints(runtime,prompt,plan);
        if(index+3<=prompt_count&&!runtime->cancelled)return 0; // more chunks pending
    }
    uint64_t budget=64; // bounds the one-token path when chunking is off
    for(;index<prompt_count&&budget;--budget,index++){
        const int status=flyweight_v2_qwen_runtime_decode(runtime,prompt[index],&next_token);
        if(status)return status;
        if(sampling&&sampling->active()&&index+1==prompt_count)
            next_token=qwen_sample_last_logits(*runtime,*sampling,next_token);
        qwen_prompt_checkpoints(runtime,prompt,plan);
    }
    done=(index>=prompt_count);
    return 0;
}

// Post-prefill expert placement. Manual mode selects N experts per layer.
// Automatic immutable-hybrid mode chooses a request-horizon-sized prepared
// working set from prompt-local frequency plus decayed persistent history,
// bounded by cache capacity and wall time. A low-value prompt leaves the
// previous pinned set untouched, which is important for short side requests.
static void qwen_seed_prefill_experts(
        FlyweightV2QwenRuntime& runtime,
        std::uint64_t requested_generation_tokens) {
    const char* setting=std::getenv("FLYWEIGHT_PREFILL_CACHE_SEED");
    long requested=static_cast<long>(runtime.options.prefill_cache_seed);
    bool automatic=runtime.options.prefill_cache_seed_auto!=0;
    if(setting){
        if(std::strcmp(setting,"auto")==0){
            automatic=true;requested=0;
        }else if(std::strcmp(setting,"off")==0){
            automatic=false;requested=0;
        }else{
            automatic=false;requested=std::strtol(setting,nullptr,10);
        }
    }
    const auto expert_policy=qwen_expert_policy(
        runtime,flyweight::v2::ExpertExecutionPhase::prepare);
    if(runtime.gemma4||
       !expert_policy.is_hybrid()||runtime.expert_slots.empty())return;
    // Auto placement warms the cache whether or not residency is frozen after
    // it: the seed decides the *starting* map, mutability decides whether decode
    // may improve on it. Gating the seed on immutable residency meant the two
    // could not be had together.
    if(!automatic&&requested<=0)return;
    if(runtime.expert_residency_frozen){
        if(automatic)++runtime.prefill_cache_seed_auto_skips;
        return;
    }
    const auto started=std::chrono::steady_clock::now();
    const auto experts=runtime.model->config.expert_count;
    const auto cache_layers=qwen_cache_layer_count(runtime);
    const auto& sequence=runtime.sequences[runtime.active_sequence];
    std::uint32_t max_history_frequency=0;
    for(const auto frequency:sequence.prompt_expert_history)
        max_history_frequency=std::max(max_history_frequency,frequency);
    const bool useful_prompt=flyweight::v2::expert_seed::has_useful_prompt(
        sequence.prompt_expert_observations,
        static_cast<std::uint32_t>(runtime.layers.size()),
        runtime.model->config.expert_used_count);
    if(automatic&&!flyweight::v2::expert_seed::should_seed(
            sequence.prompt_expert_observations,
            static_cast<std::uint32_t>(runtime.layers.size()),
            runtime.model->config.expert_used_count,
            max_history_frequency)){
        ++runtime.prefill_cache_seed_auto_skips;
        return;
    }
    if(automatic&&!useful_prompt&&std::any_of(
            runtime.expert_slots.begin(),runtime.expert_slots.end(),
            [](const QwenExpertSlot& slot){return slot.pinned;})){
        ++runtime.prefill_cache_seed_auto_skips;
        return;
    }
    const std::size_t per_layer=automatic
        ? flyweight::v2::expert_seed::auto_experts_per_layer(
            runtime.expert_slots.size(),cache_layers,
            requested_generation_tokens)
        : static_cast<std::size_t>(std::clamp<long>(requested,1,256));
    if(!per_layer){
        if(automatic)++runtime.prefill_cache_seed_auto_skips;
        return;
    }

    // Rank every layer first, then consume candidates round-robin by rank.
    // This prevents a time/byte bound from letting early layers monopolize
    // the placement phase.
    std::vector<std::vector<std::uint32_t>> ranked(runtime.layers.size());
    for(std::uint32_t layer=0;layer<runtime.layers.size();++layer){
        const auto slot_begin=runtime.expert_slots.size()*layer/cache_layers;
        const auto slot_end=runtime.expert_slots.size()*(layer+1)/cache_layers;
        const auto layer_budget=std::min(per_layer,slot_end-slot_begin);
        auto& candidates=ranked[layer];
        candidates.reserve(experts);
        for(std::uint32_t expert=0;expert<experts;++expert){
            const auto index=static_cast<std::size_t>(layer)*experts+expert;
            const auto prompt_frequency=useful_prompt&&
                index<sequence.prompt_expert_frequency.size()
                    ? sequence.prompt_expert_frequency[index]:0;
            const auto history_frequency=
                index<sequence.prompt_expert_history.size()
                    ? sequence.prompt_expert_history[index]:0;
            if(prompt_frequency||history_frequency)
                candidates.push_back(expert);
        }
        std::sort(candidates.begin(),candidates.end(),[&](std::uint32_t left,std::uint32_t right){
            const auto left_index=static_cast<std::size_t>(layer)*experts+left;
            const auto right_index=static_cast<std::size_t>(layer)*experts+right;
            const auto left_prompt=useful_prompt&&
                left_index<sequence.prompt_expert_frequency.size()
                ? sequence.prompt_expert_frequency[left_index]:0;
            const auto right_prompt=useful_prompt&&
                right_index<sequence.prompt_expert_frequency.size()
                ? sequence.prompt_expert_frequency[right_index]:0;
            const auto left_history=left_index<sequence.prompt_expert_history.size()
                ? sequence.prompt_expert_history[left_index]:0;
            const auto right_history=right_index<sequence.prompt_expert_history.size()
                ? sequence.prompt_expert_history[right_index]:0;
            const auto& a=runtime.expert_history[left_index];
            const auto& b=runtime.expert_history[right_index];
            const auto a_score=flyweight::v2::expert_seed::score(left_prompt,left_history);
            const auto b_score=flyweight::v2::expert_seed::score(right_prompt,right_history);
            return a_score!=b_score?a_score>b_score:
                (a.last_used!=b.last_used?a.last_used>b.last_used:left<right);
        });
        if(candidates.size()>layer_budget)candidates.resize(layer_budget);
    }
    struct Selection{std::uint32_t layer,expert;};
    std::vector<Selection> selections;
    for(std::size_t rank=0;rank<per_layer;++rank)
        for(std::uint32_t layer=0;layer<ranked.size();++layer)
            if(rank<ranked[layer].size())
                selections.push_back({layer,ranked[layer][rank]});
    if(selections.empty()){
        if(automatic)++runtime.prefill_cache_seed_auto_skips;
        return;
    }

    // The replacement is now known to be useful. Release the previous prompt's
    // pins while retaining its pages as ordinary LRU candidates.
    for(auto& slot:runtime.expert_slots)slot.pinned=false;
    auto* staging=static_cast<std::uint8_t*>(runtime.host_staging);
    std::uint64_t seeded=0,selected=0,uploaded_bytes=0;
    struct PendingUpload{std::uint64_t device,host_offset,bytes;};
    std::vector<PendingUpload> pending;
    std::uint64_t cursor=0;
    auto flush=[&](){
        for(const auto& upload:pending)
            if(flyweight_gpu_upload(upload.device,staging+upload.host_offset,
                                  upload.bytes,runtime.stream)!=0)
                throw std::runtime_error("native Qwen prefill cache seed upload failed");
        if(!pending.empty()&&flyweight_gpu_stream_sync(runtime.stream)!=0)
            throw std::runtime_error("native Qwen prefill cache seed synchronization failed");
        pending.clear();cursor=0;
    };
    for(const auto selection:selections){
        // Bytes, not elapsed time: the stop point decides which experts get
        // pinned, and pinning decides placement, so it must be a pure function
        // of the selection list. The wall-clock bound this replaces made the
        // pinned set -- and therefore greedy output -- vary with upload speed.
        if(automatic&&
           uploaded_bytes>=flyweight::v2::expert_seed::kAutoMaxUploadBytes){
            ++runtime.prefill_cache_seed_budget_stops;break;
        }
        const auto layer=selection.layer,expert=selection.expert;
        const auto key=(static_cast<std::uint64_t>(layer)<<32)|expert;
        const auto resident=runtime.expert_residency.find(key);
        if(resident!=runtime.expert_residency.end()){
            runtime.expert_slots[resident->second].pinned=true;
            ++selected;
            continue;
        }
        const auto& plan=runtime.layers[layer];
        std::uint64_t bundle_bytes=0;
        for(int role=0;role<3;++role)
            bundle_bytes+=runtime.model->tensors[plan.expert_tensors[role]].size/experts;
        const auto slot_index=select_expert_cache_slot(
            runtime,layer,expert,false,false);
        if(slot_index==kNoExpertSlot)continue;
            if(bundle_bytes>runtime.host_staging_bytes)
                throw std::runtime_error("native Qwen prefill cache seed expert exceeds staging capacity");
            if(cursor+bundle_bytes>runtime.host_staging_bytes)flush();
            const auto bundle_start=cursor;
            for(int role=0;role<3;++role){
                const auto& tensor=runtime.model->tensors[plan.expert_tensors[role]];
                const auto bytes=tensor.size/experts;
                const auto offset=static_cast<std::uint64_t>(expert)*bytes;
                std::memcpy(staging+cursor,tensor_data(*runtime.model,tensor)+offset,bytes);
                cursor+=bytes;
            }
            auto& slot=runtime.expert_slots[slot_index];
            if(slot.valid)runtime.expert_residency.erase(slot.key);
            slot.key=key;slot.valid=true;slot.pinned=true;
            slot.prefetch_pins=0;slot.last_used=++runtime.expert_clock;
            runtime.expert_residency[key]=slot_index;
            pending.push_back({runtime.expert_cache+slot_index*runtime.expert_slot_bytes,
                               bundle_start,bundle_bytes});
            ++seeded;
            ++selected;
            uploaded_bytes+=bundle_bytes;
    }
    flush();
    runtime.prefill_cache_seeded_experts+=seeded;
    runtime.prefill_cache_seed_selected_experts+=selected;
    runtime.prefill_cache_seed_bytes+=uploaded_bytes;
    runtime.prefill_cache_seed_nanoseconds+=
        std::chrono::duration_cast<std::chrono::nanoseconds>(
            std::chrono::steady_clock::now()-started).count();
}

// Warm the file-backed pages for prompt-hot CPU experts before the first
// generated token. Selection is round-robin by rank across layers: every layer
// runs once per token, so allowing one layer to consume the entire byte budget
// would make a poor first-token working set. The pages remain ordinary,
// reclaimable OS cache; no second copy of the model is allocated.
static void qwen_prefetch_cpu_experts(FlyweightV2QwenRuntime& runtime) {
    const bool automatic=runtime.options.cpu_prefetch_auto!=0;
    const auto expert_policy=qwen_expert_policy(
        runtime,flyweight::v2::ExpertExecutionPhase::decode);
    if(runtime.gemma4||
       !expert_policy.routed_cpu_execution_allowed()||
       (!runtime.options.cpu_prefetch_mib&&!automatic))return;
    constexpr std::uint64_t mib=1024ULL*1024ULL;
    constexpr std::uint64_t reserve=2ULL*1024ULL*1024ULL*1024ULL;
    const auto available=available_host_memory();
    if(available<=reserve)return;
    std::uint64_t budget=0;
    if(automatic){
        budget=std::clamp<std::uint64_t>(available/64,64*mib,256*mib);
    }else budget=static_cast<std::uint64_t>(runtime.options.cpu_prefetch_mib)*mib;
    budget=std::min(budget,available-reserve);
    if(!budget)return;
    runtime.cpu_prefetch_last_budget_bytes=budget;
    const auto experts=runtime.model->config.expert_count;
    struct Candidate { std::uint32_t expert; std::uint32_t frequency; std::uint64_t last_used; };
    std::vector<std::vector<Candidate>> ranked(runtime.layers.size());
    for(std::uint32_t layer=0;layer<runtime.layers.size();++layer){
        auto& list=ranked[layer];
        for(std::uint32_t expert=0;expert<experts;++expert){
            const auto& history=runtime.expert_history[static_cast<std::size_t>(layer)*experts+expert];
            if(history.frequency)list.push_back({expert,history.frequency,history.last_used});
        }
        std::sort(list.begin(),list.end(),[](const Candidate&a,const Candidate&b){
            return a.frequency!=b.frequency?a.frequency>b.frequency:a.last_used>b.last_used;
        });
    }
    // A split checkpoint spreads experts over several mappings, so a range is
    // only meaningful against the mapping its tensor came from.
    struct Range { const std::uint8_t* base; std::uint64_t offset,bytes; };
    std::vector<Range> ranges;
    std::uint64_t selected_bytes=0,selected_experts=0;
    for(std::size_t rank=0;;++rank){
        bool any=false;
        for(std::uint32_t layer=0;layer<runtime.layers.size();++layer){
            if(rank>=ranked[layer].size())continue;
            any=true;
            const auto expert=ranked[layer][rank].expert;
            const auto& plan=runtime.layers[layer];
            std::uint64_t bundle=0;
            for(int role=0;role<3;++role)
                bundle+=runtime.model->tensors[plan.expert_tensors[role]].size/experts;
            if(bundle>budget-selected_bytes)continue;
            for(int role=0;role<3;++role){
                const auto& tensor=runtime.model->tensors[plan.expert_tensors[role]];
                const auto bytes=tensor.size/experts;
                ranges.push_back({tensor.source?tensor.source:runtime.model->data,
                    tensor.offset+static_cast<std::uint64_t>(expert)*bytes,bytes});
            }
            selected_bytes+=bundle;++selected_experts;
        }
        if(!any||selected_bytes==budget)break;
    }
    if(ranges.empty())return;
    std::sort(ranges.begin(),ranges.end(),[](const Range&a,const Range&b){
        return a.base!=b.base?a.base<b.base:a.offset<b.offset;});
    const auto started=std::chrono::steady_clock::now();
#if defined(_WIN32)
    SYSTEM_INFO si{}; GetSystemInfo(&si);
    const std::uint64_t page=si.dwPageSize>0?static_cast<std::uint64_t>(si.dwPageSize):4096ULL;
#else
    const long page_value=sysconf(_SC_PAGESIZE);
    const std::uint64_t page=page_value>0?static_cast<std::uint64_t>(page_value):4096ULL;
#endif
    struct ColdPage { const volatile std::uint8_t* address; };
    std::vector<std::uintptr_t> inspected;
    std::vector<ColdPage> cold;
    bool residency_known=true;
    inspected.reserve(static_cast<std::size_t>((selected_bytes+page-1)/page));
    cold.reserve(static_cast<std::size_t>((selected_bytes+page-1)/page));
    for(const auto& range:ranges){
        const auto begin=reinterpret_cast<std::uintptr_t>(range.base+range.offset);
        const auto end=begin+range.bytes;
        const auto aligned_begin=begin-(begin%page);
        const auto aligned_end=((end+page-1)/page)*page;
        const auto count=(aligned_end-aligned_begin)/page;
#if defined(__linux__)
        std::vector<unsigned char> resident(static_cast<std::size_t>(count));
        const bool known=mincore(reinterpret_cast<void*>(aligned_begin),
                                 aligned_end-aligned_begin,resident.data())==0;
        if(!known)residency_known=false;
#elif defined(_WIN32)
        // Use QueryWorkingSetEx to check page residency on Windows
        std::vector<PSAPI_WORKING_SET_EX_INFORMATION> wsinfo(static_cast<std::size_t>(count));
        for(std::uint64_t i=0;i<count;++i)
            wsinfo[i].VirtualAddress=reinterpret_cast<PVOID>(aligned_begin+i*page);
        const bool known=QueryWorkingSetEx(GetCurrentProcess(),wsinfo.data(),
            static_cast<DWORD>(count*sizeof(PSAPI_WORKING_SET_EX_INFORMATION)))!=0;
        if(!known)residency_known=false;
#else
        residency_known=false;
#endif
        for(std::uint64_t index=0;index<count;++index){
            const auto address=aligned_begin+index*page;
            inspected.push_back(address);
#if defined(__linux__)
            if(known&&(resident[static_cast<std::size_t>(index)]&1U))continue;
#elif defined(_WIN32)
            if(known&&wsinfo[index].VirtualAttributes.Valid)continue;
#endif
            cold.push_back({reinterpret_cast<const volatile std::uint8_t*>(address)});
        }
    }
    std::sort(inspected.begin(),inspected.end());
    inspected.erase(std::unique(inspected.begin(),inspected.end()),inspected.end());
    std::sort(cold.begin(),cold.end(),[](const ColdPage&a,const ColdPage&b){
        return a.address<b.address;
    });
    cold.erase(std::unique(cold.begin(),cold.end(),[](const ColdPage&a,const ColdPage&b){
        return a.address==b.address;
    }),cold.end());
    const auto observed_cold_pages=cold.size();
    if(automatic){
        const bool enough_pages=observed_cold_pages*page>=8*mib;
        const bool enough_pressure=observed_cold_pages*10>=inspected.size();
        if(!residency_known||!enough_pages||!enough_pressure){
            cold.clear();
            ++runtime.cpu_prefetch_auto_skips;
        }
    }
#if !defined(_WIN32)
    for(const auto& range:ranges)
        if(!cold.empty()&&runtime.model->fd>=0)
            (void)posix_fadvise(runtime.model->fd,static_cast<off_t>(range.offset),
                               static_cast<off_t>(range.bytes),POSIX_FADV_WILLNEED);
#endif
    std::uint8_t checksum=runtime.cpu_prefetch_checksum;
    for(const auto& item:cold)checksum^=*item.address;
    runtime.cpu_prefetch_checksum=checksum;
    runtime.cpu_prefetch_experts+=selected_experts;
    runtime.cpu_prefetch_bytes+=selected_bytes;
    runtime.cpu_prefetch_pages+=inspected.size();
    runtime.cpu_prefetch_cold_pages+=observed_cold_pages;
    runtime.cpu_prefetch_loaded_pages+=cold.size();
    runtime.cpu_prefetch_nanoseconds+=std::chrono::duration_cast<std::chrono::nanoseconds>(
        std::chrono::steady_clock::now()-started).count();
}

// Save this prompt's end-of-prefill state so the next turn only prefills its
// suffix, and re-enable expert-cache admission for decode.
static void qwen_prompt_finish(FlyweightV2QwenRuntime* runtime,
        const uint32_t* prompt, uint64_t prompt_count, uint32_t next_token,
        uint64_t requested_generation_tokens) {
    runtime->cache_admission_enabled=true;
    qwen_prefetch_cpu_experts(*runtime);
    qwen_seed_prefill_experts(*runtime,requested_generation_tokens);
    if(!runtime->prefill_snapshot_bytes)return;
    // Prefer the reserved slot when mid checkpoints exist, else the slot already
    // tracking this conversation, else a free slot, else the LRU.
    QwenPrefillSnapshot*slot=nullptr;
    if(runtime->prefill_checkpoint_interval&&runtime->prefill_snapshots.size()>1){
        // Mid-prefill checkpoints own slots [0,size-1); the exact end-of-prompt
        // snapshot has the reserved last slot so it never evicts a mid.
        slot=&runtime->prefill_snapshots.back();
    }
    if(!slot)for(auto&candidate:runtime->prefill_snapshots)
        if(candidate.valid&&candidate.tokens.size()<=prompt_count&&std::equal(candidate.tokens.begin(),candidate.tokens.end(),prompt)){slot=&candidate;break;}
    if(!slot)for(auto&candidate:runtime->prefill_snapshots)if(!candidate.valid){slot=&candidate;break;}
    if(!slot){slot=&runtime->prefill_snapshots[0];for(auto&candidate:runtime->prefill_snapshots)if(candidate.clock<slot->clock)slot=&candidate;}
    if(!(slot->valid&&slot->tokens.size()==prompt_count)){
        qwen_prefill_snapshot_copy(*runtime,slot->device,false);
        slot->tokens.assign(prompt,prompt+prompt_count);
        slot->last_output=next_token;
        // next_token may have come from the sampler; the runtime flag tracks
        // whether it still equals the unadjusted argmax (see the sampler).
        slot->last_output_greedy=runtime->last_output_greedy;
        slot->valid=true;
    }
    slot->clock=++runtime->prefill_snapshot_clock;
}

// Drafting recursively feeds the MTP block's own hidden state into the next
// speculative step. Those entries are useful only inside the current round:
// once the target has verified a prefix, its KV entries must be rebuilt from
// the target model's true hidden states. Keeping the recursive draft entries
// makes the cache drift farther from training semantics after every round and
// rapidly destroys acceptance.
// FLYWEIGHT_MTP_FOLD_CHECK=1: after a fold rebuilt the DeltaNet state, prove it
// bit-identical to the full replay forward it replaced. Downloads the folded
// conv/recurrent state, restores the snapshot a second time, runs the old
// replay path, downloads again and compares every byte. Replay is idempotent
// over the accepted prefix (same KV slots, same hidden rows, same argmax), so
// a passing check leaves the runtime exactly where the fold did. Validating
// state bits directly -- not output plausibility -- is the discipline that
// catches a fold drifting from the verify recurrence by a single rounding.
//
// The oracle is exact only where the forward is row-count-stable, which the
// CPU backend is -- the fixture test runs this check green there. On the GPU
// the check fires at ulp level (measured 4e-6 at the first delta layer) and
// that is the *replay* drifting, not the fold: dense_rows switches kernels on
// row count (batched rows kernel above one row, warp matvec at one), so a
// rows=valid replay never reproduced the rows=wanted batch bit-for-bit. The
// old rollback path silently committed those drifted bits; the fold preserves
// the batch's own bits, which is the more faithful of the two. Keep this
// check for the CPU parity suite and for reading the printed max_diff -- a
// large value still means a real bug, ulps mean the kernel-selection seam.
static void qwen_mtp_fold_check(
    FlyweightV2QwenRuntime& runtime, const std::uint32_t* inputs,
    std::uint32_t valid
) {
    struct Saved { std::vector<float> conv, recurrent; };
    auto capture=[&](std::vector<Saved>&stash){
        stash.clear();
        for(std::uint32_t layer_number=0;layer_number<runtime.layers.size();++layer_number){
            auto&layer=runtime.layers[layer_number];
            if(layer.attention)continue;
            const auto&conv=runtime.model->tensors[tensor_index(*runtime.model,"blk."+std::to_string(layer_number)+".ssm_conv1d.weight")];
            const auto&a=runtime.model->tensors[tensor_index(*runtime.model,"blk."+std::to_string(layer_number)+".ssm_a")];
            const auto&norm=runtime.model->tensors[tensor_index(*runtime.model,"blk."+std::to_string(layer_number)+".ssm_norm.weight")];
            Saved saved;
            saved.conv.resize(conv.shape[0]*conv.shape[1]);
            saved.recurrent.resize(a.shape[0]*norm.shape[0]*norm.shape[0]);
            if(flyweight_gpu_download(saved.conv.data(),runtime.state+layer.state_first,saved.conv.size()*sizeof(float),runtime.stream)!=0
               ||flyweight_gpu_download(saved.recurrent.data(),runtime.state+layer.state_second,saved.recurrent.size()*sizeof(float),runtime.stream)!=0)
                throw std::runtime_error("native MTP fold check download failed");
            stash.push_back(std::move(saved));
        }
        if(flyweight_gpu_stream_sync(runtime.stream)!=0)
            throw std::runtime_error("native MTP fold check sync failed");
    };
    std::vector<Saved> folded;
    capture(folded);
    qwen_snapshot_delta_state(runtime,true);
    std::array<std::uint32_t,8> replay_tokens{};
    std::uint64_t replay_hidden=0;
    qwen_verify_target_rows(
        runtime,inputs,static_cast<int>(valid),replay_tokens.data(),&replay_hidden);
    std::vector<Saved> replayed;
    capture(replayed);
    for(std::size_t index=0;index<folded.size();++index){
        auto compare=[&](const std::vector<float>&fold,const std::vector<float>&replay,const char*what){
            if(std::memcmp(fold.data(),replay.data(),fold.size()*sizeof(float))==0)return;
            float maximum=0.0f;
            for(std::size_t element=0;element<fold.size();++element)
                maximum=std::max(maximum,std::fabs(fold[element]-replay[element]));
            std::fprintf(stderr,"mtp fold check failed: delta layer %zu %s state max_diff=%g rows=%u\n",
                index,what,maximum,valid);
            throw std::runtime_error("native MTP fold state diverged from replay");
        };
        compare(folded[index].conv,replayed[index].conv,"conv");
        compare(folded[index].recurrent,replayed[index].recurrent,"recurrent");
    }
}

static void qwen_mtp_commit_true_cache(
    FlyweightV2QwenRuntime& runtime, std::uint64_t base_cache_tokens,
    const std::uint32_t* inputs, std::uint32_t count,
    std::uint64_t verified_hidden
) {
    if (!count) return;
    // The hand-over width, not the model's hidden width: on qwen4exp every one
    // of these vectors is a hyper-connection stream row (hc_count*hidden).
    const int hidden = runtime.qwen4exp
        ? static_cast<int>(runtime.model->config.hyper_connection_count
                           * runtime.model->config.hidden_size)
        : static_cast<int>(runtime.model->config.hidden_size);
    const int hidden_elements = static_cast<int>(count) * hidden;
    const auto stable_hidden =
        runtime.state + runtime.mtp_verified_hidden_offset;
    void* batch_copy_args[] = {
        &verified_hidden, const_cast<std::uint64_t*>(&stable_hidden),
        const_cast<int*>(&hidden_elements),
    };
    if (flyweight_gpu_launch_named(
            "qwen_copy_vector", (hidden_elements + 255) / 256, 1, 256, 0,
            runtime.stream, batch_copy_args) != 0) {
        throw std::runtime_error("native MTP verified-hidden snapshot failed");
    }

    // Draft step zero consumed the target model's real hidden state, so its KV
    // entry already has exactly the semantics of qwen_mtp_append_pair.  Only
    // later recursive steps used draft hidden states and need rebuilding.
    runtime.mtp_cache_tokens = base_cache_tokens + 1;
    const auto initial_hidden =
        runtime.state + runtime.mtp_target_hidden_offset;
    // The batch2 shortcut is the qwen-shaped fusion; qwen4exp takes the plain
    // per-pair path instead (correct, one extra launch set at count==3).
    if(count==3&&!runtime.qwen4exp){
        qwen_mtp_append_pair_batch2(
            runtime,inputs+1,stable_hidden,
            stable_hidden+static_cast<std::uint64_t>(hidden)*sizeof(float));
    }else for (std::uint32_t index = 1; index < count; ++index) {
        const auto preceding_hidden = stable_hidden +
            static_cast<std::uint64_t>(index - 1)*hidden*sizeof(float);
        qwen_mtp_append_pair(
            runtime, inputs[index], preceding_hidden);
    }

    auto final_source = stable_hidden + static_cast<std::uint64_t>(count - 1)
        * hidden * sizeof(float);
    auto final_target = initial_hidden;
    void* final_copy_args[] = {
        &final_source, &final_target, const_cast<int*>(&hidden),
    };
    if (flyweight_gpu_launch_named(
            "qwen_copy_vector", (hidden + 255) / 256, 1, 256, 0,
            runtime.stream, final_copy_args) != 0 ||
        flyweight_gpu_stream_sync(runtime.stream) != 0) {
        throw std::runtime_error("native MTP true-cache commit failed");
    }
}

// One MTP draft-and-verify round for the active sequence: draft `wanted` tokens
// from the draft block, verify them in a single batched target pass, and commit
// the prefix the target agrees with. Writes the committed tokens to `out` (which
// must hold at least `wanted` entries) and returns how many were committed --
// always at least one, because the verifier's own token is committed even when
// every draft is rejected. The returned tokens are target outputs, so drafting
// quality only moves the acceptance rate, never the text.
static uint32_t qwen_mtp_round(FlyweightV2QwenRuntime&runtime,uint32_t next_token,uint32_t wanted,uint32_t*out){
    if(wanted>8)wanted=8;
    if(!wanted)wanted=1;
    // The draft block's KV cache is sized for the context limit and, unlike the
    // target caches, is never rewound by a new request. Recycling it when it
    // would overflow keeps a long-running server in bounds; the cost is a few
    // poor drafts, not a wrong answer.
    if(runtime.mtp_cache_tokens+wanted+1>runtime.options.context_limit)runtime.mtp_cache_tokens=0;
    const auto base_cache_tokens=runtime.mtp_cache_tokens;
    std::array<uint32_t,8>drafts{};
    uint32_t draft_input=next_token;
    std::uint64_t draft_hidden=runtime.state+runtime.mtp_target_hidden_offset;
    const auto draft_started=std::chrono::steady_clock::now();
    for(uint32_t index=0;index<wanted;++index){
        drafts[index]=runtime.qwen4exp
            ?qwen4exp_mtp_draft(runtime,draft_input,draft_hidden)
            :qwen_mtp_draft(runtime,draft_input,draft_hidden);
        draft_input=drafts[index];
        draft_hidden=runtime.state+runtime.mtp_draft_hidden_offset;
    }
    runtime.mtp_draft_nanoseconds+=std::chrono::duration_cast<std::chrono::nanoseconds>(std::chrono::steady_clock::now()-draft_started).count();
    std::array<uint32_t,8>inputs{},verified{};
    inputs[0]=next_token;
    for(uint32_t index=1;index<wanted;++index)inputs[index]=drafts[index-1];
    // Arm the fold: the verify pass records each DeltaNet layer's transition
    // inputs in its retention arena so a rejection can rebuild the state
    // without re-running the forward. FLYWEIGHT_MTP_FOLD=0 restores the full
    // replay path.
    const char*fold_env=std::getenv("FLYWEIGHT_MTP_FOLD");
    const bool fold_enabled=!(fold_env&&fold_env[0]=='0');
    // The fold rebuilds DeltaNet state and nothing else. qwen4exp's PLE layer
    // carries a SECOND recurrent ring -- the dilated conv -- and the fold has
    // no retention for its inputs, so a rejection would restore that ring to
    // the pre-round state and never replay the accepted token into it. The
    // ring then runs one token behind per round, which does not show up as an
    // immediate wrong token: the conv taps at 0, dilation, 2*dilation back, so
    // the drift only reaches the output a few steps later (measured: token 3
    // on a dilation-3 fixture). A checkpoint with a PLE layer therefore takes
    // the full-replay rollback until the ring joins the retention arena.
    const bool ple_blocks_fold=!runtime.model->config.ple_layers.empty();
    runtime.mtp_fold_capture=fold_enabled&&runtime.mtp_delta_layers&&!ple_blocks_fold;
    runtime.mtp_fold_valid=fold_enabled&&!ple_blocks_fold;
    runtime.mtp_fold_rows=wanted;
    qwen_snapshot_delta_state(runtime,false);
    const auto batch_started=std::chrono::steady_clock::now();
    std::uint64_t batch_hidden=0;
    qwen_verify_target_rows(runtime,inputs.data(),static_cast<int>(wanted),verified.data(),&batch_hidden);
    runtime.mtp_fold_capture=false;
    runtime.mtp_verify_nanoseconds+=std::chrono::duration_cast<std::chrono::nanoseconds>(std::chrono::steady_clock::now()-batch_started).count();
    uint32_t valid=0;
    bool rejected=false;
    for(;valid<wanted;++valid)if(verified[valid]!=drafts[valid]){++valid;rejected=true;break;}
    if(rejected&&valid<wanted&&runtime.mtp_fold_valid){
        // Fold rollback: the batch verify already produced correct KV rows,
        // hidden states and verified tokens for the accepted prefix -- only
        // the DeltaNet conv/recurrent state ran ahead through the rejected
        // rows. Restore the snapshot and replay just the recurrence over the
        // accepted tokens (a no-op for attention-only models) instead of
        // re-running the whole forward.
        const auto rollback_started=std::chrono::steady_clock::now();
        qwen_snapshot_delta_state(runtime,true);
        qwen_mtp_fold_delta_state(runtime,static_cast<int>(valid));
        if(std::getenv("FLYWEIGHT_MTP_FOLD_CHECK"))
            qwen_mtp_fold_check(runtime,inputs.data(),valid);
        qwen_mtp_commit_true_cache(
            runtime,base_cache_tokens,inputs.data(),valid,batch_hidden);
        runtime.processed_tokens.insert(runtime.processed_tokens.end(),inputs.begin(),inputs.begin()+valid);
        runtime.position+=valid;
        runtime.last_output_token=verified[valid-1];
        runtime.last_output_greedy=true;
        runtime.decode_calls+=valid;
        const auto rollback_elapsed=std::chrono::duration_cast<std::chrono::nanoseconds>(std::chrono::steady_clock::now()-rollback_started).count();
        runtime.decode_nanoseconds+=rollback_elapsed;
        runtime.mtp_accepted_tokens+=valid-1;
        ++runtime.mtp_rejected_tokens;
        runtime.mtp_rollback_nanoseconds+=rollback_elapsed;
    }else if(rejected&&valid<wanted){
        const auto rollback_started=std::chrono::steady_clock::now();
        const auto batch_rejected_token=verified[valid-1];
        std::vector<float>batch_trace;
        const bool trace=std::getenv("FLYWEIGHT_MTP_TRACE")!=nullptr;
        const int trace_hidden=static_cast<int>(runtime.model->config.hidden_size);
        if(trace){
            batch_trace.resize(trace_hidden);
            const auto trace_source=batch_hidden+static_cast<std::uint64_t>(valid-1)*trace_hidden*sizeof(float);
            if(flyweight_gpu_download(batch_trace.data(),trace_source,trace_hidden*sizeof(float),runtime.stream)!=0||flyweight_gpu_stream_sync(runtime.stream)!=0)throw std::runtime_error("native MTP trace download failed");
        }
        qwen_snapshot_delta_state(runtime,true);
        std::uint64_t replay_hidden=0;
        const auto replay_started=std::chrono::steady_clock::now();
        qwen_verify_target_rows(runtime,inputs.data(),static_cast<int>(valid),verified.data(),&replay_hidden);
        qwen_mtp_commit_true_cache(
            runtime,base_cache_tokens,inputs.data(),valid,replay_hidden);
        runtime.processed_tokens.insert(runtime.processed_tokens.end(),inputs.begin(),inputs.begin()+valid);
        runtime.position+=valid;
        runtime.last_output_token=verified[valid-1];
        runtime.last_output_greedy=true;
        runtime.decode_calls+=valid;
        runtime.decode_nanoseconds+=std::chrono::duration_cast<std::chrono::nanoseconds>(std::chrono::steady_clock::now()-replay_started).count();
        if(trace){
            std::vector<float>replay_trace(trace_hidden);
            const auto trace_source=runtime.state+runtime.mtp_target_hidden_offset;
            if(flyweight_gpu_download(replay_trace.data(),trace_source,trace_hidden*sizeof(float),runtime.stream)!=0||flyweight_gpu_stream_sync(runtime.stream)!=0)throw std::runtime_error("native MTP replay trace download failed");
            float maximum=0.0f;
            for(int index=0;index<trace_hidden;++index)maximum=std::max(maximum,std::fabs(batch_trace[index]-replay_trace[index]));
            std::fprintf(stderr,"mtp reject position=%llu row=%u draft=%u batch=%u replay=%u hidden_max_diff=%g\n",static_cast<unsigned long long>(runtime.position-valid),valid-1,drafts[valid-1],batch_rejected_token,verified[valid-1],maximum);
        }
        runtime.mtp_accepted_tokens+=valid-1;
        ++runtime.mtp_rejected_tokens;
        runtime.mtp_rollback_nanoseconds+=std::chrono::duration_cast<std::chrono::nanoseconds>(std::chrono::steady_clock::now()-rollback_started).count();
    }else{
        qwen_mtp_commit_true_cache(
            runtime,base_cache_tokens,inputs.data(),wanted,batch_hidden);
        runtime.processed_tokens.insert(runtime.processed_tokens.end(),inputs.begin(),inputs.begin()+wanted);
        runtime.position+=wanted;
        runtime.last_output_token=verified[wanted-1];
        runtime.last_output_greedy=true;
        runtime.decode_calls+=wanted;
        runtime.decode_nanoseconds+=std::chrono::duration_cast<std::chrono::nanoseconds>(std::chrono::steady_clock::now()-batch_started).count();
        runtime.mtp_accepted_tokens+=rejected?wanted-1:wanted;
        if(rejected)++runtime.mtp_rejected_tokens;
        valid=wanted;
    }
    for(uint32_t index=0;index<valid;++index)out[index]=verified[index];
    return valid;
}

static bool qwen_mtp_adaptive_enabled(){
    const char*setting=std::getenv("FLYWEIGHT_MTP_ADAPTIVE");
    return !setting||setting[0]!='0';
}

static constexpr std::uint32_t kQwenMtpBaselineTokens=8;
static constexpr std::uint32_t kQwenMtpTrialRounds=4;
// Tokens discarded before calibration starts. The first tokens after prefill
// are far slower than steady state -- cold expert residency, GPU clocks still
// ramping -- and the old gate spent its entire sequential baseline there before
// timing MTP afterwards. That inflated the baseline enough that MTP measured as
// a win and was kept while actually running ~30% slower.
static constexpr std::uint32_t kQwenMtpWarmupTokens=8;
static constexpr std::uint64_t kQwenMtpKeepPercent=80;

static bool qwen_mtp_should_draft(const FlyweightV2QwenRuntime&runtime){
    if(!qwen_mtp_adaptive_enabled())return runtime.options.mtp_drafts!=0;
    if(runtime.options.mtp_drafts<2||runtime.mtp_adaptive_disabled)return false;
    if(runtime.mtp_calibration_done)return true;
    if(runtime.mtp_calibration_warmup_tokens<kQwenMtpWarmupTokens)return false;
    // Alternate the two arms so both are sampled in the same thermal and cache
    // state. Whichever arm fills first stops, and the other runs to its target.
    const bool need_decode=
        runtime.mtp_calibration_decode_tokens<kQwenMtpBaselineTokens;
    const bool need_rounds=
        runtime.mtp_calibration_rounds<kQwenMtpTrialRounds;
    if(need_decode&&need_rounds)return runtime.mtp_calibration_draft_turn;
    // One arm is full, so run the other. Both full is unreachable: whichever
    // record_* call filled the last slot ran qwen_mtp_finish_calibration, and
    // mtp_calibration_done returned above.
    return need_rounds;
}

static void qwen_mtp_finish_calibration(FlyweightV2QwenRuntime&runtime);

static void qwen_mtp_record_decode(
    FlyweightV2QwenRuntime&runtime,std::uint64_t nanoseconds
){
    if(!qwen_mtp_adaptive_enabled()||runtime.mtp_adaptive_disabled||
       runtime.mtp_calibration_done)return;
    if(runtime.mtp_calibration_warmup_tokens<kQwenMtpWarmupTokens){
        ++runtime.mtp_calibration_warmup_tokens;
        return;
    }
    runtime.mtp_calibration_draft_turn=true;
    if(runtime.mtp_calibration_decode_tokens<kQwenMtpBaselineTokens){
        runtime.mtp_calibration_decode_nanoseconds+=nanoseconds;
        ++runtime.mtp_calibration_decode_tokens;
    }
    qwen_mtp_finish_calibration(runtime);
}

// Both arms full -> decide. This must be reachable from whichever record_*
// call completes the pair: the sequential arm can be the last to fill, and
// deciding only inside record_round left should_draft returning false forever
// with the verdict never reached -- MTP off by accident rather than by verdict.
static void qwen_mtp_finish_calibration(FlyweightV2QwenRuntime&runtime){
    if(runtime.mtp_calibration_done||
       runtime.mtp_calibration_rounds<kQwenMtpTrialRounds||
       runtime.mtp_calibration_decode_tokens<kQwenMtpBaselineTokens||
       !runtime.mtp_calibration_round_tokens)return;
    runtime.mtp_calibration_done=true;
    const auto baseline_per_token=
        runtime.mtp_calibration_decode_nanoseconds/
        runtime.mtp_calibration_decode_tokens;
    const auto mtp_per_token=
        runtime.mtp_calibration_round_nanoseconds/
        runtime.mtp_calibration_round_tokens;
    // The verifier is intentionally layer-major, so it is not bit-identical to
    // token-major decode.  Keep it only for a decisive win: besides absorbing
    // timing and rejection variance, the short trial then ends well before
    // harmless rounding differences can accumulate into a greedy-token change.
    const bool keep=
        mtp_per_token*100<baseline_per_token*kQwenMtpKeepPercent;
    if(!keep)runtime.mtp_adaptive_disabled=true;
    // Report both verdicts. Only the fallback used to be logged, so a gate that
    // wrongly kept MTP -- the case that costs the user throughput -- was
    // completely silent.
    if(!runtime.mtp_adaptive_reported){
        std::fprintf(stderr,
            "[flyweight] MTP calibration: %llu us/token speculative vs "
            "%llu us/token ordinary decode -- %s\n",
            static_cast<unsigned long long>(mtp_per_token/1000),
            static_cast<unsigned long long>(baseline_per_token/1000),
            keep?"keeping MTP":"falling back to ordinary decode");
        runtime.mtp_adaptive_reported=true;
    }
}

static void qwen_mtp_record_round(
    FlyweightV2QwenRuntime&runtime,std::uint64_t nanoseconds,
    std::uint32_t committed
){
    if(!qwen_mtp_adaptive_enabled()||runtime.mtp_adaptive_disabled||
       runtime.mtp_calibration_done)return;
    runtime.mtp_calibration_draft_turn=false;
    runtime.mtp_calibration_round_nanoseconds+=nanoseconds;
    runtime.mtp_calibration_round_tokens+=committed;
    ++runtime.mtp_calibration_rounds;
    qwen_mtp_finish_calibration(runtime);
}

int flyweight_v2_qwen_runtime_generate(FlyweightV2QwenRuntime*runtime,const uint32_t*prompt,uint64_t prompt_count,uint64_t max_tokens,FlyweightV2TokenCallback callback,void*user){return guarded([&]{
    if(!runtime||!prompt||!prompt_count||!max_tokens||!callback)throw std::runtime_error("invalid native Qwen generation arguments");
    if(prompt_count>runtime->options.context_limit||
       max_tokens>runtime->options.context_limit-prompt_count)
        throw std::runtime_error("native Qwen generation exceeds the context limit");
    if(!runtime->engine_tasks.empty()||!runtime->engine_pending.empty())throw std::runtime_error("the cooperative engine is active; blocking generate is unavailable");
    qwen_unfreeze_expert_residency(*runtime);
    QwenExpertHistorySaveGuard history_save{*runtime};
    QwenKvOccupancyGuard kv_occupancy{*runtime};
    // Pick the decode slot for this prompt before any reuse/diagnostics run.
    // Slots may differ in size, so the router is told what the whole request
    // will occupy, not just the prompt.
    qwen_route_sequence(*runtime, prompt, prompt_count, prompt_count + max_tokens);
    QwenPromptPlan plan;
    int status=qwen_prompt_begin(runtime,prompt,prompt_count,plan);if(status)return status;
    uint64_t index=plan.prompt_start;
    uint32_t next_token=plan.next_token;
    bool prefill_done=false;
    while(!prefill_done){
        status=qwen_prefill_unit(runtime,prompt,prompt_count,plan,index,next_token,prefill_done);
        if(status)return status;
    }
    qwen_prompt_finish(runtime,prompt,prompt_count,next_token,max_tokens);
    QwenResidencyEpochGuard residency_epoch{*runtime};
    if(runtime->options.mtp_drafts){
        uint64_t emitted=0;
        if(callback(next_token,user)!=0)return 0;
        ++emitted;
        while(emitted<max_tokens&&!runtime->cancelled){
            if(!qwen_mtp_should_draft(*runtime)){
                const auto decode_started=std::chrono::steady_clock::now();
                status=flyweight_v2_qwen_runtime_decode(
                    runtime,next_token,&next_token);
                if(status)return status;
                qwen_mtp_record_decode(
                    *runtime,
                    std::chrono::duration_cast<std::chrono::nanoseconds>(
                        std::chrono::steady_clock::now()-decode_started).count());
                if(callback(next_token,user)!=0)return 0;
                ++emitted;
                continue;
            }
            const auto wanted=static_cast<uint32_t>(std::min<uint64_t>(
                runtime->options.mtp_drafts,max_tokens-emitted
            ));
            std::array<uint32_t,8>produced{};
            const auto round_started=std::chrono::steady_clock::now();
            const auto valid=qwen_mtp_round(*runtime,next_token,wanted,produced.data());
            qwen_mtp_record_round(
                *runtime,
                std::chrono::duration_cast<std::chrono::nanoseconds>(
                    std::chrono::steady_clock::now()-round_started).count(),
                valid);
            for(uint32_t index=0;index<valid&&emitted<max_tokens;++index){
                next_token=produced[index];
                if(callback(next_token,user)!=0)return 0;
                ++emitted;
            }
        }
        return 0;
    }
    for(uint64_t index=0;index<max_tokens&&!runtime->cancelled;index++){
        if(callback(next_token,user)!=0)break;
        if(index+1<max_tokens){status=flyweight_v2_qwen_runtime_decode(runtime,next_token,&next_token);if(status)return status;}
    }
    return 0;
});}

static void qwen_task_submit_impl(FlyweightV2QwenRuntime*runtime,
        const uint32_t*prompt,uint64_t prompt_count,uint64_t max_tokens,
        const uint32_t*stop_tokens,uint64_t stop_count,float temperature,
        uint32_t top_k,float top_p,float repetition_penalty,
        float presence_penalty,float frequency_penalty,uint32_t penalty_window,
        uint64_t seed,bool has_seed,uint64_t*task_id,
        const char*tool_specification=nullptr) {
    if(!runtime||!prompt||!prompt_count||!max_tokens||!task_id)throw std::runtime_error("invalid native Qwen task arguments");
    if(prompt_count>runtime->options.context_limit||
       max_tokens>runtime->options.context_limit-prompt_count)
        throw std::runtime_error("native Qwen generation exceeds the context limit");
    if(!std::isfinite(temperature)||temperature<0.0f)
        throw std::runtime_error("native Qwen temperature must be finite and non-negative");
    if(!std::isfinite(top_p)||top_p<=0.0f||top_p>1.0f)
        throw std::runtime_error("native Qwen top_p must be in (0, 1]");
    // A penalty below 1 rewards repetition, which is the failure this exists
    // to prevent; an unbounded one silences a token the answer may legitimately
    // need. Both are far more likely to be a caller's unit mistake than intent.
    if(!std::isfinite(repetition_penalty)||repetition_penalty<1.0f||
       repetition_penalty>2.0f)
        throw std::runtime_error("native Qwen repetition_penalty must be in [1, 2]");
    // These two carry their OpenAI names, so they take the OpenAI range --
    // including the negative half, which rewards repetition. Nothing here wants
    // that, but a client that asks for it is asking deliberately.
    if(!std::isfinite(presence_penalty)||presence_penalty<-2.0f||
       presence_penalty>2.0f)
        throw std::runtime_error("native Qwen presence_penalty must be in [-2, 2]");
    if(!std::isfinite(frequency_penalty)||frequency_penalty<-2.0f||
       frequency_penalty>2.0f)
        throw std::runtime_error("native Qwen frequency_penalty must be in [-2, 2]");
    QwenEngineTask task;
    task.prompt.assign(prompt,prompt+prompt_count);
    if(stop_tokens&&stop_count)task.stop_tokens.assign(stop_tokens,stop_tokens+stop_count);
    task.max_tokens=max_tokens;
    task.sampling.temperature=temperature;
    task.sampling.top_k=top_k;
    task.sampling.top_p=top_p;
    task.sampling.repetition_penalty=repetition_penalty;
    task.sampling.presence_penalty=presence_penalty;
    task.sampling.frequency_penalty=frequency_penalty;
    task.sampling.penalty_window=penalty_window;
    // An escape hatch for the constraint, because it sits between the model and
    // every tool call: if it ever refuses something a checkpoint legitimately
    // emits, this restores the previous behaviour without a rebuild, and the
    // tolerant parser in the server catches what it used to catch.
    static const bool grammar_enabled=[]{
        const char*setting=std::getenv("FLYWEIGHT_TOOL_GRAMMAR");
        return !setting||setting[0]!='0';
    }();
    // The same hatch for the response constraint, separately: one can be
    // switched off without giving up the other.
    static const bool response_grammar_enabled=[]{
        const char*setting=std::getenv("FLYWEIGHT_RESPONSE_GRAMMAR");
        return !setting||setting[0]!='0';
    }();
    // A specification the caller could not parse is worth failing on: silently
    // dropping it would serve unconstrained calls that look constrained.
    if(tool_specification&&*tool_specification){
        try{
            const auto constraints=
                flyweight::v2::tools::parse_constraints(tool_specification);
            if(grammar_enabled&&!constraints.tools.empty())
                task.sampling.grammar=
                    flyweight::v2::tools::Grammar(constraints.tools);
            // Same escape hatch as the grammar: FLYWEIGHT_TOOL_GRAMMAR=0
            // restores the previous (unconstrained) behaviour for both.
            if(grammar_enabled&&constraints.tool_calls_forbidden)
                task.sampling.markup_ban.enable();
            if(response_grammar_enabled&&constraints.response_enabled)
                task.sampling.response_grammar=
                    flyweight::v2::tools::ResponseGrammar(
                        constraints.response_shape,constraints.thinking_open);
        }catch(const std::exception&error){
            throw std::runtime_error(
                std::string("native Qwen constraint specification is invalid: ")+
                error.what());
        }
    }
    std::lock_guard<std::mutex> lock(runtime->engine_mutex);
    constexpr std::size_t kMaxQueuedTasks = 256;
    if(runtime->engine_pending.size()>=kMaxQueuedTasks||
       runtime->engine_tasks.size()>=kMaxQueuedTasks-runtime->engine_pending.size())
        throw std::runtime_error("native Qwen engine queue is full");
    task.id=runtime->engine_next_task_id++;
    task.sampling.rng=has_seed?seed:
        static_cast<std::uint64_t>(std::chrono::steady_clock::now().time_since_epoch().count())^
        (task.id*0x9e3779b97f4a7c15ULL);
    if(!task.sampling.rng)task.sampling.rng=0x9e3779b97f4a7c15ULL;
    *task_id=task.id;
    runtime->engine_pending.push_back(std::move(task));
}

int flyweight_v2_qwen_task_submit(FlyweightV2QwenRuntime*runtime,const uint32_t*prompt,uint64_t prompt_count,uint64_t max_tokens,const uint32_t*stop_tokens,uint64_t stop_count,uint64_t*task_id){return guarded([&]{
    qwen_task_submit_impl(runtime,prompt,prompt_count,max_tokens,stop_tokens,
        stop_count,0.0f,20,0.95f,1.0f,0.0f,0.0f,64,0,false,task_id);
    return 0;
});}

int flyweight_v2_qwen_task_submit_penalties(FlyweightV2QwenRuntime*runtime,const uint32_t*prompt,uint64_t prompt_count,uint64_t max_tokens,const uint32_t*stop_tokens,uint64_t stop_count,float temperature,uint32_t top_k,float top_p,float repetition_penalty,float presence_penalty,float frequency_penalty,uint32_t penalty_window,uint64_t seed,uint32_t has_seed,uint64_t*task_id){return guarded([&]{
    qwen_task_submit_impl(runtime,prompt,prompt_count,max_tokens,stop_tokens,
        stop_count,temperature,top_k,top_p,repetition_penalty,presence_penalty,
        frequency_penalty,penalty_window,seed,has_seed!=0,task_id);
    return 0;
});}

// The penalties entry point plus the tool specification the sampler constrains
// tool calls to. Null or empty leaves the sampler unconstrained, which is what
// every request without tools wants.
int flyweight_v2_qwen_task_submit_grammar(FlyweightV2QwenRuntime*runtime,const uint32_t*prompt,uint64_t prompt_count,uint64_t max_tokens,const uint32_t*stop_tokens,uint64_t stop_count,float temperature,uint32_t top_k,float top_p,float repetition_penalty,float presence_penalty,float frequency_penalty,uint32_t penalty_window,uint64_t seed,uint32_t has_seed,const char*tool_specification,uint64_t*task_id){return guarded([&]{
    qwen_task_submit_impl(runtime,prompt,prompt_count,max_tokens,stop_tokens,
        stop_count,temperature,top_k,top_p,repetition_penalty,presence_penalty,
        frequency_penalty,penalty_window,seed,has_seed!=0,task_id,
        tool_specification);
    return 0;
});}

int flyweight_v2_qwen_task_submit_sampling(FlyweightV2QwenRuntime*runtime,const uint32_t*prompt,uint64_t prompt_count,uint64_t max_tokens,const uint32_t*stop_tokens,uint64_t stop_count,float temperature,uint32_t top_k,float top_p,uint64_t seed,uint32_t has_seed,uint64_t*task_id){return guarded([&]{
    qwen_task_submit_impl(runtime,prompt,prompt_count,max_tokens,stop_tokens,
        stop_count,temperature,top_k,top_p,1.0f,0.0f,0.0f,64,
        seed,has_seed!=0,task_id);
    return 0;
});}

int flyweight_v2_qwen_task_cancel(FlyweightV2QwenRuntime*runtime,uint64_t task_id){return guarded([&]{
    if(!runtime||!task_id)throw std::runtime_error("invalid native Qwen task handle");
    std::lock_guard<std::mutex> lock(runtime->engine_mutex);
    runtime->engine_cancel_requests.push_back(task_id);
    return 0;
});}

// Move the active slot's mirrored bookkeeping (position/last_output/processed
// tokens) back into sequences[active] (park) or out again (unpark), so code can
// address every slot uniformly through sequences[] without switching.
static void qwen_park_active(FlyweightV2QwenRuntime& rt, bool park) {
    QwenSequence& a = rt.sequences[rt.active_sequence];
    if (park) {
        a.position = rt.position;
        a.last_output_token = rt.last_output_token;
        a.last_output_greedy = rt.last_output_greedy;
        a.processed_tokens.swap(rt.processed_tokens);
    } else {
        rt.position = a.position;
        rt.last_output_token = a.last_output_token;
        rt.last_output_greedy = a.last_output_greedy;
        rt.processed_tokens.swap(a.processed_tokens);
    }
}

// Decode ONE token for each of `n` sequences with layer-level CPU/GPU overlap.
// Everything runs on the single stream with the existing single-row kernels;
// the win is scheduling: per layer, every sequence's GPU pre-MoE work (norms,
// attention/DeltaNet, router + async readback, shared experts) is queued FIRST,
// so while the CPU runs sequence A's expert phase (route policy, paging,
// qwen_cpu_moe) the GPU is already executing sequence B's queued work. Decode
// is dominated by that CPU phase, so overlap approaches aggregate 2x at n=2.
// Per-sequence workspace/host slices keep buffers independent; the shared
// expert-paging staging area is fenced with staging_event against its async
// stream-queued consumers. Requires the caller to have parked the active
// mirror; requires a decode policy with CPU expert execution and no MTP.
static void qwen_decode_multi(FlyweightV2QwenRuntime* runtime, std::size_t n,
        const std::size_t* slots, const std::uint32_t* inputs, std::uint32_t* outputs) {
    qwen_absorb_registration(*runtime);
    const auto decode_started = std::chrono::steady_clock::now();
    const auto expert_policy=qwen_expert_policy(
        *runtime,flyweight::v2::ExpertExecutionPhase::decode);
    const int hidden_size = static_cast<int>(runtime->model->config.hidden_size);
    const int experts = static_cast<int>(runtime->model->config.expert_count);
    const int configured_top_k = static_cast<int>(runtime->model->config.expert_used_count);
    const int top_k = (runtime->options.expert_top_k > 0 && static_cast<int>(runtime->options.expert_top_k) < configured_top_k)
        ? static_cast<int>(runtime->options.expert_top_k) : configured_top_k;
    const float epsilon = runtime->model->config.rms_norm_epsilon ? runtime->model->config.rms_norm_epsilon : 1.0e-6f;
    const int intermediate = static_cast<int>(runtime->moe_intermediate);
    auto* staging = static_cast<std::uint8_t*>(runtime->host_staging);
    const std::uint64_t shared_base = n * runtime->decode_host_block_bytes;
    struct Seq {
        std::size_t slot; std::uint32_t input; std::uint64_t state, position;
        std::uint64_t hidden, residual, normalized, first, second, third, fourth,
            dense_q8, dense_q8_scales, activated, router_logits,
            selected_device, route_weights, logits, argmax_device,
            attention_scores;
        // qwen4exp gated-residual slices of this sequence's workspace copy.
        std::uint64_t hc_streams, hc_normed, hc_wide, hc_low, hc_gates,
            ple_embed;
        // qwen4exp QSA scratch slices.
        std::uint64_t qsa_query, qsa_block_scores, qsa_slots;
        std::int32_t* selected_host; float *cpu_weights, *cpu_input, *cpu_activated, *cpu_output;
        std::uint64_t* winner_host;
    };
    std::vector<Seq> seqs(n);
    const auto&workspace_layout=runtime->decode_workspace_layout;
    const auto&host_layout=runtime->decode_host_layout;
    if(n>runtime->workspace_bytes/workspace_layout.bytes)
        throw std::runtime_error("native Qwen multi-decode workspace overflow");
    for (std::size_t i = 0; i < n; ++i) {
        Seq& s = seqs[i];
        s.slot = slots[i]; s.input = inputs[i];
        s.state = runtime->sequences[s.slot].state;
        s.position = runtime->sequences[s.slot].position;
        if (s.position >= runtime->options.context_limit) throw std::runtime_error("native Qwen context limit exceeded");
        if (s.input >= runtime->model->config.vocabulary_size) throw std::runtime_error("native Qwen input token is out of range");
        const auto base=runtime->workspace+i*workspace_layout.bytes;
        s.hidden = workspace_layout.hidden.address(base);
        s.residual = workspace_layout.residual.address(base);
        s.normalized = workspace_layout.normalized.address(base);
        s.first = workspace_layout.first.address(base);
        s.second = workspace_layout.second.address(base);
        s.third = workspace_layout.third.address(base);
        s.fourth = workspace_layout.fourth.address(base);
        s.dense_q8 = workspace_layout.dense_q8.address(base);
        s.dense_q8_scales = workspace_layout.dense_q8_scales.address(base);
        s.activated = workspace_layout.activated.address(base);
        s.router_logits = workspace_layout.router_logits.address(base);
        s.selected_device = workspace_layout.selected_device.address(base);
        s.route_weights = workspace_layout.route_weights.address(base);
        s.logits = workspace_layout.logits.address(base);
        s.argmax_device = workspace_layout.argmax_device.address(base);
        s.attention_scores = workspace_layout.attention_scores.address(base);
        s.hc_streams = workspace_layout.streams.address(base);
        s.hc_normed = workspace_layout.hc_normed.address(base);
        s.hc_wide = workspace_layout.hc_wide.address(base);
        s.hc_low = workspace_layout.hc_low.address(base);
        s.hc_gates = workspace_layout.hc_gates.address(base);
        s.ple_embed = workspace_layout.ple_embed.address(base);
        s.qsa_query = workspace_layout.qsa_query.address(base);
        s.qsa_block_scores = workspace_layout.qsa_block_scores.address(base);
        s.qsa_slots = workspace_layout.qsa_slots.address(base);
        auto* block = staging + i * runtime->decode_host_block_bytes;
        s.selected_host = reinterpret_cast<std::int32_t*>(
            block+host_layout.selected.offset);
        s.cpu_weights = reinterpret_cast<float*>(block+host_layout.weights.offset);
        s.cpu_input = reinterpret_cast<float*>(block+host_layout.input.offset);
        s.cpu_activated = reinterpret_cast<float*>(
            block+host_layout.activated.offset);
        s.cpu_output = reinterpret_cast<float*>(block+host_layout.output.offset);
        s.winner_host = reinterpret_cast<std::uint64_t*>(
            block+host_layout.winner.offset);
    }
    auto launch_named = [&](const char* name, std::uint32_t gx, std::uint32_t gy, std::uint32_t bx, void** args, std::uint32_t shared = 0) { if (flyweight_gpu_launch_named(name, gx, gy, bx, shared, runtime->stream, args) != 0) throw std::runtime_error(std::string("native Qwen CUDA kernel failed: ") + name); };
    std::uint64_t q8_cached_input = 0, q8_cached_normalized = 0;
    auto rms = [&](std::uint64_t input, std::uint64_t weights, std::uint64_t output) { int one_centered = 0; q8_cached_input = 0; void* args[] = {&input, &weights, &output, const_cast<int*>(&hidden_size), const_cast<float*>(&epsilon), &one_centered}; launch_named("rms_norm", 1, 1, 1024, args); };
    auto q8 = [&](std::uint64_t matrix, std::uint64_t input, std::uint64_t output, int in_size, int out_size) { if (flyweight_gpu_q8_matvec_transposed(matrix, input, output, in_size, out_size, runtime->stream) != 0) throw std::runtime_error("native Qwen Q8 projection failed"); };
    auto f32 = [&](std::uint64_t matrix, std::uint64_t input, std::uint64_t output, int in_size, int out_size) { void* args[] = {&matrix, &input, &output, &in_size, &out_size}; launch_named("qwen_f32_matvec_warp", (out_size + 7) / 8, 1, 256, args); };
    const char* iq2_q8_setting = std::getenv("FLYWEIGHT_IQ2_Q8_DECODE");
    const bool iq2_q8_enabled =
        !iq2_q8_setting || iq2_q8_setting[0] != '0';
    std::uint64_t active_dense_q8 = 0, active_dense_q8_scales = 0;
    auto q8_decode = [&](const char* kernel, std::uint64_t matrix,
                         std::uint64_t input, std::uint64_t output,
                         int in_size, int out_size) {
        if (!iq2_q8_enabled || (in_size & 255) ||
            !active_dense_q8 || !active_dense_q8_scales) return false;
        if (input != q8_cached_normalized || input != q8_cached_input) {
            void* quant_args[] = {
                &input, &active_dense_q8, &active_dense_q8_scales, &in_size};
            launch_named(
                "quantize_q8_blocks", (in_size + 31) / 32, 1, 32, quant_args);
            q8_cached_input = (input == q8_cached_normalized) ? input : 0;
        }
        void* matvec_args[] = {
            &matrix, &active_dense_q8, &active_dense_q8_scales, &output,
            &in_size, &out_size};
        launch_named(kernel, out_size, 1, 128, matvec_args);
        return true;
    };
    // Same type dispatch as the single-token decode: dense weights carry the
    // checkpoint's own type (bf16 for the NVFP4 builds), not always Q8_0.
    auto dense_matvec = [&](std::size_t index, std::uint64_t input, std::uint64_t output, int in_size, int out_size) {
        std::uint64_t matrix = runtime->device_tensors[index];
        const auto type = qwen_device_type(*runtime, index);
        if (type == 0) { void* args[] = {&matrix, &input, &output, &in_size, &out_size}; launch_named("qwen_f32_matvec_warp", (out_size + 7) / 8, 1, 256, args); return; }
        // bf16_matvec takes (rows, columns), the reverse of the quantized matvecs.
        if (type == 30) { void* args[] = {&matrix, &input, &output, &out_size, &in_size}; launch_named("bf16_matvec_warp", (out_size + 7) / 8, 1, 256, args); return; }
        const auto* format = flyweight::v2::qwen_format(type);
        if (format && format->matvec_q8_warp &&
            q8_decode(format->matvec_q8_warp, matrix, input, output, in_size, out_size)) return;
        const int status = qwen_matvec_driver(
            type, matrix, input, output, in_size, out_size, runtime->stream);
        if (status == 0) return;
        if (status == kQwenMatvecUnsupported)
            throw std::runtime_error("native Qwen dense projection type is unsupported: " + std::to_string(type));
        throw std::runtime_error("native Qwen dense projection failed");
    };
    auto add = [&](std::uint64_t target, std::uint64_t source) { float scale = 1.0f; int count = hidden_size; void* args[] = {&target, &source, &scale, &count}; launch_named("scaled_add", (hidden_size + 255) / 256, 1, 256, args); };
    // qwen4exp gated-residual bookends, per sequence; same math as the
    // single-sequence decode path, addressed off each Seq's workspace copy.
    const int hc_count = static_cast<int>(runtime->model->config.hyper_connection_count);
    const int hc_low_rank = static_cast<int>(runtime->model->config.hyper_connection_low_rank);
    const int hc_wide_size = hc_count * hidden_size;
    auto hc_pre_multi = [&](Seq& s, std::uint64_t norm_weights,
                            std::uint64_t down_index, std::uint64_t up_index,
                            std::uint64_t inject_index) {
        void* norm_args[] = {const_cast<std::uint64_t*>(&s.hc_streams), &norm_weights,
            const_cast<std::uint64_t*>(&s.hc_normed),
            const_cast<int*>(&hidden_size), const_cast<float*>(&epsilon)};
        launch_named("qwen4_group_rms", hc_count, 1, 256, norm_args);
        dense_matvec(down_index, s.hc_normed, s.hc_low, hc_wide_size, hc_low_rank);
        float divisor = static_cast<float>(hc_count);
        void* silu_args[] = {const_cast<std::uint64_t*>(&s.hc_low),
            const_cast<int*>(&hc_low_rank), &divisor};
        launch_named("qwen4_silu_scale",
            (static_cast<std::uint32_t>(hc_low_rank) + 255) / 256, 1, 256, silu_args);
        dense_matvec(up_index, s.hc_low, s.hc_wide, hc_low_rank, hc_wide_size);
        if (inject_index != std::numeric_limits<std::uint64_t>::max())
            dense_matvec(inject_index, s.hc_normed, s.hc_gates, hc_wide_size, hc_count);
        void* mix_args[] = {const_cast<std::uint64_t*>(&s.hc_normed),
            const_cast<std::uint64_t*>(&s.hc_wide),
            const_cast<std::uint64_t*>(&s.normalized),
            const_cast<int*>(&hc_count), const_cast<int*>(&hidden_size)};
        launch_named("qwen4_hc_mix",
            (static_cast<std::uint32_t>(hidden_size) + 255) / 256, 1, 256, mix_args);
        q8_cached_input = 0;
    };
    auto hc_post_multi = [&](Seq& s, std::uint64_t block_output) {
        void* inject_args[] = {const_cast<std::uint64_t*>(&s.hc_streams),
            &block_output, const_cast<std::uint64_t*>(&s.hc_gates),
            const_cast<int*>(&hc_count), const_cast<int*>(&hidden_size)};
        launch_named("qwen4_hc_inject",
            (static_cast<std::uint32_t>(hc_wide_size) + 255) / 256, 1, 256,
            inject_args);
    };
    auto ple_multi = [&](Seq& s, const QwenLayerPlan& layer) {
        dense_matvec(layer.ple_key, s.ple_embed, s.hc_wide, hidden_size, hc_wide_size);
        auto norm_key = runtime->device_tensors[layer.ple_norm_key];
        void* key_args[] = {const_cast<std::uint64_t*>(&s.hc_wide), &norm_key,
            const_cast<std::uint64_t*>(&s.hc_wide),
            const_cast<int*>(&hidden_size), const_cast<float*>(&epsilon)};
        launch_named("qwen4_group_rms", hc_count, 1, 256, key_args);
        auto norm_query = runtime->device_tensors[layer.ple_norm_query];
        void* query_args[] = {const_cast<std::uint64_t*>(&s.hc_streams), &norm_query,
            const_cast<std::uint64_t*>(&s.hc_normed),
            const_cast<int*>(&hidden_size), const_cast<float*>(&epsilon)};
        launch_named("qwen4_group_rms", hc_count, 1, 256, query_args);
        void* gate_args[] = {const_cast<std::uint64_t*>(&s.hc_wide),
            const_cast<std::uint64_t*>(&s.hc_normed),
            const_cast<std::uint64_t*>(&s.hc_gates),
            const_cast<int*>(&hidden_size)};
        launch_named("qwen4_ple_gate", hc_count, 1, 256, gate_args);
        dense_matvec(layer.ple_value, s.ple_embed, s.normalized, hidden_size, hidden_size);
        void* gv_args[] = {const_cast<std::uint64_t*>(&s.normalized),
            const_cast<std::uint64_t*>(&s.hc_gates),
            const_cast<std::uint64_t*>(&s.hc_wide),
            const_cast<int*>(&hc_count), const_cast<int*>(&hidden_size)};
        launch_named("qwen4_ple_gv",
            (static_cast<std::uint32_t>(hc_wide_size) + 255) / 256, 1, 256, gv_args);
        auto norm_conv = runtime->device_tensors[layer.ple_norm_conv];
        void* gvn_args[] = {const_cast<std::uint64_t*>(&s.hc_wide), &norm_conv,
            const_cast<std::uint64_t*>(&s.hc_normed),
            const_cast<int*>(&hidden_size), const_cast<float*>(&epsilon)};
        launch_named("qwen4_group_rms", hc_count, 1, 256, gvn_args);
        const auto& conv = runtime->model->tensors[layer.ple_conv];
        int kernel_size = static_cast<int>(conv.shape[0]);
        int dilation = static_cast<int>(std::max<std::uint32_t>(
            runtime->model->config.ple_ngram_size, 1));
        auto conv_weights = runtime->device_tensors[layer.ple_conv];
        std::uint64_t conv_state = s.state + layer.state_third;
        void* conv_args[] = {const_cast<std::uint64_t*>(&s.hc_normed), &conv_weights,
            &conv_state, const_cast<std::uint64_t*>(&s.hc_normed),
            const_cast<int*>(&hc_wide_size), &kernel_size, &dilation};
        launch_named("qwen4_ple_conv_step",
            (static_cast<std::uint32_t>(hc_wide_size) + 255) / 256, 1, 256, conv_args);
        void* ple_add_args[] = {const_cast<std::uint64_t*>(&s.hc_streams),
            const_cast<std::uint64_t*>(&s.hc_wide),
            const_cast<std::uint64_t*>(&s.hc_normed),
            const_cast<int*>(&hc_wide_size)};
        launch_named("qwen4_ple_add",
            (static_cast<std::uint32_t>(hc_wide_size) + 255) / 256, 1, 256,
            ple_add_args);
        q8_cached_input = 0;
    };
    for (auto& s : seqs) {
        const std::uint32_t embedding_token = s.input;
        const auto embedding = qwen_stage_embedding_rows(*runtime, &embedding_token, 1);
        const int token = runtime->embeddings_host_resident ? 0 : static_cast<int>(s.input); int width = hidden_size;
        void* args[] = {const_cast<std::uint64_t*>(&embedding), const_cast<std::uint64_t*>(&s.hidden), const_cast<int*>(&token), &width};
        launch_named(qwen_embedding_kernel(qwen_device_type(*runtime, runtime->token_embeddings), false), (hidden_size + 255) / 256, 1, 256, args);
        if (runtime->qwen4exp) {
            void* init_args[] = {const_cast<std::uint64_t*>(&s.hidden),
                const_cast<std::uint64_t*>(&s.hc_streams),
                const_cast<int*>(&hc_count), const_cast<int*>(&hidden_size)};
            launch_named("qwen4_hc_init",
                (static_cast<std::uint32_t>(hc_wide_size) + 255) / 256, 1, 256,
                init_args);
            if (runtime->ple_table != std::numeric_limits<std::uint64_t>::max() &&
                !runtime->model->config.ple_layers.empty())
                // The engine parks the active slot before batching
                // (qwen_park_active swaps the mirror into the slot vector), so
                // the per-slot vector is authoritative for EVERY slot here.
                qwen4_stage_ple_embed(
                    *runtime, s.input,
                    runtime->sequences[s.slot].processed_tokens, s.ple_embed);
        }
    }
    static const bool env_nvfp4_persistent_b=[]{const char*s=std::getenv("FLYWEIGHT_NVFP4_PERSISTENT");return s&&s[0]=='1';}();
    static const bool env_nvfp4_tc_b=[]{const char*s=std::getenv("FLYWEIGHT_NVFP4_DECODE_TENSOR_CORES");return s&&s[0]=='1';}();
    static const bool env_nvfp4_tc_trace_b=std::getenv("FLYWEIGHT_NVFP4_TENSOR_CORE_TRACE")!=nullptr;
    static const bool env_nvfp4_tiled_b=[]{const char*s=std::getenv("FLYWEIGHT_NVFP4_TILED");return s&&s[0]=='1';}();
    for (std::uint32_t layer_number = 0; layer_number < runtime->layers.size(); ++layer_number) {
        auto& layer = runtime->layers[layer_number];
        auto tensor = [&](std::size_t role) { return runtime->device_tensors[layer.static_tensors.at(role)]; };
        auto dense = [&](std::size_t role, std::uint64_t input, std::uint64_t output, int in_size, int out_size) { dense_matvec(layer.static_tensors.at(role), input, output, in_size, out_size); };
        const std::size_t moe_base = layer.attention ? 7 : 10;
        // Pass A: queue every sequence's GPU work up to (and including) the
        // router readback + shared experts, recording that sequence's event.
        for (auto& s : seqs) {
            active_dense_q8 = s.dense_q8;
            active_dense_q8_scales = s.dense_q8_scales;
            // Each sequence owns its own scratch, so switching sequences drops
            // the memo along with the buffer it referred to.
            q8_cached_normalized = s.normalized;
            q8_cached_input = 0;
            if (runtime->qwen4exp) {
                if (layer.ple_conv != std::numeric_limits<std::uint64_t>::max())
                    ple_multi(s, layer);
                hc_pre_multi(s, tensor(0), layer.hc_attn_down,
                             layer.hc_attn_up, layer.hc_attn_inject);
            } else rms(s.hidden, tensor(0), s.normalized);
            if (!layer.attention) {
                int channels = static_cast<int>(runtime->model->tensors[layer.static_tensors[1]].shape[1]);
                int gate_elements = static_cast<int>(runtime->model->tensors[layer.static_tensors[2]].shape[1]);
                int value_heads = static_cast<int>(runtime->model->tensors[layer.static_tensors[8]].shape[0]);
                int head_dim = static_cast<int>(runtime->model->tensors[layer.static_tensors[9]].shape[0]);
                int value_dim = value_heads * head_dim;
                int key_heads = (channels - value_dim) / (2 * head_dim);
                dense(1, s.normalized, s.first, hidden_size, channels);
                dense(2, s.normalized, s.second, hidden_size, gate_elements);
                dense(4, s.normalized, s.third, hidden_size, value_heads);
                dense(5, s.normalized, s.third + value_heads * sizeof(float), hidden_size, value_heads);
                int kernel_size = static_cast<int>(runtime->model->tensors[layer.static_tensors[6]].shape[0]);
                std::uint64_t conv_state = s.state + layer.state_first;
                auto conv_weights = tensor(6);
                void* conv_args[] = {const_cast<std::uint64_t*>(&s.first), &conv_weights, &conv_state, const_cast<std::uint64_t*>(&s.fourth), &channels, &kernel_size};
                launch_named("delta_conv_step", (channels + 255) / 256, 1, 256, conv_args);
                std::uint64_t recurrent_state = s.state + layer.state_second;
                auto decay = tensor(8), dt = tensor(7), norm = tensor(9);
                std::uint64_t beta = s.third + value_heads * sizeof(float);
                int gate_sigmoid = runtime->qwen4exp ? 1 : 0;
                void* recurrent_args[] = {const_cast<std::uint64_t*>(&s.fourth), const_cast<std::uint64_t*>(&s.second), &beta, const_cast<std::uint64_t*>(&s.third), &decay, &dt, &norm, &recurrent_state, const_cast<std::uint64_t*>(&s.first), &key_heads, &value_heads, &head_dim, const_cast<float*>(&epsilon), &gate_sigmoid};
                // The same kernel selection as single-token decode: the split
                // form reduces in a different order than the serial one, so a
                // driver that picked differently would let a batched token
                // diverge from the identical solo decode.
                static const bool multi_delta_serial =
                    std::getenv("FLYWEIGHT_DELTA_SERIAL") != nullptr;
                int recurrent_slices = head_dim > 0 ? 1024 / head_dim : 0;
                if (recurrent_slices > 4) recurrent_slices = 4;
                if (recurrent_slices >= 1 && !multi_delta_serial) {
                    const int recurrent_block = head_dim * recurrent_slices;
                    const std::uint32_t recurrent_shared =
                        static_cast<std::uint32_t>(
                            (4 * head_dim + recurrent_block) * sizeof(float));
                    launch_named("qwen_delta_recurrent_split", value_heads, 1,
                                 recurrent_block, recurrent_args, recurrent_shared);
                } else {
                    launch_named("qwen_delta_recurrent", value_heads, 1, 256,
                                 recurrent_args);
                }
                dense(3, s.first, s.residual, value_dim, hidden_size);
                if (runtime->qwen4exp) hc_post_multi(s, s.residual);
                else add(s.residual, s.hidden);
            } else {
                const int heads = static_cast<int>(runtime->model->config.attention_heads);
                const int kv_heads = static_cast<int>(runtime->model->config.attention_kv_heads);
                const int head_dim = static_cast<int>(runtime->model->tensors[layer.static_tensors[2]].shape[1] / kv_heads);
                const int q_size = heads * 2 * head_dim, kv_size = kv_heads * head_dim;
                dense(1, s.normalized, s.first, hidden_size, q_size);
                dense(2, s.normalized, s.second, hidden_size, kv_size);
                dense(3, s.normalized, s.third, hidden_size, kv_size);
                const int rotary = static_cast<int>(runtime->model->config.rotary_dimension ? runtime->model->config.rotary_dimension : head_dim);
                const int position = static_cast<int>(s.position);
                const float theta = runtime->model->config.rope_freq_base ? runtime->model->config.rope_freq_base : 1000000.0f;
                auto qnorm = tensor(5), knorm = tensor(6);
                std::uint64_t queries = s.fourth, gates = s.fourth + q_size / 2 * sizeof(float);
                void* q_args[] = {const_cast<std::uint64_t*>(&s.first), &qnorm, &queries, &gates, const_cast<int*>(&heads), const_cast<int*>(&head_dim), const_cast<int*>(&rotary), const_cast<int*>(&position), const_cast<float*>(&theta), const_cast<float*>(&epsilon)};
                launch_named("qwen_attention_query", heads, 1, 256, q_args);
                std::uint64_t keys = s.first;
                void* k_args[] = {const_cast<std::uint64_t*>(&s.second), &knorm, &keys, const_cast<int*>(&kv_heads), const_cast<int*>(&head_dim), const_cast<int*>(&rotary), const_cast<int*>(&position), const_cast<float*>(&theta), const_cast<float*>(&epsilon)};
                launch_named("qwen_attention_key", kv_heads, 1, 256, k_args);
                std::uint64_t cache_keys = s.state + layer.state_first, cache_values = s.state + layer.state_second;
                const auto view=attention_cache_view(layer,s.position);
                int slot=view.slot,capacity=view.capacity;
                void* k_store_args[] = {&keys, &cache_keys, const_cast<int*>(&kv_heads), const_cast<int*>(&head_dim), &slot, &capacity};
                launch_named(kv_store_kernel(runtime->options.cache_type_k,true), kv_heads, 1, 256, k_store_args);
                void* v_store_args[] = {const_cast<std::uint64_t*>(&s.third), &cache_values, const_cast<int*>(&kv_heads), const_cast<int*>(&head_dim), &slot, &capacity};
                launch_named(kv_store_kernel(runtime->options.cache_type_v,false), kv_heads, 1, 256, v_store_args);
                std::uint64_t attended = s.second; int tokens=view.tokens,first_slot=view.first;
                float scale = 1.0f / std::sqrt(static_cast<float>(head_dim));
                // QSA indexer, per slot: same shape as single-token decode --
                // maintain ring + block keys always, select past the budget.
                int qsa_selected = 0;
                if (qwen_qsa_layer(*runtime, layer)) {
                    const int qsa_heads = static_cast<int>(runtime->model->config.indexer_head_count);
                    const int key_len = static_cast<int>(runtime->model->config.indexer_key_length);
                    const int ratio = static_cast<int>(layer.qsa_ratio);
                    const int qsa_rotary = std::min(rotary, key_len);
                    const std::uint64_t qsa_raw = s.qsa_query + static_cast<std::uint64_t>(qsa_heads) * key_len * sizeof(float);
                    std::uint64_t ring = s.state + layer.qsa_ring;
                    std::uint64_t blocks = s.state + layer.qsa_blocks;
                    auto knorm_w = runtime->device_tensors[layer.indexer_k_norm];
                    dense_matvec(layer.indexer_k, s.normalized, qsa_raw, hidden_size, key_len);
                    void* store_args[] = {const_cast<std::uint64_t*>(&qsa_raw), &ring, &blocks, &knorm_w,
                        const_cast<int*>(&position), const_cast<int*>(&ratio),
                        const_cast<int*>(&key_len), const_cast<int*>(&qsa_rotary),
                        const_cast<float*>(&theta), const_cast<float*>(&epsilon)};
                    launch_named("qsa_key_store", 1, 1, 256, store_args);
                    if (qwen_qsa_selecting(*runtime, layer, s.position)) {
                        const int complete = (position + 1) / ratio;
                        dense_matvec(layer.indexer_q, s.normalized, s.qsa_query, hidden_size, qsa_heads * key_len);
                        auto qnorm_w = runtime->device_tensors[layer.indexer_q_norm];
                        std::uint64_t prepped = s.first;
                        void* prep_args[] = {const_cast<std::uint64_t*>(&s.qsa_query), &qnorm_w, &prepped,
                            const_cast<int*>(&qsa_heads), const_cast<int*>(&key_len),
                            const_cast<int*>(&qsa_rotary), const_cast<int*>(&position),
                            const_cast<float*>(&theta), const_cast<float*>(&epsilon)};
                        launch_named("qwen_attention_key", qsa_heads, 1, 256, prep_args);
                        void* score_args[] = {&prepped, &blocks, const_cast<std::uint64_t*>(&s.qsa_block_scores),
                            const_cast<int*>(&complete), const_cast<int*>(&qsa_heads),
                            const_cast<int*>(&key_len)};
                        launch_named("qsa_block_scores",
                            (static_cast<std::uint32_t>(complete) + 255) / 256, 1, 256, score_args);
                        qsa_selected = qwen_qsa_select(*runtime, layer, s.qsa_block_scores, s.qsa_slots, s.position);
                    }
                }
                if (qsa_selected) {
                    void* score_args[] = {&queries, &cache_keys, const_cast<std::uint64_t*>(&s.qsa_slots),
                        const_cast<std::uint64_t*>(&s.attention_scores),
                        const_cast<int*>(&heads), const_cast<int*>(&kv_heads),
                        const_cast<int*>(&head_dim), &qsa_selected, &capacity, &scale};
                    launch_named(kv_scores_indexed_kernel(*runtime), heads,
                        (static_cast<std::uint32_t>(qsa_selected) + 255) / 256, 256, score_args);
                    void* value_args[] = {const_cast<std::uint64_t*>(&s.attention_scores), &cache_values,
                        const_cast<std::uint64_t*>(&s.qsa_slots), &attended,
                        const_cast<int*>(&heads), const_cast<int*>(&kv_heads),
                        const_cast<int*>(&head_dim), &qsa_selected, &capacity};
                    launch_named(kv_values_indexed_kernel(*runtime), heads, 1, 256, value_args);
                } else {
                const char* fused_tiles=kv_fused_tiles_kernel(*runtime);
                const int fused_tile_tokens=kv_fused_tile_tokens(*runtime);
                const bool cublas_done=
                    qwen_turbo_cublas_attention(
                        *runtime,queries,s.first,cache_keys,cache_values,
                        s.attention_scores,attended,heads,kv_heads,head_dim,
                        tokens,capacity,first_slot,scale)||
                    (qwen_cublas_attention_eligible(
                        *runtime,tokens,first_slot,capacity)&&
                    flyweight_gpu_attention_f16_cublas(
                        queries,s.first,cache_keys,cache_values,
                        s.attention_scores,attended,runtime->stream,
                        heads,kv_heads,head_dim,tokens,capacity,first_slot,
                        scale)==0);
                if(cublas_done){
                    // Tensor-core GQA attention already wrote `attended`.
                }else if(runtime->fused_attention&&fused_tiles&&
                   head_dim==128&&
                   heads/kv_heads<=8&&
                   (tokens+fused_tile_tokens-1)/fused_tile_tokens<=512&&
                   static_cast<std::uint64_t>((tokens+fused_tile_tokens-1)/fused_tile_tokens)*130<=
                       runtime->options.context_limit){
                    const int tile_count=(tokens+fused_tile_tokens-1)/fused_tile_tokens;
                    void* fused_args[] = {&queries, &cache_keys, &cache_values,
                        const_cast<std::uint64_t*>(&s.attention_scores),
                        const_cast<int*>(&heads),
                        const_cast<int*>(&kv_heads), const_cast<int*>(&head_dim),
                        &tokens, &capacity, &first_slot, &scale};
                    launch_named(fused_tiles, kv_fused_grid_heads(*runtime,heads,kv_heads), tile_count, 256, fused_args);
                    void* merge_args[] = {
                        const_cast<std::uint64_t*>(&s.attention_scores),
                        &attended, const_cast<int*>(&heads),
                        const_cast<int*>(&head_dim),
                        const_cast<int*>(&tile_count)};
                    launch_named(kv_fused_merge_kernel_name(*runtime,128),
                                 heads, 1, 256, merge_args);
                }else{
                    void* score_args[] = {&queries, &cache_keys, const_cast<std::uint64_t*>(&s.attention_scores), const_cast<int*>(&heads), const_cast<int*>(&kv_heads), const_cast<int*>(&head_dim), &tokens, &capacity, &first_slot, &scale};
                    launch_named(kv_scores_ring_kernel(*runtime), heads, (tokens + 255) / 256, 256, score_args);
                    void* value_args[] = {const_cast<std::uint64_t*>(&s.attention_scores), &cache_values, &attended, const_cast<int*>(&heads), const_cast<int*>(&kv_heads), const_cast<int*>(&head_dim), &tokens, &capacity, &first_slot};
                    launch_named(kv_values_ring_kernel(*runtime), heads, 1, 256, value_args);
                }
                }
                std::uint64_t gated = s.third; int elements = heads * head_dim;
                void* gate_args[] = {&attended, &gates, &gated, &elements};
                launch_named("qwen_attention_gate", (elements + 255) / 256, 1, 256, gate_args);
                dense(4, gated, s.residual, elements, hidden_size);
                if (runtime->qwen4exp) hc_post_multi(s, s.residual);
                else add(s.residual, s.hidden);
            }
            if (runtime->qwen4exp)
                hc_pre_multi(s, tensor(moe_base), layer.hc_ffn_down,
                             layer.hc_ffn_up, layer.hc_ffn_inject);
            else rms(s.residual, tensor(moe_base), s.normalized);
            if (layer.dense_ffn) {
                // Dense block: one SwiGLU over the layer's own gate/up/down,
                // with no router, shared expert or expert paging to run.
                const int dense_intermediate = static_cast<int>(runtime->moe_intermediate);
                if (layer.ffn_on_host) {
                    auto* scratch = static_cast<float*>(runtime->dense_host);
                    float* host_input = scratch;
                    // dense_host owns exactly two hidden-sized DMA endpoints;
                    // qwen_cpu_dense_ffn keeps its activation in dense_scratch.
                    float* host_output = host_input + hidden_size;
                    if (flyweight_gpu_download(host_input, s.normalized, hidden_size * sizeof(float), runtime->stream) != 0 ||
                        flyweight_gpu_stream_sync(runtime->stream) != 0) throw std::runtime_error("native dense host FFN input transfer failed");
                    const auto host_started = std::chrono::steady_clock::now();
                    qwen_cpu_dense_ffn(*runtime, layer, host_input, host_output);
                    runtime->dense_host_nanoseconds += std::chrono::duration_cast<std::chrono::nanoseconds>(std::chrono::steady_clock::now() - host_started).count();
                    if (flyweight_gpu_upload(s.third, host_output, hidden_size * sizeof(float), runtime->stream) != 0) throw std::runtime_error("native dense host FFN output transfer failed");
                } else {
                const auto up_half = s.first + static_cast<std::uint64_t>(dense_intermediate) * sizeof(float);
                dense(moe_base + 1, s.normalized, s.first, hidden_size, dense_intermediate);
                dense(moe_base + 2, s.normalized, up_half, hidden_size, dense_intermediate);
                int dense_count = dense_intermediate;
                void* dense_silu_args[] = {const_cast<std::uint64_t*>(&s.first), const_cast<std::uint64_t*>(&s.second), &dense_count};
                launch_named("silu_mul", (static_cast<std::uint32_t>(dense_count) + 255) / 256, 1, 256, dense_silu_args);
                dense(moe_base + 3, s.second, s.third, dense_intermediate, hidden_size);
                }
            } else {
            dense(moe_base + 1, s.normalized, s.router_logits, hidden_size, experts);
            if (flyweight_gpu_route_topk(s.router_logits, s.selected_device, s.route_weights, experts, top_k, runtime->stream) != 0) throw std::runtime_error("native Qwen routing failed");
            if (flyweight_gpu_download(s.selected_host, s.selected_device, top_k * sizeof(std::int32_t), runtime->stream) != 0 ||
                flyweight_gpu_download(s.cpu_weights, s.route_weights, top_k * sizeof(float), runtime->stream) != 0 ||
                flyweight_gpu_download(s.cpu_input, s.normalized, hidden_size * sizeof(float), runtime->stream) != 0) throw std::runtime_error("native Qwen route transfer failed");
            if (flyweight_gpu_event_record(runtime->slot_events[s.slot], runtime->stream) != 0) throw std::runtime_error("native Qwen route event failed");
            auto shared_gate_matrix = tensor(moe_base + 2), shared_up_matrix = tensor(moe_base + 3);
            // The device type, not the stored one, exactly as single decode
            // dispatches: bf16 shared experts are requantized to Q8_0 on
            // upload and must still take the fused path. This block used to
            // hardcode the Q8 kernel for every non-NVFP4 type, which decoded
            // f32/bf16/K-quant shared experts as Q8_0 bytes -- the parity
            // harness caught it as token 0 on every batched decode.
            const auto shexp_type = qwen_device_type(*runtime, layer.static_tensors.at(moe_base + 2));
            if (shexp_type == 40) {
                auto shared_gate_scale = layer.shared_gate_scale, shared_up_scale = layer.shared_up_scale;
                void* silu_args[] = {&shared_gate_matrix, &shared_up_matrix, const_cast<std::uint64_t*>(&s.normalized), const_cast<std::uint64_t*>(&s.second), const_cast<int*>(&hidden_size), const_cast<int*>(&intermediate), &shared_gate_scale, &shared_up_scale};
                launch_named("nvfp4_swiglu_transposed", intermediate, 1, 256, silu_args);
                auto shared_down_matrix = tensor(moe_base + 4); auto shared_down_scale = layer.shared_down_scale;
                void* down_args[] = {&shared_down_matrix, const_cast<std::uint64_t*>(&s.second), const_cast<std::uint64_t*>(&s.third), const_cast<int*>(&intermediate), const_cast<int*>(&hidden_size), &shared_down_scale};
                launch_named("nvfp4_matvec_transposed", hidden_size, 1, 256, down_args);
            } else if (shexp_type == 8) {
                void* silu_args[] = {&shared_gate_matrix, &shared_up_matrix, const_cast<std::uint64_t*>(&s.normalized), const_cast<std::uint64_t*>(&s.second), const_cast<int*>(&hidden_size), const_cast<int*>(&intermediate)};
                launch_named("q8_swiglu_transposed_warp", (intermediate + 7) / 8, 1, 256, silu_args);
                dense(moe_base + 4, s.second, s.third, intermediate, hidden_size);
            } else {
                // No fused SwiGLU for this type: project gate and up into one
                // contiguous pair and let silu_mul combine them, as single
                // decode does.
                const auto up_half = s.first + static_cast<std::uint64_t>(intermediate) * sizeof(float);
                dense(moe_base + 2, s.normalized, s.first, hidden_size, intermediate);
                dense(moe_base + 3, s.normalized, up_half, hidden_size, intermediate);
                int count = intermediate;
                void* silu_args[] = {const_cast<std::uint64_t*>(&s.first), const_cast<std::uint64_t*>(&s.second), &count};
                launch_named("silu_mul", (static_cast<std::uint32_t>(count) + 255) / 256, 1, 256, silu_args);
                dense(moe_base + 4, s.second, s.third, intermediate, hidden_size);
            }
            auto shared_gate = tensor(moe_base + 5);
            {
                const auto sg_type=runtime->model->tensors[layer.static_tensors.at(moe_base+5)].type;
                void* shared_args[] = {const_cast<std::uint64_t*>(&s.normalized), &shared_gate, const_cast<std::uint64_t*>(&s.third), const_cast<int*>(&hidden_size)};
                launch_named(sg_type==30?"qwen_shared_scale_bf16":"qwen_shared_scale", 1, 1, 256, shared_args);
            }
            }
        }
        // All predictions queued by the preceding layer target this layer.
        // One stream wait covers every sequence's upload batch; predictions
        // made below target layer+1 and continue overlapping this layer.
        qwen_wait_for_prefetch_layer(*runtime,layer_number);
        // The fused two-sequence CPU MoE interleaves both rows through one
        // weight pass (pair/oct dots, dequant+GEMM), which reduces in a
        // different order than qwen_cpu_moe -- so a batched token can differ
        // from the identical request run alone, i.e. whether another request
        // is in flight changes this one's greedy output. That is the ornith
        // failure class, so determinism is the default and the fused path is
        // explicit opt-in until a measured decision promotes it.
        static const char*batch_cpu_moe=std::getenv("FLYWEIGHT_BATCHED_CPU_MOE");
        const bool batch_cpu_supported=(flyweight_cpu_features()&2u)!=0;
        if(expert_policy.is_cpu()&&n==2&&batch_cpu_supported&&
           batch_cpu_moe&&batch_cpu_moe[0]=='1'){
            const auto expert_started=timing_enabled()?std::chrono::steady_clock::now():std::chrono::steady_clock::time_point{};
            thread_local std::vector<std::int32_t> batch_selected;
            thread_local std::vector<float> batch_weights,batch_inputs,batch_activated,batch_down,batch_outputs;
            batch_selected.assign(n*top_k,0);batch_weights.assign(n*top_k,0.0f);
            batch_inputs.resize(n*hidden_size);batch_activated.resize(n*top_k*intermediate);
            batch_down.resize(n*top_k*hidden_size);batch_outputs.resize(n*hidden_size);
            for(std::size_t index=0;index<n;++index){
                auto&s=seqs[index];
                const auto wait_started=timing_enabled()?std::chrono::steady_clock::now():std::chrono::steady_clock::time_point{};
                if(flyweight_gpu_event_sync(runtime->slot_events[s.slot])!=0)
                    throw std::runtime_error("native Qwen route event failed");
                if(timing_enabled())runtime->route_wait_nanoseconds+=
                    std::chrono::duration_cast<std::chrono::nanoseconds>(
                        std::chrono::steady_clock::now()-wait_started
                    ).count();
                int route_count=top_k;
                if(runtime->options.expert_top_k>0||
                   (runtime->options.expert_top_p>0.0f&&
                    runtime->options.expert_top_p<1.0f))
                    route_count=apply_expert_router_policy(
                        s.selected_host,s.cpu_weights,top_k,
                        static_cast<int>(runtime->options.expert_top_k),
                        runtime->options.expert_top_p
                    );
                runtime->route_expert_sum+=
                    static_cast<std::uint64_t>(route_count);
                qwen_observe_and_prefetch_next_layer(
                    *runtime,
                    runtime->sequences[s.slot].expert_prefetch,
                    layer_number,s.selected_host,route_count
                );
                std::copy_n(
                    s.selected_host,route_count,
                    batch_selected.data()+index*top_k
                );
                std::copy_n(
                    s.cpu_weights,route_count,
                    batch_weights.data()+index*top_k
                );
                std::copy_n(
                    s.cpu_input,hidden_size,
                    batch_inputs.data()+index*hidden_size
                );
            }
            const auto compute_started=timing_enabled()?std::chrono::steady_clock::now():std::chrono::steady_clock::time_point{};
            qwen_cpu_moe_rows(*runtime,layer,batch_selected.data(),batch_weights.data(),static_cast<int>(n),top_k,batch_inputs.data(),batch_activated.data(),batch_down.data(),batch_outputs.data());
            if(timing_enabled())runtime->expert_compute_nanoseconds+=std::chrono::duration_cast<std::chrono::nanoseconds>(std::chrono::steady_clock::now()-compute_started).count();
            for(std::size_t index=0;index<n;++index){auto&s=seqs[index];if(flyweight_gpu_upload(s.fourth,batch_outputs.data()+index*hidden_size,hidden_size*sizeof(float),runtime->stream)!=0)throw std::runtime_error("native CPU MoE output upload failed");add(s.third,s.fourth);if(runtime->qwen4exp)hc_post_multi(s,s.third);else add(s.residual,s.third);std::swap(s.hidden,s.residual);}
            if(timing_enabled())runtime->expert_page_nanoseconds+=std::chrono::duration_cast<std::chrono::nanoseconds>(std::chrono::steady_clock::now()-expert_started).count();
            continue;
        }
        // Pass B: serial CPU expert phases; while sequence i runs on the CPU,
        // the GPU is still executing the queued pass-A work of the later ones.
        for (auto& s : seqs) {
            const auto route_wait_started = timing_enabled()?std::chrono::steady_clock::now():std::chrono::steady_clock::time_point{};
            if (flyweight_gpu_event_sync(runtime->slot_events[s.slot]) != 0) throw std::runtime_error("native Qwen route event failed");
            if(timing_enabled())runtime->route_wait_nanoseconds += std::chrono::duration_cast<std::chrono::nanoseconds>(std::chrono::steady_clock::now() - route_wait_started).count();
            int route_count = top_k;
            if (runtime->options.expert_top_k > 0 || (runtime->options.expert_top_p > 0.0f && runtime->options.expert_top_p < 1.0f))
                route_count = apply_expert_router_policy(s.selected_host, s.cpu_weights, top_k, static_cast<int>(runtime->options.expert_top_k), runtime->options.expert_top_p);
            runtime->route_expert_sum += static_cast<std::uint64_t>(route_count);
            const auto pager_started = timing_enabled()?std::chrono::steady_clock::now():std::chrono::steady_clock::time_point{};
            qwen_observe_and_prefetch_next_layer(
                *runtime,
                runtime->sequences[s.slot].expert_prefetch,
                layer_number,s.selected_host,route_count
            );
            if (expert_policy.is_cpu()) {
                const auto compute_started = timing_enabled()?std::chrono::steady_clock::now():std::chrono::steady_clock::time_point{};
                qwen_cpu_moe(*runtime, layer, s.selected_host, s.cpu_weights, route_count, s.cpu_input, s.cpu_activated, s.cpu_output);
                if(timing_enabled())runtime->expert_compute_nanoseconds += std::chrono::duration_cast<std::chrono::nanoseconds>(std::chrono::steady_clock::now() - compute_started).count();
                if (flyweight_gpu_upload(s.fourth, s.cpu_output, hidden_size * sizeof(float), runtime->stream) != 0) throw std::runtime_error("native CPU MoE output upload failed");
                add(s.third, s.fourth);
            } else {
                if (runtime->expert_slots.empty()) throw std::runtime_error("native hybrid MoE requires an expert cache budget");
                // The shared paging area's previous contents may still be
                // consumed by stream-queued uploads; fence before rewriting.
                if (flyweight_gpu_event_sync(runtime->staging_event) != 0) throw std::runtime_error("native Qwen staging event failed");
                auto* pag = staging + shared_base;
                std::array<std::int32_t, 256> cpu_selected{};
                std::array<float, 256> cpu_compact_weights{}, gpu_compact_weights{};
                std::array<float, 256> gpu_gate_scales{}, gpu_up_scales{}, gpu_down_scales{};
                std::array<std::uint64_t, 256> gate_pointers{}, up_pointers{},
                    down_pointers{}, native_pointers{};
                int cpu_count = 0, gpu_count = 0;
                std::uint64_t staging_cursor = 0;
                struct PendingUpload {
                    std::uint64_t device, host_offset, bytes;
                    std::size_t slot_index;
                };
                std::array<PendingUpload, 256> pending{}; int pending_count = 0;
                for (int rank = 0; rank < route_count; ++rank) {
                    const int expert = s.selected_host[rank];
                    if (expert < 0 || expert >= experts) throw std::runtime_error("native hybrid MoE selected an invalid expert");
                    const auto cache_key = (static_cast<std::uint64_t>(layer_number) << 32) | static_cast<std::uint32_t>(expert);
                    auto resident = runtime->expert_residency.find(cache_key);
                    if (resident != runtime->expert_residency.end()) {
                        const auto slot_index = resident->second; auto& slot = runtime->expert_slots[slot_index];
                        const auto& history = record_expert_access(
                            *runtime, layer_number, expert);
                        if (expert_policy.residency_may_change())
                            slot.last_used = history.last_used;
                        record_expert_cache_hit(*runtime,slot);
                        const auto device_base = runtime->expert_cache + slot_index * runtime->expert_slot_bytes; std::uint64_t role_offset = 0;
                        for (int role = 0; role < 3; ++role) { const auto bytes = runtime->model->tensors[layer.expert_tensors[role]].size / experts; const auto pointer = device_base + role_offset; if (role == 0) gate_pointers[gpu_count] = pointer; else if (role == 1) up_pointers[gpu_count] = pointer; else down_pointers[gpu_count] = pointer; role_offset += bytes; }
                        if (slot.native_valid && runtime->expert_native_cache)
                            native_pointers[gpu_count] =
                                runtime->expert_native_cache +
                                slot_index * runtime->expert_slot_bytes;
                        gpu_compact_weights[gpu_count] = s.cpu_weights[rank];
                        // See the decode path: gate/up before SiLU, down folded into `up`.
                        gpu_gate_scales[gpu_count] = qwen_expert_role_scale(*runtime, layer.expert_gate_scale, expert);
                        gpu_down_scales[gpu_count] = qwen_expert_role_scale(*runtime, layer.expert_down_scale, expert);
                        gpu_up_scales[gpu_count] = qwen_expert_role_scale(*runtime, layer.expert_up_scale, expert)
                            * gpu_down_scales[gpu_count];
                        ++gpu_count; continue;
                    }
                    ++runtime->expert_cache_misses; cpu_selected[cpu_count] = expert; cpu_compact_weights[cpu_count]=s.cpu_weights[rank];
                    if(layer.expert_down_scale!=std::numeric_limits<std::uint64_t>::max()){
                        const auto&st=runtime->model->tensors[layer.expert_down_scale];
                        const auto experts_count=runtime->model->config.expert_count;
                        const auto scale_bytes=st.size/experts_count;
                        float ds=1.0f;std::memcpy(&ds,tensor_data(*runtime->model,st)+static_cast<std::uint64_t>(expert)*scale_bytes,sizeof(float));
                        cpu_compact_weights[cpu_count]*=ds;
                    }
                    ++cpu_count;
                    const auto slot_index = select_expert_cache_slot(*runtime, layer_number, expert, true);
                    if (slot_index == kNoExpertSlot) continue;
                    auto& slot = runtime->expert_slots[slot_index]; slot.key = cache_key; slot.valid = true; slot.native_valid = false; slot.last_used = ++runtime->expert_clock; runtime->expert_residency[cache_key] = slot_index;
                    const auto slot_base = runtime->expert_cache + slot_index * runtime->expert_slot_bytes;
                    if (layer.expert_dma) {
                        std::uint64_t role_offset = 0;
                        for (int role = 0; role < 3; ++role) { const auto& t = runtime->model->tensors[layer.expert_tensors[role]]; const auto bytes = t.size / experts; const auto offset = static_cast<std::uint64_t>(expert) * bytes; if (flyweight_gpu_upload(slot_base + role_offset, tensor_data(*runtime->model,t) + offset, bytes, runtime->stream) != 0) throw std::runtime_error("native hybrid MoE DMA cache upload failed"); role_offset += bytes; }
                        if(runtime->expert_native_cache){
                            const auto gate_bytes=runtime->model->tensors[
                                layer.expert_tensors[0]].size/experts;
                            const auto up_bytes=runtime->model->tensors[
                                layer.expert_tensors[1]].size/experts;
                            const int status=flyweight_gpu_nvfp4_prepare_expert(
                                slot_base,slot_base+gate_bytes,
                                slot_base+gate_bytes+up_bytes,
                                runtime->expert_native_cache+
                                    slot_index*runtime->expert_slot_bytes,
                                runtime->stream,hidden_size,intermediate);
                            if(status!=0)throw std::runtime_error(
                                "persistent NVFP4 expert preparation failed");
                            slot.native_valid=true;
                        }
                    } else {
                        const auto bundle_start = staging_cursor;
                        for (int role = 0; role < 3; ++role) { const auto& t = runtime->model->tensors[layer.expert_tensors[role]]; const auto bytes = t.size / experts; const auto offset = static_cast<std::uint64_t>(expert) * bytes; if (staging_cursor + bytes > runtime->expert_staging_bytes) throw std::runtime_error("native hybrid MoE staging overflow"); std::memcpy(pag + staging_cursor, tensor_data(*runtime->model,t) + offset, bytes); staging_cursor += bytes; }
                        pending[pending_count++] = {
                            slot_base, bundle_start,
                            staging_cursor - bundle_start, slot_index};
                    }
                }
                if (gpu_count) {
                    const auto table_bytes = static_cast<std::uint64_t>(gpu_count) * (3 * sizeof(std::uint64_t) + 4 * sizeof(float));
                    const auto table_host = device_align(staging_cursor); const auto table_device = runtime->expert_staging + runtime->expert_staging_bytes - device_align(table_bytes);
                    if (table_host + table_bytes > runtime->expert_staging_bytes) throw std::runtime_error("native hybrid MoE pointer staging overflow");
                    std::memcpy(pag + table_host, gate_pointers.data(), gpu_count * sizeof(std::uint64_t));
                    std::memcpy(pag + table_host + gpu_count * sizeof(std::uint64_t), up_pointers.data(), gpu_count * sizeof(std::uint64_t));
                    std::memcpy(pag + table_host + 2 * gpu_count * sizeof(std::uint64_t), down_pointers.data(), gpu_count * sizeof(std::uint64_t));
                    std::memcpy(pag + table_host + 3 * gpu_count * sizeof(std::uint64_t), gpu_compact_weights.data(), gpu_count * sizeof(float));
                    std::memcpy(pag + table_host + 3 * gpu_count * sizeof(std::uint64_t) + gpu_count * sizeof(float), gpu_gate_scales.data(), gpu_count * sizeof(float));
                    std::memcpy(pag + table_host + 3 * gpu_count * sizeof(std::uint64_t) + 2 * gpu_count * sizeof(float), gpu_up_scales.data(), gpu_count * sizeof(float));
                    std::memcpy(pag + table_host + 3 * gpu_count * sizeof(std::uint64_t) + 3 * gpu_count * sizeof(float), gpu_down_scales.data(), gpu_count * sizeof(float));
                    if (flyweight_gpu_upload(table_device, pag + table_host, table_bytes, runtime->stream) != 0) throw std::runtime_error("native hybrid MoE table upload failed");
                    const auto gate_table = table_device, up_table = gate_table + gpu_count * sizeof(std::uint64_t), down_table = up_table + gpu_count * sizeof(std::uint64_t), weight_table = down_table + gpu_count * sizeof(std::uint64_t);
                    const auto gate_scale_table = weight_table + gpu_count * sizeof(float), up_scale_table = gate_scale_table + gpu_count * sizeof(float), down_scale_table = up_scale_table + gpu_count * sizeof(float);
                    const auto gate_type = runtime->model->tensors[layer.expert_tensors[0]].type;
                    const auto down_type = runtime->model->tensors[layer.expert_tensors[2]].type;
        // The grouped dispatch below ends in a k-quant fallback, so an
        // unhandled type would be decoded as Q5_K rather than rejected. Prepare
        // already routes such models to the CPU; this makes the silent path
        // unreachable if that ever stops holding.
        if(!qwen_gpu_expert_type_supported(gate_type)||
           !qwen_gpu_expert_type_supported(down_type))
            throw std::runtime_error(
                "native GPU expert quantization is unsupported: "+
                std::to_string(gate_type)+"/"+std::to_string(down_type));
                    const bool persistent_enabled =
                        env_nvfp4_persistent_b &&
                        runtime->expert_native_cache &&
                        std::all_of(
                            native_pointers.begin(),
                            native_pointers.begin()+gpu_count,
                            [](std::uint64_t pointer){return pointer!=0;});
                    const bool tc_enabled = env_nvfp4_tc_b;
                    bool tc_done = false;
                    if (persistent_enabled && gate_type == 40 &&
                        down_type == 40) {
                        const int tc_status =
                            flyweight_gpu_nvfp4_moe_persistent(
                                native_pointers.data(),weight_table,gate_scale_table,
                                up_scale_table,down_scale_table,s.normalized,
                                s.activated,s.third,runtime->stream,
                                hidden_size,intermediate,gpu_count);
                        if(tc_status==0){
                            ++runtime->nvfp4_tensor_core_moe_calls;
                            runtime->nvfp4_tensor_core_moe_last_status=0;
                            tc_done=true;
                        }else{
                            ++runtime->nvfp4_tensor_core_moe_fallbacks;
                            runtime->nvfp4_tensor_core_moe_last_status=
                                tc_status;
                            if(env_nvfp4_tc_trace_b)
                                std::fprintf(
                                    stderr,
                                    "[nvfp4-persistent] fallback status=%d "
                                    "experts=%d\n",tc_status,gpu_count);
                        }
                    }
                    if (!tc_done && tc_enabled && gate_type == 40 && down_type == 40) {
                        const int tc_status = flyweight_gpu_nvfp4_moe_cublas(
                            gate_table, up_table, down_table, s.normalized,
                            s.activated, s.third, weight_table, gate_scale_table,
                            up_scale_table, down_scale_table, runtime->stream, hidden_size,
                            intermediate, gpu_count);
                        if (tc_status == 0) {
                            ++runtime->nvfp4_tensor_core_moe_calls;
                            runtime->nvfp4_tensor_core_moe_last_status = 0;
                            tc_done = true;
                        } else {
                            ++runtime->nvfp4_tensor_core_moe_fallbacks;
                            runtime->nvfp4_tensor_core_moe_last_status = tc_status;
                            if (env_nvfp4_tc_trace_b)
                                std::fprintf(stderr, "[nvfp4-tc] multi-decode fallback status=%d experts=%d\n", tc_status, gpu_count);
                        }
                    }
                    if (!tc_done) {
                        const bool nvfp4_tiled = env_nvfp4_tiled_b;
                        void* gate_up_args[] = {const_cast<std::uint64_t*>(&gate_table), const_cast<std::uint64_t*>(&up_table), &s.normalized, &s.activated, const_cast<int*>(&hidden_size), const_cast<int*>(&intermediate), &gpu_count, const_cast<std::uint64_t*>(&gate_scale_table), const_cast<std::uint64_t*>(&up_scale_table)};
                        launch_named(qwen_grouped_swiglu_name(gate_type,nvfp4_tiled,false).c_str(), gate_type == 40 && nvfp4_tiled ? (intermediate + 7) / 8 : intermediate, gpu_count, 256, gate_up_args);
                        const int status = qwen_launch_grouped_accumulate(runtime->stream,down_type,down_table,s.activated,s.third,weight_table,intermediate,hidden_size,gpu_count);
                        if (status != 0) throw std::runtime_error("native hybrid MoE down projection failed");
                    }
                }
                for (int index = 0; index < pending_count; ++index) {
                    const auto& upload=pending[index];
                    if (flyweight_gpu_upload(
                            upload.device,pag+upload.host_offset,upload.bytes,
                            runtime->stream)!=0)
                        throw std::runtime_error(
                            "native hybrid MoE cache upload failed");
                    if(runtime->expert_native_cache){
                        const auto gate_bytes=runtime->model->tensors[
                            layer.expert_tensors[0]].size/experts;
                        const auto up_bytes=runtime->model->tensors[
                            layer.expert_tensors[1]].size/experts;
                        const int status=flyweight_gpu_nvfp4_prepare_expert(
                            upload.device,upload.device+gate_bytes,
                            upload.device+gate_bytes+up_bytes,
                            runtime->expert_native_cache+
                                upload.slot_index*runtime->expert_slot_bytes,
                            runtime->stream,hidden_size,intermediate);
                        if(status!=0)throw std::runtime_error(
                            "persistent NVFP4 expert preparation failed");
                        runtime->expert_slots[
                            upload.slot_index].native_valid=true;
                    }
                }
                if (flyweight_gpu_event_record(runtime->staging_event, runtime->stream) != 0) throw std::runtime_error("native Qwen staging event failed");
                if (cpu_count) {
                    const auto compute_started = timing_enabled()?std::chrono::steady_clock::now():std::chrono::steady_clock::time_point{};
                    qwen_cpu_moe(*runtime, layer, cpu_selected.data(), cpu_compact_weights.data(), cpu_count, s.cpu_input, s.cpu_activated, s.cpu_output);
                    if(timing_enabled())runtime->expert_compute_nanoseconds += std::chrono::duration_cast<std::chrono::nanoseconds>(std::chrono::steady_clock::now() - compute_started).count();
                    if (flyweight_gpu_upload(s.fourth, s.cpu_output, hidden_size * sizeof(float), runtime->stream) != 0) throw std::runtime_error("native hybrid MoE output upload failed");
                    add(s.third, s.fourth);
                }
            }
            if(timing_enabled())runtime->expert_page_nanoseconds += std::chrono::duration_cast<std::chrono::nanoseconds>(std::chrono::steady_clock::now() - pager_started).count();
            if (runtime->qwen4exp) hc_post_multi(s, s.third);
            else add(s.residual, s.third);
            std::swap(s.hidden, s.residual);
        }
    }
    for (auto& s : seqs) {
        if (runtime->qwen4exp)
            hc_pre_multi(s, runtime->device_tensors[runtime->output_hc_norm],
                         runtime->output_hc_down, runtime->output_hc_up,
                         std::numeric_limits<std::uint64_t>::max());
        else rms(s.hidden, runtime->device_tensors[runtime->final_norm], s.normalized);
        int vocabulary = static_cast<int>(runtime->model->config.vocabulary_size);
        if (flyweight_gpu_memset(s.argmax_device, 0, sizeof(std::uint64_t), runtime->stream) != 0) throw std::runtime_error("native Qwen argmax reset failed");
        auto lm_head = runtime->device_tensors[runtime->lm_head];
        void* argmax_args[] = {&lm_head, const_cast<std::uint64_t*>(&s.normalized), const_cast<std::uint64_t*>(&s.argmax_device), const_cast<int*>(&hidden_size), &vocabulary};
        launch_named(qwen_lm_head_argmax_kernel(runtime->lm_head_type), (vocabulary + 7) / 8, 1, 256, argmax_args);
        if (flyweight_gpu_download(s.winner_host, s.argmax_device, sizeof(std::uint64_t), runtime->stream) != 0) throw std::runtime_error("native Qwen output transfer failed");
    }
    const auto tail_wait_started = timing_enabled()?std::chrono::steady_clock::now():std::chrono::steady_clock::time_point{};
    if (flyweight_gpu_stream_sync(runtime->stream) != 0) throw std::runtime_error("native Qwen output synchronization failed");
    if(timing_enabled())runtime->tail_wait_nanoseconds += std::chrono::duration_cast<std::chrono::nanoseconds>(std::chrono::steady_clock::now() - tail_wait_started).count();
    for (std::size_t i = 0; i < n; ++i) {
        outputs[i] = 0xffffffffu - static_cast<std::uint32_t>(*seqs[i].winner_host);
        auto& sequence = runtime->sequences[seqs[i].slot];
        sequence.last_output_token = outputs[i];
        sequence.last_output_greedy = true;
        sequence.processed_tokens.push_back(inputs[i]);
        ++sequence.position;
    }
    runtime->decode_calls += n;
    ++runtime->multi_decode_batches;
    runtime->multi_decode_tokens += n;
    runtime->decode_nanoseconds += std::chrono::duration_cast<std::chrono::nanoseconds>(std::chrono::steady_clock::now() - decode_started).count();
}

// Route a pending task to a slot the way qwen_route_sequence does, but aware of
// slot ownership: a slot owned by another running task is untouchable, and if
// the busiest match for this prompt IS an owned slot (same conversation already
// generating), the task waits instead of duplicating the conversation elsewhere.
static bool qwen_engine_try_start(FlyweightV2QwenRuntime& runtime, QwenEngineTask& task) {
    const auto* prompt = task.prompt.data();
    const std::uint64_t prompt_count = task.prompt.size();
    constexpr std::size_t kNone = std::numeric_limits<std::size_t>::max();
    auto tokens_of = [&](std::size_t i) -> const std::vector<std::uint32_t>& {
        return i == runtime.active_sequence ? runtime.processed_tokens
                                            : runtime.sequences[i].processed_tokens;
    };
    auto owned = [&](std::size_t i) { return runtime.slot_owner[i] >= 0; };
    // Slots may differ in size; one too small for this request is not a
    // candidate however well it matches. See qwen_route_sequence.
    const std::uint64_t need = prompt_count + task.max_tokens;
    auto fits = [&](std::size_t i) { return qwen_slot_context(runtime, i) >= need; };
    std::size_t best = kNone;
    std::uint64_t best_match = 0, busy_match = 0;
    for (std::size_t i = 0; i < runtime.sequences.size(); ++i) {
        if (!fits(i)) continue;
        const auto& tokens = tokens_of(i);
        const std::uint64_t m = qwen_sequence_match(tokens, prompt, prompt_count);
        if (!qwen_cache_match_useful(m,tokens.size())) continue;
        if (owned(i)) busy_match = std::max(busy_match, m);
        else if (m > best_match) { best_match = m; best = i; }
    }
    // Smallest fitting free slot first, LRU within a size -- so a short task
    // takes scratch and leaves the full-context slot free.
    std::size_t free_lru = kNone;
    for (std::size_t i = 0; i < runtime.sequences.size(); ++i) {
        if (owned(i) || !fits(i)) continue;
        if (free_lru == kNone) { free_lru = i; continue; }
        const auto here = qwen_slot_context(runtime, i);
        const auto there = qwen_slot_context(runtime, free_lru);
        if (here != there ? here < there
                          : runtime.sequences[i].clock < runtime.sequences[free_lru].clock)
            free_lru = i;
    }
    if (free_lru == kNone) return false;  // no free slot big enough: wait
    const bool host_cache = runtime.host_cache_limit_bytes != 0;
    std::size_t best_host = kNone;
    std::uint64_t best_host_match = 0;
    if (host_cache)
        for (std::size_t i = 0; i < runtime.host_prompts.size(); ++i) {
            const auto& t = runtime.host_prompts[i].tokens;
            const std::uint64_t m = qwen_sequence_match(t, prompt, prompt_count);
            if (m > best_host_match && qwen_cache_match_useful(m,t.size())) {
                best_host_match = m; best_host = i;
            }
        }
    // The conversation is already live on a busy slot and nothing free/host
    // beats it: wait for that task to finish rather than forking the history.
    if (busy_match > best_match && busy_match > best_host_match) return false;
    std::size_t chosen;
    if (best != kNone && best_match >= best_host_match) {
        chosen = best;
        const auto& tokens = tokens_of(chosen);
        if (host_cache && best_match < tokens.size())
            qwen_spill_slot_to_host(runtime, chosen);
    } else if (best_host != kNone) {
        qwen_spill_slot_to_host(runtime, free_lru);
        best_host=kNone;
        best_host_match=0;
        for(std::size_t i=0;i<runtime.host_prompts.size();++i){
            const auto&t=runtime.host_prompts[i].tokens;
            const auto m=qwen_sequence_match(t,prompt,prompt_count);
            if(m>best_host_match&&qwen_cache_match_useful(m,t.size())){
                best_host_match=m;best_host=i;
            }
        }
        if(best_host!=kNone)qwen_restore_host_to_slot(runtime,best_host,free_lru);
        chosen = free_lru;
    } else {
        if (host_cache) qwen_spill_slot_to_host(runtime, free_lru);
        chosen = free_lru;
    }
    qwen_switch_sequence(runtime, chosen);
    runtime.sequences[chosen].clock = ++runtime.sequence_clock;
    runtime.slot_owner[chosen] = static_cast<long long>(task.id);
    task.slot = chosen;
    // A constrained task reaches the sampler on its very first generated token,
    // so it needs the prompt's last logits kept for the same reason a sampled
    // one does.
    if (qwen_prompt_begin(&runtime, prompt, prompt_count, task.plan,
                          task.sampling.active()) != 0)
        throw std::runtime_error("native Qwen prompt admission failed");
    task.index = task.plan.prompt_start;
    task.next_token = task.plan.next_token;
    if (task.index >= prompt_count) {  // full reuse: nothing to prefill
        qwen_prompt_finish(
            &runtime,prompt,prompt_count,task.next_token,task.max_tokens);
        task.phase = 2;
    } else {
        task.phase = 1;
    }
    return true;
}

int flyweight_v2_qwen_engine_step(FlyweightV2QwenRuntime*runtime,FlyweightV2QwenTaskEvent*events,uint64_t capacity,uint64_t*count){return guarded([&]{
    if(!runtime||!events||!capacity||!count)throw std::runtime_error("invalid native Qwen engine arguments");
    if(!runtime->decode_ready)throw std::runtime_error("native Qwen runtime is not prepared for decode");
    *count=0;
    {   // Admit new submissions and cancellation requests.
        std::lock_guard<std::mutex> lock(runtime->engine_mutex);
        for(auto&task:runtime->engine_pending)runtime->engine_tasks.push_back(std::move(task));
        runtime->engine_pending.clear();
        for(const auto id:runtime->engine_cancel_requests)
            for(auto&task:runtime->engine_tasks)if(task.id==id)task.cancelled=true;
        runtime->engine_cancel_requests.clear();
    }
    if(runtime->engine_tasks.empty()){
        qwen_unfreeze_expert_residency(*runtime);
        return 0;
    }
    auto emit=[&](std::uint64_t id,std::uint32_t token,std::uint32_t kind){
        // The visit-loop guard reserves room for every event this step can
        // produce, so a full buffer here is a scheduling bug -- but it must
        // never become a write past the caller's (Python-owned) array.
        if(*count>=capacity){
            std::fprintf(stderr,"[flyweight] engine event buffer full; dropped event (task %llu kind %u)\n",
                static_cast<unsigned long long>(id),static_cast<unsigned>(kind));
            return;
        }
        events[*count]=FlyweightV2QwenTaskEvent{id,token,kind};++*count;
    };
    std::vector<std::uint64_t> finished;
    std::vector<QwenEngineTask*> pending_decode;
    const std::size_t total=runtime->engine_tasks.size();
    // Capacity guard: 2 for this visit, plus one reserved slot per already
    // deferred task -- each may still emit a terminal failure event after the
    // loop (batch failure, sampling failure, decode failure), and those emits
    // are what used to run past the buffer.
    for(std::size_t visit=0;visit<total&&*count+pending_decode.size()+2<=capacity;++visit){
        auto&task=runtime->engine_tasks[(runtime->engine_cursor+visit)%total];
        try{
            if(task.cancelled&&task.phase!=2){finished.push_back(task.id);emit(task.id,0,1);continue;}
            if(task.phase==0){
                if(!qwen_engine_try_start(*runtime,task))continue;
                // Report the exact prefix/snapshot reuse point before doing
                // more work. Yield this visit so the server can print a useful
                // "starting at" line before the first potentially long chunk.
                emit(task.id,static_cast<std::uint32_t>(task.index),3);
                continue;
            }
            if(task.phase==1){
                qwen_switch_sequence(*runtime,task.slot);
                runtime->cache_admission_enabled=false;
                bool done=false;
                const int status=qwen_prefill_unit(runtime,task.prompt.data(),task.prompt.size(),task.plan,task.index,task.next_token,done,&task.sampling);
                if(status)throw std::runtime_error(
                    error.empty()?"native Qwen prefill failed":
                    "native Qwen prefill failed: "+error);
                if(done){
                    qwen_prompt_finish(
                        runtime,task.prompt.data(),task.prompt.size(),
                        task.next_token,task.max_tokens);
                    task.phase=2;
                }
                emit(task.id,static_cast<std::uint32_t>(task.index),3);
                continue;
            }
            // phase 2: emit the buffered token, then either finish or defer
            // computing the next one -- the emit-then-compute order matches the
            // blocking decode loop, so a single-task engine run stays
            // bit-identical to blocking generate.
            qwen_freeze_expert_residency(*runtime);
            emit(task.id,task.next_token,0);
            ++task.emitted;
            const bool stopped=std::find(task.stop_tokens.begin(),task.stop_tokens.end(),task.next_token)!=task.stop_tokens.end();
            if(stopped||task.cancelled||task.emitted>=task.max_tokens){
                finished.push_back(task.id);emit(task.id,0,1);continue;
            }
            if(!task.drafted.empty()){
                // An earlier MTP round already committed this token; no decode.
                task.next_token=task.drafted.front();
                task.drafted.pop_front();
                continue;
            }
            pending_decode.push_back(&task);
        }catch(const std::exception&error){
            // Isolate the failure: this task dies, the others keep running.
            std::fprintf(stderr,"[flyweight] engine task %llu failed: %s\n",
                static_cast<unsigned long long>(task.id),error.what());
            finished.push_back(task.id);emit(task.id,0,2);
        }
    }
    // Compute the deferred next tokens. Two or more sequences go through the
    // multi-sequence driver (layer-level CPU/GPU overlap); a single one keeps
    // the plain decode path.
    if(!pending_decode.empty()){
        runtime->cache_admission_enabled=true;
        std::size_t batched=0;
        const auto expert_policy=qwen_expert_policy(
            *runtime,flyweight::v2::ExpertExecutionPhase::decode);
        if(pending_decode.size()>=2&&
           runtime->multi_decode_capacity>=2&&
           !runtime->expert_residency_frozen&&
           expert_policy.routed_cpu_execution_allowed()){
            batched=std::min<std::size_t>(pending_decode.size(),runtime->multi_decode_capacity);
            std::vector<std::size_t> slots(batched);
            std::vector<std::uint32_t> batch_inputs(batched),batch_outputs(batched);
            for(std::size_t i=0;i<batched;++i){slots[i]=pending_decode[i]->slot;batch_inputs[i]=pending_decode[i]->next_token;}
            qwen_park_active(*runtime,true);
            bool batch_ok=false;
            try{
                qwen_decode_multi(runtime,batched,slots.data(),batch_inputs.data(),batch_outputs.data());
                qwen_park_active(*runtime,false);
                for(std::size_t i=0;i<batched;++i)pending_decode[i]->next_token=batch_outputs[i];
                batch_ok=true;
            }catch(const std::exception&error){
                qwen_park_active(*runtime,false);
                // The whole batch shares one stream of work; fail all of it.
                std::fprintf(stderr,"[flyweight] engine decode batch failed: %s\n",error.what());
                for(std::size_t i=0;i<batched;++i){finished.push_back(pending_decode[i]->id);emit(pending_decode[i]->id,0,2);}
            }
            // A sampled task in the batch draws from its row's logits: the
            // multi driver leaves each row's final normalized hidden in its
            // workspace slice, and the sampler projects the head itself, so
            // pointing last_sampling_* at row i replays exactly what the
            // serial decode-then-sample path does. Rows are sampled one at a
            // time on the shared stream; the sampler's scratch (topk buffers,
            // Q8 quantization, host staging) lives in regions distinct from
            // every row's normalized/logits, so later rows stay intact.
            if(batch_ok)for(std::size_t i=0;i<batched;++i){
                auto*task=pending_decode[i];
                if(!task->sampling.active())continue;
                try{
                    qwen_switch_sequence(*runtime,task->slot);
                    const auto row=runtime->workspace+
                        i*runtime->decode_workspace_layout.bytes;
                    runtime->last_sampling_normalized=
                        runtime->decode_workspace_layout.normalized.address(row);
                    runtime->last_sampling_logits=
                        runtime->decode_workspace_layout.logits.address(row);
                    task->next_token=qwen_sample_last_logits(
                        *runtime,task->sampling,task->next_token);
                }catch(const std::exception&error){
                    std::fprintf(stderr,
                        "[flyweight] engine task %llu sampling failed: %s\n",
                        static_cast<unsigned long long>(task->id),error.what());
                    finished.push_back(task->id);emit(task->id,0,2);
                }
            }
        }
        for(std::size_t i=batched;i<pending_decode.size();++i){
            auto*task=pending_decode[i];
            try{
                qwen_switch_sequence(*runtime,task->slot);
                // Speculative decoding commits the verifier's greedy argmax, so
                // it can only serve tasks that are themselves greedy; a sampled
                // task would silently lose its temperature.
                // Speculative rounds commit tokens the sampler never sees,
                // so a constrained task cannot draft either -- its call
                // would be built out of unchecked tokens.
                if(runtime->options.mtp_drafts&&!task->sampling.active()&&
                   qwen_mtp_should_draft(*runtime)){
                    const auto wanted=static_cast<uint32_t>(std::min<std::uint64_t>(
                        runtime->options.mtp_drafts,task->max_tokens-task->emitted
                    ));
                    std::array<uint32_t,8>produced{};
                    const auto round_started=std::chrono::steady_clock::now();
                    const auto valid=qwen_mtp_round(*runtime,task->next_token,wanted,produced.data());
                    qwen_mtp_record_round(
                        *runtime,
                        std::chrono::duration_cast<std::chrono::nanoseconds>(
                            std::chrono::steady_clock::now()-round_started).count(),
                        valid);
                    task->next_token=produced[0];
                    for(uint32_t index=1;index<valid;++index)task->drafted.push_back(produced[index]);
                }else{
                    const auto decode_started=std::chrono::steady_clock::now();
                    const int status=flyweight_v2_qwen_runtime_decode(runtime,task->next_token,&task->next_token);
                    if(status)throw std::runtime_error("native Qwen decode failed");
                    if(runtime->options.mtp_drafts&&!task->sampling.active())
                        qwen_mtp_record_decode(
                            *runtime,
                            std::chrono::duration_cast<std::chrono::nanoseconds>(
                                std::chrono::steady_clock::now()-decode_started).count());
                    if(task->sampling.active())
                        task->next_token=qwen_sample_last_logits(*runtime,task->sampling,task->next_token);
                }
            }catch(const std::exception&error){
                std::fprintf(stderr,"[flyweight] engine task %llu decode failed: %s\n",
                    static_cast<unsigned long long>(task->id),error.what());
                finished.push_back(task->id);emit(task->id,0,2);
            }
        }
    }
    if(total)runtime->engine_cursor=(runtime->engine_cursor+1)%total;
    {   // The erase changes engine_tasks.size(), which submit reads under
        // engine_mutex for its queue-full check; unlocked it was a data race.
        std::lock_guard<std::mutex> lock(runtime->engine_mutex);
        for(const auto id:finished){
            for(std::size_t i=0;i<runtime->slot_owner.size();++i)
                if(runtime->slot_owner[i]==static_cast<long long>(id))runtime->slot_owner[i]=-1;
            runtime->engine_tasks.erase(
                std::remove_if(runtime->engine_tasks.begin(),runtime->engine_tasks.end(),
                    [&](const QwenEngineTask&t){return t.id==id;}),
                runtime->engine_tasks.end());
        }
    }
    if(!finished.empty()){
        qwen_save_expert_history(*runtime);
        // The engine's request boundary, matching the blocking path's guard.
        qwen_note_kv_occupancy(*runtime);
    }
    if(runtime->engine_tasks.empty())
        qwen_unfreeze_expert_residency(*runtime);
    return 0;
});}

}
